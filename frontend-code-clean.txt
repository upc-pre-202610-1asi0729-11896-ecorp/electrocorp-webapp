============================================================
ELECTROCORP FRONTEND CLEAN CODE EXPORT
Generated at: 06/15/2026 14:31:13
Root: C:\Users\Asus\OneDrive\Desktop\Electrocorp\electrocorp-webapp
============================================================


============================================================
FILE: .editorconfig
============================================================

# Editor configuration, see https://editorconfig.org
root = true

[*]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true

[*.{ts,html,scss,json,java,properties,yml,yaml,md}]
charset = utf-8

[*.ts]
quote_type = single
ij_typescript_use_double_quotes = false

[*.md]
max_line_length = off
trim_trailing_whitespace = false


============================================================
FILE: .gitignore
============================================================

# See https://docs.github.com/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# Compiled output
/dist
dist/
/tmp
/out-tsc
out-tsc/
/bazel-out

# Node
/node_modules
node_modules/
npm-debug.log
yarn-error.log

# IDEs and editors
.idea/
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# Visual Studio Code
.vscode/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/mcp.json
.history/*

# Miscellaneous
/.angular/cache
.angular/
.sass-cache/
/connect.lock
/coverage
coverage/
/libpeerconnection.log
testem.log
/typings
__screenshots__/

# System files
.DS_Store
Thumbs.db


============================================================
FILE: .prettierrc
============================================================

{
  "printWidth": 100,
  "singleQuote": true,
  "overrides": [
    {
      "files": "*.html",
      "options": {
        "parser": "angular"
      }
    }
  ]
}


============================================================
FILE: angular.json
============================================================

{
  "$schema": "./node_modules/@angular/cli/lib/config/schema.json",
  "version": 1,
  "cli": {
    "packageManager": "npm",
    "analytics": false
  },
  "newProjectRoot": "projects",
  "projects": {
    "electrocorp-webapp": {
      "projectType": "application",
      "schematics": {},
      "root": "",
      "sourceRoot": "src",
      "prefix": "app",
      "architect": {
        "build": {
          "builder": "@angular/build:application",
          "options": {
            "browser": "src/main.ts",
            "tsConfig": "tsconfig.app.json",
            "aot": true,
            "assets": [
              {
                "glob": "**/*",
                "input": "public"
              }
            ],
            "styles": [
              "src/styles.scss"
            ]
          },
          "configurations": {
            "production": {
              "budgets": [
                {
                  "type": "initial",
                  "maximumWarning": "500kB",
                  "maximumError": "1MB"
                },
                {
                  "type": "anyComponentStyle",
                  "maximumWarning": "4kB",
                  "maximumError": "16kB"
                }
              ],
              "optimization": {
                "scripts": true,
                "styles": {
                  "minify": true,
                  "inlineCritical": true
                },
                "fonts": {
                  "inline": false
                }
              },
              "outputHashing": "all"
            },
            "development": {
              "optimization": false,
              "extractLicenses": false,
              "sourceMap": true
            }
          },
          "defaultConfiguration": "production"
        },
        "serve": {
          "builder": "@angular/build:dev-server",
          "configurations": {
            "production": {
              "buildTarget": "electrocorp-webapp:build:production"
            },
            "development": {
              "buildTarget": "electrocorp-webapp:build:development"
            }
          },
          "defaultConfiguration": "development"
        },
        "test": {
          "builder": "@angular/build:unit-test"
        }
      }
    }
  }
}


============================================================
FILE: Dockerfile
============================================================

FROM node:20-alpine AS build

WORKDIR /app

COPY package*.json ./

RUN npm ci

COPY . .

RUN npm run build

FROM nginx:1.27-alpine

COPY nginx.conf /etc/nginx/conf.d/default.conf

COPY --from=build /app/dist/electrocorp-webapp/browser /usr/share/nginx/html

EXPOSE 10000

CMD ["nginx", "-g", "daemon off;"]


============================================================
FILE: nginx.conf
============================================================

server {
  listen 10000;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location ~* \.(?:js|css|png|jpg|jpeg|gif|ico|svg|webp|woff2?)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
    try_files $uri =404;
  }
}


============================================================
FILE: package.json
============================================================

{
  "name": "electrocorp-webapp",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test",
    "smoke:device-group": "node scripts/smoke-device-group-flow.mjs"
  },
  "private": true,
  "packageManager": "npm@11.12.1",
  "dependencies": {
    "@angular/common": "^21.2.0",
    "@angular/compiler": "^21.2.0",
    "@angular/core": "^21.2.0",
    "@angular/forms": "^21.2.0",
    "@angular/platform-browser": "^21.2.0",
    "@angular/router": "^21.2.0",
    "@ngx-translate/core": "^17.0.0",
    "@ngx-translate/http-loader": "^17.0.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0",
    "zone.js": "^0.16.2"
  },
  "devDependencies": {
    "@angular/build": "^21.2.10",
    "@angular/cli": "^21.2.10",
    "@angular/compiler-cli": "^21.2.0",
    "jsdom": "^28.0.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2",
    "vitest": "^4.0.8"
  }
}


============================================================
FILE: public\assets\i18n\en.json
============================================================

{
  "app": {
    "name": "ElectroCorp"
  },
  "nav": {
    "home": "Home",
    "about": "About",
    "userSettings": "Settings",
    "devices": "Devices",
    "routines": "Routines",
    "energy": "Energy",
    "history": "History",
    "plans": "Plans",
    "alerts": "Alerts",
    "billing": "Billing",
    "deviceGroups": "Groups",
    "alertRules": "Alert rules",
    "notificationPreferences": "Notifications",
    "locations": "Sites",
    "rooms": "Rooms",
    "assignments": "Assignments",
    "reports": "Reports",
    "energyGoals": "Goals",
    "supportTickets": "Support",
    "maintenanceTickets": "Maintenance",
    "operation": "Operation",
    "energySection": "Energy",
    "alertsSection": "Alerts",
    "workplace": "Spaces",
    "spaces": "Spaces",
    "support": "Support",
    "settings": "Settings",
    "operationModes": "Modes",
    "consumption": "Consumption",
    "inbox": "Inbox",
    "rules": "Rules",
    "preferences": "Preferences"
  },
  "onboarding": {
    "locationRequiredEyebrow": "Location required",
    "locationRequiredTitle": "Create a location to start using this feature.",
    "locationRequiredDescription": "You do not have a location configured yet. Create your first location to organize rooms, devices, routines, goals, alerts and reports.",
    "createLocationAction": "Create location"
  },
  "auth": {
    "loginEyebrow": "Welcome back",
    "loginTitle": "Login",
    "loginSubtitle": "Access your ElectroCorp dashboard to manage devices, routines and energy consumption.",
    "registerEyebrow": "Create access",
    "registerTitle": "Register",
    "registerSubtitle": "Create your ElectroCorp account to start managing smart energy devices.",
    "email": "Email",
    "password": "Password",
    "fullName": "Full name",
    "signIn": "Sign In",
    "signUp": "Create account",
    "createAccount": "Create account",
    "backToLogin": "Back to login",
    "logout": "Logout",
    "signInError": "Could not sign in. Check your email and password.",
    "signUpError": "Could not register the user.",
    "emailAlreadyExists": "This email is already registered.",
    "fullNameRequired": "Enter your full name.",
    "invalidEmail": "Enter a valid email address.",
    "invalidEmailProvider": "Only Gmail, Outlook or Hotmail emails are allowed.",
    "passwordTooShort": "The password must be at least 8 characters long.",
    "forgotPassword": "Forgot password?",
    "recoverPasswordEyebrow": "Account recovery",
    "recoverPasswordTitle": "Recover password",
    "recoverPasswordSubtitle": "Enter your email and we will send you recovery instructions.",
    "sendRecoveryEmail": "Send recovery email",
    "recoverPasswordSent": "Recovery email sent successfully.",
    "recoverPasswordError": "Could not send the recovery email.",
    "profileUpdated": "Profile updated successfully.",
    "profileUpdateError": "Could not update the profile.",
    "deleteAccountError": "Could not delete the account.",
    "deleteAccountConfirmationError": "The confirmation text is not valid."
  },
  "home": {
    "eyebrow": "Smart energy",
    "title": "Control your energy with ElectroCorp",
    "content": "Monitor devices, automate routines and review energy consumption from a modern platform.",
    "manageDevices": "Manage devices",
    "viewEnergy": "View energy",
    "cardDevices": "Devices",
    "cardRoutines": "Routines",
    "cardConsumption": "Current consumption",
    "cardAlerts": "Alerts",
    "activeDevices": "active devices",
    "enabledRoutines": "enabled routines",
    "averageConsumption": "Average",
    "unreadAlerts": "unread alerts",
    "energyOverview": "Energy overview",
    "subscriptionOverview": "Subscription overview",
    "alertOverview": "Alert overview",
    "openEnergyDashboard": "Open energy dashboard",
    "openPlans": "View plans",
    "openAlerts": "View alerts",
    "noPlanDescription": "Select a plan to activate ElectroCorp benefits.",
    "alertDescription": "Review consumption alerts, automation notices and important system events.",
    "setupHint": "Select or create a site to focus Home.",
    "setupEmpty": "Home activates when a site exists.",
    "operationalCenterTitle": "Operations center",
    "operationalCenterDescription": "Summary view of the active site, its spaces, operation, consumption, alerts, and service.",
    "loadingEyebrow": "Loading",
    "loadingTitle": "Syncing operational information",
    "loadingDescription": "We are reading sites, rooms, devices, energy, goals, alerts, and service.",
    "mainSummaryAria": "Main summary",
    "activeSiteEyebrow": "Active site",
    "noActiveSite": "No active site",
    "noRegisteredLocation": "No registered location",
    "go": "Go",
    "noSitesTitle": "No sites configured",
    "noSitesDescription": "Create a site to organize rooms, devices, and consumption by context.",
    "consumption": "Consumption",
    "generalStatusEyebrow": "Overall status",
    "siteSummaryTitle": "Site summary",
    "noOperationalContextTitle": "No operational context",
    "siteSummaryDescription": "Data calculated from the elements assigned to the active site.",
    "missingSiteTitle": "A site is required",
    "missingSiteDescription": "The operational summary needs an active site.",
    "criticalAlerts": "Critical alerts",
    "openSupport": "Open support",
    "siteRoomsTitle": "Site rooms",
    "roomsRegisteredDescription": "{{count}} room(s) registered in the active context.",
    "noActiveSiteRoomsDescription": "Create or select a site to view rooms.",
    "noRoomsTitle": "No rooms",
    "noRoomsDescription": "Create rooms to place devices in real zones.",
    "withDevices": "With devices",
    "withoutDevices": "Without devices",
    "averageDevicesShort": "Avg. dev.",
    "assignedEquipmentTitle": "Assigned equipment",
    "assignedEquipmentDescription": "Totals, active devices, and consumption come from devices linked to the site.",
    "noAssignedDevicesTitle": "No assigned devices",
    "noAssignedDevicesDescription": "Register or assign equipment to feed consumption, groups, and routines.",
    "groupSetsTitle": "Sets",
    "groupsDescription": "Devices grouped by use or zone.",
    "noGroupsTitle": "No groups",
    "noGroupsDescription": "Create groups to operate several devices together.",
    "deviceCountShort": "{{count}} dev.",
    "moreGroups": "+{{count}} more in groups",
    "scheduledAlarmsTitle": "Scheduled alarms",
    "scheduledAlarmsDescription": "Global actions enabled for the site.",
    "noActiveRoutinesTitle": "No active routines",
    "noActiveRoutinesDescription": "Schedule hourly actions for devices, groups, or rooms.",
    "moreRoutines": "+{{count}} more in routines",
    "activeModeTitle": "Active mode",
    "activeModeDescription": "Site-specific operational configuration.",
    "affectedDevices": "{{count}} affected device(s)",
    "noActiveModeTitle": "No active mode",
    "noActiveModeDescription": "Active site modes will appear here.",
    "createMode": "Create mode",
    "energyRecordedTitle": "Recorded energy",
    "energyRecordedDescription": "Real readings associated with assigned devices.",
    "noReadingsTitle": "No readings",
    "noReadingsDescription": "When energy readings exist, the summary will appear here.",
    "today": "Today",
    "readings": "Readings",
    "peak": "Peak",
    "goalsEyebrow": "Goals",
    "activeGoalsTitle": "Active goals",
    "activeGoalsDescription": "Current goals filtered by site, room, group, or device.",
    "noActiveGoalsTitle": "No active goals",
    "noActiveGoalsDescription": "Create goals to compare consumption against real targets.",
    "recentInboxTitle": "Recent inbox",
    "recentInboxDescription": "Alerts related to the site and its elements.",
    "noRecentAlertsTitle": "No recent alerts",
    "noRecentAlertsDescription": "There are no alerts to show for this site.",
    "criticalCount": "Critical: {{count}}",
    "accountSupportTitle": "Account support",
    "accountSupportDescription": "Open tickets and maintenance related to devices in the site.",
    "noOpenTicketsTitle": "No open tickets",
    "noOpenTicketsDescription": "Support or maintenance cases will appear here when they exist.",
    "createSupport": "Create support",
    "noRoom": "No room",
    "roomNumber": "Room #{{id}}",
    "deviceStatusOn": "ON",
    "deviceStatusOff": "OFF",
    "locationTypes": {
      "home": "Home",
      "business": "Business",
      "branch": "Branch"
    },
    "alertLevels": {
      "success": "Success",
      "stable": "Stable",
      "info": "Neutral",
      "warning": "Warning",
      "critical": "Critical"
    }
  },
  "about": {
    "eyebrow": "ElectroCorp in context",
    "title": "A platform for organizing energy, spaces and decisions",
    "subtitle": "ElectroCorp helps use energy with more clarity: organize sites and rooms, control devices, automate routines and modes, review consumption, goals, alerts, service, billing and preferences from one place.",
    "actions": {
      "home": "Go to Home",
      "spaces": "Go to Spaces",
      "energy": "Go to Energy"
    },
    "heroSignals": {
      "order": {
        "label": "Order",
        "title": "Spaces before actions",
        "description": "The platform works better when you first define real sites, rooms and assignments."
      },
      "control": {
        "label": "Control",
        "title": "Devices with context",
        "description": "Devices, groups, routines and modes are easier to understand through the place and daily use they represent."
      },
      "clarity": {
        "label": "Clarity",
        "title": "Visible energy",
        "description": "Consumption, reports and goals depend on real readings to guide better decisions."
      }
    },
    "what": {
      "eyebrow": "What it is",
      "title": "ElectroCorp connects spaces, devices and energy",
      "description": "It is not a decorative screen or an isolated list of actions. It is a platform for managing spaces, controlling equipment, creating useful automations, reviewing consumption, receiving alerts and following service, maintenance, billing and preferences.",
      "points": {
        "spaces": "Sites, rooms and assignments as the base of the system.",
        "control": "Devices, groups, routines and modes for daily operation.",
        "energy": "Consumption, history, reports and goals based on available information.",
        "care": "Alerts, rules, service and preferences to act with order."
      }
    },
    "values": {
      "mission": {
        "eyebrow": "Mission",
        "title": "Help use energy with order, control and clarity",
        "description": "ElectroCorp is designed for homes, small sites and medium spaces that need to know where their equipment is, how it is used and what decisions to make to consume better."
      },
      "vision": {
        "eyebrow": "Vision",
        "title": "An intelligent, ordered and preventive energy platform",
        "description": "The vision is to grow as a tool that connects operation, consumption, alerts and service with increasing precision, while staying transparent about what is truly available."
      }
    },
    "daily": {
      "eyebrow": "Daily use",
      "title": "A practical flow for operating ElectroCorp",
      "description": "The recommended use starts from the physical space and moves toward control, automation, energy review and follow-up.",
      "items": {
        "site": {
          "title": "Choose or create an active site",
          "description": "Work from a clear site so rooms, devices and actions have context."
        },
        "rooms": {
          "title": "Organize rooms",
          "description": "Use real rooms to place equipment and keep the operational map understandable."
        },
        "devices": {
          "title": "Review devices",
          "description": "Turn equipment on or off and validate its state from operation views."
        },
        "groups": {
          "title": "Group by use",
          "description": "Create semantic groups such as lighting, climate or critical equipment to act on sets."
        },
        "routines": {
          "title": "Schedule repeatable routines",
          "description": "Use routines for frequent schedules and avoid duplicating manual actions every day."
        },
        "mode": {
          "title": "Apply complete modes",
          "description": "Modes help activate full situations, such as saving, night or work, with internal routines."
        },
        "energy": {
          "title": "Review consumption",
          "description": "Use the dashboard and history to understand available energy behavior."
        },
        "goals": {
          "title": "Compare goals",
          "description": "Use energy goals to measure progress against targets defined by scope."
        },
        "alerts": {
          "title": "Handle alerts",
          "description": "Read visible alerts and use clear rules to prevent issues or out-of-range consumption."
        },
        "service": {
          "title": "Follow service",
          "description": "Register support or maintenance when you need a record and status progress."
        },
        "billing": {
          "title": "Review billing and preferences",
          "description": "Check plan, payments, profile and available preferences without assuming external functions that are not visible."
        }
      }
    },
    "recommendations": {
      "eyebrow": "Real recommendations",
      "title": "How to get value without overconfiguring",
      "description": "ElectroCorp works better when names, scopes and rules reflect the real operation.",
      "items": {
        "site": {
          "title": "Create the site first",
          "description": "The active site prevents the rest of the data from becoming loose or hard to read."
        },
        "rooms": {
          "title": "Define rooms before operating",
          "description": "A well-named room helps find devices, routines and maintenance."
        },
        "assignments": {
          "title": "Assign devices carefully",
          "description": "Correct assignment improves control and avoids confusion when there are many devices."
        },
        "groups": {
          "title": "Use semantic groups",
          "description": "Group by real function, not only by temporary convenience."
        },
        "routines": {
          "title": "Routines for repeated schedules",
          "description": "If an action happens often, a routine is usually better than repeating it manually."
        },
        "modes": {
          "title": "Modes for complete situations",
          "description": "A mode should represent a full operational scene, not a single isolated action."
        },
        "energy": {
          "title": "Review energy and goals together",
          "description": "Consumption explains the present; goals help decide what to change."
        },
        "alerts": {
          "title": "Use alerts to prevent",
          "description": "Alerts are more useful when reviewed on time and tied to a clear action."
        },
        "rules": {
          "title": "Do not overdo rules",
          "description": "A few well-defined rules are usually more useful than many rules that are hard to maintain."
        },
        "names": {
          "title": "Clear names",
          "description": "Avoid ambiguous names. A good name reduces errors in operation and follow-up."
        }
      }
    },
    "capabilities": {
      "eyebrow": "What it already does well",
      "title": "Capabilities connected in the experience",
      "description": "These areas already exist in the platform and are presented as available functions, not future promises.",
      "items": {
        "spaces": {
          "eyebrow": "Spaces",
          "title": "Sites, rooms and assignments",
          "description": "Organizes the physical base on which operation is controlled."
        },
        "control": {
          "eyebrow": "Operation",
          "title": "Devices, groups and routines",
          "description": "Operates equipment, groups it and schedules repeatable actions."
        },
        "modes": {
          "eyebrow": "Modes",
          "title": "Situations with internal routines",
          "description": "Organizes complete actions for concrete moments of the day."
        },
        "energy": {
          "eyebrow": "Energy",
          "title": "Consumption, history, reports and goals",
          "description": "Reads available energy information and compares it with targets."
        },
        "alerts": {
          "eyebrow": "Alerts",
          "title": "Inbox, rules and visible preferences",
          "description": "Reviews alerts and adjusts rules or visible preferences in the experience."
        },
        "service": {
          "eyebrow": "Service",
          "title": "Support and maintenance",
          "description": "Creates requests and advances their state without adding unavailable actions."
        },
        "settings": {
          "eyebrow": "Account",
          "title": "Profile, billing and settings",
          "description": "Reviews user information, plan, payments and available preferences."
        }
      }
    },
    "careful": {
      "eyebrow": "Use with judgment",
      "title": "Where transparency matters",
      "description": "Some parts depend on real data, clear rules or information available by plan. That is why they are shown with honest scope.",
      "items": {
        "automation": {
          "title": "Automations",
          "description": "They should be created with clear schedules and scopes to avoid unexpected actions."
        },
        "alerts": {
          "title": "Alerts and rules",
          "description": "They work better with understandable ranges and a few rules that are easy to review."
        },
        "reports": {
          "title": "Reports",
          "description": "Report value depends on real readings and well-chosen filters."
        },
        "billing": {
          "title": "Billing",
          "description": "Visible information depends on the plan, payments and history available inside the platform."
        }
      }
    }
  },
  "notFound": {
    "title": "Page not found",
    "content": "The route you entered does not exist or was moved.",
    "goHome": "Go home"
  },
  "footer": {
    "rights": "All rights reserved."
  },
  "devices": {
    "eyebrow": "Device control",
    "title": "Smart device control",
    "subtitle": "Manage smart plugs, switches and lights from a centralized view.",
    "total": "Total",
    "active": "Active",
    "currentWatts": "Current consumption",
    "addTitle": "Add device",
    "name": "Name",
    "room": "Room",
    "type": "Type",
    "power": "Power in watts",
    "create": "Create device",
    "turnOn": "Turn on",
    "turnOff": "Turn off",
    "emptyTitle": "No devices yet",
    "emptyContent": "Add your first smart plug, switch or light to get started.",
    "loadError": "Could not load devices.",
    "createError": "Could not create device.",
    "updateError": "Could not update device.",
    "deleteError": "Could not delete device.",
    "planLimitReached": "You have reached the device limit allowed by your plan.",
    "createSuccess": "Device created successfully.",
    "pageDescription": "Control the equipment assigned to the active site and register new operational devices by room.",
    "activeSiteFallback": "active site",
    "noRoom": "No room",
    "go": "Go",
    "summary": {
      "assignedTitle": "Assigned",
      "onTitle": "On",
      "offDevices": "{{count}} off.",
      "consumptionEyebrow": "Consumption",
      "currentTitle": "Current",
      "activePowerDescription": "Active power for this site."
    },
    "toolbar": {
      "eyebrow": "Daily control",
      "title": "Equipment by room",
      "description": "Turn devices on, off, or delete them with real impact warnings."
    },
    "emptyRooms": {
      "title": "No rooms in the active site",
      "description": "Create a room before registering operational devices."
    },
    "emptyAssigned": {
      "title": "No assigned devices",
      "description": "Register a new device or review assignments for the active site."
    },
    "form": {
      "eyebrow": "New device",
      "description": "Register real equipment, its room, and nominal power.",
      "namePlaceholder": "E.g. Main light",
      "wattsLabel": "Device watts"
    },
    "powerAdvice": {
      "highTitle": "High consumption",
      "normalTitle": "Controlled monitoring",
      "highMessage": "For {{type}}, this value is considered high. If the device reports usage equal to or greater than {{watts}} W, ElectroCorp should mark it as high consumption and may generate an alert.",
      "normalMessage": "Enter only the nominal watts for {{type}}. ElectroCorp calculates kWh and cost with those W and real on-time."
    },
    "status": {
      "on": "On",
      "off": "Off",
      "maintenance": "Maintenance",
      "removed": "Removed",
      "unknown": "No status"
    },
    "card": {
      "activeConsumption": "Active consumption",
      "standby": "Standby",
      "currentConsumptionDescription": "Current device consumption",
      "noActiveConsumption": "No active consumption"
    },
    "validation": {
      "nameRequired": "Enter the device name.",
      "nameMinLength": "The device name must have at least 3 characters.",
      "roomRequired": "Select a room in the active site.",
      "powerRequired": "Device power must be greater than 0 W.",
      "roomRequiredBeforePreset": "Create a room in the active site before adding devices."
    },
    "toasts": {
      "createdAndAssigned": "Device created and assigned to {{room}}.",
      "createdWithoutAssignment": "Device created, but it could not be assigned to the active room.",
      "presetCreatedAndAssigned": "{{device}} added and assigned to {{room}}.",
      "presetCreatedWithoutAssignment": "{{device}} was created, but it could not be assigned to the active room.",
      "presetCreateError": "Could not add {{device}}.",
      "turnedOn": "Device turned on.",
      "turnedOff": "Device turned off.",
      "deletedHistoryKept": "Device deleted; history is kept.",
      "removedRoutineSingular": "Its associated routine was also removed.",
      "removedRoutinePlural": "{{count}} associated routines were also removed."
    },
    "deleteFallbackDevice": "This device",
    "deleteWarnings": {
      "groupsTitle": "Review groups",
      "groupSingular": "{{device}} belongs to the group {{group}}. If you delete it, that group will lose this device.",
      "groupPlural": "{{device}} belongs to {{count}} groups. If you delete it, those groups will lose this device.",
      "routinesTitle": "Review routines",
      "routineSingular": "{{device}} has one associated routine. If you delete {{device}}, its routine will disappear.",
      "routinePlural": "{{device}} has {{count}} associated routines. If you delete {{device}}, its routines will disappear.",
      "modesTitle": "Review modes",
      "modeSingular": "{{device}} is related to an existing mode. Deleting it may affect that configuration.",
      "modePlural": "{{device}} is related to {{count}} existing modes. Deleting it may affect those configurations.",
      "deleteTitle": "Delete device",
      "deleteMessage": "{{device}} will be deleted. This action does not affect already registered historical data."
    }
  },
  "routines": {
    "eyebrow": "Automation",
    "title": "Automatic routines",
    "subtitle": "Schedule actions to turn smart devices on or off based on defined times.",
    "total": "Total",
    "active": "Active",
    "inactive": "Inactive",
    "addTitle": "Create routine",
    "name": "Name",
    "device": "Device",
    "selectDevice": "Select a device",
    "action": "Action",
    "time": "Time",
    "create": "Create routine",
    "enable": "Enable",
    "disable": "Disable",
    "emptyTitle": "No routines yet",
    "emptyContent": "Create a routine to automate your devices.",
    "loadError": "Could not load routines.",
    "createError": "Could not create routine.",
    "updateError": "Could not update routine.",
    "deleteError": "Could not delete routine.",
    "conflictError": "There is already an active routine for that device at the same time.",
    "planLimitReached": "You have reached the routine limit allowed by your plan.",
    "createSuccess": "Routine created successfully.",
    "pageDescription": "Schedule operational alarms for devices, groups, rooms, or sites.",
    "createOperationalSuccess": "Routine created as an operational alarm.",
    "summary": {
      "scheduled": "Scheduled",
      "inActiveSite": "in the active site.",
      "pausedCount": "{{count}} paused.",
      "newRoutine": "New routine"
    },
    "toolbar": {
      "eyebrow": "Scheduled alarms",
      "title": "Global routines",
      "description": "These routines are independent from internal mode routines."
    },
    "emptyScheduledTitle": "No scheduled routines",
    "emptyScheduledDescription": "Create an operational alarm to turn targets on or off at a defined time.",
    "targetTypes": {
      "device": "Device",
      "group": "Group",
      "room": "Room",
      "workplace": "Site"
    },
    "targetTypeDescriptions": {
      "device": "Routine for a single device",
      "group": "Routine for multiple devices",
      "room": "Routine for the devices in one room",
      "workplace": "Routine for a complete energy center"
    },
    "targetOptions": {
      "deviceCount": "{{count}} devices"
    },
    "actions": {
      "turnOn": "Turn on",
      "turnOff": "Turn off"
    },
    "actionDescriptions": {
      "turnOn": "Enable automatically",
      "turnOff": "Disable automatically"
    },
    "repeat": {
      "once": "Once",
      "daily": "Daily",
      "weekly": "Weekly",
      "customInterval": "Every X days"
    },
    "repeatDescriptions": {
      "once": "Run only on the selected date",
      "daily": "Repeat every day",
      "weekly": "Choose specific days",
      "customInterval": "Repeat by interval"
    },
    "weekDays": {
      "mon": "Monday",
      "tue": "Tuesday",
      "wed": "Wednesday",
      "thu": "Thursday",
      "fri": "Friday",
      "sat": "Saturday",
      "sun": "Sunday",
      "monShort": "Mon",
      "tueShort": "Tue",
      "wedShort": "Wed",
      "thuShort": "Thu",
      "friShort": "Fri",
      "satShort": "Sat",
      "sunShort": "Sun"
    },
    "placeholders": {
      "device": "Select a device",
      "group": "Select a group",
      "room": "Select a room",
      "workplace": "Select a site"
    },
    "form": {
      "description": "Define target, action, schedule, and repetition as a global alarm.",
      "namePlaceholder": "E.g. Turn off night lights",
      "target": "Target",
      "targetTypePlaceholder": "Target type",
      "repetition": "Repetition",
      "scheduledTime": "Scheduled time",
      "days": "Days",
      "weeklyDaysAria": "Weekly repetition days",
      "everyDays": "Every how many days",
      "daysSuffix": "days"
    },
    "status": {
      "active": "Active",
      "inactive": "Inactive",
      "paused": "Paused"
    },
    "card": {
      "time": "Time",
      "detailsAria": "Routine details",
      "applicable": "Applicable",
      "blocked": "Blocked",
      "onceWithDate": "Once - {{date}}",
      "everyDays": "Every {{count}} day(s)",
      "weeklyWithDays": "Weekly - {{days}}",
      "weeklyGeneric": "weekly",
      "weeklyDaysText": "on {{days}}",
      "waitingOnceDate": "Scheduled for {{date}} at {{time}}.",
      "waitingOnce": "Scheduled for a one-time run at {{time}}.",
      "waitingWeekly": "Will run {{days}} at {{time}}.",
      "waitingInterval": "Will repeat every {{count}} day(s) at {{time}}.",
      "waitingDaily": "Will repeat every day at {{time}}.",
      "pausedOnceDate": "Paused. It will not run on {{date}}.",
      "pausedWeekly": "Paused. Weekly repetition is suspended.",
      "pausedInterval": "Paused. Interval repetition is suspended.",
      "pausedDaily": "Paused. Daily repetition is suspended."
    },
    "validation": {
      "activeSiteRequired": "Select an active site before creating routines.",
      "weeklyDayRequired": "Select at least one day for the weekly routine."
    },
    "conflict": {
      "title": "Review routine",
      "message": "There is already an active routine with the same target, time, and action. You can continue if you want to repeat that schedule."
    },
    "delete": {
      "title": "Delete routine",
      "message": "{{name}} will be deleted. The schedule will stop running.",
      "fallbackName": "this routine"
    },
    "toasts": {
      "activated": "Routine activated.",
      "paused": "Routine paused.",
      "deleted": "Routine deleted."
    },
    "list": {
      "empty": "You do not have routines yet.",
      "groupFallback": "Group #{{id}}",
      "deviceFallback": "Device #{{id}}"
    }
  },
  "energy": {
    "eyebrow": "Energy monitoring",
    "title": "Energy consumption monitoring",
    "subtitle": "Visualize your device consumption, filter history and export readings for analysis.",
    "exportCsv": "Export CSV",
    "totalWatts": "Total watts",
    "averageWatts": "Average",
    "highestReading": "Peak usage",
    "startDate": "Start date",
    "endDate": "End date",
    "filter": "Filter",
    "reset": "Reset",
    "recommendationTitle": "Savings recommendation",
    "analytics": "Analytics",
    "chartTitle": "Consumption history",
    "emptyReadings": "No energy readings available.",
    "loadError": "Could not load energy readings.",
    "noDataRecommendation": "There is not enough data yet to generate a recommendation.",
    "highRecommendation": "Your energy consumption is high. Review your highest-consuming devices.",
    "moderateRecommendation": "Your energy consumption is moderate. Review peak hours to improve savings.",
    "lowRecommendation": "Your energy consumption remains low. Keep optimizing your routines.",
    "exportNotAllowed": "Your current plan does not allow CSV report exports.",
    "highReadings": "High readings",
    "invalidDateRange": "The selected date range is not valid.",
    "exportSuccess": "CSV exported successfully."
  },
  "history": {
    "eyebrow": "Energy history",
    "title": "Consumption history",
    "subtitle": "Review registered readings, detect consumption peaks and export information for reports.",
    "registeredReadings": "Registered readings",
    "records": "records",
    "device": "Device",
    "watts": "Watts",
    "date": "Date",
    "status": "Status",
    "high": "High",
    "normal": "Normal",
    "filterTitle": "Filter readings",
    "filterDescription": "Select a date range to review registered energy consumption.",
    "registeredReadingsDescription": "Energy reading history ordered by registration date.",
    "advancedFiltersAria": "Advanced history filters",
    "scope": {
      "label": "Scope",
      "active": "Active site",
      "activeDescription": "Only readings from {{site}}",
      "activeFallbackDescription": "Use the site selected on Home",
      "global": "Global",
      "globalDescription": "All readings kept for the user"
    },
    "scopeSummary": {
      "global": "Global history across all your sites.",
      "noActiveSite": "Select an active site to view its readings.",
      "activeSite": "History isolated by active site: {{site}}."
    },
    "deviceNamePlaceholder": "Device name",
    "deviceId": "Device ID",
    "deviceIdPlaceholder": "E.g. 3",
    "readingId": "Reading ID",
    "readingIdPlaceholder": "E.g. 15",
    "minWatts": "Min. watts",
    "maxWatts": "Max. watts",
    "sortBy": "Sort by",
    "clearFilters": "Clear filters",
    "emptyEyebrow": "No readings",
    "emptyTitle": "No readings found",
    "emptyDescription": "Adjust the filters to review other energy consumption records.",
    "energy": "Energy",
    "estimatedCost": "Estimated cost",
    "time": "Time",
    "interval": "Interval",
    "readingMeta": "ID #{{deviceId}} - Reading #{{readingId}}",
    "showing": "Showing",
    "of": "of",
    "readings": "readings",
    "pageOf": "Page {{page}} of {{total}}",
    "previous": "Previous",
    "next": "Next",
    "startDate": "Start date",
    "endDate": "End date",
    "reset": "Reset",
    "statusOptions": {
      "all": "All",
      "normal": "Normal",
      "high": "High"
    },
    "sortOptions": {
      "newest": "Newest first",
      "oldest": "Oldest first",
      "wattsDesc": "Highest consumption",
      "wattsAsc": "Lowest consumption",
      "costDesc": "Highest cost"
    },
    "toasts": {
      "exportSuccess": "History exported.",
      "exportError": "Could not export history."
    },
    "emptyValues": {
      "noDate": "No date",
      "noTime": "No time",
      "undefined": "Undefined"
    }
  },
  "billing": {
    "eyebrow": "Billing",
    "title": "Service plans",
    "subtitle": "Choose the plan that best fits your home, business or multiple locations. There is no free plan; all plans are oriented to real operation.",
    "month": "month",
    "subscribe": "Subscribe",
    "selected": "Selected",
    "currentPlan": "Current plan",
    "activeSubscription": "Active subscription",
    "noPlan": "No plan",
    "cancel": "Cancel plan",
    "loadError": "Could not load plan information.",
    "subscribeError": "Could not process the subscription.",
    "cancelError": "Could not cancel the subscription.",
    "alreadySubscribed": "You are already subscribed to this plan.",
    "paymentEyebrow": "Payment confirmation",
    "paymentTitle": "Confirm payment method",
    "selectedPlan": "Selected plan",
    "amountToPay": "Amount to pay",
    "holderName": "Cardholder name",
    "cardNumber": "Card number",
    "expirationDate": "Expiration date",
    "cvv": "CVV",
    "confirmPayment": "Confirm payment",
    "cancelPayment": "Cancel",
    "invalidCardNumber": "The card number is not valid.",
    "invalidCvv": "The CVV is not valid.",
    "invalidHolderName": "Enter the cardholder name.",
    "lastPayment": "Last payment",
    "recommended": "RECOMMENDED",
    "choosePlan": "Choose plan",
    "activePlanBadge": "Active plan",
    "summaryStarterLabel": "Small homes",
    "summaryStarterText": "Ideal to start with basic monitoring and simple alerts.",
    "summaryProfessionalLabel": "Recommended",
    "summaryProfessionalText": "The best option for advanced homes and small businesses.",
    "summaryEnterpriseLabel": "Multiple locations",
    "summaryEnterpriseText": "Built for teams, locations and business operations.",
    "maxDevices": "Up to {{ count }} devices",
    "maxRoutines": "Up to {{ count }} routines",
    "reportExportEnabled": "Report export",
    "planUsage": {
      "STARTER": "For small homes",
      "PROFESSIONAL": "For advanced homes and small businesses",
      "ENTERPRISE": "For multiple business locations"
    },
    "planDescriptions": {
      "STARTER": "Basic energy management for small homes.",
      "PROFESSIONAL": "Advanced control for homes and small businesses.",
      "ENTERPRISE": "Complete platform for multiple business locations."
    },
    "planFeatures": {
      "STARTER": {
        "devices": "Up to 5 smart devices",
        "dashboard": "Basic energy dashboard",
        "alerts": "Email alerts"
      },
      "PROFESSIONAL": {
        "devices": "Up to 20 smart devices",
        "analytics": "Advanced energy analytics",
        "routines": "Automation routines",
        "reports": "Monthly reports"
      },
      "ENTERPRISE": {
        "locations": "Multiple business locations",
        "accessProfiles": "Access profiles for staff",
        "alerts": "Advanced alerts",
        "support": "Priority support"
      }
    },
    "historyEyebrow": "Billing history",
    "historyTitle": "Billing history",
    "historySubtitle": "Review your payments, invoices and current subscription status.",
    "invoices": "Invoices",
    "invoice": "Invoice",
    "issuedAt": "Issued at",
    "status": "Status",
    "downloadInvoice": "Download invoice",
    "emptyInvoicesTitle": "No invoices yet",
    "emptyInvoicesContent": "Your invoices will appear here after your payments are processed.",
    "subscribeSuccess": "Subscription activated successfully.",
    "cancelSuccess": "Subscription cancelled successfully.",
    "invalidExpirationDate": "The expiration date is not valid.",
    "invoiceNotFound": "Invoice not found."
  },
  "alerts": {
    "eyebrow": "Alert center",
    "title": "System alerts",
    "subtitle": "View important events, consumption alerts and operational notifications from ElectroCorp.",
    "total": "Total",
    "unreadCount": "Unread",
    "createTitle": "Create alert",
    "alertTitle": "Title",
    "message": "Message",
    "level": "Level",
    "create": "Create alert",
    "unread": "Unread",
    "markAsRead": "Mark as read",
    "emptyTitle": "No alerts",
    "emptyContent": "When an important event occurs, it will appear in this section.",
    "loadError": "Could not load alerts.",
    "createError": "Could not create alert.",
    "markAsReadError": "Could not mark alert as read.",
    "planLimitReached": "You have reached the alert limit allowed by your plan.",
    "criticalCount": "Critical",
    "createSuccess": "Alert created successfully.",
    "deleteError": "Could not delete the alert.",
    "inboxSubtitle": "Review active alerts, important events, and notifications allowed by your preferences.",
    "summaryAria": "Visible alerts summary",
    "refresh": "Refresh",
    "hiddenByPreferences": "{{count}} alerts are outside the inbox because of your current preferences.",
    "markAsReadSuccess": "Alert marked as read.",
    "deleteSuccess": "Alert deleted.",
    "dismissSuccess": "Alert silenced for {{minutes}} minutes.",
    "dismissError": "Could not silence the alert.",
    "read": "Read",
    "dismissForMinutes": "Silence {{minutes}} min",
    "severityCalibration": "Severity calibration",
    "calibration": "Calibration",
    "viewDetails": "View details",
    "recommendedAction": "Suggested action",
    "summary": {
      "visibleInboxOnly": "Inbox-visible only",
      "withoutSilenced": "Without silenced"
    },
    "toolbar": {
      "title": "Real alerts",
      "description": "Shows active alerts saved for your user and allowed by your output preferences."
    },
    "filters": {
      "all": "All",
      "unread": "Unread",
      "stable": "Stable",
      "success": "Achievements",
      "info": "Informational",
      "warning": "Attention",
      "critical": "Critical"
    },
    "levels": {
      "stable": "Stable",
      "success": "Success",
      "info": "Info",
      "warning": "Attention",
      "critical": "Critical"
    },
    "severityDescriptions": {
      "stable": "Within range",
      "success": "Low impact",
      "info": "Normal follow-up",
      "warning": "Preventive attention",
      "critical": "Immediate attention",
      "default": "Follow-up"
    }
  },
  "common": {
    "loading": "Loading...",
    "delete": "Delete",
    "cancel": "Cancel",
    "continue": "Continue",
    "keep": "Keep",
    "ok": "OK"
  },
  "profile": {
    "eyebrow": "User profile",
    "title": "My profile",
    "subtitle": "Manage your personal information and account settings.",
    "personalData": "Personal data",
    "saveChanges": "Save changes",
    "deleteAccount": "Delete account",
    "deleteAccountWarning": "This action is permanent and cannot be undone.",
    "deleteConfirmation": "Type DELETE to confirm."
  },
  "deviceGroups": {
    "eyebrow": "Device groups",
    "title": "Device groups",
    "subtitle": "Organize devices into groups and execute actions on them together.",
    "addTitle": "Create device group",
    "name": "Name",
    "description": "Description",
    "create": "Create group",
    "createSuccess": "Device group created successfully.",
    "createError": "Could not create the device group.",
    "updateError": "Could not update the device group.",
    "deleteError": "Could not delete the device group.",
    "executeSuccess": "Group action executed successfully.",
    "executeError": "Could not execute the group action.",
    "pageDescription": "Organize devices by usage intention within the active site.",
    "noSelection": "No selection",
    "deleteSuccess": "Group deleted.",
    "summary": {
      "createdTitle": "Created",
      "availableTitle": "Available",
      "multiGroupDescription": "can belong to multiple groups.",
      "selectionEyebrow": "Selection",
      "selectedDevices": "{{count}} selected device(s)."
    },
    "toolbar": {
      "eyebrow": "Semantic groups",
      "title": "Control by intention",
      "description": "A group can represent lighting, saving, night mode, or any real usage pattern."
    },
    "emptyDevices": {
      "title": "No assigned devices",
      "description": "Assign devices to the active site before creating groups."
    },
    "emptyGroups": {
      "title": "No groups in this site",
      "description": "Create a semantic group with devices from the same room."
    },
    "form": {
      "eyebrow": "New group",
      "description": "Select devices from the same room. A device can belong to multiple groups.",
      "namePlaceholder": "E.g. Living room lighting",
      "descriptionPlaceholder": "Group purpose",
      "pickerDescription": "Mark equipment assigned to {{location}}.",
      "selectedCount": "{{count}} selected",
      "availableCount": "{{count}} available",
      "alsoBelongsTo": "Also belongs to"
    },
    "selection": {
      "select": "Select",
      "selected": "Selected"
    },
    "state": {
      "on": "On",
      "off": "Off",
      "mixed": "Mixed"
    },
    "card": {
      "defaultDescription": "Semantic group to organize devices by usage intention.",
      "assignedDevices": "Assigned devices",
      "noAssignedDevices": "No assigned devices.",
      "createdAt": "Created",
      "noDate": "No registered date"
    },
    "validation": {
      "sameRoom": "A group must belong to a single room. Select devices from the same room.",
      "activeLocationRequired": "Select an active site before creating a group.",
      "nameRequired": "Enter a group name before creating it.",
      "deviceRequired": "Select at least one device to create the group."
    },
    "deleteFallbackGroup": "This group",
    "deleteWarnings": {
      "devicesTitle": "Review devices",
      "devicesMessage": "{{group}} contains {{count}} device(s). If you delete the group, the devices will still exist.",
      "routinesTitle": "Review routines",
      "routineSingular": "{{group}} participates in one active routine. Review whether you want to continue.",
      "routinePlural": "{{group}} participates in {{count}} active routines. Review whether you want to continue.",
      "modesTitle": "Review modes",
      "modeSingular": "{{group}} is related to an existing mode. Deleting it may affect that configuration.",
      "modePlural": "{{group}} is related to {{count}} existing modes. Deleting it may affect those configurations.",
      "deleteTitle": "Delete group",
      "deleteMessage": "{{group}} will be deleted. Devices are not deleted."
    }
  },
  "alertRules": {
    "eyebrow": "Alert rules",
    "title": "Alert rules",
    "subtitle": "Configure automatic rules to generate alerts based on consumption thresholds.",
    "total": "Total",
    "enabled": "Enabled",
    "createTitle": "Create alert rule",
    "name": "Name",
    "threshold": "Threshold",
    "create": "Create rule",
    "enable": "Enable",
    "disable": "Disable",
    "createSuccess": "Alert rule created successfully.",
    "createError": "Could not create the alert rule.",
    "updateError": "Could not update the alert rule.",
    "deleteError": "Could not delete the alert rule.",
    "pageDescription": "Define persistent criteria to classify operational events. Evaluation uses the active rules saved for your user.",
    "summaryAria": "Rules summary",
    "savedRules": "Saved rules",
    "pausedCount": "{{count}} paused",
    "activeWeight": "Active weight",
    "usedWhenEvaluating": "Used during evaluation",
    "realScoreRanges": "Real score ranges",
    "realScoreRangesDescription": "These ranges come from Preferences. Stable starts at 0 and Critical ends at 100.",
    "newRule": "New rule",
    "createPersistentRule": "Create persistent rule",
    "createPersistentRuleDescription": "The rule is saved and can participate in evaluation while it is active.",
    "namePlaceholder": "E.g. Power peak in living room",
    "metric": "Metric",
    "condition": "Condition",
    "scope": "Scope",
    "criterion": "Criterion",
    "weight": "Weight",
    "savedRulesDescription": "Enabling, pausing, or deleting affects the persisted list and the next evaluation.",
    "emptyTitle": "No saved rules",
    "emptyDescription": "Create a rule so evaluation has persisted criteria.",
    "evaluation": "Evaluation",
    "testActiveRules": "Test active rules",
    "testActiveRulesDescription": "The test uses the evaluation service and the saved active rules.",
    "observedValue": "Observed value",
    "evaluate": "Evaluate",
    "testedNotification": "Tested notification",
    "score": "Score",
    "criteria": "Criteria",
    "readingsValue": "{{count}} readings",
    "validation": {
      "nameRequired": "Enter a name for the rule."
    },
    "toasts": {
      "paused": "Rule paused.",
      "activated": "Rule activated.",
      "deleted": "Rule deleted.",
      "evaluateError": "Could not evaluate the rules.",
      "evaluated": "Rules evaluated."
    },
    "metrics": {
      "watts": "Instant power",
      "highReadingCount": "High reading count",
      "averageWatts": "Average watts"
    },
    "metricDescriptions": {
      "watts": "Watts measured in the event.",
      "highReadingCount": "Number of events marked as high in the evaluated period.",
      "averageWatts": "Observed average for the period."
    },
    "conditions": {
      "greaterOrEqual": "Reaches or exceeds",
      "greaterThan": "Exceeds",
      "greaterOrEqualLower": "reaches or exceeds",
      "greaterThanLower": "exceeds"
    },
    "scopes": {
      "general": "General"
    },
    "evaluators": {
      "activePower": "Active power",
      "dailyKwh": "Daily consumption",
      "averageWatts": "Average watts",
      "highReadingCount": "High reading count",
      "sustainedConsumption": "Sustained consumption",
      "deviceCount": "Device count",
      "goalUsage": "Goal usage",
      "goalDeadline": "Goal deadline",
      "routineContext": "Routine context",
      "costEstimate": "Estimated cost",
      "configurationCoverage": "Configured coverage"
    },
    "severityRanges": {
      "stableDescription": "Operation within the expected range.",
      "infoDescription": "Useful event for follow-up, without urgency.",
      "warningDescription": "Review before it escalates.",
      "criticalDescription": "Requires immediate action."
    }
  },
  "notificationPreferences": {
    "eyebrow": "Notification preferences",
    "title": "Notification preferences",
    "subtitle": "Configure how and when you want to receive ElectroCorp notifications.",
    "emailEnabled": "Email notifications",
    "pushEnabled": "Push notifications",
    "criticalOnly": "Critical alerts only",
    "minimumLevel": "Minimum level",
    "save": "Save preferences",
    "updateSuccess": "Notification preferences updated successfully.",
    "updateError": "Could not update notification preferences.",
    "sensitivityTitle": "Alert sensitivity",
    "sensitivityDescription": "Adjust visible categories, noise, preliminary weights, and alert output for your user.",
    "currentPreference": "Current preference",
    "editorTitle": "Preliminary editor",
    "editorDescription": "Channels, sources, and silences are saved; weights and ranges are ready for the next contract.",
    "emptyTitle": "No preferences loaded",
    "emptyDescription": "When the user preference is available, you will be able to edit inbox visibility.",
    "summary": {
      "rangesEyebrow": "Ranges",
      "rangesTitle": "Stable to Critical",
      "rangesDescription": "Limits remain contiguous to avoid overlaps.",
      "successTitle": "Positive apart",
      "successDescription": "Good events do not compete with risk.",
      "parametersEyebrow": "Parameters",
      "parametersTitle": "Weights and noise",
      "parametersDescription": "Controls priority, repetition, schedules, and sources."
    },
    "sensitivity": "Sensitivity",
    "weightedClassification": "Weighted classification",
    "weightedClassificationDescription": "Success stays outside risk. Stable, Neutral, Warning, and Critical are calculated by contiguous ranges.",
    "stableCeiling": "Stable cap",
    "neutralCeiling": "Info cap",
    "warningCeiling": "Warning cap",
    "preview": "Preview",
    "sensitiveResult": "Sensitive result",
    "previewDescription": "The technical score does not need to appear in the inbox; here it only calibrates the category.",
    "context": "Context",
    "baseSignal": "Base signal",
    "finalCategory": "Final category",
    "weightedPercent": "{{value}}% weighted",
    "positiveEvents": "Positive events",
    "positiveEventsDescription": "Correct actions, recoveries, completed goals, and successful applications.",
    "showSuccess": "Show Success",
    "showSuccessDescription": "Keeps the good visible without mixing it with risk.",
    "visibleCategories": "Visible categories",
    "allowedLevels": "Allowed levels",
    "allowedLevelsDescription": "The current contract stores these levels; Neutral persists as INFO.",
    "criticalOnlyLabel": "Critical only",
    "criticalOnlyDescription": "Reduces noise and lets only urgent events through.",
    "weighting": "Weighting",
    "contextParameters": "Parameters by context",
    "contextParametersDescription": "These weights are preliminary: they adjust the preview and prepare the next contract.",
    "weightDescription": "{{weight}}% weight",
    "output": "Output",
    "channelsAndNoise": "Channels and noise",
    "channelsAndNoiseDescription": "These parameters are already saved in the current preference.",
    "deliveryMode": "Delivery mode",
    "cooldown": "Cooldown",
    "minutes": "minutes",
    "maxPerHour": "Maximum per hour",
    "alertsUnit": "alerts",
    "silences": "Silences",
    "scheduleAndExceptions": "Schedule and exceptions",
    "scheduleAndExceptionsDescription": "Critical can break silence if you allow it.",
    "quietStart": "Silence start",
    "quietEnd": "Silence end",
    "levelDescriptions": {
      "stable": "Show everything from stable state.",
      "info": "Receive relevant events and above.",
      "warning": "Receive only preventive and critical events.",
      "critical": "Receive only urgent events."
    },
    "deliveryModes": {
      "banner": "Banner",
      "quiet": "Silent",
      "inboxOnly": "Inbox only",
      "muted": "Muted"
    },
    "deliveryModeDescriptions": {
      "banner": "Normal visible output.",
      "quiet": "No invasive noise.",
      "inboxOnly": "No popup notice.",
      "muted": "No invasive output."
    },
    "sources": {
      "device": "Devices",
      "group": "Groups",
      "room": "Rooms",
      "workplace": "Sites",
      "routine": "Routines",
      "goal": "Goals",
      "report": "Reports",
      "rule": "Rules",
      "mode": "Modes",
      "system": "System"
    },
    "sourceDescriptions": {
      "device": "Status, response, and direct equipment consumption.",
      "group": "Aggregated changes by usage intent.",
      "routine": "Execution, failures, or automation silences.",
      "goal": "Progress, risk of exceeding, or completion.",
      "rule": "Automatic thresholds and active evaluations.",
      "mode": "Application of operational profiles.",
      "system": "Platform recommendations and own events."
    },
    "toggles": {
      "inAppDescription": "Show alerts inside the app.",
      "toastTitle": "Toast",
      "toastDescription": "Popup notices during the session.",
      "dashboardTitle": "Dashboard",
      "dashboardDescription": "Reflect alerts in panels.",
      "emailTitle": "Email",
      "emailDescription": "Channel prepared for external notices.",
      "pushTitle": "Push",
      "pushDescription": "Channel prepared for device notices.",
      "groupSimilarTitle": "Group similar",
      "groupSimilarDescription": "Reduces repetition by alert thread.",
      "quietHoursTitle": "Quiet schedule",
      "quietHoursDescription": "Uses a noise-free window.",
      "criticalBreaksTitle": "Critical breaks silence",
      "criticalBreaksDescription": "Urgent events remain visible.",
      "remindersTitle": "Reminders",
      "remindersDescription": "Allows later notices.",
      "nightRoutinesTitle": "Night routines",
      "nightRoutinesDescription": "Reduces noise from night automation.",
      "goalDeadlineTitle": "Dated goals",
      "goalDeadlineDescription": "Prioritizes goals near the limit.",
      "maintenanceDescription": "Includes equipment under review.",
      "recommendationsTitle": "Recommendations",
      "recommendationsDescription": "Shows system suggestions.",
      "dailySummaryTitle": "Daily summary",
      "dailySummaryDescription": "Groups daily activity.",
      "weeklySummaryTitle": "Weekly summary",
      "weeklySummaryDescription": "Groups weekly trends."
    }
  },
  "workplace": {
    "locationsEyebrow": "Workplace",
    "locationsTitle": "Locations",
    "locationsSubtitle": "Manage business locations where ElectroCorp devices are installed.",
    "roomsEyebrow": "Rooms",
    "roomsTitle": "Rooms",
    "roomsSubtitle": "Organize rooms within each location.",
    "assignmentsEyebrow": "Device assignments",
    "assignmentsTitle": "Device assignments",
    "assignmentsSubtitle": "Assign devices to a location and room so they can participate in groups, routines, modes, goals and reports.",
    "totalLocations": "Total locations",
    "businessLocations": "Business locations",
    "branchLocations": "Branch locations",
    "totalRooms": "Total rooms",
    "totalAssignments": "Total assignments",
    "totalDevices": "Total devices",
    "createLocation": "Create location",
    "createRoom": "Create room",
    "assignDevice": "Assign device",
    "locationName": "Location name",
    "roomName": "Room name",
    "address": "Address",
    "floor": "Floor",
    "selectLocation": "Select location",
    "selectRoom": "Select room",
    "selectDevice": "Select device",
    "create": "Create",
    "loadError": "Could not load workplace information.",
    "locationCreateSuccess": "Location created successfully.",
    "locationCreateError": "Could not create the location.",
    "locationUpdateSuccess": "Location updated successfully.",
    "locationUpdateError": "Could not update the location.",
    "locationDeleteError": "Could not delete the location.",
    "roomCreateSuccess": "Room created successfully.",
    "roomCreateError": "Could not create the room.",
    "roomUpdateSuccess": "Room updated successfully.",
    "roomUpdateError": "Could not update the room.",
    "roomDeleteError": "Could not delete the room.",
    "assignmentCreateSuccess": "Device assigned successfully.",
    "assignmentCreateError": "Could not assign the device.",
    "assignmentMoveSuccess": "Device assignment moved successfully.",
    "assignmentMoveError": "Could not move the device assignment.",
    "assignmentDeleteError": "Could not delete the device assignment.",
    "deviceAlreadyAssigned": "This device is already assigned.",
    "locationTypes": {
      "home": "Home",
      "business": "Business",
      "branch": "Branch"
    },
    "locations": {
      "pageDescription": "Manage the physical places where ElectroCorp organizes rooms, devices, and consumption.",
      "summary": {
        "registeredTitle": "Registered",
        "registeredDescription": "Homes, offices, or operational branches.",
        "assignedTitle": "Assigned to site",
        "unassignedDevices": "{{count}} device(s) without a site.",
        "coverageEyebrow": "Coverage",
        "unroomedDevices": "{{count}} device(s) without a room."
      },
      "toolbar": {
        "eyebrow": "Site management",
        "title": "Create and select operating centers",
        "description": "The location can be stored as a written reference or approximate coordinates."
      },
      "empty": {
        "title": "No sites yet",
        "description": "Create a site to organize rooms, devices, groups, and consumption by physical place."
      },
      "form": {
        "eyebrow": "New site",
        "description": "Define the name, type, and reference location for this operating space.",
        "namePlaceholder": "E.g. Main office",
        "typeLabel": "Type",
        "typePlaceholder": "Site type",
        "addressLabel": "Address or reference",
        "addressPlaceholder": "Address, reference, or coordinates"
      },
      "map": {
        "title": "Approximate selector",
        "description": "OpenStreetMap is used as a visual reference without an API key. If there is no exact address, approximate coordinates are stored.",
        "iframeTitle": "OpenStreetMap reference map",
        "surfaceLabel": "Select a point on the map",
        "selectedLabel": "Selected location",
        "selectedAddress": "Selected location ({{coordinates}})"
      },
      "deleteTitle": "Delete site",
      "deleteMessage": "You are about to delete {{name}}. Related rooms and assignments may stop appearing in Spaces.",
      "deleteSuccess": "Site deleted.",
      "card": {
        "createdAt": "Created",
        "noDate": "No registered date",
        "activePower": "Active power",
        "withoutRoom": "Without room",
        "coverage": "Coverage",
        "goToRooms": "Go",
        "goToRoomsAria": "Go to this site rooms"
      }
    },
    "rooms": {
      "pageDescription": "Organize the active site by rooms to understand where devices are and how consumption is distributed.",
      "activeLocationFallback": "active site",
      "summary": {
        "activeLocationEyebrow": "Active site",
        "availableRooms": "room(s) available to organize movements.",
        "registeredTitle": "Registered",
        "registeredDescription": "Physical zones available for device assignment.",
        "assignedTitle": "Assigned",
        "linkedGroups": "{{count}} group(s) linked in the site."
      },
      "toolbar": {
        "eyebrow": "Room management",
        "title": "Add zones to {{location}}",
        "description": "Rooms help move devices and review consumption by place."
      },
      "empty": {
        "title": "No rooms in this site",
        "description": "Create a room to locate devices and move them between real zones."
      },
      "form": {
        "eyebrow": "New room",
        "description": "The room will be added to the active site and become available for device movements.",
        "namePlaceholder": "E.g. Main living room",
        "siteLabel": "Site"
      },
      "status": {
        "noDevices": "No devices",
        "active": "With activity",
        "idle": "No activity"
      },
      "card": {
        "createdAt": "Created",
        "noDate": "No registered date",
        "activeDevices": "On",
        "consumption": "Consumption",
        "relatedGroup": "Related group",
        "noLinkedGroup": "No linked group",
        "relatedRoutines": "Related routines",
        "goToAssignments": "Go",
        "goToAssignmentsAria": "Go to this room assignments"
      },
      "toasts": {
        "loadError": "Could not load rooms information.",
        "locationRequired": "Create a site before creating rooms.",
        "deleteSuccess": "Room deleted successfully."
      },
      "deleteTitle": "Delete room",
      "deleteMessageWithDevices": "This room has {{count}} assigned device(s). If you delete it, those references may be left without a room.",
      "deleteMessageEmpty": "You are about to delete this room from the active site."
    },
    "deviceTypes": {
      "plug": "Plug",
      "light": "Light",
      "switch": "Switch",
      "sensor": "Sensor",
      "other": "Other"
    },
    "assignments": {
      "pageDescription": "Drag devices between two rooms and confirm when the change affects groups, routines, or modes.",
      "activeLocationFallback": "active site",
      "noRoom": "No room",
      "noSecondRoomOption": "No other room available",
      "noSecondRoomAvailable": "No second room is available.",
      "panelItemCount": "{{count}} item(s) in this room.",
      "go": "Go",
      "summary": {
        "activeLocationEyebrow": "Active site",
        "availableRooms": "room(s) available to organize movements.",
        "movableTitle": "Movable",
        "connectedDevices": "device(s) connected to this site.",
        "interactionEyebrow": "Interaction",
        "dragTitle": "Direct drag",
        "directValue": "Direct",
        "dragDescription": "drag devices between Room 1 and Room 2."
      },
      "movement": {
        "eyebrow": "Movement between rooms",
        "title": "New assignment",
        "description": "Drag each device from one panel to the other. Group, routine, and mode confirmations appear before applying the change."
      },
      "panels": {
        "roomOne": "Room 1",
        "roomTwo": "Room 2"
      },
      "emptyRooms": {
        "title": "No rooms",
        "description": "Create rooms in the active site to move devices or groups."
      },
      "emptyAssignments": {
        "title": "No assigned devices",
        "description": "When a site has connected devices, you will be able to move them between rooms from this view."
      },
      "emptyPanel": {
        "title": "No devices",
        "description": "Drag a device here to move it to this room."
      },
      "unavailablePanel": {
        "title": "Room unavailable",
        "description": "Add another room to enable this panel."
      },
      "device": {
        "noPowerInfo": "No power information",
        "notAvailable": "Not available",
        "on": "On",
        "off": "Off",
        "maintenance": "Maintenance"
      },
      "form": {
        "operationalSite": "Operational site",
        "siteOnly": "Assign only to the site",
        "availableRoom": "Available room"
      },
      "toasts": {
        "loadError": "Could not load assignments information.",
        "moveSuccess": "Movement applied successfully.",
        "moveError": "Could not complete the movement."
      },
      "warnings": {
        "groupTitle": "Review group",
        "groupMessage": "This device belongs to the group {{group}}. If you move it, that group may become incomplete.",
        "routinesTitle": "Review routines",
        "routinesMessage": "This device participates in active routines. Review whether you want to continue.",
        "modesTitle": "Review modes",
        "modesMessage": "This device is related to a mode. Moving it may affect that mode."
      }
    }
  },
  "reporting": {
    "reportsEyebrow": "Reports",
    "reportsTitle": "Consumption reports",
    "reportsSubtitle": "Generate and review energy consumption reports.",
    "goalsEyebrow": "Energy goals",
    "goalsTitle": "Energy goals",
    "goalsSubtitle": "Define savings goals and track your progress.",
    "totalReports": "Total reports",
    "totalGoals": "Total goals",
    "activeGoals": "Active goals",
    "completedGoals": "Completed goals",
    "totalReportedWatts": "Total reported watts",
    "averageReportedWatts": "Average reported watts",
    "monthlyComparison": "Report comparison",
    "monthlyComparisonSubtitle": "Compare reports from the same period:",
    "createReport": "Create report",
    "createGoal": "Create goal",
    "reportTitle": "Report title",
    "goalTitle": "Goal title",
    "generate": "Generate",
    "export": "Export",
    "totalWatts": "Total watts",
    "averageWatts": "Average watts",
    "highestWatts": "Highest watts",
    "targetWatts": "Target watts",
    "deadline": "Deadline",
    "createdAt": "Created at",
    "generatedAt": "Generated at",
    "emptyReportsTitle": "No reports yet",
    "emptyReportsContent": "Generate your first consumption report to analyze your energy usage.",
    "emptyGoalsTitle": "No goals yet",
    "emptyGoalsContent": "Create an energy goal to start tracking savings.",
    "loadError": "Could not load reporting information.",
    "reportCreateSuccess": "Report created successfully.",
    "reportCreateError": "Could not create the report.",
    "reportDeleteError": "Could not delete the report.",
    "exportNotAllowed": "Your current plan does not allow report exports.",
    "reportNotFound": "Report not found.",
    "goalCreateSuccess": "Energy goal created successfully.",
    "goalCreateError": "Could not create the energy goal.",
    "goalUpdateSuccess": "Energy goal updated successfully.",
    "goalUpdateError": "Could not update the energy goal.",
    "goalDeleteError": "Could not delete the energy goal.",
    "createReportDescription": "Choose the report type and move the wheel to select the exact range.",
    "period": "Period",
    "periods": {
      "daily": "Daily",
      "weekly": "Weekly",
      "monthly": "Monthly",
      "yearly": "Yearly"
    },
    "periodOptions": {
      "daily": "Daily",
      "weekly": "Weekly",
      "monthly": "Monthly",
      "yearly": "Yearly"
    },
    "reportRangeEyebrow": "Report range",
    "reportRangeHint": "Drag to the right to go into the past. The current range is the final limit.",
    "rangeTitles": {
      "daily": "Select the day",
      "weekly": "Select the week",
      "monthly": "Select the month",
      "yearly": "Select the year"
    },
    "rangeLabels": {
      "currentWeek": "Current week",
      "currentYearCycle": "{{year}} - current",
      "currentYear": "Current year",
      "fullYear": "Full year"
    },
    "generatedReportsTitle": "Generated reports",
    "generatedReportsDescription": "Review created reports and open each detail from its summary.",
    "emptyReportsEyebrow": "No reports",
    "from": "From",
    "to": "To",
    "generated": "Generated",
    "emptyValue": "-",
    "reportFallbackTitle": "Report",
    "reportFallbackTitleWithId": "Report #{{id}}",
    "averageDetail": "{{value}} average",
    "reportChartAria": "{{title}}, {{total}}, {{start}} - {{end}}",
    "reportNameRequired": "Enter the report name.",
    "reportNameMinLength": "The report name must be at least 4 characters.",
    "reportRangeRequired": "Select a date range for the report.",
    "reportRangeInvalid": "The report range is not valid.",
    "reportUpdateSuccess": "Report updated.",
    "reportUnchanged": "That report already exists for the selected cycle.",
    "reportExportSuccess": "Report exported.",
    "reportExportError": "Could not export the report.",
    "reportDeleteSuccess": "Report deleted.",
    "duration": {
      "hoursMinutes": "{{hours}}h {{minutes}}m",
      "minutesOnly": "{{minutes}}m"
    },
    "invoice": {
      "eyebrow": "Professional energy invoice",
      "title": "ElectroCorp report #{{id}}",
      "close": "Close invoice",
      "totalEnergy": "Total energy",
      "exactReadings": "{{count}} exact readings within the range",
      "totalToPay": "Total to pay",
      "realKwhCalculation": "Calculated from real kWh per device",
      "monitoredDuration": "{{duration}} monitored",
      "technicalSummary": "Technical summary",
      "averagePower": "Average power",
      "detectedPeak": "Detected peak",
      "criticalReadings": "Critical readings",
      "costPerKwh": "Cost per kWh",
      "billedEnergy": "Billed energy",
      "energySubtotal": "Energy subtotal",
      "measuredDevices": "Measured devices",
      "totalOnTime": "Total ON time",
      "topConsumption": "Top consumption",
      "devicesCount": "{{count}} devices",
      "noDevicesInRange": "No devices in this range.",
      "deviceExactDetail": "Exact detail by device",
      "readings": "Readings",
      "onTime": "ON time",
      "exactKwh": "Exact kWh",
      "peak": "Peak",
      "share": "Share",
      "subtotal": "Subtotal",
      "noReadingsForReport": "No readings for this report."
    },
    "goals": {
      "summary": {
        "created": "Goals created",
        "tracking": "In tracking",
        "atRisk": "Goals at risk",
        "needAttention": "Need attention",
        "averageCompliance": "Average compliance"
      },
      "createStripTitle": "Create smart goal",
      "createStripDescription": "Define scope, limit, and tracking window with real data.",
      "modalEyebrow": "Energy goal",
      "modalDescription": "Select the tracking scope and define the energy limit.",
      "scope": "Scope",
      "goalName": "Goal name",
      "goalNamePlaceholder": "E.g. Reduce main room consumption",
      "consumptionLimit": "Consumption limit",
      "emptyEyebrow": "No goals",
      "generalOperation": "General operation",
      "fullDay": "All day",
      "deviceCountDescription": "{{count}} devices",
      "availableKwh": "{{value}} kWh available",
      "daysRemaining": "{{count}} days remaining",
      "limitLabel": "Limit: {{value}} kWh",
      "deadlineLabel": "Due: {{date}}",
      "measured": "Measured",
      "usedPercentage": "{{value}}% used",
      "devices": "Devices",
      "projection": "Projection",
      "scopeOptions": {
        "workplace": "A site",
        "room": "A room",
        "group": "A group",
        "device": "A device"
      },
      "scopeDescriptions": {
        "workplace": "Measures devices assigned to an energy site.",
        "room": "Measures devices assigned to a room.",
        "group": "Measures the devices included in a group.",
        "device": "Measures only the readings from one device."
      },
      "scopeTargetLabels": {
        "general": "Scope",
        "workplace": "Site",
        "room": "Room",
        "device": "Device",
        "group": "Group"
      },
      "scopeTargetPlaceholders": {
        "general": "Select scope",
        "workplace": "Select site",
        "room": "Select room",
        "device": "Select device",
        "group": "Select group"
      },
      "status": {
        "inControl": "In control",
        "outOfGoal": "Out of goal",
        "atRisk": "At risk",
        "goodPace": "Good pace",
        "noReadings": "No readings"
      },
      "recommendations": {
        "inControl": "The goal is progressing at the expected pace.",
        "outOfGoal": "Consumption exceeded the limit defined for this goal.",
        "atRisk": "The current pace could exceed the limit. Review the most active devices.",
        "goodPace": "You still have enough margin compared with the expected progress.",
        "noReadings": "There are no readings within this goal period yet."
      },
      "toasts": {
        "loadError": "Could not load energy goals information.",
        "nameRequired": "Enter the goal name.",
        "nameMinLength": "The goal name must be at least 4 characters.",
        "targetRequired": "Define a consumption limit greater than 0 kWh.",
        "deadlineRequired": "Select a deadline for the goal.",
        "deadlinePast": "The deadline cannot be in the past.",
        "timeWindowInvalid": "The time window must have different start and end times.",
        "scopeTargetRequired": "Select the goal target.",
        "deleteSuccess": "Energy goal deleted."
      },
      "deleteDialog": {
        "title": "Delete energy goal",
        "message": "This action will remove the goal from current tracking.",
        "confirm": "Delete",
        "cancel": "Keep"
      }
    }
  },
  "serviceManagement": {
    "supportEyebrow": "Support",
    "supportTitle": "Support",
    "supportSubtitle": "Create and track user support requests.",
    "maintenanceEyebrow": "Maintenance",
    "maintenanceTitle": "Maintenance",
    "maintenanceSubtitle": "Schedule and monitor device maintenance tasks.",
    "totalSupportTickets": "Total support tickets",
    "openTickets": "Open tickets",
    "urgentTickets": "Urgent tickets",
    "totalMaintenanceTickets": "Total maintenance tickets",
    "pendingMaintenance": "Pending maintenance",
    "scheduledMaintenance": "Scheduled maintenance",
    "completedMaintenance": "Completed maintenance",
    "createSupportTicket": "Create support ticket",
    "createMaintenanceTicket": "Create maintenance ticket",
    "subject": "Subject",
    "description": "Description",
    "scheduledDate": "Scheduled date",
    "selectDevice": "Select device",
    "create": "Create",
    "emptySupportTitle": "No support tickets",
    "emptySupportContent": "Support requests created by the user will appear here.",
    "emptyMaintenanceTitle": "No maintenance tickets",
    "emptyMaintenanceContent": "Scheduled maintenance tickets will appear here.",
    "loadError": "Could not load service management information.",
    "supportCreateSuccess": "Support ticket created successfully.",
    "supportCreateError": "Could not create the support ticket.",
    "supportUpdateSuccess": "Support ticket updated successfully.",
    "supportUpdateError": "Could not update the support ticket.",
    "supportDeleteError": "Could not delete the support ticket.",
    "maintenanceCreateSuccess": "Maintenance ticket created successfully.",
    "maintenanceCreateError": "Could not create the maintenance ticket.",
    "maintenanceUpdateSuccess": "Maintenance ticket updated successfully.",
    "maintenanceUpdateError": "Could not update the maintenance ticket.",
    "maintenanceDeleteError": "Could not delete the maintenance ticket.",
    "supportPageDescription": "Centralize user requests, prioritize open cases and keep follow-up with real statuses.",
    "supportSummaryTitle": "Support summary",
    "supportSummaryDescription": "Requests registered with traceability across the platform.",
    "openTicketsShort": "Open",
    "urgentTicketsShort": "Urgent",
    "newSupportRequest": "New request",
    "supportAttentionTitle": "Support attention",
    "supportAttentionDescription": "Register a case with subject, description and priority so Service can handle it.",
    "createSupportRequest": "Create request",
    "cases": "Cases",
    "supportCasesTitle": "Active and historical requests",
    "emptySupportRequestsTitle": "No requests yet",
    "emptySupportRequestsDescription": "When someone registers a support case, it will appear here with priority, status and date.",
    "supportModalDescription": "Register a support case with a real priority.",
    "supportSubjectPlaceholder": "E.g. Device not responding",
    "supportDescriptionPlaceholder": "Describe what is happening, where it happens and what impact it has.",
    "priority": "Priority",
    "saveSupportRequest": "Save request",
    "supportLoadError": "Could not load support information.",
    "supportDeleteSuccess": "Support request deleted.",
    "confirmDeleteSupportTitle": "Delete request",
    "confirmDeleteSupportMessage": "This request will leave the support inbox. This action cannot be undone.",
    "createdOn": "Created on {{date}}",
    "maintenancePageDescription": "Schedule interventions for real devices and follow their status without inventing dates, equipment or tasks.",
    "maintenanceSummaryTitle": "Maintenance summary",
    "maintenanceSummaryDescription": "Orders created from Service for existing devices.",
    "pendingMaintenanceShort": "Pending",
    "completedMaintenanceShort": "Completed",
    "scheduleEyebrow": "Schedule",
    "technicalScheduleTitle": "Technical agenda",
    "technicalScheduleDescription": "Select an existing device, reason and date to create an order.",
    "scheduleMaintenance": "Schedule maintenance",
    "goToDevices": "Go to devices",
    "orders": "Orders",
    "maintenanceAgendaTitle": "Maintenance agenda",
    "emptyMaintenanceDescription": "When you schedule an intervention, it will appear here with device, date and status.",
    "maintenanceModalDescription": "Use a real device and a valid date.",
    "maintenanceType": "Maintenance type",
    "reason": "Reason",
    "maintenanceDescriptionPlaceholder": "Describe the required work or the reason for review.",
    "saveMaintenance": "Save maintenance",
    "maintenanceLoadError": "Could not load maintenance information.",
    "maintenanceDeleteSuccess": "Maintenance deleted.",
    "confirmCancelMaintenanceTitle": "Cancel maintenance",
    "confirmCancelMaintenanceMessage": "The maintenance will be marked as canceled. Confirm only if it should no longer be executed.",
    "cancelMaintenance": "Cancel maintenance",
    "back": "Back",
    "confirmDeleteMaintenanceTitle": "Delete maintenance",
    "confirmDeleteMaintenanceMessage": "This schedule will leave the maintenance list. This action cannot be undone.",
    "scheduledFor": "Scheduled for {{date}}",
    "priorities": {
      "low": "Low",
      "medium": "Medium",
      "high": "High",
      "urgent": "Urgent"
    },
    "supportStatuses": {
      "open": "Open",
      "inProgress": "In progress",
      "resolved": "Resolved",
      "closed": "Closed"
    },
    "maintenanceTypes": {
      "inspection": "Inspection",
      "repair": "Repair",
      "replacement": "Replacement",
      "installation": "Installation"
    },
    "maintenanceStatuses": {
      "pending": "Pending",
      "scheduled": "Scheduled",
      "completed": "Completed",
      "canceled": "Canceled"
    },
    "validation": {
      "supportSubjectRequired": "Enter the request subject.",
      "supportSubjectMinLength": "The subject must have at least 4 characters.",
      "supportDescriptionRequired": "Describe the problem or request.",
      "descriptionMinLength": "The description must have at least 10 characters.",
      "deviceRequiredBeforeMaintenance": "Register a device before scheduling maintenance.",
      "maintenanceDeviceRequired": "Select a device for maintenance.",
      "maintenanceDescriptionRequired": "Describe the required maintenance.",
      "maintenanceDateRequired": "Select a maintenance date.",
      "maintenanceDatePast": "The maintenance date cannot be in the past.",
      "maintenanceDeviceUnknown": "Could not identify the selected device."
    }
  },
  "security": {
    "deleteAccount": "Delete account"
  },
  "energyDashboard": {
    "eyebrow": "Energy monitoring",
    "title": "Energy consumption",
    "description": "Control real consumption from active devices, review kWh, estimated costs, and saving opportunities by equipment and room.",
    "liveConsumptionEyebrow": "Live consumption",
    "liveConsumptionTitle": "Cost calculated from power and real time",
    "liveConsumptionDescription": "W is nominal power. The real cost comes from kWh, active seconds, and the energy tariff.",
    "automaticRecord": "Automatic recording",
    "secondsAbbr": "sec",
    "apply": "Apply",
    "sampleEveryPrefix": "A reading will be recorded every",
    "sampleEveryValue": "{{seconds}} seconds",
    "exportCsv": "Export CSV",
    "loading": "Loading energy information...",
    "consumptionEyebrow": "Consumption",
    "readings": "readings",
    "equipment": "devices",
    "energy": "energy",
    "cost": "cost",
    "accumulatedUse": "accumulated use",
    "period": "Period",
    "noReadingsEyebrow": "No readings",
    "noConsumptionTitle": "No consumption for this period",
    "noConsumptionDescription": "When real readings exist inside the selected period, the chart will build automatically.",
    "generalStatusEyebrow": "Overall status",
    "operationalSummaryTitle": "Operational summary",
    "normalReadings": "normal",
    "highReadingsShort": "high",
    "activeDevicesShort": "active",
    "activeDevicesNowTitle": "Devices on now",
    "costPerHourValue": "S/ {{cost}} per hour",
    "moreActiveDevices": "+{{count}} more active",
    "noActivityEyebrow": "No activity",
    "noActiveDevicesTitle": "No devices are on",
    "noActiveDevicesDescription": "Current consumption is zero because there are no devices ON.",
    "rankingEyebrow": "Ranking",
    "highestCostByDeviceTitle": "Highest cost by device",
    "moreInRanking": "+{{count}} more in ranking",
    "noCostEyebrow": "No cost",
    "noDeviceConsumptionTitle": "No devices with consumption",
    "noDeviceConsumptionDescription": "Readings with kWh will appear during use.",
    "roomsEyebrow": "Rooms",
    "consumptionByRoomTitle": "Consumption by room",
    "activeDevicesValue": "{{count}} active",
    "moreInRooms": "+{{count}} more in rooms",
    "noRoomsEyebrow": "No rooms",
    "noRoomDataTitle": "No room data",
    "noRoomDataDescription": "Assign rooms to your devices to see this breakdown.",
    "recentHighConsumptionEventsTitle": "Recent high-consumption events",
    "moreInAlerts": "+{{count}} more in alerts",
    "noAlertsEyebrow": "No alerts",
    "noRecentEventsTitle": "No recent events",
    "noRecentEventsDescription": "No high readings were detected in the visible period.",
    "latestReadingsTitle": "Latest energy readings",
    "power": "Power",
    "moreInHistory": "+{{count}} more in history",
    "noHistoryEyebrow": "No history",
    "noReadingsAvailableTitle": "No readings available",
    "noReadingsAvailableDescription": "When the system records consumption, you will see the latest readings here.",
    "intervalValue": "Interval {{duration}}",
    "chartItemAria": "{{energy}}, {{emoji}} {{device}}, {{watts}} W, {{date}}, interval {{duration}}",
    "highConsumptionRegistered": "{{device}} recorded high consumption",
    "noActiveAlerts": "No active alerts",
    "selectActiveSiteRecommendation": "Select an active site to view its consumption.",
    "noReadingsForSiteRecommendation": "There are no energy readings for {{site}} yet.",
    "highReadingsForSiteRecommendation": "Review equipment in {{site}} with high readings.",
    "stableConsumptionRecommendation": "Stable consumption in {{site}}.",
    "noRoom": "No room",
    "periods": {
      "daily": "Daily",
      "weekly": "Weekly",
      "monthly": "Monthly"
    },
    "periodDescriptions": {
      "daily": "Real readings recorded today.",
      "weekly": "Real readings from the last 7 days.",
      "monthly": "Real readings from the current month."
    },
    "chartTitles": {
      "daily": "Daily consumption",
      "weekly": "Weekly consumption",
      "monthly": "Monthly consumption"
    },
    "chartSubtitles": {
      "daily": "Real readings from today. Accumulated use adds time by device.",
      "weekly": "Real readings from the last 7 days. Accumulated use adds time by device.",
      "monthly": "Real readings from the current month. Accumulated use adds time by device."
    },
    "operationalStatus": {
      "review": "Review consumption",
      "attention": "Attention",
      "optimal": "Optimal"
    },
    "metrics": {
      "activePower": "Active power",
      "energyToday": "Energy today",
      "costToday": "Cost today",
      "costPerHour": "Cost per hour",
      "monthlyProjection": "Monthly projection",
      "activeAlerts": "Active alerts"
    },
    "metricHints": {
      "activePower": "Current base to calculate kWh while equipment is on.",
      "energyToday": "Accumulated by real intervals recorded today.",
      "costToday": "Estimated cost from real kWh.",
      "costPerHour": "Projection if current consumption stays the same.",
      "monthlyProjection": "Estimated from real kWh accumulated this month.",
      "activeAlerts": "{{count}} critical - {{level}} - {{title}}"
    },
    "readingStatuses": {
      "high": "High",
      "normal": "Normal"
    },
    "toasts": {
      "loadError": "Could not load energy information.",
      "exportSuccess": "CSV exported successfully.",
      "exportError": "Could not export the CSV.",
      "intervalUpdated": "Interval updated to {{seconds}} seconds.",
      "intervalUpdateError": "Could not update the recording interval.",
      "samplingLoadFallback": "Could not load the recording interval. 15 seconds will be used.",
      "refreshError": "Could not update energy information."
    }
  },
  "shared": {
    "numberStepper": {
      "increase": "Increase value",
      "decrease": "Decrease value"
    },
    "datePicker": {
      "placeholder": "Select date",
      "calendarAria": "Date picker",
      "previousMonth": "Previous month",
      "nextMonth": "Next month",
      "clear": "Clear",
      "today": "Today"
    },
    "timePicker": {
      "panelAria": "Time picker",
      "hoursAria": "Hours",
      "minutesAria": "Minutes"
    },
    "modal": {
      "closeForm": "Close form"
    }
  },
  "operationModes": {
    "pageDescription": "Configure modes with their own internal routines for a site or room.",
    "site": "Site",
    "internalRoutines": "Internal routines",
    "summary": {
      "context": "Context",
      "availableDevices": "{{count}} available device(s).",
      "saved": "Saved",
      "hasActiveMode": "There is an active mode.",
      "noActiveMode": "No active mode.",
      "current": "Current",
      "selectMode": "Select a mode."
    },
    "toolbar": {
      "eyebrow": "Operational configuration",
      "title": "Modes with their own routines",
      "description": "A mode routine lives inside the mode and applies only when that mode is activated."
    },
    "actions": {
      "create": "Create mode",
      "reapply": "Reapply",
      "archive": "Archive",
      "add": "Add",
      "remove": "Remove"
    },
    "empty": {
      "title": "No modes in this site",
      "description": "Create a mode with internal routines to apply a real operational configuration."
    },
    "impact": {
      "title": "Impact",
      "applicationTitle": "Mode application",
      "affectedDevices": "{{count}} device(s)",
      "ignored": "Ignored",
      "emptyTitle": "Select a mode",
      "emptyDescription": "Review which devices and internal routines it will apply before activating."
    },
    "form": {
      "newMode": "New mode",
      "description": "Define identity, application, schedule, and own internal routines.",
      "identityTitle": "Identity",
      "identityDescription": "Controlled name and description for the mode.",
      "namePlaceholder": "E.g. Safe night",
      "descriptionPlaceholder": "Operational use of the mode",
      "applicationTitle": "Application",
      "applicationDescription": "The mode can cover a complete site or a room.",
      "selectSite": "Select site",
      "selectRoom": "Select room",
      "scheduleType": "Schedule type",
      "end": "End",
      "modeRoutinesTitle": "Mode routines",
      "modeRoutinesDescription": "These routines belong only to this mode.",
      "label": "Label",
      "routineNamePlaceholder": "E.g. Turn off lights",
      "selectTarget": "Select target",
      "noTargetsInScope": "No targets are available in this scope.",
      "noInternalRoutinesTitle": "No internal routines yet",
      "noInternalRoutinesDescription": "Add at least one internal routine to save the mode."
    },
    "applicationOptions": {
      "workplace": "Complete site",
      "room": "Specific room"
    },
    "applicationDescriptions": {
      "workplace": "The mode applies to the whole active site",
      "room": "The mode applies only to one room"
    },
    "scheduleOptions": {
      "allDay": "All day",
      "custom": "Custom"
    },
    "scheduleDescriptions": {
      "allDay": "The mode keeps its configuration without a time window",
      "custom": "Define start and end"
    },
    "routineTargetTypes": {
      "device": "Device",
      "group": "Group"
    },
    "routineTargetDescriptions": {
      "device": "Action over one device",
      "group": "Action over one group"
    },
    "routineActionDescriptions": {
      "turnOn": "Enable target",
      "turnOff": "Disable target"
    },
    "routineTargets": {
      "deviceCount": "{{count}} device(s)"
    },
    "goals": {
      "description": "Reference real goals without promising extra control.",
      "emptyTitle": "No applicable goals",
      "emptyDescription": "The mode can be saved without associated goals.",
      "fallback": "Goal {{id}}"
    },
    "alertsRules": {
      "title": "Alerts/Rules",
      "description": "Use existing rules and preferences."
    },
    "rules": {
      "available": "Available rules",
      "none": "No rules",
      "noneDescription": "The mode will not apply rules"
    },
    "preferences": {
      "current": "Current preference",
      "useRegistered": "Use registered preference",
      "noneRegistered": "No registered preference",
      "numbered": "Preference {{id}}",
      "applyThis": "Apply this preference"
    },
    "toggles": {
      "applyRulesTitle": "Apply rules",
      "applyRulesDescription": "Use existing rules.",
      "applyPreferencesTitle": "Apply preferences",
      "applyPreferencesDescription": "Use existing preferences.",
      "criticalSoundTitle": "Critical sound",
      "criticalSoundDescription": "Respect critical alerts when applicable."
    },
    "validation": {
      "siteRequired": "Select a site before creating the mode.",
      "nameRequired": "Enter a name for the mode.",
      "roomRequired": "Select a room for the mode.",
      "noTargets": "No devices or groups are available in the selected scope.",
      "routineRequired": "Add an internal routine or complete the routine being edited.",
      "routineDraftRequired": "Complete the mode routine name and target.",
      "minimumRequired": "Define site, name, and at least one internal routine."
    },
    "confirm": {
      "replaceTitle": "Replace active mode",
      "replaceMessage": "There is already an active mode in this site. If you activate this mode, the previous one will be disabled.",
      "archiveTitle": "Archive mode",
      "archiveMessage": "{{name}} will be archived. Its internal routines will stop acting."
    },
    "toasts": {
      "createError": "Could not create the mode.",
      "createSuccess": "Mode created with internal routines.",
      "activateError": "Could not activate the mode.",
      "archived": "Mode archived."
    },
    "status": {
      "active": "Active",
      "inactive": "Inactive",
      "archived": "Archived"
    },
    "fallbacks": {
      "noSelectedSite": "No site selected",
      "siteUnavailable": "Site unavailable",
      "operationalSite": "Operational site",
      "roomUnavailable": "Room unavailable",
      "groupUnavailable": "Group unavailable",
      "deviceUnavailable": "Device unavailable",
      "noDescription": "No description registered."
    }
  },
  "settings": {
    "shell": {
      "ariaLabel": "Account settings",
      "eyebrow": "Account",
      "description": "Real user and platform preferences."
    },
    "nav": {
      "profile": "Profile",
      "profileDescription": "Personal data",
      "account": "Account",
      "accountDescription": "Identity and status",
      "security": "Security",
      "securityDescription": "Access and recovery",
      "billing": "Billing",
      "billingDescription": "Plan, payments and invoices",
      "platform": "Platform",
      "platformDescription": "Local preferences"
    },
    "common": {
      "email": "Email",
      "notAvailable": "Not available"
    },
    "profile": {
      "title": "Personal data",
      "pageDescription": "Manage the name and email saved in your ElectroCorp account.",
      "basicInfoTitle": "Basic information",
      "basicInfoDescription": "This data is saved in your ElectroCorp account.",
      "saveProfile": "Save profile",
      "previewEyebrow": "Preview",
      "dataUsageTitle": "How this data is used",
      "dataUsageDescription": "Quick reference for how name and email appear inside ElectroCorp.",
      "visibleName": "Visible name",
      "visibleNameDescription": "Used to identify your account in the side panel and user views.",
      "accountEmail": "Account email",
      "accountEmailDescription": "Used as the reference for access recovery and main profile data.",
      "unsavedChanges": "Unsaved changes",
      "noPendingChanges": "No pending changes",
      "statusDescription": "Save the profile to sync data with the current session.",
      "updateSuccess": "Profile updated.",
      "updateError": "Could not update the profile."
    },
    "account": {
      "title": "Account status",
      "pageDescription": "Review identity, role and general status for the authenticated user.",
      "identityDescription": "Compact summary of the authenticated user.",
      "role": "Role",
      "userFallback": "User",
      "createdAt": "Created",
      "contextEyebrow": "Context",
      "operationalReadingTitle": "Operational reading",
      "operationalReadingDescription": "Elements that help understand account scope without mixing sensitive actions.",
      "session": "Session",
      "active": "Active",
      "sessionDescription": "This view is shown only for a user authenticated by the access guard.",
      "editableData": "Editable data",
      "separatedInProfile": "Separated in Profile",
      "editableDataDescription": "Name and email are updated from Profile to keep this page as an account summary.",
      "sensitiveActions": "Sensitive actions",
      "separatedInSecurity": "Separated in Security",
      "sensitiveActionsDescription": "Recovery and account deletion live on a separate screen to reduce mistakes."
    },
    "security": {
      "title": "Access and recovery",
      "pageDescription": "Manage sensitive access actions with available real calls.",
      "accessActionsTitle": "Access actions",
      "accessActionsDescription": "Only actions with an available real call are shown.",
      "recovery": "Recovery",
      "sendRecoveryTitle": "Send link to current email",
      "sendRecoveryDescription": "Use the real access recovery flow for the email saved in the profile.",
      "sendRecoveryButton": "Send recovery",
      "dangerZone": "Delicate zone",
      "deleteAccountDescription": "Opens a strict confirmation before deleting the current account.",
      "protectionsEyebrow": "Protections",
      "securityStatusTitle": "Security status",
      "securityStatusDescription": "Summary of active controls before executing sensitive actions.",
      "recoveryEmail": "Recovery email",
      "recoveryEmailDescription": "The recovery link is requested for the current email of the authenticated account.",
      "writtenConfirmation": "Written confirmation",
      "writtenConfirmationDescription": "Deletion remains blocked until the confirmation text matches exactly.",
      "finalConfirmation": "Final confirmation",
      "dialogRequired": "Dialog required",
      "finalConfirmationDescription": "The account is deleted only after accepting the danger confirmation dialog.",
      "noRecoveryEmail": "No email is registered for access recovery.",
      "recoverySent": "Recovery request sent.",
      "recoveryError": "Could not send recovery.",
      "deleteDialogTitle": "Permanently delete your account",
      "deleteDialogMessage": "This action deletes your ElectroCorp account, closes your session and cannot be undone from the interface.",
      "deleteDialogDetail": "Before continuing, verify that you do not need access to data, invoices or preferences associated with this account.",
      "deleteDialogConfirm": "Permanently delete account",
      "deleteDialogCancel": "Do not delete",
      "requiredTextLabel": "To confirm, type exactly this word:",
      "requiredTextHint": "The delete button will remain blocked until the confirmation matches exactly.",
      "deleteAccountError": "Could not delete the account."
    },
    "platform": {
      "title": "Local preferences",
      "pageDescription": "Adjust ElectroCorp appearance and language in this browser.",
      "browserExperienceTitle": "Browser experience",
      "browserExperienceDescription": "Applied in this browser and kept locally.",
      "darkMode": "Dark mode",
      "lightMode": "Light mode",
      "darkModeDescription": "Change the interface appearance in this browser.",
      "language": "Language",
      "languages": {
        "es": "Spanish",
        "esDescription": "Local interface language.",
        "en": "English",
        "enDescription": "Local interface language.",
        "pt": "Portuguese",
        "ptDescription": "Local interface language."
      },
      "languageApplied": "Language applied in this browser.",
      "appearanceApplied": "Appearance applied in this browser.",
      "localStatusEyebrow": "Local status",
      "preferencesSummaryTitle": "Preferences summary",
      "preferencesSummaryDescription": "Confirm how the experience is configured in this browser.",
      "appearance": "Appearance",
      "appearanceDescription": "The visual preference applies to the shell and shared interface components.",
      "activeLanguage": "Active language",
      "activeLanguageDescription": "The selected language is kept locally to maintain consistent navigation.",
      "scope": "Scope",
      "local": "Local",
      "scopeDescription": "These settings live in the current browser and are ready to sync with user preferences."
    },
    "billing": {
      "title": "Plan and payments",
      "pageDescription": "Manage your active subscription, review registered payments and download invoices issued for your account.",
      "subscriptionEyebrow": "Subscription",
      "planStatusTitle": "Plan status",
      "planStatusDescription": "Real information for the active plan, operational limits and payment activity.",
      "perMonth": "per month",
      "choosePlanDescription": "Choose a plan to activate the full platform.",
      "exports": "Exports",
      "activeExports": "Active",
      "notIncluded": "Not included",
      "viewPlans": "View plans",
      "lastPayment": "Last payment",
      "noPaymentsRegistered": "No registered payments.",
      "payments": "Payments",
      "registeredMovements": "Registered movements",
      "registeredMovementsDescription": "Payments received from the real subscription flow.",
      "paymentNumber": "Payment #{{id}}",
      "method": "Method",
      "noPaymentsTitle": "No payments",
      "noPaymentsDescription": "Payments will appear when you activate or renew a plan.",
      "issuedDocuments": "Issued documents",
      "issuedDocumentsDescription": "Download only invoices that exist in your account.",
      "invoiceNumber": "Invoice #{{id}}",
      "issuedOn": "Issued on {{date}}",
      "download": "Download",
      "noInvoicesTitle": "No invoices",
      "noInvoicesDescription": "When an invoice is issued for your account, it will appear here.",
      "loadError": "Could not load billing history.",
      "invoiceNotFound": "Invoice not found.",
      "downloadSuccess": "Invoice downloaded successfully.",
      "downloadError": "Could not download the invoice.",
      "cancelPlanTitle": "Cancel plan",
      "cancelPlanMessage": "Your active subscription will be canceled. Confirm only if you no longer want to continue with the plan.",
      "cancelPlanConfirm": "Cancel plan",
      "keepPlan": "Keep plan",
      "cancelSuccess": "Plan canceled.",
      "cancelError": "Could not cancel the plan.",
      "unlimited": "Unlimited",
      "noActivePlan": "No active plan",
      "subscriptionStatuses": {
        "active": "Active",
        "cancelled": "Canceled",
        "expired": "Expired"
      },
      "paymentStatuses": {
        "approved": "Approved",
        "rejected": "Rejected",
        "pending": "Pending"
      },
      "invoiceIssued": "Issued",
      "invoiceRegistered": "Registered",
      "goToPlans": "Go to plans"
    }
  }
}


============================================================
FILE: public\assets\i18n\es.json
============================================================

{
  "app": {
    "name": "ElectroCorp"
  },
  "nav": {
    "home": "Inicio",
    "about": "Acerca de",
    "userSettings": "Ajustes",
    "devices": "Dispositivos",
    "routines": "Rutinas",
    "energy": "Energía",
    "history": "Historial",
    "plans": "Planes",
    "alerts": "Alertas",
    "billing": "Facturación",
    "deviceGroups": "Grupos",
    "alertRules": "Reglas de alerta",
    "notificationPreferences": "Preferencias de notificación",
    "locations": "Sedes",
    "rooms": "Habitaciones",
    "assignments": "Asignaciones",
    "reports": "Reportes",
    "energyGoals": "Metas energéticas",
    "supportTickets": "Soporte",
    "maintenanceTickets": "Mantenimiento",
    "operation": "Operación",
    "energySection": "Energía",
    "alertsSection": "Alertas",
    "workplace": "Espacios",
    "spaces": "Espacios",
    "support": "Soporte",
    "settings": "Settings",
    "operationModes": "Modos",
    "consumption": "Consumo",
    "inbox": "Bandeja",
    "rules": "Reglas",
    "preferences": "Preferencias"
  },
  "onboarding": {
    "locationRequiredEyebrow": "Sede requerida",
    "locationRequiredTitle": "Crea una sede para empezar a usar esta funcion.",
    "locationRequiredDescription": "Aun no tienes una sede configurada. Crea tu primera sede para organizar habitaciones, dispositivos, rutinas, metas, alertas y reportes.",
    "createLocationAction": "Crear sede"
  },
  "auth": {
    "loginEyebrow": "Bienvenido de nuevo",
    "loginTitle": "Iniciar sesión",
    "loginSubtitle": "Accede a tu panel de ElectroCorp para gestionar dispositivos, rutinas y consumo de energía.",
    "registerEyebrow": "Crear acceso",
    "registerTitle": "Registrarse",
    "registerSubtitle": "Crea tu cuenta de ElectroCorp para empezar a gestionar dispositivos inteligentes de energía.",
    "email": "Correo electrónico",
    "password": "Contraseña",
    "fullName": "Nombre completo",
    "signIn": "Ingresar",
    "signUp": "Crear cuenta",
    "createAccount": "Crear cuenta",
    "backToLogin": "Volver al login",
    "logout": "Salir",
    "signInError": "No se pudo iniciar sesión. Verifica tu correo y contraseña.",
    "signUpError": "No se pudo registrar el usuario.",
    "emailAlreadyExists": "Este correo ya está registrado.",
    "fullNameRequired": "Ingresa tu nombre completo.",
    "invalidEmail": "Ingresa un correo electrónico válido.",
    "invalidEmailProvider": "Solo se permiten correos Gmail, Outlook o Hotmail.",
    "passwordTooShort": "La contraseña debe tener al menos 8 caracteres.",
    "forgotPassword": "¿Olvidaste tu contraseña?",
    "recoverPasswordEyebrow": "Recuperación de cuenta",
    "recoverPasswordTitle": "Recuperar contraseña",
    "recoverPasswordSubtitle": "Ingresa tu correo y te enviaremos instrucciones de recuperación.",
    "sendRecoveryEmail": "Enviar correo de recuperación",
    "recoverPasswordSent": "Correo de recuperación enviado correctamente.",
    "recoverPasswordError": "No se pudo enviar el correo de recuperación.",
    "profileUpdated": "Perfil actualizado correctamente.",
    "profileUpdateError": "No se pudo actualizar el perfil.",
    "deleteAccountError": "No se pudo eliminar la cuenta.",
    "deleteAccountConfirmationError": "El texto de confirmación no es válido."
  },
  "home": {
    "eyebrow": "Energía inteligente",
    "title": "Controla tu energía con ElectroCorp",
    "content": "Monitorea dispositivos, automatiza rutinas y revisa el consumo energético desde una plataforma moderna.",
    "manageDevices": "Gestionar dispositivos",
    "viewEnergy": "Ver energía",
    "cardDevices": "Dispositivos",
    "cardRoutines": "Rutinas",
    "cardConsumption": "Consumo actual",
    "cardAlerts": "Alertas",
    "activeDevices": "dispositivos activos",
    "enabledRoutines": "rutinas activas",
    "averageConsumption": "Promedio",
    "unreadAlerts": "alertas no leídas",
    "energyOverview": "Resumen energético",
    "subscriptionOverview": "Resumen de suscripción",
    "alertOverview": "Resumen de alertas",
    "openEnergyDashboard": "Abrir panel energético",
    "openPlans": "Ver planes",
    "openAlerts": "Ver alertas",
    "noPlanDescription": "Selecciona un plan para activar los beneficios de ElectroCorp.",
    "alertDescription": "Revisa alertas de consumo, automatización y eventos importantes del sistema.",
    "setupHint": "Selecciona o crea una sede para enfocar Inicio.",
    "setupEmpty": "Inicio se activa cuando existe una sede.",
    "operationalCenterTitle": "Centro operativo",
    "operationalCenterDescription": "Vista resumida de la sede activa, sus espacios, operación, consumo, alertas y servicio.",
    "loadingEyebrow": "Cargando",
    "loadingTitle": "Sincronizando información operativa",
    "loadingDescription": "Estamos leyendo sedes, habitaciones, dispositivos, energía, metas, alertas y servicio.",
    "mainSummaryAria": "Resumen principal",
    "activeSiteEyebrow": "Sede activa",
    "noActiveSite": "Sin sede activa",
    "noRegisteredLocation": "Sin ubicación registrada",
    "go": "Ir",
    "noSitesTitle": "No hay sedes configuradas",
    "noSitesDescription": "Crea una sede para organizar habitaciones, dispositivos y consumo por contexto.",
    "consumption": "Consumo",
    "generalStatusEyebrow": "Estado general",
    "siteSummaryTitle": "Resumen de la sede",
    "noOperationalContextTitle": "Sin contexto operativo",
    "siteSummaryDescription": "Datos calculados desde los elementos asignados a la sede activa.",
    "missingSiteTitle": "Falta una sede",
    "missingSiteDescription": "El resumen operativo necesita una sede activa.",
    "criticalAlerts": "Alertas críticas",
    "openSupport": "Soporte abierto",
    "siteRoomsTitle": "Ambientes de la sede",
    "roomsRegisteredDescription": "{{count}} habitación(es) registradas en el contexto activo.",
    "noActiveSiteRoomsDescription": "Crea o selecciona una sede para ver habitaciones.",
    "noRoomsTitle": "No hay habitaciones",
    "noRoomsDescription": "Crea habitaciones para ubicar dispositivos por zonas reales.",
    "withDevices": "Con equipos",
    "withoutDevices": "Sin equipos",
    "averageDevicesShort": "Prom. disp.",
    "assignedEquipmentTitle": "Equipos asignados",
    "assignedEquipmentDescription": "Total, activos y consumo salen de los dispositivos vinculados a la sede.",
    "noAssignedDevicesTitle": "No hay dispositivos asignados",
    "noAssignedDevicesDescription": "Registra o asigna equipos para alimentar consumo, grupos y rutinas.",
    "groupSetsTitle": "Conjuntos",
    "groupsDescription": "Dispositivos agrupados por uso o zona.",
    "noGroupsTitle": "Sin grupos",
    "noGroupsDescription": "Crea grupos para operar varios dispositivos juntos.",
    "deviceCountShort": "{{count}} disp.",
    "moreGroups": "+{{count}} más en grupos",
    "scheduledAlarmsTitle": "Alarmas programadas",
    "scheduledAlarmsDescription": "Acciones globales habilitadas para la sede.",
    "noActiveRoutinesTitle": "Sin rutinas activas",
    "noActiveRoutinesDescription": "Programa acciones por hora para dispositivos, grupos o habitaciones.",
    "moreRoutines": "+{{count}} más en rutinas",
    "activeModeTitle": "Modo activo",
    "activeModeDescription": "Configuración operacional propia de la sede.",
    "affectedDevices": "{{count}} dispositivo(s) afectados",
    "noActiveModeTitle": "Sin modo activo",
    "noActiveModeDescription": "Los modos activos de la sede aparecerán aquí.",
    "createMode": "Crear modo",
    "energyRecordedTitle": "Energía registrada",
    "energyRecordedDescription": "Lecturas reales asociadas a dispositivos asignados.",
    "noReadingsTitle": "Sin lecturas",
    "noReadingsDescription": "Cuando haya lecturas de energía, el resumen aparecerá aquí.",
    "today": "Hoy",
    "readings": "Lecturas",
    "peak": "Pico",
    "goalsEyebrow": "Metas",
    "activeGoalsTitle": "Objetivos activos",
    "activeGoalsDescription": "Metas vigentes filtradas por sede, habitación, grupo o dispositivo.",
    "noActiveGoalsTitle": "Sin metas activas",
    "noActiveGoalsDescription": "Crea metas para comparar consumo contra objetivos reales.",
    "recentInboxTitle": "Bandeja reciente",
    "recentInboxDescription": "Alertas relacionadas con la sede y sus elementos.",
    "noRecentAlertsTitle": "Sin alertas recientes",
    "noRecentAlertsDescription": "No hay alertas para mostrar en esta sede.",
    "criticalCount": "Críticas: {{count}}",
    "accountSupportTitle": "Soporte de la cuenta",
    "accountSupportDescription": "Tickets abiertos y mantenimientos relacionados con dispositivos de la sede.",
    "noOpenTicketsTitle": "Sin tickets abiertos",
    "noOpenTicketsDescription": "Los casos de soporte o mantenimiento aparecerán aquí cuando existan.",
    "createSupport": "Crear soporte",
    "noRoom": "Sin habitación",
    "roomNumber": "Habitación #{{id}}",
    "deviceStatusOn": "ON",
    "deviceStatusOff": "OFF",
    "locationTypes": {
      "home": "Casa",
      "business": "Empresa",
      "branch": "Local"
    },
    "alertLevels": {
      "success": "Éxito",
      "stable": "Estable",
      "info": "Neutral",
      "warning": "Atención",
      "critical": "Crítica"
    }
  },
  "about": {
    "eyebrow": "ElectroCorp en contexto",
    "title": "Una plataforma para ordenar energia, espacios y decisiones",
    "subtitle": "ElectroCorp ayuda a usar la energia con mas claridad: organiza sedes y habitaciones, controla dispositivos, automatiza rutinas y modos, revisa consumo, metas, alertas, servicio, facturacion y preferencias desde un mismo lugar.",
    "actions": {
      "home": "Ir a Inicio",
      "spaces": "Ir a Espacios",
      "energy": "Ir a Energia"
    },
    "heroSignals": {
      "order": {
        "label": "Orden",
        "title": "Espacios antes que acciones",
        "description": "La plataforma funciona mejor cuando primero defines sedes, habitaciones y asignaciones reales."
      },
      "control": {
        "label": "Control",
        "title": "Dispositivos con contexto",
        "description": "Los dispositivos, grupos, rutinas y modos se entienden por el lugar y el uso diario que representan."
      },
      "clarity": {
        "label": "Claridad",
        "title": "Energia visible",
        "description": "El consumo, los reportes y las metas dependen de lecturas reales para orientar mejores decisiones."
      }
    },
    "what": {
      "eyebrow": "Que es",
      "title": "ElectroCorp conecta espacios, dispositivos y energia",
      "description": "No es una pantalla decorativa ni una lista aislada de acciones. Es una plataforma para administrar espacios, controlar equipos, crear automatizaciones utiles, revisar consumo, recibir alertas y dar seguimiento a soporte, mantenimiento, facturacion y preferencias.",
      "points": {
        "spaces": "Sedes, habitaciones y asignaciones como base del sistema.",
        "control": "Dispositivos, grupos, rutinas y modos para operar el dia a dia.",
        "energy": "Consumo, historial, reportes y metas basados en informacion disponible.",
        "care": "Alertas, reglas, servicio y preferencias para actuar con orden."
      }
    },
    "values": {
      "mission": {
        "eyebrow": "Mision",
        "title": "Ayudar a usar energia con orden, control y claridad",
        "description": "ElectroCorp esta pensado para hogares, sedes pequenas y espacios medianos que necesitan saber donde estan sus equipos, como se usan y que decisiones tomar para consumir mejor."
      },
      "vision": {
        "eyebrow": "Vision",
        "title": "Una plataforma energetica inteligente, ordenada y preventiva",
        "description": "La vision es crecer como una herramienta que conecte operacion, consumo, alertas y servicio de forma cada vez mas precisa, sin perder transparencia sobre lo que realmente esta disponible."
      }
    },
    "daily": {
      "eyebrow": "Uso diario",
      "title": "Un flujo practico para operar ElectroCorp",
      "description": "El uso recomendado empieza por el espacio fisico y avanza hacia control, automatizacion, revision energetica y seguimiento.",
      "items": {
        "site": {
          "title": "Elige o crea una sede activa",
          "description": "Trabaja desde una sede clara para que habitaciones, dispositivos y acciones tengan contexto."
        },
        "rooms": {
          "title": "Organiza habitaciones",
          "description": "Usa habitaciones reales para ubicar equipos y mantener el mapa operativo entendible."
        },
        "devices": {
          "title": "Revisa dispositivos",
          "description": "Enciende, apaga y valida el estado de los equipos desde vistas de operacion."
        },
        "groups": {
          "title": "Agrupa por uso",
          "description": "Crea grupos semanticos como iluminacion, climatizacion o equipos criticos para actuar por conjunto."
        },
        "routines": {
          "title": "Programa rutinas repetibles",
          "description": "Usa rutinas para horarios frecuentes y evita duplicar acciones manuales cada dia."
        },
        "mode": {
          "title": "Aplica modos completos",
          "description": "Los modos ayudan a activar situaciones completas, como ahorro, noche o trabajo, con rutinas internas."
        },
        "energy": {
          "title": "Revisa consumo",
          "description": "Consulta el dashboard y el historial para entender el comportamiento energetico disponible."
        },
        "goals": {
          "title": "Compara metas",
          "description": "Usa metas energeticas para medir avance contra objetivos definidos por alcance."
        },
        "alerts": {
          "title": "Atiende alertas",
          "description": "Lee alertas visibles y usa reglas claras para prevenir problemas o consumos fuera de rango."
        },
        "service": {
          "title": "Da seguimiento a servicio",
          "description": "Registra soporte o mantenimiento cuando necesitas dejar constancia y avanzar por estado."
        },
        "billing": {
          "title": "Revisa facturacion y preferencias",
          "description": "Consulta plan, pagos, perfil y preferencias disponibles sin asumir funciones externas no visibles."
        }
      }
    },
    "recommendations": {
      "eyebrow": "Recomendaciones reales",
      "title": "Como sacarle valor sin sobreconfigurar",
      "description": "ElectroCorp funciona mejor cuando los nombres, alcances y reglas reflejan la operacion real.",
      "items": {
        "site": {
          "title": "Crea primero la sede",
          "description": "La sede activa evita que los demas datos queden sueltos o dificiles de leer."
        },
        "rooms": {
          "title": "Define habitaciones antes de operar",
          "description": "Una habitacion bien nombrada ayuda a encontrar dispositivos, rutinas y mantenimiento."
        },
        "assignments": {
          "title": "Asigna dispositivos con cuidado",
          "description": "La asignacion correcta mejora el control y evita confusiones cuando hay muchos equipos."
        },
        "groups": {
          "title": "Usa grupos semanticos",
          "description": "Agrupa por funcion real, no solo por comodidad momentanea."
        },
        "routines": {
          "title": "Rutinas para horarios repetidos",
          "description": "Si una accion ocurre con frecuencia, una rutina suele ser mejor que repetirla manualmente."
        },
        "modes": {
          "title": "Modos para situaciones completas",
          "description": "Un modo debe representar una escena operativa completa, no una accion suelta."
        },
        "energy": {
          "title": "Revisa energia y metas juntas",
          "description": "El consumo explica el presente; las metas ayudan a decidir que cambiar."
        },
        "alerts": {
          "title": "Usa alertas para prevenir",
          "description": "Las alertas son mas utiles cuando se revisan a tiempo y se relacionan con una accion clara."
        },
        "rules": {
          "title": "No exageres las reglas",
          "description": "Pocas reglas bien definidas suelen ser mas utiles que muchas reglas dificiles de mantener."
        },
        "names": {
          "title": "Nombres claros",
          "description": "Evita nombres ambiguos. Un buen nombre reduce errores en operacion y seguimiento."
        }
      }
    },
    "capabilities": {
      "eyebrow": "Lo que ya hace bien",
      "title": "Capacidades conectadas en la experiencia",
      "description": "Estas areas ya existen en la plataforma y se presentan como funciones disponibles, no como promesas futuras.",
      "items": {
        "spaces": {
          "eyebrow": "Espacios",
          "title": "Sedes, habitaciones y asignaciones",
          "description": "Permite ordenar la base fisica sobre la que se controla la operacion."
        },
        "control": {
          "eyebrow": "Operacion",
          "title": "Dispositivos, grupos y rutinas",
          "description": "Permite operar equipos, agruparlos y programar acciones repetibles."
        },
        "modes": {
          "eyebrow": "Modos",
          "title": "Situaciones con rutinas internas",
          "description": "Permite organizar acciones completas para momentos concretos del dia."
        },
        "energy": {
          "eyebrow": "Energia",
          "title": "Consumo, historial, reportes y metas",
          "description": "Permite leer informacion energetica disponible y contrastarla con objetivos."
        },
        "alerts": {
          "eyebrow": "Alertas",
          "title": "Bandeja, reglas y preferencias visibles",
          "description": "Permite revisar alertas y ajustar reglas o preferencias visibles de la experiencia."
        },
        "service": {
          "eyebrow": "Servicio",
          "title": "Soporte y mantenimiento",
          "description": "Permite crear solicitudes y avanzar su estado sin agregar acciones no disponibles."
        },
        "settings": {
          "eyebrow": "Cuenta",
          "title": "Perfil, facturacion y ajustes",
          "description": "Permite revisar informacion de usuario, plan, pagos y preferencias disponibles."
        }
      }
    },
    "careful": {
      "eyebrow": "Uso con criterio",
      "title": "Donde conviene ser transparente",
      "description": "Algunas partes dependen de datos reales, reglas claras o informacion disponible por plan. Por eso se muestran con alcance honesto.",
      "items": {
        "automation": {
          "title": "Automatizaciones",
          "description": "Deben crearse con horarios y alcances claros para evitar acciones inesperadas."
        },
        "alerts": {
          "title": "Alertas y reglas",
          "description": "Funcionan mejor con rangos comprensibles y pocas reglas faciles de revisar."
        },
        "reports": {
          "title": "Reportes",
          "description": "El valor de los reportes depende de lecturas reales y filtros bien elegidos."
        },
        "billing": {
          "title": "Facturacion",
          "description": "La informacion visible depende del plan, pagos e historial disponible dentro de la plataforma."
        }
      }
    }
  },
  "notFound": {
    "title": "Página no encontrada",
    "content": "La ruta ingresada no existe o fue movida.",
    "goHome": "Ir al inicio"
  },
  "footer": {
    "rights": "Todos los derechos reservados."
  },
  "devices": {
    "eyebrow": "Control de dispositivos",
    "title": "Control inteligente de dispositivos",
    "subtitle": "Gestiona enchufes, interruptores y luces inteligentes desde una vista centralizada.",
    "total": "Total",
    "active": "Activos",
    "currentWatts": "Consumo actual",
    "addTitle": "Agregar dispositivo",
    "name": "Nombre",
    "room": "Habitacion",
    "type": "Tipo",
    "power": "Potencia en watts",
    "create": "Crear dispositivo",
    "turnOn": "Encender",
    "turnOff": "Apagar",
    "emptyTitle": "Todavía no hay dispositivos",
    "emptyContent": "Agrega tu primer enchufe, interruptor o luz inteligente para comenzar.",
    "loadError": "No se pudieron cargar los dispositivos.",
    "createError": "No se pudo crear el dispositivo.",
    "updateError": "No se pudo actualizar el dispositivo.",
    "deleteError": "No se pudo eliminar el dispositivo.",
    "planLimitReached": "Has alcanzado el límite de dispositivos permitido por tu plan.",
    "createSuccess": "Dispositivo creado correctamente.",
    "pageDescription": "Controla los equipos asignados a la sede activa y registra nuevos dispositivos operativos por habitacion.",
    "activeSiteFallback": "sede activa",
    "noRoom": "Sin habitacion",
    "go": "Ir",
    "summary": {
      "assignedTitle": "Asignados",
      "onTitle": "Encendidos",
      "offDevices": "{{count}} apagado(s).",
      "consumptionEyebrow": "Consumo",
      "currentTitle": "Actual",
      "activePowerDescription": "Potencia activa de esta sede."
    },
    "toolbar": {
      "eyebrow": "Control diario",
      "title": "Equipos por habitacion",
      "description": "Enciende, apaga o elimina dispositivos con advertencias de impacto real."
    },
    "emptyRooms": {
      "title": "No hay habitaciones en la sede activa",
      "description": "Crea una habitacion antes de registrar dispositivos operativos."
    },
    "emptyAssigned": {
      "title": "No hay dispositivos asignados",
      "description": "Registra un dispositivo nuevo o revisa las asignaciones de la sede activa."
    },
    "form": {
      "eyebrow": "Nuevo dispositivo",
      "description": "Registra un equipo real, su habitacion y su potencia nominal.",
      "namePlaceholder": "Ej. Luz principal",
      "wattsLabel": "Watts del dispositivo"
    },
    "powerAdvice": {
      "highTitle": "Consumo alto",
      "normalTitle": "Monitoreo controlado",
      "highMessage": "Para {{type}}, este valor se considera elevado. Si el dispositivo reporta un uso igual o mayor a {{watts}} W, ElectroCorp debe marcarlo como consumo alto y podra generar una alerta.",
      "normalMessage": "Ingresa solo los watts nominales de {{type}}. ElectroCorp calcula kWh y costo con esos W y el tiempo real encendido."
    },
    "status": {
      "on": "Encendido",
      "off": "Apagado",
      "maintenance": "Mantenimiento",
      "removed": "Retirado",
      "unknown": "Sin estado"
    },
    "card": {
      "activeConsumption": "Consumo activo",
      "standby": "En espera",
      "currentConsumptionDescription": "Consumo actual del dispositivo",
      "noActiveConsumption": "Sin consumo activo"
    },
    "validation": {
      "nameRequired": "Escribe el nombre del dispositivo.",
      "nameMinLength": "El nombre del dispositivo debe tener al menos 3 caracteres.",
      "roomRequired": "Selecciona una habitacion de la sede activa.",
      "powerRequired": "La potencia del dispositivo debe ser mayor a 0 W.",
      "roomRequiredBeforePreset": "Crea una habitacion en la sede activa antes de agregar dispositivos."
    },
    "toasts": {
      "createdAndAssigned": "Dispositivo creado y asignado a {{room}}.",
      "createdWithoutAssignment": "Dispositivo creado, pero no se pudo asignar a la habitacion activa.",
      "presetCreatedAndAssigned": "{{device}} agregado y asignado a {{room}}.",
      "presetCreatedWithoutAssignment": "{{device}} fue creado, pero no se pudo asignar a la habitacion activa.",
      "presetCreateError": "No se pudo agregar {{device}}.",
      "turnedOn": "Dispositivo encendido.",
      "turnedOff": "Dispositivo apagado.",
      "deletedHistoryKept": "Dispositivo eliminado; el historial se conserva.",
      "removedRoutineSingular": "Su rutina asociada tambien desaparecio.",
      "removedRoutinePlural": "{{count}} rutinas asociadas tambien desaparecieron."
    },
    "deleteFallbackDevice": "Este dispositivo",
    "deleteWarnings": {
      "groupsTitle": "Revisar grupos",
      "groupSingular": "{{device}} forma parte del grupo {{group}}. Si lo eliminas, ese grupo perdera este dispositivo.",
      "groupPlural": "{{device}} forma parte de {{count}} grupos. Si lo eliminas, esos grupos perderan este dispositivo.",
      "routinesTitle": "Revisar rutinas",
      "routineSingular": "{{device}} tiene una rutina asociada. Si eliminas {{device}}, su rutina desaparecera.",
      "routinePlural": "{{device}} tiene {{count}} rutinas asociadas. Si eliminas {{device}}, sus rutinas desapareceran.",
      "modesTitle": "Revisar modos",
      "modeSingular": "{{device}} esta relacionado con un modo existente. Eliminarlo puede afectar esa configuracion.",
      "modePlural": "{{device}} esta relacionado con {{count}} modos existentes. Eliminarlo puede afectar esas configuraciones.",
      "deleteTitle": "Eliminar dispositivo",
      "deleteMessage": "Se eliminara {{device}}. Esta accion no afecta datos historicos ya registrados."
    }
  },
  "routines": {
    "eyebrow": "Automatización",
    "title": "Rutinas automáticas",
    "subtitle": "Programa acciones para encender o apagar dispositivos inteligentes según horarios definidos.",
    "total": "Total",
    "active": "Activas",
    "inactive": "Inactiva",
    "addTitle": "Crear rutina",
    "name": "Nombre",
    "device": "Dispositivo",
    "selectDevice": "Selecciona un dispositivo",
    "action": "Acción",
    "time": "Horario",
    "create": "Crear rutina",
    "enable": "Activar",
    "disable": "Desactivar",
    "emptyTitle": "Todavía no hay rutinas",
    "emptyContent": "Crea una rutina para automatizar tus dispositivos.",
    "loadError": "No se pudieron cargar las rutinas.",
    "createError": "No se pudo crear la rutina.",
    "updateError": "No se pudo actualizar la rutina.",
    "deleteError": "No se pudo eliminar la rutina.",
    "conflictError": "Ya existe una rutina activa para ese dispositivo en el mismo horario.",
    "planLimitReached": "Has alcanzado el límite de rutinas permitido por tu plan.",
    "createSuccess": "Rutina creada correctamente.",
    "pageDescription": "Programa alarmas operativas para dispositivos, grupos, habitaciones o sedes.",
    "createOperationalSuccess": "Rutina creada como alarma operativa.",
    "summary": {
      "scheduled": "Programadas",
      "inActiveSite": "en la sede activa.",
      "pausedCount": "{{count}} pausada(s).",
      "newRoutine": "Nueva rutina"
    },
    "toolbar": {
      "eyebrow": "Alarmas programadas",
      "title": "Rutinas globales",
      "description": "Estas rutinas son independientes de las rutinas internas de modos."
    },
    "emptyScheduledTitle": "No hay rutinas programadas",
    "emptyScheduledDescription": "Crea una alarma operativa para encender o apagar objetivos en un horario definido.",
    "targetTypes": {
      "device": "Dispositivo",
      "group": "Grupo",
      "room": "Habitacion",
      "workplace": "Sede"
    },
    "targetTypeDescriptions": {
      "device": "Rutina para un solo equipo",
      "group": "Rutina para varios dispositivos",
      "room": "Rutina para los equipos de una habitacion",
      "workplace": "Rutina para un centro energetico completo"
    },
    "targetOptions": {
      "deviceCount": "{{count}} dispositivos"
    },
    "actions": {
      "turnOn": "Encender",
      "turnOff": "Apagar"
    },
    "actionDescriptions": {
      "turnOn": "Activar automaticamente",
      "turnOff": "Desactivar automaticamente"
    },
    "repeat": {
      "once": "Una vez",
      "daily": "Diario",
      "weekly": "Semanal",
      "customInterval": "Cada X dias"
    },
    "repeatDescriptions": {
      "once": "Activar solo en la fecha elegida",
      "daily": "Repetir todos los dias",
      "weekly": "Elegir dias especificos",
      "customInterval": "Repetir por intervalo"
    },
    "weekDays": {
      "mon": "Lunes",
      "tue": "Martes",
      "wed": "Miercoles",
      "thu": "Jueves",
      "fri": "Viernes",
      "sat": "Sabado",
      "sun": "Domingo",
      "monShort": "Lun",
      "tueShort": "Mar",
      "wedShort": "Mie",
      "thuShort": "Jue",
      "friShort": "Vie",
      "satShort": "Sab",
      "sunShort": "Dom"
    },
    "placeholders": {
      "device": "Selecciona un dispositivo",
      "group": "Selecciona un grupo",
      "room": "Selecciona una habitacion",
      "workplace": "Selecciona una sede"
    },
    "form": {
      "description": "Define objetivo, accion, horario y repeticion como una alarma global.",
      "namePlaceholder": "Ej. Apagar luces de noche",
      "target": "Objetivo",
      "targetTypePlaceholder": "Tipo de objetivo",
      "repetition": "Repeticion",
      "scheduledTime": "Hora programada",
      "days": "Dias",
      "weeklyDaysAria": "Dias de repeticion semanal",
      "everyDays": "Cada cuantos dias",
      "daysSuffix": "dias"
    },
    "status": {
      "active": "Activa",
      "inactive": "Inactiva",
      "paused": "Pausada"
    },
    "card": {
      "time": "Hora",
      "detailsAria": "Detalles de la rutina",
      "applicable": "Aplicables",
      "blocked": "Bloqueados",
      "onceWithDate": "Una vez - {{date}}",
      "everyDays": "Cada {{count}} dia(s)",
      "weeklyWithDays": "Semanal - {{days}}",
      "weeklyGeneric": "semanalmente",
      "weeklyDaysText": "los dias {{days}}",
      "waitingOnceDate": "Programada para el {{date}} a las {{time}}.",
      "waitingOnce": "Programada para una ejecucion unica a las {{time}}.",
      "waitingWeekly": "Se activara {{days}} a las {{time}}.",
      "waitingInterval": "Se repetira cada {{count}} dia(s) a las {{time}}.",
      "waitingDaily": "Se repetira todos los dias a las {{time}}.",
      "pausedOnceDate": "Pausada. No se activara el {{date}}.",
      "pausedWeekly": "Pausada. Repeticion semanal suspendida.",
      "pausedInterval": "Pausada. Intervalo suspendido.",
      "pausedDaily": "Pausada. Repeticion diaria suspendida."
    },
    "validation": {
      "activeSiteRequired": "Selecciona una sede activa antes de crear rutinas.",
      "weeklyDayRequired": "Selecciona al menos un dia para la rutina semanal."
    },
    "conflict": {
      "title": "Revisar rutina",
      "message": "Ya existe una rutina activa con el mismo objetivo, horario y accion. Puedes continuar si buscas repetir esa programacion."
    },
    "delete": {
      "title": "Eliminar rutina",
      "message": "Se eliminara {{name}}. La programacion dejara de activarse.",
      "fallbackName": "esta rutina"
    },
    "toasts": {
      "activated": "Rutina activada.",
      "paused": "Rutina pausada.",
      "deleted": "Rutina eliminada."
    },
    "list": {
      "empty": "Todavia no tienes rutinas creadas.",
      "groupFallback": "Grupo #{{id}}",
      "deviceFallback": "Dispositivo #{{id}}"
    }
  },
  "energy": {
    "eyebrow": "Energy monitoring",
    "title": "Monitoreo de consumo energético",
    "subtitle": "Visualiza el consumo de tus dispositivos, filtra el historial y exporta tus lecturas para análisis.",
    "exportCsv": "Exportar CSV",
    "totalWatts": "Watts totales",
    "averageWatts": "Promedio",
    "highestReading": "Pico máximo",
    "startDate": "Fecha inicial",
    "endDate": "Fecha final",
    "filter": "Filtrar",
    "reset": "Restablecer",
    "recommendationTitle": "Recomendación de ahorro",
    "analytics": "Analítica",
    "chartTitle": "Historial de consumo",
    "emptyReadings": "No hay lecturas energéticas disponibles.",
    "loadError": "No se pudieron cargar las lecturas energéticas.",
    "noDataRecommendation": "Aún no hay datos suficientes para generar una recomendación.",
    "highRecommendation": "Tu consumo energético es alto. Revisa tus dispositivos de mayor consumo.",
    "moderateRecommendation": "Tu consumo energético es moderado. Revisa las horas pico para mejorar el ahorro.",
    "lowRecommendation": "Tu consumo energético se mantiene bajo. Continúa optimizando tus rutinas.",
    "exportNotAllowed": "Tu plan actual no permite exportar reportes CSV.",
    "highReadings": "Lecturas altas",
    "invalidDateRange": "El rango de fechas seleccionado no es válido.",
    "exportSuccess": "CSV exportado correctamente."
  },
  "history": {
    "eyebrow": "Historial energético",
    "title": "Historial de consumo",
    "subtitle": "Revisa las lecturas registradas, detecta picos de consumo y exporta la información para reportes.",
    "registeredReadings": "Lecturas registradas",
    "records": "registros",
    "device": "Dispositivo",
    "watts": "Watts",
    "date": "Fecha",
    "status": "Estado",
    "high": "Alto",
    "normal": "Normal",
    "filterTitle": "Filtrar lecturas",
    "filterDescription": "Selecciona un rango de fechas para revisar el consumo energético registrado.",
    "registeredReadingsDescription": "Historial de lecturas energéticas ordenadas por fecha de registro.",
    "advancedFiltersAria": "Filtros avanzados del historial",
    "scope": {
      "label": "Alcance",
      "active": "Sede activa",
      "activeDescription": "Solo lecturas de {{site}}",
      "activeFallbackDescription": "Usa la sede seleccionada en Inicio",
      "global": "Global",
      "globalDescription": "Todas las lecturas conservadas del usuario"
    },
    "scopeSummary": {
      "global": "Historial global de todas tus sedes.",
      "noActiveSite": "Selecciona una sede activa para ver sus lecturas.",
      "activeSite": "Historial aislado por sede activa: {{site}}."
    },
    "deviceNamePlaceholder": "Nombre del dispositivo",
    "deviceId": "ID dispositivo",
    "deviceIdPlaceholder": "Ej. 3",
    "readingId": "ID lectura",
    "readingIdPlaceholder": "Ej. 15",
    "minWatts": "Watts mín.",
    "maxWatts": "Watts máx.",
    "sortBy": "Ordenar por",
    "clearFilters": "Limpiar filtros",
    "emptyEyebrow": "Sin lecturas",
    "emptyTitle": "No se encontraron lecturas",
    "emptyDescription": "Ajusta los filtros para revisar otros registros de consumo energético.",
    "energy": "Energía",
    "estimatedCost": "Costo estimado",
    "time": "Hora",
    "interval": "Intervalo",
    "readingMeta": "ID #{{deviceId}} - Lectura #{{readingId}}",
    "showing": "Mostrando",
    "of": "de",
    "readings": "lecturas",
    "pageOf": "Página {{page}} de {{total}}",
    "previous": "Anterior",
    "next": "Siguiente",
    "startDate": "Fecha inicial",
    "endDate": "Fecha final",
    "reset": "Restablecer",
    "statusOptions": {
      "all": "Todos",
      "normal": "Normal",
      "high": "Alto"
    },
    "sortOptions": {
      "newest": "Más recientes",
      "oldest": "Más antiguas",
      "wattsDesc": "Mayor consumo",
      "wattsAsc": "Menor consumo",
      "costDesc": "Mayor costo"
    },
    "toasts": {
      "exportSuccess": "Historial exportado.",
      "exportError": "No se pudo exportar el historial."
    },
    "emptyValues": {
      "noDate": "Sin fecha",
      "noTime": "Sin hora",
      "undefined": "No definido"
    }
  },
  "billing": {
    "eyebrow": "Billing",
    "title": "Planes de servicio",
    "subtitle": "Elige el plan que mejor se adapta a tu hogar, negocio o múltiples locales. No existe plan gratuito; todos los planes están orientados a operación real.",
    "month": "mes",
    "subscribe": "Suscribirme",
    "selected": "Seleccionado",
    "currentPlan": "Plan actual",
    "activeSubscription": "Suscripción activa",
    "noPlan": "Sin plan",
    "cancel": "Cancelar plan",
    "loadError": "No se pudo cargar la información de planes.",
    "subscribeError": "No se pudo procesar la suscripción.",
    "cancelError": "No se pudo cancelar la suscripción.",
    "alreadySubscribed": "Ya estás suscrito a este plan.",
    "paymentEyebrow": "Confirmación de pago",
    "paymentTitle": "Confirmar método de pago",
    "selectedPlan": "Plan seleccionado",
    "amountToPay": "Monto a pagar",
    "holderName": "Nombre del titular",
    "cardNumber": "Número de tarjeta",
    "expirationDate": "Fecha de vencimiento",
    "cvv": "CVV",
    "confirmPayment": "Confirmar pago",
    "cancelPayment": "Cancelar",
    "invalidCardNumber": "El número de tarjeta no es válido.",
    "invalidCvv": "El CVV no es válido.",
    "invalidHolderName": "Ingresa el nombre del titular.",
    "lastPayment": "Último pago",
    "recommended": "RECOMENDADO",
    "choosePlan": "Elegir plan",
    "activePlanBadge": "Plan activo",
    "summaryStarterLabel": "Hogares pequeños",
    "summaryStarterText": "Ideal para comenzar con monitoreo básico y alertas simples.",
    "summaryProfessionalLabel": "Recomendado",
    "summaryProfessionalText": "La mejor opción para hogares avanzados y pequeños negocios.",
    "summaryEnterpriseLabel": "Múltiples locales",
    "summaryEnterpriseText": "Pensado para equipos, sedes y operación empresarial.",
    "maxDevices": "Hasta {{ count }} dispositivos",
    "maxRoutines": "Hasta {{ count }} rutinas",
    "reportExportEnabled": "Exportación de reportes",
    "planUsage": {
      "STARTER": "Para hogares pequeños",
      "PROFESSIONAL": "Para hogares avanzados y negocios pequeños",
      "ENTERPRISE": "Para múltiples locales comerciales"
    },
    "planDescriptions": {
      "STARTER": "Gestión básica de energía para hogares pequeños.",
      "PROFESSIONAL": "Control avanzado para hogares y pequeños negocios.",
      "ENTERPRISE": "Plataforma completa para múltiples locales o empresas."
    },
    "planFeatures": {
      "STARTER": {
        "devices": "Hasta 5 dispositivos inteligentes",
        "dashboard": "Panel energético básico",
        "alerts": "Alertas por correo"
      },
      "PROFESSIONAL": {
        "devices": "Hasta 20 dispositivos inteligentes",
        "analytics": "Analítica energética avanzada",
        "routines": "Rutinas de automatización",
        "reports": "Reportes mensuales"
      },
      "ENTERPRISE": {
        "locations": "Múltiples locales comerciales",
        "accessProfiles": "Perfiles de acceso para el equipo",
        "alerts": "Alertas avanzadas",
        "support": "Soporte prioritario"
      }
    },
    "historyEyebrow": "Historial de facturación",
    "historyTitle": "Historial de facturación",
    "historySubtitle": "Revisa tus pagos, facturas y estado actual de suscripción.",
    "invoices": "Facturas",
    "invoice": "Factura",
    "issuedAt": "Emitida el",
    "status": "Estado",
    "downloadInvoice": "Descargar factura",
    "emptyInvoicesTitle": "Todavía no hay facturas",
    "emptyInvoicesContent": "Tus facturas aparecerán aquí después de procesar tus pagos.",
    "subscribeSuccess": "Suscripción activada correctamente.",
    "cancelSuccess": "Suscripción cancelada correctamente.",
    "invalidExpirationDate": "La fecha de vencimiento no es válida.",
    "invoiceNotFound": "Factura no encontrada."
  },
  "alerts": {
    "eyebrow": "Centro de alertas",
    "title": "Alertas del sistema",
    "subtitle": "Visualiza eventos importantes, alertas de consumo y notificaciones operativas de ElectroCorp.",
    "total": "Total",
    "unreadCount": "No leídas",
    "createTitle": "Crear alerta",
    "alertTitle": "Título",
    "message": "Mensaje",
    "level": "Nivel",
    "create": "Crear alerta",
    "unread": "No leída",
    "markAsRead": "Marcar como leída",
    "emptyTitle": "No hay alertas",
    "emptyContent": "Cuando ocurra un evento importante, aparecerá en esta sección.",
    "loadError": "No se pudieron cargar las alertas.",
    "createError": "No se pudo crear la alerta.",
    "markAsReadError": "No se pudo marcar la alerta como leída.",
    "planLimitReached": "Has alcanzado el límite de alertas permitido por tu plan.",
    "criticalCount": "Críticas",
    "createSuccess": "Alerta creada correctamente.",
    "deleteError": "No se pudo eliminar la alerta.",
    "inboxSubtitle": "Revisa alertas activas, eventos importantes y notificaciones permitidas por tus preferencias.",
    "summaryAria": "Resumen de alertas visibles",
    "refresh": "Actualizar",
    "hiddenByPreferences": "{{count}} alertas quedan fuera de la bandeja por tus preferencias actuales.",
    "markAsReadSuccess": "Alerta marcada como leida.",
    "deleteSuccess": "Alerta eliminada.",
    "dismissSuccess": "Alerta silenciada por {{minutes}} minutos.",
    "dismissError": "No se pudo silenciar la alerta.",
    "read": "Leida",
    "dismissForMinutes": "Silenciar {{minutes}} min",
    "severityCalibration": "Calibracion de gravedad",
    "calibration": "Calibracion",
    "viewDetails": "Ver detalles",
    "recommendedAction": "Accion sugerida",
    "summary": {
      "visibleInboxOnly": "Solo bandeja visible",
      "withoutSilenced": "Sin silenciadas"
    },
    "toolbar": {
      "title": "Alertas reales",
      "description": "Se muestran alertas activas guardadas para tu usuario y permitidas por tus preferencias de salida."
    },
    "filters": {
      "all": "Todas",
      "unread": "No leidas",
      "stable": "Estables",
      "success": "Logros",
      "info": "Informativas",
      "warning": "Atencion",
      "critical": "Criticas"
    },
    "levels": {
      "stable": "Stable",
      "success": "Exito",
      "info": "Info",
      "warning": "Atencion",
      "critical": "Critico"
    },
    "severityDescriptions": {
      "stable": "Dentro de rango",
      "success": "Bajo impacto",
      "info": "Seguimiento normal",
      "warning": "Atencion preventiva",
      "critical": "Atencion inmediata",
      "default": "Seguimiento"
    }
  },
  "common": {
    "loading": "Cargando...",
    "delete": "Eliminar",
    "cancel": "Cancelar",
    "continue": "Continuar",
    "keep": "Conservar",
    "ok": "OK"
  },
  "profile": {
    "eyebrow": "Perfil de usuario",
    "title": "Mi perfil",
    "subtitle": "Gestiona tu información personal y la configuración de tu cuenta.",
    "personalData": "Datos personales",
    "saveChanges": "Guardar cambios",
    "deleteAccount": "Eliminar cuenta",
    "deleteAccountWarning": "Esta acción es permanente y no se puede deshacer.",
    "deleteConfirmation": "Escribe ELIMINAR para confirmar."
  },
  "deviceGroups": {
    "eyebrow": "Grupos de dispositivos",
    "title": "Grupos de dispositivos",
    "subtitle": "Organiza dispositivos en grupos y ejecuta acciones sobre ellos en conjunto.",
    "addTitle": "Crear grupo de dispositivos",
    "name": "Nombre",
    "description": "Descripción",
    "create": "Crear grupo",
    "createSuccess": "Grupo de dispositivos creado correctamente.",
    "createError": "No se pudo crear el grupo de dispositivos.",
    "updateError": "No se pudo actualizar el grupo de dispositivos.",
    "deleteError": "No se pudo eliminar el grupo de dispositivos.",
    "executeSuccess": "Acción del grupo ejecutada correctamente.",
    "executeError": "No se pudo ejecutar la acción del grupo.",
    "pageDescription": "Organiza dispositivos por intencion de uso dentro de la sede activa.",
    "noSelection": "Sin seleccion",
    "deleteSuccess": "Grupo eliminado.",
    "summary": {
      "createdTitle": "Creados",
      "availableTitle": "Disponibles",
      "multiGroupDescription": "pueden pertenecer a varios grupos.",
      "selectionEyebrow": "Seleccion",
      "selectedDevices": "{{count}} dispositivo(s) seleccionados."
    },
    "toolbar": {
      "eyebrow": "Grupos semanticos",
      "title": "Control por intencion",
      "description": "Un grupo puede representar iluminacion, ahorro, noche o cualquier forma real de uso."
    },
    "emptyDevices": {
      "title": "No hay dispositivos asignados",
      "description": "Asigna dispositivos a la sede activa antes de crear grupos."
    },
    "emptyGroups": {
      "title": "No hay grupos en esta sede",
      "description": "Crea un grupo semantico con dispositivos de una misma habitacion."
    },
    "form": {
      "eyebrow": "Nuevo grupo",
      "description": "Selecciona dispositivos de una misma habitacion. Un dispositivo puede pertenecer a varios grupos.",
      "namePlaceholder": "Ej. Iluminacion sala",
      "descriptionPlaceholder": "Proposito del grupo",
      "pickerDescription": "Marca equipos asignados a {{location}}.",
      "selectedCount": "{{count}} seleccionados",
      "availableCount": "{{count}} disponibles",
      "alsoBelongsTo": "Tambien pertenece a"
    },
    "selection": {
      "select": "Seleccionar",
      "selected": "Seleccionado"
    },
    "state": {
      "on": "Encendido",
      "off": "Apagado",
      "mixed": "Mixto"
    },
    "card": {
      "defaultDescription": "Grupo semantico para organizar dispositivos por intencion de uso.",
      "assignedDevices": "Dispositivos asignados",
      "noAssignedDevices": "Sin dispositivos asignados.",
      "createdAt": "Creado",
      "noDate": "Sin fecha registrada"
    },
    "validation": {
      "sameRoom": "Un grupo debe pertenecer a una sola habitacion. Selecciona dispositivos de la misma habitacion.",
      "activeLocationRequired": "Selecciona una sede activa antes de crear un grupo.",
      "nameRequired": "Escribe un nombre para el grupo antes de crearlo.",
      "deviceRequired": "Selecciona al menos un dispositivo para crear el grupo."
    },
    "deleteFallbackGroup": "Este grupo",
    "deleteWarnings": {
      "devicesTitle": "Revisar dispositivos",
      "devicesMessage": "{{group}} contiene {{count}} dispositivo(s). Al eliminar el grupo, los dispositivos seguiran existiendo.",
      "routinesTitle": "Revisar rutinas",
      "routineSingular": "{{group}} participa en una rutina activa. Revisa si deseas continuar.",
      "routinePlural": "{{group}} participa en {{count}} rutinas activas. Revisa si deseas continuar.",
      "modesTitle": "Revisar modos",
      "modeSingular": "{{group}} esta relacionado con un modo existente. Eliminarlo puede afectar esa configuracion.",
      "modePlural": "{{group}} esta relacionado con {{count}} modos existentes. Eliminarlo puede afectar esas configuraciones.",
      "deleteTitle": "Eliminar grupo",
      "deleteMessage": "Se eliminara {{group}}. Los dispositivos no se eliminan."
    }
  },
  "alertRules": {
    "eyebrow": "Reglas de alerta",
    "title": "Reglas de alerta",
    "subtitle": "Configura reglas automáticas para generar alertas según umbrales de consumo.",
    "total": "Total",
    "enabled": "Activas",
    "createTitle": "Crear regla de alerta",
    "name": "Nombre",
    "threshold": "Umbral",
    "create": "Crear regla",
    "enable": "Activar",
    "disable": "Desactivar",
    "createSuccess": "Regla de alerta creada correctamente.",
    "createError": "No se pudo crear la regla de alerta.",
    "updateError": "No se pudo actualizar la regla de alerta.",
    "deleteError": "No se pudo eliminar la regla de alerta.",
    "pageDescription": "Define criterios persistentes para clasificar eventos operativos. La evaluacion usa las reglas activas guardadas para tu usuario.",
    "summaryAria": "Resumen de reglas",
    "savedRules": "Reglas guardadas",
    "pausedCount": "{{count}} pausadas",
    "activeWeight": "Peso activo",
    "usedWhenEvaluating": "Usado al evaluar",
    "realScoreRanges": "Rangos reales del score",
    "realScoreRangesDescription": "Estos rangos vienen de Preferencias. Estable empieza en 0 y Critica termina en 100.",
    "newRule": "Nueva regla",
    "createPersistentRule": "Crear regla persistente",
    "createPersistentRuleDescription": "La regla se guarda y puede participar en la evaluacion mientras este activa.",
    "namePlaceholder": "Ej. Pico de potencia en sala",
    "metric": "Metrica",
    "condition": "Condicion",
    "scope": "Alcance",
    "criterion": "Criterio",
    "weight": "Peso",
    "savedRulesDescription": "Activar, pausar o eliminar impacta la lista persistida y la siguiente evaluacion.",
    "emptyTitle": "No hay reglas guardadas",
    "emptyDescription": "Crea una regla para que la evaluacion tenga criterios persistidos.",
    "evaluation": "Evaluacion",
    "testActiveRules": "Probar reglas activas",
    "testActiveRulesDescription": "La prueba usa el servicio de evaluacion y las reglas activas guardadas.",
    "observedValue": "Valor observado",
    "evaluate": "Evaluar",
    "testedNotification": "Notificacion probada",
    "score": "Score",
    "criteria": "Criterios",
    "readingsValue": "{{count}} lecturas",
    "validation": {
      "nameRequired": "Escribe un nombre para la regla."
    },
    "toasts": {
      "paused": "Regla pausada.",
      "activated": "Regla activada.",
      "deleted": "Regla eliminada.",
      "evaluateError": "No se pudo evaluar las reglas.",
      "evaluated": "Reglas evaluadas."
    },
    "metrics": {
      "watts": "Potencia instantanea",
      "highReadingCount": "Cantidad de lecturas altas",
      "averageWatts": "Promedio de watts"
    },
    "metricDescriptions": {
      "watts": "Watts medidos en el evento.",
      "highReadingCount": "Numero de eventos marcados como altos en el periodo evaluado.",
      "averageWatts": "Promedio observado para el periodo."
    },
    "conditions": {
      "greaterOrEqual": "Alcanza o supera",
      "greaterThan": "Supera",
      "greaterOrEqualLower": "alcanza o supera",
      "greaterThanLower": "supera"
    },
    "scopes": {
      "general": "General"
    },
    "evaluators": {
      "activePower": "Potencia activa",
      "dailyKwh": "Consumo diario",
      "averageWatts": "Promedio watts",
      "highReadingCount": "Cantidad de lecturas altas",
      "sustainedConsumption": "Consumo sostenido",
      "deviceCount": "Cantidad de dispositivos",
      "goalUsage": "Uso de meta",
      "goalDeadline": "Vencimiento de meta",
      "routineContext": "Contexto de rutina",
      "costEstimate": "Costo estimado",
      "configurationCoverage": "Cobertura configurada"
    },
    "severityRanges": {
      "stableDescription": "Operacion dentro del rango esperado.",
      "infoDescription": "Evento util para seguimiento, sin urgencia.",
      "warningDescription": "Conviene revisar antes de que escale.",
      "criticalDescription": "Requiere accion inmediata."
    }
  },
  "notificationPreferences": {
    "eyebrow": "Preferencias de notificación",
    "title": "Preferencias de notificación",
    "subtitle": "Configura cómo y cuándo quieres recibir notificaciones de ElectroCorp.",
    "emailEnabled": "Notificaciones por correo",
    "pushEnabled": "Notificaciones push",
    "criticalOnly": "Solo alertas críticas",
    "minimumLevel": "Nivel mínimo",
    "save": "Guardar preferencias",
    "updateSuccess": "Preferencias de notificación actualizadas correctamente.",
    "updateError": "No se pudieron actualizar las preferencias de notificación.",
    "sensitivityTitle": "Sensibilidad de alertas",
    "sensitivityDescription": "Ajusta categorias visibles, ruido, pesos preliminares y salida de alertas para tu usuario.",
    "currentPreference": "Preferencia actual",
    "editorTitle": "Editor preliminar",
    "editorDescription": "Los canales, fuentes y silencios se guardan; los pesos y rangos quedan listos para el siguiente contrato.",
    "emptyTitle": "Sin preferencias cargadas",
    "emptyDescription": "Cuando la preferencia del usuario este disponible, podras editar la visibilidad de la bandeja.",
    "summary": {
      "rangesEyebrow": "Rangos",
      "rangesTitle": "Stable a Critical",
      "rangesDescription": "Los limites se mantienen contiguos para evitar cruces.",
      "successTitle": "Positivo aparte",
      "successDescription": "Los eventos buenos no compiten con el riesgo.",
      "parametersEyebrow": "Parametros",
      "parametersTitle": "Pesos y ruido",
      "parametersDescription": "Controla prioridad, repeticion, horarios y fuentes."
    },
    "sensitivity": "Sensibilidad",
    "weightedClassification": "Clasificacion ponderada",
    "weightedClassificationDescription": "Success queda fuera del riesgo. Stable, Neutral, Warning y Critical se calculan por rangos contiguos.",
    "stableCeiling": "Tope Stable",
    "neutralCeiling": "Tope Info",
    "warningCeiling": "Tope Warning",
    "preview": "Preview",
    "sensitiveResult": "Resultado sensible",
    "previewDescription": "El puntaje tecnico no necesita verse en bandeja; aqui solo sirve para calibrar la categoria.",
    "context": "Contexto",
    "baseSignal": "Senal base",
    "finalCategory": "Categoria final",
    "weightedPercent": "{{value}}% ponderado",
    "positiveEvents": "Eventos positivos",
    "positiveEventsDescription": "Acciones correctas, recuperaciones, metas cumplidas y aplicaciones exitosas.",
    "showSuccess": "Mostrar Success",
    "showSuccessDescription": "Mantiene visible lo bueno sin mezclarlo con riesgo.",
    "visibleCategories": "Categorias visibles",
    "allowedLevels": "Niveles permitidos",
    "allowedLevelsDescription": "El contrato actual guarda estos niveles; Neutral se persiste como INFO.",
    "criticalOnlyLabel": "Solo Critical",
    "criticalOnlyDescription": "Reduce ruido y deja pasar unicamente eventos urgentes.",
    "weighting": "Ponderacion",
    "contextParameters": "Parametros por contexto",
    "contextParametersDescription": "Estos pesos son preliminares: ajustan la preview y dejan listo el siguiente contrato.",
    "weightDescription": "{{weight}}% de peso",
    "output": "Salida",
    "channelsAndNoise": "Canales y ruido",
    "channelsAndNoiseDescription": "Estos parametros ya se guardan en la preferencia actual.",
    "deliveryMode": "Modo de entrega",
    "cooldown": "Cooldown",
    "minutes": "minutos",
    "maxPerHour": "Maximo por hora",
    "alertsUnit": "alertas",
    "silences": "Silencios",
    "scheduleAndExceptions": "Horario y excepciones",
    "scheduleAndExceptionsDescription": "Critical puede romper silencio si lo permites.",
    "quietStart": "Inicio silencio",
    "quietEnd": "Fin silencio",
    "levelDescriptions": {
      "stable": "Mostrar todo desde estado estable.",
      "info": "Recibir eventos relevantes y superiores.",
      "warning": "Recibir solo preventivas y criticas.",
      "critical": "Recibir solo eventos urgentes."
    },
    "deliveryModes": {
      "banner": "Banner",
      "quiet": "Silencioso",
      "inboxOnly": "Solo bandeja",
      "muted": "Silenciado"
    },
    "deliveryModeDescriptions": {
      "banner": "Salida visible normal.",
      "quiet": "Sin ruido invasivo.",
      "inboxOnly": "Sin aviso emergente.",
      "muted": "Sin salida invasiva."
    },
    "sources": {
      "device": "Dispositivos",
      "group": "Grupos",
      "room": "Habitaciones",
      "workplace": "Sedes",
      "routine": "Rutinas",
      "goal": "Metas",
      "report": "Reportes",
      "rule": "Reglas",
      "mode": "Modos",
      "system": "Sistema"
    },
    "sourceDescriptions": {
      "device": "Estado, respuesta y consumo directo de equipos.",
      "group": "Cambios agregados por intencion de uso.",
      "routine": "Ejecucion, fallos o silencios de automatizacion.",
      "goal": "Progreso, riesgo de exceder o cumplimiento.",
      "rule": "Umbrales automaticos y evaluaciones activas.",
      "mode": "Aplicacion de perfiles operativos.",
      "system": "Recomendaciones y eventos propios de plataforma."
    },
    "toggles": {
      "inAppDescription": "Mostrar alertas dentro de la app.",
      "toastTitle": "Toast",
      "toastDescription": "Avisos emergentes dentro de la sesion.",
      "dashboardTitle": "Dashboard",
      "dashboardDescription": "Reflejar alertas en paneles.",
      "emailTitle": "Correo",
      "emailDescription": "Canal preparado para avisos externos.",
      "pushTitle": "Push",
      "pushDescription": "Canal preparado para avisos del dispositivo.",
      "groupSimilarTitle": "Agrupar similares",
      "groupSimilarDescription": "Reduce repeticion por hilo de alerta.",
      "quietHoursTitle": "Horario silencioso",
      "quietHoursDescription": "Usa una ventana sin ruido.",
      "criticalBreaksTitle": "Critical rompe silencio",
      "criticalBreaksDescription": "Eventos urgentes siguen visibles.",
      "remindersTitle": "Recordatorios",
      "remindersDescription": "Permite avisos posteriores.",
      "nightRoutinesTitle": "Rutinas nocturnas",
      "nightRoutinesDescription": "Baja ruido por automatizacion nocturna.",
      "goalDeadlineTitle": "Metas con fecha",
      "goalDeadlineDescription": "Prioriza metas cerca del limite.",
      "maintenanceDescription": "Incluye equipos en revision.",
      "recommendationsTitle": "Recomendaciones",
      "recommendationsDescription": "Muestra sugerencias del sistema.",
      "dailySummaryTitle": "Resumen diario",
      "dailySummaryDescription": "Agrupa actividad del dia.",
      "weeklySummaryTitle": "Resumen semanal",
      "weeklySummaryDescription": "Agrupa tendencias semanales."
    }
  },
  "workplace": {
    "locationsEyebrow": "Espacio de trabajo",
    "locationsTitle": "Sedes",
    "locationsSubtitle": "Gestiona los locales donde se instalan dispositivos de ElectroCorp.",
    "roomsEyebrow": "Habitaciones",
    "roomsTitle": "Habitaciones",
    "roomsSubtitle": "Organiza habitaciones dentro de cada sede.",
    "assignmentsEyebrow": "Asignaciones de dispositivos",
    "assignmentsTitle": "Asignaciones de dispositivos",
    "assignmentsSubtitle": "Asigna dispositivos a una sede y habitacion para que puedan participar en grupos, rutinas, modos, metas y reportes.",
    "totalLocations": "Total de sedes",
    "businessLocations": "Locales comerciales",
    "branchLocations": "Sucursales",
    "totalRooms": "Total de habitaciones",
    "totalAssignments": "Total de asignaciones",
    "totalDevices": "Total de dispositivos",
    "createLocation": "Crear sede",
    "createRoom": "Crear habitacion",
    "assignDevice": "Asignar dispositivo",
    "locationName": "Nombre de la sede",
    "roomName": "Nombre de la habitacion",
    "address": "Dirección",
    "floor": "Piso",
    "selectLocation": "Selecciona una sede",
    "selectRoom": "Selecciona una habitacion",
    "selectDevice": "Selecciona un dispositivo",
    "create": "Crear",
    "loadError": "No se pudo cargar la información del espacio de trabajo.",
    "locationCreateSuccess": "Sede creada correctamente.",
    "locationCreateError": "No se pudo crear la sede.",
    "locationUpdateSuccess": "Sede actualizada correctamente.",
    "locationUpdateError": "No se pudo actualizar la sede.",
    "locationDeleteError": "No se pudo eliminar la sede.",
    "roomCreateSuccess": "Habitacion creada correctamente.",
    "roomCreateError": "No se pudo crear la habitacion.",
    "roomUpdateSuccess": "Habitacion actualizada correctamente.",
    "roomUpdateError": "No se pudo actualizar la habitacion.",
    "roomDeleteError": "No se pudo eliminar la habitacion.",
    "assignmentCreateSuccess": "Dispositivo asignado correctamente.",
    "assignmentCreateError": "No se pudo asignar el dispositivo.",
    "assignmentMoveSuccess": "Asignación de dispositivo movida correctamente.",
    "assignmentMoveError": "No se pudo mover la asignación del dispositivo.",
    "assignmentDeleteError": "No se pudo eliminar la asignación del dispositivo.",
    "deviceAlreadyAssigned": "Este dispositivo ya está asignado.",
    "locationTypes": {
      "home": "Casa",
      "business": "Empresa",
      "branch": "Local"
    },
    "locations": {
      "pageDescription": "Administra los lugares fisicos donde ElectroCorp organiza habitaciones, dispositivos y consumo.",
      "summary": {
        "registeredTitle": "Registradas",
        "registeredDescription": "Casas, oficinas o locales operativos.",
        "assignedTitle": "Con sede",
        "unassignedDevices": "{{count}} dispositivo(s) sin sede.",
        "coverageEyebrow": "Cobertura",
        "unroomedDevices": "{{count}} dispositivo(s) sin habitacion."
      },
      "toolbar": {
        "eyebrow": "Gestion de sedes",
        "title": "Crea y selecciona centros operativos",
        "description": "La ubicacion puede guardarse como referencia escrita o como coordenadas aproximadas."
      },
      "empty": {
        "title": "No hay sedes",
        "description": "Crea una sede para organizar habitaciones, dispositivos, grupos y consumo por lugar fisico."
      },
      "form": {
        "eyebrow": "Nueva sede",
        "description": "Define el nombre, tipo y una ubicacion de referencia para este espacio operativo.",
        "namePlaceholder": "Ej. Oficina central",
        "typeLabel": "Tipo",
        "typePlaceholder": "Tipo de sede",
        "addressLabel": "Direccion o referencia",
        "addressPlaceholder": "Direccion, referencia o coordenadas"
      },
      "map": {
        "title": "Selector aproximado",
        "description": "OpenStreetMap se usa como referencia visual sin API key. Si no hay direccion exacta, se guardan coordenadas aproximadas.",
        "iframeTitle": "Mapa OpenStreetMap de referencia",
        "surfaceLabel": "Seleccionar punto en el mapa",
        "selectedLabel": "Ubicacion seleccionada",
        "selectedAddress": "Ubicacion seleccionada ({{coordinates}})"
      },
      "deleteTitle": "Eliminar sede",
      "deleteMessage": "Vas a eliminar {{name}}. Las habitaciones y asignaciones asociadas pueden dejar de aparecer en Espacios.",
      "deleteSuccess": "Sede eliminada.",
      "card": {
        "createdAt": "Creada",
        "noDate": "Sin fecha registrada",
        "activePower": "Potencia activa",
        "withoutRoom": "Sin habitacion",
        "coverage": "Cobertura",
        "goToRooms": "Ir",
        "goToRoomsAria": "Ir a habitaciones de esta sede"
      }
    },
    "rooms": {
      "pageDescription": "Organiza la sede activa por habitaciones para entender donde estan los dispositivos y como se distribuye el consumo.",
      "activeLocationFallback": "sede activa",
      "summary": {
        "activeLocationEyebrow": "Sede activa",
        "availableRooms": "habitacion(es) disponibles para organizar movimientos.",
        "registeredTitle": "Registradas",
        "registeredDescription": "Zonas fisicas disponibles para asignar dispositivos.",
        "assignedTitle": "Asignados",
        "linkedGroups": "{{count}} grupo(s) vinculados en la sede."
      },
      "toolbar": {
        "eyebrow": "Gestion de habitaciones",
        "title": "Agrega zonas a {{location}}",
        "description": "Las habitaciones ayudan a mover dispositivos y revisar consumo por lugar."
      },
      "empty": {
        "title": "No hay habitaciones en esta sede",
        "description": "Crea una habitacion para ubicar dispositivos y moverlos entre zonas reales."
      },
      "form": {
        "eyebrow": "Nueva habitacion",
        "description": "La habitacion se agregara a la sede activa y quedara disponible para movimientos de dispositivos.",
        "namePlaceholder": "Ej. Sala principal",
        "siteLabel": "Sede"
      },
      "status": {
        "noDevices": "Sin dispositivos",
        "active": "Con actividad",
        "idle": "Sin actividad"
      },
      "card": {
        "createdAt": "Creada",
        "noDate": "Sin fecha registrada",
        "activeDevices": "Encendidos",
        "consumption": "Consumo",
        "relatedGroup": "Grupo relacionado",
        "noLinkedGroup": "Sin grupo vinculado",
        "relatedRoutines": "Rutinas relacionadas",
        "goToAssignments": "Ir",
        "goToAssignmentsAria": "Ir a asignaciones de esta habitacion"
      },
      "toasts": {
        "loadError": "No se pudo cargar la informacion de habitaciones.",
        "locationRequired": "Crea una sede antes de crear habitaciones.",
        "deleteSuccess": "Habitacion eliminada correctamente."
      },
      "deleteTitle": "Eliminar habitacion",
      "deleteMessageWithDevices": "Esta habitacion tiene {{count}} dispositivo(s) asignado(s). Si la eliminas, esas referencias pueden quedar sin habitacion.",
      "deleteMessageEmpty": "Vas a eliminar esta habitacion de la sede activa."
    },
    "deviceTypes": {
      "plug": "Enchufe",
      "light": "Luz",
      "switch": "Interruptor",
      "sensor": "Sensor",
      "other": "Otro"
    },
    "assignments": {
      "pageDescription": "Arrastra dispositivos entre dos habitaciones y confirma cuando el cambio afecte grupos, rutinas o modos.",
      "activeLocationFallback": "sede activa",
      "noRoom": "Sin habitacion",
      "noSecondRoomOption": "No hay otra habitacion disponible",
      "noSecondRoomAvailable": "No hay una segunda habitacion disponible.",
      "panelItemCount": "{{count}} elemento(s) en esta habitacion.",
      "go": "Ir",
      "summary": {
        "activeLocationEyebrow": "Sede activa",
        "availableRooms": "habitacion(es) disponibles para organizar movimientos.",
        "movableTitle": "Movibles",
        "connectedDevices": "dispositivo(s) conectados a esta sede.",
        "interactionEyebrow": "Interaccion",
        "dragTitle": "Arrastre directo",
        "directValue": "Directo",
        "dragDescription": "arrastra dispositivos entre Habitacion 1 y Habitacion 2."
      },
      "movement": {
        "eyebrow": "Movimiento entre habitaciones",
        "title": "Nueva asignacion",
        "description": "Arrastra cada dispositivo de un panel al otro. Las confirmaciones de grupo, rutina y modo aparecen antes de aplicar el cambio."
      },
      "panels": {
        "roomOne": "Habitacion 1",
        "roomTwo": "Habitacion 2"
      },
      "emptyRooms": {
        "title": "No hay habitaciones",
        "description": "Crea habitaciones en la sede activa para poder mover dispositivos o grupos."
      },
      "emptyAssignments": {
        "title": "No hay dispositivos asignados",
        "description": "Cuando una sede tenga dispositivos conectados, podras moverlos entre habitaciones desde esta vista."
      },
      "emptyPanel": {
        "title": "Sin dispositivos",
        "description": "Arrastra aqui un dispositivo para moverlo a esta habitacion."
      },
      "unavailablePanel": {
        "title": "Habitacion no disponible",
        "description": "Agrega otra habitacion para habilitar este panel."
      },
      "device": {
        "noPowerInfo": "Sin informacion de potencia",
        "notAvailable": "No disponible",
        "on": "Encendido",
        "off": "Apagado",
        "maintenance": "Mantenimiento"
      },
      "form": {
        "operationalSite": "Sede operativa",
        "siteOnly": "Asignar solo a la sede",
        "availableRoom": "Habitacion disponible"
      },
      "toasts": {
        "loadError": "No se pudo cargar la informacion de asignaciones.",
        "moveSuccess": "Movimiento aplicado correctamente.",
        "moveError": "No se pudo completar el movimiento."
      },
      "warnings": {
        "groupTitle": "Revisar grupo",
        "groupMessage": "Este dispositivo forma parte del grupo {{group}}. Si lo mueves, ese grupo puede quedar incompleto.",
        "routinesTitle": "Revisar rutinas",
        "routinesMessage": "Este dispositivo participa en rutinas activas. Revisa si deseas continuar.",
        "modesTitle": "Revisar modos",
        "modesMessage": "Este dispositivo esta relacionado con un modo. Moverlo puede afectar ese modo."
      }
    }
  },
  "reporting": {
    "reportsEyebrow": "Reportes",
    "reportsTitle": "Reportes de consumo",
    "reportsSubtitle": "Genera y revisa reportes de consumo energético.",
    "goalsEyebrow": "Metas energéticas",
    "goalsTitle": "Metas energéticas",
    "goalsSubtitle": "Define metas de ahorro y monitorea tu progreso.",
    "totalReports": "Total de reportes",
    "totalGoals": "Total de metas",
    "activeGoals": "Metas activas",
    "completedGoals": "Metas completadas",
    "totalReportedWatts": "Watts reportados totales",
    "averageReportedWatts": "Promedio de watts reportados",
    "monthlyComparison": "Comparación de reportes",
    "monthlyComparisonSubtitle": "Compara reportes del mismo periodo:",
    "createReport": "Crear reporte",
    "createGoal": "Crear meta",
    "reportTitle": "Título del reporte",
    "goalTitle": "Título de la meta",
    "generate": "Generar",
    "export": "Exportar",
    "totalWatts": "Watts totales",
    "averageWatts": "Watts promedio",
    "highestWatts": "Watts máximos",
    "targetWatts": "Watts objetivo",
    "deadline": "Fecha límite",
    "createdAt": "Creado el",
    "generatedAt": "Generado el",
    "emptyReportsTitle": "Todavía no hay reportes",
    "emptyReportsContent": "Genera tu primer reporte de consumo para analizar tu uso energético.",
    "emptyGoalsTitle": "Todavía no hay metas",
    "emptyGoalsContent": "Crea una meta energética para empezar a medir el ahorro.",
    "loadError": "No se pudo cargar la información de reportes.",
    "reportCreateSuccess": "Reporte creado correctamente.",
    "reportCreateError": "No se pudo crear el reporte.",
    "reportDeleteError": "No se pudo eliminar el reporte.",
    "exportNotAllowed": "Tu plan actual no permite exportar reportes.",
    "reportNotFound": "Reporte no encontrado.",
    "goalCreateSuccess": "Meta energética creada correctamente.",
    "goalCreateError": "No se pudo crear la meta energética.",
    "goalUpdateSuccess": "Meta energética actualizada correctamente.",
    "goalUpdateError": "No se pudo actualizar la meta energética.",
    "goalDeleteError": "No se pudo eliminar la meta energética.",
    "createReportDescription": "Elige el tipo de reporte y mueve la rueda para seleccionar el rango exacto.",
    "period": "Periodo",
    "periods": {
      "daily": "Diario",
      "weekly": "Semanal",
      "monthly": "Mensual",
      "yearly": "Anual"
    },
    "periodOptions": {
      "daily": "Diarios",
      "weekly": "Semanales",
      "monthly": "Mensuales",
      "yearly": "Anuales"
    },
    "reportRangeEyebrow": "Rango del reporte",
    "reportRangeHint": "Arrastra hacia la derecha para ir al pasado. El rango actual es el límite final.",
    "rangeTitles": {
      "daily": "Selecciona el día",
      "weekly": "Selecciona la semana",
      "monthly": "Selecciona el mes",
      "yearly": "Selecciona el año"
    },
    "rangeLabels": {
      "currentWeek": "Semana en curso",
      "currentYearCycle": "{{year}} - en curso",
      "currentYear": "Año en curso",
      "fullYear": "Año completo"
    },
    "generatedReportsTitle": "Reportes generados",
    "generatedReportsDescription": "Revisa los reportes creados y abre cada detalle desde su resumen.",
    "emptyReportsEyebrow": "Sin reportes",
    "from": "Desde",
    "to": "Hasta",
    "generated": "Generado",
    "emptyValue": "-",
    "reportFallbackTitle": "Reporte",
    "reportFallbackTitleWithId": "Reporte #{{id}}",
    "averageDetail": "{{value}} promedio",
    "reportChartAria": "{{title}}, {{total}}, {{start}} - {{end}}",
    "reportNameRequired": "Escribe el nombre del reporte.",
    "reportNameMinLength": "El nombre del reporte debe tener al menos 4 caracteres.",
    "reportRangeRequired": "Selecciona un rango de fechas para el reporte.",
    "reportRangeInvalid": "El rango del reporte no es válido.",
    "reportUpdateSuccess": "Reporte actualizado.",
    "reportUnchanged": "Ese reporte ya existe para el ciclo seleccionado.",
    "reportExportSuccess": "Reporte exportado.",
    "reportExportError": "No se pudo exportar el reporte.",
    "reportDeleteSuccess": "Reporte eliminado.",
    "duration": {
      "hoursMinutes": "{{hours}}h {{minutes}}m",
      "minutesOnly": "{{minutes}}m"
    },
    "invoice": {
      "eyebrow": "Factura energética profesional",
      "title": "Reporte ElectroCorp #{{id}}",
      "close": "Cerrar factura",
      "totalEnergy": "Energía total",
      "exactReadings": "{{count}} lecturas exactas dentro del rango",
      "totalToPay": "Total a pagar",
      "realKwhCalculation": "Calculado desde kWh reales por dispositivo",
      "monitoredDuration": "{{duration}} monitoreados",
      "technicalSummary": "Resumen técnico",
      "averagePower": "Potencia promedio",
      "detectedPeak": "Pico detectado",
      "criticalReadings": "Lecturas críticas",
      "costPerKwh": "Costo por kWh",
      "billedEnergy": "Energía facturada",
      "energySubtotal": "Subtotal energía",
      "measuredDevices": "Dispositivos medidos",
      "totalOnTime": "Tiempo ON total",
      "topConsumption": "Top consumo",
      "devicesCount": "{{count}} equipos",
      "noDevicesInRange": "No hay dispositivos en este rango.",
      "deviceExactDetail": "Detalle exacto por dispositivo",
      "readings": "Lecturas",
      "onTime": "Tiempo ON",
      "exactKwh": "kWh exactos",
      "peak": "Pico",
      "share": "Participación",
      "subtotal": "Subtotal",
      "noReadingsForReport": "No hay lecturas para este reporte."
    },
    "goals": {
      "summary": {
        "created": "Metas creadas",
        "tracking": "En seguimiento",
        "atRisk": "Metas en riesgo",
        "needAttention": "Requieren atención",
        "averageCompliance": "Cumplimiento promedio"
      },
      "createStripTitle": "Crear meta inteligente",
      "createStripDescription": "Define alcance, límite y ventana de seguimiento con datos reales.",
      "modalEyebrow": "Meta energética",
      "modalDescription": "Selecciona el alcance de seguimiento y define el límite energético.",
      "scope": "Alcance",
      "goalName": "Nombre de la meta",
      "goalNamePlaceholder": "Ej. Reducir consumo de sala principal",
      "consumptionLimit": "Límite de consumo",
      "emptyEyebrow": "Sin metas",
      "generalOperation": "Operación general",
      "fullDay": "Todo el día",
      "deviceCountDescription": "{{count}} equipos",
      "availableKwh": "{{value}} kWh disponibles",
      "daysRemaining": "{{count}} días restantes",
      "limitLabel": "Límite: {{value}} kWh",
      "deadlineLabel": "Vence: {{date}}",
      "measured": "Medido",
      "usedPercentage": "{{value}}% usado",
      "devices": "Equipos",
      "projection": "Proyección",
      "scopeOptions": {
        "workplace": "Una sede",
        "room": "Una habitación",
        "group": "Un grupo",
        "device": "Un dispositivo"
      },
      "scopeDescriptions": {
        "workplace": "Mide dispositivos asignados a un centro energético.",
        "room": "Mide dispositivos asignados a una habitación.",
        "group": "Mide los dispositivos incluidos en un grupo.",
        "device": "Mide solo las lecturas de un equipo."
      },
      "scopeTargetLabels": {
        "general": "Alcance",
        "workplace": "Sede",
        "room": "Habitación",
        "device": "Dispositivo",
        "group": "Grupo"
      },
      "scopeTargetPlaceholders": {
        "general": "Seleccionar alcance",
        "workplace": "Seleccionar sede",
        "room": "Seleccionar habitación",
        "device": "Seleccionar dispositivo",
        "group": "Seleccionar grupo"
      },
      "status": {
        "inControl": "En control",
        "outOfGoal": "Fuera de meta",
        "atRisk": "En riesgo",
        "goodPace": "Buen ritmo",
        "noReadings": "Sin lecturas"
      },
      "recommendations": {
        "inControl": "La meta avanza dentro del ritmo esperado.",
        "outOfGoal": "El consumo superó el límite definido para esta meta.",
        "atRisk": "El ritmo actual podría superar el límite. Revisa los equipos con mayor actividad.",
        "goodPace": "Mantienes margen suficiente frente al avance esperado.",
        "noReadings": "Aún no hay lecturas dentro del periodo de esta meta."
      },
      "toasts": {
        "loadError": "No se pudo cargar la información de metas energéticas.",
        "nameRequired": "Escribe el nombre de la meta.",
        "nameMinLength": "El nombre de la meta debe tener al menos 4 caracteres.",
        "targetRequired": "Define un límite de consumo mayor a 0 kWh.",
        "deadlineRequired": "Selecciona una fecha límite para la meta.",
        "deadlinePast": "La fecha límite no puede estar en el pasado.",
        "timeWindowInvalid": "La ventana horaria debe tener una hora de inicio y fin diferentes.",
        "scopeTargetRequired": "Selecciona el objetivo de la meta.",
        "deleteSuccess": "Meta energética eliminada."
      },
      "deleteDialog": {
        "title": "Eliminar meta energética",
        "message": "Esta acción quitará la meta del seguimiento actual.",
        "confirm": "Eliminar",
        "cancel": "Conservar"
      }
    }
  },
  "serviceManagement": {
    "supportEyebrow": "Soporte",
    "supportTitle": "Soporte",
    "supportSubtitle": "Crea y da seguimiento a solicitudes de soporte del usuario.",
    "maintenanceEyebrow": "Mantenimiento",
    "maintenanceTitle": "Mantenimiento",
    "maintenanceSubtitle": "Programa y monitorea tareas de mantenimiento de dispositivos.",
    "totalSupportTickets": "Total de tickets de soporte",
    "openTickets": "Tickets abiertos",
    "urgentTickets": "Tickets urgentes",
    "totalMaintenanceTickets": "Total de tickets de mantenimiento",
    "pendingMaintenance": "Mantenimientos pendientes",
    "scheduledMaintenance": "Mantenimientos programados",
    "completedMaintenance": "Mantenimientos completados",
    "createSupportTicket": "Crear ticket de soporte",
    "createMaintenanceTicket": "Crear ticket de mantenimiento",
    "subject": "Asunto",
    "description": "Descripción",
    "scheduledDate": "Fecha programada",
    "selectDevice": "Selecciona un dispositivo",
    "create": "Crear",
    "emptySupportTitle": "No hay tickets de soporte",
    "emptySupportContent": "Los tickets de soporte creados por el usuario aparecerán aquí.",
    "emptyMaintenanceTitle": "No hay tickets de mantenimiento",
    "emptyMaintenanceContent": "Los tickets de mantenimiento programados aparecerán aquí.",
    "loadError": "No se pudo cargar la información de gestión de servicios.",
    "supportCreateSuccess": "Ticket de soporte creado correctamente.",
    "supportCreateError": "No se pudo crear el ticket de soporte.",
    "supportUpdateSuccess": "Ticket de soporte actualizado correctamente.",
    "supportUpdateError": "No se pudo actualizar el ticket de soporte.",
    "supportDeleteError": "No se pudo eliminar el ticket de soporte.",
    "maintenanceCreateSuccess": "Ticket de mantenimiento creado correctamente.",
    "maintenanceCreateError": "No se pudo crear el ticket de mantenimiento.",
    "maintenanceUpdateSuccess": "Ticket de mantenimiento actualizado correctamente.",
    "maintenanceUpdateError": "No se pudo actualizar el ticket de mantenimiento.",
    "maintenanceDeleteError": "No se pudo eliminar el ticket de mantenimiento.",
    "supportPageDescription": "Centraliza solicitudes del usuario, prioriza casos abiertos y conserva el seguimiento con estados reales.",
    "supportSummaryTitle": "Resumen de soporte",
    "supportSummaryDescription": "Solicitudes registradas con trazabilidad en la plataforma.",
    "openTicketsShort": "Abiertas",
    "urgentTicketsShort": "Urgentes",
    "newSupportRequest": "Nueva solicitud",
    "supportAttentionTitle": "Atencion de soporte",
    "supportAttentionDescription": "Registra un caso con asunto, descripcion y prioridad para atenderlo desde Servicio.",
    "createSupportRequest": "Crear solicitud",
    "cases": "Casos",
    "supportCasesTitle": "Solicitudes activas e historicas",
    "emptySupportRequestsTitle": "No hay solicitudes",
    "emptySupportRequestsDescription": "Cuando alguien registre un caso de soporte, aparecera aqui con prioridad, estado y fecha.",
    "supportModalDescription": "Registra un caso de soporte con prioridad real.",
    "supportSubjectPlaceholder": "Ej. Equipo sin respuesta",
    "supportDescriptionPlaceholder": "Describe que ocurre, donde sucede y que impacto tiene.",
    "priority": "Prioridad",
    "saveSupportRequest": "Guardar solicitud",
    "supportLoadError": "No se pudo cargar la informacion de soporte.",
    "supportDeleteSuccess": "Solicitud de soporte eliminada.",
    "confirmDeleteSupportTitle": "Eliminar solicitud",
    "confirmDeleteSupportMessage": "Esta solicitud saldra de la bandeja de soporte. La accion no se puede deshacer.",
    "createdOn": "Creada el {{date}}",
    "maintenancePageDescription": "Programa intervenciones sobre dispositivos reales y sigue su estado sin inventar fechas, equipos ni tareas.",
    "maintenanceSummaryTitle": "Resumen de mantenimiento",
    "maintenanceSummaryDescription": "Ordenes creadas desde Servicio para dispositivos existentes.",
    "pendingMaintenanceShort": "Pendientes",
    "completedMaintenanceShort": "Completados",
    "scheduleEyebrow": "Programar",
    "technicalScheduleTitle": "Agenda tecnica",
    "technicalScheduleDescription": "Selecciona un dispositivo existente, motivo y fecha para crear una orden.",
    "scheduleMaintenance": "Programar mantenimiento",
    "goToDevices": "Ir a dispositivos",
    "orders": "Ordenes",
    "maintenanceAgendaTitle": "Agenda de mantenimiento",
    "emptyMaintenanceDescription": "Cuando programes una intervencion, aparecera aqui con dispositivo, fecha y estado.",
    "maintenanceModalDescription": "Usa un dispositivo real y una fecha valida.",
    "maintenanceType": "Tipo de mantenimiento",
    "reason": "Motivo",
    "maintenanceDescriptionPlaceholder": "Describe el trabajo requerido o el motivo de la revision.",
    "saveMaintenance": "Guardar mantenimiento",
    "maintenanceLoadError": "No se pudo cargar la informacion de mantenimiento.",
    "maintenanceDeleteSuccess": "Mantenimiento eliminado.",
    "confirmCancelMaintenanceTitle": "Cancelar mantenimiento",
    "confirmCancelMaintenanceMessage": "El mantenimiento quedara marcado como cancelado. Confirma solo si ya no debe ejecutarse.",
    "cancelMaintenance": "Cancelar mantenimiento",
    "back": "Volver",
    "confirmDeleteMaintenanceTitle": "Eliminar mantenimiento",
    "confirmDeleteMaintenanceMessage": "Esta programacion saldra de la lista de mantenimiento. La accion no se puede deshacer.",
    "scheduledFor": "Programado para {{date}}",
    "priorities": {
      "low": "Baja",
      "medium": "Media",
      "high": "Alta",
      "urgent": "Urgente"
    },
    "supportStatuses": {
      "open": "Abierto",
      "inProgress": "En progreso",
      "resolved": "Resuelto",
      "closed": "Cerrado"
    },
    "maintenanceTypes": {
      "inspection": "Inspeccion",
      "repair": "Reparacion",
      "replacement": "Reemplazo",
      "installation": "Instalacion"
    },
    "maintenanceStatuses": {
      "pending": "Pendiente",
      "scheduled": "Programado",
      "completed": "Completado",
      "canceled": "Cancelado"
    },
    "validation": {
      "supportSubjectRequired": "Escribe el asunto de la solicitud.",
      "supportSubjectMinLength": "El asunto debe tener al menos 4 caracteres.",
      "supportDescriptionRequired": "Describe el problema o la solicitud.",
      "descriptionMinLength": "La descripcion debe tener al menos 10 caracteres.",
      "deviceRequiredBeforeMaintenance": "Registra un dispositivo antes de programar mantenimiento.",
      "maintenanceDeviceRequired": "Selecciona un dispositivo para mantenimiento.",
      "maintenanceDescriptionRequired": "Describe el mantenimiento requerido.",
      "maintenanceDateRequired": "Selecciona una fecha de mantenimiento.",
      "maintenanceDatePast": "La fecha de mantenimiento no puede estar en el pasado.",
      "maintenanceDeviceUnknown": "No se pudo identificar el dispositivo seleccionado."
    }
  },
  "security": {
    "deleteAccount": "Eliminar cuenta"
  },
  "energyDashboard": {
    "eyebrow": "Monitoreo energético",
    "title": "Consumo energético",
    "description": "Controla el consumo real de los dispositivos encendidos, revisa kWh, costos estimados y oportunidades de ahorro por equipo y habitación.",
    "liveConsumptionEyebrow": "Consumo en vivo",
    "liveConsumptionTitle": "Gasto calculado desde potencia y tiempo real",
    "liveConsumptionDescription": "Los W son potencia nominal. El gasto real sale de kWh, segundos encendidos y tarifa energética.",
    "automaticRecord": "Registro automático",
    "secondsAbbr": "seg",
    "apply": "Aplicar",
    "sampleEveryPrefix": "Se registrará una lectura cada",
    "sampleEveryValue": "{{seconds}} segundos",
    "exportCsv": "Exportar CSV",
    "loading": "Cargando información energética...",
    "consumptionEyebrow": "Consumo",
    "readings": "lecturas",
    "equipment": "equipos",
    "energy": "energía",
    "cost": "costo",
    "accumulatedUse": "uso acumulado",
    "period": "Periodo",
    "noReadingsEyebrow": "Sin lecturas",
    "noConsumptionTitle": "No hay consumo para este periodo",
    "noConsumptionDescription": "Cuando existan lecturas reales dentro del periodo seleccionado, el gráfico se construirá automáticamente.",
    "generalStatusEyebrow": "Estado general",
    "operationalSummaryTitle": "Resumen operativo",
    "normalReadings": "normales",
    "highReadingsShort": "altas",
    "activeDevicesShort": "activos",
    "activeDevicesNowTitle": "Dispositivos encendidos ahora",
    "costPerHourValue": "S/ {{cost}} por hora",
    "moreActiveDevices": "+{{count}} más en activos",
    "noActivityEyebrow": "Sin actividad",
    "noActiveDevicesTitle": "No hay equipos encendidos",
    "noActiveDevicesDescription": "El consumo actual está en cero porque no hay dispositivos ON.",
    "rankingEyebrow": "Ranking",
    "highestCostByDeviceTitle": "Mayor gasto por dispositivo",
    "moreInRanking": "+{{count}} más en ranking",
    "noCostEyebrow": "Sin gasto",
    "noDeviceConsumptionTitle": "No hay dispositivos con consumo",
    "noDeviceConsumptionDescription": "Las lecturas con kWh aparecerán durante el uso.",
    "roomsEyebrow": "Ambientes",
    "consumptionByRoomTitle": "Consumo por habitación",
    "activeDevicesValue": "{{count}} activos",
    "moreInRooms": "+{{count}} más en ambientes",
    "noRoomsEyebrow": "Sin habitaciones",
    "noRoomDataTitle": "Sin datos por habitación",
    "noRoomDataDescription": "Asigna habitaciones a tus dispositivos para ver este desglose.",
    "recentHighConsumptionEventsTitle": "Eventos recientes de alto consumo",
    "moreInAlerts": "+{{count}} más en alertas",
    "noAlertsEyebrow": "Sin alertas",
    "noRecentEventsTitle": "No hay eventos recientes",
    "noRecentEventsDescription": "No se detectaron lecturas altas en el periodo visible.",
    "latestReadingsTitle": "Últimas lecturas energéticas",
    "power": "Potencia",
    "moreInHistory": "+{{count}} más en historial",
    "noHistoryEyebrow": "Sin historial",
    "noReadingsAvailableTitle": "No hay lecturas disponibles",
    "noReadingsAvailableDescription": "Cuando el sistema registre consumo, aquí verás las últimas lecturas.",
    "intervalValue": "Intervalo {{duration}}",
    "chartItemAria": "{{energy}}, {{emoji}} {{device}}, {{watts}} W, {{date}}, intervalo {{duration}}",
    "highConsumptionRegistered": "{{device}} registró consumo alto",
    "noActiveAlerts": "Sin alertas activas",
    "selectActiveSiteRecommendation": "Selecciona una sede activa para ver su consumo.",
    "noReadingsForSiteRecommendation": "Aún no hay lecturas energéticas para {{site}}.",
    "highReadingsForSiteRecommendation": "Revisa los equipos de {{site}} con lecturas altas.",
    "stableConsumptionRecommendation": "Consumo estable en {{site}}.",
    "noRoom": "Sin habitación",
    "periods": {
      "daily": "Diario",
      "weekly": "Semanal",
      "monthly": "Mensual"
    },
    "periodDescriptions": {
      "daily": "Lecturas reales registradas hoy.",
      "weekly": "Lecturas reales de los últimos 7 días.",
      "monthly": "Lecturas reales del mes actual."
    },
    "chartTitles": {
      "daily": "Consumo diario",
      "weekly": "Consumo semanal",
      "monthly": "Consumo mensual"
    },
    "chartSubtitles": {
      "daily": "Lecturas reales de hoy. El uso acumulado suma tiempo por equipo.",
      "weekly": "Lecturas reales de los últimos 7 días. El uso acumulado suma tiempo por equipo.",
      "monthly": "Lecturas reales del mes actual. El uso acumulado suma tiempo por equipo."
    },
    "operationalStatus": {
      "review": "Revisar consumo",
      "attention": "Atención",
      "optimal": "Óptimo"
    },
    "metrics": {
      "activePower": "Potencia activa",
      "energyToday": "Energía hoy",
      "costToday": "Gasto hoy",
      "costPerHour": "Costo por hora",
      "monthlyProjection": "Proyección mensual",
      "activeAlerts": "Alertas activas"
    },
    "metricHints": {
      "activePower": "Base actual para calcular kWh mientras hay equipos encendidos.",
      "energyToday": "Acumulado por intervalos reales registrados hoy.",
      "costToday": "Costo estimado desde kWh reales.",
      "costPerHour": "Proyección si el consumo actual se mantiene.",
      "monthlyProjection": "Estimado desde los kWh reales acumulados del mes.",
      "activeAlerts": "{{count}} críticas - {{level}} - {{title}}"
    },
    "readingStatuses": {
      "high": "Alta",
      "normal": "Normal"
    },
    "toasts": {
      "loadError": "No se pudo cargar la información energética.",
      "exportSuccess": "CSV exportado correctamente.",
      "exportError": "No se pudo exportar el CSV.",
      "intervalUpdated": "Intervalo actualizado a {{seconds}} segundos.",
      "intervalUpdateError": "No se pudo actualizar el intervalo de registro.",
      "samplingLoadFallback": "No se pudo cargar el intervalo de registro. Se usarán 15 segundos.",
      "refreshError": "No se pudo actualizar la información energética."
    }
  },
  "shared": {
    "numberStepper": {
      "increase": "Aumentar valor",
      "decrease": "Reducir valor"
    },
    "datePicker": {
      "placeholder": "Seleccionar fecha",
      "calendarAria": "Selector de fecha",
      "previousMonth": "Mes anterior",
      "nextMonth": "Mes siguiente",
      "clear": "Limpiar",
      "today": "Hoy"
    },
    "timePicker": {
      "panelAria": "Selector de hora",
      "hoursAria": "Horas",
      "minutesAria": "Minutos"
    },
    "modal": {
      "closeForm": "Cerrar formulario"
    }
  },
  "operationModes": {
    "pageDescription": "Configura modos con rutinas internas propias para una sede o una habitacion.",
    "site": "Sede",
    "internalRoutines": "Rutinas internas",
    "summary": {
      "context": "Contexto",
      "availableDevices": "{{count}} dispositivo(s) disponibles.",
      "saved": "Guardados",
      "hasActiveMode": "Hay un modo activo.",
      "noActiveMode": "Sin modo activo.",
      "current": "Actual",
      "selectMode": "Selecciona un modo."
    },
    "toolbar": {
      "eyebrow": "Configuracion operativa",
      "title": "Modos con rutinas propias",
      "description": "Las rutinas de un modo viven dentro del modo y se aplican solo al activar ese modo."
    },
    "actions": {
      "create": "Crear modo",
      "reapply": "Reaplicar",
      "archive": "Archivar",
      "add": "Agregar",
      "remove": "Quitar"
    },
    "empty": {
      "title": "No hay modos en esta sede",
      "description": "Crea un modo con rutinas internas para aplicar una configuracion operativa real."
    },
    "impact": {
      "title": "Impacto",
      "applicationTitle": "Aplicacion del modo",
      "affectedDevices": "{{count}} dispositivo(s)",
      "ignored": "Ignorados",
      "emptyTitle": "Selecciona un modo",
      "emptyDescription": "Revisa que dispositivos y rutinas internas aplicara antes de activar."
    },
    "form": {
      "newMode": "Nuevo modo",
      "description": "Define identidad, aplicacion, horario y rutinas internas propias.",
      "identityTitle": "Identidad",
      "identityDescription": "Nombre y descripcion controlada del modo.",
      "namePlaceholder": "Ej. Noche segura",
      "descriptionPlaceholder": "Uso operativo del modo",
      "applicationTitle": "Aplicacion",
      "applicationDescription": "El modo puede cubrir una sede completa o una habitacion.",
      "selectSite": "Selecciona sede",
      "selectRoom": "Selecciona habitacion",
      "scheduleType": "Tipo de horario",
      "end": "Fin",
      "modeRoutinesTitle": "Rutinas del modo",
      "modeRoutinesDescription": "Estas rutinas pertenecen solo a este modo.",
      "label": "Etiqueta",
      "routineNamePlaceholder": "Ej. Apagar luces",
      "selectTarget": "Selecciona objetivo",
      "noTargetsInScope": "No hay objetivos disponibles en este alcance.",
      "noInternalRoutinesTitle": "Aun no hay rutinas internas",
      "noInternalRoutinesDescription": "Agrega al menos una rutina interna para guardar el modo."
    },
    "applicationOptions": {
      "workplace": "Sede completa",
      "room": "Habitacion especifica"
    },
    "applicationDescriptions": {
      "workplace": "El modo aplica a toda la sede activa",
      "room": "El modo aplica solo a una habitacion"
    },
    "scheduleOptions": {
      "allDay": "Todo el dia",
      "custom": "Personalizado"
    },
    "scheduleDescriptions": {
      "allDay": "El modo mantiene su configuracion sin ventana horaria",
      "custom": "Define inicio y fin"
    },
    "routineTargetTypes": {
      "device": "Dispositivo",
      "group": "Grupo"
    },
    "routineTargetDescriptions": {
      "device": "Accion sobre un equipo",
      "group": "Accion sobre un grupo"
    },
    "routineActionDescriptions": {
      "turnOn": "Activar objetivo",
      "turnOff": "Desactivar objetivo"
    },
    "routineTargets": {
      "deviceCount": "{{count}} dispositivo(s)"
    },
    "goals": {
      "description": "Referencia metas reales sin prometer control adicional.",
      "emptyTitle": "Sin metas aplicables",
      "emptyDescription": "El modo puede guardarse sin metas asociadas.",
      "fallback": "Meta {{id}}"
    },
    "alertsRules": {
      "title": "Alertas/Reglas",
      "description": "Usa reglas y preferencias existentes."
    },
    "rules": {
      "available": "Reglas disponibles",
      "none": "Sin reglas",
      "noneDescription": "El modo no aplicara reglas"
    },
    "preferences": {
      "current": "Preferencia actual",
      "useRegistered": "Usar preferencia registrada",
      "noneRegistered": "Sin preferencia registrada",
      "numbered": "Preferencia {{id}}",
      "applyThis": "Aplicar esta preferencia"
    },
    "toggles": {
      "applyRulesTitle": "Aplicar reglas",
      "applyRulesDescription": "Usa reglas existentes.",
      "applyPreferencesTitle": "Aplicar preferencias",
      "applyPreferencesDescription": "Usa preferencias existentes.",
      "criticalSoundTitle": "Sonido critico",
      "criticalSoundDescription": "Respeta alertas criticas si aplica."
    },
    "validation": {
      "siteRequired": "Selecciona una sede antes de crear el modo.",
      "nameRequired": "Escribe un nombre para el modo.",
      "roomRequired": "Selecciona una habitacion para el modo.",
      "noTargets": "No hay dispositivos o grupos disponibles en el alcance seleccionado.",
      "routineRequired": "Agrega una rutina interna o completa la rutina en edicion.",
      "routineDraftRequired": "Completa nombre y objetivo de la rutina del modo.",
      "minimumRequired": "Define sede, nombre y al menos una rutina interna."
    },
    "confirm": {
      "replaceTitle": "Reemplazar modo activo",
      "replaceMessage": "Ya hay un modo activo en esta sede. Si activas este modo, el anterior se desactivara.",
      "archiveTitle": "Archivar modo",
      "archiveMessage": "Se archivara {{name}}. Sus rutinas internas dejaran de actuar."
    },
    "toasts": {
      "createError": "No se pudo crear el modo.",
      "createSuccess": "Modo creado con rutinas internas.",
      "activateError": "No se pudo activar el modo.",
      "archived": "Modo archivado."
    },
    "status": {
      "active": "Activo",
      "inactive": "Inactivo",
      "archived": "Archivado"
    },
    "fallbacks": {
      "noSelectedSite": "Sin sede seleccionada",
      "siteUnavailable": "Sede no disponible",
      "operationalSite": "Sede operativa",
      "roomUnavailable": "Habitacion no disponible",
      "groupUnavailable": "Grupo no disponible",
      "deviceUnavailable": "Dispositivo no disponible",
      "noDescription": "Sin descripcion registrada."
    }
  },
  "settings": {
    "shell": {
      "ariaLabel": "Configuracion de cuenta",
      "eyebrow": "Cuenta",
      "description": "Preferencias reales de usuario y plataforma."
    },
    "nav": {
      "profile": "Perfil",
      "profileDescription": "Datos personales",
      "account": "Cuenta",
      "accountDescription": "Identidad y estado",
      "security": "Seguridad",
      "securityDescription": "Acceso y recuperacion",
      "billing": "Facturacion",
      "billingDescription": "Plan, pagos y facturas",
      "platform": "Plataforma",
      "platformDescription": "Preferencias locales"
    },
    "common": {
      "email": "Correo",
      "notAvailable": "No disponible"
    },
    "profile": {
      "title": "Datos personales",
      "pageDescription": "Administra el nombre y correo que se guardan en tu cuenta de ElectroCorp.",
      "basicInfoTitle": "Informacion basica",
      "basicInfoDescription": "Estos datos se guardan en tu cuenta de ElectroCorp.",
      "saveProfile": "Guardar perfil",
      "previewEyebrow": "Vista previa",
      "dataUsageTitle": "Uso de estos datos",
      "dataUsageDescription": "Referencia rapida de como se reflejan nombre y correo dentro de ElectroCorp.",
      "visibleName": "Nombre visible",
      "visibleNameDescription": "Se usa para identificar tu cuenta en el panel lateral y en las vistas de usuario.",
      "accountEmail": "Correo de cuenta",
      "accountEmailDescription": "Es la referencia para recuperacion de acceso y datos principales del perfil.",
      "unsavedChanges": "Cambios sin guardar",
      "noPendingChanges": "Sin cambios pendientes",
      "statusDescription": "Guarda el perfil para sincronizar los datos con la sesion actual.",
      "updateSuccess": "Perfil actualizado.",
      "updateError": "No se pudo actualizar el perfil."
    },
    "account": {
      "title": "Estado de cuenta",
      "pageDescription": "Revisa la identidad, rol y estado general del usuario autenticado.",
      "identityDescription": "Resumen compacto del usuario autenticado.",
      "role": "Rol",
      "userFallback": "Usuario",
      "createdAt": "Creada",
      "contextEyebrow": "Contexto",
      "operationalReadingTitle": "Lectura operativa",
      "operationalReadingDescription": "Elementos que ayudan a entender el alcance de esta cuenta sin mezclar acciones sensibles.",
      "session": "Sesion",
      "active": "Activa",
      "sessionDescription": "Esta vista solo se muestra para un usuario autenticado por el guard de acceso.",
      "editableData": "Datos editables",
      "separatedInProfile": "Separados en Perfil",
      "editableDataDescription": "Nombre y correo se actualizan desde Perfil para mantener esta pagina como resumen de cuenta.",
      "sensitiveActions": "Acciones sensibles",
      "separatedInSecurity": "Separadas en Seguridad",
      "sensitiveActionsDescription": "Recuperacion y eliminacion de cuenta viven en una pantalla independiente para reducir errores."
    },
    "security": {
      "title": "Acceso y recuperacion",
      "pageDescription": "Gestiona acciones sensibles de acceso con llamadas reales disponibles.",
      "accessActionsTitle": "Acciones de acceso",
      "accessActionsDescription": "Solo se muestran acciones con llamada real disponible.",
      "recovery": "Recuperacion",
      "sendRecoveryTitle": "Enviar enlace al correo actual",
      "sendRecoveryDescription": "Usa el flujo real de recuperacion de acceso para el correo guardado en el perfil.",
      "sendRecoveryButton": "Enviar recuperacion",
      "dangerZone": "Zona delicada",
      "deleteAccountDescription": "Abre una confirmacion estricta antes de borrar la cuenta actual.",
      "protectionsEyebrow": "Protecciones",
      "securityStatusTitle": "Estado de seguridad",
      "securityStatusDescription": "Resumen de controles activos antes de ejecutar acciones sensibles.",
      "recoveryEmail": "Correo de recuperacion",
      "recoveryEmailDescription": "El enlace de recuperacion se solicita para el correo actual de la cuenta autenticada.",
      "writtenConfirmation": "Confirmacion escrita",
      "writtenConfirmationDescription": "La eliminacion queda bloqueada hasta que el texto de confirmacion coincida exactamente.",
      "finalConfirmation": "Confirmacion final",
      "dialogRequired": "Dialogo requerido",
      "finalConfirmationDescription": "La cuenta solo se elimina despues de aceptar el dialogo de confirmacion con tono de peligro.",
      "noRecoveryEmail": "No hay correo registrado para recuperar acceso.",
      "recoverySent": "Solicitud de recuperacion enviada.",
      "recoveryError": "No se pudo enviar la recuperacion.",
      "deleteDialogTitle": "Eliminar permanentemente tu cuenta",
      "deleteDialogMessage": "Esta accion elimina tu cuenta de ElectroCorp, cierra tu sesion y no puede deshacerse desde la interfaz.",
      "deleteDialogDetail": "Antes de continuar, revisa que no necesites recuperar acceso a datos, facturas o preferencias asociadas a esta cuenta.",
      "deleteDialogConfirm": "Eliminar cuenta permanentemente",
      "deleteDialogCancel": "No eliminar",
      "requiredTextLabel": "Para confirmar, escribe exactamente esta palabra:",
      "requiredTextHint": "El boton de eliminacion permanecera bloqueado hasta que la confirmacion coincida exactamente.",
      "deleteAccountError": "No se pudo eliminar la cuenta."
    },
    "platform": {
      "title": "Preferencias locales",
      "pageDescription": "Ajusta la apariencia e idioma de ElectroCorp en este navegador.",
      "browserExperienceTitle": "Experiencia del navegador",
      "browserExperienceDescription": "Se aplican en este navegador y se conservan localmente.",
      "darkMode": "Modo oscuro",
      "lightMode": "Modo claro",
      "darkModeDescription": "Cambia la apariencia de la interfaz en este navegador.",
      "language": "Idioma",
      "languages": {
        "es": "Espanol",
        "esDescription": "Idioma local de la interfaz.",
        "en": "English",
        "enDescription": "Local interface language.",
        "pt": "Portugues",
        "ptDescription": "Idioma local da interface."
      },
      "languageApplied": "Idioma aplicado en este navegador.",
      "appearanceApplied": "Apariencia aplicada en este navegador.",
      "localStatusEyebrow": "Estado local",
      "preferencesSummaryTitle": "Resumen de preferencias",
      "preferencesSummaryDescription": "Confirma como esta configurada la experiencia en este navegador.",
      "appearance": "Apariencia",
      "appearanceDescription": "La preferencia visual se aplica al shell y componentes compartidos de la interfaz.",
      "activeLanguage": "Idioma activo",
      "activeLanguageDescription": "El idioma seleccionado se conserva localmente para mantener la navegacion consistente.",
      "scope": "Alcance",
      "local": "Local",
      "scopeDescription": "Estos ajustes viven en el navegador actual y quedan listos para sincronizarse con preferencias de usuario."
    },
    "billing": {
      "title": "Plan y pagos",
      "pageDescription": "Administra tu suscripcion activa, revisa pagos registrados y descarga facturas emitidas para tu cuenta.",
      "subscriptionEyebrow": "Suscripcion",
      "planStatusTitle": "Estado del plan",
      "planStatusDescription": "Informacion real del plan activo, limites operativos y actividad de pago.",
      "perMonth": "al mes",
      "choosePlanDescription": "Elige un plan para activar la plataforma completa.",
      "exports": "Exportes",
      "activeExports": "Activos",
      "notIncluded": "No incluidos",
      "viewPlans": "Ver planes",
      "lastPayment": "Ultimo pago",
      "noPaymentsRegistered": "No hay pagos registrados.",
      "payments": "Pagos",
      "registeredMovements": "Movimientos registrados",
      "registeredMovementsDescription": "Pagos recibidos desde el flujo real de suscripcion.",
      "paymentNumber": "Pago #{{id}}",
      "method": "Metodo",
      "noPaymentsTitle": "No hay pagos",
      "noPaymentsDescription": "Los pagos apareceran cuando actives o renueves un plan.",
      "issuedDocuments": "Documentos emitidos",
      "issuedDocumentsDescription": "Descarga solo facturas existentes en tu cuenta.",
      "invoiceNumber": "Factura #{{id}}",
      "issuedOn": "Emitida el {{date}}",
      "download": "Descargar",
      "noInvoicesTitle": "No hay facturas",
      "noInvoicesDescription": "Cuando exista una factura emitida para tu cuenta, aparecera aqui.",
      "loadError": "No se pudo cargar el historial de facturacion.",
      "invoiceNotFound": "No se encontro la factura.",
      "downloadSuccess": "Factura descargada correctamente.",
      "downloadError": "No se pudo descargar la factura.",
      "cancelPlanTitle": "Cancelar plan",
      "cancelPlanMessage": "Tu suscripcion activa quedara cancelada. Confirma solo si ya no quieres continuar con el plan.",
      "cancelPlanConfirm": "Cancelar plan",
      "keepPlan": "Conservar plan",
      "cancelSuccess": "Plan cancelado.",
      "cancelError": "No se pudo cancelar el plan.",
      "unlimited": "Ilimitado",
      "noActivePlan": "Sin plan activo",
      "subscriptionStatuses": {
        "active": "Activo",
        "cancelled": "Cancelado",
        "expired": "Vencido"
      },
      "paymentStatuses": {
        "approved": "Aprobado",
        "rejected": "Rechazado",
        "pending": "Pendiente"
      },
      "invoiceIssued": "Emitida",
      "invoiceRegistered": "Registrada",
      "goToPlans": "Ir a planes"
    }
  }
}


============================================================
FILE: public\assets\i18n\pt.json
============================================================

{
  "app": {
    "name": "ElectroCorp"
  },
  "nav": {
    "home": "Início",
    "about": "Sobre",
    "userSettings": "Ajustes",
    "devices": "Dispositivos",
    "routines": "Rotinas",
    "energy": "Energia",
    "history": "Histórico",
    "plans": "Planos",
    "alerts": "Alertas",
    "billing": "Faturamento",
    "deviceGroups": "Grupos",
    "alertRules": "Regras de alerta",
    "notificationPreferences": "Preferências de notificação",
    "locations": "Localizações",
    "rooms": "Ambientes",
    "assignments": "Atribuições",
    "reports": "Relatórios",
    "energyGoals": "Metas energéticas",
    "supportTickets": "Suporte",
    "maintenanceTickets": "Manutencao",
    "operation": "Operação",
    "energySection": "Energia",
    "alertsSection": "Alertas",
    "workplace": "Espaços",
    "support": "Suporte",
    "spaces": "Espacos",
    "settings": "Settings",
    "operationModes": "Modos",
    "consumption": "Consumo",
    "inbox": "Caixa",
    "rules": "Regras",
    "preferences": "Preferencias"
  },
  "onboarding": {
    "locationRequiredEyebrow": "Localizacao requerida",
    "locationRequiredTitle": "Crie uma localizacao para comecar a usar este recurso.",
    "locationRequiredDescription": "Voce ainda nao tem uma localizacao configurada. Crie sua primeira localizacao para organizar ambientes, dispositivos, rotinas, metas, alertas e relatorios.",
    "createLocationAction": "Criar localizacao"
  },
  "auth": {
    "loginEyebrow": "Bem-vindo de volta",
    "loginTitle": "Entrar",
    "loginSubtitle": "Acesse seu painel da ElectroCorp para gerenciar dispositivos, rotinas e consumo de energia.",
    "registerEyebrow": "Criar acesso",
    "registerTitle": "Registrar-se",
    "registerSubtitle": "Crie sua conta da ElectroCorp para começar a gerenciar dispositivos inteligentes de energia.",
    "email": "E-mail",
    "password": "Senha",
    "fullName": "Nome completo",
    "signIn": "Entrar",
    "signUp": "Criar conta",
    "createAccount": "Criar conta",
    "backToLogin": "Voltar ao login",
    "logout": "Sair",
    "signInError": "Não foi possível entrar. Verifique seu e-mail e senha.",
    "signUpError": "Não foi possível registrar o usuário.",
    "emailAlreadyExists": "Este e-mail já está registrado.",
    "fullNameRequired": "Digite seu nome completo.",
    "invalidEmail": "Digite um endereço de e-mail válido.",
    "invalidEmailProvider": "Apenas e-mails Gmail, Outlook ou Hotmail são permitidos.",
    "passwordTooShort": "A senha deve ter pelo menos 8 caracteres.",
    "forgotPassword": "Esqueceu sua senha?",
    "recoverPasswordEyebrow": "Recuperação de conta",
    "recoverPasswordTitle": "Recuperar senha",
    "recoverPasswordSubtitle": "Digite seu e-mail e enviaremos instruções de recuperação.",
    "sendRecoveryEmail": "Enviar e-mail de recuperação",
    "recoverPasswordSent": "E-mail de recuperação enviado com sucesso.",
    "recoverPasswordError": "Não foi possível enviar o e-mail de recuperação.",
    "profileUpdated": "Perfil atualizado com sucesso.",
    "profileUpdateError": "Não foi possível atualizar o perfil.",
    "deleteAccountError": "Não foi possível excluir a conta.",
    "deleteAccountConfirmationError": "O texto de confirmação não é válido."
  },
  "home": {
    "eyebrow": "Energia inteligente",
    "title": "Controle sua energia com a ElectroCorp",
    "content": "Monitore dispositivos, automatize rotinas e revise o consumo energético a partir de uma plataforma moderna.",
    "manageDevices": "Gerenciar dispositivos",
    "viewEnergy": "Ver energia",
    "cardDevices": "Dispositivos",
    "cardRoutines": "Rotinas",
    "cardConsumption": "Consumo atual",
    "cardAlerts": "Alertas",
    "activeDevices": "dispositivos ativos",
    "enabledRoutines": "rotinas ativas",
    "averageConsumption": "Média",
    "unreadAlerts": "alertas não lidos",
    "energyOverview": "Resumo energético",
    "subscriptionOverview": "Resumo da assinatura",
    "alertOverview": "Resumo de alertas",
    "openEnergyDashboard": "Abrir painel energético",
    "openPlans": "Ver planos",
    "openAlerts": "Ver alertas",
    "noPlanDescription": "Selecione um plano para ativar os benefícios da ElectroCorp.",
    "alertDescription": "Revise alertas de consumo, automação e eventos importantes do sistema.",
    "setupHint": "Selecione ou crie uma sede para focar o Inicio.",
    "setupEmpty": "O Inicio e ativado quando existe uma sede.",
    "operationalCenterTitle": "Centro operacional",
    "operationalCenterDescription": "Visão resumida da sede ativa, seus espaços, operação, consumo, alertas e serviço.",
    "loadingEyebrow": "Carregando",
    "loadingTitle": "Sincronizando informações operacionais",
    "loadingDescription": "Estamos lendo sedes, ambientes, dispositivos, energia, metas, alertas e serviço.",
    "mainSummaryAria": "Resumo principal",
    "activeSiteEyebrow": "Sede ativa",
    "noActiveSite": "Sem sede ativa",
    "noRegisteredLocation": "Sem localização registrada",
    "go": "Ir",
    "noSitesTitle": "Não há sedes configuradas",
    "noSitesDescription": "Crie uma sede para organizar ambientes, dispositivos e consumo por contexto.",
    "consumption": "Consumo",
    "generalStatusEyebrow": "Estado geral",
    "siteSummaryTitle": "Resumo da sede",
    "noOperationalContextTitle": "Sem contexto operacional",
    "siteSummaryDescription": "Dados calculados a partir dos elementos atribuídos à sede ativa.",
    "missingSiteTitle": "Falta uma sede",
    "missingSiteDescription": "O resumo operacional precisa de uma sede ativa.",
    "criticalAlerts": "Alertas críticos",
    "openSupport": "Suporte aberto",
    "siteRoomsTitle": "Ambientes da sede",
    "roomsRegisteredDescription": "{{count}} ambiente(s) registrados no contexto ativo.",
    "noActiveSiteRoomsDescription": "Crie ou selecione uma sede para ver ambientes.",
    "noRoomsTitle": "Não há ambientes",
    "noRoomsDescription": "Crie ambientes para posicionar dispositivos em zonas reais.",
    "withDevices": "Com equipamentos",
    "withoutDevices": "Sem equipamentos",
    "averageDevicesShort": "Méd. disp.",
    "assignedEquipmentTitle": "Equipamentos atribuídos",
    "assignedEquipmentDescription": "Total, ativos e consumo vêm dos dispositivos vinculados à sede.",
    "noAssignedDevicesTitle": "Não há dispositivos atribuídos",
    "noAssignedDevicesDescription": "Registre ou atribua equipamentos para alimentar consumo, grupos e rotinas.",
    "groupSetsTitle": "Conjuntos",
    "groupsDescription": "Dispositivos agrupados por uso ou zona.",
    "noGroupsTitle": "Sem grupos",
    "noGroupsDescription": "Crie grupos para operar vários dispositivos juntos.",
    "deviceCountShort": "{{count}} disp.",
    "moreGroups": "+{{count}} mais em grupos",
    "scheduledAlarmsTitle": "Alarmes programados",
    "scheduledAlarmsDescription": "Ações globais habilitadas para a sede.",
    "noActiveRoutinesTitle": "Sem rotinas ativas",
    "noActiveRoutinesDescription": "Programe ações por hora para dispositivos, grupos ou ambientes.",
    "moreRoutines": "+{{count}} mais em rotinas",
    "activeModeTitle": "Modo ativo",
    "activeModeDescription": "Configuração operacional própria da sede.",
    "affectedDevices": "{{count}} dispositivo(s) afetados",
    "noActiveModeTitle": "Sem modo ativo",
    "noActiveModeDescription": "Os modos ativos da sede aparecerão aqui.",
    "createMode": "Criar modo",
    "energyRecordedTitle": "Energia registrada",
    "energyRecordedDescription": "Leituras reais associadas a dispositivos atribuídos.",
    "noReadingsTitle": "Sem leituras",
    "noReadingsDescription": "Quando houver leituras de energia, o resumo aparecerá aqui.",
    "today": "Hoje",
    "readings": "Leituras",
    "peak": "Pico",
    "goalsEyebrow": "Metas",
    "activeGoalsTitle": "Objetivos ativos",
    "activeGoalsDescription": "Metas vigentes filtradas por sede, ambiente, grupo ou dispositivo.",
    "noActiveGoalsTitle": "Sem metas ativas",
    "noActiveGoalsDescription": "Crie metas para comparar consumo com objetivos reais.",
    "recentInboxTitle": "Caixa recente",
    "recentInboxDescription": "Alertas relacionados à sede e seus elementos.",
    "noRecentAlertsTitle": "Sem alertas recentes",
    "noRecentAlertsDescription": "Não há alertas para mostrar nesta sede.",
    "criticalCount": "Críticos: {{count}}",
    "accountSupportTitle": "Suporte da conta",
    "accountSupportDescription": "Tickets abertos e manutenções relacionadas aos dispositivos da sede.",
    "noOpenTicketsTitle": "Sem tickets abertos",
    "noOpenTicketsDescription": "Casos de suporte ou manutenção aparecerão aqui quando existirem.",
    "createSupport": "Criar suporte",
    "noRoom": "Sem ambiente",
    "roomNumber": "Ambiente #{{id}}",
    "deviceStatusOn": "ON",
    "deviceStatusOff": "OFF",
    "locationTypes": {
      "home": "Casa",
      "business": "Empresa",
      "branch": "Filial"
    },
    "alertLevels": {
      "success": "Sucesso",
      "stable": "Estável",
      "info": "Neutro",
      "warning": "Atenção",
      "critical": "Crítico"
    }
  },
  "about": {
    "eyebrow": "ElectroCorp em contexto",
    "title": "Uma plataforma para organizar energia, espaços e decisões",
    "subtitle": "A ElectroCorp ajuda a usar energia com mais clareza: organiza sedes e ambientes, controla dispositivos, automatiza rotinas e modos, revisa consumo, metas, alertas, serviço, faturamento e preferências em um só lugar.",
    "actions": {
      "home": "Ir para Inicio",
      "spaces": "Ir para Espaços",
      "energy": "Ir para Energia"
    },
    "heroSignals": {
      "order": {
        "label": "Ordem",
        "title": "Espaços antes de ações",
        "description": "A plataforma funciona melhor quando primeiro você define sedes, ambientes e atribuições reais."
      },
      "control": {
        "label": "Controle",
        "title": "Dispositivos com contexto",
        "description": "Dispositivos, grupos, rotinas e modos ficam mais claros pelo lugar e pelo uso diário que representam."
      },
      "clarity": {
        "label": "Clareza",
        "title": "Energia visível",
        "description": "Consumo, relatórios e metas dependem de leituras reais para orientar melhores decisões."
      }
    },
    "what": {
      "eyebrow": "O que e",
      "title": "A ElectroCorp conecta espaços, dispositivos e energia",
      "description": "Não é uma tela decorativa nem uma lista isolada de ações. É uma plataforma para administrar espaços, controlar equipamentos, criar automações úteis, revisar consumo, receber alertas e acompanhar suporte, manutenção, faturamento e preferências.",
      "points": {
        "spaces": "Sedes, ambientes e atribuições como base do sistema.",
        "control": "Dispositivos, grupos, rotinas e modos para a operação diária.",
        "energy": "Consumo, histórico, relatórios e metas com base na informação disponível.",
        "care": "Alertas, regras, serviço e preferências para agir com ordem."
      }
    },
    "values": {
      "mission": {
        "eyebrow": "Missão",
        "title": "Ajudar a usar energia com ordem, controle e clareza",
        "description": "A ElectroCorp foi pensada para casas, pequenas sedes e espaços médios que precisam saber onde estão seus equipamentos, como são usados e que decisões tomar para consumir melhor."
      },
      "vision": {
        "eyebrow": "Visão",
        "title": "Uma plataforma energética inteligente, organizada e preventiva",
        "description": "A visão é crescer como uma ferramenta que conecta operação, consumo, alertas e serviço com cada vez mais precisão, sem perder transparência sobre o que realmente está disponível."
      }
    },
    "daily": {
      "eyebrow": "Uso diário",
      "title": "Um fluxo prático para operar a ElectroCorp",
      "description": "O uso recomendado começa pelo espaço físico e avança para controle, automação, revisão energética e acompanhamento.",
      "items": {
        "site": {
          "title": "Escolha ou crie uma sede ativa",
          "description": "Trabalhe a partir de uma sede clara para que ambientes, dispositivos e ações tenham contexto."
        },
        "rooms": {
          "title": "Organize ambientes",
          "description": "Use ambientes reais para localizar equipamentos e manter o mapa operacional compreensível."
        },
        "devices": {
          "title": "Revise dispositivos",
          "description": "Ligue, desligue e valide o estado dos equipamentos pelas telas de operação."
        },
        "groups": {
          "title": "Agrupe por uso",
          "description": "Crie grupos semânticos como iluminação, climatização ou equipamentos críticos para agir por conjunto."
        },
        "routines": {
          "title": "Programe rotinas repetíveis",
          "description": "Use rotinas para horários frequentes e evite duplicar ações manuais todos os dias."
        },
        "mode": {
          "title": "Aplique modos completos",
          "description": "Os modos ajudam a ativar situações completas, como economia, noite ou trabalho, com rotinas internas."
        },
        "energy": {
          "title": "Revise consumo",
          "description": "Consulte o painel e o histórico para entender o comportamento energético disponível."
        },
        "goals": {
          "title": "Compare metas",
          "description": "Use metas energéticas para medir avanço contra objetivos definidos por alcance."
        },
        "alerts": {
          "title": "Atenda alertas",
          "description": "Leia alertas visíveis e use regras claras para prevenir problemas ou consumos fora de faixa."
        },
        "service": {
          "title": "Acompanhe serviço",
          "description": "Registre suporte ou manutenção quando precisar deixar registro e avançar por estado."
        },
        "billing": {
          "title": "Revise faturamento e preferências",
          "description": "Consulte plano, pagamentos, perfil e preferências disponíveis sem assumir funções externas não visíveis."
        }
      }
    },
    "recommendations": {
      "eyebrow": "Recomendações reais",
      "title": "Como obter valor sem configurar demais",
      "description": "A ElectroCorp funciona melhor quando nomes, alcances e regras refletem a operação real.",
      "items": {
        "site": {
          "title": "Crie primeiro a sede",
          "description": "A sede ativa evita que os demais dados fiquem soltos ou difíceis de ler."
        },
        "rooms": {
          "title": "Defina ambientes antes de operar",
          "description": "Um ambiente bem nomeado ajuda a encontrar dispositivos, rotinas e manutenção."
        },
        "assignments": {
          "title": "Atribua dispositivos com cuidado",
          "description": "A atribuição correta melhora o controle e evita confusão quando há muitos equipamentos."
        },
        "groups": {
          "title": "Use grupos semânticos",
          "description": "Agrupe por função real, não apenas por conveniência momentânea."
        },
        "routines": {
          "title": "Rotinas para horários repetidos",
          "description": "Se uma ação acontece com frequência, uma rotina costuma ser melhor do que repeti-la manualmente."
        },
        "modes": {
          "title": "Modos para situações completas",
          "description": "Um modo deve representar uma cena operacional completa, não uma ação isolada."
        },
        "energy": {
          "title": "Revise energia e metas juntas",
          "description": "O consumo explica o presente; as metas ajudam a decidir o que mudar."
        },
        "alerts": {
          "title": "Use alertas para prevenir",
          "description": "Alertas são mais úteis quando revisados a tempo e ligados a uma ação clara."
        },
        "rules": {
          "title": "Não exagere nas regras",
          "description": "Poucas regras bem definidas costumam ser mais úteis que muitas regras difíceis de manter."
        },
        "names": {
          "title": "Nomes claros",
          "description": "Evite nomes ambíguos. Um bom nome reduz erros na operação e no acompanhamento."
        }
      }
    },
    "capabilities": {
      "eyebrow": "O que ja faz bem",
      "title": "Capacidades conectadas na experiência",
      "description": "Estas áreas já existem na plataforma e são apresentadas como funções disponíveis, não como promessas futuras.",
      "items": {
        "spaces": {
          "eyebrow": "Espaços",
          "title": "Sedes, ambientes e atribuições",
          "description": "Permite organizar a base física sobre a qual a operação é controlada."
        },
        "control": {
          "eyebrow": "Operação",
          "title": "Dispositivos, grupos e rotinas",
          "description": "Permite operar equipamentos, agrupá-los e programar ações repetíveis."
        },
        "modes": {
          "eyebrow": "Modos",
          "title": "Situações com rotinas internas",
          "description": "Permite organizar ações completas para momentos concretos do dia."
        },
        "energy": {
          "eyebrow": "Energia",
          "title": "Consumo, histórico, relatórios e metas",
          "description": "Permite ler informação energética disponível e compará-la com objetivos."
        },
        "alerts": {
          "eyebrow": "Alertas",
          "title": "Bandeja, regras e preferências visíveis",
          "description": "Permite revisar alertas e ajustar regras ou preferências visíveis da experiência."
        },
        "service": {
          "eyebrow": "Serviço",
          "title": "Suporte e manutenção",
          "description": "Permite criar solicitações e avançar seu estado sem adicionar ações indisponíveis."
        },
        "settings": {
          "eyebrow": "Conta",
          "title": "Perfil, faturamento e ajustes",
          "description": "Permite revisar informação de usuário, plano, pagamentos e preferências disponíveis."
        }
      }
    },
    "careful": {
      "eyebrow": "Uso com critério",
      "title": "Onde convém ser transparente",
      "description": "Algumas partes dependem de dados reais, regras claras ou informação disponível por plano. Por isso são mostradas com alcance honesto.",
      "items": {
        "automation": {
          "title": "Automações",
          "description": "Devem ser criadas com horários e alcances claros para evitar ações inesperadas."
        },
        "alerts": {
          "title": "Alertas e regras",
          "description": "Funcionam melhor com faixas compreensíveis e poucas regras fáceis de revisar."
        },
        "reports": {
          "title": "Relatórios",
          "description": "O valor dos relatórios depende de leituras reais e filtros bem escolhidos."
        },
        "billing": {
          "title": "Faturamento",
          "description": "A informação visível depende do plano, pagamentos e histórico disponíveis dentro da plataforma."
        }
      }
    }
  },
  "notFound": {
    "title": "Página não encontrada",
    "content": "A rota inserida não existe ou foi movida.",
    "goHome": "Ir ao início"
  },
  "devices": {
    "eyebrow": "Controle de dispositivos",
    "title": "Controle inteligente de dispositivos",
    "subtitle": "Gerencie tomadas, interruptores e luzes inteligentes a partir de uma visão centralizada.",
    "total": "Total",
    "active": "Ativos",
    "currentWatts": "Consumo atual",
    "addTitle": "Adicionar dispositivo",
    "name": "Nome",
    "room": "Ambiente",
    "type": "Tipo",
    "power": "Potência em watts",
    "create": "Criar dispositivo",
    "turnOn": "Ligar",
    "turnOff": "Desligar",
    "emptyTitle": "Ainda não há dispositivos",
    "emptyContent": "Adicione sua primeira tomada, interruptor ou luz inteligente para começar.",
    "loadError": "Não foi possível carregar os dispositivos.",
    "createError": "Não foi possível criar o dispositivo.",
    "updateError": "Não foi possível atualizar o dispositivo.",
    "deleteError": "Não foi possível excluir o dispositivo.",
    "planLimitReached": "Você atingiu o limite de dispositivos permitido pelo seu plano.",
    "createSuccess": "Dispositivo criado com sucesso.",
    "pageDescription": "Controle os equipamentos atribuidos ao local ativo e registre novos dispositivos operacionais por ambiente.",
    "activeSiteFallback": "local ativo",
    "noRoom": "Sem ambiente",
    "go": "Ir",
    "summary": {
      "assignedTitle": "Atribuidos",
      "onTitle": "Ligados",
      "offDevices": "{{count}} desligado(s).",
      "consumptionEyebrow": "Consumo",
      "currentTitle": "Atual",
      "activePowerDescription": "Potencia ativa deste local."
    },
    "toolbar": {
      "eyebrow": "Controle diario",
      "title": "Equipamentos por ambiente",
      "description": "Ligue, desligue ou exclua dispositivos com avisos de impacto real."
    },
    "emptyRooms": {
      "title": "Nao ha ambientes no local ativo",
      "description": "Crie um ambiente antes de registrar dispositivos operacionais."
    },
    "emptyAssigned": {
      "title": "Nao ha dispositivos atribuidos",
      "description": "Registre um novo dispositivo ou revise as atribuicoes do local ativo."
    },
    "form": {
      "eyebrow": "Novo dispositivo",
      "description": "Registre um equipamento real, seu ambiente e sua potencia nominal.",
      "namePlaceholder": "Ex. Luz principal",
      "wattsLabel": "Watts do dispositivo"
    },
    "powerAdvice": {
      "highTitle": "Consumo alto",
      "normalTitle": "Monitoramento controlado",
      "highMessage": "Para {{type}}, este valor e considerado alto. Se o dispositivo reportar uso igual ou maior que {{watts}} W, a ElectroCorp deve marca-lo como consumo alto e podera gerar um alerta.",
      "normalMessage": "Informe apenas os watts nominais de {{type}}. A ElectroCorp calcula kWh e custo com esses W e o tempo real ligado."
    },
    "status": {
      "on": "Ligado",
      "off": "Desligado",
      "maintenance": "Manutencao",
      "removed": "Removido",
      "unknown": "Sem estado"
    },
    "card": {
      "activeConsumption": "Consumo ativo",
      "standby": "Em espera",
      "currentConsumptionDescription": "Consumo atual do dispositivo",
      "noActiveConsumption": "Sem consumo ativo"
    },
    "validation": {
      "nameRequired": "Digite o nome do dispositivo.",
      "nameMinLength": "O nome do dispositivo deve ter pelo menos 3 caracteres.",
      "roomRequired": "Selecione um ambiente do local ativo.",
      "powerRequired": "A potencia do dispositivo deve ser maior que 0 W.",
      "roomRequiredBeforePreset": "Crie um ambiente no local ativo antes de adicionar dispositivos."
    },
    "toasts": {
      "createdAndAssigned": "Dispositivo criado e atribuido a {{room}}.",
      "createdWithoutAssignment": "Dispositivo criado, mas nao foi possivel atribui-lo ao ambiente ativo.",
      "presetCreatedAndAssigned": "{{device}} adicionado e atribuido a {{room}}.",
      "presetCreatedWithoutAssignment": "{{device}} foi criado, mas nao foi possivel atribui-lo ao ambiente ativo.",
      "presetCreateError": "Nao foi possivel adicionar {{device}}.",
      "turnedOn": "Dispositivo ligado.",
      "turnedOff": "Dispositivo desligado.",
      "deletedHistoryKept": "Dispositivo excluido; o historico foi preservado.",
      "removedRoutineSingular": "Sua rotina associada tambem foi removida.",
      "removedRoutinePlural": "{{count}} rotinas associadas tambem foram removidas."
    },
    "deleteFallbackDevice": "Este dispositivo",
    "deleteWarnings": {
      "groupsTitle": "Revisar grupos",
      "groupSingular": "{{device}} faz parte do grupo {{group}}. Se voce exclui-lo, esse grupo perdera este dispositivo.",
      "groupPlural": "{{device}} faz parte de {{count}} grupos. Se voce exclui-lo, esses grupos perderao este dispositivo.",
      "routinesTitle": "Revisar rotinas",
      "routineSingular": "{{device}} tem uma rotina associada. Se voce excluir {{device}}, sua rotina desaparecera.",
      "routinePlural": "{{device}} tem {{count}} rotinas associadas. Se voce excluir {{device}}, suas rotinas desaparecerao.",
      "modesTitle": "Revisar modos",
      "modeSingular": "{{device}} esta relacionado a um modo existente. Exclui-lo pode afetar essa configuracao.",
      "modePlural": "{{device}} esta relacionado a {{count}} modos existentes. Exclui-lo pode afetar essas configuracoes.",
      "deleteTitle": "Excluir dispositivo",
      "deleteMessage": "{{device}} sera excluido. Esta acao nao afeta dados historicos ja registrados."
    }
  },
  "routines": {
    "eyebrow": "Automação",
    "title": "Rotinas automáticas",
    "subtitle": "Programe ações para ligar ou desligar dispositivos inteligentes conforme horários definidos.",
    "total": "Total",
    "active": "Ativas",
    "inactive": "Inativa",
    "addTitle": "Criar rotina",
    "name": "Nome",
    "device": "Dispositivo",
    "selectDevice": "Selecione um dispositivo",
    "action": "Ação",
    "time": "Horário",
    "create": "Criar rotina",
    "enable": "Ativar",
    "disable": "Desativar",
    "emptyTitle": "Ainda não há rotinas",
    "emptyContent": "Crie uma rotina para automatizar seus dispositivos.",
    "loadError": "Não foi possível carregar as rotinas.",
    "createError": "Não foi possível criar a rotina.",
    "updateError": "Não foi possível atualizar a rotina.",
    "deleteError": "Não foi possível excluir a rotina.",
    "conflictError": "Já existe uma rotina ativa para esse dispositivo no mesmo horário.",
    "planLimitReached": "Você atingiu o limite de rotinas permitido pelo seu plano.",
    "createSuccess": "Rotina criada com sucesso.",
    "pageDescription": "Programe alarmes operacionais para dispositivos, grupos, cômodos ou sedes.",
    "createOperationalSuccess": "Rotina criada como alarme operacional.",
    "summary": {
      "scheduled": "Programadas",
      "inActiveSite": "na sede ativa.",
      "pausedCount": "{{count}} pausada(s).",
      "newRoutine": "Nova rotina"
    },
    "toolbar": {
      "eyebrow": "Alarmes programados",
      "title": "Rotinas globais",
      "description": "Estas rotinas são independentes das rotinas internas dos modos."
    },
    "emptyScheduledTitle": "Não há rotinas programadas",
    "emptyScheduledDescription": "Crie um alarme operacional para ligar ou desligar alvos em um horário definido.",
    "targetTypes": {
      "device": "Dispositivo",
      "group": "Grupo",
      "room": "Cômodo",
      "workplace": "Sede"
    },
    "targetTypeDescriptions": {
      "device": "Rotina para um único equipamento",
      "group": "Rotina para vários dispositivos",
      "room": "Rotina para os equipamentos de um cômodo",
      "workplace": "Rotina para um centro energético completo"
    },
    "targetOptions": {
      "deviceCount": "{{count}} dispositivos"
    },
    "actions": {
      "turnOn": "Ligar",
      "turnOff": "Desligar"
    },
    "actionDescriptions": {
      "turnOn": "Ativar automaticamente",
      "turnOff": "Desativar automaticamente"
    },
    "repeat": {
      "once": "Uma vez",
      "daily": "Diário",
      "weekly": "Semanal",
      "customInterval": "A cada X dias"
    },
    "repeatDescriptions": {
      "once": "Executar apenas na data escolhida",
      "daily": "Repetir todos os dias",
      "weekly": "Escolher dias específicos",
      "customInterval": "Repetir por intervalo"
    },
    "weekDays": {
      "mon": "Segunda-feira",
      "tue": "Terça-feira",
      "wed": "Quarta-feira",
      "thu": "Quinta-feira",
      "fri": "Sexta-feira",
      "sat": "Sábado",
      "sun": "Domingo",
      "monShort": "Seg",
      "tueShort": "Ter",
      "wedShort": "Qua",
      "thuShort": "Qui",
      "friShort": "Sex",
      "satShort": "Sáb",
      "sunShort": "Dom"
    },
    "placeholders": {
      "device": "Selecione um dispositivo",
      "group": "Selecione um grupo",
      "room": "Selecione um cômodo",
      "workplace": "Selecione uma sede"
    },
    "form": {
      "description": "Defina alvo, ação, horário e repetição como um alarme global.",
      "namePlaceholder": "Ex. Desligar luzes à noite",
      "target": "Alvo",
      "targetTypePlaceholder": "Tipo de alvo",
      "repetition": "Repetição",
      "scheduledTime": "Horário programado",
      "days": "Dias",
      "weeklyDaysAria": "Dias de repetição semanal",
      "everyDays": "A cada quantos dias",
      "daysSuffix": "dias"
    },
    "status": {
      "active": "Ativa",
      "inactive": "Inativa",
      "paused": "Pausada"
    },
    "card": {
      "time": "Hora",
      "detailsAria": "Detalhes da rotina",
      "applicable": "Aplicáveis",
      "blocked": "Bloqueados",
      "onceWithDate": "Uma vez - {{date}}",
      "everyDays": "A cada {{count}} dia(s)",
      "weeklyWithDays": "Semanal - {{days}}",
      "weeklyGeneric": "semanalmente",
      "weeklyDaysText": "nos dias {{days}}",
      "waitingOnceDate": "Programada para {{date}} às {{time}}.",
      "waitingOnce": "Programada para uma execução única às {{time}}.",
      "waitingWeekly": "Será ativada {{days}} às {{time}}.",
      "waitingInterval": "Será repetida a cada {{count}} dia(s) às {{time}}.",
      "waitingDaily": "Será repetida todos os dias às {{time}}.",
      "pausedOnceDate": "Pausada. Não será ativada em {{date}}.",
      "pausedWeekly": "Pausada. Repetição semanal suspensa.",
      "pausedInterval": "Pausada. Intervalo suspenso.",
      "pausedDaily": "Pausada. Repetição diária suspensa."
    },
    "validation": {
      "activeSiteRequired": "Selecione uma sede ativa antes de criar rotinas.",
      "weeklyDayRequired": "Selecione pelo menos um dia para a rotina semanal."
    },
    "conflict": {
      "title": "Revisar rotina",
      "message": "Já existe uma rotina ativa com o mesmo alvo, horário e ação. Você pode continuar se quiser repetir essa programação."
    },
    "delete": {
      "title": "Excluir rotina",
      "message": "{{name}} será excluída. A programação deixará de ser ativada.",
      "fallbackName": "esta rotina"
    },
    "toasts": {
      "activated": "Rotina ativada.",
      "paused": "Rotina pausada.",
      "deleted": "Rotina excluída."
    },
    "list": {
      "empty": "Você ainda não tem rotinas criadas.",
      "groupFallback": "Grupo #{{id}}",
      "deviceFallback": "Dispositivo #{{id}}"
    }
  },
  "footer": {
    "rights": "Todos os direitos reservados."
  },
  "energy": {
    "eyebrow": "Monitoramento de energia",
    "title": "Monitoramento do consumo energético",
    "subtitle": "Visualize o consumo dos seus dispositivos, filtre o histórico e exporte suas leituras para análise.",
    "exportCsv": "Exportar CSV",
    "totalWatts": "Watts totais",
    "averageWatts": "Média",
    "highestReading": "Pico máximo",
    "startDate": "Data inicial",
    "endDate": "Data final",
    "filter": "Filtrar",
    "reset": "Redefinir",
    "recommendationTitle": "Recomendação de economia",
    "analytics": "Analítica",
    "chartTitle": "Histórico de consumo",
    "emptyReadings": "Não há leituras energéticas disponíveis.",
    "loadError": "Não foi possível carregar as leituras energéticas.",
    "noDataRecommendation": "Ainda não há dados suficientes para gerar uma recomendação.",
    "highRecommendation": "Seu consumo energético está alto. Revise os dispositivos de maior consumo.",
    "moderateRecommendation": "Seu consumo energético é moderado. Revise os horários de pico para melhorar a economia.",
    "lowRecommendation": "Seu consumo energético permanece baixo. Continue otimizando suas rotinas.",
    "exportNotAllowed": "Seu plano atual não permite exportar relatórios CSV.",
    "highReadings": "Leituras altas",
    "invalidDateRange": "O intervalo de datas selecionado não é válido.",
    "exportSuccess": "CSV exportado com sucesso."
  },
  "history": {
    "eyebrow": "Histórico energético",
    "title": "Histórico de consumo",
    "subtitle": "Revise as leituras registradas, detecte picos de consumo e exporte as informações para relatórios.",
    "registeredReadings": "Leituras registradas",
    "records": "registros",
    "device": "Dispositivo",
    "watts": "Watts",
    "date": "Data",
    "status": "Estado",
    "high": "Alto",
    "normal": "Normal",
    "filterTitle": "Filtrar leituras",
    "filterDescription": "Selecione um intervalo de datas para revisar o consumo energético registrado.",
    "registeredReadingsDescription": "Histórico de leituras energéticas ordenadas por data de registro.",
    "advancedFiltersAria": "Filtros avançados do histórico",
    "scope": {
      "label": "Escopo",
      "active": "Sede ativa",
      "activeDescription": "Somente leituras de {{site}}",
      "activeFallbackDescription": "Use a sede selecionada no Início",
      "global": "Global",
      "globalDescription": "Todas as leituras preservadas do usuário"
    },
    "scopeSummary": {
      "global": "Histórico global de todas as suas sedes.",
      "noActiveSite": "Selecione uma sede ativa para ver suas leituras.",
      "activeSite": "Histórico isolado por sede ativa: {{site}}."
    },
    "deviceNamePlaceholder": "Nome do dispositivo",
    "deviceId": "ID do dispositivo",
    "deviceIdPlaceholder": "Ex. 3",
    "readingId": "ID da leitura",
    "readingIdPlaceholder": "Ex. 15",
    "minWatts": "Watts mín.",
    "maxWatts": "Watts máx.",
    "sortBy": "Ordenar por",
    "clearFilters": "Limpar filtros",
    "emptyEyebrow": "Sem leituras",
    "emptyTitle": "Nenhuma leitura encontrada",
    "emptyDescription": "Ajuste os filtros para revisar outros registros de consumo energético.",
    "energy": "Energia",
    "estimatedCost": "Custo estimado",
    "time": "Hora",
    "interval": "Intervalo",
    "readingMeta": "ID #{{deviceId}} - Leitura #{{readingId}}",
    "showing": "Mostrando",
    "of": "de",
    "readings": "leituras",
    "pageOf": "Página {{page}} de {{total}}",
    "previous": "Anterior",
    "next": "Seguinte",
    "startDate": "Data inicial",
    "endDate": "Data final",
    "reset": "Redefinir",
    "statusOptions": {
      "all": "Todos",
      "normal": "Normal",
      "high": "Alto"
    },
    "sortOptions": {
      "newest": "Mais recentes",
      "oldest": "Mais antigas",
      "wattsDesc": "Maior consumo",
      "wattsAsc": "Menor consumo",
      "costDesc": "Maior custo"
    },
    "toasts": {
      "exportSuccess": "Histórico exportado.",
      "exportError": "Não foi possível exportar o histórico."
    },
    "emptyValues": {
      "noDate": "Sem data",
      "noTime": "Sem hora",
      "undefined": "Não definido"
    }
  },
  "billing": {
    "eyebrow": "Billing",
    "title": "Planos de serviço",
    "subtitle": "Escolha o plano que melhor se adapta à sua casa, negócio ou múltiplos locais. Não existe plano gratuito; todos os planos são orientados à operação real.",
    "month": "mês",
    "subscribe": "Assinar",
    "selected": "Selecionado",
    "currentPlan": "Plano atual",
    "activeSubscription": "Assinatura ativa",
    "noPlan": "Sem plano",
    "cancel": "Cancelar plano",
    "loadError": "Não foi possível carregar as informações dos planos.",
    "subscribeError": "Não foi possível processar a assinatura.",
    "cancelError": "Não foi possível cancelar a assinatura.",
    "alreadySubscribed": "Você já está inscrito neste plano.",
    "paymentEyebrow": "Confirmação de pagamento",
    "paymentTitle": "Confirmar método de pagamento",
    "selectedPlan": "Plano selecionado",
    "amountToPay": "Valor a pagar",
    "holderName": "Nome do titular",
    "cardNumber": "Número do cartão",
    "expirationDate": "Data de vencimento",
    "cvv": "CVV",
    "confirmPayment": "Confirmar pagamento",
    "cancelPayment": "Cancelar",
    "invalidCardNumber": "O número do cartão não é válido.",
    "invalidCvv": "O CVV não é válido.",
    "invalidHolderName": "Digite o nome do titular.",
    "lastPayment": "Último pagamento",
    "recommended": "RECOMENDADO",
    "choosePlan": "Escolher plano",
    "activePlanBadge": "Plano ativo",
    "summaryStarterLabel": "Casas pequenas",
    "summaryStarterText": "Ideal para começar com monitoramento básico e alertas simples.",
    "summaryProfessionalLabel": "Recomendado",
    "summaryProfessionalText": "A melhor opção para casas avançadas e pequenos negócios.",
    "summaryEnterpriseLabel": "Múltiplos locais",
    "summaryEnterpriseText": "Criado para equipes, locais e operações empresariais.",
    "maxDevices": "Até {{ count }} dispositivos",
    "maxRoutines": "Até {{ count }} rotinas",
    "reportExportEnabled": "Exportação de relatórios",
    "planUsage": {
      "STARTER": "Para casas pequenas",
      "PROFESSIONAL": "Para casas avançadas e pequenos negócios",
      "ENTERPRISE": "Para múltiplos locais comerciais"
    },
    "planDescriptions": {
      "STARTER": "Gestão básica de energia para casas pequenas.",
      "PROFESSIONAL": "Controle avançado para casas e pequenos negócios.",
      "ENTERPRISE": "Plataforma completa para múltiplos locais comerciais."
    },
    "planFeatures": {
      "STARTER": {
        "devices": "Até 5 dispositivos inteligentes",
        "dashboard": "Painel energético básico",
        "alerts": "Alertas por e-mail"
      },
      "PROFESSIONAL": {
        "devices": "Até 20 dispositivos inteligentes",
        "analytics": "Analítica energética avançada",
        "routines": "Rotinas de automação",
        "reports": "Relatórios mensais"
      },
      "ENTERPRISE": {
        "locations": "Múltiplos locais comerciais",
        "accessProfiles": "Perfis de acesso para a equipe",
        "alerts": "Alertas avançados",
        "support": "Suporte prioritário"
      }
    },
    "historyEyebrow": "Histórico de faturamento",
    "historyTitle": "Histórico de faturamento",
    "historySubtitle": "Revise seus pagamentos, faturas e estado atual da assinatura.",
    "invoices": "Faturas",
    "invoice": "Fatura",
    "issuedAt": "Emitida em",
    "status": "Estado",
    "downloadInvoice": "Baixar fatura",
    "emptyInvoicesTitle": "Ainda não há faturas",
    "emptyInvoicesContent": "Suas faturas aparecerão aqui depois que seus pagamentos forem processados.",
    "subscribeSuccess": "Assinatura ativada com sucesso.",
    "cancelSuccess": "Assinatura cancelada com sucesso.",
    "invalidExpirationDate": "A data de vencimento não é válida.",
    "invoiceNotFound": "Fatura não encontrada."
  },
  "alerts": {
    "eyebrow": "Centro de alertas",
    "title": "Alertas do sistema",
    "subtitle": "Visualize eventos importantes, alertas de consumo e notificações operacionais da ElectroCorp.",
    "total": "Total",
    "unreadCount": "Não lidas",
    "createTitle": "Criar alerta",
    "alertTitle": "Título",
    "message": "Mensagem",
    "level": "Nível",
    "create": "Criar alerta",
    "unread": "Não lida",
    "markAsRead": "Marcar como lida",
    "emptyTitle": "Não há alertas",
    "emptyContent": "Quando ocorrer um evento importante, ele aparecerá nesta seção.",
    "loadError": "Não foi possível carregar os alertas.",
    "createError": "Não foi possível criar o alerta.",
    "markAsReadError": "Não foi possível marcar o alerta como lido.",
    "planLimitReached": "Você atingiu o limite de alertas permitido pelo seu plano.",
    "criticalCount": "Críticas",
    "createSuccess": "Alerta criado com sucesso.",
    "deleteError": "Não foi possível excluir o alerta.",
    "inboxSubtitle": "Revise alertas ativas, eventos importantes e notificações permitidas pelas suas preferências.",
    "summaryAria": "Resumo de alertas visíveis",
    "refresh": "Atualizar",
    "hiddenByPreferences": "{{count}} alertas ficam fora da bandeja por suas preferências atuais.",
    "markAsReadSuccess": "Alerta marcada como lida.",
    "deleteSuccess": "Alerta excluída.",
    "dismissSuccess": "Alerta silenciada por {{minutes}} minutos.",
    "dismissError": "Não foi possível silenciar o alerta.",
    "read": "Lida",
    "dismissForMinutes": "Silenciar {{minutes}} min",
    "severityCalibration": "Calibração de gravidade",
    "calibration": "Calibração",
    "viewDetails": "Ver detalhes",
    "recommendedAction": "Ação sugerida",
    "summary": {
      "visibleInboxOnly": "Somente bandeja visível",
      "withoutSilenced": "Sem silenciadas"
    },
    "toolbar": {
      "title": "Alertas reais",
      "description": "São exibidos alertas ativos salvos para seu usuário e permitidos por suas preferências de saída."
    },
    "filters": {
      "all": "Todas",
      "unread": "Não lidas",
      "stable": "Estáveis",
      "success": "Conquistas",
      "info": "Informativas",
      "warning": "Atenção",
      "critical": "Críticas"
    },
    "levels": {
      "stable": "Stable",
      "success": "Sucesso",
      "info": "Info",
      "warning": "Atenção",
      "critical": "Crítico"
    },
    "severityDescriptions": {
      "stable": "Dentro do intervalo",
      "success": "Baixo impacto",
      "info": "Acompanhamento normal",
      "warning": "Atenção preventiva",
      "critical": "Atenção imediata",
      "default": "Acompanhamento"
    }
  },
  "common": {
    "loading": "Carregando...",
    "delete": "Excluir",
    "cancel": "Cancelar",
    "continue": "Continuar",
    "keep": "Manter",
    "ok": "OK"
  },
  "profile": {
    "eyebrow": "Perfil do usuário",
    "title": "Meu perfil",
    "subtitle": "Gerencie suas informações pessoais e configurações da conta.",
    "personalData": "Dados pessoais",
    "saveChanges": "Salvar alterações",
    "deleteAccount": "Excluir conta",
    "deleteAccountWarning": "Esta ação é permanente e não pode ser desfeita.",
    "deleteConfirmation": "Digite EXCLUIR para confirmar."
  },
  "deviceGroups": {
    "eyebrow": "Grupos de dispositivos",
    "title": "Grupos de dispositivos",
    "subtitle": "Organize dispositivos em grupos e execute ações neles em conjunto.",
    "addTitle": "Criar grupo de dispositivos",
    "name": "Nome",
    "description": "Descrição",
    "create": "Criar grupo",
    "createSuccess": "Grupo de dispositivos criado com sucesso.",
    "createError": "Não foi possível criar o grupo de dispositivos.",
    "updateError": "Não foi possível atualizar o grupo de dispositivos.",
    "deleteError": "Não foi possível excluir o grupo de dispositivos.",
    "executeSuccess": "Ação do grupo executada com sucesso.",
    "executeError": "Não foi possível executar a ação do grupo.",
    "pageDescription": "Organize dispositivos por intencao de uso dentro do local ativo.",
    "noSelection": "Sem selecao",
    "deleteSuccess": "Grupo excluido.",
    "summary": {
      "createdTitle": "Criados",
      "availableTitle": "Disponiveis",
      "multiGroupDescription": "podem pertencer a varios grupos.",
      "selectionEyebrow": "Selecao",
      "selectedDevices": "{{count}} dispositivo(s) selecionados."
    },
    "toolbar": {
      "eyebrow": "Grupos semanticos",
      "title": "Controle por intencao",
      "description": "Um grupo pode representar iluminacao, economia, noite ou qualquer forma real de uso."
    },
    "emptyDevices": {
      "title": "Nao ha dispositivos atribuidos",
      "description": "Atribua dispositivos ao local ativo antes de criar grupos."
    },
    "emptyGroups": {
      "title": "Nao ha grupos neste local",
      "description": "Crie um grupo semantico com dispositivos do mesmo ambiente."
    },
    "form": {
      "eyebrow": "Novo grupo",
      "description": "Selecione dispositivos do mesmo ambiente. Um dispositivo pode pertencer a varios grupos.",
      "namePlaceholder": "Ex. Iluminacao da sala",
      "descriptionPlaceholder": "Proposito do grupo",
      "pickerDescription": "Marque equipamentos atribuidos a {{location}}.",
      "selectedCount": "{{count}} selecionados",
      "availableCount": "{{count}} disponiveis",
      "alsoBelongsTo": "Tambem pertence a"
    },
    "selection": {
      "select": "Selecionar",
      "selected": "Selecionado"
    },
    "state": {
      "on": "Ligado",
      "off": "Desligado",
      "mixed": "Misto"
    },
    "card": {
      "defaultDescription": "Grupo semantico para organizar dispositivos por intencao de uso.",
      "assignedDevices": "Dispositivos atribuidos",
      "noAssignedDevices": "Sem dispositivos atribuidos.",
      "createdAt": "Criado",
      "noDate": "Sem data registrada"
    },
    "validation": {
      "sameRoom": "Um grupo deve pertencer a um unico ambiente. Selecione dispositivos do mesmo ambiente.",
      "activeLocationRequired": "Selecione um local ativo antes de criar um grupo.",
      "nameRequired": "Digite um nome para o grupo antes de cria-lo.",
      "deviceRequired": "Selecione pelo menos um dispositivo para criar o grupo."
    },
    "deleteFallbackGroup": "Este grupo",
    "deleteWarnings": {
      "devicesTitle": "Revisar dispositivos",
      "devicesMessage": "{{group}} contem {{count}} dispositivo(s). Ao excluir o grupo, os dispositivos continuarao existindo.",
      "routinesTitle": "Revisar rotinas",
      "routineSingular": "{{group}} participa de uma rotina ativa. Revise se deseja continuar.",
      "routinePlural": "{{group}} participa de {{count}} rotinas ativas. Revise se deseja continuar.",
      "modesTitle": "Revisar modos",
      "modeSingular": "{{group}} esta relacionado a um modo existente. Exclui-lo pode afetar essa configuracao.",
      "modePlural": "{{group}} esta relacionado a {{count}} modos existentes. Exclui-lo pode afetar essas configuracoes.",
      "deleteTitle": "Excluir grupo",
      "deleteMessage": "{{group}} sera excluido. Os dispositivos nao sao excluidos."
    }
  },
  "alertRules": {
    "eyebrow": "Regras de alerta",
    "title": "Regras de alerta",
    "subtitle": "Configure regras automáticas para gerar alertas com base em limites de consumo.",
    "total": "Total",
    "enabled": "Ativas",
    "createTitle": "Criar regra de alerta",
    "name": "Nome",
    "threshold": "Limite",
    "create": "Criar regra",
    "enable": "Ativar",
    "disable": "Desativar",
    "createSuccess": "Regra de alerta criada com sucesso.",
    "createError": "Não foi possível criar a regra de alerta.",
    "updateError": "Não foi possível atualizar a regra de alerta.",
    "deleteError": "Não foi possível excluir a regra de alerta.",
    "pageDescription": "Defina critérios persistentes para classificar eventos operacionais. A avaliação usa as regras ativas salvas para seu usuário.",
    "summaryAria": "Resumo de regras",
    "savedRules": "Regras salvas",
    "pausedCount": "{{count}} pausadas",
    "activeWeight": "Peso ativo",
    "usedWhenEvaluating": "Usado ao avaliar",
    "realScoreRanges": "Faixas reais do score",
    "realScoreRangesDescription": "Essas faixas vêm de Preferências. Stable começa em 0 e Critical termina em 100.",
    "newRule": "Nova regra",
    "createPersistentRule": "Criar regra persistente",
    "createPersistentRuleDescription": "A regra é salva e pode participar da avaliação enquanto estiver ativa.",
    "namePlaceholder": "Ex. Pico de potência na sala",
    "metric": "Métrica",
    "condition": "Condição",
    "scope": "Alcance",
    "criterion": "Critério",
    "weight": "Peso",
    "savedRulesDescription": "Ativar, pausar ou excluir impacta a lista persistida e a próxima avaliação.",
    "emptyTitle": "Não há regras salvas",
    "emptyDescription": "Crie uma regra para que a avaliação tenha critérios persistidos.",
    "evaluation": "Avaliação",
    "testActiveRules": "Testar regras ativas",
    "testActiveRulesDescription": "O teste usa o serviço de avaliação e as regras ativas salvas.",
    "observedValue": "Valor observado",
    "evaluate": "Avaliar",
    "testedNotification": "Notificação testada",
    "score": "Score",
    "criteria": "Critérios",
    "readingsValue": "{{count}} leituras",
    "validation": {
      "nameRequired": "Digite um nome para a regra."
    },
    "toasts": {
      "paused": "Regra pausada.",
      "activated": "Regra ativada.",
      "deleted": "Regra excluída.",
      "evaluateError": "Não foi possível avaliar as regras.",
      "evaluated": "Regras avaliadas."
    },
    "metrics": {
      "watts": "Potência instantânea",
      "highReadingCount": "Quantidade de leituras altas",
      "averageWatts": "Média de watts"
    },
    "metricDescriptions": {
      "watts": "Watts medidos no evento.",
      "highReadingCount": "Número de eventos marcados como altos no período avaliado.",
      "averageWatts": "Média observada para o período."
    },
    "conditions": {
      "greaterOrEqual": "Alcança ou supera",
      "greaterThan": "Supera",
      "greaterOrEqualLower": "alcança ou supera",
      "greaterThanLower": "supera"
    },
    "scopes": {
      "general": "Geral"
    },
    "evaluators": {
      "activePower": "Potência ativa",
      "dailyKwh": "Consumo diário",
      "averageWatts": "Média watts",
      "highReadingCount": "Quantidade de leituras altas",
      "sustainedConsumption": "Consumo sustentado",
      "deviceCount": "Quantidade de dispositivos",
      "goalUsage": "Uso de meta",
      "goalDeadline": "Vencimento de meta",
      "routineContext": "Contexto de rotina",
      "costEstimate": "Custo estimado",
      "configurationCoverage": "Cobertura configurada"
    },
    "severityRanges": {
      "stableDescription": "Operação dentro do intervalo esperado.",
      "infoDescription": "Evento útil para acompanhamento, sem urgência.",
      "warningDescription": "Convém revisar antes que escale.",
      "criticalDescription": "Requer ação imediata."
    }
  },
  "notificationPreferences": {
    "eyebrow": "Preferências de notificação",
    "title": "Preferências de notificação",
    "subtitle": "Configure como e quando deseja receber notificações da ElectroCorp.",
    "emailEnabled": "Notificações por e-mail",
    "pushEnabled": "Notificações push",
    "criticalOnly": "Somente alertas críticos",
    "minimumLevel": "Nível mínimo",
    "save": "Salvar preferências",
    "updateSuccess": "Preferências de notificação atualizadas com sucesso.",
    "updateError": "Não foi possível atualizar as preferências de notificação.",
    "sensitivityTitle": "Sensibilidade de alertas",
    "sensitivityDescription": "Ajuste categorias visíveis, ruído, pesos preliminares e saída de alertas para seu usuário.",
    "currentPreference": "Preferência atual",
    "editorTitle": "Editor preliminar",
    "editorDescription": "Canais, fontes e silências são salvos; pesos e faixas ficam prontos para o próximo contrato.",
    "emptyTitle": "Sem preferências carregadas",
    "emptyDescription": "Quando a preferência do usuário estiver disponível, você poderá editar a visibilidade da bandeja.",
    "summary": {
      "rangesEyebrow": "Faixas",
      "rangesTitle": "Stable a Critical",
      "rangesDescription": "Os limites permanecem contíguos para evitar cruzamentos.",
      "successTitle": "Positivo separado",
      "successDescription": "Eventos bons não competem com risco.",
      "parametersEyebrow": "Parâmetros",
      "parametersTitle": "Pesos e ruído",
      "parametersDescription": "Controla prioridade, repetição, horários e fontes."
    },
    "sensitivity": "Sensibilidade",
    "weightedClassification": "Classificação ponderada",
    "weightedClassificationDescription": "Success fica fora do risco. Stable, Neutral, Warning e Critical são calculados por faixas contíguas.",
    "stableCeiling": "Limite Stable",
    "neutralCeiling": "Limite Info",
    "warningCeiling": "Limite Warning",
    "preview": "Preview",
    "sensitiveResult": "Resultado sensível",
    "previewDescription": "O score técnico não precisa aparecer na bandeja; aqui serve apenas para calibrar a categoria.",
    "context": "Contexto",
    "baseSignal": "Sinal base",
    "finalCategory": "Categoria final",
    "weightedPercent": "{{value}}% ponderado",
    "positiveEvents": "Eventos positivos",
    "positiveEventsDescription": "Ações corretas, recuperações, metas cumpridas e aplicações bem-sucedidas.",
    "showSuccess": "Mostrar Success",
    "showSuccessDescription": "Mantém o positivo visível sem misturá-lo com risco.",
    "visibleCategories": "Categorias visíveis",
    "allowedLevels": "Níveis permitidos",
    "allowedLevelsDescription": "O contrato atual salva esses níveis; Neutral persiste como INFO.",
    "criticalOnlyLabel": "Somente Critical",
    "criticalOnlyDescription": "Reduz ruído e deixa passar apenas eventos urgentes.",
    "weighting": "Ponderação",
    "contextParameters": "Parâmetros por contexto",
    "contextParametersDescription": "Esses pesos são preliminares: ajustam o preview e preparam o próximo contrato.",
    "weightDescription": "{{weight}}% de peso",
    "output": "Saída",
    "channelsAndNoise": "Canais e ruído",
    "channelsAndNoiseDescription": "Estes parâmetros já são salvos na preferência atual.",
    "deliveryMode": "Modo de entrega",
    "cooldown": "Cooldown",
    "minutes": "minutos",
    "maxPerHour": "Máximo por hora",
    "alertsUnit": "alertas",
    "silences": "Silências",
    "scheduleAndExceptions": "Horário e exceções",
    "scheduleAndExceptionsDescription": "Critical pode quebrar o silêncio se você permitir.",
    "quietStart": "Início silêncio",
    "quietEnd": "Fim silêncio",
    "levelDescriptions": {
      "stable": "Mostrar tudo desde estado estável.",
      "info": "Receber eventos relevantes e superiores.",
      "warning": "Receber somente preventivos e críticos.",
      "critical": "Receber somente eventos urgentes."
    },
    "deliveryModes": {
      "banner": "Banner",
      "quiet": "Silencioso",
      "inboxOnly": "Somente bandeja",
      "muted": "Silenciado"
    },
    "deliveryModeDescriptions": {
      "banner": "Saída visível normal.",
      "quiet": "Sem ruído invasivo.",
      "inboxOnly": "Sem aviso emergente.",
      "muted": "Sem saída invasiva."
    },
    "sources": {
      "device": "Dispositivos",
      "group": "Grupos",
      "room": "Cômodos",
      "workplace": "Sedes",
      "routine": "Rotinas",
      "goal": "Metas",
      "report": "Relatórios",
      "rule": "Regras",
      "mode": "Modos",
      "system": "Sistema"
    },
    "sourceDescriptions": {
      "device": "Estado, resposta e consumo direto dos equipamentos.",
      "group": "Mudanças agregadas por intenção de uso.",
      "routine": "Execução, falhas ou silências de automação.",
      "goal": "Progresso, risco de exceder ou cumprimento.",
      "rule": "Limiares automáticos e avaliações ativas.",
      "mode": "Aplicação de perfis operacionais.",
      "system": "Recomendações e eventos próprios da plataforma."
    },
    "toggles": {
      "inAppDescription": "Mostrar alertas dentro do app.",
      "toastTitle": "Toast",
      "toastDescription": "Avisos emergentes dentro da sessão.",
      "dashboardTitle": "Dashboard",
      "dashboardDescription": "Refletir alertas em painéis.",
      "emailTitle": "E-mail",
      "emailDescription": "Canal preparado para avisos externos.",
      "pushTitle": "Push",
      "pushDescription": "Canal preparado para avisos do dispositivo.",
      "groupSimilarTitle": "Agrupar similares",
      "groupSimilarDescription": "Reduz repetição por thread de alerta.",
      "quietHoursTitle": "Horário silencioso",
      "quietHoursDescription": "Usa uma janela sem ruído.",
      "criticalBreaksTitle": "Critical quebra silêncio",
      "criticalBreaksDescription": "Eventos urgentes seguem visíveis.",
      "remindersTitle": "Lembretes",
      "remindersDescription": "Permite avisos posteriores.",
      "nightRoutinesTitle": "Rotinas noturnas",
      "nightRoutinesDescription": "Reduz ruído por automação noturna.",
      "goalDeadlineTitle": "Metas com data",
      "goalDeadlineDescription": "Prioriza metas perto do limite.",
      "maintenanceDescription": "Inclui equipamentos em revisão.",
      "recommendationsTitle": "Recomendações",
      "recommendationsDescription": "Mostra sugestões do sistema.",
      "dailySummaryTitle": "Resumo diário",
      "dailySummaryDescription": "Agrupa atividade do dia.",
      "weeklySummaryTitle": "Resumo semanal",
      "weeklySummaryDescription": "Agrupa tendências semanais."
    }
  },
  "workplace": {
    "locationsEyebrow": "Espaço de trabalho",
    "locationsTitle": "Localizações",
    "locationsSubtitle": "Gerencie os locais onde os dispositivos da ElectroCorp estão instalados.",
    "roomsEyebrow": "Ambientes",
    "roomsTitle": "Ambientes",
    "roomsSubtitle": "Organize ambientes dentro de cada localização.",
    "assignmentsEyebrow": "Atribuições de dispositivos",
    "assignmentsTitle": "Atribuições de dispositivos",
    "assignmentsSubtitle": "Atribua dispositivos a uma localizacao e ambiente para que possam participar de grupos, rotinas, modos, metas e relatorios.",
    "totalLocations": "Total de localizações",
    "businessLocations": "Locais comerciais",
    "branchLocations": "Filiais",
    "totalRooms": "Total de ambientes",
    "totalAssignments": "Total de atribuições",
    "totalDevices": "Total de dispositivos",
    "createLocation": "Criar localização",
    "createRoom": "Criar ambiente",
    "assignDevice": "Atribuir dispositivo",
    "locationName": "Nome da localização",
    "roomName": "Nome do ambiente",
    "address": "Endereço",
    "floor": "Andar",
    "selectLocation": "Selecione uma localização",
    "selectRoom": "Selecione um ambiente",
    "selectDevice": "Selecione um dispositivo",
    "create": "Criar",
    "loadError": "Não foi possível carregar as informações do espaço de trabalho.",
    "locationCreateSuccess": "Localização criada com sucesso.",
    "locationCreateError": "Não foi possível criar a localização.",
    "locationUpdateSuccess": "Localização atualizada com sucesso.",
    "locationUpdateError": "Não foi possível atualizar a localização.",
    "locationDeleteError": "Não foi possível excluir a localização.",
    "roomCreateSuccess": "Ambiente criado com sucesso.",
    "roomCreateError": "Não foi possível criar o ambiente.",
    "roomUpdateSuccess": "Ambiente atualizado com sucesso.",
    "roomUpdateError": "Não foi possível atualizar o ambiente.",
    "roomDeleteError": "Não foi possível excluir o ambiente.",
    "assignmentCreateSuccess": "Dispositivo atribuído com sucesso.",
    "assignmentCreateError": "Não foi possível atribuir o dispositivo.",
    "assignmentMoveSuccess": "Atribuição de dispositivo movida com sucesso.",
    "assignmentMoveError": "Não foi possível mover a atribuição do dispositivo.",
    "assignmentDeleteError": "Não foi possível excluir a atribuição do dispositivo.",
    "deviceAlreadyAssigned": "Este dispositivo já está atribuído.",
    "locationTypes": {
      "home": "Casa",
      "business": "Empresa",
      "branch": "Filial"
    },
    "locations": {
      "pageDescription": "Gerencie os locais fisicos onde a ElectroCorp organiza ambientes, dispositivos e consumo.",
      "summary": {
        "registeredTitle": "Registradas",
        "registeredDescription": "Casas, escritorios ou filiais operacionais.",
        "assignedTitle": "Com local",
        "unassignedDevices": "{{count}} dispositivo(s) sem local.",
        "coverageEyebrow": "Cobertura",
        "unroomedDevices": "{{count}} dispositivo(s) sem ambiente."
      },
      "toolbar": {
        "eyebrow": "Gestao de locais",
        "title": "Crie e selecione centros operacionais",
        "description": "A localizacao pode ser salva como referencia escrita ou coordenadas aproximadas."
      },
      "empty": {
        "title": "Nao ha locais",
        "description": "Crie um local para organizar ambientes, dispositivos, grupos e consumo por lugar fisico."
      },
      "form": {
        "eyebrow": "Novo local",
        "description": "Defina o nome, tipo e uma localizacao de referencia para este espaco operacional.",
        "namePlaceholder": "Ex. Escritorio central",
        "typeLabel": "Tipo",
        "typePlaceholder": "Tipo de local",
        "addressLabel": "Endereco ou referencia",
        "addressPlaceholder": "Endereco, referencia ou coordenadas"
      },
      "map": {
        "title": "Seletor aproximado",
        "description": "OpenStreetMap e usado como referencia visual sem chave de API. Se nao houver endereco exato, coordenadas aproximadas serao salvas.",
        "iframeTitle": "Mapa de referencia do OpenStreetMap",
        "surfaceLabel": "Selecionar ponto no mapa",
        "selectedLabel": "Localizacao selecionada",
        "selectedAddress": "Localizacao selecionada ({{coordinates}})"
      },
      "deleteTitle": "Excluir local",
      "deleteMessage": "Voce vai excluir {{name}}. Ambientes e atribuicoes associadas podem deixar de aparecer em Espacos.",
      "deleteSuccess": "Local excluido.",
      "card": {
        "createdAt": "Criada",
        "noDate": "Sem data registrada",
        "activePower": "Potencia ativa",
        "withoutRoom": "Sem ambiente",
        "coverage": "Cobertura",
        "goToRooms": "Ir",
        "goToRoomsAria": "Ir para os ambientes deste local"
      }
    },
    "rooms": {
      "pageDescription": "Organize o local ativo por ambientes para entender onde estao os dispositivos e como o consumo se distribui.",
      "activeLocationFallback": "local ativo",
      "summary": {
        "activeLocationEyebrow": "Local ativo",
        "availableRooms": "ambiente(s) disponiveis para organizar movimentos.",
        "registeredTitle": "Registrados",
        "registeredDescription": "Zonas fisicas disponiveis para atribuir dispositivos.",
        "assignedTitle": "Atribuidos",
        "linkedGroups": "{{count}} grupo(s) vinculados ao local."
      },
      "toolbar": {
        "eyebrow": "Gestao de ambientes",
        "title": "Adicione zonas a {{location}}",
        "description": "Ambientes ajudam a mover dispositivos e revisar consumo por lugar."
      },
      "empty": {
        "title": "Nao ha ambientes neste local",
        "description": "Crie um ambiente para localizar dispositivos e move-los entre zonas reais."
      },
      "form": {
        "eyebrow": "Novo ambiente",
        "description": "O ambiente sera adicionado ao local ativo e ficara disponivel para movimentos de dispositivos.",
        "namePlaceholder": "Ex. Sala principal",
        "siteLabel": "Local"
      },
      "status": {
        "noDevices": "Sem dispositivos",
        "active": "Com atividade",
        "idle": "Sem atividade"
      },
      "card": {
        "createdAt": "Criada",
        "noDate": "Sem data registrada",
        "activeDevices": "Ligados",
        "consumption": "Consumo",
        "relatedGroup": "Grupo relacionado",
        "noLinkedGroup": "Sem grupo vinculado",
        "relatedRoutines": "Rotinas relacionadas",
        "goToAssignments": "Ir",
        "goToAssignmentsAria": "Ir para atribuicoes deste ambiente"
      },
      "toasts": {
        "loadError": "Nao foi possivel carregar as informacoes de ambientes.",
        "locationRequired": "Crie um local antes de criar ambientes.",
        "deleteSuccess": "Ambiente excluido com sucesso."
      },
      "deleteTitle": "Excluir ambiente",
      "deleteMessageWithDevices": "Este ambiente tem {{count}} dispositivo(s) atribuido(s). Se voce exclui-lo, essas referencias podem ficar sem ambiente.",
      "deleteMessageEmpty": "Voce vai excluir este ambiente do local ativo."
    },
    "deviceTypes": {
      "plug": "Tomada",
      "light": "Luz",
      "switch": "Interruptor",
      "sensor": "Sensor",
      "other": "Outro"
    },
    "assignments": {
      "pageDescription": "Arraste dispositivos entre dois ambientes e confirme quando a alteracao afetar grupos, rotinas ou modos.",
      "activeLocationFallback": "local ativo",
      "noRoom": "Sem ambiente",
      "noSecondRoomOption": "Nao ha outro ambiente disponivel",
      "noSecondRoomAvailable": "Nao ha um segundo ambiente disponivel.",
      "panelItemCount": "{{count}} elemento(s) neste ambiente.",
      "go": "Ir",
      "summary": {
        "activeLocationEyebrow": "Local ativo",
        "availableRooms": "ambiente(s) disponiveis para organizar movimentos.",
        "movableTitle": "Moveis",
        "connectedDevices": "dispositivo(s) conectados a este local.",
        "interactionEyebrow": "Interacao",
        "dragTitle": "Arraste direto",
        "directValue": "Direto",
        "dragDescription": "arraste dispositivos entre Ambiente 1 e Ambiente 2."
      },
      "movement": {
        "eyebrow": "Movimento entre ambientes",
        "title": "Nova atribuicao",
        "description": "Arraste cada dispositivo de um painel para o outro. Confirmacoes de grupo, rotina e modo aparecem antes de aplicar a alteracao."
      },
      "panels": {
        "roomOne": "Ambiente 1",
        "roomTwo": "Ambiente 2"
      },
      "emptyRooms": {
        "title": "Nao ha ambientes",
        "description": "Crie ambientes no local ativo para poder mover dispositivos ou grupos."
      },
      "emptyAssignments": {
        "title": "Nao ha dispositivos atribuidos",
        "description": "Quando um local tiver dispositivos conectados, voce podera move-los entre ambientes nesta visualizacao."
      },
      "emptyPanel": {
        "title": "Sem dispositivos",
        "description": "Arraste aqui um dispositivo para move-lo para este ambiente."
      },
      "unavailablePanel": {
        "title": "Ambiente indisponivel",
        "description": "Adicione outro ambiente para habilitar este painel."
      },
      "device": {
        "noPowerInfo": "Sem informacao de potencia",
        "notAvailable": "Nao disponivel",
        "on": "Ligado",
        "off": "Desligado",
        "maintenance": "Manutencao"
      },
      "form": {
        "operationalSite": "Local operacional",
        "siteOnly": "Atribuir apenas ao local",
        "availableRoom": "Ambiente disponivel"
      },
      "toasts": {
        "loadError": "Nao foi possivel carregar as informacoes de atribuicoes.",
        "moveSuccess": "Movimento aplicado com sucesso.",
        "moveError": "Nao foi possivel concluir o movimento."
      },
      "warnings": {
        "groupTitle": "Revisar grupo",
        "groupMessage": "Este dispositivo faz parte do grupo {{group}}. Se voce move-lo, esse grupo pode ficar incompleto.",
        "routinesTitle": "Revisar rotinas",
        "routinesMessage": "Este dispositivo participa de rotinas ativas. Revise se deseja continuar.",
        "modesTitle": "Revisar modos",
        "modesMessage": "Este dispositivo esta relacionado a um modo. Move-lo pode afetar esse modo."
      }
    }
  },
  "reporting": {
    "reportsEyebrow": "Relatórios",
    "reportsTitle": "Relatórios de consumo",
    "reportsSubtitle": "Gere e revise relatórios de consumo energético.",
    "goalsEyebrow": "Metas energéticas",
    "goalsTitle": "Metas energéticas",
    "goalsSubtitle": "Defina metas de economia e acompanhe seu progresso.",
    "totalReports": "Total de relatórios",
    "totalGoals": "Total de metas",
    "activeGoals": "Metas ativas",
    "completedGoals": "Metas concluídas",
    "totalReportedWatts": "Watts reportados totais",
    "averageReportedWatts": "Média de watts reportados",
    "monthlyComparison": "Comparação de relatórios",
    "monthlyComparisonSubtitle": "Compare relatórios do mesmo período:",
    "createReport": "Criar relatório",
    "createGoal": "Criar meta",
    "reportTitle": "Título do relatório",
    "goalTitle": "Título da meta",
    "generate": "Gerar",
    "export": "Exportar",
    "totalWatts": "Watts totais",
    "averageWatts": "Watts médios",
    "highestWatts": "Watts máximos",
    "targetWatts": "Watts-alvo",
    "deadline": "Prazo",
    "createdAt": "Criado em",
    "generatedAt": "Gerado em",
    "emptyReportsTitle": "Ainda não há relatórios",
    "emptyReportsContent": "Gere seu primeiro relatório de consumo para analisar seu uso de energia.",
    "emptyGoalsTitle": "Ainda não há metas",
    "emptyGoalsContent": "Crie uma meta energética para começar a medir a economia.",
    "loadError": "Não foi possível carregar as informações de relatórios.",
    "reportCreateSuccess": "Relatório criado com sucesso.",
    "reportCreateError": "Não foi possível criar o relatório.",
    "reportDeleteError": "Não foi possível excluir o relatório.",
    "exportNotAllowed": "Seu plano atual não permite exportar relatórios.",
    "reportNotFound": "Relatório não encontrado.",
    "goalCreateSuccess": "Meta energética criada com sucesso.",
    "goalCreateError": "Não foi possível criar a meta energética.",
    "goalUpdateSuccess": "Meta energética atualizada com sucesso.",
    "goalUpdateError": "Não foi possível atualizar a meta energética.",
    "goalDeleteError": "Não foi possível excluir a meta energética.",
    "createReportDescription": "Escolha o tipo de relatório e mova a roda para selecionar o intervalo exato.",
    "period": "Período",
    "periods": {
      "daily": "Diário",
      "weekly": "Semanal",
      "monthly": "Mensal",
      "yearly": "Anual"
    },
    "periodOptions": {
      "daily": "Diários",
      "weekly": "Semanais",
      "monthly": "Mensais",
      "yearly": "Anuais"
    },
    "reportRangeEyebrow": "Intervalo do relatório",
    "reportRangeHint": "Arraste para a direita para voltar ao passado. O intervalo atual é o limite final.",
    "rangeTitles": {
      "daily": "Selecione o dia",
      "weekly": "Selecione a semana",
      "monthly": "Selecione o mês",
      "yearly": "Selecione o ano"
    },
    "rangeLabels": {
      "currentWeek": "Semana atual",
      "currentYearCycle": "{{year}} - em curso",
      "currentYear": "Ano atual",
      "fullYear": "Ano completo"
    },
    "generatedReportsTitle": "Relatórios gerados",
    "generatedReportsDescription": "Revise os relatórios criados e abra cada detalhe a partir do resumo.",
    "emptyReportsEyebrow": "Sem relatórios",
    "from": "Desde",
    "to": "Até",
    "generated": "Gerado",
    "emptyValue": "-",
    "reportFallbackTitle": "Relatório",
    "reportFallbackTitleWithId": "Relatório #{{id}}",
    "averageDetail": "{{value}} média",
    "reportChartAria": "{{title}}, {{total}}, {{start}} - {{end}}",
    "reportNameRequired": "Digite o nome do relatório.",
    "reportNameMinLength": "O nome do relatório deve ter pelo menos 4 caracteres.",
    "reportRangeRequired": "Selecione um intervalo de datas para o relatório.",
    "reportRangeInvalid": "O intervalo do relatório não é válido.",
    "reportUpdateSuccess": "Relatório atualizado.",
    "reportUnchanged": "Esse relatório já existe para o ciclo selecionado.",
    "reportExportSuccess": "Relatório exportado.",
    "reportExportError": "Não foi possível exportar o relatório.",
    "reportDeleteSuccess": "Relatório eliminado.",
    "duration": {
      "hoursMinutes": "{{hours}}h {{minutes}}m",
      "minutesOnly": "{{minutes}}m"
    },
    "invoice": {
      "eyebrow": "Fatura energética profissional",
      "title": "Relatório ElectroCorp #{{id}}",
      "close": "Fechar fatura",
      "totalEnergy": "Energia total",
      "exactReadings": "{{count}} leituras exatas dentro do intervalo",
      "totalToPay": "Total a pagar",
      "realKwhCalculation": "Calculado a partir de kWh reais por dispositivo",
      "monitoredDuration": "{{duration}} monitorados",
      "technicalSummary": "Resumo técnico",
      "averagePower": "Potência média",
      "detectedPeak": "Pico detectado",
      "criticalReadings": "Leituras críticas",
      "costPerKwh": "Custo por kWh",
      "billedEnergy": "Energia faturada",
      "energySubtotal": "Subtotal de energia",
      "measuredDevices": "Dispositivos medidos",
      "totalOnTime": "Tempo ON total",
      "topConsumption": "Top consumo",
      "devicesCount": "{{count}} equipamentos",
      "noDevicesInRange": "Não há dispositivos neste intervalo.",
      "deviceExactDetail": "Detalhe exato por dispositivo",
      "readings": "Leituras",
      "onTime": "Tempo ON",
      "exactKwh": "kWh exatos",
      "peak": "Pico",
      "share": "Participação",
      "subtotal": "Subtotal",
      "noReadingsForReport": "Não há leituras para este relatório."
    },
    "goals": {
      "summary": {
        "created": "Metas criadas",
        "tracking": "Em acompanhamento",
        "atRisk": "Metas em risco",
        "needAttention": "Requerem atenção",
        "averageCompliance": "Cumprimento médio"
      },
      "createStripTitle": "Criar meta inteligente",
      "createStripDescription": "Defina escopo, limite e janela de acompanhamento com dados reais.",
      "modalEyebrow": "Meta energética",
      "modalDescription": "Selecione o escopo de acompanhamento e defina o limite energético.",
      "scope": "Escopo",
      "goalName": "Nome da meta",
      "goalNamePlaceholder": "Ex. Reduzir consumo da sala principal",
      "consumptionLimit": "Limite de consumo",
      "emptyEyebrow": "Sem metas",
      "generalOperation": "Operação geral",
      "fullDay": "Todo o dia",
      "deviceCountDescription": "{{count}} equipamentos",
      "availableKwh": "{{value}} kWh disponíveis",
      "daysRemaining": "{{count}} dias restantes",
      "limitLabel": "Limite: {{value}} kWh",
      "deadlineLabel": "Vence: {{date}}",
      "measured": "Medido",
      "usedPercentage": "{{value}}% usado",
      "devices": "Equipamentos",
      "projection": "Projeção",
      "scopeOptions": {
        "workplace": "Uma sede",
        "room": "Uma sala",
        "group": "Um grupo",
        "device": "Um dispositivo"
      },
      "scopeDescriptions": {
        "workplace": "Mede dispositivos atribuídos a uma sede energética.",
        "room": "Mede dispositivos atribuídos a uma sala.",
        "group": "Mede os dispositivos incluídos em um grupo.",
        "device": "Mede apenas as leituras de um equipamento."
      },
      "scopeTargetLabels": {
        "general": "Escopo",
        "workplace": "Sede",
        "room": "Sala",
        "device": "Dispositivo",
        "group": "Grupo"
      },
      "scopeTargetPlaceholders": {
        "general": "Selecionar escopo",
        "workplace": "Selecionar sede",
        "room": "Selecionar sala",
        "device": "Selecionar dispositivo",
        "group": "Selecionar grupo"
      },
      "status": {
        "inControl": "Em controle",
        "outOfGoal": "Fora da meta",
        "atRisk": "Em risco",
        "goodPace": "Bom ritmo",
        "noReadings": "Sem leituras"
      },
      "recommendations": {
        "inControl": "A meta avança dentro do ritmo esperado.",
        "outOfGoal": "O consumo superou o limite definido para esta meta.",
        "atRisk": "O ritmo atual pode superar o limite. Revise os equipamentos com maior atividade.",
        "goodPace": "Você mantém margem suficiente em relação ao avanço esperado.",
        "noReadings": "Ainda não há leituras dentro do período desta meta."
      },
      "toasts": {
        "loadError": "Não foi possível carregar as informações de metas energéticas.",
        "nameRequired": "Digite o nome da meta.",
        "nameMinLength": "O nome da meta deve ter pelo menos 4 caracteres.",
        "targetRequired": "Defina um limite de consumo maior que 0 kWh.",
        "deadlineRequired": "Selecione uma data limite para a meta.",
        "deadlinePast": "A data limite não pode estar no passado.",
        "timeWindowInvalid": "A janela horária deve ter hora de início e fim diferentes.",
        "scopeTargetRequired": "Selecione o objetivo da meta.",
        "deleteSuccess": "Meta energética eliminada."
      },
      "deleteDialog": {
        "title": "Eliminar meta energética",
        "message": "Esta ação removerá a meta do acompanhamento atual.",
        "confirm": "Eliminar",
        "cancel": "Manter"
      }
    }
  },
  "serviceManagement": {
    "supportEyebrow": "Suporte",
    "supportTitle": "Tickets de suporte",
    "supportSubtitle": "Crie e acompanhe solicitações de suporte do usuário.",
    "maintenanceEyebrow": "Manutenção",
    "maintenanceTitle": "Tickets de manutenção",
    "maintenanceSubtitle": "Agende e monitore tarefas de manutenção de dispositivos.",
    "totalSupportTickets": "Total de tickets de suporte",
    "openTickets": "Tickets abertos",
    "urgentTickets": "Tickets urgentes",
    "totalMaintenanceTickets": "Total de tickets de manutenção",
    "pendingMaintenance": "Manutenções pendentes",
    "scheduledMaintenance": "Manutenções agendadas",
    "completedMaintenance": "Manutenções concluídas",
    "createSupportTicket": "Criar ticket de suporte",
    "createMaintenanceTicket": "Criar ticket de manutenção",
    "subject": "Assunto",
    "description": "Descrição",
    "scheduledDate": "Data agendada",
    "selectDevice": "Selecione um dispositivo",
    "create": "Criar",
    "emptySupportTitle": "Não há tickets de suporte",
    "emptySupportContent": "Os tickets de suporte criados pelo usuário aparecerão aqui.",
    "emptyMaintenanceTitle": "Não há tickets de manutenção",
    "emptyMaintenanceContent": "Os tickets de manutenção agendados aparecerão aqui.",
    "loadError": "Não foi possível carregar as informações de gestão de serviços.",
    "supportCreateSuccess": "Ticket de suporte criado com sucesso.",
    "supportCreateError": "Não foi possível criar o ticket de suporte.",
    "supportUpdateSuccess": "Ticket de suporte atualizado com sucesso.",
    "supportUpdateError": "Não foi possível atualizar o ticket de suporte.",
    "supportDeleteError": "Não foi possível excluir o ticket de suporte.",
    "maintenanceCreateSuccess": "Ticket de manutenção criado com sucesso.",
    "maintenanceCreateError": "Não foi possível criar o ticket de manutenção.",
    "maintenanceUpdateSuccess": "Ticket de manutenção atualizado com sucesso.",
    "maintenanceUpdateError": "Não foi possível atualizar o ticket de manutenção.",
    "maintenanceDeleteError": "Não foi possível excluir o ticket de manutenção.",
    "supportPageDescription": "Centralize solicitações do usuário, priorize casos abertos e mantenha o acompanhamento com estados reais.",
    "supportSummaryTitle": "Resumo de suporte",
    "supportSummaryDescription": "Solicitações registradas com rastreabilidade na plataforma.",
    "openTicketsShort": "Abertas",
    "urgentTicketsShort": "Urgentes",
    "newSupportRequest": "Nova solicitação",
    "supportAttentionTitle": "Atendimento de suporte",
    "supportAttentionDescription": "Registre um caso com assunto, descrição e prioridade para atendimento pelo Serviço.",
    "createSupportRequest": "Criar solicitação",
    "cases": "Casos",
    "supportCasesTitle": "Solicitações ativas e históricas",
    "emptySupportRequestsTitle": "Não há solicitações",
    "emptySupportRequestsDescription": "Quando alguém registrar um caso de suporte, ele aparecerá aqui com prioridade, estado e data.",
    "supportModalDescription": "Registre um caso de suporte com prioridade real.",
    "supportSubjectPlaceholder": "Ex. Equipamento sem resposta",
    "supportDescriptionPlaceholder": "Descreva o que acontece, onde acontece e qual impacto tem.",
    "priority": "Prioridade",
    "saveSupportRequest": "Salvar solicitação",
    "supportLoadError": "Não foi possível carregar as informações de suporte.",
    "supportDeleteSuccess": "Solicitação de suporte excluída.",
    "confirmDeleteSupportTitle": "Excluir solicitação",
    "confirmDeleteSupportMessage": "Esta solicitação sairá da bandeja de suporte. Esta ação não pode ser desfeita.",
    "createdOn": "Criada em {{date}}",
    "maintenancePageDescription": "Agende intervenções em dispositivos reais e acompanhe seu estado sem inventar datas, equipamentos ou tarefas.",
    "maintenanceSummaryTitle": "Resumo de manutenção",
    "maintenanceSummaryDescription": "Ordens criadas pelo Serviço para dispositivos existentes.",
    "pendingMaintenanceShort": "Pendentes",
    "completedMaintenanceShort": "Concluídas",
    "scheduleEyebrow": "Agendar",
    "technicalScheduleTitle": "Agenda técnica",
    "technicalScheduleDescription": "Selecione um dispositivo existente, motivo e data para criar uma ordem.",
    "scheduleMaintenance": "Agendar manutenção",
    "goToDevices": "Ir para dispositivos",
    "orders": "Ordens",
    "maintenanceAgendaTitle": "Agenda de manutenção",
    "emptyMaintenanceDescription": "Quando você agendar uma intervenção, ela aparecerá aqui com dispositivo, data e estado.",
    "maintenanceModalDescription": "Use um dispositivo real e uma data válida.",
    "maintenanceType": "Tipo de manutenção",
    "reason": "Motivo",
    "maintenanceDescriptionPlaceholder": "Descreva o trabalho necessário ou o motivo da revisão.",
    "saveMaintenance": "Salvar manutenção",
    "maintenanceLoadError": "Não foi possível carregar as informações de manutenção.",
    "maintenanceDeleteSuccess": "Manutenção excluída.",
    "confirmCancelMaintenanceTitle": "Cancelar manutenção",
    "confirmCancelMaintenanceMessage": "A manutenção será marcada como cancelada. Confirme somente se ela não deve mais ser executada.",
    "cancelMaintenance": "Cancelar manutenção",
    "back": "Voltar",
    "confirmDeleteMaintenanceTitle": "Excluir manutenção",
    "confirmDeleteMaintenanceMessage": "Esta programação sairá da lista de manutenção. Esta ação não pode ser desfeita.",
    "scheduledFor": "Agendada para {{date}}",
    "priorities": {
      "low": "Baixa",
      "medium": "Média",
      "high": "Alta",
      "urgent": "Urgente"
    },
    "supportStatuses": {
      "open": "Aberto",
      "inProgress": "Em progresso",
      "resolved": "Resolvido",
      "closed": "Fechado"
    },
    "maintenanceTypes": {
      "inspection": "Inspeção",
      "repair": "Reparo",
      "replacement": "Substituição",
      "installation": "Instalação"
    },
    "maintenanceStatuses": {
      "pending": "Pendente",
      "scheduled": "Agendada",
      "completed": "Concluída",
      "canceled": "Cancelada"
    },
    "validation": {
      "supportSubjectRequired": "Digite o assunto da solicitação.",
      "supportSubjectMinLength": "O assunto deve ter pelo menos 4 caracteres.",
      "supportDescriptionRequired": "Descreva o problema ou a solicitação.",
      "descriptionMinLength": "A descrição deve ter pelo menos 10 caracteres.",
      "deviceRequiredBeforeMaintenance": "Registre um dispositivo antes de agendar manutenção.",
      "maintenanceDeviceRequired": "Selecione um dispositivo para manutenção.",
      "maintenanceDescriptionRequired": "Descreva a manutenção necessária.",
      "maintenanceDateRequired": "Selecione uma data de manutenção.",
      "maintenanceDatePast": "A data de manutenção não pode estar no passado.",
      "maintenanceDeviceUnknown": "Não foi possível identificar o dispositivo selecionado."
    }
  },
  "security": {
    "deleteAccount": "Excluir conta"
  },
  "energyDashboard": {
    "eyebrow": "Monitoramento de energia",
    "title": "Consumo energético",
    "description": "Controle o consumo real dos dispositivos ligados, revise kWh, custos estimados e oportunidades de economia por equipamento e ambiente.",
    "liveConsumptionEyebrow": "Consumo ao vivo",
    "liveConsumptionTitle": "Gasto calculado a partir de potência e tempo real",
    "liveConsumptionDescription": "W é potência nominal. O gasto real vem de kWh, segundos ligados e tarifa energética.",
    "automaticRecord": "Registro automático",
    "secondsAbbr": "seg",
    "apply": "Aplicar",
    "sampleEveryPrefix": "Uma leitura será registrada a cada",
    "sampleEveryValue": "{{seconds}} segundos",
    "exportCsv": "Exportar CSV",
    "loading": "Carregando informações energéticas...",
    "consumptionEyebrow": "Consumo",
    "readings": "leituras",
    "equipment": "equipamentos",
    "energy": "energia",
    "cost": "custo",
    "accumulatedUse": "uso acumulado",
    "period": "Período",
    "noReadingsEyebrow": "Sem leituras",
    "noConsumptionTitle": "Não há consumo para este período",
    "noConsumptionDescription": "Quando houver leituras reais no período selecionado, o gráfico será construído automaticamente.",
    "generalStatusEyebrow": "Estado geral",
    "operationalSummaryTitle": "Resumo operacional",
    "normalReadings": "normais",
    "highReadingsShort": "altas",
    "activeDevicesShort": "ativos",
    "activeDevicesNowTitle": "Dispositivos ligados agora",
    "costPerHourValue": "S/ {{cost}} por hora",
    "moreActiveDevices": "+{{count}} mais em ativos",
    "noActivityEyebrow": "Sem atividade",
    "noActiveDevicesTitle": "Não há equipamentos ligados",
    "noActiveDevicesDescription": "O consumo atual está em zero porque não há dispositivos ON.",
    "rankingEyebrow": "Ranking",
    "highestCostByDeviceTitle": "Maior gasto por dispositivo",
    "moreInRanking": "+{{count}} mais no ranking",
    "noCostEyebrow": "Sem gasto",
    "noDeviceConsumptionTitle": "Não há dispositivos com consumo",
    "noDeviceConsumptionDescription": "As leituras com kWh aparecerão durante o uso.",
    "roomsEyebrow": "Ambientes",
    "consumptionByRoomTitle": "Consumo por ambiente",
    "activeDevicesValue": "{{count}} ativos",
    "moreInRooms": "+{{count}} mais em ambientes",
    "noRoomsEyebrow": "Sem ambientes",
    "noRoomDataTitle": "Sem dados por ambiente",
    "noRoomDataDescription": "Atribua ambientes aos seus dispositivos para ver este detalhamento.",
    "recentHighConsumptionEventsTitle": "Eventos recentes de alto consumo",
    "moreInAlerts": "+{{count}} mais em alertas",
    "noAlertsEyebrow": "Sem alertas",
    "noRecentEventsTitle": "Não há eventos recentes",
    "noRecentEventsDescription": "Não foram detectadas leituras altas no período visível.",
    "latestReadingsTitle": "Últimas leituras energéticas",
    "power": "Potência",
    "moreInHistory": "+{{count}} mais no histórico",
    "noHistoryEyebrow": "Sem histórico",
    "noReadingsAvailableTitle": "Não há leituras disponíveis",
    "noReadingsAvailableDescription": "Quando o sistema registrar consumo, você verá as últimas leituras aqui.",
    "intervalValue": "Intervalo {{duration}}",
    "chartItemAria": "{{energy}}, {{emoji}} {{device}}, {{watts}} W, {{date}}, intervalo {{duration}}",
    "highConsumptionRegistered": "{{device}} registrou consumo alto",
    "noActiveAlerts": "Sem alertas ativos",
    "selectActiveSiteRecommendation": "Selecione uma sede ativa para ver seu consumo.",
    "noReadingsForSiteRecommendation": "Ainda não há leituras energéticas para {{site}}.",
    "highReadingsForSiteRecommendation": "Revise os equipamentos de {{site}} com leituras altas.",
    "stableConsumptionRecommendation": "Consumo estável em {{site}}.",
    "noRoom": "Sem ambiente",
    "periods": {
      "daily": "Diário",
      "weekly": "Semanal",
      "monthly": "Mensal"
    },
    "periodDescriptions": {
      "daily": "Leituras reais registradas hoje.",
      "weekly": "Leituras reais dos últimos 7 dias.",
      "monthly": "Leituras reais do mês atual."
    },
    "chartTitles": {
      "daily": "Consumo diário",
      "weekly": "Consumo semanal",
      "monthly": "Consumo mensal"
    },
    "chartSubtitles": {
      "daily": "Leituras reais de hoje. O uso acumulado soma tempo por equipamento.",
      "weekly": "Leituras reais dos últimos 7 dias. O uso acumulado soma tempo por equipamento.",
      "monthly": "Leituras reais do mês atual. O uso acumulado soma tempo por equipamento."
    },
    "operationalStatus": {
      "review": "Revisar consumo",
      "attention": "Atenção",
      "optimal": "Ótimo"
    },
    "metrics": {
      "activePower": "Potência ativa",
      "energyToday": "Energia hoje",
      "costToday": "Gasto hoje",
      "costPerHour": "Custo por hora",
      "monthlyProjection": "Projeção mensal",
      "activeAlerts": "Alertas ativos"
    },
    "metricHints": {
      "activePower": "Base atual para calcular kWh enquanto os equipamentos estão ligados.",
      "energyToday": "Acumulado por intervalos reais registrados hoje.",
      "costToday": "Custo estimado a partir de kWh reais.",
      "costPerHour": "Projeção se o consumo atual se mantiver.",
      "monthlyProjection": "Estimado a partir dos kWh reais acumulados no mês.",
      "activeAlerts": "{{count}} críticos - {{level}} - {{title}}"
    },
    "readingStatuses": {
      "high": "Alta",
      "normal": "Normal"
    },
    "toasts": {
      "loadError": "Não foi possível carregar as informações energéticas.",
      "exportSuccess": "CSV exportado com sucesso.",
      "exportError": "Não foi possível exportar o CSV.",
      "intervalUpdated": "Intervalo atualizado para {{seconds}} segundos.",
      "intervalUpdateError": "Não foi possível atualizar o intervalo de registro.",
      "samplingLoadFallback": "Não foi possível carregar o intervalo de registro. Serão usados 15 segundos.",
      "refreshError": "Não foi possível atualizar as informações energéticas."
    }
  },
  "shared": {
    "numberStepper": {
      "increase": "Aumentar valor",
      "decrease": "Reduzir valor"
    },
    "datePicker": {
      "placeholder": "Selecionar data",
      "calendarAria": "Seletor de data",
      "previousMonth": "Mês anterior",
      "nextMonth": "Mês seguinte",
      "clear": "Limpar",
      "today": "Hoje"
    },
    "timePicker": {
      "panelAria": "Seletor de hora",
      "hoursAria": "Horas",
      "minutesAria": "Minutos"
    },
    "modal": {
      "closeForm": "Fechar formulário"
    }
  },
  "operationModes": {
    "pageDescription": "Configure modos com rotinas internas próprias para uma sede ou cômodo.",
    "site": "Sede",
    "internalRoutines": "Rotinas internas",
    "summary": {
      "context": "Contexto",
      "availableDevices": "{{count}} dispositivo(s) disponíveis.",
      "saved": "Salvos",
      "hasActiveMode": "Há um modo ativo.",
      "noActiveMode": "Sem modo ativo.",
      "current": "Atual",
      "selectMode": "Selecione um modo."
    },
    "toolbar": {
      "eyebrow": "Configuração operacional",
      "title": "Modos com rotinas próprias",
      "description": "As rotinas de um modo vivem dentro do modo e se aplicam somente ao ativar esse modo."
    },
    "actions": {
      "create": "Criar modo",
      "reapply": "Reaplicar",
      "archive": "Arquivar",
      "add": "Adicionar",
      "remove": "Remover"
    },
    "empty": {
      "title": "Não há modos nesta sede",
      "description": "Crie um modo com rotinas internas para aplicar uma configuração operacional real."
    },
    "impact": {
      "title": "Impacto",
      "applicationTitle": "Aplicação do modo",
      "affectedDevices": "{{count}} dispositivo(s)",
      "ignored": "Ignorados",
      "emptyTitle": "Selecione um modo",
      "emptyDescription": "Revise quais dispositivos e rotinas internas serão aplicados antes de ativar."
    },
    "form": {
      "newMode": "Novo modo",
      "description": "Defina identidade, aplicação, horário e rotinas internas próprias.",
      "identityTitle": "Identidade",
      "identityDescription": "Nome e descrição controlada do modo.",
      "namePlaceholder": "Ex. Noite segura",
      "descriptionPlaceholder": "Uso operacional do modo",
      "applicationTitle": "Aplicação",
      "applicationDescription": "O modo pode cobrir uma sede completa ou um cômodo.",
      "selectSite": "Selecione sede",
      "selectRoom": "Selecione cômodo",
      "scheduleType": "Tipo de horário",
      "end": "Fim",
      "modeRoutinesTitle": "Rotinas do modo",
      "modeRoutinesDescription": "Estas rotinas pertencem somente a este modo.",
      "label": "Etiqueta",
      "routineNamePlaceholder": "Ex. Desligar luzes",
      "selectTarget": "Selecione alvo",
      "noTargetsInScope": "Não há alvos disponíveis neste escopo.",
      "noInternalRoutinesTitle": "Ainda não há rotinas internas",
      "noInternalRoutinesDescription": "Adicione pelo menos uma rotina interna para salvar o modo."
    },
    "applicationOptions": {
      "workplace": "Sede completa",
      "room": "Cômodo específico"
    },
    "applicationDescriptions": {
      "workplace": "O modo se aplica a toda a sede ativa",
      "room": "O modo se aplica somente a um cômodo"
    },
    "scheduleOptions": {
      "allDay": "Todo o dia",
      "custom": "Personalizado"
    },
    "scheduleDescriptions": {
      "allDay": "O modo mantém sua configuração sem janela horária",
      "custom": "Definir início e fim"
    },
    "routineTargetTypes": {
      "device": "Dispositivo",
      "group": "Grupo"
    },
    "routineTargetDescriptions": {
      "device": "Ação sobre um equipamento",
      "group": "Ação sobre um grupo"
    },
    "routineActionDescriptions": {
      "turnOn": "Ativar alvo",
      "turnOff": "Desativar alvo"
    },
    "routineTargets": {
      "deviceCount": "{{count}} dispositivo(s)"
    },
    "goals": {
      "description": "Referência a metas reais sem prometer controle adicional.",
      "emptyTitle": "Sem metas aplicáveis",
      "emptyDescription": "O modo pode ser salvo sem metas associadas.",
      "fallback": "Meta {{id}}"
    },
    "alertsRules": {
      "title": "Alertas/Regras",
      "description": "Use regras e preferências existentes."
    },
    "rules": {
      "available": "Regras disponíveis",
      "none": "Sem regras",
      "noneDescription": "O modo não aplicará regras"
    },
    "preferences": {
      "current": "Preferência atual",
      "useRegistered": "Usar preferência registrada",
      "noneRegistered": "Sem preferência registrada",
      "numbered": "Preferência {{id}}",
      "applyThis": "Aplicar esta preferência"
    },
    "toggles": {
      "applyRulesTitle": "Aplicar regras",
      "applyRulesDescription": "Usa regras existentes.",
      "applyPreferencesTitle": "Aplicar preferências",
      "applyPreferencesDescription": "Usa preferências existentes.",
      "criticalSoundTitle": "Som crítico",
      "criticalSoundDescription": "Respeita alertas críticos se aplicável."
    },
    "validation": {
      "siteRequired": "Selecione uma sede antes de criar o modo.",
      "nameRequired": "Digite um nome para o modo.",
      "roomRequired": "Selecione um cômodo para o modo.",
      "noTargets": "Não há dispositivos ou grupos disponíveis no escopo selecionado.",
      "routineRequired": "Adicione uma rotina interna ou complete a rotina em edição.",
      "routineDraftRequired": "Complete nome e alvo da rotina do modo.",
      "minimumRequired": "Defina sede, nome e pelo menos uma rotina interna."
    },
    "confirm": {
      "replaceTitle": "Substituir modo ativo",
      "replaceMessage": "Já existe um modo ativo nesta sede. Se você ativar este modo, o anterior será desativado.",
      "archiveTitle": "Arquivar modo",
      "archiveMessage": "{{name}} será arquivado. Suas rotinas internas deixarão de atuar."
    },
    "toasts": {
      "createError": "Não foi possível criar o modo.",
      "createSuccess": "Modo criado com rotinas internas.",
      "activateError": "Não foi possível ativar o modo.",
      "archived": "Modo arquivado."
    },
    "status": {
      "active": "Ativo",
      "inactive": "Inativo",
      "archived": "Arquivado"
    },
    "fallbacks": {
      "noSelectedSite": "Sem sede selecionada",
      "siteUnavailable": "Sede indisponível",
      "operationalSite": "Sede operacional",
      "roomUnavailable": "Cômodo indisponível",
      "groupUnavailable": "Grupo indisponível",
      "deviceUnavailable": "Dispositivo indisponível",
      "noDescription": "Sem descrição registrada."
    }
  },
  "settings": {
    "shell": {
      "ariaLabel": "Configurações da conta",
      "eyebrow": "Conta",
      "description": "Preferências reais de usuário e plataforma."
    },
    "nav": {
      "profile": "Perfil",
      "profileDescription": "Dados pessoais",
      "account": "Conta",
      "accountDescription": "Identidade e estado",
      "security": "Segurança",
      "securityDescription": "Acesso e recuperação",
      "billing": "Faturamento",
      "billingDescription": "Plano, pagamentos e faturas",
      "platform": "Plataforma",
      "platformDescription": "Preferências locais"
    },
    "common": {
      "email": "E-mail",
      "notAvailable": "Não disponível"
    },
    "profile": {
      "title": "Dados pessoais",
      "pageDescription": "Gerencie o nome e e-mail salvos na sua conta ElectroCorp.",
      "basicInfoTitle": "Informações básicas",
      "basicInfoDescription": "Estes dados são salvos na sua conta ElectroCorp.",
      "saveProfile": "Salvar perfil",
      "previewEyebrow": "Prévia",
      "dataUsageTitle": "Uso destes dados",
      "dataUsageDescription": "Referência rápida de como nome e e-mail aparecem dentro da ElectroCorp.",
      "visibleName": "Nome visível",
      "visibleNameDescription": "Usado para identificar sua conta no painel lateral e nas visualizações de usuário.",
      "accountEmail": "E-mail da conta",
      "accountEmailDescription": "Usado como referência para recuperação de acesso e dados principais do perfil.",
      "unsavedChanges": "Alterações não salvas",
      "noPendingChanges": "Sem alterações pendentes",
      "statusDescription": "Salve o perfil para sincronizar os dados com a sessão atual.",
      "updateSuccess": "Perfil atualizado.",
      "updateError": "Não foi possível atualizar o perfil."
    },
    "account": {
      "title": "Estado da conta",
      "pageDescription": "Revise identidade, função e estado geral do usuário autenticado.",
      "identityDescription": "Resumo compacto do usuário autenticado.",
      "role": "Função",
      "userFallback": "Usuário",
      "createdAt": "Criada",
      "contextEyebrow": "Contexto",
      "operationalReadingTitle": "Leitura operacional",
      "operationalReadingDescription": "Elementos que ajudam a entender o alcance desta conta sem misturar ações sensíveis.",
      "session": "Sessão",
      "active": "Ativa",
      "sessionDescription": "Esta visualização aparece apenas para um usuário autenticado pelo guard de acesso.",
      "editableData": "Dados editáveis",
      "separatedInProfile": "Separados em Perfil",
      "editableDataDescription": "Nome e e-mail são atualizados em Perfil para manter esta página como resumo da conta.",
      "sensitiveActions": "Ações sensíveis",
      "separatedInSecurity": "Separadas em Segurança",
      "sensitiveActionsDescription": "Recuperação e exclusão de conta ficam em uma tela independente para reduzir erros."
    },
    "security": {
      "title": "Acesso e recuperação",
      "pageDescription": "Gerencie ações sensíveis de acesso com chamadas reais disponíveis.",
      "accessActionsTitle": "Ações de acesso",
      "accessActionsDescription": "Apenas ações com chamada real disponível são exibidas.",
      "recovery": "Recuperação",
      "sendRecoveryTitle": "Enviar link para o e-mail atual",
      "sendRecoveryDescription": "Use o fluxo real de recuperação de acesso para o e-mail salvo no perfil.",
      "sendRecoveryButton": "Enviar recuperação",
      "dangerZone": "Zona delicada",
      "deleteAccountDescription": "Abre uma confirmação rigorosa antes de excluir a conta atual.",
      "protectionsEyebrow": "Proteções",
      "securityStatusTitle": "Estado de segurança",
      "securityStatusDescription": "Resumo de controles ativos antes de executar ações sensíveis.",
      "recoveryEmail": "E-mail de recuperação",
      "recoveryEmailDescription": "O link de recuperação é solicitado para o e-mail atual da conta autenticada.",
      "writtenConfirmation": "Confirmação escrita",
      "writtenConfirmationDescription": "A exclusão fica bloqueada até que o texto de confirmação coincida exatamente.",
      "finalConfirmation": "Confirmação final",
      "dialogRequired": "Diálogo obrigatório",
      "finalConfirmationDescription": "A conta só é excluída depois de aceitar o diálogo de confirmação com tom de perigo.",
      "noRecoveryEmail": "Não há e-mail registrado para recuperar acesso.",
      "recoverySent": "Solicitação de recuperação enviada.",
      "recoveryError": "Não foi possível enviar a recuperação.",
      "deleteDialogTitle": "Excluir permanentemente sua conta",
      "deleteDialogMessage": "Esta ação exclui sua conta ElectroCorp, encerra sua sessão e não pode ser desfeita pela interface.",
      "deleteDialogDetail": "Antes de continuar, confirme que você não precisa recuperar acesso a dados, faturas ou preferências associadas a esta conta.",
      "deleteDialogConfirm": "Excluir conta permanentemente",
      "deleteDialogCancel": "Não excluir",
      "requiredTextLabel": "Para confirmar, digite exatamente esta palavra:",
      "requiredTextHint": "O botão de exclusão permanecerá bloqueado até que a confirmação coincida exatamente.",
      "deleteAccountError": "Não foi possível excluir a conta."
    },
    "platform": {
      "title": "Preferências locais",
      "pageDescription": "Ajuste a aparência e o idioma da ElectroCorp neste navegador.",
      "browserExperienceTitle": "Experiência do navegador",
      "browserExperienceDescription": "Aplicadas neste navegador e mantidas localmente.",
      "darkMode": "Modo escuro",
      "lightMode": "Modo claro",
      "darkModeDescription": "Altere a aparência da interface neste navegador.",
      "language": "Idioma",
      "languages": {
        "es": "Espanhol",
        "esDescription": "Idioma local da interface.",
        "en": "Inglês",
        "enDescription": "Idioma local da interface.",
        "pt": "Português",
        "ptDescription": "Idioma local da interface."
      },
      "languageApplied": "Idioma aplicado neste navegador.",
      "appearanceApplied": "Aparência aplicada neste navegador.",
      "localStatusEyebrow": "Estado local",
      "preferencesSummaryTitle": "Resumo de preferências",
      "preferencesSummaryDescription": "Confirme como a experiência está configurada neste navegador.",
      "appearance": "Aparência",
      "appearanceDescription": "A preferência visual se aplica ao shell e aos componentes compartilhados da interface.",
      "activeLanguage": "Idioma ativo",
      "activeLanguageDescription": "O idioma selecionado é mantido localmente para preservar a navegação consistente.",
      "scope": "Alcance",
      "local": "Local",
      "scopeDescription": "Estes ajustes vivem no navegador atual e ficam prontos para sincronização com preferências de usuário."
    },
    "billing": {
      "title": "Plano e pagamentos",
      "pageDescription": "Gerencie sua assinatura ativa, revise pagamentos registrados e baixe faturas emitidas para sua conta.",
      "subscriptionEyebrow": "Assinatura",
      "planStatusTitle": "Estado do plano",
      "planStatusDescription": "Informações reais do plano ativo, limites operacionais e atividade de pagamento.",
      "perMonth": "por mês",
      "choosePlanDescription": "Escolha um plano para ativar a plataforma completa.",
      "exports": "Exportações",
      "activeExports": "Ativas",
      "notIncluded": "Não incluídas",
      "viewPlans": "Ver planos",
      "lastPayment": "Último pagamento",
      "noPaymentsRegistered": "Não há pagamentos registrados.",
      "payments": "Pagamentos",
      "registeredMovements": "Movimentos registrados",
      "registeredMovementsDescription": "Pagamentos recebidos pelo fluxo real de assinatura.",
      "paymentNumber": "Pagamento #{{id}}",
      "method": "Método",
      "noPaymentsTitle": "Não há pagamentos",
      "noPaymentsDescription": "Os pagamentos aparecerão quando você ativar ou renovar um plano.",
      "issuedDocuments": "Documentos emitidos",
      "issuedDocumentsDescription": "Baixe apenas faturas existentes na sua conta.",
      "invoiceNumber": "Fatura #{{id}}",
      "issuedOn": "Emitida em {{date}}",
      "download": "Baixar",
      "noInvoicesTitle": "Não há faturas",
      "noInvoicesDescription": "Quando houver uma fatura emitida para sua conta, ela aparecerá aqui.",
      "loadError": "Não foi possível carregar o histórico de faturamento.",
      "invoiceNotFound": "Fatura não encontrada.",
      "downloadSuccess": "Fatura baixada com sucesso.",
      "downloadError": "Não foi possível baixar a fatura.",
      "cancelPlanTitle": "Cancelar plano",
      "cancelPlanMessage": "Sua assinatura ativa será cancelada. Confirme somente se você não deseja continuar com o plano.",
      "cancelPlanConfirm": "Cancelar plano",
      "keepPlan": "Manter plano",
      "cancelSuccess": "Plano cancelado.",
      "cancelError": "Não foi possível cancelar o plano.",
      "unlimited": "Ilimitado",
      "noActivePlan": "Sem plano ativo",
      "subscriptionStatuses": {
        "active": "Ativo",
        "cancelled": "Cancelado",
        "expired": "Expirado"
      },
      "paymentStatuses": {
        "approved": "Aprovado",
        "rejected": "Rejeitado",
        "pending": "Pendente"
      },
      "invoiceIssued": "Emitida",
      "invoiceRegistered": "Registrada",
      "goToPlans": "Ir para planos"
    }
  }
}


============================================================
FILE: README.md
============================================================

# ElectrocorpWebapp

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 21.2.10.

## Development server

To start a local development server, run:

```bash
ng serve
```

Once the server is running, open your browser and navigate to `http://localhost:4200/`. The application will automatically reload whenever you modify any of the source files.

## Code scaffolding

Angular CLI includes powerful code scaffolding tools. To generate a new component, run:

```bash
ng generate component component-name
```

For a complete list of available schematics (such as `components`, `directives`, or `pipes`), run:

```bash
ng generate --help
```

## Building

To build the project run:

```bash
ng build
```

This will compile your project and store the build artifacts in the `dist/` directory. By default, the production build optimizes your application for performance and speed.

## Running unit tests

To execute unit tests with the [Vitest](https://vitest.dev/) test runner, use the following command:

```bash
ng test
```

## Running end-to-end tests

For end-to-end (e2e) testing, run:

```bash
ng e2e
```

Angular CLI does not come with an end-to-end testing framework by default. You can choose one that suits your needs.

## Additional Resources

For more information on using the Angular CLI, including detailed command references, visit the [Angular CLI Overview and Command Reference](https://angular.dev/tools/cli) page.

============================================================
FILE: src\app\app.config.ts
============================================================

import {
  ApplicationConfig,
  APP_INITIALIZER,
  provideBrowserGlobalErrorListeners,
  provideZonelessChangeDetection,
} from '@angular/core';
import { provideRouter } from '@angular/router';
import { provideHttpClient, withFetch, withInterceptors } from '@angular/common/http';

import { provideTranslateService } from '@ngx-translate/core';
import { provideTranslateHttpLoader } from '@ngx-translate/http-loader';

import { routes } from './app.routes';
import { environment } from '../environments/environment';
import { API_BASE_URL } from './shared/infrastructure/api/api-config';
import { authInterceptor } from './shared/application/interceptors/auth.interceptor';
import { IamFacade } from './iam/application/services/iam.facade';
import { DEFAULT_LANGUAGE } from './shared/infrastructure/constants/app-language';

export const appConfig: ApplicationConfig = {
  providers: [
    provideBrowserGlobalErrorListeners(),
    provideZonelessChangeDetection(),
    provideRouter(routes),
    provideHttpClient(withFetch(), withInterceptors([authInterceptor])),

    {
      provide: APP_INITIALIZER,
      multi: true,
      deps: [IamFacade],
      useFactory: (iamFacade: IamFacade) => () => iamFacade.restoreSession(),
    },

    {
      provide: API_BASE_URL,
      useValue: environment.apiBaseUrl,
    },

    provideTranslateService({
      loader: provideTranslateHttpLoader({
        prefix: '/assets/i18n/',
        suffix: '.json',
      }),
      fallbackLang: DEFAULT_LANGUAGE,
      lang: DEFAULT_LANGUAGE,
    }),
  ],
};


============================================================
FILE: src\app\app.html
============================================================

<app-layout />

============================================================
FILE: src\app\app.routes.ts
============================================================

import { Routes } from '@angular/router';

import { BILLING_ROUTES } from './billing/presentation/routes/billing.routes';
import { DEVICE_CONTROL_ROUTES } from './device-control/presentation/routes/device-control.routes';
import { ENERGY_MONITORING_ROUTES } from './energy-monitoring/presentation/routes/energy-monitoring.routes';
import { IAM_ROUTES } from './iam/presentation/routes/iam.routes';
import { NOTIFICATIONS_ROUTES } from './notifications/presentation/routes/notifications.routes';
import { REPORTING_ROUTES } from './reporting/presentation/routes/reporting.routes';
import { SERVICE_MANAGEMENT_ROUTES } from './service-management/presentation/routes/service-management.routes';
import { activeSubscriptionGuard } from './shared/application/guards/active-subscription.guard';
import { authGuard } from './shared/application/guards/auth.guard';
import { WORKPLACE_ROUTES } from './workplace/presentation/routes/workplace.routes';

export const routes: Routes = [
  {
    path: '',
    redirectTo: 'iam/login',
    pathMatch: 'full',
  },

  {
    path: 'settings',
    canActivate: [authGuard],
    loadComponent: () =>
      import(
        './shared/presentation/components/settings-shell/settings-shell.component'
      ).then((m) => m.SettingsShellComponent),
    children: [
      {
        path: '',
        redirectTo: 'profile',
        pathMatch: 'full',
      },
      {
        path: 'profile',
        data: { title: 'Perfil' },
        loadComponent: () =>
          import('./iam/presentation/pages/profile/profile-page.component').then(
            (m) => m.ProfilePageComponent
          ),
      },
      {
        path: 'account',
        data: { title: 'Cuenta' },
        loadComponent: () =>
          import('./iam/presentation/pages/account/account-page.component').then(
            (m) => m.AccountPageComponent
          ),
      },
      {
        path: 'security',
        data: { title: 'Seguridad' },
        loadComponent: () =>
          import('./iam/presentation/pages/security/security-page.component').then(
            (m) => m.SecurityPageComponent
          ),
      },
      {
        path: 'notifications',
        redirectTo: '/alerts/preferences',
        pathMatch: 'full',
      },
      {
        path: 'billing',
        data: { title: 'Facturacion' },
        loadComponent: () =>
          import(
            './billing/presentation/pages/billing-history/billing-history-page.component'
          ).then((m) => m.BillingHistoryPageComponent),
      },
      {
        path: 'platform',
        data: { title: 'Plataforma' },
        loadComponent: () =>
          import('./iam/presentation/pages/platform/platform-page.component').then(
            (m) => m.PlatformPageComponent
          ),
      },
    ],
  },

  ...IAM_ROUTES,
  ...BILLING_ROUTES,
  ...DEVICE_CONTROL_ROUTES,
  ...ENERGY_MONITORING_ROUTES,
  ...NOTIFICATIONS_ROUTES,
  ...WORKPLACE_ROUTES,
  ...REPORTING_ROUTES,
  ...SERVICE_MANAGEMENT_ROUTES,

  {
    path: 'home',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Home' },
    loadComponent: () =>
      import('./shared/presentation/pages/home/home-page.component').then(
        (m) => m.HomePageComponent
      ),
  },
  {
    path: 'about',
    data: { title: 'About' },
    loadComponent: () =>
      import('./shared/presentation/pages/about/about-page.component').then(
        (m) => m.AboutPageComponent
      ),
  },
  {
    path: '**',
    canActivate: [authGuard],
    data: { title: 'Page Not Found' },
    loadComponent: () =>
      import(
        './shared/presentation/pages/not-found/not-found-page.component'
        ).then((m) => m.NotFoundPageComponent),
  },
];


============================================================
FILE: src\app\app.scss
============================================================

:host {
  display: block;
  min-height: 100vh;
}

============================================================
FILE: src\app\app.spec.ts
============================================================

import { TestBed } from '@angular/core/testing';
import { App } from './app';

describe('App', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [App],
    }).compileComponents();
  });

  it('should create the app', () => {
    const fixture = TestBed.createComponent(App);
    const app = fixture.componentInstance;
    expect(app).toBeTruthy();
  });

  it('should render title', async () => {
    const fixture = TestBed.createComponent(App);
    await fixture.whenStable();
    const compiled = fixture.nativeElement as HTMLElement;
    expect(compiled.querySelector('h1')?.textContent).toContain('Hello, electrocorp-webapp');
  });
});

============================================================
FILE: src\app\app.ts
============================================================

import { Component, OnInit } from '@angular/core';
import { ActivatedRoute, NavigationEnd, Router } from '@angular/router';
import { Title } from '@angular/platform-browser';
import { filter } from 'rxjs';

import { AppLayoutComponent } from './shared/presentation/components/app-layout/app-layout.component';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [AppLayoutComponent],
  templateUrl: './app.html',
  styleUrls: ['./app.scss'],
})
export class App implements OnInit {
  private readonly appName = 'ElectroCorp';

  constructor(
    private readonly router: Router,
    private readonly activatedRoute: ActivatedRoute,
    private readonly titleService: Title
  ) {}

  ngOnInit(): void {
    this.updatePageTitle();

    this.router.events
      .pipe(filter((event) => event instanceof NavigationEnd))
      .subscribe(() => this.updatePageTitle());
  }

  private updatePageTitle(): void {
    let route = this.activatedRoute;

    while (route.firstChild) {
      route = route.firstChild;
    }

    const pageTitle = route.snapshot.data['title'] ?? 'WebApp';

    this.titleService.setTitle(`${this.appName} | ${pageTitle}`);
  }
}

============================================================
FILE: src\app\billing\application\commands\cancel-subscription.command.ts
============================================================

export interface CancelSubscriptionCommand {
  subscriptionId: number;
}


============================================================
FILE: src\app\billing\application\commands\download-invoice.command.ts
============================================================

export interface DownloadInvoiceCommand {
  invoiceId: number;
}


============================================================
FILE: src\app\billing\application\commands\payment-form.command.ts
============================================================

import { PlanCode } from '../../domain/model/plan.entity';

export interface PaymentFormCommand {
  planCode: PlanCode;
  holderName: string;
  cardNumber: string;
  expirationDate: string;
  cvv: string;
}


============================================================
FILE: src\app\billing\application\services\billing.facade.ts
============================================================

import { Injectable } from '@angular/core';
import { firstValueFrom } from 'rxjs';

import { Invoice } from '../../domain/model/invoice.entity';

import { CancelSubscriptionCommand } from '../commands/cancel-subscription.command';
import { DownloadInvoiceCommand } from '../commands/download-invoice.command';
import { PaymentFormCommand } from '../commands/payment-form.command';

import { InvoicesApiService } from '../../infrastructure/api/invoices-api.service';
import { PaymentsApiService } from '../../infrastructure/api/payments-api.service';
import { PlansApiService } from '../../infrastructure/api/plans-api.service';
import { SubscriptionsApiService } from '../../infrastructure/api/subscriptions-api.service';

import { InvoiceAssembler } from '../../infrastructure/assemblers/invoice.assembler';
import { PaymentAssembler } from '../../infrastructure/assemblers/payment.assembler';
import { PlanAssembler } from '../../infrastructure/assemblers/plan.assembler';
import { SubscriptionAssembler } from '../../infrastructure/assemblers/subscription.assembler';

import { PaymentValidationService } from '../../domain/services/payment-validation.service';
import { SubscriptionPolicyService } from '../../domain/services/subscription-policy.service';

import { BillingStore } from '../stores/billing.store';

@Injectable({
  providedIn: 'root',
})
export class BillingFacade {
  private readonly planAssembler = new PlanAssembler();
  private readonly subscriptionAssembler = new SubscriptionAssembler();
  private readonly paymentAssembler = new PaymentAssembler();
  private readonly invoiceAssembler = new InvoiceAssembler();

  get plans() {
    return this.store.plans;
  }

  get activeSubscription() {
    return this.store.activeSubscription;
  }

  get payments() {
    return this.store.payments;
  }

  get invoices() {
    return this.store.invoices;
  }

  get lastPayment() {
    return this.store.lastPayment;
  }

  get loading() {
    return this.store.loading;
  }

  get error() {
    return this.store.error;
  }

  get hasActiveSubscription() {
    return this.store.hasActiveSubscription;
  }

  get activePlanCode() {
    return this.store.activePlanCode;
  }

  get activePlan() {
    return this.store.activePlan;
  }

  constructor(
    private readonly plansApi: PlansApiService,
    private readonly subscriptionsApi: SubscriptionsApiService,
    private readonly paymentsApi: PaymentsApiService,
    private readonly invoicesApi: InvoicesApiService,
    private readonly subscriptionPolicyService: SubscriptionPolicyService,
    private readonly paymentValidationService: PaymentValidationService,
    private readonly store: BillingStore
  ) {}

  async loadBilling(): Promise<void> {
    this.startRequest();

    try {
      await this.refreshBillingData();
    } catch (error) {
      console.error(error);
      this.store.setError('billing.loadError');
    } finally {
      this.finishRequest();
    }
  }

  async loadPlans(): Promise<void> {
    const responses = await firstValueFrom(this.plansApi.findAll());

    this.store.setPlans(
      responses.map((response) => this.planAssembler.toEntity(response))
    );
  }

  async loadActiveSubscription(): Promise<void> {
    const response = await firstValueFrom(
      this.subscriptionsApi.findCurrent()
    );

    const activeSubscription = response
      ? this.subscriptionAssembler.toEntity(response)
      : null;

    this.store.setActiveSubscription(activeSubscription);
  }

  async loadPayments(): Promise<void> {
    const responses = await firstValueFrom(
      this.paymentsApi.findCurrentUserPayments()
    );

    const payments = responses
      .map((response) => this.paymentAssembler.toEntity(response))
      .sort((first, second) => second.id - first.id);

    this.store.setPayments(payments);
    this.store.setLastPayment(payments[0] ?? null);
  }

  async loadInvoices(): Promise<void> {
    const responses = await firstValueFrom(
      this.invoicesApi.findCurrentUserInvoices()
    );

    const invoices = responses
      .map((response) => this.invoiceAssembler.toEntity(response))
      .sort((first, second) => {
        const firstDate = new Date(first.issuedAt).getTime();
        const secondDate = new Date(second.issuedAt).getTime();

        return secondDate - firstDate;
      });

    this.store.setInvoices(invoices);
  }

  async processPaymentAndSubscribe(command: PaymentFormCommand): Promise<boolean> {
    this.startRequest();

    try {
      this.validatePaymentCommand(command);

      const response = await firstValueFrom(
        this.subscriptionsApi.checkout({
          planCode: command.planCode,
          holderName: command.holderName.trim(),
          cardNumber: command.cardNumber,
          expirationDate: command.expirationDate,
          cvv: command.cvv,
        })
      );

      this.store.setActiveSubscription(
        this.subscriptionAssembler.toEntity(response)
      );

      await this.refreshBillingData();

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('billing.subscribeError');

      await this.safeRefreshBillingData();

      return false;
    } finally {
      this.finishRequest();
    }
  }

  async cancelSubscription(_command: CancelSubscriptionCommand): Promise<boolean> {
    this.startRequest();

    try {
      await this.cancelActiveSubscriptionByCurrentUser();
      await this.refreshBillingData();

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('billing.cancelError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async cancelCurrentSubscription(): Promise<boolean> {
    this.startRequest();

    try {
      const currentSubscription = this.store.activeSubscription();

      if (!this.subscriptionPolicyService.canCancelSubscription(currentSubscription)) {
        this.store.setError('billing.cancelError');
        return false;
      }

      await this.cancelActiveSubscriptionByCurrentUser();
      await this.refreshBillingData();

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('billing.cancelError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  downloadInvoice(command: DownloadInvoiceCommand): void {
    const invoice: Invoice | undefined = this.store
      .invoices()
      .find((item) => item.id === command.invoiceId);

    if (!invoice) {
      this.store.setError('billing.invoiceNotFound');
      return;
    }

    const content = [
      'ELECTROCORP INVOICE',
      `Invoice ID: ${invoice.id}`,
      `Invoice Number: ${invoice.invoiceNumber}`,
      `User ID: ${invoice.userId}`,
      `Amount: ${invoice.currency} ${invoice.totalAmount}`,
      `Issued At: ${invoice.issuedAt}`,
    ].join('\n');

    const blob = new Blob([content], {
      type: 'text/plain;charset=utf-8',
    });

    const url = URL.createObjectURL(blob);
    const anchor = document.createElement('a');

    anchor.href = url;
    anchor.download = `electrocorp-invoice-${invoice.id}.txt`;
    anchor.click();

    URL.revokeObjectURL(url);
  }

  clearMessages(): void {
    this.store.clearMessages();
  }

  clearActiveSubscription(): void {
    this.store.setActiveSubscription(null);
  }

  private async cancelActiveSubscriptionByCurrentUser(): Promise<void> {
    await firstValueFrom(
      this.subscriptionsApi.cancelCurrent()
    );

    this.store.setActiveSubscription(null);
  }

  private validatePaymentCommand(command: PaymentFormCommand): void {
    this.store.setError(null);

    if (!this.paymentValidationService.isValidHolderName(command.holderName)) {
      this.store.setError('billing.invalidHolderName');
      throw new Error('Invalid holder name.');
    }

    if (!this.paymentValidationService.isValidCardNumber(command.cardNumber)) {
      this.store.setError('billing.invalidCardNumber');
      throw new Error('Invalid card number.');
    }

    if (!this.paymentValidationService.isValidExpirationDate(command.expirationDate)) {
      this.store.setError('billing.invalidExpirationDate');
      throw new Error('Invalid expiration date.');
    }

    if (!this.paymentValidationService.isValidCvv(command.cvv)) {
      this.store.setError('billing.invalidCvv');
      throw new Error('Invalid CVV.');
    }
  }

  private async refreshBillingData(): Promise<void> {
    await Promise.all([
      this.loadPlans(),
      this.loadActiveSubscription(),
      this.loadPayments(),
      this.loadInvoices(),
    ]);
  }

  private async safeRefreshBillingData(): Promise<void> {
    try {
      await this.refreshBillingData();
    } catch (error) {
      console.error(error);
    }
  }

  private startRequest(): void {
    this.store.setLoading(true);
    this.store.clearMessages();
  }

  private finishRequest(): void {
    this.store.setLoading(false);
  }
}


============================================================
FILE: src\app\billing\application\stores\billing.store.ts
============================================================

import { Injectable, computed, signal } from '@angular/core';

import { Invoice } from '../../domain/model/invoice.entity';
import { Payment } from '../../domain/model/payment.entity';
import { Plan, PlanCode } from '../../domain/model/plan.entity';
import { Subscription } from '../../domain/model/subscription.entity';

@Injectable({
  providedIn: 'root',
})
export class BillingStore {
  private readonly plansSignal = signal<Plan[]>([]);
  private readonly activeSubscriptionSignal = signal<Subscription | null>(null);
  private readonly paymentsSignal = signal<Payment[]>([]);
  private readonly invoicesSignal = signal<Invoice[]>([]);
  private readonly lastPaymentSignal = signal<Payment | null>(null);

  private readonly loadingSignal = signal<boolean>(false);
  private readonly errorSignal = signal<string | null>(null);

  readonly plans = computed(() => this.plansSignal());
  readonly activeSubscription = computed(() => this.activeSubscriptionSignal());
  readonly payments = computed(() => this.paymentsSignal());
  readonly invoices = computed(() => this.invoicesSignal());
  readonly lastPayment = computed(() => this.lastPaymentSignal());

  readonly loading = computed(() => this.loadingSignal());
  readonly error = computed(() => this.errorSignal());

  readonly hasActiveSubscription = computed(
    () => this.activeSubscriptionSignal()?.isActive ?? false
  );

  readonly activePlanCode = computed<PlanCode | null>(() => {
    const subscription = this.activeSubscriptionSignal();
    return subscription?.isActive ? subscription.planCode : null;
  });

  readonly activePlan = computed<Plan | null>(() => {
    const activeCode = this.activePlanCode();

    if (!activeCode) return null;

    return this.plansSignal().find((plan) => plan.code === activeCode) ?? null;
  });

  setPlans(value: Plan[]): void {
    this.plansSignal.set(value);
  }

  setActiveSubscription(value: Subscription | null): void {
    this.activeSubscriptionSignal.set(value);
  }

  setPayments(value: Payment[]): void {
    this.paymentsSignal.set(value);
  }

  setInvoices(value: Invoice[]): void {
    this.invoicesSignal.set(value);
  }

  setLastPayment(value: Payment | null): void {
    this.lastPaymentSignal.set(value);
  }

  prependPayment(value: Payment): void {
    this.paymentsSignal.update((payments) => [value, ...payments]);
    this.lastPaymentSignal.set(value);
  }

  prependInvoice(value: Invoice): void {
    this.invoicesSignal.update((invoices) => [value, ...invoices]);
  }

  setLoading(value: boolean): void {
    this.loadingSignal.set(value);
  }

  setError(value: string | null): void {
    this.errorSignal.set(value);
  }

  clearMessages(): void {
    this.errorSignal.set(null);
  }

  reset(): void {
    this.plansSignal.set([]);
    this.activeSubscriptionSignal.set(null);
    this.paymentsSignal.set([]);
    this.invoicesSignal.set([]);
    this.lastPaymentSignal.set(null);
    this.loadingSignal.set(false);
    this.errorSignal.set(null);
  }
}


============================================================
FILE: src\app\billing\domain\model\invoice.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export class Invoice extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _invoiceNumber: string;
  private readonly _totalAmount: number;
  private readonly _currency: string;
  private readonly _issuedAt: string;

  constructor(props: {
    id: number;
    userId: number;
    invoiceNumber: string;
    totalAmount: number;
    currency: string;
    issuedAt: string;
  }) {
    super(props.id);
    this._userId = props.userId;
    this._invoiceNumber = props.invoiceNumber;
    this._totalAmount = props.totalAmount;
    this._currency = props.currency;
    this._issuedAt = props.issuedAt;
  }

  get userId(): number {
    return this._userId;
  }

  get invoiceNumber(): string {
    return this._invoiceNumber;
  }

  get totalAmount(): number {
    return this._totalAmount;
  }

  get currency(): string {
    return this._currency;
  }

  get issuedAt(): string {
    return this._issuedAt;
  }
}


============================================================
FILE: src\app\billing\domain\model\payment.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export type PaymentMethod = 'CARD';
export type PaymentStatus = 'APPROVED' | 'REJECTED' | 'PENDING';

export class Payment extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _amount: number;
  private readonly _currency: string;
  private readonly _status: PaymentStatus;
  private readonly _paymentMethod: string;

  constructor(props: {
    id: number;
    userId: number;
    amount: number;
    currency: string;
    status: PaymentStatus;
    paymentMethod: string;
  }) {
    super(props.id);
    this._userId = props.userId;
    this._amount = props.amount;
    this._currency = props.currency;
    this._status = props.status;
    this._paymentMethod = props.paymentMethod;
  }

  get userId(): number {
    return this._userId;
  }

  get amount(): number {
    return this._amount;
  }

  get currency(): string {
    return this._currency;
  }

  get status(): PaymentStatus {
    return this._status;
  }

  get paymentMethod(): string {
    return this._paymentMethod;
  }

  get isApproved(): boolean {
    return this._status === 'APPROVED';
  }
}

============================================================
FILE: src\app\billing\domain\model\plan.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export type PlanCode = 'STARTER' | 'PROFESSIONAL' | 'ENTERPRISE';

export class Plan extends BaseEntity<number> {
  private readonly _code: PlanCode;
  private readonly _name: string;
  private readonly _monthlyPrice: number;
  private readonly _currency: string;
  private readonly _maxDevices: number | null;
  private readonly _maxRoutines: number | null;
  private readonly _maxAlerts: number | null;
  private readonly _reportExportEnabled: boolean;

  constructor(props: {
    id: number;
    code: PlanCode;
    name: string;
    monthlyPrice: number;
    currency: string;
    maxDevices: number | null;
    maxRoutines: number | null;
    maxAlerts: number | null;
    reportExportEnabled: boolean;
  }) {
    super(props.id);
    this._code = props.code;
    this._name = props.name;
    this._monthlyPrice = props.monthlyPrice;
    this._currency = props.currency;
    this._maxDevices = props.maxDevices;
    this._maxRoutines = props.maxRoutines;
    this._maxAlerts = props.maxAlerts;
    this._reportExportEnabled = props.reportExportEnabled;
  }

  get code(): PlanCode {
    return this._code;
  }

  get name(): string {
    return this._name;
  }

  get monthlyPrice(): number {
    return this._monthlyPrice;
  }

  get currency(): string {
    return this._currency;
  }

  get maxDevices(): number | null {
    return this._maxDevices;
  }

  get maxRoutines(): number | null {
    return this._maxRoutines;
  }

  get maxAlerts(): number | null {
    return this._maxAlerts;
  }

  get reportExportEnabled(): boolean {
    return this._reportExportEnabled;
  }

  get isStarter(): boolean {
    return this._code === 'STARTER';
  }

  get isProfessional(): boolean {
    return this._code === 'PROFESSIONAL';
  }

  get isEnterprise(): boolean {
    return this._code === 'ENTERPRISE';
  }
}

============================================================
FILE: src\app\billing\domain\model\subscription.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';
import { PlanCode } from './plan.entity';

export type SubscriptionStatus = 'ACTIVE' | 'CANCELLED' | 'EXPIRED';

export class Subscription extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _planCode: PlanCode;
  private readonly _status: SubscriptionStatus;
  private readonly _active: boolean;

  constructor(props: {
    id: number;
    userId: number;
    planCode: PlanCode;
    status: SubscriptionStatus;
    active: boolean;
  }) {
    super(props.id);
    this._userId = props.userId;
    this._planCode = props.planCode;
    this._status = props.status;
    this._active = props.active;
  }

  get userId(): number {
    return this._userId;
  }

  get planCode(): PlanCode {
    return this._planCode;
  }

  get status(): SubscriptionStatus {
    return this._status;
  }

  get active(): boolean {
    return this._active;
  }

  get isActive(): boolean {
    return this._active;
  }
}

============================================================
FILE: src\app\billing\domain\services\payment-validation.service.ts
============================================================

import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class PaymentValidationService {
  isValidCardNumber(cardNumber: string): boolean {
    const sanitized = this.sanitizeCardNumber(cardNumber);
    return sanitized.length >= 13 && sanitized.length <= 19;
  }

  isValidCvv(cvv: string): boolean {
    const sanitized = cvv.trim();
    return /^\d{3,4}$/.test(sanitized);
  }

  isValidHolderName(holderName: string): boolean {
    return holderName.trim().length >= 3;
  }

  isValidExpirationDate(expirationDate: string): boolean {
    return /^(0[1-9]|1[0-2])\/\d{2}$/.test(expirationDate.trim());
  }

  sanitizeCardNumber(cardNumber: string): string {
    return cardNumber.replace(/\s/g, '');
  }

  getLastFourDigits(cardNumber: string): string {
    return this.sanitizeCardNumber(cardNumber).slice(-4);
  }
}

============================================================
FILE: src\app\billing\domain\services\plan-permission.service.ts
============================================================

import { Injectable } from '@angular/core';
import { PlanCode } from '../model/plan.entity';

export interface PlanPermissions {
  maxDevices: number;
  maxRoutines: number;
  maxManualAlerts: number;
  canExportCsv: boolean;
  canAccessEnergyHistory: boolean;
  canUseAdvancedAnalytics: boolean;
  canUseMultipleLocations: boolean;
}

@Injectable({
  providedIn: 'root',
})
export class PlanPermissionService {
  private readonly permissions: Record<PlanCode, PlanPermissions> = {
    STARTER: {
      maxDevices: 5,
      maxRoutines: 3,
      maxManualAlerts: 5,
      canExportCsv: false,
      canAccessEnergyHistory: false,
      canUseAdvancedAnalytics: false,
      canUseMultipleLocations: false,
    },
    PROFESSIONAL: {
      maxDevices: 20,
      maxRoutines: 15,
      maxManualAlerts: 30,
      canExportCsv: true,
      canAccessEnergyHistory: true,
      canUseAdvancedAnalytics: true,
      canUseMultipleLocations: false,
    },
    ENTERPRISE: {
      maxDevices: 100,
      maxRoutines: 100,
      maxManualAlerts: 9999,
      canExportCsv: true,
      canAccessEnergyHistory: true,
      canUseAdvancedAnalytics: true,
      canUseMultipleLocations: true,
    },
  };

  getPermissions(planCode: PlanCode | null): PlanPermissions | null {
    if (!planCode) return null;
    return this.permissions[planCode];
  }

  canCreateDevice(planCode: PlanCode | null, currentDevices: number): boolean {
    const permissions = this.getPermissions(planCode);
    if (!permissions) return false;
    return currentDevices < permissions.maxDevices;
  }

  canCreateRoutine(planCode: PlanCode | null, currentRoutines: number): boolean {
    const permissions = this.getPermissions(planCode);
    if (!permissions) return false;
    return currentRoutines < permissions.maxRoutines;
  }

  canCreateManualAlert(planCode: PlanCode | null, currentAlerts: number): boolean {
    const permissions = this.getPermissions(planCode);
    if (!permissions) return false;
    return currentAlerts < permissions.maxManualAlerts;
  }

  canExportCsv(planCode: PlanCode | null): boolean {
    return this.getPermissions(planCode)?.canExportCsv ?? false;
  }

  canAccessEnergyHistory(planCode: PlanCode | null): boolean {
    return this.getPermissions(planCode)?.canAccessEnergyHistory ?? false;
  }

  canUseAdvancedAnalytics(planCode: PlanCode | null): boolean {
    return this.getPermissions(planCode)?.canUseAdvancedAnalytics ?? false;
  }

  canUseMultipleLocations(planCode: PlanCode | null): boolean {
    return this.getPermissions(planCode)?.canUseMultipleLocations ?? false;
  }
}

============================================================
FILE: src\app\billing\domain\services\subscription-policy.service.ts
============================================================

import { Injectable } from '@angular/core';

import { PlanCode } from '../model/plan.entity';
import { Subscription } from '../model/subscription.entity';

@Injectable({
  providedIn: 'root',
})
export class SubscriptionPolicyService {
  canSubscribeToPlan(
    currentSubscription: Subscription | null,
    targetPlanCode: PlanCode
  ): boolean {
    if (!currentSubscription) return true;
    if (!currentSubscription.isActive) return true;

    return currentSubscription.planCode !== targetPlanCode;
  }

  canCancelSubscription(subscription: Subscription | null): boolean {
    return subscription !== null && subscription.isActive;
  }
}

============================================================
FILE: src\app\billing\infrastructure\api\invoices-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { Invoice } from '../../domain/model/invoice.entity';
import { InvoiceAssembler } from '../assemblers/invoice.assembler';
import { InvoiceResource } from '../resources/invoice.resource';
import { InvoiceResponse } from '../responses/invoice.response';

@Injectable({
  providedIn: 'root',
})
export class InvoicesApiService extends BaseApiService<
  Invoice,
  InvoiceResource,
  InvoiceResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(http, apiBaseUrl, 'billing/invoices', new InvoiceAssembler());
  }

  findCurrentUserInvoices(): Observable<InvoiceResponse[]> {
    return this.http.get<InvoiceResponse[]>(
      this.resourceEndpoint
    );
  }
}


============================================================
FILE: src\app\billing\infrastructure\api\payments-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { Payment } from '../../domain/model/payment.entity';
import { PaymentAssembler } from '../assemblers/payment.assembler';
import { PaymentResource } from '../resources/payment.resource';
import { PaymentResponse } from '../responses/payment.response';

@Injectable({
  providedIn: 'root',
})
export class PaymentsApiService extends BaseApiService<
  Payment,
  PaymentResource,
  PaymentResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(http, apiBaseUrl, 'billing/payments', new PaymentAssembler());
  }

  findCurrentUserPayments(): Observable<PaymentResponse[]> {
    return this.http.get<PaymentResponse[]>(
      this.resourceEndpoint
    );
  }
}


============================================================
FILE: src\app\billing\infrastructure\api\plans-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { Plan } from '../../domain/model/plan.entity';
import { PlanAssembler } from '../assemblers/plan.assembler';
import { PlanResource } from '../resources/plan.resource';
import { PlanResponse } from '../responses/plan.response';

@Injectable({
  providedIn: 'root',
})
export class PlansApiService extends BaseApiService<
  Plan,
  PlanResource,
  PlanResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(http, apiBaseUrl, 'billing/plans', new PlanAssembler());
  }
}

============================================================
FILE: src\app\billing\infrastructure\api\subscriptions-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { Subscription } from '../../domain/model/subscription.entity';
import { SubscriptionAssembler } from '../assemblers/subscription.assembler';
import { CheckoutSubscriptionResource } from '../resources/checkout-subscription.resource';
import { SubscriptionResource } from '../resources/subscription.resource';
import { SubscriptionResponse } from '../responses/subscription.response';

@Injectable({
  providedIn: 'root',
})
export class SubscriptionsApiService extends BaseApiService<
  Subscription,
  SubscriptionResource,
  SubscriptionResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(http, apiBaseUrl, 'billing/subscriptions', new SubscriptionAssembler());
  }

  findCurrent(): Observable<SubscriptionResponse | null> {
    return this.http.get<SubscriptionResponse | null>(
      `${this.resourceEndpoint}/current`
    );
  }

  cancelCurrent(): Observable<void> {
    return this.http.delete<void>(
      `${this.resourceEndpoint}/current`
    );
  }

  checkout(resource: CheckoutSubscriptionResource): Observable<SubscriptionResponse> {
    return this.http.post<SubscriptionResponse>(
      `${this.resourceEndpoint}/checkout`,
      resource
    );
  }
}


============================================================
FILE: src\app\billing\infrastructure\assemblers\invoice.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { Invoice } from '../../domain/model/invoice.entity';
import { InvoiceResource } from '../resources/invoice.resource';
import { InvoiceResponse } from '../responses/invoice.response';

export class InvoiceAssembler extends BaseAssembler<
  Invoice,
  InvoiceResource,
  InvoiceResponse
> {
  override toEntity(response: InvoiceResponse): Invoice {
    return new Invoice({
      id: response.id,
      userId: response.userId,
      invoiceNumber: response.invoiceNumber,
      totalAmount: response.totalAmount,
      currency: response.currency,
      issuedAt: response.issuedAt,
    });
  }

  override toResource(entity: Invoice): InvoiceResource {
    return {
      userId: entity.userId,
      invoiceNumber: entity.invoiceNumber,
      totalAmount: entity.totalAmount,
      currency: entity.currency,
      issuedAt: entity.issuedAt,
    };
  }
}


============================================================
FILE: src\app\billing\infrastructure\assemblers\payment.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { Payment } from '../../domain/model/payment.entity';
import { PaymentResource } from '../resources/payment.resource';
import { PaymentResponse } from '../responses/payment.response';

export class PaymentAssembler extends BaseAssembler<
  Payment,
  PaymentResource,
  PaymentResponse
> {
  override toEntity(response: PaymentResponse): Payment {
    return new Payment({
      id: response.id,
      userId: response.userId,
      amount: response.amount,
      currency: response.currency,
      status: response.status,
      paymentMethod: response.paymentMethod,
    });
  }

  override toResource(entity: Payment): PaymentResource {
    throw new Error('PaymentResource must be created from payment form data.');
  }
}

============================================================
FILE: src\app\billing\infrastructure\assemblers\plan.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { Plan } from '../../domain/model/plan.entity';
import { PlanResource } from '../resources/plan.resource';
import { PlanResponse } from '../responses/plan.response';

export class PlanAssembler extends BaseAssembler<
  Plan,
  PlanResource,
  PlanResponse
> {
  override toEntity(response: PlanResponse): Plan {
    return new Plan({
      id: response.id,
      code: response.code,
      name: response.name,
      monthlyPrice: response.monthlyPrice,
      currency: response.currency,
      maxDevices: response.maxDevices,
      maxRoutines: response.maxRoutines,
      maxAlerts: response.maxAlerts,
      reportExportEnabled: response.reportExportEnabled,
    });
  }

  override toResource(entity: Plan): PlanResource {
    return {
      code: entity.code,
      name: entity.name,
      monthlyPrice: entity.monthlyPrice,
      currency: entity.currency,
      maxDevices: entity.maxDevices,
      maxRoutines: entity.maxRoutines,
      maxAlerts: entity.maxAlerts,
      reportExportEnabled: entity.reportExportEnabled,
    };
  }
}

============================================================
FILE: src\app\billing\infrastructure\assemblers\subscription.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { Subscription } from '../../domain/model/subscription.entity';
import { SubscriptionResource } from '../resources/subscription.resource';
import { SubscriptionResponse } from '../responses/subscription.response';

export class SubscriptionAssembler extends BaseAssembler<
  Subscription,
  SubscriptionResource,
  SubscriptionResponse
> {
  override toEntity(response: SubscriptionResponse): Subscription {
    return new Subscription({
      id: response.id,
      userId: response.userId,
      planCode: response.planCode,
      status: response.status,
      active: response.active,
    });
  }

  override toResource(entity: Subscription): SubscriptionResource {
    return {
      userId: entity.userId,
      planCode: entity.planCode,
    };
  }
}

============================================================
FILE: src\app\billing\infrastructure\resources\checkout-subscription.resource.ts
============================================================

import { PlanCode } from '../../domain/model/plan.entity';

export interface CheckoutSubscriptionResource {
  planCode: PlanCode;
  holderName: string;
  cardNumber: string;
  expirationDate: string;
  cvv: string;
}


============================================================
FILE: src\app\billing\infrastructure\resources\invoice.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';

export interface InvoiceResource extends BaseResource {
  userId: number;
  invoiceNumber: string;
  totalAmount: number;
  currency: string;
  issuedAt: string;
}


============================================================
FILE: src\app\billing\infrastructure\resources\payment.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';

export interface PaymentResource extends BaseResource {
  userId: number;
  subscriptionId: number;
  holderName: string;
  cardNumber: string;
  expirationDate: string;
  cvv: string;
}

============================================================
FILE: src\app\billing\infrastructure\resources\plan.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';
import { PlanCode } from '../../domain/model/plan.entity';

export interface PlanResource extends BaseResource {
  code: PlanCode;
  name: string;
  monthlyPrice: number;
  currency: string;
  maxDevices: number | null;
  maxRoutines: number | null;
  maxAlerts: number | null;
  reportExportEnabled: boolean;
}

============================================================
FILE: src\app\billing\infrastructure\resources\subscription.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';
import { PlanCode } from '../../domain/model/plan.entity';

export interface SubscriptionResource extends BaseResource {
  userId: number;
  planCode: PlanCode;
}

============================================================
FILE: src\app\billing\infrastructure\responses\invoice.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';

export interface InvoiceResponse extends BaseResponse<number> {
  userId: number;
  invoiceNumber: string;
  totalAmount: number;
  currency: string;
  issuedAt: string;
}


============================================================
FILE: src\app\billing\infrastructure\responses\payment.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';
import { PaymentStatus } from '../../domain/model/payment.entity';

export interface PaymentResponse extends BaseResponse<number> {
  userId: number;
  amount: number;
  currency: string;
  status: PaymentStatus;
  paymentMethod: string;
}

============================================================
FILE: src\app\billing\infrastructure\responses\plan.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';
import { PlanCode } from '../../domain/model/plan.entity';

export interface PlanResponse extends BaseResponse<number> {
  code: PlanCode;
  name: string;
  monthlyPrice: number;
  currency: string;
  maxDevices: number | null;
  maxRoutines: number | null;
  maxAlerts: number | null;
  reportExportEnabled: boolean;
}

============================================================
FILE: src\app\billing\infrastructure\responses\subscription.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';
import { PlanCode } from '../../domain/model/plan.entity';
import { SubscriptionStatus } from '../../domain/model/subscription.entity';

export interface SubscriptionResponse extends BaseResponse<number> {
  userId: number;
  planCode: PlanCode;
  status: SubscriptionStatus;
  active: boolean;
}

============================================================
FILE: src\app\billing\presentation\components\invoice-card\invoice-card.component.html
============================================================

<article class="invoice-card">
  <div>
    <p class="eyebrow">{{ invoice.invoiceNumber }}</p>
    <h2>{{ 'billing.invoice' | translate }} #{{ invoice.id }}</h2>

    <p>
      {{ 'billing.issuedAt' | translate }}:
      <strong>{{ invoice.issuedAt }}</strong>
    </p>
  </div>

  <div class="amount">
    <span>{{ invoice.currency === 'PEN' ? 'S/' : invoice.currency }}</span>
    <strong>{{ invoice.totalAmount }}</strong>
  </div>

  <app-button variant="secondary" size="sm" (pressed)="onDownload()">
    {{ 'billing.downloadInvoice' | translate }}
  </app-button>
</article>


============================================================
FILE: src\app\billing\presentation\components\invoice-card\invoice-card.component.scss
============================================================

.invoice-card {
  display: grid;
  grid-template-columns: 1fr auto auto;
  align-items: center;
  gap: 18px;
  padding: 20px;
  border: 1px solid var(--border-color);
  border-radius: 22px;
  background: var(--surface-color);
}

.eyebrow {
  width: fit-content;
  margin: 0 0 8px;
  padding: 6px 10px;
  border-radius: 999px;
  color: var(--text-on-action);
  background: var(--ec-action-bg);
  font-size: 0.76rem;
  font-weight: 900;
}

h2 {
  margin: 0 0 8px;
}

p {
  margin: 4px 0;
  color: var(--text-secondary);
}

.amount {
  display: flex;
  align-items: baseline;
  gap: 5px;
}

.amount span {
  color: var(--text-secondary);
  font-weight: 800;
}

.amount strong {
  font-size: 2rem;
}

button {
  min-height: 42px;
  border: 0;
  border-radius: 999px;
  padding: 0 16px;
  color: var(--text-on-action);
  background: var(--ec-action-bg);
  cursor: pointer;
  font-weight: 900;
}

@media (max-width: 760px) {
  .invoice-card {
    grid-template-columns: 1fr;
  }
}


============================================================
FILE: src\app\billing\presentation\components\invoice-card\invoice-card.component.ts
============================================================

import { Component, EventEmitter, Input, Output } from '@angular/core';
import { TranslateModule } from '@ngx-translate/core';

import { Invoice } from '../../../domain/model/invoice.entity';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';

@Component({
  selector: 'app-invoice-card',
  standalone: true,
  imports: [TranslateModule, AppButtonComponent],
  templateUrl: './invoice-card.component.html',
  styleUrls: ['./invoice-card.component.scss'],
})
export class InvoiceCardComponent {
  @Input({ required: true }) invoice!: Invoice;

  @Output() download = new EventEmitter<number>();

  onDownload(): void {
    this.download.emit(this.invoice.id);
  }
}


============================================================
FILE: src\app\billing\presentation\components\payment-form\payment-form.component.html
============================================================

<app-modal-form-shell
  [open]="true"
  [eyebrow]="'about.careful.items.billing.title' | translate"
  title="Confirmar plan"
  description="Registra el pago para activar el plan seleccionado."
  (closed)="onCancel()"
>
  <section class="amount-card">
    <span>{{ 'billing.selectedPlan' | translate }}</span>
    <strong>{{ plan.name }}</strong>
    <p>S/ {{ plan.monthlyPrice }}</p>
  </section>

  <form class="payment-form ec-scrollbar" (ngSubmit)="onConfirm()">
    <label>
      {{ 'billing.holderName' | translate }}
      <input
        type="text"
        name="holderName"
        [(ngModel)]="holderName"
        autocomplete="off"
        required
      />
    </label>

    <label>
      {{ 'billing.cardNumber' | translate }}
      <input
        type="text"
        name="cardNumber"
        [ngModel]="cardNumber"
        (ngModelChange)="onCardNumberChange($event)"
        autocomplete="off"
        inputmode="numeric"
        maxlength="19"
        required
      />
    </label>

    <div class="row">
      <label>
        {{ 'billing.expirationDate' | translate }}
        <input
          type="text"
          name="expirationDate"
          [ngModel]="expirationDate"
          (ngModelChange)="onExpirationDateChange($event)"
          autocomplete="off"
          inputmode="numeric"
          maxlength="5"
          required
        />
      </label>

      <label>
        {{ 'billing.cvv' | translate }}
        <input
          type="password"
          name="cvv"
          [ngModel]="cvv"
          (ngModelChange)="onCvvChange($event)"
          autocomplete="off"
          inputmode="numeric"
          maxlength="4"
          required
        />
      </label>
    </div>

    <div class="actions">
      <app-button type="submit" variant="create" [disabled]="loading">
        @if (loading) {
          {{ 'common.loading' | translate }}
        } @else {
          {{ 'billing.confirmPayment' | translate }}
        }
      </app-button>

      <app-button type="button" variant="secondary" (pressed)="onCancel()">
        {{ 'billing.cancelPayment' | translate }}
      </app-button>
    </div>
  </form>
</app-modal-form-shell>


============================================================
FILE: src\app\billing\presentation\components\payment-form\payment-form.component.scss
============================================================

.amount-card {
  display: grid;
  gap: 8px;
  margin-bottom: 18px;
  padding: 16px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background: var(--surface-card);
}

.amount-card span {
  color: var(--accent-color);
  font-size: 0.75rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.amount-card strong {
  color: var(--text-primary);
  font-size: 1.35rem;
}

.amount-card p {
  margin: 0;
  color: var(--text-secondary);
  font-weight: 900;
}

.payment-form {
  display: grid;
  gap: 16px;
}

.row {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14px;
}

label {
  display: grid;
  gap: 8px;
  color: var(--text-primary);
  font-weight: 850;
}

input {
  width: 100%;
  min-height: 46px;
  padding: 0 14px;
  border: 1px solid var(--border-color);
  border-radius: 14px;
  color: var(--text-primary);
  background: var(--field-bg);
  font: inherit;
  transition:
    border-color 0.18s ease,
    box-shadow 0.18s ease,
    background 0.18s ease;
}

input:focus {
  outline: none;
  border-color: var(--border-strong);
  box-shadow: var(--ec-focus-ring);
}

.actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  flex-wrap: wrap;
}

@media (max-width: 640px) {
  .row {
    grid-template-columns: 1fr;
  }

  .actions {
    flex-direction: column-reverse;
  }
}


============================================================
FILE: src\app\billing\presentation\components\payment-form\payment-form.component.ts
============================================================

import { Component, EventEmitter, Input, Output } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { TranslateModule } from '@ngx-translate/core';

import { Plan } from '../../../domain/model/plan.entity';
import { PaymentFormCommand } from '../../../application/commands/payment-form.command';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { ModalFormShellComponent } from '../../../../shared/presentation/components/modal-form-shell/modal-form-shell.component';

@Component({
  selector: 'app-payment-form',
  standalone: true,
  imports: [FormsModule, TranslateModule, AppButtonComponent, ModalFormShellComponent],
  templateUrl: './payment-form.component.html',
  styleUrls: ['./payment-form.component.scss'],
})
export class PaymentFormComponent {
  @Input({ required: true }) plan!: Plan;
  @Input() loading = false;

  @Output() confirmed = new EventEmitter<PaymentFormCommand>();
  @Output() canceled = new EventEmitter<void>();

  holderName = '';
  cardNumber = '';
  expirationDate = '';
  cvv = '';

  onCardNumberChange(value: string): void {
    const digits = value.replace(/\D/g, '').slice(0, 16);
    this.cardNumber = digits.replace(/(.{4})/g, '$1 ').trim();
  }

  onExpirationDateChange(value: string): void {
    const digits = value.replace(/\D/g, '').slice(0, 4);

    if (digits.length <= 2) {
      this.expirationDate = digits;
      return;
    }

    this.expirationDate = `${digits.slice(0, 2)}/${digits.slice(2)}`;
  }

  onCvvChange(value: string): void {
    this.cvv = value.replace(/\D/g, '').slice(0, 4);
  }

  onConfirm(): void {
    this.confirmed.emit({
      planCode: this.plan.code,
      holderName: this.holderName,
      cardNumber: this.cardNumber,
      expirationDate: this.expirationDate,
      cvv: this.cvv,
    });
  }

  onCancel(): void {
    this.canceled.emit();
  }
}


============================================================
FILE: src\app\billing\presentation\components\plan-card\plan-card.component.html
============================================================

<article
  class="plan-card"
  [class.recommended]="isRecommended"
  [class.active]="isSelected"
>
  @if (isRecommended) {
    <div class="recommend-chip">
      {{ 'billing.recommended' | translate }}
    </div>
  }

  @if (isSelected) {
    <div class="active-chip">
      {{ 'billing.activePlanBadge' | translate }}
    </div>
  }

  <div class="plan-header">
    <h2>{{ plan.name }}</h2>
    <p class="usage">{{ usageKey | translate }}</p>
    <p class="description">
      {{ descriptionKey | translate }}
    </p>
  </div>

  <div class="price">
    <span>{{ plan.currency === 'PEN' ? 'S/' : plan.currency }}</span>
    <strong>{{ plan.monthlyPrice | number: '1.2-2' }}</strong>
    <small>/{{ 'billing.month' | translate }}</small>
  </div>

  <ul class="features">
    @for (featureKey of featureKeys; track featureKey) {
      <li>
        <span class="feature-icon">✓</span>
        <span>{{ featureKey | translate }}</span>
      </li>
    }
  </ul>

  <div class="limits">
    @if (plan.maxDevices !== null) {
      <span>{{ 'billing.maxDevices' | translate: { count: plan.maxDevices } }}</span>
    }

    @if (plan.maxRoutines !== null) {
      <span>{{ 'billing.maxRoutines' | translate: { count: plan.maxRoutines } }}</span>
    }

    @if (plan.reportExportEnabled) {
      <span>{{ 'billing.reportExportEnabled' | translate }}</span>
    }
  </div>

  <app-button
    variant="create"
    [disabled]="isSelected"
    [block]="true"
    (pressed)="onSelect()"
  >
    @if (isSelected) {
      {{ 'billing.selected' | translate }}
    } @else {
      {{ 'billing.choosePlan' | translate }}
    }
  </app-button>
</article>


============================================================
FILE: src\app\billing\presentation\components\plan-card\plan-card.component.scss
============================================================

:host {
  display: block;
  height: 100%;
  min-width: 0;
}

.plan-card {
  position: relative;
  isolation: isolate;
  overflow: visible;
  min-height: 585px;
  height: 100%;
  display: grid;
  grid-template-rows: auto auto auto 1fr auto auto;
  gap: 22px;
  padding: 52px 30px 30px;
  border: 1px solid var(--border-color);
  border-radius: 30px;
  background: var(--surface-card);
  box-shadow: var(--ec-shadow-soft);
  transition:
    transform 0.28s ease,
    box-shadow 0.28s ease,
    border-color 0.28s ease;
  z-index: 1;
}

.plan-card::before {
  content: '';
  position: absolute;
  inset: 0;
  background:
    radial-gradient(circle at 80% 88%, var(--ec-accent-soft), transparent 34%),
    radial-gradient(circle at 18% 8%, color-mix(in srgb, var(--ec-accent-soft) 72%, transparent), transparent 28%);
  opacity: 0.65;
  z-index: -2;
}

.plan-card::after {
  content: '';
  position: absolute;
  right: -90px;
  bottom: -90px;
  width: 240px;
  height: 240px;
  border-radius: 50%;
  background: radial-gradient(
    circle,
    var(--ec-accent-soft) 0%,
    color-mix(in srgb, var(--ec-accent-soft) 58%, transparent) 42%,
    transparent 72%
  );
  opacity: 0;
  transform: scale(0.82);
  transition: opacity 0.28s ease, transform 0.28s ease;
  z-index: -1;
}

.plan-card:hover {
  transform: translateY(-9px);
  border-color: var(--border-strong);
  box-shadow: var(--ec-shadow-panel);
}

.plan-card:hover::after {
  opacity: 1;
  transform: scale(1);
}

.plan-card.recommended {
  border-color: var(--border-strong);
  box-shadow:
    var(--ec-action-shadow),
    var(--ec-shadow-soft);
  transform: translateY(-8px);
  z-index: 5;
}

.plan-card.recommended::after {
  opacity: 1;
  transform: scale(1.08);
}

.plan-card.active {
  border-color: var(--ec-status-success-border);
  box-shadow:
    0 28px 70px var(--ec-status-success-bg),
    var(--ec-shadow-soft);
}

.recommend-chip {
  position: absolute;
  top: 0;
  left: 50%;
  z-index: 10;
  transform: translate(-50%, -50%);
  padding: 14px 30px;
  border-radius: 999px;
  background: var(--ec-create-bg);
  color: var(--text-on-action);
  font-size: 0.88rem;
  font-weight: 950;
  letter-spacing: 0.05em;
  box-shadow: var(--ec-create-shadow);
  white-space: nowrap;
}

.active-chip {
  position: absolute;
  top: 22px;
  right: 22px;
  z-index: 2;
  padding: 8px 12px;
  border: 1px solid var(--ec-status-success-border);
  border-radius: 999px;
  background: var(--ec-status-success-bg);
  color: var(--ec-status-success-text);
  font-size: 0.74rem;
  font-weight: 950;
  letter-spacing: 0.05em;
}

.plan-header {
  display: grid;
  gap: 12px;
}

h2 {
  margin: 0;
  color: var(--text-primary);
  font-size: clamp(2rem, 3vw, 2.35rem);
  line-height: 1.05;
  letter-spacing: -0.045em;
}

.usage {
  width: fit-content;
  margin: 0;
  padding: 8px 13px;
  border-radius: 999px;
  background: var(--ec-accent-soft);
  color: var(--accent-color);
  font-size: 0.78rem;
  font-weight: 950;
  line-height: 1;
}

.description {
  margin: 0;
  color: var(--ec-copy-color);
  line-height: 1.65;
  font-size: 1rem;
}

.price {
  display: flex;
  align-items: baseline;
  gap: 8px;
}

.price span {
  color: var(--text-secondary);
  font-size: 1rem;
  font-weight: 900;
}

.price strong {
  color: var(--text-primary);
  font-size: clamp(2.85rem, 5vw, 3.55rem);
  line-height: 1;
  letter-spacing: -0.05em;
}

.price small {
  color: var(--text-secondary);
  font-size: 0.9rem;
  font-weight: 900;
}

.features {
  display: grid;
  gap: 14px;
  margin: 0;
  padding: 0;
  list-style: none;
}

.features li {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  color: var(--text-primary);
  font-size: 0.98rem;
  font-weight: 850;
  line-height: 1.45;
}

.feature-icon {
  flex: 0 0 24px;
  width: 24px;
  height: 24px;
  display: grid;
  place-items: center;
  margin-top: 1px;
  border-radius: 50%;
  background: var(--ec-accent-soft);
  color: var(--accent-color);
  font-size: 0.82rem;
  font-weight: 950;
}

.limits {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  align-self: end;
}

.limits span {
  padding: 7px 10px;
  border: 1px solid var(--border-color);
  border-radius: 999px;
  background: var(--ec-surface-subtle);
  color: var(--text-secondary);
  font-size: 0.76rem;
  font-weight: 850;
}

button {
  width: 100%;
  min-height: 54px;
  border: 0;
  border-radius: 999px;
  color: var(--text-on-action);
  background: var(--ec-create-bg);
  cursor: pointer;
  font-size: 1rem;
  font-weight: 950;
  box-shadow: var(--ec-create-shadow);
  transition:
    transform 0.22s ease,
    box-shadow 0.22s ease,
    opacity 0.22s ease;
}

button:hover:not(:disabled) {
  transform: translateY(-2px);
  box-shadow: var(--ec-create-shadow-hover);
}

button:disabled {
  opacity: 0.78;
  cursor: not-allowed;
  color: var(--ec-status-success-text);
  background: var(--ec-status-success-bg);
  box-shadow: none;
}

@media (max-width: 1050px) {
  .plan-card {
    min-height: auto;
  }
}

@media (max-width: 640px) {
  .plan-card {
    padding: 30px 22px 24px;
    border-radius: 24px;
  }

  .recommend-chip {
    padding: 11px 20px;
    font-size: 0.78rem;
  }

  .active-chip {
    position: static;
    width: fit-content;
  }

  h2 {
    font-size: 1.72rem;
  }
}


============================================================
FILE: src\app\billing\presentation\components\plan-card\plan-card.component.ts
============================================================

import { Component, EventEmitter, Input, Output } from '@angular/core';
import { DecimalPipe } from '@angular/common';
import { TranslateModule } from '@ngx-translate/core';

import { Plan, PlanCode } from '../../../domain/model/plan.entity';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';

@Component({
  selector: 'app-plan-card',
  standalone: true,
  imports: [TranslateModule, DecimalPipe, AppButtonComponent],
  templateUrl: './plan-card.component.html',
  styleUrls: ['./plan-card.component.scss'],
})
export class PlanCardComponent {
  @Input({ required: true }) plan!: Plan;
  @Input() activePlanCode: PlanCode | null = null;

  @Output() selected = new EventEmitter<Plan>();

  private readonly featureKeysByPlan: Record<PlanCode, string[]> = {
    STARTER: ['devices', 'dashboard', 'alerts'],
    PROFESSIONAL: ['devices', 'analytics', 'routines', 'reports'],
    ENTERPRISE: ['locations', 'accessProfiles', 'alerts', 'support'],
  };

  get isSelected(): boolean {
    return this.activePlanCode === this.plan.code;
  }

  get isRecommended(): boolean {
    return this.plan.code === 'PROFESSIONAL';
  }

  get descriptionKey(): string {
    return `billing.planDescriptions.${this.plan.code}`;
  }

  get featureKeys(): string[] {
    return this.featureKeysByPlan[this.plan.code].map(
      (feature) => `billing.planFeatures.${this.plan.code}.${feature}`
    );
  }

  get usageKey(): string {
    return `billing.planUsage.${this.plan.code}`;
  }

  onSelect(): void {
    if (!this.isSelected) {
      this.selected.emit(this.plan);
    }
  }
}


============================================================
FILE: src\app\billing\presentation\pages\billing-history\billing-history-page.component.html
============================================================

<section class="settings-page billing-settings-page">
  <header class="ec-page-heading">
    <p class="ec-page-eyebrow">{{ 'settings.nav.billing' | translate }}</p>
    <h1 class="ec-page-title">{{ 'settings.billing.title' | translate }}</h1>
    <p class="ec-page-description">
      {{ 'settings.billing.pageDescription' | translate }}
    </p>
  </header>

  @if (billingFacade.loading()) {
    <div class="loading-wrapper">
      <app-loading-spinner />
    </div>
  } @else {
    <app-settings-section
      [eyebrow]="'settings.billing.subscriptionEyebrow' | translate"
      [title]="'settings.billing.planStatusTitle' | translate"
      [description]="'settings.billing.planStatusDescription' | translate"
      [stackedHeader]="true"
    >
      <section class="billing-overview">
        <article class="plan-card">
          <header class="plan-card__header">
            <div>
              <span>{{ 'billing.currentPlan' | translate }}</span>
              <h2>{{ billingFacade.activePlan()?.name || ('settings.billing.noActivePlan' | translate) }}</h2>
            </div>

            <strong class="status-pill">
              {{ subscriptionStatusLabel(billingFacade.activeSubscription()?.status) }}
            </strong>
          </header>

          <div class="plan-card__price">
            @if (billingFacade.activePlan()) {
              <strong>
                {{ formatMoney(
                  billingFacade.activePlan()?.monthlyPrice,
                  billingFacade.activePlan()?.currency
                ) }}
              </strong>
              <span>{{ 'settings.billing.perMonth' | translate }}</span>
            } @else {
              <strong>-</strong>
              <span>{{ 'settings.billing.choosePlanDescription' | translate }}</span>
            }
          </div>

          <dl class="plan-facts">
            <div>
              <dt>{{ 'nav.devices' | translate }}</dt>
              <dd>{{ formatLimit(billingFacade.activePlan()?.maxDevices) }}</dd>
            </div>
            <div>
              <dt>{{ 'nav.routines' | translate }}</dt>
              <dd>{{ formatLimit(billingFacade.activePlan()?.maxRoutines) }}</dd>
            </div>
            <div>
              <dt>{{ 'nav.alerts' | translate }}</dt>
              <dd>{{ formatLimit(billingFacade.activePlan()?.maxAlerts) }}</dd>
            </div>
            <div>
              <dt>{{ 'settings.billing.exports' | translate }}</dt>
              <dd>
                @if (billingFacade.activePlan()?.reportExportEnabled) {
                  {{ 'settings.billing.activeExports' | translate }}
                } @else {
                  {{ 'settings.billing.notIncluded' | translate }}
                }
              </dd>
            </div>
          </dl>

          <div class="plan-actions">
            <div class="plan-actions__left">
              <app-button
                variant="create"
                link="/plans"
                [label]="'settings.billing.goToPlans' | translate"
              />
            </div>

            <div class="plan-actions__right">
              @if (billingFacade.hasActiveSubscription()) {
                <app-button
                  variant="danger"
                  [disabled]="billingFacade.loading()"
                  (pressed)="cancelSubscription()"
                  [label]="'settings.billing.cancelPlanConfirm' | translate"
                />
              }
            </div>
          </div>
        </article>

        <aside class="billing-side">
          <article class="summary-card summary-card--payment">
            <span>{{ 'settings.billing.lastPayment' | translate }}</span>
            @if (billingFacade.lastPayment()) {
              <strong>
                {{ formatMoney(
                  billingFacade.lastPayment()?.amount,
                  billingFacade.lastPayment()?.currency
                ) }}
              </strong>
              <p>{{ paymentStatusLabel(billingFacade.lastPayment()?.status) }}</p>
            } @else {
              <strong>-</strong>
              <p>{{ 'settings.billing.noPaymentsRegistered' | translate }}</p>
            }
          </article>

          <article class="summary-grid">
            <div>
              <span>{{ 'settings.billing.payments' | translate }}</span>
              <strong>{{ billingFacade.payments().length }}</strong>
            </div>
            <div>
              <span>{{ 'billing.invoices' | translate }}</span>
              <strong>{{ billingFacade.invoices().length }}</strong>
            </div>
          </article>
        </aside>
      </section>
    </app-settings-section>

    <app-settings-section
      [eyebrow]="'settings.billing.payments' | translate"
      [title]="'settings.billing.registeredMovements' | translate"
      [description]="'settings.billing.registeredMovementsDescription' | translate"
      [stackedHeader]="true"
    >
      <div class="record-list">
        @for (payment of billingFacade.payments(); track payment.id) {
          <article class="record-card payment-card">
            <div class="record-card__main">
              <span>{{ 'settings.billing.paymentNumber' | translate: { id: payment.id } }}</span>
              <strong>{{ formatMoney(payment.amount, payment.currency) }}</strong>
            </div>

            <div class="record-card__meta">
              <span>{{ 'settings.billing.method' | translate }}</span>
              <strong>{{ payment.paymentMethod }}</strong>
            </div>

            <strong class="state-pill">
              {{ paymentStatusLabel(payment.status) }}
            </strong>
          </article>
        } @empty {
          <app-empty-state
            [eyebrow]="'settings.billing.payments' | translate"
            [title]="'settings.billing.noPaymentsTitle' | translate"
            [description]="'settings.billing.noPaymentsDescription' | translate"
          >
            <app-button variant="create" size="sm" link="/plans">
              {{ 'settings.billing.viewPlans' | translate }}
            </app-button>
          </app-empty-state>
        }
      </div>
    </app-settings-section>

    <app-settings-section
      [eyebrow]="'billing.invoices' | translate"
      [title]="'settings.billing.issuedDocuments' | translate"
      [description]="'settings.billing.issuedDocumentsDescription' | translate"
      [stackedHeader]="true"
    >
      <div class="record-list">
        @for (invoice of billingFacade.invoices(); track invoice.id) {
          <article class="record-card invoice-card">
            <div class="record-card__main">
              <span>{{ invoice.invoiceNumber }}</span>
              <strong>{{ 'settings.billing.invoiceNumber' | translate: { id: invoice.id } }}</strong>
              <p>{{ 'settings.billing.issuedOn' | translate: { date: formatDate(invoice.issuedAt) } }}</p>
            </div>

            <div class="record-card__meta">
              <span>{{ invoiceStateLabel(invoice) }}</span>
              <strong>{{ formatMoney(invoice.totalAmount, invoice.currency) }}</strong>
            </div>

            <app-button
              variant="secondary"
              size="sm"
              (pressed)="downloadInvoice(invoice.id)"
            >
              {{ 'settings.billing.download' | translate }}
            </app-button>
          </article>
        } @empty {
          <app-empty-state
            [eyebrow]="'billing.invoices' | translate"
            [title]="'settings.billing.noInvoicesTitle' | translate"
            [description]="'settings.billing.noInvoicesDescription' | translate"
          />
        }
      </div>
    </app-settings-section>
  }
</section>


============================================================
FILE: src\app\billing\presentation\pages\billing-history\billing-history-page.component.scss
============================================================

.billing-settings-page {
  display: grid;
  gap: 24px;
}

h1,
h2,
h3,
p,
dl {
  margin: 0;
}

.billing-overview {
  display: grid;
  grid-template-columns: minmax(0, 1.6fr) minmax(260px, 0.8fr);
  gap: 16px;
  align-items: stretch;
}

.plan-card,
.summary-card,
.summary-grid,
.record-card {
  border: 1px solid var(--border-color);
  background:
    var(--ec-card-highlight),
    var(--surface-card);
  box-shadow:
    var(--ec-inset-highlight),
    var(--ec-shadow-soft);
}

.plan-card {
  display: grid;
  gap: 20px;
  min-width: 0;
  padding: 22px;
  border-radius: 24px;
}

.plan-card__header,
.plan-actions,
.record-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.plan-card__header,
.plan-actions,
.record-card {
  gap: 14px;
}

.plan-card__header {
  align-items: flex-start;
}

.plan-card__header span,
.summary-card span,
.summary-grid span,
.record-card span {
  color: var(--accent-color);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.plan-card h2 {
  margin-top: 8px;
  color: var(--text-primary);
  font-size: clamp(1.8rem, 4vw, 3rem);
  line-height: 1;
}

.status-pill,
.state-pill {
  width: fit-content;
  border-radius: 999px;
  background: var(--ec-status-success-bg);
  color: var(--ec-status-success-text);
  font-size: 0.78rem;
  font-weight: 950;
  line-height: 1;
  white-space: nowrap;
}

.status-pill {
  padding: 8px 12px;
}

.state-pill {
  padding: 7px 10px;
}

.plan-card__price {
  display: flex;
  gap: 10px;
  align-items: baseline;
  flex-wrap: wrap;
  padding: 18px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background: var(--ec-surface-subtle);
}

.plan-card__price strong {
  color: var(--text-primary);
  font-size: clamp(2rem, 4vw, 3.35rem);
  line-height: 0.95;
}

.plan-card__price span {
  color: var(--text-secondary);
  font-weight: 850;
}

.plan-facts {
  display: grid;
  grid-template-columns: repeat(4, minmax(86px, 1fr));
  gap: 10px;
}

.plan-facts div,
.summary-grid div {
  display: grid;
  gap: 6px;
  justify-items: start;
  min-width: 0;
  padding: 13px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  background: var(--ec-surface-subtle);
  text-align: left;
}

.plan-facts dt {
  margin: 0;
  color: var(--text-muted);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.plan-facts dd {
  margin: 0;
}

.plan-facts dd,
.summary-grid strong,
.record-card strong {
  color: var(--text-primary);
  font-weight: 950;
}

.plan-actions {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  align-items: center;
  justify-content: stretch;
  flex-wrap: wrap;
  width: 100%;
}

.plan-actions__left,
.plan-actions__right {
  display: flex;
  align-items: center;
  min-width: 0;
}

.plan-actions__left {
  justify-content: flex-start;
}

.plan-actions__right {
  justify-content: flex-end;
}

.billing-side {
  display: grid;
  gap: 12px;
}

.summary-card,
.summary-grid {
  border-radius: 22px;
}

.summary-card {
  display: grid;
  align-content: center;
  gap: 10px;
  min-height: 190px;
  padding: 22px;
}

.summary-card strong {
  color: var(--text-primary);
  font-size: clamp(1.8rem, 3vw, 2.4rem);
  line-height: 1;
}

.summary-card p,
.record-card p {
  color: var(--text-secondary);
  font-weight: 800;
  line-height: 1.45;
}

.summary-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 10px;
  padding: 12px;
}

.record-list {
  display: grid;
  gap: 12px;
}

.record-card {
  min-width: 0;
  padding: 16px;
  border-radius: 18px;
}

.record-card__main,
.record-card__meta {
  display: grid;
  gap: 5px;
  min-width: 0;
}

.record-card__main {
  flex: 1 1 auto;
}

.record-card__main strong {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.record-card__meta {
  flex: 0 0 auto;
  justify-items: end;
  text-align: right;
}

.record-card__meta strong {
  font-size: 1.15rem;
}

.loading-wrapper {
  display: grid;
  place-items: center;
  min-height: 220px;
}

@media (max-width: 1080px) {
  .billing-overview {
    grid-template-columns: 1fr;
  }

  .plan-facts {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 720px) {
  .plan-card__header,
  .record-card {
    align-items: stretch;
    flex-direction: column;
  }

  .record-card__meta {
    justify-items: start;
    text-align: left;
  }

  .summary-grid {
    grid-template-columns: 1fr;
  }

  .plan-actions {
    grid-template-columns: 1fr;
  }

  .plan-actions__left,
  .plan-actions__right {
    justify-content: stretch;
  }
}


============================================================
FILE: src\app\billing\presentation\pages\billing-history\billing-history-page.component.ts
============================================================

import { TranslateModule, TranslateService } from '@ngx-translate/core';
import { Component, OnInit } from '@angular/core';

import { BillingFacade } from '../../../application/services/billing.facade';

import { ConfirmDialogService } from '../../../../shared/application/services/confirm-dialog.service';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { SettingsSectionComponent } from '../../../../shared/presentation/components/settings-section/settings-section.component';
import { Invoice } from '../../../domain/model/invoice.entity';
import { PaymentStatus } from '../../../domain/model/payment.entity';
import { SubscriptionStatus } from '../../../domain/model/subscription.entity';

@Component({
  selector: 'app-billing-history-page',
  standalone: true,
  imports: [
    TranslateModule,
    AppButtonComponent,
    EmptyStateComponent,
    LoadingSpinnerComponent,
    SettingsSectionComponent,
  ],
  templateUrl: './billing-history-page.component.html',
  styleUrls: ['./billing-history-page.component.scss'],
})
export class BillingHistoryPageComponent implements OnInit {
  constructor(
    readonly billingFacade: BillingFacade,
    private readonly toastService: ToastService,
    private readonly confirmDialog: ConfirmDialogService,
    private readonly translate: TranslateService,
    private readonly uiPreferences: UiPreferencesService
  ) {}

  async ngOnInit(): Promise<void> {
    await this.billingFacade.loadBilling();

    if (this.billingFacade.error()) {
      this.toastService.error(this.t('settings.billing.loadError'));
    }
  }

  downloadInvoice(invoiceId: number): void {
    const invoice = this.billingFacade
      .invoices()
      .find((item) => item.id === invoiceId);

    if (!invoice) {
      this.toastService.error(this.t('settings.billing.invoiceNotFound'));
      return;
    }

    try {
      this.billingFacade.downloadInvoice({ invoiceId });
      this.toastService.success(this.t('settings.billing.downloadSuccess'));
    } catch (error) {
      console.error(error);
      this.toastService.error(this.t('settings.billing.downloadError'));
    }
  }

  async cancelSubscription(): Promise<void> {
    const confirmed = await this.confirmDialog.confirm({
      title: this.t('settings.billing.cancelPlanTitle'),
      message: this.t('settings.billing.cancelPlanMessage'),
      confirmLabel: this.t('settings.billing.cancelPlanConfirm'),
      cancelLabel: this.t('settings.billing.keepPlan'),
      tone: 'warning',
    });

    if (!confirmed) {
      return;
    }

    const success = await this.billingFacade.cancelCurrentSubscription();

    if (success) {
      this.toastService.info(this.t('settings.billing.cancelSuccess'));
      return;
    }

    this.toastService.error(this.t('settings.billing.cancelError'));
  }

  formatMoney(amount: number | undefined, currency = 'PEN'): string {
    if (amount === undefined) {
      return '-';
    }

    const symbol = currency === 'PEN' ? 'S/' : currency;
    return `${symbol} ${amount.toFixed(2)}`;
  }

  formatLimit(value: number | null | undefined): string {
    return value === null || value === undefined
      ? this.t('settings.billing.unlimited')
      : String(value);
  }

  formatDate(value: string | undefined): string {
    if (!value) {
      return '-';
    }

    const date = new Date(value);

    if (Number.isNaN(date.getTime())) {
      return value;
    }

    return new Intl.DateTimeFormat(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
    }).format(date);
  }

  subscriptionStatusLabel(status: SubscriptionStatus | undefined): string {
    const labels: Record<SubscriptionStatus, string> = {
      ACTIVE: 'settings.billing.subscriptionStatuses.active',
      CANCELLED: 'settings.billing.subscriptionStatuses.cancelled',
      EXPIRED: 'settings.billing.subscriptionStatuses.expired',
    };

    return this.t(status ? labels[status] : 'settings.billing.noActivePlan');
  }

  paymentStatusLabel(status: PaymentStatus | undefined): string {
    const labels: Record<PaymentStatus, string> = {
      APPROVED: 'settings.billing.paymentStatuses.approved',
      REJECTED: 'settings.billing.paymentStatuses.rejected',
      PENDING: 'settings.billing.paymentStatuses.pending',
    };

    return status ? this.t(labels[status]) : '-';
  }

  invoiceStateLabel(invoice: Invoice): string {
    return invoice.issuedAt
      ? this.t('settings.billing.invoiceIssued')
      : this.t('settings.billing.invoiceRegistered');
  }

  private currentLocale(): string {
    const localeByLanguage: Record<string, string> = {
      es: 'es-PE',
      en: 'en-US',
      pt: 'pt-BR',
    };

    return localeByLanguage[this.uiPreferences.currentLanguage()] ?? localeByLanguage['es'];
  }

  private t(key: string): string {
    return this.translate.instant(key);
  }
}


============================================================
FILE: src\app\billing\presentation\pages\plans\plans-page.component.html
============================================================

<section class="plans-page">
  <section class="plan-summary-strip" aria-label="Plan comparison summary">
    <article>
      <span>{{ 'billing.summaryStarterLabel' | translate }}</span>
      <strong>{{ 'billing.summaryStarterText' | translate }}</strong>
    </article>

    <article class="highlighted">
      <span>{{ 'billing.summaryProfessionalLabel' | translate }}</span>
      <strong>{{ 'billing.summaryProfessionalText' | translate }}</strong>
    </article>

    <article>
      <span>{{ 'billing.summaryEnterpriseLabel' | translate }}</span>
      <strong>{{ 'billing.summaryEnterpriseText' | translate }}</strong>
    </article>
  </section>

  @if (billingFacade.loading()) {
    <div class="loading-wrapper">
      <app-loading-spinner />
    </div>
  } @else {
    @if (billingFacade.activePlan()) {
      <section class="current-plan">
        <div>
          <span>{{ 'billing.currentPlan' | translate }}</span>
          <strong>{{ billingFacade.activePlan()?.name }}</strong>
        </div>

        <app-button
          variant="danger"
          size="sm"
          [disabled]="billingFacade.loading()"
          (pressed)="cancelSubscription()"
        >
          {{ 'billing.cancel' | translate }}
        </app-button>
      </section>
    } @else {
      <section class="current-plan empty">
        <div>
          <span>{{ 'billing.currentPlan' | translate }}</span>
          <strong>{{ 'billing.noPlan' | translate }}</strong>
        </div>
      </section>
    }

    <section class="plans-grid">
      @for (plan of billingFacade.plans(); track plan.id) {
        <app-plan-card
          [plan]="plan"
          [activePlanCode]="billingFacade.activePlanCode()"
          (selected)="openPayment($event)"
        />
      }
    </section>
  }

  @if (selectedPlan) {
    <app-payment-form
      [plan]="selectedPlan"
      [loading]="billingFacade.loading()"
      (confirmed)="confirmPayment($event)"
      (canceled)="closePayment()"
    />
  }
</section>


============================================================
FILE: src\app\billing\presentation\pages\plans\plans-page.component.scss
============================================================

:host {
  display: block;
  width: 100%;
}

.plans-page {
  position: relative;
  isolation: isolate;
  width: min(1180px, calc(100vw - 48px));
  margin: 0 auto;
  padding: 56px 0 64px;
  display: grid;
  gap: 34px;
}

.plans-page::before {
  content: '';
  position: absolute;
  top: 120px;
  left: -180px;
  width: 360px;
  height: 360px;
  border-radius: 50%;
  background: radial-gradient(
    circle,
    var(--ec-accent-soft) 0%,
    color-mix(in srgb, var(--ec-accent-soft) 52%, transparent) 42%,
    transparent 72%
  );
  z-index: -1;
  pointer-events: none;
}

.plans-page::after {
  content: '';
  position: absolute;
  right: -170px;
  bottom: 120px;
  width: 400px;
  height: 400px;
  border-radius: 50%;
  background: radial-gradient(
    circle,
    var(--ec-accent-soft) 0%,
    color-mix(in srgb, var(--ec-accent-soft) 58%, transparent) 44%,
    transparent 74%
  );
  z-index: -1;
  pointer-events: none;
}

.page-heading {
  max-width: 860px;
  animation: riseUp 0.55s ease both;
}

.eyebrow {
  width: fit-content;
  margin: 0 0 16px;
  padding: 9px 16px;
  border: 1px solid var(--border-color);
  border-radius: 999px;
  color: var(--accent-color);
  background: var(--ec-surface-subtle);
  font-size: 0.84rem;
  font-weight: 900;
  text-transform: uppercase;
  letter-spacing: 0.14em;
}

h1 {
  margin: 0 0 14px;
  color: var(--text-primary);
  font-size: clamp(2.8rem, 6vw, 4.8rem);
  line-height: 0.98;
  letter-spacing: -0.05em;
}

.page-heading p {
  margin: 0;
  max-width: 760px;
  color: var(--ec-copy-color);
  line-height: 1.75;
  font-size: 1.06rem;
}

.plan-summary-strip {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;
  animation: riseUp 0.6s ease both;
}

.plan-summary-strip article {
  min-height: 92px;
  padding: 18px 20px;
  border: 1px solid var(--border-color);
  border-radius: 22px;
  background: var(--surface-card);
  box-shadow: var(--ec-shadow-soft);
}

.plan-summary-strip article.highlighted {
  border-color: var(--border-strong);
  background:
    var(--ec-card-highlight),
    var(--surface-card);
  box-shadow: var(--ec-shadow-soft);
}

.plan-summary-strip span {
  display: block;
  margin-bottom: 8px;
  color: var(--accent-color);
  font-size: 0.76rem;
  font-weight: 900;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.plan-summary-strip strong {
  display: block;
  color: var(--text-primary);
  font-size: 0.98rem;
  line-height: 1.45;
}

.current-plan {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  padding: 22px 24px;
  border: 1px solid var(--border-color);
  border-radius: 24px;
  background: var(--surface-card);
  box-shadow: var(--ec-shadow-soft);
  animation: riseUp 0.65s ease both;
}

.current-plan.empty {
  border-style: dashed;
}

.current-plan span {
  display: block;
  margin-bottom: 5px;
  color: var(--text-secondary);
  font-size: 0.86rem;
  font-weight: 900;
}

.current-plan strong {
  display: block;
  color: var(--text-primary);
  font-size: 1.55rem;
  line-height: 1.1;
}

.current-plan button {
  min-height: 44px;
  border: 1px solid var(--ec-status-danger-border);
  border-radius: 999px;
  padding: 0 18px;
  color: var(--ec-status-danger-text);
  background: var(--ec-status-danger-bg);
  cursor: pointer;
  font-weight: 900;
  transition: transform 0.22s ease, background 0.22s ease;
}

.current-plan button:hover {
  transform: translateY(-2px);
  background: var(--ec-status-danger-bg-strong);
}

.plans-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 24px;
  align-items: stretch;
  overflow: visible;
}

.plans-grid app-plan-card {
  display: block;
  min-width: 0;
  animation: riseUp 0.6s ease both;
}

.plans-grid app-plan-card:nth-child(2) {
  animation-delay: 0.08s;
}

.plans-grid app-plan-card:nth-child(3) {
  animation-delay: 0.16s;
}

.loading-wrapper {
  display: grid;
  place-items: center;
  min-height: 260px;
}

@keyframes riseUp {
  from {
    opacity: 0;
    transform: translateY(18px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }

  to {
    opacity: 1;
  }
}

@keyframes modalIn {
  from {
    opacity: 0;
    transform: translateY(18px) scale(0.98);
  }

  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

@media (max-width: 1050px) {
  .plans-page {
    width: min(760px, calc(100vw - 36px));
  }

  .plan-summary-strip,
  .plans-grid {
    grid-template-columns: 1fr;
  }

  .current-plan {
    align-items: flex-start;
    flex-direction: column;
  }
}

@media (max-width: 640px) {
  .plans-page {
    width: min(100%, calc(100vw - 28px));
    padding-top: 36px;
  }

  h1 {
    font-size: 2.65rem;
  }

  .plan-summary-strip article,
  .current-plan {
    border-radius: 18px;
  }
}


============================================================
FILE: src\app\billing\presentation\pages\plans\plans-page.component.ts
============================================================

import { Component, OnInit } from '@angular/core';
import { TranslateModule } from '@ngx-translate/core';

import { BillingFacade } from '../../../application/services/billing.facade';
import { PaymentFormCommand } from '../../../application/commands/payment-form.command';
import { Plan } from '../../../domain/model/plan.entity';

import { PlanCardComponent } from '../../components/plan-card/plan-card.component';
import { PaymentFormComponent } from '../../components/payment-form/payment-form.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { ConfirmDialogService } from '../../../../shared/application/services/confirm-dialog.service';
import { ToastService } from '../../../../shared/application/services/toast.service';

@Component({
  selector: 'app-plans-page',
  standalone: true,
  imports: [
    TranslateModule,
    PlanCardComponent,
    PaymentFormComponent,
    LoadingSpinnerComponent,
    AppButtonComponent,
  ],
  templateUrl: './plans-page.component.html',
  styleUrls: ['./plans-page.component.scss'],
})
export class PlansPageComponent implements OnInit {
  selectedPlan: Plan | null = null;

  constructor(
    readonly billingFacade: BillingFacade,
    private readonly toastService: ToastService,
    private readonly confirmDialog: ConfirmDialogService
  ) {}

  async ngOnInit(): Promise<void> {
    await this.billingFacade.loadBilling();

    if (this.billingFacade.error()) {
      this.toastService.error('No se pudo cargar la información de facturación.');
    }
  }

  openPayment(plan: Plan): void {
    this.billingFacade.clearMessages();
    this.selectedPlan = plan;
  }

  closePayment(): void {
    this.selectedPlan = null;
  }

  async confirmPayment(command: PaymentFormCommand): Promise<void> {
    const success = await this.billingFacade.processPaymentAndSubscribe(command);

    if (success) {
      this.selectedPlan = null;
      this.toastService.success('Pago registrado y plan activado.');
      return;
    }

    this.toastService.error('No se pudo registrar el pago.');
  }

  async cancelSubscription(): Promise<void> {
    const confirmed = await this.confirmDialog.confirm({
      title: 'Cancelar plan',
      message: 'Tu suscripcion activa quedara cancelada. Confirma solo si ya no quieres continuar con el plan.',
      confirmLabel: 'Cancelar plan',
      cancelLabel: 'Conservar plan',
      tone: 'warning',
    });

    if (!confirmed) {
      return;
    }

    const success = await this.billingFacade.cancelCurrentSubscription();

    if (success) {
      this.toastService.info('Suscripción cancelada.');
      return;
    }

    this.toastService.error('No se pudo cancelar la suscripción.');
  }
}


============================================================
FILE: src\app\billing\presentation\routes\billing.routes.ts
============================================================

import { Routes } from '@angular/router';

import { authGuard } from '../../../shared/application/guards/auth.guard';

export const BILLING_ROUTES: Routes = [
  {
    path: 'plans',
    canActivate: [authGuard],
    data: { title: 'Planes' },
    loadComponent: () =>
      import('../pages/plans/plans-page.component').then(
        (m) => m.PlansPageComponent
      ),
  },
  {
    path: 'billing/history',
    redirectTo: 'settings/billing',
    pathMatch: 'full',
  },
];


============================================================
FILE: src\app\device-control\application\commands\create-device.command.ts
============================================================

import { DeviceType } from '../../domain/model/device.entity';

export interface CreateDeviceCommand {
  name: string;
  room?: string | null;
  type: DeviceType;
  powerWatts: number;
}


============================================================
FILE: src\app\device-control\application\commands\create-device-group.command.ts
============================================================

export interface CreateDeviceGroupCommand {
  name: string;
  description: string;
  deviceIds: number[];
}


============================================================
FILE: src\app\device-control\application\commands\create-operation-mode.command.ts
============================================================

import { OperationModeRoutine } from '../../domain/model/operation-mode.entity';

export interface CreateOperationModeCommand {
  locationId: number;
  name: string;
  description: string;
  roomIds: number[];
  groupIds: number[];
  deviceIds: number[];
  turnOnDeviceIds: number[];
  turnOffDeviceIds: number[];
  keepOnDeviceIds: number[];
  routineIds?: number[];
  routinesToEnableIds?: number[];
  routinesToDisableIds?: number[];
  goalIds: number[];
  internalRoutines: OperationModeRoutine[];
  allDay: boolean;
  startsAt: string;
  endsAt: string;
  ruleProfileId: number | null;
  preferenceId: number | null;
  applyRuleProfile: boolean;
  applyNotificationPreference: boolean;
  applyRoutines?: boolean;
  preserveCriticalSound: boolean;
}


============================================================
FILE: src\app\device-control\application\commands\create-routine.command.ts
============================================================

import {
  RoutineAction,
  RoutineRepeatType,
  RoutineTargetType,
} from '../../domain/model/routine.entity';

export interface CreateRoutineCommand {
  name: string;
  action: RoutineAction;
  time: string;
  targetType: RoutineTargetType;
  targetId: number;
  deviceId?: number | null;
  groupId?: number | null;
  repeatType?: RoutineRepeatType;
  daysOfWeek?: string | null;
  intervalDays?: number | null;
  startsOn?: string | null;
}


============================================================
FILE: src\app\device-control\application\commands\execute-group-action.command.ts
============================================================

import { DeviceStatus } from '../../domain/model/device.entity';

export interface ExecuteGroupActionCommand {
  groupId: number;
  status: DeviceStatus;
}


============================================================
FILE: src\app\device-control\application\commands\update-device-group.command.ts
============================================================

export interface UpdateDeviceGroupCommand {
  groupId: number;
  name: string;
  description: string;
  deviceIds: number[];
}


============================================================
FILE: src\app\device-control\application\commands\update-device-status.command.ts
============================================================

import { DeviceStatus } from '../../domain/model/device.entity';

export interface UpdateDeviceStatusCommand {
  deviceId: number;
  status: DeviceStatus;
}


============================================================
FILE: src\app\device-control\application\commands\update-routine-status.command.ts
============================================================

export interface UpdateRoutineStatusCommand {
  routineId: number;
  enabled: boolean;
}


============================================================
FILE: src\app\device-control\application\services\device-control.facade.ts
============================================================

import { Injectable } from '@angular/core';
import { firstValueFrom } from 'rxjs';

import { BillingFacade } from '../../../billing/application/services/billing.facade';
import { PlanPermissionService } from '../../../billing/domain/services/plan-permission.service';

import { Device, DeviceStatus } from '../../domain/model/device.entity';
import { DeviceGroup } from '../../domain/model/device-group.entity';
import { OperationMode, OperationModeActivation, OperationModePreview } from '../../domain/model/operation-mode.entity';
import { Routine } from '../../domain/model/routine.entity';

import { CreateDeviceCommand } from '../commands/create-device.command';
import { CreateDeviceGroupCommand } from '../commands/create-device-group.command';
import { CreateOperationModeCommand } from '../commands/create-operation-mode.command';
import { CreateRoutineCommand } from '../commands/create-routine.command';
import { ExecuteGroupActionCommand } from '../commands/execute-group-action.command';
import { UpdateDeviceGroupCommand } from '../commands/update-device-group.command';
import { UpdateDeviceStatusCommand } from '../commands/update-device-status.command';
import { UpdateRoutineStatusCommand } from '../commands/update-routine-status.command';

import { DeviceGroupsApiService } from '../../infrastructure/api/device-groups-api.service';
import { DevicesApiService } from '../../infrastructure/api/devices-api.service';
import { OperationModesApiService } from '../../infrastructure/api/operation-modes-api.service';
import { RoutinesApiService } from '../../infrastructure/api/routines-api.service';

import { DeviceGroupAssembler } from '../../infrastructure/assemblers/device-group.assembler';
import { DeviceAssembler } from '../../infrastructure/assemblers/device.assembler';
import { OperationModeAssembler } from '../../infrastructure/assemblers/operation-mode.assembler';
import { RoutineAssembler } from '../../infrastructure/assemblers/routine.assembler';

import { DeviceGroupPolicyService } from '../../domain/services/device-group-policy.service';
import { RoutineConflictCheckerService } from '../../domain/services/routine-conflict-checker.service';

import { DeviceControlStore } from '../stores/device-control.store';

@Injectable({
  providedIn: 'root',
})
export class DeviceControlFacade {
  private readonly deviceAssembler = new DeviceAssembler();
  private readonly routineAssembler = new RoutineAssembler();
  private readonly deviceGroupAssembler = new DeviceGroupAssembler();
  private readonly operationModeAssembler = new OperationModeAssembler();

  get devices() {
    return this.store.devices;
  }

  get routines() {
    return this.store.routines;
  }

  get deviceGroups() {
    return this.store.deviceGroups;
  }

  get operationModes() {
    return this.store.operationModes;
  }

  get loading() {
    return this.store.loading;
  }

  get error() {
    return this.store.error;
  }

  get activeDevices() {
    return this.store.activeDevices;
  }

  get totalCurrentWatts() {
    return this.store.totalCurrentWatts;
  }

  get enabledRoutines() {
    return this.store.enabledRoutines;
  }

  constructor(
    private readonly devicesApi: DevicesApiService,
    private readonly routinesApi: RoutinesApiService,
    private readonly deviceGroupsApi: DeviceGroupsApiService,
    private readonly operationModesApi: OperationModesApiService,
    private readonly billingFacade: BillingFacade,
    private readonly planPermissionService: PlanPermissionService,
    private readonly routineConflictChecker: RoutineConflictCheckerService,
    private readonly deviceGroupPolicy: DeviceGroupPolicyService,
    private readonly store: DeviceControlStore
  ) {}

  async loadDeviceControl(): Promise<void> {
    this.startRequest();

    try {
      await Promise.all([
        this.loadDevices(),
        this.loadRoutines(),
        this.loadDeviceGroups(),
        this.loadOperationModes(),
      ]);
    } catch (error) {
      console.error(error);
      this.store.setError('devices.loadError');
    } finally {
      this.finishRequest();
    }
  }

  async loadDevices(): Promise<void> {
    const responses = await firstValueFrom(this.devicesApi.findAllForCurrentUser());

    this.store.setDevices(
      responses.map((response) => this.deviceAssembler.toEntity(response))
    );
  }

  async createDevice(command: CreateDeviceCommand): Promise<Device | null> {
    this.startRequest();

    try {
      const activePlanCode = this.billingFacade.activePlanCode();

      const canCreate = this.planPermissionService.canCreateDevice(
        activePlanCode,
        this.store.devices().length
      );

      if (!canCreate) {
        this.store.setError('devices.planLimitReached');
        return null;
      }

      if (!command.name.trim()) {
        this.store.setError('devices.createError');
        return null;
      }

      const powerRules: Record<string, { min: number; max: number }> = {
        PLUG: { min: 5, max: 2500 },
        LIGHT: { min: 3, max: 150 },
        SWITCH: { min: 5, max: 1800 },
        SENSOR: { min: 1, max: 30 },
        OTHER: { min: 1, max: 1000 },
      };

      const rule = powerRules[command.type];
      const watts = Number(command.powerWatts);

      if (!rule || !Number.isFinite(watts) || watts < rule.min || watts > rule.max) {
        this.store.setError('devices.invalidPowerRange');
        return null;
      }

      const response = await firstValueFrom(
        this.devicesApi.create({
          name: command.name.trim(),
          room: command.room?.trim() ?? '',
          type: command.type,
          powerWatts: Number(command.powerWatts),
          status: 'OFF',
          createdAt: new Date().toISOString().slice(0, 10),
        })
      );

      const device = this.deviceAssembler.toEntity(response);
      this.store.prependDevice(device);

      return device;
    } catch (error) {
      console.error(error);
      this.store.setError('devices.createError');
      return null;
    } finally {
      this.finishRequest();
    }
  }

  async updateDeviceStatus(command: UpdateDeviceStatusCommand): Promise<boolean> {
    this.startRequest();

    try {
      const response = await firstValueFrom(
        this.devicesApi.updateStatus({
          deviceId: command.deviceId,
          status: command.status,
        })
      );

      const updatedDevice = this.deviceAssembler.toEntity(response);

      this.store.updateDevice(updatedDevice);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('devices.updateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async toggleDevice(device: Device): Promise<boolean> {
    const nextStatus: DeviceStatus = device.isOn ? 'OFF' : 'ON';

    return this.updateDeviceStatus({
      deviceId: device.id,
      status: nextStatus,
    });
  }

  async deleteDevice(deviceId: number): Promise<boolean> {
    this.startRequest();

    try {
      await firstValueFrom(this.devicesApi.delete(deviceId));
      this.store.removeDevice(deviceId);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('devices.deleteError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async loadRoutines(): Promise<void> {
    try {
      const response = await firstValueFrom(
        this.routinesApi.findAllForCurrentUser()
      );

      const routines = response.map((item) => this.routineAssembler.toEntity(item));

      this.store.setRoutines(routines);
    } catch (error) {
      console.error(error);
      this.store.setError('routines.loadError');
    }
  }

  async createRoutine(command: CreateRoutineCommand): Promise<boolean> {
    this.startRequest();

    try {
      const response = await firstValueFrom(
        this.routinesApi.create({
          deviceId: command.deviceId ?? null,
          groupId: command.groupId ?? null,
          targetType: command.targetType,
          targetId: command.targetId,
          name: command.name.trim(),
          action: command.action,
          time: command.time,
          repeatType: command.repeatType ?? 'DAILY',
          daysOfWeek: command.daysOfWeek ?? '',
          intervalDays: command.intervalDays ?? 1,
          startsOn: command.startsOn ?? null,
          enabled: true,
        })
      );

      const routine = this.routineAssembler.toEntity(response);

      this.store.prependRoutine(routine);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('routines.createError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async updateRoutineStatus(command: UpdateRoutineStatusCommand): Promise<boolean> {
    this.startRequest();

    try {
      const response = await firstValueFrom(
        this.routinesApi.updateStatus({
          routineId: command.routineId,
          enabled: command.enabled,
        })
      );

      const routine = this.routineAssembler.toEntity(response);

      this.store.updateRoutine(routine);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('routines.updateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async deleteRoutine(routineId: number): Promise<boolean> {
    this.startRequest();

    try {
      await firstValueFrom(this.routinesApi.delete(routineId));

      this.store.removeRoutine(routineId);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('routines.deleteError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async executeRoutine(routineId: number): Promise<boolean> {
    this.startRequest();

    try {
      const response = await firstValueFrom(
        this.routinesApi.execute(routineId)
      );

      const routine = this.routineAssembler.toEntity(response);

      this.store.updateRoutine(routine);
      await this.loadDevices();
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('routines.executeError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async loadDeviceGroups(): Promise<void> {
    const responses = await firstValueFrom(
      this.deviceGroupsApi.findAllForCurrentUser()
    );

    this.store.setDeviceGroups(
      responses.map((response) => this.deviceGroupAssembler.toEntity(response))
    );
  }

  async createDeviceGroup(command: CreateDeviceGroupCommand): Promise<boolean> {
    this.startRequest();

    try {
      if (!this.deviceGroupPolicy.canCreateGroup(command.name, command.deviceIds)) {
        this.store.setError('deviceGroups.createError');
        return false;
      }

      const response = await firstValueFrom(
        this.deviceGroupsApi.create({
          name: command.name.trim(),
          description: command.description?.trim() ?? '',
          deviceIds: command.deviceIds,
          createdAt: new Date().toISOString().slice(0, 10),
        })
      );

      const group = this.deviceGroupAssembler.toEntity(response);

      this.store.prependDeviceGroup(group);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('deviceGroups.createError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async updateDeviceGroup(command: UpdateDeviceGroupCommand): Promise<boolean> {
    this.startRequest();

    try {
      const response = await firstValueFrom(
        this.deviceGroupsApi.patch(command.groupId, {
          name: command.name.trim(),
          description: command.description.trim(),
          deviceIds: command.deviceIds,
        })
      );

      const updatedGroup = this.deviceGroupAssembler.toEntity(response);

      this.store.updateDeviceGroup(updatedGroup);

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('deviceGroups.updateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async deleteDeviceGroup(groupId: number): Promise<boolean> {
    this.startRequest();

    try {
      await firstValueFrom(this.deviceGroupsApi.delete(groupId));

      this.store.removeDeviceGroup(groupId);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('deviceGroups.deleteError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async executeGroupAction(command: ExecuteGroupActionCommand): Promise<boolean> {
    this.startRequest();

    try {
      const group = this.store
        .deviceGroups()
        .find((item) => item.id === command.groupId);

      if (!group || !this.deviceGroupPolicy.canExecuteGroupAction(group)) {
        this.store.setError('deviceGroups.executeError');
        return false;
      }

      await firstValueFrom(
        this.deviceGroupsApi.executeAction({
          groupId: command.groupId,
          status: command.status,
        })
      );

      await this.loadDevices();

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('deviceGroups.executeError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async loadOperationModes(): Promise<void> {
    const responses = await firstValueFrom(
      this.operationModesApi.findAllForCurrentUser()
    );

    this.store.setOperationModes(
      responses.map((response) => this.operationModeAssembler.toEntity(response))
    );
  }

  async createOperationMode(command: CreateOperationModeCommand): Promise<boolean> {
    this.startRequest();

    try {
      const response = await firstValueFrom(
        this.operationModesApi.create({
          ...command,
          routineIds: command.routineIds ?? [],
          routinesToEnableIds: command.routinesToEnableIds ?? [],
          routinesToDisableIds: command.routinesToDisableIds ?? [],
          applyRoutines: command.applyRoutines ?? false,
        })
      );

      this.store.prependOperationMode(this.operationModeAssembler.toEntity(response));
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('operationModes.createError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async previewOperationMode(modeId: number): Promise<OperationModePreview | null> {
    try {
      const response = await firstValueFrom(
        this.operationModesApi.preview(modeId)
      );

      return this.operationModeAssembler.toPreview(response);
    } catch (error) {
      console.error(error);
      this.store.setError('operationModes.previewError');
      return null;
    }
  }

  async activateOperationMode(modeId: number): Promise<OperationModeActivation | null> {
    this.startRequest();

    try {
      const response = await firstValueFrom(
        this.operationModesApi.activate(modeId)
      );
      const result = this.operationModeAssembler.toActivation(response);

      this.store.updateOperationMode(result.mode);
      await Promise.all([this.loadDevices(), this.loadRoutines(), this.loadOperationModes()]);
      return result;
    } catch (error) {
      console.error(error);
      this.store.setError('operationModes.activateError');
      return null;
    } finally {
      this.finishRequest();
    }
  }

  async archiveOperationMode(modeId: number): Promise<boolean> {
    this.startRequest();

    try {
      await firstValueFrom(this.operationModesApi.archive(modeId));

      this.store.removeOperationMode(modeId);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('operationModes.archiveError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  getDeviceName(deviceId: number): string {
    return (
      this.store.devices().find((device) => device.id === deviceId)?.name ??
      `Device #${deviceId}`
    );
  }

  clearMessages(): void {
    this.store.clearMessages();
  }

  private startRequest(): void {
    this.store.setLoading(true);
    this.store.clearMessages();
  }

  private finishRequest(): void {
    this.store.setLoading(false);
  }
}


============================================================
FILE: src\app\device-control\application\stores\device-control.store.ts
============================================================

import { Injectable, computed, signal } from '@angular/core';

import { Device } from '../../domain/model/device.entity';
import { DeviceGroup } from '../../domain/model/device-group.entity';
import { OperationMode } from '../../domain/model/operation-mode.entity';
import { Routine } from '../../domain/model/routine.entity';

@Injectable({
  providedIn: 'root',
})
export class DeviceControlStore {
  private readonly devicesSignal = signal<Device[]>([]);
  private readonly routinesSignal = signal<Routine[]>([]);
  private readonly deviceGroupsSignal = signal<DeviceGroup[]>([]);
  private readonly operationModesSignal = signal<OperationMode[]>([]);

  private readonly loadingSignal = signal<boolean>(false);
  private readonly errorSignal = signal<string | null>(null);

  readonly devices = computed(() => this.devicesSignal());
  readonly routines = computed(() => this.routinesSignal());
  readonly deviceGroups = computed(() => this.deviceGroupsSignal());
  readonly operationModes = computed(() => this.operationModesSignal());

  readonly loading = computed(() => this.loadingSignal());
  readonly error = computed(() => this.errorSignal());

  readonly activeDevices = computed(() =>
    this.devicesSignal().filter((device) => device.isOn)
  );

  readonly totalCurrentWatts = computed(() =>
    this.activeDevices().reduce((total, device) => total + device.powerWatts, 0)
  );

  readonly enabledRoutines = computed(() =>
    this.routinesSignal().filter((routine) => routine.enabled)
  );

  setDevices(value: Device[]): void {
    this.devicesSignal.set(value);
  }

  prependDevice(value: Device): void {
    this.devicesSignal.update((devices) => [value, ...devices]);
  }

  updateDevice(value: Device): void {
    this.devicesSignal.update((devices) =>
      devices.map((device) => (device.id === value.id ? value : device))
    );
  }

  removeDevice(deviceId: number): void {
    this.devicesSignal.update((devices) =>
      devices.filter((device) => device.id !== deviceId)
    );

    this.routinesSignal.update((routines) =>
      routines.filter(
        (routine) =>
          !(
            routine.targetType === 'DEVICE' &&
            (routine.deviceId === deviceId || routine.targetId === deviceId)
          )
      )
    );

    this.deviceGroupsSignal.update((groups) =>
      groups.map(
        (group) =>
          new DeviceGroup({
            id: group.id,
            userId: group.userId,
            name: group.name,
            description: group.description,
            deviceIds: group.deviceIds.filter((id) => id !== deviceId),
            createdAt: group.createdAt,
          })
      )
    );
  }

  setRoutines(value: Routine[]): void {
    this.routinesSignal.set(value);
  }

  prependRoutine(value: Routine): void {
    this.routinesSignal.update((routines) => [value, ...routines]);
  }

  updateRoutine(value: Routine): void {
    this.routinesSignal.update((routines) =>
      routines.map((routine) => (routine.id === value.id ? value : routine))
    );
  }

  removeRoutine(routineId: number): void {
    this.routinesSignal.update((routines) =>
      routines.filter((routine) => routine.id !== routineId)
    );
  }

  setDeviceGroups(value: DeviceGroup[]): void {
    this.deviceGroupsSignal.set(value);
  }

  prependDeviceGroup(value: DeviceGroup): void {
    this.deviceGroupsSignal.update((groups) => [value, ...groups]);
  }

  updateDeviceGroup(value: DeviceGroup): void {
    this.deviceGroupsSignal.update((groups) =>
      groups.map((group) => (group.id === value.id ? value : group))
    );
  }

  removeDeviceGroup(groupId: number): void {
    this.deviceGroupsSignal.update((groups) =>
      groups.filter((group) => group.id !== groupId)
    );
  }

  setOperationModes(value: OperationMode[]): void {
    this.operationModesSignal.set(value);
  }

  prependOperationMode(value: OperationMode): void {
    this.operationModesSignal.update((modes) => [value, ...modes]);
  }

  updateOperationMode(value: OperationMode): void {
    this.operationModesSignal.update((modes) =>
      modes.map((mode) => (mode.id === value.id ? value : mode))
    );
  }

  removeOperationMode(modeId: number): void {
    this.operationModesSignal.update((modes) =>
      modes.filter((mode) => mode.id !== modeId)
    );
  }

  setLoading(value: boolean): void {
    this.loadingSignal.set(value);
  }

  setError(value: string | null): void {
    this.errorSignal.set(value);
  }

  clearMessages(): void {
    this.errorSignal.set(null);
  }

  reset(): void {
    this.devicesSignal.set([]);
    this.routinesSignal.set([]);
    this.deviceGroupsSignal.set([]);
    this.operationModesSignal.set([]);
    this.loadingSignal.set(false);
    this.errorSignal.set(null);
  }
}


============================================================
FILE: src\app\device-control\domain\model\device.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export type DeviceStatus = 'ON' | 'OFF' | 'MAINTENANCE' | 'REMOVED';
export type DeviceType = 'PLUG' | 'LIGHT' | 'SWITCH' | 'SENSOR' | 'OTHER';

export class Device extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _name: string;
  private readonly _room: string;
  private readonly _type: DeviceType;
  private readonly _powerWatts: number;
  private readonly _status: DeviceStatus;
  private readonly _createdAt: string;

  constructor(props: {
    id: number;
    userId: number;
    name: string;
    room?: string | null;
    type: DeviceType;
    powerWatts: number;
    status: DeviceStatus;
    createdAt: string;
  }) {
    super(props.id);
    this._userId = props.userId;
    this._name = props.name;
    this._room = props.room?.trim() ?? '';
    this._type = props.type;
    this._powerWatts = props.powerWatts;
    this._status = props.status;
    this._createdAt = props.createdAt;
  }

  get userId(): number {
    return this._userId;
  }

  get name(): string {
    return this._name;
  }

  get room(): string {
    return this._room;
  }

  get type(): DeviceType {
    return this._type;
  }

  get powerWatts(): number {
    return this._powerWatts;
  }

  get status(): DeviceStatus {
    return this._status;
  }

  get createdAt(): string {
    return this._createdAt;
  }

  get isOn(): boolean {
    return this._status === 'ON';
  }

  get isOff(): boolean {
    return this._status === 'OFF';
  }

  get isInMaintenance(): boolean {
    return this._status === 'MAINTENANCE';
  }

  get isRemoved(): boolean {
    return this._status === 'REMOVED';
  }

  get canReceiveOperationalChanges(): boolean {
    return this.isOn || this.isOff;
  }
}


============================================================
FILE: src\app\device-control\domain\model\device-group.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export class DeviceGroup extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _name: string;
  private readonly _description: string;
  private readonly _deviceIds: number[];
  private readonly _createdAt: string;

  constructor(props: {
    id: number;
    userId: number;
    name: string;
    description?: string | null;
    deviceIds?: number[] | null;
    createdAt?: string | null;
  }) {
    super(props.id);

    this._userId = props.userId;
    this._name = props.name;
    this._description = props.description ?? '';
    this._deviceIds = Array.isArray(props.deviceIds) ? props.deviceIds : [];
    this._createdAt =
      props.createdAt ?? new Date().toISOString().slice(0, 10);
  }

  get userId(): number {
    return this._userId;
  }

  get name(): string {
    return this._name;
  }

  get description(): string {
    return this._description;
  }

  get deviceIds(): number[] {
    return this._deviceIds;
  }

  get createdAt(): string {
    return this._createdAt;
  }

  get deviceCount(): number {
    return this._deviceIds.length;
  }

  get hasDevices(): boolean {
    return this._deviceIds.length > 0;
  }

  containsDevice(deviceId: number): boolean {
    return this._deviceIds.includes(deviceId);
  }
}

============================================================
FILE: src\app\device-control\domain\model\operation-mode.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export type OperationModeStatus = 'DRAFT' | 'ACTIVE' | 'INACTIVE' | 'ARCHIVED';
export type OperationModeRoutineTargetType = 'DEVICE' | 'GROUP';
export type OperationModeRoutineAction = 'TURN_ON' | 'TURN_OFF';

export interface OperationModeRoutine {
  name: string;
  targetType: OperationModeRoutineTargetType;
  targetId: number;
  action: OperationModeRoutineAction;
  triggerTime: string;
  enabled: boolean;
}

export class OperationMode extends BaseEntity<number> {
  constructor(
    private readonly props: {
      id: number;
      userId: number;
      locationId: number;
      name: string;
      description: string;
      status: OperationModeStatus;
      roomIds: number[];
      groupIds: number[];
      deviceIds: number[];
      turnOnDeviceIds: number[];
      turnOffDeviceIds: number[];
      keepOnDeviceIds: number[];
      routineIds: number[];
      routinesToEnableIds: number[];
      routinesToDisableIds: number[];
      goalIds: number[];
      internalRoutines: OperationModeRoutine[];
      allDay: boolean;
      startsAt: string;
      endsAt: string;
      ruleProfileId: number | null;
      preferenceId: number | null;
      applyRuleProfile: boolean;
      applyNotificationPreference: boolean;
      applyRoutines: boolean;
      preserveCriticalSound: boolean;
      lastActivatedAt: string | null;
    }
  ) {
    super(props.id);
  }

  get userId(): number { return this.props.userId; }
  get locationId(): number { return this.props.locationId; }
  get name(): string { return this.props.name; }
  get description(): string { return this.props.description; }
  get status(): OperationModeStatus { return this.props.status; }
  get roomIds(): number[] { return this.props.roomIds; }
  get groupIds(): number[] { return this.props.groupIds; }
  get deviceIds(): number[] { return this.props.deviceIds; }
  get turnOnDeviceIds(): number[] { return this.props.turnOnDeviceIds; }
  get turnOffDeviceIds(): number[] { return this.props.turnOffDeviceIds; }
  get keepOnDeviceIds(): number[] { return this.props.keepOnDeviceIds; }
  get routineIds(): number[] { return this.props.routineIds; }
  get routinesToEnableIds(): number[] { return this.props.routinesToEnableIds; }
  get routinesToDisableIds(): number[] { return this.props.routinesToDisableIds; }
  get goalIds(): number[] { return this.props.goalIds; }
  get internalRoutines(): OperationModeRoutine[] { return this.props.internalRoutines; }
  get allDay(): boolean { return this.props.allDay; }
  get startsAt(): string { return this.props.startsAt; }
  get endsAt(): string { return this.props.endsAt; }
  get ruleProfileId(): number | null { return this.props.ruleProfileId; }
  get preferenceId(): number | null { return this.props.preferenceId; }
  get applyRuleProfile(): boolean { return this.props.applyRuleProfile; }
  get applyNotificationPreference(): boolean { return this.props.applyNotificationPreference; }
  get applyRoutines(): boolean { return this.props.applyRoutines; }
  get preserveCriticalSound(): boolean { return this.props.preserveCriticalSound; }
  get lastActivatedAt(): string | null { return this.props.lastActivatedAt; }
}

export interface OperationModePreview {
  modeId: number;
  locationId: number;
  locationName: string;
  affectedDeviceIds: number[];
  ignoredRemovedDeviceIds: number[];
  ignoredMaintenanceDeviceIds: number[];
  roomIds: number[];
  groupIds: number[];
  routineIds: number[];
  internalRoutineCount?: number;
  goalIds: number[];
  ruleProfileId: number | null;
  preferenceId: number | null;
  evidence: string;
  explanation: string;
  recommendedAction: string;
}

export interface OperationModeActivation {
  mode: OperationMode;
  turnedOnDeviceIds: number[];
  turnedOffDeviceIds: number[];
  ignoredRemovedDeviceIds: number[];
  ignoredMaintenanceDeviceIds: number[];
  enabledRoutineIds: number[];
  disabledRoutineIds: number[];
  evidence: string;
  explanation: string;
  recommendedAction: string;
}


============================================================
FILE: src\app\device-control\domain\model\routine.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export type RoutineAction = 'TURN_ON' | 'TURN_OFF';
export type RoutineTargetType = 'DEVICE' | 'GROUP' | 'ROOM' | 'WORKPLACE';
export type RoutineRepeatType = 'ONCE' | 'DAILY' | 'WEEKLY' | 'CUSTOM_INTERVAL';

export class Routine extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _deviceId: number | null;
  private readonly _groupId: number | null;
  private readonly _targetType: RoutineTargetType;
  private readonly _targetId: number;
  private readonly _targetName: string;
  private readonly _name: string;
  private readonly _action: RoutineAction;
  private readonly _time: string;
  private readonly _repeatType: RoutineRepeatType;
  private readonly _daysOfWeek: string;
  private readonly _intervalDays: number;
  private readonly _startsOn: string | null;
  private readonly _applicableDeviceCount: number;
  private readonly _blockedDeviceCount: number;
  private readonly _enabled: boolean;

  constructor(props: {
    id: number;
    userId: number;
    deviceId?: number | null;
    groupId?: number | null;
    targetType?: RoutineTargetType | null;
    targetId?: number | null;
    targetName?: string | null;
    name: string;
    action: RoutineAction;
    time: string;
    repeatType?: RoutineRepeatType | null;
    daysOfWeek?: string | null;
    intervalDays?: number | null;
    startsOn?: string | null;
    applicableDeviceCount?: number | null;
    blockedDeviceCount?: number | null;
    enabled: boolean;
  }) {
    super(props.id);

    this._userId = props.userId;
    this._deviceId = props.deviceId ?? null;
    this._groupId = props.groupId ?? null;
    this._targetType = props.targetType ?? (props.groupId ? 'GROUP' : 'DEVICE');
    this._targetId = props.targetId ?? props.groupId ?? props.deviceId ?? 0;
    this._targetName = props.targetName ?? '';
    this._name = props.name;
    this._action = props.action;
    this._time = props.time;
    this._repeatType = props.repeatType ?? 'DAILY';
    this._daysOfWeek = props.daysOfWeek ?? '';
    this._intervalDays = props.intervalDays && props.intervalDays > 0 ? props.intervalDays : 1;
    this._startsOn = props.startsOn ?? null;
    this._applicableDeviceCount = props.applicableDeviceCount ?? 0;
    this._blockedDeviceCount = props.blockedDeviceCount ?? 0;
    this._enabled = props.enabled;
  }

  get userId(): number {
    return this._userId;
  }

  get deviceId(): number | null {
    return this._deviceId;
  }

  get groupId(): number | null {
    return this._groupId;
  }

  get targetType(): RoutineTargetType {
    return this._targetType;
  }

  get targetId(): number {
    return this._targetId;
  }

  get targetName(): string {
    return this._targetName;
  }

  get name(): string {
    return this._name;
  }

  get action(): RoutineAction {
    return this._action;
  }

  get time(): string {
    return this._time;
  }

  get repeatType(): RoutineRepeatType {
    return this._repeatType;
  }

  get daysOfWeek(): string {
    return this._daysOfWeek;
  }

  get intervalDays(): number {
    return this._intervalDays;
  }

  get startsOn(): string | null {
    return this._startsOn;
  }

  get applicableDeviceCount(): number {
    return this._applicableDeviceCount;
  }

  get blockedDeviceCount(): number {
    return this._blockedDeviceCount;
  }

  get enabled(): boolean {
    return this._enabled;
  }

  get isGroupRoutine(): boolean {
    return this._targetType === 'GROUP';
  }

  get actionLabel(): string {
    return this._action === 'TURN_ON' ? 'Encender' : 'Apagar';
  }

  get targetTypeLabel(): string {
    const labels: Record<RoutineTargetType, string> = {
      DEVICE: 'Dispositivo',
      GROUP: 'Grupo',
      ROOM: 'Habitacion',
      WORKPLACE: 'Sede',
    };

    return labels[this._targetType];
  }

  get repeatLabel(): string {
    if (this._repeatType === 'ONCE') {
      return this._startsOn ? `Una vez - ${this._startsOn}` : 'Una vez';
    }

    if (this._repeatType === 'WEEKLY') {
      return this._daysOfWeek ? `Semanal - ${this._daysOfWeek}` : 'Semanal';
    }

    if (this._repeatType === 'CUSTOM_INTERVAL') {
      return `Cada ${this._intervalDays} dia${this._intervalDays === 1 ? '' : 's'}`;
    }

    return 'Diario';
  }
}


============================================================
FILE: src\app\device-control\domain\model\routine-conflict-candidate.model.ts
============================================================

import {
  RoutineAction,
  RoutineTargetType,
} from './routine.entity';

export interface RoutineConflictCandidate {
  action: RoutineAction;
  time: string;
  targetType: RoutineTargetType;
  targetId: number;
}


============================================================
FILE: src\app\device-control\domain\services\device-group-policy.service.ts
============================================================

import { Injectable } from '@angular/core';

import { DeviceGroup } from '../model/device-group.entity';

@Injectable({
  providedIn: 'root',
})
export class DeviceGroupPolicyService {
  canCreateGroup(
    name: string,
    deviceIds: number[] | null | undefined
  ): boolean {
    return Boolean(name?.trim()) && Array.isArray(deviceIds) && deviceIds.length > 0;
  }

  canExecuteGroupAction(group: DeviceGroup | null | undefined): boolean {
    return Boolean(group?.hasDevices);
  }
}

============================================================
FILE: src\app\device-control\domain\services\routine-conflict-checker.service.ts
============================================================

import { Injectable } from '@angular/core';

import { Routine } from '../model/routine.entity';
import { RoutineConflictCandidate } from '../model/routine-conflict-candidate.model';

@Injectable({
  providedIn: 'root',
})
export class RoutineConflictCheckerService {
  hasConflict(routines: Routine[], candidate: RoutineConflictCandidate): boolean {
    return routines.some(
      (routine) =>
        routine.enabled &&
        routine.targetType === candidate.targetType &&
        routine.targetId === candidate.targetId &&
        routine.time === candidate.time &&
        routine.action === candidate.action
    );
  }
}


============================================================
FILE: src\app\device-control\infrastructure\api\device-groups-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { DeviceStatus } from '../../domain/model/device.entity';
import { DeviceGroupResource } from '../resources/device-group.resource';
import { DeviceGroupResponse } from '../responses/device-group.response';

@Injectable({
  providedIn: 'root',
})
export class DeviceGroupsApiService {
  private readonly resourcePath: string;

  constructor(
    private readonly http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    this.resourcePath = `${apiBaseUrl}/device-groups`;
  }

  findAllForCurrentUser(): Observable<DeviceGroupResponse[]> {
    return this.http.get<DeviceGroupResponse[]>(this.resourcePath);
  }

  create(resource: DeviceGroupResource): Observable<DeviceGroupResponse> {
    return this.http.post<DeviceGroupResponse>(this.resourcePath, resource);
  }

  patch(
    groupId: number,
    resource: Partial<DeviceGroupResource>
  ): Observable<DeviceGroupResponse> {
    return this.http.patch<DeviceGroupResponse>(
      `${this.resourcePath}/${groupId}`,
      resource
    );
  }

  executeAction(payload: {
    groupId: number;
    status: DeviceStatus;
  }): Observable<void> {
    return this.http.patch<void>(
      `${this.resourcePath}/${payload.groupId}/execute`,
      {
        status: payload.status,
      }
    );
  }

  delete(id: number): Observable<void> {
    return this.http.delete<void>(`${this.resourcePath}/${id}`);
  }
}


============================================================
FILE: src\app\device-control\infrastructure\api\devices-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { Device } from '../../domain/model/device.entity';
import { DeviceAssembler } from '../assemblers/device.assembler';
import { DeviceResource } from '../resources/device.resource';
import { DeviceResponse } from '../responses/device.response';

@Injectable({
  providedIn: 'root',
})
export class DevicesApiService extends BaseApiService<
  Device,
  DeviceResource,
  DeviceResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(http, apiBaseUrl, 'devices', new DeviceAssembler());
  }

  findAllForCurrentUser(): Observable<DeviceResponse[]> {
    return this.http.get<DeviceResponse[]>(this.resourceEndpoint);
  }

  toggle(payload: {
    deviceId: number;
  }): Observable<DeviceResponse> {
    return this.http.patch<DeviceResponse>(
      `${this.resourceEndpoint}/${payload.deviceId}/toggle`,
      {}
    );
  }

  updateStatus(payload: {
    deviceId: number;
    status: string;
  }): Observable<DeviceResponse> {
    return this.http.patch<DeviceResponse>(
      `${this.resourceEndpoint}/${payload.deviceId}/status`,
      { status: payload.status }
    );
  }
}


============================================================
FILE: src\app\device-control\infrastructure\api\operation-modes-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { OperationModeResource } from '../resources/operation-mode.resource';
import {
  OperationModeActivationResponse,
  OperationModePreviewResponse,
  OperationModeResponse,
} from '../responses/operation-mode.response';

@Injectable({
  providedIn: 'root',
})
export class OperationModesApiService {
  private readonly endpoint: string;

  constructor(
    private readonly http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    this.endpoint = `${apiBaseUrl}/operation-modes`;
  }

  findAllForCurrentUser(): Observable<OperationModeResponse[]> {
    return this.http.get<OperationModeResponse[]>(this.endpoint);
  }

  create(resource: OperationModeResource): Observable<OperationModeResponse> {
    return this.http.post<OperationModeResponse>(this.endpoint, resource);
  }

  preview(modeId: number): Observable<OperationModePreviewResponse> {
    return this.http.get<OperationModePreviewResponse>(`${this.endpoint}/${modeId}/preview`);
  }

  activate(modeId: number): Observable<OperationModeActivationResponse> {
    return this.http.patch<OperationModeActivationResponse>(`${this.endpoint}/${modeId}/activate`, {});
  }

  archive(modeId: number): Observable<void> {
    return this.http.delete<void>(`${this.endpoint}/${modeId}`);
  }
}


============================================================
FILE: src\app\device-control\infrastructure\api\routines-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { RoutineResource } from '../resources/routine.resource';
import { RoutineResponse } from '../responses/routine.response';

@Injectable({
  providedIn: 'root',
})
export class RoutinesApiService {
  private readonly resourcePath: string;

  constructor(
    private readonly http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    this.resourcePath = `${apiBaseUrl}/routines`;
  }

  findAllForCurrentUser(): Observable<RoutineResponse[]> {
    return this.http.get<RoutineResponse[]>(this.resourcePath);
  }

  create(resource: RoutineResource): Observable<RoutineResponse> {
    return this.http.post<RoutineResponse>(this.resourcePath, resource);
  }

  updateStatus(payload: {
    routineId: number;
    enabled: boolean;
  }): Observable<RoutineResponse> {
    return this.http.patch<RoutineResponse>(
      `${this.resourcePath}/${payload.routineId}/status`,
      {
        enabled: payload.enabled,
      }
    );
  }

  execute(routineId: number): Observable<RoutineResponse> {
    return this.http.patch<RoutineResponse>(
      `${this.resourcePath}/${routineId}/execute`,
      {}
    );
  }

  delete(routineId: number): Observable<void> {
    return this.http.delete<void>(`${this.resourcePath}/${routineId}`);
  }
}


============================================================
FILE: src\app\device-control\infrastructure\assemblers\device.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { Device } from '../../domain/model/device.entity';
import { DeviceResource } from '../resources/device.resource';
import { DeviceResponse } from '../responses/device.response';

export class DeviceAssembler extends BaseAssembler<
  Device,
  DeviceResource,
  DeviceResponse
> {
  override toEntity(response: DeviceResponse): Device {
    return new Device({
      id: response.id,
      userId: response.userId,
      name: response.name,
      room: response.room,
      type: response.type,
      powerWatts: response.powerWatts,
      status: response.status,
      createdAt: response.createdAt,
    });
  }

  override toResource(entity: Device): DeviceResource {
    return {
      userId: entity.userId,
      name: entity.name,
      room: entity.room,
      type: entity.type,
      powerWatts: entity.powerWatts,
      status: entity.status,
      createdAt: entity.createdAt,
    };
  }
}

============================================================
FILE: src\app\device-control\infrastructure\assemblers\device-group.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { DeviceGroup } from '../../domain/model/device-group.entity';
import { DeviceGroupResource } from '../resources/device-group.resource';
import { DeviceGroupResponse } from '../responses/device-group.response';

export class DeviceGroupAssembler extends BaseAssembler<
  DeviceGroup,
  DeviceGroupResource,
  DeviceGroupResponse
> {
  override toEntity(response: DeviceGroupResponse): DeviceGroup {
    return new DeviceGroup({
      id: response.id,
      userId: response.userId,
      name: response.name,
      description: response.description ?? '',
      deviceIds: Array.isArray(response.deviceIds) ? response.deviceIds : [],
      createdAt:
        response.createdAt ?? new Date().toISOString().slice(0, 10),
    });
  }

  override toResource(entity: DeviceGroup): DeviceGroupResource {
    return {
      userId: entity.userId,
      name: entity.name,
      description: entity.description,
      deviceIds: entity.deviceIds,
      createdAt: entity.createdAt,
    };
  }
}

============================================================
FILE: src\app\device-control\infrastructure\assemblers\operation-mode.assembler.ts
============================================================

import {
  OperationMode,
  OperationModeActivation,
  OperationModePreview,
} from '../../domain/model/operation-mode.entity';
import {
  OperationModeActivationResponse,
  OperationModePreviewResponse,
  OperationModeResponse,
} from '../responses/operation-mode.response';

export class OperationModeAssembler {
  toEntity(response: OperationModeResponse): OperationMode {
    return new OperationMode({
      id: response.id,
      userId: response.userId,
      locationId: response.locationId,
      name: response.name,
      description: response.description,
      status: response.status,
      roomIds: response.roomIds ?? [],
      groupIds: response.groupIds ?? [],
      deviceIds: response.deviceIds ?? [],
      turnOnDeviceIds: response.turnOnDeviceIds ?? [],
      turnOffDeviceIds: response.turnOffDeviceIds ?? [],
      keepOnDeviceIds: response.keepOnDeviceIds ?? [],
      routineIds: response.routineIds ?? [],
      routinesToEnableIds: response.routinesToEnableIds ?? [],
      routinesToDisableIds: response.routinesToDisableIds ?? [],
      goalIds: response.goalIds ?? [],
      internalRoutines: response.internalRoutines ?? [],
      allDay: response.allDay ?? true,
      startsAt: response.startsAt ?? '00:00',
      endsAt: response.endsAt ?? '23:59',
      ruleProfileId: response.ruleProfileId ?? null,
      preferenceId: response.preferenceId ?? null,
      applyRuleProfile: response.applyRuleProfile ?? true,
      applyNotificationPreference: response.applyNotificationPreference ?? true,
      applyRoutines: response.applyRoutines ?? true,
      preserveCriticalSound: response.preserveCriticalSound ?? true,
      lastActivatedAt: response.lastActivatedAt ?? null,
    });
  }

  toPreview(response: OperationModePreviewResponse): OperationModePreview {
    return response;
  }

  toActivation(response: OperationModeActivationResponse): OperationModeActivation {
    return {
      ...response,
      mode: this.toEntity(response.mode),
    };
  }
}


============================================================
FILE: src\app\device-control\infrastructure\assemblers\routine.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { Routine } from '../../domain/model/routine.entity';
import { RoutineResource } from '../resources/routine.resource';
import { RoutineResponse } from '../responses/routine.response';

export class RoutineAssembler extends BaseAssembler<
  Routine,
  RoutineResource,
  RoutineResponse
> {
  override toEntity(response: RoutineResponse): Routine {
    return new Routine({
      id: response.id,
      userId: response.userId,
      deviceId: response.deviceId ?? null,
      groupId: response.groupId ?? null,
      targetType: response.targetType ?? (response.groupId ? 'GROUP' : 'DEVICE'),
      targetId: response.targetId ?? response.groupId ?? response.deviceId ?? 0,
      targetName: response.targetName ?? '',
      name: response.name,
      action: response.action,
      time: response.time,
      repeatType: response.repeatType ?? 'DAILY',
      daysOfWeek: response.daysOfWeek ?? '',
      intervalDays: response.intervalDays ?? 1,
      startsOn: response.startsOn ?? null,
      applicableDeviceCount: response.applicableDeviceCount ?? 0,
      blockedDeviceCount: response.blockedDeviceCount ?? 0,
      enabled: response.enabled,
    });
  }

  override toResource(entity: Routine): RoutineResource {
    return {
      userId: entity.userId,
      deviceId: entity.deviceId,
      groupId: entity.groupId,
      targetType: entity.targetType,
      targetId: entity.targetId,
      name: entity.name,
      action: entity.action,
      time: entity.time,
      repeatType: entity.repeatType,
      daysOfWeek: entity.daysOfWeek,
      intervalDays: entity.intervalDays,
      startsOn: entity.startsOn,
      enabled: entity.enabled,
    };
  }
}


============================================================
FILE: src\app\device-control\infrastructure\resources\device.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';
import {
  DeviceStatus,
  DeviceType,
} from '../../domain/model/device.entity';

export interface DeviceResource extends BaseResource {
  userId?: number;
  name: string;
  room?: string | null;
  type: DeviceType;
  powerWatts: number;
  status: DeviceStatus;
  createdAt: string;
}


============================================================
FILE: src\app\device-control\infrastructure\resources\device-group.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';

export interface DeviceGroupResource extends BaseResource {
  userId?: number;
  name: string;
  description: string;
  deviceIds: number[];
  createdAt: string;
}


============================================================
FILE: src\app\device-control\infrastructure\resources\operation-mode.resource.ts
============================================================

import { OperationModeRoutine } from '../../domain/model/operation-mode.entity';

export interface OperationModeResource {
  userId?: number;
  locationId: number;
  name: string;
  description: string;
  roomIds: number[];
  groupIds: number[];
  deviceIds: number[];
  turnOnDeviceIds: number[];
  turnOffDeviceIds: number[];
  keepOnDeviceIds: number[];
  routineIds: number[];
  routinesToEnableIds: number[];
  routinesToDisableIds: number[];
  goalIds: number[];
  internalRoutines: OperationModeRoutine[];
  allDay: boolean;
  startsAt: string;
  endsAt: string;
  ruleProfileId: number | null;
  preferenceId: number | null;
  applyRuleProfile: boolean;
  applyNotificationPreference: boolean;
  applyRoutines: boolean;
  preserveCriticalSound: boolean;
}


============================================================
FILE: src\app\device-control\infrastructure\resources\routine.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';
import {
  RoutineAction,
  RoutineRepeatType,
  RoutineTargetType,
} from '../../domain/model/routine.entity';

export interface RoutineResource extends BaseResource {
  userId?: number;
  deviceId?: number | null;
  groupId?: number | null;
  targetType: RoutineTargetType;
  targetId: number;
  name: string;
  action: RoutineAction;
  time: string;
  repeatType?: RoutineRepeatType;
  daysOfWeek?: string | null;
  intervalDays?: number | null;
  startsOn?: string | null;
  enabled?: boolean;
}


============================================================
FILE: src\app\device-control\infrastructure\responses\device.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';
import {
  DeviceStatus,
  DeviceType,
} from '../../domain/model/device.entity';

export interface DeviceResponse extends BaseResponse<number> {
  userId: number;
  name: string;
  room?: string | null;
  type: DeviceType;
  powerWatts: number;
  status: DeviceStatus;
  createdAt: string;
}


============================================================
FILE: src\app\device-control\infrastructure\responses\device-group.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';

export interface DeviceGroupResponse extends BaseResponse<number> {
  userId: number;
  name: string;
  description?: string | null;
  deviceIds?: number[] | null;
  createdAt?: string | null;
}

============================================================
FILE: src\app\device-control\infrastructure\responses\operation-mode.response.ts
============================================================

import { OperationModeRoutine, OperationModeStatus } from '../../domain/model/operation-mode.entity';

export interface OperationModeResponse {
  id: number;
  userId: number;
  locationId: number;
  name: string;
  description: string;
  status: OperationModeStatus;
  roomIds: number[];
  groupIds: number[];
  deviceIds: number[];
  turnOnDeviceIds: number[];
  turnOffDeviceIds: number[];
  keepOnDeviceIds: number[];
  routineIds: number[];
  routinesToEnableIds: number[];
  routinesToDisableIds: number[];
  goalIds: number[];
  internalRoutines: OperationModeRoutine[];
  allDay: boolean;
  startsAt: string;
  endsAt: string;
  ruleProfileId: number | null;
  preferenceId: number | null;
  applyRuleProfile: boolean;
  applyNotificationPreference: boolean;
  applyRoutines: boolean;
  preserveCriticalSound: boolean;
  lastActivatedAt: string | null;
}

export interface OperationModePreviewResponse {
  modeId: number;
  locationId: number;
  locationName: string;
  affectedDeviceIds: number[];
  ignoredRemovedDeviceIds: number[];
  ignoredMaintenanceDeviceIds: number[];
  roomIds: number[];
  groupIds: number[];
  routineIds: number[];
  internalRoutineCount?: number;
  goalIds: number[];
  ruleProfileId: number | null;
  preferenceId: number | null;
  evidence: string;
  explanation: string;
  recommendedAction: string;
}

export interface OperationModeActivationResponse {
  mode: OperationModeResponse;
  turnedOnDeviceIds: number[];
  turnedOffDeviceIds: number[];
  ignoredRemovedDeviceIds: number[];
  ignoredMaintenanceDeviceIds: number[];
  enabledRoutineIds: number[];
  disabledRoutineIds: number[];
  evidence: string;
  explanation: string;
  recommendedAction: string;
}


============================================================
FILE: src\app\device-control\infrastructure\responses\routine.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';
import {
  RoutineAction,
  RoutineRepeatType,
  RoutineTargetType,
} from '../../domain/model/routine.entity';

export interface RoutineResponse extends BaseResponse<number> {
  userId: number;
  deviceId?: number | null;
  groupId?: number | null;
  targetType?: RoutineTargetType | null;
  targetId?: number | null;
  targetName?: string | null;
  name: string;
  action: RoutineAction;
  time: string;
  repeatType?: RoutineRepeatType | null;
  daysOfWeek?: string | null;
  intervalDays?: number | null;
  startsOn?: string | null;
  applicableDeviceCount?: number | null;
  blockedDeviceCount?: number | null;
  enabled: boolean;
}


============================================================
FILE: src\app\device-control\presentation\components\device-card\device-card.component.html
============================================================

<article
  class="device-card"
  [class.on]="device.isOn"
  [class.removing]="removing"
  [class.powering-on]="powerTransition === 'powering-on'"
  [class.powering-off]="powerTransition === 'powering-off'"
>
  <div class="device-card__top">
    <span class="device-card__icon" aria-hidden="true">{{ deviceIcon }}</span>

    <div class="device-card__main">
      <p class="type">{{ deviceTypeLabelKey | translate }}</p>
      <h2 [title]="device.name">{{ device.name }}</h2>
      <small>
        {{ locationName || ('devices.activeSiteFallback' | translate) }} /
        {{ device.room || ('devices.noRoom' | translate) }}
      </small>
    </div>

    <span class="status">
      {{ statusLabelKey | translate }}
    </span>
  </div>

  <div class="device-card__power">
    <div>
      <span>{{ (device.isOn ? 'devices.card.activeConsumption' : 'devices.card.standby') | translate }}</span>
      <p>{{ (device.isOn ? 'devices.card.currentConsumptionDescription' : 'devices.card.noActiveConsumption') | translate }}</p>
    </div>

    <strong>{{ device.isOn ? device.powerWatts : 0 }} W</strong>
  </div>

  <div class="device-card__actions">
    <app-button
      variant="action"
      size="sm"
      (pressed)="onToggle()"
      [disabled]="removing || !device.canReceiveOperationalChanges"
    >
      @if (device.isOn) {
        {{ 'devices.turnOff' | translate }}
      } @else {
        {{ 'devices.turnOn' | translate }}
      }
    </app-button>

    <app-button variant="danger" size="sm" [disabled]="removing" (pressed)="onRemove()">
      {{ 'common.delete' | translate }}
    </app-button>
  </div>
</article>


============================================================
FILE: src\app\device-control\presentation\components\device-card\device-card.component.scss
============================================================

:host {
  display: block;
  overflow-anchor: none;
}

.device-card {
  position: relative;
  isolation: isolate;
  display: grid;
  grid-template-rows: auto minmax(86px, auto) auto;
  gap: 16px;
  min-height: 232px;
  padding: 20px;
  border: 1px solid var(--border-color);
  border-radius: 24px;
  background:
    var(--ec-card-highlight),
    var(--surface-card);
  box-shadow:
    var(--ec-shadow-soft),
    var(--ec-inset-highlight);
  box-sizing: border-box;
  contain: paint;
  overflow: hidden;
  overflow-anchor: none;
  transition:
    border-color 0.24s ease,
    box-shadow 0.24s ease,
    background 0.3s ease;
}

.device-card::before,
.device-card::after {
  content: '';
  position: absolute;
  pointer-events: none;
  z-index: -1;
}

.device-card::before {
  inset: 0;
  background:
    linear-gradient(90deg, rgba(255, 255, 255, 0.055), transparent 28%),
    linear-gradient(180deg, rgba(255, 255, 255, 0.045), transparent 38%);
  opacity: 0.72;
}

.device-card::after {
  inset: -42%;
  opacity: 0;
  transform: translateX(-36%) rotate(10deg);
  background:
    radial-gradient(circle at 50% 50%, rgba(125, 211, 252, 0.36), transparent 30%),
    linear-gradient(90deg, transparent, rgba(34, 197, 94, 0.32), transparent);
  filter: blur(2px);
}

.device-card:hover {
  border-color: var(--border-strong);
  box-shadow:
    var(--ec-shadow-panel),
    var(--ec-inset-highlight);
}

.device-card.on {
  border-color: var(--ec-status-success-border);
  background:
    radial-gradient(circle at 14% 8%, var(--ec-status-success-bg), transparent 36%),
    var(--ec-card-highlight),
    var(--surface-card);
  box-shadow:
    0 24px 58px var(--ec-status-success-bg),
    var(--ec-inset-highlight);
}

.device-card.powering-on::after {
  animation: energy-sweep-on 0.76s ease both;
}

.device-card.powering-off::after {
  background:
    radial-gradient(circle at 50% 50%, rgba(148, 163, 184, 0.32), transparent 30%),
    linear-gradient(90deg, transparent, rgba(59, 130, 246, 0.24), transparent);
  animation: energy-sweep-off 0.68s ease both;
}

.device-card.removing {
  pointer-events: none;
  animation: device-card-remove 0.52s cubic-bezier(0.55, 0, 0.1, 1) both;
}

.device-card__top {
  display: grid;
  grid-template-columns: 50px minmax(0, 1fr) auto;
  gap: 14px;
  align-items: start;
  min-width: 0;
}

.device-card__icon {
  display: grid;
  width: 50px;
  height: 50px;
  place-items: center;
  border-radius: 18px;
  background:
    linear-gradient(145deg, rgba(79, 195, 255, 0.16), rgba(99, 102, 241, 0.18));
  box-shadow:
    0 14px 28px rgba(3, 7, 18, 0.22),
    0 1px 0 rgba(255, 255, 255, 0.06) inset;
  font-size: 1.4rem;
  line-height: 1;
}

.device-card.on .device-card__icon {
  background:
    linear-gradient(145deg, rgba(34, 197, 94, 0.28), rgba(20, 184, 166, 0.2));
  box-shadow:
    0 16px 32px rgba(34, 197, 94, 0.2),
    0 1px 0 rgba(255, 255, 255, 0.08) inset;
}

.device-card.powering-on .device-card__icon {
  animation: icon-charge 0.56s ease both;
}

.device-card.powering-off .device-card__icon {
  animation: icon-discharge 0.5s ease both;
}

.device-card__main {
  min-width: 0;
}

.type {
  width: fit-content;
  margin: 0 0 8px;
  padding: 5px 9px;
  border-radius: 999px;
  color: var(--text-on-action);
  background: var(--ec-action-bg);
  font-size: 0.68rem;
  font-weight: 950;
}

h2 {
  display: -webkit-box;
  min-height: 2.34em;
  margin: 0;
  overflow: hidden;
  color: var(--text-primary);
  font-size: 1.25rem;
  line-height: 1.17;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
  text-overflow: ellipsis;
}

.device-card__main small {
  display: block;
  margin-top: 6px;
  color: var(--text-secondary);
  font-size: 0.76rem;
  font-weight: 750;
  line-height: 1.25;
}

.status {
  align-self: start;
  padding: 7px 11px;
  border-radius: 999px;
  color: var(--text-primary);
  background: var(--ec-surface-muted);
  box-shadow:
    0 10px 20px rgba(3, 7, 18, 0.24),
    0 1px 0 rgba(255, 255, 255, 0.05) inset;
  font-weight: 950;
  line-height: 1;
}

.device-card.on .status {
  color: var(--ec-status-success-text);
  background: var(--ec-status-success-bg);
  box-shadow:
    0 14px 28px rgba(34, 197, 94, 0.25),
    0 0 0 1px rgba(187, 247, 208, 0.12) inset;
}

.device-card.powering-on .status {
  animation: status-pop 0.42s ease both;
}

.device-card.powering-off .status {
  animation: status-dim 0.38s ease both;
}

.device-card__power {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  padding: 16px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background: var(--ec-surface-subtle);
  transition:
    border-color 0.24s ease,
    box-shadow 0.24s ease,
    background 0.24s ease;
}

.device-card.on .device-card__power {
  border-color: var(--ec-status-success-border);
  background:
    linear-gradient(135deg, var(--ec-status-success-bg), transparent);
}

.device-card.powering-on .device-card__power {
  animation: power-panel-on 0.58s ease both;
}

.device-card.powering-off .device-card__power {
  animation: power-panel-off 0.54s ease both;
}

.device-card__power span {
  display: block;
  color: var(--text-primary);
  font-size: 0.78rem;
  font-weight: 950;
  line-height: 1.2;
}

.device-card__power p {
  margin: 5px 0 0;
  color: var(--text-muted);
  font-size: 0.78rem;
  font-weight: 760;
  line-height: 1.35;
}

.device-card__power strong {
  color: var(--text-primary);
  font-size: 1.85rem;
  font-weight: 950;
  line-height: 1;
  white-space: nowrap;
}

.device-card.on .device-card__power strong {
  text-shadow: 0 0 24px rgba(34, 197, 94, 0.34);
}

.device-card__actions {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 10px;
  align-items: center;
}

app-button {
  justify-self: start;
}


============================================================
FILE: src\app\device-control\presentation\components\device-card\device-card.component.ts
============================================================

import {
  Component,
  EventEmitter,
  Input,
  OnChanges,
  OnDestroy,
  Output,
  SimpleChanges,
} from '@angular/core';
import { TranslateModule } from '@ngx-translate/core';

import { Device, DeviceType } from '../../../domain/model/device.entity';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';

type PowerTransition = 'powering-on' | 'powering-off' | null;

@Component({
  selector: 'app-device-card',
  standalone: true,
  imports: [TranslateModule, AppButtonComponent],
  templateUrl: './device-card.component.html',
  styleUrls: ['./device-card.component.scss'],
})
export class DeviceCardComponent implements OnChanges, OnDestroy {
  @Input({ required: true }) device!: Device;
  @Input() locationName = '';
  @Input() removing = false;

  @Output() toggle = new EventEmitter<Device>();
  @Output() remove = new EventEmitter<number>();

  powerTransition: PowerTransition = null;

  private transitionTimer: ReturnType<typeof setTimeout> | null = null;

  ngOnChanges(changes: SimpleChanges): void {
    const deviceChange = changes['device'];
    const previous = deviceChange?.previousValue as Device | undefined;
    const current = deviceChange?.currentValue as Device | undefined;

    if (
      !previous ||
      !current ||
      previous.id !== current.id ||
      previous.isOn === current.isOn
    ) {
      return;
    }

    this.playPowerTransition(current.isOn ? 'powering-on' : 'powering-off');
  }

  ngOnDestroy(): void {
    if (this.transitionTimer) {
      clearTimeout(this.transitionTimer);
    }
  }

  get deviceIcon(): string {
    const icons: Record<DeviceType, string> = {
      PLUG: '🔌',
      LIGHT: '💡',
      SWITCH: '🔘',
      SENSOR: '📡',
      OTHER: '⚡',
    };

    return icons[this.device.type] ?? '⚡';
  }

  get deviceTypeLabel(): string {
    return this.deviceTypeLabelKey;
  }

  get deviceTypeLabelKey(): string {
    const labels: Record<DeviceType, string> = {
      PLUG: 'workplace.deviceTypes.plug',
      LIGHT: 'workplace.deviceTypes.light',
      SWITCH: 'workplace.deviceTypes.switch',
      SENSOR: 'workplace.deviceTypes.sensor',
      OTHER: 'workplace.deviceTypes.other',
    };

    return labels[this.device.type];
  }

  get statusLabel(): string {
    return this.statusLabelKey;
  }

  get statusLabelKey(): string {
    if (this.device.isOn) {
      return 'devices.status.on';
    }

    if (this.device.isOff) {
      return 'devices.status.off';
    }

    if (this.device.isInMaintenance) {
      return 'devices.status.maintenance';
    }

    return 'devices.status.removed';
  }

  onToggle(): void {
    if (this.removing || !this.device.canReceiveOperationalChanges) {
      return;
    }

    this.toggle.emit(this.device);
  }

  onRemove(): void {
    if (this.removing) {
      return;
    }

    this.remove.emit(this.device.id);
  }

  private playPowerTransition(state: Exclude<PowerTransition, null>): void {
    if (this.transitionTimer) {
      clearTimeout(this.transitionTimer);
    }

    this.powerTransition = state;
    this.transitionTimer = setTimeout(() => {
      this.powerTransition = null;
      this.transitionTimer = null;
    }, 740);
  }
}


============================================================
FILE: src\app\device-control\presentation\components\device-group-card\device-group-card.component.html
============================================================

<article
  class="group-card"
  [class.on]="hasActiveDevices"
  [class.mixed]="hasActiveDevices && !allDevicesOn"
  [class.removing]="removing"
  [class.powering-on]="powerTransition === 'powering-on'"
  [class.powering-off]="powerTransition === 'powering-off'"
>
  <div class="group-card__top">
    <span class="group-card__icon" aria-hidden="true">🧩</span>

    <div class="group-card__identity">
      <p class="group-card__room">{{ roomName }}</p>
      <h3 [title]="group.name">{{ group.name }}</h3>
    </div>

    <span class="group-card__status">{{ groupStateLabelKey | translate }}</span>
  </div>

  <p
    class="group-card__description"
    [class.group-card__description--muted]="!group.description"
  >
    @if (group.description) {
      {{ group.description }}
    } @else {
      {{ 'deviceGroups.card.defaultDescription' | translate }}
    }
  </p>

  <section class="group-card__metrics">
    <article>
      <span>{{ 'nav.devices' | translate }}</span>
      <strong>{{ devices.length }}</strong>
    </article>

    <article>
      <span>{{ 'devices.active' | translate }}</span>
      <strong>{{ activeDeviceCount }}</strong>
    </article>

    <article>
      <span>{{ 'devices.currentWatts' | translate }}</span>
      <strong>{{ currentWatts }} W</strong>
    </article>
  </section>

  <section class="group-card__devices">
    <header>
      <span>{{ 'deviceGroups.card.assignedDevices' | translate }}</span>
      <strong>{{ devices.length }}</strong>
    </header>

    @if (devices.length === 0) {
      <p class="group-card__empty-devices">{{ 'deviceGroups.card.noAssignedDevices' | translate }}</p>
    } @else {
      <div class="group-card__device-list">
        @for (device of devices; track device.id) {
          <article class="group-card__device" [class.on]="device.isOn">
            <span class="group-card__device-icon">{{ getDeviceIcon(device.type) }}</span>

            <span class="group-card__device-main">
              <strong [title]="device.name">{{ device.name }}</strong>
              <small>{{ getDeviceTypeLabelKey(device.type) | translate }} - {{ device.powerWatts }} W</small>
            </span>

            <em>{{ getDeviceStatusLabelKey(device) | translate }}</em>
          </article>
        }
      </div>
    }
  </section>

  <div class="group-card__footer">
    <span class="group-card__created">{{ 'deviceGroups.card.createdAt' | translate }}: {{ formattedCreatedAt }}</span>

    <div class="group-card__actions">
      <app-button
        variant="action"
        size="sm"
        [disabled]="!canExecute"
        (pressed)="turnOn()"
      >
        {{ 'devices.turnOn' | translate }}
      </app-button>

      <app-button
        variant="neutral"
        size="sm"
        [disabled]="!canExecute"
        (pressed)="turnOff()"
      >
        {{ 'devices.turnOff' | translate }}
      </app-button>

      <app-button
        variant="danger"
        size="sm"
        [disabled]="removing"
        (pressed)="onRemove()"
      >
        {{ 'common.delete' | translate }}
      </app-button>
    </div>
  </div>
</article>


============================================================
FILE: src\app\device-control\presentation\components\device-group-card\device-group-card.component.scss
============================================================

:host {
  display: block;
  overflow-anchor: none;
}

.group-card {
  position: relative;
  isolation: isolate;
  display: grid;
  gap: 18px;
  padding: 22px;
  border: 1px solid var(--border-color);
  border-radius: 26px;
  background:
    var(--ec-card-highlight),
    var(--surface-card);
  box-shadow:
    var(--ec-shadow-soft),
    var(--ec-inset-highlight);
  contain: paint;
  overflow: hidden;
  transition:
    border-color 0.24s ease,
    box-shadow 0.24s ease,
    background 0.3s ease,
    opacity 0.24s ease,
    transform 0.24s ease;
}

.group-card::before,
.group-card::after {
  content: '';
  position: absolute;
  pointer-events: none;
  z-index: -1;
}

.group-card::before {
  inset: 0;
  background:
    linear-gradient(90deg, rgba(255, 255, 255, 0.055), transparent 28%),
    linear-gradient(180deg, rgba(255, 255, 255, 0.045), transparent 38%);
  opacity: 0.72;
}

.group-card::after {
  inset: -42%;
  opacity: 0;
  transform: translateX(-36%) rotate(10deg);
  background:
    radial-gradient(circle at 50% 50%, rgba(125, 211, 252, 0.36), transparent 30%),
    linear-gradient(90deg, transparent, rgba(34, 197, 94, 0.32), transparent);
  filter: blur(2px);
}

.group-card:hover {
  border-color: var(--border-strong);
  box-shadow:
    var(--ec-shadow-panel),
    var(--ec-inset-highlight);
}

.group-card.on {
  border-color: var(--ec-status-success-border);
  background:
    radial-gradient(circle at 14% 8%, var(--ec-status-success-bg), transparent 36%),
    var(--ec-card-highlight),
    var(--surface-card);
}

.group-card.mixed {
  border-color: var(--ec-status-warning-border);
  background:
    radial-gradient(circle at 14% 8%, var(--ec-status-warning-bg), transparent 36%),
    var(--ec-card-highlight),
    var(--surface-card);
}

.group-card.powering-on::after {
  animation: energy-sweep-on 0.76s ease both;
}

.group-card.powering-off::after {
  background:
    radial-gradient(circle at 50% 50%, rgba(148, 163, 184, 0.32), transparent 30%),
    linear-gradient(90deg, transparent, rgba(59, 130, 246, 0.24), transparent);
  animation: energy-sweep-off 0.68s ease both;
}

.group-card.removing {
  pointer-events: none;
  animation: group-card-remove 0.52s cubic-bezier(0.55, 0, 0.1, 1) both;
}

.group-card__top {
  display: grid;
  grid-template-columns: 50px minmax(0, 1fr) auto;
  gap: 14px;
  align-items: start;
  min-width: 0;
}

.group-card__icon {
  display: grid;
  width: 50px;
  height: 50px;
  place-items: center;
  border-radius: 18px;
  background:
    linear-gradient(145deg, rgba(79, 195, 255, 0.16), rgba(99, 102, 241, 0.18));
  box-shadow:
    0 14px 28px rgba(3, 7, 18, 0.22),
    0 1px 0 rgba(255, 255, 255, 0.06) inset;
  font-size: 1.28rem;
  line-height: 1;
}

.group-card.on .group-card__icon {
  background:
    linear-gradient(145deg, rgba(34, 197, 94, 0.28), rgba(20, 184, 166, 0.2));
  box-shadow:
    0 16px 32px rgba(34, 197, 94, 0.2),
    0 1px 0 rgba(255, 255, 255, 0.08) inset;
}

.group-card.powering-on .group-card__icon {
  animation: icon-charge 0.56s ease both;
}

.group-card.powering-off .group-card__icon {
  animation: icon-discharge 0.5s ease both;
}

.group-card__identity {
  min-width: 0;
}

.group-card__room {
  width: fit-content;
  margin: 0 0 8px;
  padding: 5px 9px;
  border-radius: 999px;
  color: var(--accent-color);
  background: var(--ec-accent-soft);
  font-size: 0.68rem;
  font-weight: 950;
  letter-spacing: 0.07em;
  text-transform: uppercase;
}

.group-card h3 {
  display: -webkit-box;
  min-height: 2.25em;
  margin: 0;
  overflow: hidden;
  color: var(--text-primary);
  font-size: 1.35rem;
  line-height: 1.12;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
  text-overflow: ellipsis;
}

.group-card__status {
  align-self: start;
  padding: 7px 11px;
  border-radius: 999px;
  color: var(--text-primary);
  background: var(--ec-surface-muted);
  box-shadow:
    0 10px 20px rgba(3, 7, 18, 0.24),
    0 1px 0 rgba(255, 255, 255, 0.05) inset;
  font-weight: 950;
  line-height: 1;
}

.group-card.on .group-card__status {
  color: var(--ec-status-success-text);
  background: var(--ec-status-success-bg);
}

.group-card.mixed .group-card__status {
  color: var(--ec-status-warning-text);
  background: var(--ec-status-warning-bg);
}

.group-card.powering-on .group-card__status {
  animation: status-pop 0.42s ease both;
}

.group-card.powering-off .group-card__status {
  animation: status-dim 0.38s ease both;
}

.group-card__description {
  min-height: 2.7em;
  margin: 0;
  color: var(--text-secondary);
  line-height: 1.45;
}

.group-card__description--muted {
  color: var(--text-muted);
}

.group-card__metrics {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 10px;
}

.group-card__metrics article {
  padding: 13px 14px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  background: var(--ec-surface-subtle);
}

.group-card__metrics span {
  display: block;
  color: var(--text-muted);
  font-size: 0.68rem;
  font-weight: 950;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.group-card__metrics strong {
  display: block;
  margin-top: 7px;
  color: var(--text-primary);
  font-size: 1.1rem;
  font-weight: 950;
  line-height: 1;
}

.group-card__devices {
  display: grid;
  gap: 12px;
  padding: 16px;
  border: 1px solid var(--border-color);
  border-radius: 20px;
  background: var(--ec-surface-subtle);
}

.group-card__devices header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}

.group-card__devices header span {
  color: var(--text-primary);
  font-size: 0.78rem;
  font-weight: 950;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.group-card__devices header strong {
  min-width: 30px;
  min-height: 30px;
  display: inline-grid;
  place-items: center;
  border-radius: 999px;
  color: var(--accent-color);
  background: var(--ec-accent-soft);
  font-size: 0.8rem;
  font-weight: 950;
}

.group-card__empty-devices {
  margin: 0;
  color: var(--text-muted);
  font-weight: 800;
}

.group-card__device-list {
  display: grid;
  gap: 9px;
}

.group-card__device {
  display: grid;
  grid-template-columns: 38px minmax(0, 1fr) auto;
  gap: 11px;
  align-items: center;
  padding: 10px;
  border: 1px solid var(--border-color);
  border-radius: 15px;
  background: var(--ec-surface-subtle);
}

.group-card__device.on {
  border-color: var(--ec-status-success-border);
  background: var(--ec-status-success-bg);
}

.group-card__device-icon {
  display: grid;
  width: 38px;
  height: 38px;
  place-items: center;
  border-radius: 14px;
  background: var(--ec-accent-soft);
  font-size: 1.08rem;
  line-height: 1;
}

.group-card__device-main {
  min-width: 0;
}

.group-card__device-main strong {
  display: block;
  overflow: hidden;
  color: var(--text-primary);
  font-size: 0.9rem;
  font-weight: 950;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.group-card__device-main small {
  display: block;
  margin-top: 3px;
  color: var(--text-muted);
  font-size: 0.76rem;
  font-weight: 780;
}

.group-card__device em {
  padding: 5px 8px;
  border-radius: 999px;
  color: var(--text-primary);
  background: var(--ec-surface-muted);
  font-size: 0.68rem;
  font-style: normal;
  font-weight: 950;
}

.group-card__device.on em {
  color: var(--ec-status-success-text);
  background: var(--ec-status-success-bg);
}

.group-card__footer {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 14px;
  align-items: center;
}

.group-card__created {
  width: fit-content;
  padding: 7px 10px;
  border-radius: 999px;
  color: var(--text-secondary);
  background: var(--ec-surface-subtle);
  font-size: 0.76rem;
  font-weight: 850;
}

.group-card__actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  flex-wrap: wrap;
}

.group-card__actions app-button {
  flex: 0 0 auto;
}

@media (max-width: 940px) {
  .group-card__footer {
    grid-template-columns: 1fr;
  }

  .group-card__actions {
    justify-content: flex-start;
  }
}

@media (max-width: 680px) {
  .group-card {
    padding: 18px;
  }

  .group-card__top,
  .group-card__footer {
    grid-template-columns: 1fr;
  }

  .group-card__metrics {
    grid-template-columns: 1fr;
  }

  .group-card__actions {
    flex-direction: column;
  }
}


============================================================
FILE: src\app\device-control\presentation\components\device-group-card\device-group-card.component.ts
============================================================

import { Component, EventEmitter, Input, OnDestroy, Output } from '@angular/core';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';
import { DeviceGroup } from '../../../domain/model/device-group.entity';
import { Device, DeviceStatus, DeviceType } from '../../../domain/model/device.entity';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';

type GroupPowerTransition = 'powering-on' | 'powering-off' | null;

const localeByLanguage: Record<string, string> = {
  es: 'es-PE',
  en: 'en-US',
  pt: 'pt-BR',
};

@Component({
  selector: 'app-device-group-card',
  standalone: true,
  imports: [TranslateModule, AppButtonComponent],
  templateUrl: './device-group-card.component.html',
  styleUrls: ['./device-group-card.component.scss'],
})
export class DeviceGroupCardComponent implements OnDestroy {
  @Input({ required: true }) group!: DeviceGroup;
  @Input() devices: Device[] = [];
  @Input() roomName = '';
  @Input() removing = false;

  @Output() execute = new EventEmitter<{
    groupId: number;
    status: DeviceStatus;
  }>();

  @Output() remove = new EventEmitter<number>();

  powerTransition: GroupPowerTransition = null;

  private transitionTimer: ReturnType<typeof setTimeout> | null = null;

  constructor(
    private readonly translate: TranslateService,
    private readonly uiPreferences: UiPreferencesService
  ) {}

  ngOnDestroy(): void {
    if (this.transitionTimer) {
      clearTimeout(this.transitionTimer);
    }
  }

  get canExecute(): boolean {
    return this.group.hasDevices && !this.removing;
  }

  get activeDeviceCount(): number {
    return this.devices.filter((device) => device.isOn).length;
  }

  get currentWatts(): number {
    return this.devices.reduce(
      (total, device) => total + (device.isOn ? device.powerWatts : 0),
      0
    );
  }

  get hasActiveDevices(): boolean {
    return this.activeDeviceCount > 0;
  }

  get allDevicesOn(): boolean {
    return this.devices.length > 0 && this.activeDeviceCount === this.devices.length;
  }

  get groupStateLabel(): string {
    return this.translate.instant(this.groupStateLabelKey);
  }

  get groupStateLabelKey(): string {
    if (this.allDevicesOn) {
      return 'deviceGroups.state.on';
    }

    if (this.hasActiveDevices) {
      return 'deviceGroups.state.mixed';
    }

    return 'deviceGroups.state.off';
  }

  get formattedCreatedAt(): string {
    if (!this.group.createdAt) {
      return this.translate.instant('deviceGroups.card.noDate');
    }

    const date = new Date(this.group.createdAt);

    if (Number.isNaN(date.getTime())) {
      return this.group.createdAt;
    }

    return date.toLocaleDateString(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
    });
  }

  getDeviceIcon(type: DeviceType): string {
    const icons: Record<DeviceType, string> = {
      PLUG: '🔌',
      LIGHT: '💡',
      SWITCH: '🔘',
      SENSOR: '📡',
      OTHER: '⚡',
    };

    return icons[type] ?? '⚡';
  }

  getDeviceTypeLabel(type: DeviceType): string {
    return this.translate.instant(this.getDeviceTypeLabelKey(type));
  }

  getDeviceTypeLabelKey(type: DeviceType): string {
    const labels: Record<DeviceType, string> = {
      PLUG: 'workplace.deviceTypes.plug',
      LIGHT: 'workplace.deviceTypes.light',
      SWITCH: 'workplace.deviceTypes.switch',
      SENSOR: 'workplace.deviceTypes.sensor',
      OTHER: 'workplace.deviceTypes.other',
    };

    return labels[type];
  }

  getDeviceStatusLabel(device: Device): string {
    return this.translate.instant(this.getDeviceStatusLabelKey(device));
  }

  getDeviceStatusLabelKey(device: Device): string {
    if (device.isOn) return 'devices.status.on';
    if (device.isOff) return 'devices.status.off';
    if (device.isInMaintenance) return 'devices.status.maintenance';
    return 'devices.status.removed';
  }

  turnOn(): void {
    if (!this.canExecute) {
      return;
    }

    this.playPowerTransition('powering-on');
    this.execute.emit({
      groupId: this.group.id,
      status: 'ON',
    });
  }

  turnOff(): void {
    if (!this.canExecute) {
      return;
    }

    this.playPowerTransition('powering-off');
    this.execute.emit({
      groupId: this.group.id,
      status: 'OFF',
    });
  }

  onRemove(): void {
    if (this.removing) {
      return;
    }

    this.remove.emit(this.group.id);
  }

  private playPowerTransition(state: Exclude<GroupPowerTransition, null>): void {
    if (this.transitionTimer) {
      clearTimeout(this.transitionTimer);
    }

    this.powerTransition = state;
    this.transitionTimer = setTimeout(() => {
      this.powerTransition = null;
      this.transitionTimer = null;
    }, 760);
  }

  private currentLocale(): string {
    return localeByLanguage[this.uiPreferences.currentLanguage()] ?? localeByLanguage['es'];
  }
}


============================================================
FILE: src\app\device-control\presentation\components\device-list\device-list.component.html
============================================================

<section class="device-room-list">
  @for (group of roomGroups; track group.roomName) {
    <article class="device-room-section">
      <header class="device-room-section__header">
        <div>
          <p>{{ 'devices.room' | translate }}</p>
          <h2>{{ group.roomName || ('devices.noRoom' | translate) }}</h2>
        </div>

        <dl class="device-room-section__stats">
          <div>
            <dt>{{ 'nav.devices' | translate }}</dt>
            <dd>{{ group.devices.length }}</dd>
          </div>

          <div>
            <dt>{{ 'devices.active' | translate }}</dt>
            <dd>{{ group.activeCount }}</dd>
          </div>

          <div>
            <dt>{{ 'devices.currentWatts' | translate }}</dt>
            <dd>{{ group.currentWatts }} W</dd>
          </div>
        </dl>
      </header>

      <div class="device-list">
        @for (device of group.devices; track device.id) {
          <div
            class="device-card-shell"
            [class.device-card-shell--removing]="isDeviceRemoving(device.id)"
          >
            <app-device-card
              [device]="device"
              [locationName]="locationName"
              [removing]="isDeviceRemoving(device.id)"
              (toggle)="toggle.emit($event)"
              (remove)="remove.emit($event)"
            />
          </div>
        }
      </div>
    </article>
  }
</section>


============================================================
FILE: src\app\device-control\presentation\components\device-list\device-list.component.scss
============================================================

.device-room-list {
  display: grid;
  gap: 26px;
  overflow-anchor: none;
}

.device-room-section {
  display: grid;
  gap: 22px;
  padding: 24px;
  border: 1px solid var(--border-color);
  border-radius: 30px;
  background:
    radial-gradient(circle at 8% 0%, var(--ec-accent-soft), transparent 36%),
    var(--surface-card);
  box-shadow:
    var(--ec-shadow-panel),
    var(--ec-inset-highlight);
  overflow-anchor: none;
}

.device-room-section__header {
  display: flex;
  align-items: end;
  justify-content: space-between;
  gap: 22px;
}

.device-room-section__header p {
  width: fit-content;
  margin: 0 0 8px;
  padding: 6px 10px;
  border-radius: 999px;
  color: var(--accent-color);
  background: var(--ec-accent-soft);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.device-room-section__header h2 {
  margin: 0;
  color: var(--text-primary);
  font-size: clamp(1.35rem, 2.2vw, 1.9rem);
  line-height: 1.05;
}

.device-room-section__stats {
  display: grid;
  grid-template-columns: repeat(3, minmax(92px, 1fr));
  gap: 10px;
  margin: 0;
}

.device-room-section__stats div {
  min-width: 0;
  padding: 12px 14px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  background: var(--ec-surface-subtle);
}

.device-room-section__stats dt {
  color: var(--text-muted);
  font-size: 0.68rem;
  font-weight: 950;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.device-room-section__stats dd {
  margin: 6px 0 0;
  color: var(--text-primary);
  font-size: 1rem;
  font-weight: 950;
  line-height: 1;
}

.device-list {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 18px;
  overflow-anchor: none;
}

.device-card-shell {
  max-height: 340px;
  opacity: 1;
  overflow: hidden;
  transform: translateX(0);
  animation: device-card-enter 0.42s cubic-bezier(0.22, 1, 0.36, 1) both;
  will-change: max-height;
  transition:
    max-height 0.34s cubic-bezier(0.22, 1, 0.36, 1),
    margin 0.34s cubic-bezier(0.22, 1, 0.36, 1);
}

.device-card-shell--removing {
  max-height: 0;
  margin-top: -18px;
  animation: none;
  transition-delay: 0.42s;
}

@keyframes device-card-enter {
  from {
    opacity: 0;
    transform: translateY(12px) scale(0.985);
  }

  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

@media (min-width: 1280px) {
  .device-list {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }
}

@media (max-width: 860px) {
  .device-room-section__header {
    align-items: stretch;
    flex-direction: column;
  }

  .device-room-section__stats {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }
}

@media (max-width: 720px) {
  .device-list,
  .device-room-section__stats {
    grid-template-columns: 1fr;
  }

  .device-room-section {
    padding: 20px;
    border-radius: 24px;
  }
}


============================================================
FILE: src\app\device-control\presentation\components\device-list\device-list.component.ts
============================================================

import { TranslateModule } from '@ngx-translate/core';
import { Component, EventEmitter, Input, Output } from '@angular/core';

import { Device } from '../../../domain/model/device.entity';
import { DeviceCardComponent } from '../device-card/device-card.component';

interface DeviceRoomGroup {
  roomName: string;
  devices: Device[];
  activeCount: number;
  currentWatts: number;
}

@Component({
  selector: 'app-device-list',
  standalone: true,
  imports: [
    TranslateModule,DeviceCardComponent],
  templateUrl: './device-list.component.html',
  styleUrls: ['./device-list.component.scss'],
})
export class DeviceListComponent {
  @Input() devices: Device[] = [];
  @Input() locationName = '';
  @Input() removingDeviceIds = new Set<number>();

  @Output() toggle = new EventEmitter<Device>();
  @Output() remove = new EventEmitter<number>();

  get roomGroups(): DeviceRoomGroup[] {
    const groups = new Map<string, Device[]>();

    for (const device of this.devices) {
      const roomName = device.room?.trim() || '';
      const roomDevices = groups.get(roomName) ?? [];
      roomDevices.push(device);
      groups.set(roomName, roomDevices);
    }

    return [...groups.entries()]
      .map(([roomName, devices]) => ({
        roomName,
        devices,
        activeCount: devices.filter((device) => device.isOn).length,
        currentWatts: devices.reduce(
          (total, device) => total + (device.isOn ? device.powerWatts : 0),
          0
        ),
      }))
      .sort((left, right) => left.roomName.localeCompare(right.roomName));
  }

  isDeviceRemoving(deviceId: number): boolean {
    return this.removingDeviceIds.has(deviceId);
  }
}


============================================================
FILE: src\app\device-control\presentation\components\routine-card\routine-card.component.html
============================================================

<article
  class="routine-card"
  [class.enabled]="routine.enabled"
  [class.paused]="!routine.enabled"
  [class.activating]="powerTransition === 'activating'"
  [class.deactivating]="powerTransition === 'deactivating'"
  [class.removing]="removing"
>
  <header class="routine-card__header">
    <div class="routine-card__identity">
      <span class="routine-card__icon" aria-hidden="true">&#9200;</span>

      <div class="routine-card__title-block">
        <div class="routine-card__title-row">
          <h3 [title]="routine.name">{{ routine.name }}</h3>

          <span class="routine-status" [class.enabled]="routine.enabled">
            {{ statusLabelKey | translate }}
          </span>
        </div>

        <p>
          {{ actionLabelKey | translate }} {{ targetTypeLabelKey | translate }}
        </p>
      </div>
    </div>

    <div class="routine-card__time">
      <span>{{ 'routines.card.time' | translate }}</span>
      <strong>{{ routine.time }}</strong>
    </div>
  </header>

  <section class="routine-card__details" [attr.aria-label]="'routines.card.detailsAria' | translate">
    <article>
      <span>{{ 'routines.form.target' | translate }}</span>
      <strong>{{ targetTypeLabelKey | translate }}: {{ targetName }}</strong>
    </article>

    <article>
      <span>{{ 'routines.form.repetition' | translate }}</span>
      <strong>{{ repeatDisplay }}</strong>
    </article>

    <article>
      <span>{{ 'routines.card.applicable' | translate }}</span>
      <strong>{{ routine.applicableDeviceCount }}</strong>
    </article>

    @if (routine.blockedDeviceCount > 0) {
      <article class="warning">
        <span>{{ 'routines.card.blocked' | translate }}</span>
        <strong>{{ routine.blockedDeviceCount }}</strong>
      </article>
    }
  </section>

  <footer class="routine-card__footer">
    <span class="routine-card__waiting">
      {{ waitingLabel }}
    </span>

    <div class="routine-card__actions">
      <app-button
        class="routine-action"
        variant="action"
        [disabled]="removing"
        (pressed)="onToggle()"
      >
        {{ toggleLabelKey | translate }}
      </app-button>

      <app-button
        class="routine-action"
        variant="danger"
        [disabled]="removing"
        (pressed)="onRemove()"
      >
        {{ 'common.delete' | translate }}
      </app-button>
    </div>
  </footer>
</article>


============================================================
FILE: src\app\device-control\presentation\components\routine-card\routine-card.component.scss
============================================================

:host {
  display: block;
  overflow-anchor: none;
}

.routine-card {
  position: relative;
  display: grid;
  gap: 18px;
  padding: 22px;
  border: 1px solid var(--border-color);
  border-radius: 28px;
  background:
    var(--ec-card-highlight),
    var(--surface-card);
  box-shadow:
    var(--ec-shadow-soft),
    var(--ec-inset-highlight);
  overflow: hidden;
  contain: paint;
  overflow-anchor: none;
  isolation: isolate;
  transition:
    border-color 0.24s ease,
    box-shadow 0.24s ease,
    background 0.3s ease,
    transform 0.22s ease,
    opacity 0.22s ease;
}

.routine-card::before,
.routine-card::after {
  content: '';
  position: absolute;
  z-index: -1;
  pointer-events: none;
}

.routine-card::before {
  inset: 0;
  opacity: 0.64;
  background:
    linear-gradient(90deg, rgba(255, 255, 255, 0.05), transparent 34%),
    radial-gradient(circle at 92% 12%, rgba(88, 101, 242, 0.14), transparent 28%);
}

.routine-card::after {
  inset: -42%;
  opacity: 0;
  transform: translateX(-36%) rotate(10deg);
  background:
    radial-gradient(circle at 50% 50%, rgba(125, 211, 252, 0.36), transparent 30%),
    linear-gradient(90deg, transparent, rgba(34, 197, 94, 0.32), transparent);
  filter: blur(2px);
}

.routine-card:hover {
  transform: translateY(-2px);
  border-color: var(--border-strong);
  box-shadow:
    var(--ec-shadow-panel),
    var(--ec-inset-highlight);
}

.routine-card.paused {
  border-color: var(--border-color);
  background:
    radial-gradient(circle at 0% 0%, var(--ec-surface-subtle), transparent 34%),
    var(--surface-card);
}

.routine-card.enabled {
  border-color: var(--ec-status-success-border);
  background:
    radial-gradient(circle at 0% 0%, var(--ec-status-success-bg), transparent 34%),
    var(--ec-card-highlight),
    var(--surface-card);
  box-shadow:
    0 22px 54px var(--ec-status-success-bg),
    var(--ec-inset-highlight);
}

.routine-card.activating::after {
  animation: routine-energy-sweep-on 0.76s ease both;
}

.routine-card.deactivating::after {
  background:
    radial-gradient(circle at 50% 50%, rgba(148, 163, 184, 0.32), transparent 30%),
    linear-gradient(90deg, transparent, rgba(59, 130, 246, 0.24), transparent);
  animation: routine-energy-sweep-off 0.68s ease both;
}

.routine-card.removing {
  pointer-events: none;
  animation: routine-card-remove 0.52s cubic-bezier(0.55, 0, 0.1, 1) both;
}

.routine-card__header,
.routine-card__footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  min-width: 0;
}

.routine-card__identity {
  display: grid;
  grid-template-columns: auto minmax(0, 1fr);
  gap: 14px;
  align-items: center;
  min-width: 0;
}

.routine-card__icon {
  width: 48px;
  height: 48px;
  display: grid;
  place-items: center;
  border-radius: 17px;
  color: var(--text-on-action);
  background:
    linear-gradient(145deg, rgba(16, 185, 129, 0.28), rgba(79, 195, 255, 0.14));
  box-shadow:
    0 16px 34px rgba(16, 185, 129, 0.12),
    0 1px 0 rgba(255, 255, 255, 0.08) inset;
  font-size: 1.32rem;
  font-weight: 950;
  letter-spacing: 0;
}

.routine-card__icon.off {
  background:
    linear-gradient(145deg, rgba(248, 113, 113, 0.28), rgba(88, 101, 242, 0.12));
  box-shadow:
    0 16px 34px rgba(248, 113, 113, 0.1),
    0 1px 0 rgba(255, 255, 255, 0.08) inset;
}

.routine-card.activating .routine-card__icon {
  animation: routine-icon-charge 0.56s ease both;
}

.routine-card.deactivating .routine-card__icon {
  animation: routine-icon-discharge 0.5s ease both;
}

.routine-card__title-block {
  min-width: 0;
  display: grid;
  gap: 7px;
}

.routine-card__title-row {
  display: flex;
  align-items: center;
  gap: 10px;
  min-width: 0;
}

.routine-card h3 {
  max-width: 340px;
  margin: 0;
  color: var(--text-primary);
  font-size: 1.28rem;
  line-height: 1.08;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.routine-card p {
  margin: 0;
  color: var(--text-secondary);
  font-size: 0.92rem;
  font-weight: 800;
}

.routine-status {
  flex: 0 0 auto;
  width: fit-content;
  padding: 6px 10px;
  border-radius: 999px;
  color: var(--text-secondary);
  background: var(--ec-surface-subtle);
  font-size: 0.72rem;
  font-weight: 950;
}

.routine-status.enabled {
  color: var(--accent-color);
  background: var(--ec-accent-soft);
  box-shadow: 0 0 22px rgba(79, 195, 255, 0.1);
}

.routine-card__time {
  flex: 0 0 auto;
  width: fit-content;
  min-width: 76px;
  display: grid;
  gap: 3px;
  justify-items: center;
  padding: 9px 11px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  background: var(--ec-surface-subtle);
}

.routine-card__time span,
.routine-card__details span {
  color: var(--text-muted);
  font-size: 0.68rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.routine-card__time strong {
  color: var(--text-primary);
  font-size: 1.08rem;
  line-height: 1;
}

.routine-card__details {
  display: grid;
  grid-template-columns: minmax(180px, 1.5fr) minmax(160px, 1.2fr) minmax(100px, 0.7fr) minmax(100px, 0.7fr);
  gap: 10px;
}

.routine-card__details article {
  min-width: 0;
  display: grid;
  gap: 7px;
  padding: 12px 13px;
  border: 1px solid var(--border-color);
  border-radius: 17px;
  background: var(--ec-surface-subtle);
}

.routine-card__details strong {
  min-width: 0;
  color: var(--text-primary);
  font-size: 0.9rem;
  line-height: 1.1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.routine-card__details .warning {
  border-color: var(--ec-status-warning-border);
  background: var(--ec-status-warning-bg);
}

.routine-card__details .warning span,
.routine-card__details .warning strong {
  color: var(--ec-status-warning-text);
}

.routine-card__waiting {
  min-width: 0;
  color: var(--text-muted);
  font-size: 0.82rem;
  font-weight: 900;
}

.routine-card__actions {
  flex: 0 0 auto;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 10px;
  flex-wrap: wrap;
}

.routine-action {
  min-width: 118px;
}

@media (max-width: 980px) {
  .routine-card__header,
  .routine-card__footer {
    align-items: stretch;
    flex-direction: column;
  }

  .routine-card__time {
    justify-items: start;
  }

  .routine-card__details {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .routine-card__actions {
    justify-content: flex-start;
  }
}

@media (max-width: 620px) {
  .routine-card {
    padding: 18px;
    border-radius: 24px;
  }

  .routine-card__details {
    grid-template-columns: 1fr;
  }

  .routine-card__title-row {
    align-items: flex-start;
    flex-direction: column;
  }

  .routine-card h3 {
    max-width: 100%;
  }

  .routine-card__actions,
  .routine-action {
    width: 100%;
  }
}


============================================================
FILE: src\app\device-control\presentation\components\routine-card\routine-card.component.ts
============================================================

import {
  Component,
  EventEmitter,
  Input,
  OnChanges,
  OnDestroy,
  Output,
  SimpleChanges,
} from '@angular/core';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { Routine, RoutineAction, RoutineTargetType } from '../../../domain/model/routine.entity';

type RoutinePowerTransition = 'activating' | 'deactivating' | null;

const localeByLanguage: Record<string, string> = {
  es: 'es-PE',
  en: 'en-US',
  pt: 'pt-BR',
};

@Component({
  selector: 'app-routine-card',
  standalone: true,
  imports: [TranslateModule, AppButtonComponent],
  templateUrl: './routine-card.component.html',
  styleUrls: ['./routine-card.component.scss'],
})
export class RoutineCardComponent implements OnChanges, OnDestroy {
  @Input({ required: true }) routine!: Routine;
  @Input() removing = false;

  @Output() toggle = new EventEmitter<Routine>();
  @Output() remove = new EventEmitter<number>();

  powerTransition: RoutinePowerTransition = null;

  private transitionTimer: ReturnType<typeof setTimeout> | null = null;

  constructor(
    private readonly translate: TranslateService,
    private readonly uiPreferences: UiPreferencesService
  ) {}

  ngOnChanges(changes: SimpleChanges): void {
    const routineChange = changes['routine'];
    const previous = routineChange?.previousValue as Routine | undefined;
    const current = routineChange?.currentValue as Routine | undefined;

    if (
      !previous ||
      !current ||
      previous.id !== current.id ||
      previous.enabled === current.enabled
    ) {
      return;
    }

    this.playPowerTransition(current.enabled ? 'activating' : 'deactivating');
  }

  ngOnDestroy(): void {
    if (this.transitionTimer) {
      clearTimeout(this.transitionTimer);
    }
  }

  get targetName(): string {
    return this.routine.targetName || `#${this.routine.targetId}`;
  }

  get repeatDisplay(): string {
    if (this.routine.repeatType === 'ONCE') {
      return this.routine.startsOn
        ? this.t('routines.card.onceWithDate', { date: this.formatDate(this.routine.startsOn) })
        : this.t('routines.repeat.once');
    }

    if (this.routine.repeatType === 'WEEKLY') {
      return this.formatWeekDays();
    }

    if (this.routine.repeatType === 'CUSTOM_INTERVAL') {
      return this.t('routines.card.everyDays', { count: this.routine.intervalDays });
    }

    return this.t('routines.repeat.daily');
  }

  get waitingLabel(): string {
    if (!this.routine.enabled) {
      return this.pausedLabel();
    }

    if (this.routine.repeatType === 'ONCE') {
      return this.routine.startsOn
        ? this.t('routines.card.waitingOnceDate', {
          date: this.formatDate(this.routine.startsOn),
          time: this.routine.time,
        })
        : this.t('routines.card.waitingOnce', { time: this.routine.time });
    }

    if (this.routine.repeatType === 'WEEKLY') {
      return this.t('routines.card.waitingWeekly', {
        days: this.weekDaysText(),
        time: this.routine.time,
      });
    }

    if (this.routine.repeatType === 'CUSTOM_INTERVAL') {
      return this.t('routines.card.waitingInterval', {
        count: this.routine.intervalDays,
        time: this.routine.time,
      });
    }

    return this.t('routines.card.waitingDaily', { time: this.routine.time });
  }

  get statusLabelKey(): string {
    return this.routine.enabled ? 'routines.status.active' : 'routines.status.paused';
  }

  get actionLabelKey(): string {
    return this.getActionLabelKey(this.routine.action);
  }

  get targetTypeLabelKey(): string {
    return this.getTargetTypeLabelKey(this.routine.targetType);
  }

  get toggleLabelKey(): string {
    return this.routine.enabled ? 'routines.disable' : 'routines.enable';
  }

  onToggle(): void {
    if (this.removing) {
      return;
    }

    this.toggle.emit(this.routine);
  }

  onRemove(): void {
    if (this.removing) {
      return;
    }

    this.remove.emit(this.routine.id);
  }

  private playPowerTransition(state: Exclude<RoutinePowerTransition, null>): void {
    if (this.transitionTimer) {
      clearTimeout(this.transitionTimer);
    }

    this.powerTransition = state;
    this.transitionTimer = setTimeout(() => {
      this.powerTransition = null;
      this.transitionTimer = null;
    }, 760);
  }

  private pausedLabel(): string {
    if (this.routine.repeatType === 'ONCE' && this.routine.startsOn) {
      return this.t('routines.card.pausedOnceDate', {
        date: this.formatDate(this.routine.startsOn),
      });
    }

    if (this.routine.repeatType === 'WEEKLY') {
      return this.t('routines.card.pausedWeekly');
    }

    if (this.routine.repeatType === 'CUSTOM_INTERVAL') {
      return this.t('routines.card.pausedInterval');
    }

    return this.t('routines.card.pausedDaily');
  }

  private formatWeekDays(): string {
    if (!this.routine.daysOfWeek) {
      return this.t('routines.repeat.weekly');
    }

    const labels: Record<string, string> = {
      MON: this.t('routines.weekDays.monShort'),
      TUE: this.t('routines.weekDays.tueShort'),
      WED: this.t('routines.weekDays.wedShort'),
      THU: this.t('routines.weekDays.thuShort'),
      FRI: this.t('routines.weekDays.friShort'),
      SAT: this.t('routines.weekDays.satShort'),
      SUN: this.t('routines.weekDays.sunShort'),
    };

    const days = this.routine.daysOfWeek
      .split(',')
      .map((day) => labels[day.trim()] ?? day.trim())
      .filter(Boolean);

    return days.length > 0
      ? this.t('routines.card.weeklyWithDays', { days: days.join(', ') })
      : this.t('routines.repeat.weekly');
  }

  private weekDaysText(): string {
    if (!this.routine.daysOfWeek) {
      return this.t('routines.card.weeklyGeneric');
    }

    const labels: Record<string, string> = {
      MON: this.t('routines.weekDays.monShort'),
      TUE: this.t('routines.weekDays.tueShort'),
      WED: this.t('routines.weekDays.wedShort'),
      THU: this.t('routines.weekDays.thuShort'),
      FRI: this.t('routines.weekDays.friShort'),
      SAT: this.t('routines.weekDays.satShort'),
      SUN: this.t('routines.weekDays.sunShort'),
    };

    const days = this.routine.daysOfWeek
      .split(',')
      .map((day) => labels[day.trim()] ?? day.trim())
      .filter(Boolean);

    return days.length > 0
      ? this.t('routines.card.weeklyDaysText', { days: days.join(', ') })
      : this.t('routines.card.weeklyGeneric');
  }

  private formatDate(value: string): string {
    const [year, month, day] = value.split('-');

    if (!year || !month || !day) {
      return value;
    }

    const date = new Date(`${year}-${month}-${day}T00:00:00`);

    if (Number.isNaN(date.getTime())) {
      return value;
    }

    return date.toLocaleDateString(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
    });
  }

  private getActionLabelKey(action: RoutineAction): string {
    return action === 'TURN_ON'
      ? 'routines.actions.turnOn'
      : 'routines.actions.turnOff';
  }

  private getTargetTypeLabelKey(targetType: RoutineTargetType): string {
    const labels: Record<RoutineTargetType, string> = {
      DEVICE: 'routines.targetTypes.device',
      GROUP: 'routines.targetTypes.group',
      ROOM: 'routines.targetTypes.room',
      WORKPLACE: 'routines.targetTypes.workplace',
    };

    return labels[targetType];
  }

  private currentLocale(): string {
    return localeByLanguage[this.uiPreferences.currentLanguage()] ?? localeByLanguage['es'];
  }

  private t(key: string, params?: Record<string, unknown>): string {
    return this.translate.instant(key, params);
  }
}


============================================================
FILE: src\app\device-control\presentation\components\routine-list\routine-list.component.html
============================================================

@if (routines.length === 0) {
  <div class="empty-routines">
    {{ 'routines.list.empty' | translate }}
  </div>
} @else {
  <section class="routine-list">
    @for (routine of routines; track routine.id) {
      <article class="routine-item">
        <div class="routine-item__icon">
          {{ getTargetIcon(routine) }}
        </div>

        <div class="routine-item__content">
          <div class="routine-item__top">
            <h3>{{ routine.name }}</h3>

            <span class="routine-status" [class.enabled]="routine.enabled">
              {{ getStatusLabelKey(routine) | translate }}
            </span>
          </div>

          <p>
            {{ getActionLabelKey(routine) | translate }}
            <strong>{{ getTargetTypeLabelKey(routine) | translate }}</strong>
          </p>

          <div class="routine-item__meta">
            <span>
              {{ getTargetTypeLabelKey(routine) | translate }}:
              {{ getTargetName(routine) }}
            </span>

            <span>
              {{ 'routines.card.time' | translate }}: {{ routine.time }}
            </span>
          </div>
        </div>

        <div class="routine-item__actions">
          <app-button
            class="routine-action"
            variant="action"
            (pressed)="onToggle(routine)"
          >
            {{ getToggleLabelKey(routine) | translate }}
          </app-button>

          <app-button
            class="routine-action"
            variant="danger"
            (pressed)="onRemove(routine.id)"
          >
            {{ 'common.delete' | translate }}
          </app-button>
        </div>
      </article>
    }
  </section>
}


============================================================
FILE: src\app\device-control\presentation\components\routine-list\routine-list.component.scss
============================================================

:host {
  display: block;
}

.routine-list {
  display: grid;
  gap: 20px;
}

.routine-item {
  display: grid;
  grid-template-columns: auto 1fr auto;
  gap: 18px;
  align-items: center;
  padding: 24px;
  border: 1px solid var(--border-color);
  border-radius: 26px;
  background: var(--surface-card);
  box-shadow: var(--ec-shadow-soft);
}

.routine-item__icon {
  width: 46px;
  height: 46px;
  display: grid;
  place-items: center;
  border-radius: 16px;
  background: var(--ec-accent-soft);
  color: var(--accent-color);
  font-weight: 950;
}

.routine-item__content {
  display: grid;
  gap: 10px;
  min-width: 0;
}

.routine-item__top {
  display: flex;
  align-items: center;
  gap: 12px;
  flex-wrap: wrap;
}

.routine-item h3 {
  margin: 0;
  color: var(--text-primary);
  font-size: 1.35rem;
  line-height: 1.1;
}

.routine-item p {
  margin: 0;
  color: var(--text-secondary);
}

.routine-item strong {
  color: var(--text-primary);
}

.routine-status {
  width: fit-content;
  padding: 7px 11px;
  border-radius: 999px;
  background: var(--ec-status-danger-bg);
  color: var(--ec-status-danger-text);
  font-size: 0.76rem;
  font-weight: 900;
}

.routine-status.enabled {
  background: var(--ec-accent-soft);
  color: var(--accent-color);
}

.routine-item__meta {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
}

.routine-item__meta span {
  width: fit-content;
  padding: 7px 10px;
  border-radius: 999px;
  background: var(--ec-surface-subtle);
  color: var(--text-secondary);
  font-size: 0.76rem;
  font-weight: 800;
}

.routine-item__actions {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
  justify-content: flex-end;
}

.routine-action {
  min-width: 118px;
}

.empty-routines {
  padding: 20px;
  border: 1px dashed var(--border-color);
  border-radius: 20px;
  background: var(--ec-surface-subtle);
  color: var(--text-secondary);
  font-weight: 800;
}

@media (max-width: 860px) {
  .routine-item {
    grid-template-columns: auto 1fr;
  }

  .routine-item__actions {
    grid-column: 1 / -1;
    justify-content: flex-start;
  }
}

@media (max-width: 640px) {
  .routine-item {
    grid-template-columns: 1fr;
  }

  .routine-item__actions {
    flex-direction: column;
    align-items: stretch;
  }

  .routine-action {
    width: 100%;
  }
}


============================================================
FILE: src\app\device-control\presentation\components\routine-list\routine-list.component.ts
============================================================

import { Component, EventEmitter, Input, Output } from '@angular/core';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { DeviceGroup } from '../../../domain/model/device-group.entity';
import { Device } from '../../../domain/model/device.entity';
import { Routine, RoutineAction, RoutineTargetType } from '../../../domain/model/routine.entity';

@Component({
  selector: 'app-routine-list',
  standalone: true,
  imports: [TranslateModule, AppButtonComponent],
  templateUrl: './routine-list.component.html',
  styleUrls: ['./routine-list.component.scss'],
})
export class RoutineListComponent {
  @Input({ required: true }) routines: Routine[] = [];
  @Input() devices: Device[] = [];
  @Input() deviceGroups: DeviceGroup[] = [];

  @Output() toggle = new EventEmitter<Routine>();
  @Output() remove = new EventEmitter<number>();

  constructor(private readonly translate: TranslateService) {}

  getTargetName(routine: Routine): string {
    if (routine.targetName) {
      return routine.targetName;
    }

    if (routine.targetType === 'GROUP') {
      const group = this.deviceGroups.find((item) => item.id === routine.groupId);

      return group?.name ?? this.t('routines.list.groupFallback', {
        id: routine.groupId ?? routine.targetId,
      });
    }

    const device = this.devices.find((item) => item.id === routine.deviceId);

    return device?.name ?? this.t('routines.list.deviceFallback', {
      id: routine.deviceId ?? routine.targetId,
    });
  }

  getTargetTypeLabelKey(routine: Routine): string {
    return this.getTargetTypeKey(routine.targetType);
  }

  getStatusLabelKey(routine: Routine): string {
    return routine.enabled ? 'routines.status.active' : 'routines.status.inactive';
  }

  getActionLabelKey(routine: Routine): string {
    return this.getActionKey(routine.action);
  }

  getToggleLabelKey(routine: Routine): string {
    return routine.enabled ? 'routines.disable' : 'routines.enable';
  }

  getTargetTypeLabel(routine: Routine): string {
    return this.t(this.getTargetTypeKey(routine.targetType));
  }

  getActionLabel(routine: Routine): string {
    return this.t(this.getActionKey(routine.action));
  }

  private getTargetTypeKey(targetType: RoutineTargetType): string {
    const labels: Record<RoutineTargetType, string> = {
      DEVICE: 'routines.targetTypes.device',
      GROUP: 'routines.targetTypes.group',
      ROOM: 'routines.targetTypes.room',
      WORKPLACE: 'routines.targetTypes.workplace',
    };

    return labels[targetType] ?? 'routines.targetTypes.device';
  }

  getTargetIcon(routine: Routine): string {
    const icons: Record<RoutineTargetType, string> = {
      DEVICE: 'D',
      GROUP: 'G',
      ROOM: 'H',
      WORKPLACE: 'S',
    };

    return icons[routine.targetType] ?? 'D';
  }

  private getActionKey(action: RoutineAction): string {
    return action === 'TURN_ON'
      ? 'routines.actions.turnOn'
      : 'routines.actions.turnOff';
  }

  onToggle(routine: Routine): void {
    this.toggle.emit(routine);
  }

  onRemove(routineId: number): void {
    this.remove.emit(routineId);
  }

  private t(key: string, params?: Record<string, unknown>): string {
    return this.translate.instant(key, params);
  }
}


============================================================
FILE: src\app\device-control\presentation\pages\device-groups\device-groups-page.component.html
============================================================

<section class="device-groups-page ec-page">
  <header class="ec-page-heading">
    <p class="ec-page-eyebrow">{{ 'nav.operation' | translate }}</p>
    <h1 class="ec-page-title">{{ 'nav.deviceGroups' | translate }}</h1>
    <p class="ec-page-description">
      {{ 'deviceGroups.pageDescription' | translate }}
    </p>
  </header>

  <section class="operation-summary">
    <app-section-card
      size="small"
      [eyebrow]="'nav.deviceGroups' | translate"
      [title]="'deviceGroups.summary.createdTitle' | translate"
    >
      <strong>{{ activeGroups.length }}</strong>
      <p>{{ activeLocationName }}</p>
    </app-section-card>

    <app-section-card
      size="small"
      [eyebrow]="'nav.devices' | translate"
      [title]="'deviceGroups.summary.availableTitle' | translate"
    >
      <strong>{{ assignedDeviceCount }}</strong>
      <p>{{ 'deviceGroups.summary.multiGroupDescription' | translate }}</p>
    </app-section-card>

    <app-section-card
      size="small"
      [eyebrow]="'deviceGroups.summary.selectionEyebrow' | translate"
      [title]="'devices.room' | translate"
    >
      <strong>{{ selectedRoomName }}</strong>
      <p>{{ 'deviceGroups.summary.selectedDevices' | translate: { count: selectedCount } }}</p>
    </app-section-card>
  </section>

  <section class="operation-toolbar">
    <div>
      <p class="section-eyebrow">{{ 'deviceGroups.toolbar.eyebrow' | translate }}</p>
      <h2>{{ 'deviceGroups.toolbar.title' | translate }}</h2>
      <p>{{ 'deviceGroups.toolbar.description' | translate }}</p>
    </div>

    <app-button
      variant="create"
      [disabled]="sortedDevices.length === 0"
      (pressed)="openCreateModal()"
    >
      {{ 'deviceGroups.create' | translate }}
    </app-button>
  </section>

  @if (showInitialLoading) {
    <div class="loading-wrapper">
      <app-loading-spinner />
    </div>
  } @else if (sortedDevices.length === 0) {
    <app-empty-state
      [eyebrow]="'nav.devices' | translate"
      [title]="'deviceGroups.emptyDevices.title' | translate"
      [description]="'deviceGroups.emptyDevices.description' | translate"
      [actionLabel]="'devices.go' | translate"
      actionLink="/spaces/assignments"
      actionVariant="neutral"
    />
  } @else if (groupRoomSections.length === 0) {
    <app-empty-state
      [eyebrow]="'nav.deviceGroups' | translate"
      [title]="'deviceGroups.emptyGroups.title' | translate"
      [description]="'deviceGroups.emptyGroups.description' | translate"
    />
  } @else {
    <section class="group-room-list">
      @for (section of groupRoomSections; track section.roomName) {
        <app-section-card class="group-room-section" size="large">
          <header class="group-room-section__header">
            <div>
              <p>{{ 'devices.room' | translate }}</p>
              <h3>{{ displayRoomName(section.roomName) }}</h3>
            </div>

            <dl class="group-room-section__stats">
              <div>
                <dt>{{ 'nav.deviceGroups' | translate }}</dt>
                <dd>{{ section.groupCount }}</dd>
              </div>

              <div>
                <dt>{{ 'nav.devices' | translate }}</dt>
                <dd>{{ section.deviceCount }}</dd>
              </div>

              <div>
                <dt>{{ 'devices.active' | translate }}</dt>
                <dd>{{ section.activeDeviceCount }}</dd>
              </div>

              <div>
                <dt>{{ 'devices.currentWatts' | translate }}</dt>
                <dd>{{ section.currentWatts }} W</dd>
              </div>
            </dl>
          </header>

          <div class="groups-list">
            @for (item of section.groups; track item.group.id) {
              <div
                class="group-card-shell"
                [class.group-card-shell--removing]="isGroupRemoving(item.group.id)"
              >
                <app-device-group-card
                  [group]="item.group"
                  [roomName]="displayRoomName(item.roomName)"
                  [devices]="item.devices"
                  [removing]="isGroupRemoving(item.group.id)"
                  (execute)="executeGroupAction($event)"
                  (remove)="deleteGroup($event)"
                />
              </div>
            }
          </div>
        </app-section-card>
      }
    </section>
  }

  <app-modal-form-shell
    [open]="createModalOpen"
    [eyebrow]="'deviceGroups.form.eyebrow' | translate"
    [title]="'deviceGroups.create' | translate"
    [description]="'deviceGroups.form.description' | translate"
    (closed)="closeCreateModal()"
  >
    <form class="group-form" (ngSubmit)="createGroup()">
      <div class="fields-row">
        <label class="field">
          <span>{{ 'devices.name' | translate }}</span>
          <input
            type="text"
            name="name"
            [(ngModel)]="name"
            [placeholder]="'deviceGroups.form.namePlaceholder' | translate"
            required
          />
        </label>

        <label class="field">
          <span>{{ 'deviceGroups.description' | translate }}</span>
          <input
            type="text"
            name="description"
            [(ngModel)]="description"
            [placeholder]="'deviceGroups.form.descriptionPlaceholder' | translate"
          />
        </label>
      </div>

      <section class="picker-card">
        <div class="picker-card__header">
          <div>
            <h3>{{ 'nav.devices' | translate }}</h3>
            <p>{{ 'deviceGroups.form.pickerDescription' | translate: { location: activeLocationName } }}</p>
          </div>
          <span>{{ 'deviceGroups.form.selectedCount' | translate: { count: selectedCount } }}</span>
        </div>

        <div class="picker-room-list">
          @for (section of selectableDeviceRoomSections; track section.roomName) {
            <article class="picker-room-section">
              <header class="picker-room-section__header">
                <div>
                  <p>{{ 'devices.room' | translate }}</p>
                  <h4>{{ displayRoomName(section.roomName) }}</h4>
                </div>

                <div class="picker-room-section__stats">
                  <span>{{ 'deviceGroups.form.availableCount' | translate: { count: section.availableCount } }}</span>
                  <span>{{ 'deviceGroups.form.selectedCount' | translate: { count: section.selectedCount } }}</span>
                </div>
              </header>

              <div class="checkbox-grid">
                @for (device of section.devices; track device.id) {
                  <label
                    class="device-option"
                    [class.selected]="isDeviceSelected(device.id)"
                  >
                    <input
                      class="device-option__native-input"
                      type="checkbox"
                      [checked]="isDeviceSelected(device.id)"
                      (change)="toggleDeviceSelection(device.id, $any($event.target).checked)"
                    />

                    <span class="device-option__check">
                      <span class="device-option__check-icon">{{ 'common.ok' | translate }}</span>
                    </span>

                    <span class="device-option__icon">
                      {{ getDeviceIcon(device.type) }}
                    </span>

                    <span class="device-option__content">
                      <strong>{{ device.name }}</strong>
                      <small>{{ displayRoomName(section.roomName) }} - {{ device.powerWatts }} W</small>
                      @if (getGroupMembershipLabel(device.id); as membershipLabel) {
                        <em>{{ 'deviceGroups.form.alsoBelongsTo' | translate }}: {{ membershipLabel }}</em>
                      }
                    </span>

                    <span class="device-option__tag">
                      {{ getDeviceSelectionLabel(device.id) }}
                    </span>
                  </label>
                }
              </div>
            </article>
          }
        </div>
      </section>

      <div class="form-actions">
        <app-button variant="neutral" (pressed)="closeCreateModal()">{{ 'common.cancel' | translate }}</app-button>
        <app-button type="submit" variant="create" [disabled]="groupSubmitDisabled">
          {{ 'deviceGroups.create' | translate }}
        </app-button>
      </div>
    </form>
  </app-modal-form-shell>
</section>


============================================================
FILE: src\app\device-control\presentation\pages\device-groups\device-groups-page.component.scss
============================================================

.device-groups-page {
  display: grid;
  gap: 24px;
}

.operation-summary {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;

  strong {
    display: block;
    margin-bottom: 6px;
    color: var(--text-color);
    font-size: clamp(1.35rem, 2.6vw, 2.1rem);
    line-height: 1;
  }

  p {
    margin: 0;
    color: var(--text-secondary);
    font-weight: 700;
  }
}

.operation-toolbar {
  display: flex;
  justify-content: space-between;
  gap: 18px;
  align-items: center;
  border: 1px solid var(--border-color);
  border-radius: 24px;
  padding: 22px;
  background: var(--surface-color);

  h2,
  p {
    margin: 0;
  }

  h2 {
    color: var(--text-color);
    font-size: 1.3rem;
  }

  p:not(.section-eyebrow) {
    margin-top: 6px;
    color: var(--text-secondary);
    font-weight: 700;
  }
}

.section-eyebrow {
  margin: 0 0 6px;
  color: var(--accent-color);
  font-size: 0.74rem;
  font-weight: 900;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.loading-wrapper {
  min-height: 220px;
  display: grid;
  place-items: center;
}

.group-room-list,
.groups-list,
.group-form,
.picker-room-list {
  display: grid;
  gap: 18px;
}

.group-room-section {
  display: block;

  ::ng-deep .section-card {
    display: grid;
    gap: 20px;
  }
}

.group-room-section__header,
.picker-room-section__header,
.picker-card__header {
  display: flex;
  justify-content: space-between;
  gap: 18px;
  align-items: flex-start;
}

.group-room-section__header p,
.picker-room-section__header p {
  width: fit-content;
  margin: 0 0 7px;
  border-radius: 999px;
  padding: 5px 9px;
  color: var(--accent-color);
  background: color-mix(in srgb, var(--accent-color) 12%, transparent);
  font-size: 0.7rem;
  font-weight: 900;
  text-transform: uppercase;
}

.group-room-section__header h3,
.picker-room-section__header h4,
.picker-card__header h3 {
  margin: 0;
  color: var(--text-color);
}

.group-room-section__stats {
  display: grid;
  grid-template-columns: repeat(4, minmax(92px, 1fr));
  gap: 10px;
  margin: 0;

  div {
    border: 1px solid var(--border-color);
    border-radius: 16px;
    padding: 12px 14px;
    background: color-mix(in srgb, var(--surface-color) 88%, var(--app-bg));
  }

  dt {
    color: var(--text-secondary);
    font-size: 0.68rem;
    font-weight: 900;
    text-transform: uppercase;
  }

  dd {
    margin: 6px 0 0;
    color: var(--text-color);
    font-weight: 900;
  }
}

.group-card-shell {
  max-height: 820px;
  opacity: 1;
  overflow: hidden;
  transition:
    max-height 0.34s cubic-bezier(0.22, 1, 0.36, 1),
    margin 0.34s cubic-bezier(0.22, 1, 0.36, 1);
}

.group-card-shell--removing {
  max-height: 0;
  margin-top: -18px;
  transition-delay: 0.42s;
}

.fields-row {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14px;
}

.field {
  display: grid;
  gap: 8px;

  span {
    color: var(--text-secondary);
    font-size: 0.8rem;
    font-weight: 900;
  }
}

input {
  width: 100%;
  min-height: 48px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  padding: 13px 14px;
  color: var(--text-primary);
  background: color-mix(in srgb, var(--surface-color) 84%, var(--app-bg));
  box-sizing: border-box;
  font: inherit;
  outline: none;
}

.picker-card {
  display: grid;
  gap: 16px;
  border: 1px solid var(--border-color);
  border-radius: 22px;
  padding: 18px;
  background: color-mix(in srgb, var(--surface-color) 92%, var(--app-bg));
}

.picker-card__header p {
  margin: 6px 0 0;
  color: var(--text-secondary);
  font-weight: 700;
}

.picker-card__header > span,
.picker-room-section__stats span {
  min-height: 32px;
  display: inline-grid;
  place-items: center;
  border-radius: 999px;
  padding: 0 11px;
  color: var(--accent-color);
  background: color-mix(in srgb, var(--accent-color) 12%, transparent);
  font-size: 0.76rem;
  font-weight: 900;
}

.picker-room-section {
  display: grid;
  gap: 14px;
  border: 1px solid var(--border-color);
  border-radius: 20px;
  padding: 16px;
}

.picker-room-section__stats {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  justify-content: flex-end;
}

.checkbox-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12px;
}

.device-option {
  position: relative;
  min-height: 112px;
  display: grid;
  grid-template-columns: 28px 42px minmax(0, 1fr);
  align-items: center;
  gap: 12px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  padding: 14px;
  background: color-mix(in srgb, var(--surface-color) 88%, var(--app-bg));
  cursor: pointer;
}

.device-option.selected {
  border-color: color-mix(in srgb, var(--accent-color) 55%, var(--border-color));
  background: color-mix(in srgb, var(--accent-color) 10%, var(--surface-color));
}

.device-option__native-input {
  position: absolute;
  width: 0;
  height: 0;
  opacity: 0;
  pointer-events: none;
}

.device-option__check,
.device-option__icon {
  display: grid;
  place-items: center;
  border-radius: 12px;
}

.device-option__check {
  width: 28px;
  height: 28px;
  border: 1px solid var(--border-color);
}

.device-option__check-icon {
  color: var(--accent-color);
  font-size: 0.68rem;
  font-weight: 900;
  opacity: 0;
}

.device-option.selected .device-option__check-icon {
  opacity: 1;
}

.device-option__icon {
  width: 42px;
  height: 42px;
  color: var(--accent-color);
  background: color-mix(in srgb, var(--accent-color) 12%, transparent);
  font-size: 1.2rem;
  font-weight: 900;
  line-height: 1;
}

.device-option__content {
  display: grid;
  gap: 5px;
  min-width: 0;

  strong {
    color: var(--text-color);
  }

  small,
  em {
    color: var(--text-secondary);
    font-size: 0.78rem;
    font-style: normal;
    font-weight: 700;
  }
}

.device-option__tag {
  position: absolute;
  top: 12px;
  right: 12px;
  border-radius: 999px;
  padding: 5px 9px;
  color: var(--text-secondary);
  background: color-mix(in srgb, var(--surface-color) 70%, var(--app-bg));
  font-size: 0.7rem;
  font-weight: 900;
}

.form-actions {
  display: flex;
  justify-content: flex-end;
  gap: 12px;
  flex-wrap: wrap;
}

@media (max-width: 920px) {
  .operation-summary,
  .fields-row,
  .checkbox-grid {
    grid-template-columns: 1fr;
  }

  .operation-toolbar,
  .group-room-section__header,
  .picker-room-section__header,
  .picker-card__header {
    align-items: flex-start;
    flex-direction: column;
  }

  .group-room-section__stats {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 640px) {
  .group-room-section__stats {
    grid-template-columns: 1fr;
  }
}


============================================================
FILE: src\app\device-control\presentation\pages\device-groups\device-groups-page.component.ts
============================================================

import { Component, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { DeviceControlFacade } from '../../../application/services/device-control.facade';
import { Device, DeviceStatus } from '../../../domain/model/device.entity';
import { DeviceGroup } from '../../../domain/model/device-group.entity';
import { ActiveWorkplaceContextService } from '../../../../workplace/application/services/active-workplace-context.service';
import { WorkplaceFacade } from '../../../../workplace/application/services/workplace.facade';

import { DeviceGroupCardComponent } from '../../components/device-group-card/device-group-card.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { ConfirmDialogOptions, ConfirmDialogService } from '../../../../shared/application/services/confirm-dialog.service';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { ModalFormShellComponent } from '../../../../shared/presentation/components/modal-form-shell/modal-form-shell.component';
import { SectionCardComponent } from '../../../../shared/presentation/components/section-card/section-card.component';

interface DeviceSelectionRoomSection {
  roomName: string;
  devices: Device[];
  selectedCount: number;
  availableCount: number;
}

interface DeviceGroupView {
  group: DeviceGroup;
  roomName: string;
  devices: Device[];
}

interface DeviceGroupRoomSection {
  roomName: string;
  groups: DeviceGroupView[];
  groupCount: number;
  deviceCount: number;
  activeDeviceCount: number;
  currentWatts: number;
}

@Component({
  selector: 'app-device-groups-page',
  standalone: true,
  imports: [
    FormsModule,
    TranslateModule,
    DeviceGroupCardComponent,
    LoadingSpinnerComponent,
    AppButtonComponent,
    EmptyStateComponent,
    ModalFormShellComponent,
    SectionCardComponent,
  ],
  templateUrl: './device-groups-page.component.html',
  styleUrls: ['./device-groups-page.component.scss'],
})
export class DeviceGroupsPageComponent implements OnInit {
  name = '';
  description = '';
  selectedDeviceIds: number[] = [];
  initialLoadComplete = false;
  createModalOpen = false;
  removingGroupIds = new Set<number>();

  constructor(
    readonly deviceControlFacade: DeviceControlFacade,
    readonly workplaceFacade: WorkplaceFacade,
    private readonly activeWorkplaceContext: ActiveWorkplaceContextService,
    private readonly toastService: ToastService,
    private readonly confirmDialog: ConfirmDialogService,
    private readonly translate: TranslateService
  ) {}

  async ngOnInit(): Promise<void> {
    try {
      await Promise.all([
        this.deviceControlFacade.loadDeviceControl(),
        this.workplaceFacade.loadWorkplace(),
      ]);

      this.activeWorkplaceContext.ensureActiveLocation(this.workplaceFacade.locations());
    } finally {
      this.initialLoadComplete = true;
    }
  }

  get showInitialLoading(): boolean {
    return this.deviceControlFacade.loading() && !this.initialLoadComplete;
  }

  get selectedCount(): number {
    return this.selectedDeviceIds.length;
  }

  get groupNameIsValid(): boolean {
    return Boolean(this.name.trim());
  }

  get groupSubmitDisabled(): boolean {
    return (
      this.deviceControlFacade.loading() ||
      !this.activeLocationId ||
      !this.groupNameIsValid ||
      this.selectedCount === 0
    );
  }

  get assignedDeviceCount(): number {
    return this.sortedDevices.length;
  }

  get selectedRoomName(): string {
    const firstDeviceId = this.selectedDeviceIds[0];

    if (!firstDeviceId) {
      return this.t('deviceGroups.noSelection');
    }

    const device = this.deviceControlFacade.devices().find((item) => item.id === firstDeviceId);
    return device ? this.displayRoomName(this.getDeviceRoomName(device)) : this.t('deviceGroups.noSelection');
  }

  get activeLocationId(): number | null {
    return this.activeWorkplaceContext.activeLocationId();
  }

  get activeLocationName(): string {
    const locationId = this.activeLocationId;
    return locationId
      ? this.workplaceFacade.getLocationName(locationId)
      : this.t('devices.activeSiteFallback');
  }

  get activeDeviceIds(): Set<number> {
    const locationId = this.activeLocationId;
    if (!locationId) {
      return new Set<number>();
    }

    return new Set(
      this.workplaceFacade
        .deviceAssignments()
        .filter((assignment) => assignment.locationId === locationId)
        .map((assignment) => assignment.deviceId)
    );
  }

  get activeGroups(): DeviceGroup[] {
    const activeDeviceIds = this.activeDeviceIds;
    return this.deviceControlFacade
      .deviceGroups()
      .filter((group) => group.deviceIds.some((deviceId) => activeDeviceIds.has(deviceId)));
  }

  get sortedDevices(): Device[] {
    const activeDeviceIds = this.activeDeviceIds;
    return [...this.deviceControlFacade.devices()]
      .filter((device) => activeDeviceIds.has(device.id))
      .sort((first, second) => this.getDeviceRoomName(first).localeCompare(this.getDeviceRoomName(second)) || first.name.localeCompare(second.name));
  }

  get selectableDeviceRoomSections(): DeviceSelectionRoomSection[] {
    const sections = new Map<string, Device[]>();

    for (const device of this.sortedDevices) {
      const roomName = this.getDeviceRoomName(device);
      const devices = sections.get(roomName) ?? [];
      devices.push(device);
      sections.set(roomName, devices);
    }

    return [...sections.entries()]
      .map(([roomName, devices]) => ({
        roomName,
        devices,
        selectedCount: devices.filter((device) => this.isDeviceSelected(device.id)).length,
        availableCount: devices.length,
      }))
      .sort((left, right) => left.roomName.localeCompare(right.roomName));
  }

  get groupRoomSections(): DeviceGroupRoomSection[] {
    const sections = new Map<string, DeviceGroupView[]>();

    for (const group of this.activeGroups) {
      const devices = this.getGroupDevices(group);
      const roomName = devices[0] ? this.getDeviceRoomName(devices[0]) : '';
      const groups = sections.get(roomName) ?? [];
      groups.push({ group, roomName, devices });
      sections.set(roomName, groups);
    }

    return [...sections.entries()]
      .map(([roomName, groups]) => {
        const devices = groups.flatMap((item) => item.devices);

        return {
          roomName,
          groups,
          groupCount: groups.length,
          deviceCount: devices.length,
          activeDeviceCount: devices.filter((device) => device.isOn).length,
          currentWatts: devices.reduce(
            (total, device) => total + (device.isOn ? device.powerWatts : 0),
            0
          ),
        };
      })
      .sort((left, right) => left.roomName.localeCompare(right.roomName));
  }

  isDeviceSelected(deviceId: number): boolean {
    return this.selectedDeviceIds.includes(deviceId);
  }

  toggleDeviceSelection(deviceId: number, checked: boolean): void {
    if (checked) {
      const device = this.deviceControlFacade.devices().find((item) => item.id === deviceId);
      const firstDevice = this.deviceControlFacade.devices().find((item) => item.id === this.selectedDeviceIds[0]);

      if (device && firstDevice && this.getDeviceRoomName(device) !== this.getDeviceRoomName(firstDevice)) {
        this.toastService.warning(this.t('deviceGroups.validation.sameRoom'));
        return;
      }

      this.selectedDeviceIds = Array.from(
        new Set([...this.selectedDeviceIds, deviceId])
      );
      return;
    }

    this.selectedDeviceIds = this.selectedDeviceIds.filter((id) => id !== deviceId);
  }

  openCreateModal(): void {
    this.createModalOpen = true;
  }

  closeCreateModal(): void {
    this.createModalOpen = false;
  }

  async createGroup(): Promise<void> {
    if (!this.activeLocationId) {
      this.toastService.warning(this.t('deviceGroups.validation.activeLocationRequired'));
      return;
    }

    if (!this.groupNameIsValid) {
      this.toastService.warning(this.t('deviceGroups.validation.nameRequired'));
      return;
    }

    if (this.selectedCount === 0) {
      this.toastService.warning(this.t('deviceGroups.validation.deviceRequired'));
      return;
    }

    const success = await this.deviceControlFacade.createDeviceGroup({
      name: this.name,
      description: this.description,
      deviceIds: this.selectedDeviceIds,
    });

    if (success) {
      this.name = '';
      this.description = '';
      this.selectedDeviceIds = [];
      this.closeCreateModal();
      this.toastService.success(this.t('deviceGroups.createSuccess'));
      return;
    }

    this.toastService.error(this.t('deviceGroups.createError'));
  }

  async executeGroupAction(command: {
    groupId: number;
    status: DeviceStatus;
  }): Promise<void> {
    const viewport = this.captureViewport();
    this.stabilizeViewport(viewport);

    const success = await this.deviceControlFacade.executeGroupAction(command);
    this.stabilizeViewport(viewport);

    if (success) {
      this.toastService.info(this.t('deviceGroups.executeSuccess'));
      return;
    }

    this.toastService.error(this.t('deviceGroups.executeError'));
  }

  async deleteGroup(groupId: number): Promise<void> {
    if (this.removingGroupIds.has(groupId)) {
      return;
    }

    const confirmed = await this.confirmDialog.confirmSequence(
      this.buildGroupDeleteWarnings(groupId)
    );

    if (!confirmed) {
      return;
    }

    const viewport = this.captureViewport();
    this.markGroupAsRemoving(groupId, true);
    this.stabilizeViewport(viewport);
    await this.delay(760);

    const success = await this.deviceControlFacade.deleteDeviceGroup(groupId);
    this.stabilizeViewport(viewport);

    if (success) {
      this.toastService.info(this.t('deviceGroups.deleteSuccess'));
      return;
    }

    this.markGroupAsRemoving(groupId, false);
    this.toastService.error(this.t('deviceGroups.deleteError'));
  }

  getDeviceIcon(type: string): string {
    switch (type) {
      case 'LIGHT':
        return '💡';
      case 'SENSOR':
        return '📡';
      case 'SWITCH':
        return '🔘';
      case 'PLUG':
        return '🔌';
      default:
        return '⚡';
    }
  }

  getGroupNamesByDeviceId(deviceId: number): string[] {
    return this.deviceControlFacade
      .deviceGroups()
      .filter((item) => item.deviceIds.includes(deviceId))
      .map((item) => item.name);
  }

  getGroupMembershipLabel(deviceId: number): string {
    return this.getGroupNamesByDeviceId(deviceId).join(', ');
  }

  getDeviceSelectionLabel(deviceId: number): string {
    if (this.isDeviceSelected(deviceId)) {
      return this.t('deviceGroups.selection.selected');
    }

    return this.t('deviceGroups.selection.select');
  }

  isGroupRemoving(groupId: number): boolean {
    return this.removingGroupIds.has(groupId);
  }

  private buildGroupDeleteWarnings(groupId: number): ConfirmDialogOptions[] {
    const group = this.deviceControlFacade.deviceGroups().find((item) => item.id === groupId);
    const groupName = group?.name ?? this.t('deviceGroups.deleteFallbackGroup');
    const warnings: ConfirmDialogOptions[] = [];
    const activeRoutines = this.deviceControlFacade
      .routines()
      .filter((routine) =>
        routine.enabled &&
        routine.targetType === 'GROUP' &&
        (routine.groupId === groupId || routine.targetId === groupId)
      );
    const relatedModes = this.deviceControlFacade
      .operationModes()
      .filter((mode) => mode.status !== 'ARCHIVED' && mode.groupIds.includes(groupId));

    if (group?.deviceIds.length) {
      warnings.push({
        title: this.t('deviceGroups.deleteWarnings.devicesTitle'),
        message: this.t('deviceGroups.deleteWarnings.devicesMessage', {
          group: groupName,
          count: group.deviceIds.length,
        }),
        confirmLabel: this.t('common.continue'),
        cancelLabel: this.t('common.keep'),
        tone: 'warning',
      });
    }

    if (activeRoutines.length > 0) {
      warnings.push({
        title: this.t('deviceGroups.deleteWarnings.routinesTitle'),
        message: activeRoutines.length === 1
          ? this.t('deviceGroups.deleteWarnings.routineSingular', { group: groupName })
          : this.t('deviceGroups.deleteWarnings.routinePlural', { group: groupName, count: activeRoutines.length }),
        confirmLabel: this.t('common.continue'),
        cancelLabel: this.t('common.keep'),
        tone: 'warning',
      });
    }

    if (relatedModes.length > 0) {
      warnings.push({
        title: this.t('deviceGroups.deleteWarnings.modesTitle'),
        message: relatedModes.length === 1
          ? this.t('deviceGroups.deleteWarnings.modeSingular', { group: groupName })
          : this.t('deviceGroups.deleteWarnings.modePlural', { group: groupName, count: relatedModes.length }),
        confirmLabel: this.t('common.continue'),
        cancelLabel: this.t('common.keep'),
        tone: 'warning',
      });
    }

    warnings.push({
      title: this.t('deviceGroups.deleteWarnings.deleteTitle'),
      message: this.t('deviceGroups.deleteWarnings.deleteMessage', { group: groupName }),
      confirmLabel: this.t('common.delete'),
      cancelLabel: this.t('common.keep'),
      tone: 'danger',
    });

    return warnings;
  }

  private getGroupDevices(group: DeviceGroup): Device[] {
    const activeDeviceIds = this.activeDeviceIds;
    const devicesById = new Map(
      this.deviceControlFacade.devices().map((device) => [device.id, device])
    );

    return group.deviceIds
      .filter((deviceId) => activeDeviceIds.has(deviceId))
      .map((deviceId) => devicesById.get(deviceId))
      .filter((device): device is Device => Boolean(device));
  }

  private getDeviceRoomName(device: Device): string {
    const locationId = this.activeLocationId;
    const assignment = this.workplaceFacade
      .deviceAssignments()
      .find(
        (item) =>
          item.deviceId === device.id &&
          (!locationId || item.locationId === locationId)
      );

    if (assignment?.roomId) {
      const room = this.workplaceFacade
        .rooms()
        .find((item) => item.id === assignment.roomId);

      if (room?.name) {
        return room.name;
      }
    }

    return device.room?.trim() || '';
  }

  displayRoomName(roomName: string): string {
    return roomName || this.t('devices.noRoom');
  }

  private markGroupAsRemoving(groupId: number, removing: boolean): void {
    const next = new Set(this.removingGroupIds);

    if (removing) {
      next.add(groupId);
    } else {
      next.delete(groupId);
    }

    this.removingGroupIds = next;
  }

  private delay(milliseconds: number): Promise<void> {
    return new Promise((resolve) => window.setTimeout(resolve, milliseconds));
  }

  private captureViewport(): { x: number; y: number } | null {
    if (typeof window === 'undefined') {
      return null;
    }

    return {
      x: window.scrollX,
      y: window.scrollY,
    };
  }

  private stabilizeViewport(viewport: { x: number; y: number } | null): void {
    if (!viewport || typeof window === 'undefined') {
      return;
    }

    const restore = () => {
      if (
        Math.abs(window.scrollX - viewport.x) > 1 ||
        Math.abs(window.scrollY - viewport.y) > 1
      ) {
        window.scrollTo(viewport.x, viewport.y);
      }
    };

    requestAnimationFrame(restore);
    window.setTimeout(restore, 48);
    window.setTimeout(restore, 140);
  }

  private t(key: string, params?: Record<string, unknown>): string {
    return this.translate.instant(key, params);
  }
}


============================================================
FILE: src\app\device-control\presentation\pages\devices\devices-page.component.html
============================================================

<section class="devices-page ec-page">
  <header class="ec-page-heading">
    <p class="ec-page-eyebrow">{{ 'nav.operation' | translate }}</p>
    <h1 class="ec-page-title">{{ 'nav.devices' | translate }}</h1>
    <p class="ec-page-description">
      {{ 'devices.pageDescription' | translate }}
    </p>
  </header>

  <section class="operation-summary">
    <app-section-card
      size="small"
      [eyebrow]="'nav.devices' | translate"
      [title]="'devices.summary.assignedTitle' | translate"
    >
      <strong>{{ assignedDevices.length }}</strong>
      <p>{{ activeLocationName }}</p>
    </app-section-card>

    <app-section-card
      size="small"
      [eyebrow]="'history.status' | translate"
      [title]="'devices.summary.onTitle' | translate"
    >
      <strong>{{ assignedActiveDevices.length }}</strong>
      <p>{{ 'devices.summary.offDevices' | translate: { count: assignedDevices.length - assignedActiveDevices.length } }}</p>
    </app-section-card>

    <app-section-card
      size="small"
      [eyebrow]="'devices.summary.consumptionEyebrow' | translate"
      [title]="'devices.summary.currentTitle' | translate"
    >
      <strong>{{ assignedCurrentWatts }} W</strong>
      <p>{{ 'devices.summary.activePowerDescription' | translate }}</p>
    </app-section-card>
  </section>

  <section class="operation-toolbar">
    <div>
      <p class="section-eyebrow">{{ 'devices.toolbar.eyebrow' | translate }}</p>
      <h2>{{ 'devices.toolbar.title' | translate }}</h2>
      <p>{{ 'devices.toolbar.description' | translate }}</p>
    </div>

    <app-button
      variant="create"
      [disabled]="activeRooms.length === 0"
      (pressed)="openCreateModal()"
    >
      {{ 'devices.create' | translate }}
    </app-button>
  </section>

  @if (showInitialLoading) {
    <div class="loading-wrapper">
      <app-loading-spinner />
    </div>
  } @else if (activeRooms.length === 0) {
    <app-empty-state
      [eyebrow]="'nav.rooms' | translate"
      [title]="'devices.emptyRooms.title' | translate"
      [description]="'devices.emptyRooms.description' | translate"
      [actionLabel]="'devices.go' | translate"
      actionLink="/spaces/rooms"
    />
  } @else if (assignedDevices.length > 0) {
    <app-device-list
      [devices]="assignedDevices"
      [locationName]="activeLocationName"
      [removingDeviceIds]="removingDeviceIds"
      (toggle)="toggleDevice($event)"
      (remove)="deleteDevice($event)"
    />
  } @else {
    <app-empty-state
      [eyebrow]="'nav.devices' | translate"
      [title]="'devices.emptyAssigned.title' | translate"
      [description]="'devices.emptyAssigned.description' | translate"
      [actionLabel]="'devices.go' | translate"
      actionLink="/spaces/assignments"
      actionVariant="neutral"
    />
  }

  <app-modal-form-shell
    [open]="createModalOpen"
    [eyebrow]="'devices.form.eyebrow' | translate"
    [title]="'devices.create' | translate"
    [description]="'devices.form.description' | translate"
    (closed)="closeCreateModal()"
  >
    <form class="modal-form" (ngSubmit)="createDevice()">
      <label class="field">
        <span>{{ 'devices.name' | translate }}</span>
        <input
          type="text"
          name="name"
          [(ngModel)]="name"
          [placeholder]="'devices.form.namePlaceholder' | translate"
          required
        />
      </label>

      <label class="field">
        <span>{{ 'devices.room' | translate }}</span>
        <app-dropdown
          [options]="roomOptions"
          [value]="roomValue"
          [placeholder]="'devices.room' | translate"
          (valueChange)="selectRoom($event)"
        />
      </label>

      <label class="field">
        <span>{{ 'devices.type' | translate }}</span>
        <app-dropdown
          [options]="deviceTypeOptions"
          [value]="type"
          [placeholder]="'devices.type' | translate"
          (valueChange)="selectDeviceType($event)"
        />
      </label>

      <div class="field">
        <app-number-stepper
          [label]="'devices.form.wattsLabel' | translate"
          suffix="W"
          [value]="powerWatts"
          [min]="currentPowerConfig.min"
          [max]="currentPowerConfig.max"
          [step]="currentPowerConfig.step"
          [allowEmpty]="false"
          (valueChange)="onPowerWattsChange($event)"
        />
      </div>

      <div class="power-advice" [class.high]="isHighPowerSelection">
        <span>{{ powerAdviceTitle }}</span>
        <p>{{ powerAdviceMessage }}</p>
      </div>

      <div class="form-actions">
        <app-button variant="neutral" (pressed)="closeCreateModal()">{{ 'common.cancel' | translate }}</app-button>
        <app-button
          type="submit"
          variant="create"
          [disabled]="deviceControlFacade.loading() || activeRooms.length === 0"
        >
          {{ 'devices.create' | translate }}
        </app-button>
      </div>
    </form>
  </app-modal-form-shell>
</section>


============================================================
FILE: src\app\device-control\presentation\pages\devices\devices-page.component.scss
============================================================

.devices-page {
  display: grid;
  gap: 24px;
}

.operation-summary {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;

  strong {
    display: block;
    margin-bottom: 6px;
    color: var(--text-color);
    font-size: clamp(1.65rem, 3vw, 2.25rem);
    line-height: 1;
  }

  p {
    margin: 0;
    color: var(--text-secondary);
    font-weight: 700;
  }
}

.operation-toolbar {
  display: flex;
  justify-content: space-between;
  gap: 18px;
  align-items: center;
  border: 1px solid var(--border-color);
  border-radius: 24px;
  padding: 22px;
  background: var(--surface-color);

  h2,
  p {
    margin: 0;
  }

  h2 {
    color: var(--text-color);
    font-size: 1.3rem;
  }

  p:not(.section-eyebrow) {
    margin-top: 6px;
    color: var(--text-secondary);
    font-weight: 700;
  }
}

.section-eyebrow {
  margin: 0 0 6px;
  color: var(--accent-color);
  font-size: 0.74rem;
  font-weight: 900;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.loading-wrapper {
  min-height: 220px;
  display: grid;
  place-items: center;
}

.modal-form {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
}

.field {
  display: grid;
  gap: 8px;
  min-width: 0;

  span {
    color: var(--text-secondary);
    font-size: 0.8rem;
    font-weight: 900;
  }
}

input {
  width: 100%;
  min-height: 48px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  padding: 13px 14px;
  color: var(--text-primary);
  background: color-mix(in srgb, var(--surface-color) 84%, var(--app-bg));
  box-sizing: border-box;
  font: inherit;
  outline: none;
}

input:focus {
  border-color: color-mix(in srgb, var(--accent-color) 70%, var(--border-color));
  box-shadow: 0 0 0 4px color-mix(in srgb, var(--accent-color) 12%, transparent);
}

.power-advice,
.form-actions {
  grid-column: 1 / -1;
}

.power-advice {
  display: grid;
  gap: 8px;
  border: 1px solid color-mix(in srgb, var(--accent-color) 22%, var(--border-color));
  border-radius: 18px;
  padding: 15px;
  background: color-mix(in srgb, var(--accent-color) 7%, var(--surface-color));

  span {
    width: fit-content;
    border-radius: 999px;
    padding: 5px 10px;
    color: var(--accent-color);
    background: color-mix(in srgb, var(--accent-color) 12%, transparent);
    font-size: 0.76rem;
    font-weight: 900;
  }

  p {
    margin: 0;
    color: var(--text-secondary);
    font-weight: 700;
    line-height: 1.5;
  }
}

.power-advice.high {
  border-color: var(--ec-status-danger-border);
  background: color-mix(in srgb, var(--ec-status-danger-bg) 72%, var(--surface-color));

  span {
    color: var(--ec-status-danger-text);
    background: var(--ec-status-danger-bg);
  }
}

.form-actions {
  display: flex;
  justify-content: flex-end;
  gap: 12px;
  flex-wrap: wrap;
}

@media (max-width: 860px) {
  .operation-summary,
  .modal-form {
    grid-template-columns: 1fr;
  }

  .operation-toolbar {
    align-items: flex-start;
    flex-direction: column;
  }
}


============================================================
FILE: src\app\device-control\presentation\pages\devices\devices-page.component.ts
============================================================

import { Component, HostListener, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { AppNumberStepperComponent } from '../../../../shared/presentation/components/app-number-stepper/app-number-stepper.component';

import { DeviceControlFacade } from '../../../application/services/device-control.facade';
import { Device, DeviceType } from '../../../domain/model/device.entity';
import { ActiveWorkplaceContextService } from '../../../../workplace/application/services/active-workplace-context.service';
import { WorkplaceFacade } from '../../../../workplace/application/services/workplace.facade';
import { Room } from '../../../../workplace/domain/model/room.entity';

import { DeviceListComponent } from '../../components/device-list/device-list.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { ConfirmDialogOptions, ConfirmDialogService } from '../../../../shared/application/services/confirm-dialog.service';
import { Routine } from '../../../domain/model/routine.entity';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { ModalFormShellComponent } from '../../../../shared/presentation/components/modal-form-shell/modal-form-shell.component';
import { SectionCardComponent } from '../../../../shared/presentation/components/section-card/section-card.component';

interface DevicePreset {
  name: string;
  room: string;
  type: DeviceType;
  powerWatts: number;
  icon: string;
}

@Component({
  selector: 'app-devices-page',
  standalone: true,
  imports: [
    FormsModule,
    TranslateModule,
    DeviceListComponent,
    LoadingSpinnerComponent,
    AppDropdownComponent,
    AppNumberStepperComponent,
    AppButtonComponent,
    EmptyStateComponent,
    ModalFormShellComponent,
    SectionCardComponent,
  ],
  templateUrl: './devices-page.component.html',
  styleUrls: ['./devices-page.component.scss'],
})
export class DevicesPageComponent implements OnInit {
  name = '';
  selectedRoomId: number | null = null;
  type: DeviceType = 'PLUG';
  powerWatts = 150;
  isTypeDropdownOpen = false;
  initialLoadComplete = false;
  createModalOpen = false;
  removingDeviceIds = new Set<number>();

  readonly devicePresets: DevicePreset[] = [
    {
      name: 'Lámpara de sala',
      room: 'Sala',
      type: 'LIGHT',
      powerWatts: 12,
      icon: '💡',
    },
    {
      name: 'Smart Plug cocina',
      room: 'Cocina',
      type: 'PLUG',
      powerWatts: 150,
      icon: '🔌',
    },
    {
      name: 'Sensor de movimiento',
      room: 'Entrada',
      type: 'SENSOR',
      powerWatts: 5,
      icon: '📡',
    },
    {
      name: 'Interruptor principal',
      room: 'Sala',
      type: 'SWITCH',
      powerWatts: 60,
      icon: '🔘',
    },
    {
      name: 'Televisor',
      room: 'Sala',
      type: 'PLUG',
      powerWatts: 120,
      icon: '📺',
    },
    {
      name: 'Microondas',
      room: 'Cocina',
      type: 'PLUG',
      powerWatts: 1200,
      icon: '🍳',
    },
    {
      name: 'Refrigeradora',
      room: 'Cocina',
      type: 'PLUG',
      powerWatts: 250,
      icon: '❄️',
    },
    {
      name: 'Aire acondicionado',
      room: 'Dormitorio',
      type: 'PLUG',
      powerWatts: 1800,
      icon: '🌀',
    },
  ];

  async createPresetDevice(preset: DevicePreset): Promise<void> {
    const room = this.resolveRoomForPreset(preset.room);

    if (!room) {
      this.toastService.warning(this.t('devices.validation.roomRequiredBeforePreset'));
      return;
    }

    const device = await this.deviceControlFacade.createDevice({
      name: preset.name,
      room: room.name,
      type: preset.type,
      powerWatts: preset.powerWatts,
    });

    if (device) {
      const assigned = await this.assignCreatedDevice(device, room);

      if (assigned) {
        this.toastService.success(this.t('devices.toasts.presetCreatedAndAssigned', {
          device: preset.name,
          room: room.name,
        }));
        return;
      }

      this.toastService.warning(this.t('devices.toasts.presetCreatedWithoutAssignment', {
        device: preset.name,
      }));
      return;
    }

    this.toastService.error(this.t('devices.toasts.presetCreateError', { device: preset.name }));
  }

  readonly deviceTypeOptions: DropdownOption[] = [
    { value: 'PLUG', label: 'PLUG', labelKey: 'workplace.deviceTypes.plug' },
    { value: 'LIGHT', label: 'LIGHT', labelKey: 'workplace.deviceTypes.light' },
    { value: 'SWITCH', label: 'SWITCH', labelKey: 'workplace.deviceTypes.switch' },
    { value: 'SENSOR', label: 'SENSOR', labelKey: 'workplace.deviceTypes.sensor' },
    { value: 'OTHER', label: 'OTHER', labelKey: 'workplace.deviceTypes.other' },
  ];

  readonly powerConfigByType: Record<
    DeviceType,
    { min: number; max: number; step: number; defaultValue: number; warningAt: number }
  > = {
    PLUG: { min: 5, max: 2500, step: 5, defaultValue: 150, warningAt: 1800 },
    LIGHT: { min: 3, max: 150, step: 1, defaultValue: 12, warningAt: 100 },
    SWITCH: { min: 5, max: 1800, step: 5, defaultValue: 60, warningAt: 1300 },
    SENSOR: { min: 1, max: 30, step: 1, defaultValue: 5, warningAt: 20 },
    OTHER: { min: 1, max: 1000, step: 5, defaultValue: 80, warningAt: 700 },
  };

  constructor(
    readonly deviceControlFacade: DeviceControlFacade,
    readonly workplaceFacade: WorkplaceFacade,
    private readonly activeWorkplaceContext: ActiveWorkplaceContextService,
    private readonly toastService: ToastService,
    private readonly confirmDialog: ConfirmDialogService,
    private readonly translate: TranslateService
  ) {}

  async ngOnInit(): Promise<void> {
    try {
      await this.workplaceFacade.loadWorkplace();
      this.activeWorkplaceContext.ensureActiveLocation(this.workplaceFacade.locations());
      this.ensureSelectedRoom();
    } finally {
      this.initialLoadComplete = true;
    }
  }

  get showInitialLoading(): boolean {
    return this.deviceControlFacade.loading() && !this.initialLoadComplete;
  }

  get activeLocationId(): number | null {
    return this.activeWorkplaceContext.activeLocationId();
  }

  get activeRooms(): Room[] {
    const locationId = this.activeLocationId;
    return locationId
      ? this.workplaceFacade.rooms().filter((room) => room.locationId === locationId)
      : [];
  }

  get selectedRoom(): Room | null {
    return (
      this.activeRooms.find((room) => room.id === this.selectedRoomId) ??
      this.activeRooms[0] ??
      null
    );
  }

  get roomValue(): string | null {
    return this.selectedRoom ? String(this.selectedRoom.id) : null;
  }

  get roomOptions(): DropdownOption[] {
    return this.activeRooms.map((room) => ({
      label: room.name,
      value: String(room.id),
    }));
  }

  get activeLocationName(): string {
    const locationId = this.activeLocationId;
    return locationId
      ? this.workplaceFacade.getLocationName(locationId)
      : this.t('devices.activeSiteFallback');
  }

  get assignedDeviceIds(): Set<number> {
    const locationId = this.activeLocationId;

    if (!locationId) {
      return new Set<number>();
    }

    return new Set(
      this.workplaceFacade
        .deviceAssignments()
        .filter((assignment) => assignment.locationId === locationId)
        .map((assignment) => assignment.deviceId)
    );
  }

  get assignedDevices(): Device[] {
    const assignedIds = this.assignedDeviceIds;
    return this.deviceControlFacade
      .devices()
      .filter((device) => assignedIds.has(device.id));
  }

  get assignedActiveDevices(): Device[] {
    return this.assignedDevices.filter((device) => device.isOn);
  }

  get assignedCurrentWatts(): number {
    return this.assignedActiveDevices.reduce(
      (total, device) => total + device.powerWatts,
      0
    );
  }

  get currentPowerConfig() {
    return this.powerConfigByType[this.type];
  }

  openCreateModal(): void {
    this.ensureSelectedRoom();
    this.createModalOpen = true;
  }

  closeCreateModal(): void {
    this.createModalOpen = false;
  }

  get isHighPowerSelection(): boolean {
    return this.powerWatts >= this.currentPowerConfig.warningAt;
  }

  get powerAdviceTitle(): string {
    return this.t(this.isHighPowerSelection ? 'devices.powerAdvice.highTitle' : 'devices.powerAdvice.normalTitle');
  }

  get powerAdviceMessage(): string {
    const config = this.currentPowerConfig;
    const typeLabel = this.getDeviceTypeLabel(this.type);

    if (this.isHighPowerSelection) {
      return this.t('devices.powerAdvice.highMessage', {
        type: typeLabel,
        watts: config.warningAt,
      });
    }

    return this.t('devices.powerAdvice.normalMessage', { type: typeLabel });
  }

  getDeviceTypeLabel(type: DeviceType): string {
    const labels: Record<DeviceType, string> = {
      PLUG: 'workplace.deviceTypes.plug',
      LIGHT: 'workplace.deviceTypes.light',
      SWITCH: 'workplace.deviceTypes.switch',
      SENSOR: 'workplace.deviceTypes.sensor',
      OTHER: 'workplace.deviceTypes.other',
    };

    return this.t(labels[type]);
  }

  getDeviceTypeIcon(type: DeviceType): string {
    const icons: Record<DeviceType, string> = {
      PLUG: '🔌',
      LIGHT: '💡',
      SWITCH: '🔘',
      SENSOR: '📡',
      OTHER: '⚡',
    };

    return icons[type] ?? '⚡';
  }

  onTypeChange(): void {
    this.powerWatts = this.currentPowerConfig.defaultValue;
  }

  onPowerBlur(): void {
    const config = this.currentPowerConfig;

    if (!Number.isFinite(this.powerWatts)) {
      this.powerWatts = config.defaultValue;
      return;
    }

    if (this.powerWatts < config.min) {
      this.powerWatts = config.min;
      return;
    }

    if (this.powerWatts > config.max) {
      this.powerWatts = config.max;
    }
  }

  onPowerWattsChange(value: number | null): void {
    this.powerWatts = value ?? this.currentPowerConfig.defaultValue;
    this.onPowerBlur();
  }

  async createDevice(): Promise<void> {
  const name = this.name.trim();
  const room = this.selectedRoom;

  if (!name) {
    this.toastService.warning(this.t('devices.validation.nameRequired'));
    return;
  }

  if (name.length < 3) {
    this.toastService.warning(this.t('devices.validation.nameMinLength'));
    return;
  }

  if (!room) {
    this.toastService.warning(this.t('devices.validation.roomRequired'));
    return;
  }

  this.onPowerBlur();

  if (!Number.isFinite(this.powerWatts) || this.powerWatts <= 0) {
    this.toastService.warning(this.t('devices.validation.powerRequired'));
    return;
  }

  const device = await this.deviceControlFacade.createDevice({
    name,
    room: room.name,
    type: this.type,
    powerWatts: this.powerWatts,
  });

  if (device) {
    const assigned = await this.assignCreatedDevice(device, room);

    if (!assigned) {
      this.toastService.warning(this.t('devices.toasts.createdWithoutAssignment'));
      return;
    }

    this.name = '';
    this.type = 'PLUG';
    this.powerWatts = this.powerConfigByType.PLUG.defaultValue;
    this.closeCreateModal();

    this.toastService.success(this.t('devices.toasts.createdAndAssigned', { room: room.name }));
    return;
  }

  this.toastService.error(this.t('devices.createError'));
}

  async toggleDevice(device: Device): Promise<void> {
    const viewport = this.captureViewport();
    this.stabilizeViewport(viewport);

    const success = await this.deviceControlFacade.toggleDevice(device);
    this.stabilizeViewport(viewport);

    if (success) {
      this.toastService.info(
        device.isOn ? this.t('devices.toasts.turnedOff') : this.t('devices.toasts.turnedOn')
      );
    } else {
      this.toastService.error(this.t('devices.updateError'));
    }
  }

  async deleteDevice(deviceId: number): Promise<void> {
    if (this.removingDeviceIds.has(deviceId)) {
      return;
    }

    const warnings = this.buildDeviceDeleteWarnings(deviceId);

    if (warnings.length > 0) {
      const confirmed = await this.confirmDialog.confirmSequence(warnings);

      if (!confirmed) {
        return;
      }
    }

    await this.performDeviceDelete(deviceId, this.getDirectDeviceRoutines(deviceId).length);
  }

  private async performDeviceDelete(deviceId: number, removedRoutineCount: number): Promise<void> {
    if (this.removingDeviceIds.has(deviceId)) {
      return;
    }

    const viewport = this.captureViewport();
    this.markDeviceAsRemoving(deviceId, true);
    this.stabilizeViewport(viewport);
    await this.delay(760);

    const success = await this.deviceControlFacade.deleteDevice(deviceId);
    this.stabilizeViewport(viewport);

    if (success) {
      const routineCopy = removedRoutineCount > 0
        ? removedRoutineCount === 1
          ? ` ${this.t('devices.toasts.removedRoutineSingular')}`
          : ` ${this.t('devices.toasts.removedRoutinePlural', { count: removedRoutineCount })}`
        : '';

      this.toastService.info(`${this.t('devices.toasts.deletedHistoryKept')}${routineCopy}`);
    } else {
      this.markDeviceAsRemoving(deviceId, false);
      this.toastService.error(this.t('devices.deleteError'));
    }
  }

  get selectedDeviceTypeLabel(): string {
    return this.getDeviceTypeLabel(this.type);
  }

  selectRoom(value: string): void {
    const roomId = Number(value);
    this.selectedRoomId = Number.isFinite(roomId) ? roomId : null;
  }

  toggleTypeDropdown(): void {
    this.isTypeDropdownOpen = !this.isTypeDropdownOpen;
  }

  selectDeviceType(value: string): void {
    this.type = value as DeviceType;
    this.onTypeChange();
  }

  @HostListener('document:click', ['$event'])
  closeDropdownOnOutsideClick(event: MouseEvent): void {
    const target = event.target as HTMLElement;

    if (!target.closest('.custom-select')) {
      this.isTypeDropdownOpen = false;
    }
  }

  private ensureSelectedRoom(): void {
    const roomStillActive = this.activeRooms.some((room) => room.id === this.selectedRoomId);

    if (!roomStillActive) {
      this.selectedRoomId = this.activeRooms[0]?.id ?? null;
    }
  }

  private resolveRoomForPreset(roomName: string): Room | null {
    this.ensureSelectedRoom();

    return (
      this.activeRooms.find((room) => this.normalize(room.name) === this.normalize(roomName)) ??
      this.selectedRoom
    );
  }

  private async assignCreatedDevice(device: Device, room: Room): Promise<boolean> {
    const locationId = this.activeLocationId;

    if (!locationId) {
      return false;
    }

    return this.workplaceFacade.assignDevice({
      deviceId: device.id,
      locationId,
      roomId: room.id,
    });
  }

  private normalize(value: string): string {
    return value.trim().toLocaleLowerCase();
  }

  private getDirectDeviceRoutines(deviceId: number): Routine[] {
    return this.deviceControlFacade
      .routines()
      .filter(
        (routine) =>
          routine.enabled &&
          routine.targetType === 'DEVICE' &&
          (routine.deviceId === deviceId || routine.targetId === deviceId)
      );
  }

  private buildDeviceDeleteWarnings(deviceId: number): ConfirmDialogOptions[] {
    const deviceName =
      this.deviceControlFacade.devices().find((device) => device.id === deviceId)?.name ??
      this.t('devices.deleteFallbackDevice');
    const warnings: ConfirmDialogOptions[] = [];
    const groupNames = this.deviceControlFacade
      .deviceGroups()
      .filter((group) => group.deviceIds.includes(deviceId))
      .map((group) => group.name);
    const directRoutines = this.getDirectDeviceRoutines(deviceId);
    const relatedModes = this.deviceControlFacade
      .operationModes()
      .filter((mode) =>
        mode.status !== 'ARCHIVED' &&
        [
          ...mode.deviceIds,
          ...mode.turnOnDeviceIds,
          ...mode.turnOffDeviceIds,
          ...mode.keepOnDeviceIds,
        ].includes(deviceId)
      );

    if (groupNames.length > 0) {
      warnings.push({
        title: this.t('devices.deleteWarnings.groupsTitle'),
        message: groupNames.length === 1
          ? this.t('devices.deleteWarnings.groupSingular', { device: deviceName, group: groupNames[0] })
          : this.t('devices.deleteWarnings.groupPlural', { device: deviceName, count: groupNames.length }),
        confirmLabel: this.t('common.continue'),
        cancelLabel: this.t('common.keep'),
        tone: 'warning',
      });
    }

    if (directRoutines.length > 0) {
      warnings.push({
        title: this.t('devices.deleteWarnings.routinesTitle'),
        message: this.getDeviceDeleteMessage(deviceId, directRoutines.length),
        confirmLabel: this.t('common.continue'),
        cancelLabel: this.t('common.keep'),
        tone: 'warning',
      });
    }

    if (relatedModes.length > 0) {
      warnings.push({
        title: this.t('devices.deleteWarnings.modesTitle'),
        message: relatedModes.length === 1
          ? this.t('devices.deleteWarnings.modeSingular', { device: deviceName })
          : this.t('devices.deleteWarnings.modePlural', { device: deviceName, count: relatedModes.length }),
        confirmLabel: this.t('common.continue'),
        cancelLabel: this.t('common.keep'),
        tone: 'warning',
      });
    }

    warnings.push({
      title: this.t('devices.deleteWarnings.deleteTitle'),
      message: this.t('devices.deleteWarnings.deleteMessage', { device: deviceName }),
      confirmLabel: this.t('common.delete'),
      cancelLabel: this.t('common.keep'),
      tone: 'danger',
    });

    return warnings;
  }

  private getDeviceDeleteMessage(deviceId: number, routineCount: number): string {
    const deviceName =
      this.deviceControlFacade.devices().find((device) => device.id === deviceId)?.name ??
      this.t('devices.deleteFallbackDevice');

    return routineCount === 1
      ? this.t('devices.deleteWarnings.routineSingular', { device: deviceName })
      : this.t('devices.deleteWarnings.routinePlural', { device: deviceName, count: routineCount });
  }

  private markDeviceAsRemoving(deviceId: number, removing: boolean): void {
    const next = new Set(this.removingDeviceIds);

    if (removing) {
      next.add(deviceId);
    } else {
      next.delete(deviceId);
    }

    this.removingDeviceIds = next;
  }

  private delay(milliseconds: number): Promise<void> {
    return new Promise((resolve) => window.setTimeout(resolve, milliseconds));
  }

  private captureViewport(): { x: number; y: number } | null {
    if (typeof window === 'undefined') {
      return null;
    }

    return {
      x: window.scrollX,
      y: window.scrollY,
    };
  }

  private stabilizeViewport(viewport: { x: number; y: number } | null): void {
    if (!viewport || typeof window === 'undefined') {
      return;
    }

    const restore = () => {
      if (
        Math.abs(window.scrollX - viewport.x) > 1 ||
        Math.abs(window.scrollY - viewport.y) > 1
      ) {
        window.scrollTo(viewport.x, viewport.y);
      }
    };

    requestAnimationFrame(restore);
    window.setTimeout(restore, 48);
    window.setTimeout(restore, 140);
  }

  private t(key: string, params?: Record<string, unknown>): string {
    return this.translate.instant(key, params);
  }
}


============================================================
FILE: src\app\device-control\presentation\pages\operation-modes\operation-modes-page.component.html
============================================================

<section class="operation-modes-page ec-page">
  <header class="ec-page-heading">
    <p class="ec-page-eyebrow">{{ 'nav.operation' | translate }}</p>
    <h1 class="ec-page-title">{{ 'nav.operationModes' | translate }}</h1>
    <p class="ec-page-description">
      {{ 'operationModes.pageDescription' | translate }}
    </p>
  </header>

  <section class="mode-summary">
    <app-section-card size="small" [eyebrow]="'home.activeSiteEyebrow' | translate" [title]="'operationModes.summary.context' | translate">
      <strong>{{ selectedLocationName }}</strong>
      <p>{{ 'operationModes.summary.availableDevices' | translate: { count: availableDevices.length } }}</p>
    </app-section-card>

    <app-section-card size="small" [eyebrow]="'nav.operationModes' | translate" [title]="'operationModes.summary.saved' | translate">
      <strong>{{ modesInActiveWorkplace.length }}</strong>
      <p>{{ (activeMode ? 'operationModes.summary.hasActiveMode' : 'operationModes.summary.noActiveMode') | translate }}</p>
    </app-section-card>

    <app-section-card size="small" [eyebrow]="'operationModes.internalRoutines' | translate" [title]="'operationModes.summary.current' | translate">
      <strong>{{ selectedMode?.internalRoutines?.length ?? 0 }}</strong>
      <p>{{ selectedMode ? selectedMode.name : ('operationModes.summary.selectMode' | translate) }}</p>
    </app-section-card>
  </section>

  <section class="operation-toolbar">
    <div>
      <p class="section-eyebrow">{{ 'operationModes.toolbar.eyebrow' | translate }}</p>
      <h2>{{ 'operationModes.toolbar.title' | translate }}</h2>
      <p>{{ 'operationModes.toolbar.description' | translate }}</p>
    </div>

    <app-button variant="create" (pressed)="openCreateModal()">
      {{ 'operationModes.actions.create' | translate }}
    </app-button>
  </section>

  @if (modesInActiveWorkplace.length === 0) {
    <app-empty-state
      [eyebrow]="'nav.operationModes' | translate"
      [title]="'operationModes.empty.title' | translate"
      [description]="'operationModes.empty.description' | translate"
    />
  } @else {
    <section class="mode-grid">
      @for (mode of modesInActiveWorkplace; track mode.id) {
        <app-section-card class="mode-card" size="medium">
          <article class="mode-card__body" [class.mode-card__body--active]="mode.status === 'ACTIVE'">
            <header class="mode-card__header">
              <div>
                <p>{{ getModeRoomName(mode) }}</p>
                <h3>{{ mode.name }}</h3>
              </div>

              <app-severity-badge [level]="mode.status === 'ACTIVE' ? 'SUCCESS' : 'STABLE'" />
            </header>

            <p class="mode-card__description">
              {{ mode.description || ('operationModes.fallbacks.noDescription' | translate) }}
            </p>

            <dl class="mode-card__facts">
              <div>
                <dt>{{ 'routines.time' | translate }}</dt>
                <dd>{{ getModeScheduleLabel(mode) }}</dd>
              </div>
              <div>
                <dt>{{ 'nav.routines' | translate }}</dt>
                <dd>{{ mode.internalRoutines.length }}</dd>
              </div>
              <div>
                <dt>{{ 'history.status' | translate }}</dt>
                <dd>{{ getModeStatusLabel(mode) }}</dd>
              </div>
            </dl>

            <div class="mode-card__actions">
              <app-button variant="neutral" size="sm" (pressed)="selectMode(mode)">{{ 'operationModes.impact.title' | translate }}</app-button>
              <app-button variant="action" size="sm" (pressed)="activateMode(mode)">
                {{ (mode.status === 'ACTIVE' ? 'operationModes.actions.reapply' : 'routines.enable') | translate }}
              </app-button>
              <app-button variant="danger" size="sm" (pressed)="archiveMode(mode)">{{ 'operationModes.actions.archive' | translate }}</app-button>
            </div>
          </article>
        </app-section-card>
      }
    </section>
  }

  <app-section-card [eyebrow]="'operationModes.impact.title' | translate" [title]="'operationModes.impact.applicationTitle' | translate">
    @if (selectedMode && preview) {
      <section class="impact-panel">
        <header>
          <div>
            <h3>{{ selectedMode.name }}</h3>
            <p>{{ preview.evidence }}</p>
          </div>
          <strong>{{ 'operationModes.impact.affectedDevices' | translate: { count: preview.affectedDeviceIds.length } }}</strong>
        </header>

        <div class="impact-grid">
          <article>
            <span>{{ 'operationModes.site' | translate }}</span>
            <strong>{{ preview.locationName }}</strong>
          </article>
          <article>
            <span>{{ 'operationModes.internalRoutines' | translate }}</span>
            <strong>{{ selectedMode.internalRoutines.length }}</strong>
          </article>
          <article>
            <span>{{ 'operationModes.impact.ignored' | translate }}</span>
            <strong>{{ preview.ignoredRemovedDeviceIds.length + preview.ignoredMaintenanceDeviceIds.length }}</strong>
          </article>
        </div>

        <div class="routine-readout">
          @for (routine of selectedMode.internalRoutines; track routine.name + routine.targetId + routine.triggerTime) {
            <article>
              <span>{{ routine.triggerTime }}</span>
              <strong>{{ routine.name }}</strong>
              <p>{{ getInternalRoutineActionLabel(routine) }} - {{ getInternalRoutineTargetName(routine) }}</p>
            </article>
          }
        </div>

        <p class="impact-note">{{ preview.recommendedAction }}</p>
      </section>
    } @else {
      <app-empty-state
        [eyebrow]="'operationModes.impact.title' | translate"
        [title]="'operationModes.impact.emptyTitle' | translate"
        [description]="'operationModes.impact.emptyDescription' | translate"
        [compact]="true"
      />
    }
  </app-section-card>

  <app-modal-form-shell
    [open]="createModalOpen"
    [eyebrow]="'operationModes.form.newMode' | translate"
    [title]="'operationModes.actions.create' | translate"
    [description]="'operationModes.form.description' | translate"
    (closed)="closeCreateModal()"
  >
    <form class="mode-form" (ngSubmit)="createMode()">
      <section class="form-section">
        <header>
          <span>1</span>
          <div>
            <h3>{{ 'operationModes.form.identityTitle' | translate }}</h3>
            <p>{{ 'operationModes.form.identityDescription' | translate }}</p>
          </div>
        </header>

        <div class="form-grid">
          <label class="field">
            <span>{{ 'devices.name' | translate }}</span>
            <input name="name" [(ngModel)]="name" [placeholder]="'operationModes.form.namePlaceholder' | translate" required />
          </label>

          <label class="field field--wide">
            <span>{{ 'devices.form.description' | translate }}</span>
            <textarea
              name="description"
              rows="3"
              [(ngModel)]="description"
              [placeholder]="'operationModes.form.descriptionPlaceholder' | translate"
            ></textarea>
          </label>
        </div>
      </section>

      <section class="form-section">
        <header>
          <span>2</span>
          <div>
            <h3>{{ 'operationModes.form.applicationTitle' | translate }}</h3>
            <p>{{ 'operationModes.form.applicationDescription' | translate }}</p>
          </div>
        </header>

        <div class="form-grid">
          <label class="field">
            <span>{{ 'operationModes.site' | translate }}</span>
            <app-dropdown
              [options]="locationOptions"
              [value]="locationValue"
              [placeholder]="'operationModes.form.selectSite' | translate"
              (valueChange)="selectLocation($event)"
            />
          </label>

          <label class="field">
            <span>{{ 'operationModes.form.applicationTitle' | translate }}</span>
            <app-dropdown
              [options]="applicationOptions"
              [value]="applicationScope"
              [placeholder]="'operationModes.form.applicationTitle' | translate"
              (valueChange)="selectApplication($event)"
            />
          </label>

          @if (applicationScope === 'ROOM') {
            <label class="field field--reveal">
              <span>{{ 'devices.room' | translate }}</span>
              <app-dropdown
                [options]="roomOptions"
                [value]="roomValue"
                [placeholder]="'operationModes.form.selectRoom' | translate"
                (valueChange)="selectRoom($event)"
              />
            </label>
          }
        </div>
      </section>

      <section class="form-section">
        <header>
          <span>3</span>
          <div>
            <h3>{{ 'routines.time' | translate }}</h3>
            <p>{{ modeWindowLabel }}</p>
          </div>
        </header>

        <div class="form-grid">
          <label class="field">
            <span>{{ 'operationModes.form.scheduleType' | translate }}</span>
            <app-dropdown
              [options]="scheduleOptions"
              [value]="schedule"
              [placeholder]="'routines.time' | translate"
              (valueChange)="selectSchedule($event)"
            />
          </label>

          @if (schedule === 'CUSTOM') {
            <div class="field field--reveal">
              <app-time-picker [label]="'nav.home' | translate" [(value)]="startsAt" />
            </div>
            <div class="field field--reveal">
              <app-time-picker [label]="'operationModes.form.end' | translate" [(value)]="endsAt" />
            </div>
          }
        </div>
      </section>

      <section class="form-section">
        <header>
          <span>4</span>
          <div>
            <h3>{{ 'operationModes.form.modeRoutinesTitle' | translate }}</h3>
            <p>{{ 'operationModes.form.modeRoutinesDescription' | translate }}</p>
          </div>
        </header>

        <div class="routine-builder">
          <label class="field">
            <span>{{ 'operationModes.form.label' | translate }}</span>
            <input name="routineName" [(ngModel)]="routineName" [placeholder]="'operationModes.form.routineNamePlaceholder' | translate" />
          </label>

          <label class="field">
            <span>{{ 'routines.form.target' | translate }}</span>
            <app-dropdown
              [options]="routineTargetTypeOptions"
              [value]="routineTargetType"
              [placeholder]="'devices.type' | translate"
              (valueChange)="selectRoutineTargetType($event)"
            />
          </label>

          <label class="field">
            <span>{{ getRoutineTargetTypeLabel() }}</span>
            <app-dropdown
              [options]="routineTargetOptions"
              [value]="routineTargetId ? '' + routineTargetId : null"
              [placeholder]="'operationModes.form.selectTarget' | translate"
              (valueChange)="selectRoutineTarget($event)"
            />
            @if (routineTargetOptions.length === 0) {
              <small class="field__hint">
                {{ 'operationModes.form.noTargetsInScope' | translate }}
              </small>
            }
          </label>

          <label class="field">
            <span>{{ 'routines.action' | translate }}</span>
            <app-dropdown
              [options]="routineActionOptions"
              [value]="routineAction"
              [placeholder]="'routines.action' | translate"
              (valueChange)="selectRoutineAction($event)"
            />
          </label>

          <div class="field routine-builder__time">
            <app-time-picker [label]="'routines.card.time' | translate" [(value)]="routineTime" />
          </div>

          <div class="field routine-builder__action">
            <span aria-hidden="true">{{ 'operationModes.actions.add' | translate }}</span>
            <app-button
              type="button"
              variant="create"
              [block]="true"
              (pressed)="addInternalRoutine()"
            >
              {{ 'operationModes.actions.add' | translate }}
            </app-button>
          </div>
        </div>

        @if (internalRoutines.length === 0) {
          <app-empty-state
            [eyebrow]="'nav.routines' | translate"
            [title]="'operationModes.form.noInternalRoutinesTitle' | translate"
            [description]="'operationModes.form.noInternalRoutinesDescription' | translate"
            [compact]="true"
          />
        } @else {
          <div class="routine-draft-list">
            @for (routine of internalRoutines; track routine.clientId) {
              <article>
                <span class="routine-draft-list__icon" aria-hidden="true">&#9200;</span>

                <div class="routine-draft-list__content">
                  <strong>{{ routine.name }}</strong>
                  <p>{{ getInternalRoutineActionLabel(routine) }} - {{ getInternalRoutineTargetName(routine) }}</p>
                </div>

                <div class="routine-draft-list__time">
                  <span>{{ 'routines.card.time' | translate }}</span>
                  <strong>{{ routine.triggerTime }}</strong>
                </div>

                <app-button
                  class="routine-draft-list__action"
                  variant="danger"
                  size="sm"
                  (pressed)="removeInternalRoutine(routine.clientId)"
                >
                  {{ 'operationModes.actions.remove' | translate }}
                </app-button>
              </article>
            }
          </div>
        }
      </section>

      <section class="form-section">
        <header>
          <span>5</span>
          <div>
            <h3>{{ 'about.capabilities.items.energy.eyebrow' | translate }}</h3>
            <p>{{ 'operationModes.goals.description' | translate }}</p>
          </div>
        </header>

        @if (availableGoals.length === 0) {
          <app-empty-state
            [eyebrow]="'reporting.goals' | translate"
            [title]="'operationModes.goals.emptyTitle' | translate"
            [description]="'operationModes.goals.emptyDescription' | translate"
            [compact]="true"
          />
        } @else {
          <div class="chip-grid">
            @for (goal of availableGoals; track goal.id) {
              <app-button
                type="button"
                size="sm"
                [variant]="selectedGoalIds.has(goal.id) ? 'action' : 'ghost'"
                (pressed)="toggleGoal(goal.id)"
              >
                {{ goal.title }}
              </app-button>
            }
          </div>
        }
      </section>

      <section class="form-section">
        <header>
          <span>6</span>
          <div>
            <h3>{{ 'operationModes.alertsRules.title' | translate }}</h3>
            <p>{{ 'operationModes.alertsRules.description' | translate }}</p>
          </div>
        </header>

        <div class="form-grid">
          <label class="field">
            <span>{{ 'nav.rules' | translate }}</span>
            <app-dropdown
              [options]="ruleOptions"
              [value]="ruleProfileValue"
              [placeholder]="'nav.rules' | translate"
              (valueChange)="selectRuleProfile($event)"
            />
          </label>

          <label class="field">
            <span>{{ 'nav.preferences' | translate }}</span>
            <app-dropdown
              [options]="preferenceOptions"
              [value]="preferenceValue"
              [placeholder]="'nav.preferences' | translate"
              (valueChange)="selectPreference($event)"
            />
          </label>
        </div>

        <div class="toggle-grid">
          <app-setting-toggle [title]="'operationModes.toggles.applyRulesTitle' | translate" [description]="'operationModes.toggles.applyRulesDescription' | translate" [(checked)]="applyRules" />
          <app-setting-toggle [title]="'operationModes.toggles.applyPreferencesTitle' | translate" [description]="'operationModes.toggles.applyPreferencesDescription' | translate" [(checked)]="applyPreferences" />
          <app-setting-toggle [title]="'operationModes.toggles.criticalSoundTitle' | translate" [description]="'operationModes.toggles.criticalSoundDescription' | translate" [(checked)]="preserveCriticalSound" />
        </div>
      </section>

      <footer class="form-actions">
        @if (createDisabledReason) {
          <p class="form-actions__hint">{{ createDisabledReason }}</p>
        }

        <div class="form-actions__buttons">
          <app-button type="button" variant="neutral" (pressed)="closeCreateModal()">{{ 'billing.cancelPayment' | translate }}</app-button>
          <app-button type="submit" variant="create" [disabled]="createDisabled">{{ 'operationModes.actions.create' | translate }}</app-button>
        </div>
      </footer>
    </form>
  </app-modal-form-shell>
</section>


============================================================
FILE: src\app\device-control\presentation\pages\operation-modes\operation-modes-page.component.scss
============================================================

.operation-modes-page {
  display: grid;
  gap: 24px;
}

.mode-summary,
.mode-grid,
.impact-grid,
.form-grid,
.routine-builder,
.toggle-grid {
  display: grid;
  gap: 14px;
}

.mode-summary {
  grid-template-columns: repeat(3, minmax(0, 1fr));
}

.operation-toolbar,
.mode-card__header,
.impact-panel header,
.form-actions__buttons {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.mode-card__header,
.impact-panel header,
.form-actions__buttons {
  gap: 16px;
}

.operation-toolbar {
  gap: 18px;
  padding: 22px;
  border: 1px solid var(--border-color);
  border-radius: 24px;
  background: var(--surface-color);
}

.mode-card h3,
.impact-panel h3,
.form-section h3 {
  margin: 0;
  color: var(--text-primary);
}

.mode-card p,
.impact-panel p,
.form-section p,
.routine-draft-list p {
  margin: 0;
  color: var(--text-secondary);
  font-weight: 750;
  line-height: 1.45;
}

.operation-toolbar h2,
.operation-toolbar p {
  margin: 0;
}

.operation-toolbar h2 {
  color: var(--text-color);
  font-size: 1.3rem;
}

.operation-toolbar p:not(.section-eyebrow) {
  margin-top: 6px;
  color: var(--text-secondary);
  font-weight: 700;
}

.operation-toolbar .section-eyebrow {
  margin: 0 0 6px;
  color: var(--accent-color);
  font-size: 0.74rem;
  font-weight: 900;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.section-eyebrow,
.mode-card__header p,
.impact-grid span,
.form-section header > span,
.field > span,
.routine-draft-list span,
.routine-readout span,
:host ::ng-deep .mode-form .time-picker__label {
  color: var(--accent-color);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.mode-grid {
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
}

.mode-card__body {
  display: grid;
  gap: 14px;
  min-height: 100%;
}

.mode-card__description {
  display: -webkit-box;
  min-height: 42px;
  overflow: hidden;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

.mode-card__facts {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 10px;
  margin: 0;
}

.mode-card__facts div,
.impact-grid article,
.routine-readout article,
.routine-draft-list article {
  border: 1px solid var(--border-color);
  border-radius: 16px;
  background: var(--ec-surface-subtle);
  padding: 12px;
}

.mode-card__facts dt,
.mode-card__facts dd {
  margin: 0;
}

.mode-card__facts dt {
  color: var(--text-secondary);
  font-size: 0.72rem;
  font-weight: 900;
}

.mode-card__facts dd,
.impact-grid strong,
.impact-panel header strong,
.routine-readout strong,
.routine-draft-list strong {
  color: var(--text-primary);
  font-weight: 950;
}

.mode-card__actions {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.impact-panel {
  display: grid;
  gap: 16px;
}

.impact-grid {
  grid-template-columns: repeat(3, minmax(0, 1fr));
}

.routine-readout,
.routine-draft-list {
  display: grid;
  gap: 10px;
}

.routine-readout {
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
}

.impact-note {
  padding: 12px 14px;
  border-radius: 14px;
  background: var(--ec-accent-soft);
}

.mode-form,
.form-section,
.field {
  display: grid;
}

.mode-form {
  gap: 14px;
  --time-picker-trigger-height: 48px;
}

.form-section {
  gap: 14px;
  padding: 16px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background: var(--ec-surface-subtle);
}

.form-section header {
  display: grid;
  grid-template-columns: auto minmax(0, 1fr);
  gap: 12px;
  align-items: start;
}

.form-section header > span {
  display: grid;
  width: 32px;
  height: 32px;
  place-items: center;
  border-radius: 999px;
  background: var(--ec-accent-soft);
}

.form-grid,
.routine-builder {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}

.routine-builder {
  align-items: end;
}

.field {
  gap: 8px;
  min-width: 0;
}

.field__hint,
.form-actions__hint {
  margin: 0;
  color: var(--text-muted);
  font-size: 0.78rem;
  font-weight: 800;
  line-height: 1.35;
}

.field--wide {
  grid-column: span 2;
}

.field--reveal {
  animation: revealField 180ms ease both;
}

.mode-form input,
.mode-form textarea {
  width: 100%;
  min-width: 0;
  border: 1px solid var(--border-color);
  border-radius: 14px;
  background: var(--field-bg);
  color: var(--text-primary);
  font: inherit;
  font-weight: 750;
  padding: 12px 13px;
  outline: none;
}

:host ::ng-deep .routine-builder__action .app-button {
  width: 100%;
}

.routine-draft-list article {
  display: grid;
  grid-template-columns: auto minmax(0, 1fr) auto auto;
  align-items: center;
  gap: 14px;
}

.routine-draft-list__icon,
.routine-draft-list__time {
  display: grid;
  place-items: center;
  border-radius: 14px;
  background: var(--ec-accent-soft);
}

.routine-draft-list__icon {
  width: 42px;
  height: 42px;
  font-size: 1.18rem;
}

.routine-draft-list__time {
  min-width: 74px;
  padding: 8px 10px;
}

.mode-form textarea {
  resize: vertical;
  max-height: 120px;
}

.chip-grid,
.toggle-grid {
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
}

.form-actions {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 14px;
  align-items: center;
  padding-top: 6px;
}

.form-actions__hint {
  justify-self: start;
}

.form-actions__buttons {
  justify-content: flex-end;
}

@keyframes revealField {
  from {
    opacity: 0;
    transform: translateY(-6px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@media (max-width: 1100px) {
  .mode-summary,
  .impact-grid,
  .form-grid {
    grid-template-columns: 1fr;
  }

  .field--wide {
    grid-column: auto;
  }
}

@media (max-width: 720px) {
  .operation-toolbar,
  .mode-card__header,
  .impact-panel header,
  .routine-draft-list article,
  .form-actions__buttons {
    align-items: stretch;
    flex-direction: column;
  }

  .form-actions {
    grid-template-columns: 1fr;
  }

  .mode-card__facts {
    grid-template-columns: 1fr;
  }

  .routine-builder,
  .routine-draft-list article,
  .toggle-grid {
    grid-template-columns: 1fr;
  }
}


============================================================
FILE: src\app\device-control\presentation\pages\operation-modes\operation-modes-page.component.ts
============================================================

import { Component, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { DeviceControlFacade } from '../../../application/services/device-control.facade';
import { CreateOperationModeCommand } from '../../../application/commands/create-operation-mode.command';
import {
  OperationMode,
  OperationModePreview,
  OperationModeRoutine,
  OperationModeRoutineAction,
  OperationModeRoutineTargetType,
} from '../../../domain/model/operation-mode.entity';
import { Device } from '../../../domain/model/device.entity';
import { DeviceGroup } from '../../../domain/model/device-group.entity';
import { WorkplaceFacade } from '../../../../workplace/application/services/workplace.facade';
import { ActiveWorkplaceContextService } from '../../../../workplace/application/services/active-workplace-context.service';
import { ReportingFacade } from '../../../../reporting/application/services/reporting.facade';
import { NotificationsFacade } from '../../../../notifications/application/services/notifications.facade';
import { ConfirmDialogService } from '../../../../shared/application/services/confirm-dialog.service';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { AppTimePickerComponent } from '../../../../shared/presentation/components/app-time-picker/app-time-picker.component';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { ModalFormShellComponent } from '../../../../shared/presentation/components/modal-form-shell/modal-form-shell.component';
import { SectionCardComponent } from '../../../../shared/presentation/components/section-card/section-card.component';
import { SettingToggleComponent } from '../../../../shared/presentation/components/setting-toggle/setting-toggle.component';
import { SeverityBadgeComponent } from '../../../../shared/presentation/components/severity-badge/severity-badge.component';

type ModeApplicationScope = 'WORKPLACE' | 'ROOM';
type ModeSchedule = 'ALL_DAY' | 'CUSTOM';

interface InternalRoutineDraft extends OperationModeRoutine {
  clientId: number;
}

@Component({
  selector: 'app-operation-modes-page',
  standalone: true,
  imports: [
    TranslateModule,
    FormsModule,
    AppButtonComponent,
    AppDropdownComponent,
    AppTimePickerComponent,
    EmptyStateComponent,
    ModalFormShellComponent,
    SectionCardComponent,
    SettingToggleComponent,
    SeverityBadgeComponent,
  ],
  templateUrl: './operation-modes-page.component.html',
  styleUrls: ['./operation-modes-page.component.scss'],
})
export class OperationModesPageComponent implements OnInit {
  name = '';
  description = '';
  locationId: number | null = null;
  applicationScope: ModeApplicationScope = 'WORKPLACE';
  selectedRoomId: number | null = null;
  schedule: ModeSchedule = 'ALL_DAY';
  startsAt = '08:00';
  endsAt = '18:00';
  selectedGoalIds = new Set<number>();
  ruleProfileId: number | null = null;
  preferenceId: number | null = null;
  applyRules = true;
  applyPreferences = true;
  preserveCriticalSound = true;
  createModalOpen = false;
  selectedModeId: number | null = null;
  preview: OperationModePreview | null = null;

  routineName = '';
  routineTargetType: OperationModeRoutineTargetType = 'DEVICE';
  routineTargetId: number | null = null;
  routineAction: OperationModeRoutineAction = 'TURN_ON';
  routineTime = '08:00';
  internalRoutines: InternalRoutineDraft[] = [];

  private nextRoutineClientId = 1;

  readonly applicationOptions: DropdownOption[] = [
    {
      label: '',
      labelKey: 'operationModes.applicationOptions.workplace',
      value: 'WORKPLACE',
      descriptionKey: 'operationModes.applicationDescriptions.workplace',
    },
    {
      label: '',
      labelKey: 'operationModes.applicationOptions.room',
      value: 'ROOM',
      descriptionKey: 'operationModes.applicationDescriptions.room',
    },
  ];

  readonly scheduleOptions: DropdownOption[] = [
    {
      label: '',
      labelKey: 'operationModes.scheduleOptions.allDay',
      value: 'ALL_DAY',
      descriptionKey: 'operationModes.scheduleDescriptions.allDay',
    },
    {
      label: '',
      labelKey: 'operationModes.scheduleOptions.custom',
      value: 'CUSTOM',
      descriptionKey: 'operationModes.scheduleDescriptions.custom',
    },
  ];

  readonly routineTargetTypeOptions: DropdownOption[] = [
    {
      label: '',
      labelKey: 'operationModes.routineTargetTypes.device',
      value: 'DEVICE',
      descriptionKey: 'operationModes.routineTargetDescriptions.device',
    },
    {
      label: '',
      labelKey: 'operationModes.routineTargetTypes.group',
      value: 'GROUP',
      descriptionKey: 'operationModes.routineTargetDescriptions.group',
    },
  ];

  readonly routineActionOptions: DropdownOption[] = [
    {
      label: '',
      labelKey: 'routines.actions.turnOn',
      value: 'TURN_ON',
      descriptionKey: 'operationModes.routineActionDescriptions.turnOn',
    },
    {
      label: '',
      labelKey: 'routines.actions.turnOff',
      value: 'TURN_OFF',
      descriptionKey: 'operationModes.routineActionDescriptions.turnOff',
    },
  ];

  constructor(
    readonly deviceControlFacade: DeviceControlFacade,
    readonly workplaceFacade: WorkplaceFacade,
    readonly reportingFacade: ReportingFacade,
    readonly notificationsFacade: NotificationsFacade,
    private readonly activeWorkplaceContext: ActiveWorkplaceContextService,
    private readonly confirmDialog: ConfirmDialogService,
    private readonly toastService: ToastService,
    private readonly translate: TranslateService
  ) {}

  async ngOnInit(): Promise<void> {
    await Promise.all([
      this.deviceControlFacade.loadDeviceControl(),
      this.workplaceFacade.loadWorkplace(),
      this.reportingFacade.loadReporting(),
      this.notificationsFacade.loadNotifications(),
    ]);

    const activeLocation = this.activeWorkplaceContext.ensureActiveLocation(
      this.workplaceFacade.locations()
    );

    this.locationId = activeLocation?.id ?? null;
  }

  get modes(): OperationMode[] {
    return this.deviceControlFacade.operationModes();
  }

  get modesInActiveWorkplace(): OperationMode[] {
    return this.locationId
      ? this.modes.filter((mode) => mode.locationId === this.locationId)
      : this.modes;
  }

  get activeMode(): OperationMode | null {
    return this.modesInActiveWorkplace.find((mode) => mode.status === 'ACTIVE') ?? null;
  }

  get selectedMode(): OperationMode | null {
    return this.modes.find((mode) => mode.id === this.selectedModeId) ?? null;
  }

  get selectedLocationName(): string {
    if (!this.locationId) {
      return this.t('operationModes.fallbacks.noSelectedSite');
    }

    return this.workplaceFacade.locations().find((location) => location.id === this.locationId)?.name
      ?? this.t('operationModes.fallbacks.siteUnavailable');
  }

  get locationValue(): string | null {
    return this.locationId ? String(this.locationId) : null;
  }

  get locationOptions(): DropdownOption[] {
    return this.workplaceFacade.locations().map((location) => ({
      label: location.name,
      value: String(location.id),
      description: location.address || this.t('operationModes.fallbacks.operationalSite'),
    }));
  }

  get roomValue(): string | null {
    return this.selectedRoomId ? String(this.selectedRoomId) : null;
  }

  get roomOptions(): DropdownOption[] {
    return this.availableRooms.map((room) => ({
      label: room.name,
      value: String(room.id),
      description: this.selectedLocationName,
    }));
  }

  get availableRooms() {
    return this.locationId
      ? this.workplaceFacade.rooms().filter((room) => room.locationId === this.locationId)
      : [];
  }

  get assignedDeviceIds(): Set<number> {
    if (!this.locationId) {
      return new Set<number>();
    }

    return new Set(
      this.workplaceFacade
        .deviceAssignments()
        .filter((assignment) => assignment.locationId === this.locationId)
        .map((assignment) => assignment.deviceId)
    );
  }

  get scopedDeviceIds(): Set<number> {
    if (!this.locationId) {
      return new Set<number>();
    }

    const assignments = this.workplaceFacade.deviceAssignments().filter((assignment) => {
      if (assignment.locationId !== this.locationId) {
        return false;
      }

      return this.applicationScope !== 'ROOM' || this.selectedRoomId === null
        ? true
        : assignment.roomId === this.selectedRoomId;
    });

    return new Set(assignments.map((assignment) => assignment.deviceId));
  }

  get availableDevices(): Device[] {
    const scopedIds = this.scopedDeviceIds;
    return this.deviceControlFacade.devices().filter((device) => scopedIds.has(device.id));
  }

  get availableGroups(): DeviceGroup[] {
    const scopedIds = this.scopedDeviceIds;
    return this.deviceControlFacade
      .deviceGroups()
      .filter((group) => group.deviceIds.length > 0 && group.deviceIds.every((deviceId) => scopedIds.has(deviceId)));
  }

  get routineTargetOptions(): DropdownOption[] {
    if (this.routineTargetType === 'GROUP') {
      return this.availableGroups.map((group) => ({
        label: group.name,
        value: String(group.id),
        description: this.t('operationModes.routineTargets.deviceCount', { count: group.deviceCount }),
      }));
    }

    return this.availableDevices.map((device) => ({
      label: device.name,
      value: String(device.id),
      description: `${device.room || this.t('devices.noRoom')} - ${device.powerWatts} W`,
      disabled: !device.canReceiveOperationalChanges,
    }));
  }

  get availableGoals() {
    const scopedDeviceIds = this.scopedDeviceIds;
    const roomIds = new Set(this.availableRooms.map((room) => room.id));
    const groupIds = new Set(this.availableGroups.map((group) => group.id));

    return this.reportingFacade.energyGoals().filter((goal) => {
      if (goal.scopeType === 'GENERAL') {
        return true;
      }

      if (goal.scopeType === 'WORKPLACE') {
        return goal.scopeId === this.locationId;
      }

      if (goal.scopeType === 'ROOM') {
        return Boolean(goal.scopeId && roomIds.has(goal.scopeId));
      }

      if (goal.scopeType === 'GROUP') {
        return Boolean(goal.scopeId && groupIds.has(goal.scopeId));
      }

      return Boolean(goal.scopeId && scopedDeviceIds.has(goal.scopeId));
    });
  }

  get ruleProfileValue(): string {
    return this.ruleProfileId ? String(this.ruleProfileId) : 'none';
  }

  get ruleOptions(): DropdownOption[] {
    const profiles = this.notificationsFacade.alertRuleProfiles().map((profile) => ({
      label: profile.name,
      value: String(profile.id),
      description: this.t('operationModes.rules.available'),
    }));

    return [
      {
        label: this.t('operationModes.rules.none'),
        value: 'none',
        description: this.t('operationModes.rules.noneDescription'),
      },
      ...profiles,
    ];
  }

  get preferenceValue(): string {
    return this.preferenceId ? String(this.preferenceId) : 'current';
  }

  get preferenceOptions(): DropdownOption[] {
    const currentPreference = this.selectedPreferenceId;

    return [
      {
        label: this.t('operationModes.preferences.current'),
        value: 'current',
        description: currentPreference
          ? this.t('operationModes.preferences.useRegistered')
          : this.t('operationModes.preferences.noneRegistered'),
      },
      ...(currentPreference
        ? [
            {
              label: this.t('operationModes.preferences.numbered', { id: currentPreference }),
              value: String(currentPreference),
              description: this.t('operationModes.preferences.applyThis'),
            },
          ]
        : []),
    ];
  }

  get selectedPreferenceId(): number | null {
    return this.notificationsFacade.notificationPreference()?.id ?? null;
  }

  get createDisabled(): boolean {
    return !this.locationId
      || !this.name.trim()
      || !this.hasRoutineForSubmit
      || (this.applicationScope === 'ROOM' && !this.selectedRoomId);
  }

  get hasCompleteRoutineDraft(): boolean {
    return Boolean(this.routineName.trim())
      && this.routineTargetId !== null
      && Number.isFinite(this.routineTargetId);
  }

  get hasRoutineForSubmit(): boolean {
    return this.internalRoutines.length > 0 || this.hasCompleteRoutineDraft;
  }

  get createDisabledReason(): string {
    if (!this.locationId) {
      return this.t('operationModes.validation.siteRequired');
    }

    if (!this.name.trim()) {
      return this.t('operationModes.validation.nameRequired');
    }

    if (this.applicationScope === 'ROOM' && !this.selectedRoomId) {
      return this.t('operationModes.validation.roomRequired');
    }

    if (!this.hasRoutineForSubmit && this.routineTargetOptions.length === 0) {
      return this.t('operationModes.validation.noTargets');
    }

    if (!this.hasRoutineForSubmit) {
      return this.t('operationModes.validation.routineRequired');
    }

    return '';
  }

  get modeWindowLabel(): string {
    return this.schedule === 'ALL_DAY'
      ? this.t('operationModes.scheduleOptions.allDay')
      : `${this.startsAt} - ${this.endsAt}`;
  }

  openCreateModal(): void {
    this.createModalOpen = true;
  }

  closeCreateModal(): void {
    this.createModalOpen = false;
  }

  selectLocation(value: string): void {
    const nextLocationId = Number(value);
    this.locationId = Number.isFinite(nextLocationId) ? nextLocationId : null;

    if (this.locationId) {
      this.activeWorkplaceContext.setActiveLocation(this.locationId);
    }

    this.selectedRoomId = null;
    this.internalRoutines = [];
    this.routineTargetId = null;
  }

  selectApplication(value: string): void {
    this.applicationScope = value as ModeApplicationScope;
    this.selectedRoomId = null;
    this.internalRoutines = [];
    this.routineTargetId = null;
  }

  selectRoom(value: string): void {
    const roomId = Number(value);
    this.selectedRoomId = Number.isFinite(roomId) ? roomId : null;
    this.internalRoutines = [];
    this.routineTargetId = null;
  }

  selectSchedule(value: string): void {
    this.schedule = value as ModeSchedule;
  }

  selectRoutineTargetType(value: string): void {
    this.routineTargetType = value as OperationModeRoutineTargetType;
    this.routineTargetId = null;
  }

  selectRoutineTarget(value: string): void {
    const targetId = Number(value);
    this.routineTargetId = Number.isFinite(targetId) ? targetId : null;
  }

  selectRoutineAction(value: string): void {
    this.routineAction = value as OperationModeRoutineAction;
  }

  selectRuleProfile(value: string): void {
    this.ruleProfileId = value === 'none' ? null : Number(value);
  }

  selectPreference(value: string): void {
    this.preferenceId = value === 'current' ? null : Number(value);
  }

  toggleGoal(goalId: number): void {
    if (this.selectedGoalIds.has(goalId)) {
      this.selectedGoalIds.delete(goalId);
      return;
    }

    this.selectedGoalIds.add(goalId);
  }

  addInternalRoutine(): void {
    const routine = this.getPendingRoutineDraft();

    if (!routine) {
      this.toastService.warning(this.t('operationModes.validation.routineDraftRequired'));
      return;
    }

    this.internalRoutines = [
      ...this.internalRoutines,
      {
        clientId: this.nextRoutineClientId++,
        ...routine,
      },
    ];

    this.routineName = '';
    this.routineTargetId = null;
  }

  removeInternalRoutine(clientId: number): void {
    this.internalRoutines = this.internalRoutines.filter((routine) => routine.clientId !== clientId);
  }

  async createMode(): Promise<void> {
    if (this.createDisabled || !this.locationId) {
      this.toastService.warning(this.createDisabledReason || this.t('operationModes.validation.minimumRequired'));
      return;
    }

    if (this.applicationScope === 'ROOM' && !this.selectedRoomId) {
      this.toastService.warning(this.t('operationModes.validation.roomRequired'));
      return;
    }

    const internalRoutines = this.buildInternalRoutinesForSubmit();

    const command: CreateOperationModeCommand = {
      locationId: this.locationId,
      name: this.name.trim(),
      description: this.description.trim(),
      roomIds: this.applicationScope === 'ROOM' && this.selectedRoomId ? [this.selectedRoomId] : [],
      groupIds: [],
      deviceIds: [],
      turnOnDeviceIds: [],
      turnOffDeviceIds: [],
      keepOnDeviceIds: [],
      goalIds: Array.from(this.selectedGoalIds),
      internalRoutines,
      allDay: this.schedule === 'ALL_DAY',
      startsAt: this.startsAt,
      endsAt: this.endsAt,
      ruleProfileId: this.ruleProfileId,
      preferenceId: this.preferenceId ?? this.selectedPreferenceId,
      applyRuleProfile: this.applyRules,
      applyNotificationPreference: this.applyPreferences,
      preserveCriticalSound: this.preserveCriticalSound,
    };

    const success = await this.deviceControlFacade.createOperationMode(command);
    if (!success) {
      this.toastService.error(this.t('operationModes.toasts.createError'));
      return;
    }

    this.toastService.success(this.t('operationModes.toasts.createSuccess'));
    this.resetForm();
    this.closeCreateModal();
  }

  async selectMode(mode: OperationMode): Promise<void> {
    this.selectedModeId = mode.id;
    this.preview = await this.deviceControlFacade.previewOperationMode(mode.id);
  }

  async activateMode(mode: OperationMode): Promise<void> {
    const activeMode = this.activeMode;
    if (activeMode && activeMode.id !== mode.id) {
      const accepted = await this.confirmDialog.confirm({
        title: this.t('operationModes.confirm.replaceTitle'),
        message: this.t('operationModes.confirm.replaceMessage'),
        confirmLabel: this.t('routines.enable'),
        cancelLabel: this.t('common.cancel'),
        tone: 'warning',
      });

      if (!accepted) {
        return;
      }
    }

    const result = await this.deviceControlFacade.activateOperationMode(mode.id);
    if (!result) {
      this.toastService.error(this.t('operationModes.toasts.activateError'));
      return;
    }

    this.selectedModeId = mode.id;
    this.preview = await this.deviceControlFacade.previewOperationMode(mode.id);
    this.toastService.info(result.evidence);
  }

  async archiveMode(mode: OperationMode): Promise<void> {
    const accepted = await this.confirmDialog.confirm({
      title: this.t('operationModes.confirm.archiveTitle'),
      message: this.t('operationModes.confirm.archiveMessage', { name: mode.name }),
      confirmLabel: this.t('operationModes.actions.archive'),
      cancelLabel: this.t('common.keep'),
      tone: 'danger',
    });

    if (!accepted) {
      return;
    }

    const success = await this.deviceControlFacade.archiveOperationMode(mode.id);
    if (success) {
      this.selectedModeId = null;
      this.preview = null;
      this.toastService.info(this.t('operationModes.toasts.archived'));
    }
  }

  getModeRoomName(mode: OperationMode): string {
    if (mode.roomIds.length === 0) {
      return this.t('operationModes.applicationOptions.workplace');
    }

    return this.workplaceFacade.rooms().find((room) => room.id === mode.roomIds[0])?.name
      ?? this.t('operationModes.fallbacks.roomUnavailable');
  }

  getModeScheduleLabel(mode: OperationMode): string {
    return mode.allDay ? this.t('operationModes.scheduleOptions.allDay') : `${mode.startsAt} - ${mode.endsAt}`;
  }

  getModeStatusLabel(mode: OperationMode): string {
    const labels: Record<string, string> = {
      ACTIVE: 'operationModes.status.active',
      INACTIVE: 'operationModes.status.inactive',
      DRAFT: 'operationModes.status.inactive',
      ARCHIVED: 'operationModes.status.archived',
    };

    return this.t(labels[mode.status] ?? 'operationModes.status.inactive');
  }

  getRuleProfileName(profileId: number | null): string {
    if (!profileId) {
      return this.t('operationModes.rules.none');
    }

    return this.notificationsFacade.alertRuleProfiles().find((profile) => profile.id === profileId)?.name
      ?? this.t('nav.rules');
  }

  getGoalName(goalId: number): string {
    return this.reportingFacade.energyGoals().find((goal) => goal.id === goalId)?.title
      ?? this.t('operationModes.goals.fallback', { id: goalId });
  }

  getInternalRoutineTargetName(routine: OperationModeRoutine): string {
    if (routine.targetType === 'GROUP') {
      return this.deviceControlFacade.deviceGroups().find((group) => group.id === routine.targetId)?.name
        ?? this.t('operationModes.fallbacks.groupUnavailable');
    }

    return this.deviceControlFacade.devices().find((device) => device.id === routine.targetId)?.name
      ?? this.t('operationModes.fallbacks.deviceUnavailable');
  }

  getInternalRoutineActionLabel(routine: OperationModeRoutine): string {
    return routine.action === 'TURN_ON'
      ? this.t('routines.actions.turnOn')
      : this.t('routines.actions.turnOff');
  }

  getRoutineTargetTypeLabelKey(targetType: OperationModeRoutineTargetType): string {
    return targetType === 'GROUP'
      ? 'operationModes.routineTargetTypes.group'
      : 'operationModes.routineTargetTypes.device';
  }

  getRoutineTargetTypeLabel(): string {
    return this.t(this.getRoutineTargetTypeLabelKey(this.routineTargetType));
  }

  getPreviewDeviceNames(): string[] {
    return this.preview?.affectedDeviceIds.map((deviceId) => this.deviceControlFacade.getDeviceName(deviceId)) ?? [];
  }

  private resetForm(): void {
    this.name = '';
    this.description = '';
    this.applicationScope = 'WORKPLACE';
    this.selectedRoomId = null;
    this.schedule = 'ALL_DAY';
    this.startsAt = '08:00';
    this.endsAt = '18:00';
    this.selectedGoalIds.clear();
    this.ruleProfileId = null;
    this.preferenceId = null;
    this.applyRules = true;
    this.applyPreferences = true;
    this.preserveCriticalSound = true;
    this.routineName = '';
    this.routineTargetType = 'DEVICE';
    this.routineTargetId = null;
    this.routineAction = 'TURN_ON';
    this.routineTime = '08:00';
    this.internalRoutines = [];
  }

  private getPendingRoutineDraft(): OperationModeRoutine | null {
    if (!this.hasCompleteRoutineDraft || this.routineTargetId === null) {
      return null;
    }

    return {
      name: this.routineName.trim(),
      targetType: this.routineTargetType,
      targetId: this.routineTargetId,
      action: this.routineAction,
      triggerTime: this.routineTime,
      enabled: true,
    };
  }

  private buildInternalRoutinesForSubmit(): OperationModeRoutine[] {
    const committedRoutines = this.internalRoutines.map(({ clientId, ...routine }) => routine);
    const pendingRoutine = this.getPendingRoutineDraft();

    return pendingRoutine ? [...committedRoutines, pendingRoutine] : committedRoutines;
  }

  private t(key: string, params?: Record<string, unknown>): string {
    return this.translate.instant(key, params);
  }
}


============================================================
FILE: src\app\device-control\presentation\pages\routines\routines-page.component.html
============================================================

<section class="routines-page ec-page">
  <header class="ec-page-heading">
    <p class="ec-page-eyebrow">{{ 'nav.operation' | translate }}</p>
    <h1 class="ec-page-title">{{ 'nav.routines' | translate }}</h1>
    <p class="ec-page-description">
      {{ 'routines.pageDescription' | translate }}
    </p>
  </header>

  <section class="operation-summary">
    <app-section-card size="small" [eyebrow]="'nav.routines' | translate" [title]="'routines.summary.scheduled' | translate">
      <strong>{{ displayedRoutines.length }}</strong>
      <p>{{ 'routines.summary.inActiveSite' | translate }}</p>
    </app-section-card>

    <app-section-card size="small" [eyebrow]="'history.status' | translate" [title]="'routines.active' | translate">
      <strong>{{ activeRoutineCount }}</strong>
      <p>{{ 'routines.summary.pausedCount' | translate: { count: inactiveRoutineCount } }}</p>
    </app-section-card>

    <app-section-card size="small" [eyebrow]="'routines.time' | translate" [title]="'routines.summary.newRoutine' | translate">
      <strong>{{ time }}</strong>
      <p>{{ targetLabel }} / {{ actionLabel }}</p>
    </app-section-card>
  </section>

  <section class="operation-toolbar">
    <div>
      <p class="section-eyebrow">{{ 'routines.toolbar.eyebrow' | translate }}</p>
      <h2>{{ 'routines.toolbar.title' | translate }}</h2>
      <p>{{ 'routines.toolbar.description' | translate }}</p>
    </div>

    <app-button variant="create" (pressed)="openCreateModal()">
      {{ 'routines.create' | translate }}
    </app-button>
  </section>

  @if (deviceControlFacade.loading() && displayedRoutines.length === 0) {
    <div class="loading-wrapper">
      <app-loading-spinner />
    </div>
  } @else if (displayedRoutines.length === 0) {
    <app-empty-state
      [eyebrow]="'nav.routines' | translate"
      [title]="'routines.emptyScheduledTitle' | translate"
      [description]="'routines.emptyScheduledDescription' | translate"
    />
  } @else {
    <section class="routines-list">
      @for (routine of displayedRoutines; track routine.id) {
        <div
          class="routine-card-shell"
          [class.routine-card-shell--removing]="isRoutineRemoving(routine.id)"
        >
          <app-routine-card
            [routine]="routine"
            [removing]="isRoutineRemoving(routine.id)"
            (toggle)="toggleRoutine($event)"
            (remove)="deleteRoutine($event)"
          />
        </div>
      }
    </section>
  }

  <app-modal-form-shell
    [open]="createModalOpen"
    [eyebrow]="'routines.summary.newRoutine' | translate"
    [title]="'routines.addTitle' | translate"
    [description]="'routines.form.description' | translate"
    (closed)="closeCreateModal()"
  >
    <form class="routine-form" (ngSubmit)="createRoutine()">
      <label class="field">
        <span>{{ 'devices.name' | translate }}</span>
        <input
          type="text"
          name="name"
          [(ngModel)]="name"
          [placeholder]="'routines.form.namePlaceholder' | translate"
          required
        />
      </label>

      <label class="field">
        <span>{{ 'routines.form.target' | translate }}</span>
        <app-dropdown
          [options]="targetTypeOptions"
          [value]="targetType"
          [placeholder]="'routines.form.targetTypePlaceholder' | translate"
          (valueChange)="selectTargetType($event)"
        />
      </label>

      <label class="field">
        <span>{{ targetLabel }}</span>
        <app-dropdown
          [options]="targetOptions"
          [value]="targetValue"
          [placeholder]="targetPlaceholder"
          (valueChange)="selectTarget($event)"
        />
      </label>

      <label class="field">
        <span>{{ 'routines.action' | translate }}</span>
        <app-dropdown
          [options]="cleanActionOptions"
          [value]="action"
          [placeholder]="'routines.action' | translate"
          (valueChange)="selectAction($event)"
        />
      </label>

      <label class="field">
        <span>{{ 'routines.form.repetition' | translate }}</span>
        <app-dropdown
          [options]="repeatOptions"
          [value]="repeatType"
          [placeholder]="'routines.form.repetition' | translate"
          (valueChange)="selectRepeatType($event)"
        />
      </label>

      <div class="field">
        <app-time-picker
          [label]="'routines.form.scheduledTime' | translate"
          [value]="time"
          (valueChange)="selectTime($event)"
        />
      </div>

      @if (repeatType === 'WEEKLY') {
        <section class="field routine-conditional routine-days">
          <span>{{ 'routines.form.days' | translate }}</span>

          <div class="day-picker" [attr.aria-label]="'routines.form.weeklyDaysAria' | translate">
            @for (day of weekDays; track day.value) {
              <app-button
                type="button"
                size="sm"
                [variant]="isDaySelected(day.value) ? 'action' : 'ghost'"
                (pressed)="toggleDay(day.value)"
              >
                {{ day.labelKey | translate }}
              </app-button>
            }
          </div>
        </section>
      }

      @if (repeatType === 'CUSTOM_INTERVAL') {
        <section class="routine-conditional">
          <app-number-stepper
            [label]="'routines.form.everyDays' | translate"
            [suffix]="'routines.form.daysSuffix' | translate"
            [value]="intervalDays"
            [min]="1"
            [step]="1"
            [allowEmpty]="false"
            (valueChange)="onIntervalDaysChange($event)"
          />
        </section>
      }

      @if (repeatType === 'ONCE') {
        <section class="routine-conditional">
          <app-date-picker
            [label]="'history.date' | translate"
            [min]="minStartsOn"
            [(value)]="startsOn"
          />
        </section>
      }

      <div class="form-actions">
        <app-button variant="neutral" (pressed)="closeCreateModal()">{{ 'billing.cancelPayment' | translate }}</app-button>
        <app-button
          type="submit"
          variant="create"
          [disabled]="deviceControlFacade.loading() || !targetId || !name.trim()"
        >
          {{ 'routines.create' | translate }}
        </app-button>
      </div>
    </form>
  </app-modal-form-shell>
</section>


============================================================
FILE: src\app\device-control\presentation\pages\routines\routines-page.component.scss
============================================================

.routines-page {
  display: grid;
  gap: 24px;
}

.operation-summary {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;

  strong {
    display: block;
    margin-bottom: 6px;
    color: var(--text-color);
    font-size: clamp(1.55rem, 3vw, 2.15rem);
    line-height: 1;
  }

  p {
    margin: 0;
    color: var(--text-secondary);
    font-weight: 700;
  }
}

.operation-toolbar {
  display: flex;
  justify-content: space-between;
  gap: 18px;
  align-items: center;
  border: 1px solid var(--border-color);
  border-radius: 24px;
  padding: 22px;
  background: var(--surface-color);

  h2,
  p {
    margin: 0;
  }

  h2 {
    color: var(--text-color);
    font-size: 1.3rem;
  }

  p:not(.section-eyebrow) {
    margin-top: 6px;
    color: var(--text-secondary);
    font-weight: 700;
  }
}

.section-eyebrow {
  margin: 0 0 6px;
  color: var(--accent-color);
  font-size: 0.74rem;
  font-weight: 900;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.routine-form {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
}

.field {
  display: grid;
  gap: 8px;
  min-width: 0;

  span {
    color: var(--text-secondary);
    font-size: 0.8rem;
    font-weight: 900;
  }
}

input {
  width: 100%;
  min-height: 48px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  padding: 13px 14px;
  color: var(--text-primary);
  background: color-mix(in srgb, var(--surface-color) 84%, var(--app-bg));
  box-sizing: border-box;
  font: inherit;
  outline: none;
}

.routine-conditional,
.routine-days,
.form-actions {
  grid-column: 1 / -1;
}

.routine-conditional {
  animation: routine-field-reveal 0.24s cubic-bezier(0.22, 1, 0.36, 1) both;
}

.day-picker {
  display: grid;
  grid-template-columns: repeat(7, minmax(0, 1fr));
  gap: 8px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  padding: 8px;
  background: color-mix(in srgb, var(--surface-color) 90%, var(--app-bg));
}

.form-actions {
  display: flex;
  justify-content: flex-end;
  gap: 12px;
  flex-wrap: wrap;
}

.routines-list {
  display: grid;
  gap: 16px;
}

.routine-card-shell {
  max-height: 520px;
  opacity: 1;
  overflow: hidden;
  transition:
    max-height 0.34s cubic-bezier(0.22, 1, 0.36, 1),
    margin 0.34s cubic-bezier(0.22, 1, 0.36, 1);
}

.routine-card-shell--removing {
  max-height: 0;
  margin-top: -16px;
  transition-delay: 0.42s;
}

.loading-wrapper {
  min-height: 220px;
  display: grid;
  place-items: center;
}

@keyframes routine-field-reveal {
  from {
    opacity: 0;
    transform: translateY(-5px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@media (max-width: 860px) {
  .operation-summary,
  .routine-form {
    grid-template-columns: 1fr;
  }

  .operation-toolbar {
    align-items: flex-start;
    flex-direction: column;
  }

  .day-picker {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}


============================================================
FILE: src\app\device-control\presentation\pages\routines\routines-page.component.ts
============================================================

import { Component, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { DeviceControlFacade } from '../../../application/services/device-control.facade';
import {
  Routine,
  RoutineAction,
  RoutineRepeatType,
  RoutineTargetType,
} from '../../../domain/model/routine.entity';
import { Device, DeviceStatus } from '../../../domain/model/device.entity';
import { WorkplaceFacade } from '../../../../workplace/application/services/workplace.facade';
import { ActiveWorkplaceContextService } from '../../../../workplace/application/services/active-workplace-context.service';

import { RoutineCardComponent } from '../../components/routine-card/routine-card.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppDatePickerComponent } from '../../../../shared/presentation/components/app-date-picker/app-date-picker.component';
import { AppNumberStepperComponent } from '../../../../shared/presentation/components/app-number-stepper/app-number-stepper.component';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { AppTimePickerComponent } from '../../../../shared/presentation/components/app-time-picker/app-time-picker.component';
import { ConfirmDialogService } from '../../../../shared/application/services/confirm-dialog.service';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { ModalFormShellComponent } from '../../../../shared/presentation/components/modal-form-shell/modal-form-shell.component';
import { SectionCardComponent } from '../../../../shared/presentation/components/section-card/section-card.component';
import { RoutineConflictCheckerService } from '../../../domain/services/routine-conflict-checker.service';

type TimePart = 'hour' | 'minute';

@Component({
  selector: 'app-routines-page',
  standalone: true,
  imports: [
    FormsModule,
    TranslateModule,
    AppDropdownComponent,
    AppDatePickerComponent,
    AppNumberStepperComponent,
    AppButtonComponent,
    AppTimePickerComponent,
    EmptyStateComponent,
    ModalFormShellComponent,
    SectionCardComponent,
    RoutineCardComponent,
    LoadingSpinnerComponent,
  ],
  templateUrl: './routines-page.component.html',
  styleUrls: ['./routines-page.component.scss'],
})
export class RoutinesPageComponent implements OnInit {
  name = '';
  targetType: RoutineTargetType = 'DEVICE';
  targetId: number | null = null;
  action: RoutineAction = 'TURN_ON';
  repeatType: RoutineRepeatType = 'DAILY';
  selectedDays = new Set<string>();
  intervalDays = 2;
  startsOn = this.toInputDate(new Date());

  selectedHour = 8;
  selectedMinute = 0;
  createModalOpen = false;
  removingRoutineIds = new Set<number>();

  readonly targetTypeOptions: DropdownOption[] = [
    {
      label: '',
      labelKey: 'routines.targetTypes.device',
      value: 'DEVICE',
      descriptionKey: 'routines.targetTypeDescriptions.device',
    },
    {
      label: '',
      labelKey: 'routines.targetTypes.group',
      value: 'GROUP',
      descriptionKey: 'routines.targetTypeDescriptions.group',
    },
    {
      label: '',
      labelKey: 'routines.targetTypes.room',
      value: 'ROOM',
      descriptionKey: 'routines.targetTypeDescriptions.room',
    },
    {
      label: '',
      labelKey: 'routines.targetTypes.workplace',
      value: 'WORKPLACE',
      descriptionKey: 'routines.targetTypeDescriptions.workplace',
    },
  ];

  readonly cleanActionOptions: DropdownOption[] = [
    {
      label: '',
      labelKey: 'routines.actions.turnOn',
      value: 'TURN_ON',
      descriptionKey: 'routines.actionDescriptions.turnOn',
    },
    {
      label: '',
      labelKey: 'routines.actions.turnOff',
      value: 'TURN_OFF',
      descriptionKey: 'routines.actionDescriptions.turnOff',
    },
  ];

  readonly repeatOptions: DropdownOption[] = [
    {
      label: '',
      labelKey: 'routines.repeat.once',
      value: 'ONCE',
      descriptionKey: 'routines.repeatDescriptions.once',
    },
    {
      label: '',
      labelKey: 'routines.repeat.daily',
      value: 'DAILY',
      descriptionKey: 'routines.repeatDescriptions.daily',
    },
    {
      label: '',
      labelKey: 'routines.repeat.weekly',
      value: 'WEEKLY',
      descriptionKey: 'routines.repeatDescriptions.weekly',
    },
    {
      label: '',
      labelKey: 'routines.repeat.customInterval',
      value: 'CUSTOM_INTERVAL',
      descriptionKey: 'routines.repeatDescriptions.customInterval',
    },
  ];

  readonly weekDays = [
    { label: 'Lun', labelKey: 'routines.weekDays.monShort', nameKey: 'routines.weekDays.mon', value: 'MON' },
    { label: 'Mar', labelKey: 'routines.weekDays.tueShort', nameKey: 'routines.weekDays.tue', value: 'TUE' },
    { label: 'Mie', labelKey: 'routines.weekDays.wedShort', nameKey: 'routines.weekDays.wed', value: 'WED' },
    { label: 'Jue', labelKey: 'routines.weekDays.thuShort', nameKey: 'routines.weekDays.thu', value: 'THU' },
    { label: 'Vie', labelKey: 'routines.weekDays.friShort', nameKey: 'routines.weekDays.fri', value: 'FRI' },
    { label: 'Sab', labelKey: 'routines.weekDays.satShort', nameKey: 'routines.weekDays.sat', value: 'SAT' },
    { label: 'Dom', labelKey: 'routines.weekDays.sunShort', nameKey: 'routines.weekDays.sun', value: 'SUN' },
  ];

  constructor(
    readonly deviceControlFacade: DeviceControlFacade,
    readonly workplaceFacade: WorkplaceFacade,
    private readonly activeWorkplaceContext: ActiveWorkplaceContextService,
    private readonly toastService: ToastService,
    private readonly confirmDialog: ConfirmDialogService,
    private readonly routineConflictChecker: RoutineConflictCheckerService,
    private readonly translate: TranslateService
  ) {}

  async ngOnInit(): Promise<void> {
    await Promise.all([
      this.deviceControlFacade.loadDeviceControl(),
      this.workplaceFacade.loadWorkplace(),
    ]);

    this.activeWorkplaceContext.ensureActiveLocation(this.workplaceFacade.locations());
  }

  get activeRoutineCount(): number {
    return this.displayedRoutines
      .filter((routine) => routine.enabled).length;
  }

  get inactiveRoutineCount(): number {
    return this.displayedRoutines
      .filter((routine) => !routine.enabled).length;
  }

  get formattedHour(): string {
    return this.formatTimePart(this.selectedHour);
  }

  get formattedMinute(): string {
    return this.formatTimePart(this.selectedMinute);
  }

  get time(): string {
    return `${this.formattedHour}:${this.formattedMinute}`;
  }

  get minStartsOn(): string {
    return this.toInputDate(new Date());
  }

  openCreateModal(): void {
    this.createModalOpen = true;
  }

  closeCreateModal(): void {
    this.createModalOpen = false;
  }

  get targetValue(): string | null {
    return this.targetId !== null ? String(this.targetId) : null;
  }

  get targetOptions(): DropdownOption[] {
    if (this.targetType === 'GROUP') {
      return this.availableGroups.map((group) => ({
        label: group.name,
        value: String(group.id),
        description: this.t('routines.targetOptions.deviceCount', { count: group.deviceCount }),
        disabled: !group.hasDevices,
      }));
    }

    if (this.targetType === 'ROOM') {
      return this.availableRooms.map((room) => ({
        label: room.name,
        value: String(room.id),
        description: this.workplaceFacade.getLocationName(room.locationId),
      }));
    }

    if (this.targetType === 'WORKPLACE') {
      return this.availableLocations.map((location) => ({
        label: location.name,
        value: String(location.id),
        description: location.address,
      }));
    }

    return this.availableDevices.map((device) => ({
      label: device.name,
      value: String(device.id),
      description: `${device.room || this.t('devices.noRoom')} - ${device.powerWatts} W - ${this.getDeviceStatusLabel(device)}`,
      disabled: !device.canReceiveOperationalChanges,
    }));
  }

  get activeLocationId(): number | null {
    return this.activeWorkplaceContext.activeLocationId();
  }

  get activeDeviceIds(): Set<number> {
    const locationId = this.activeLocationId;
    if (!locationId) {
      return new Set<number>();
    }

    return new Set(
      this.workplaceFacade
        .deviceAssignments()
        .filter((assignment) => assignment.locationId === locationId)
        .map((assignment) => assignment.deviceId)
    );
  }

  get availableLocations() {
    const locationId = this.activeLocationId;
    return this.workplaceFacade.locations().filter((location) => location.id === locationId);
  }

  get availableRooms() {
    const locationId = this.activeLocationId;
    return this.workplaceFacade.rooms().filter((room) => room.locationId === locationId);
  }

  get availableDevices() {
    const activeDeviceIds = this.activeDeviceIds;
    return this.deviceControlFacade.devices().filter((device) => activeDeviceIds.has(device.id));
  }

  get availableGroups() {
    const activeDeviceIds = this.activeDeviceIds;
    return this.deviceControlFacade
      .deviceGroups()
      .filter((group) => group.deviceIds.some((deviceId) => activeDeviceIds.has(deviceId)));
  }

  get displayedRoutines(): Routine[] {
    const locationId = this.activeLocationId;
    const deviceIds = this.activeDeviceIds;
    const roomIds = new Set(this.availableRooms.map((room) => room.id));
    const groupIds = new Set(this.availableGroups.map((group) => group.id));

    return this.deviceControlFacade.routines().filter((routine) => {
      if (!locationId) {
        return false;
      }

      if (routine.targetType === 'WORKPLACE') {
        return routine.targetId === locationId;
      }

      if (routine.targetType === 'ROOM') {
        return roomIds.has(routine.targetId);
      }

      if (routine.targetType === 'GROUP') {
        return Boolean(routine.groupId && groupIds.has(routine.groupId)) || groupIds.has(routine.targetId);
      }

      return Boolean(routine.deviceId && deviceIds.has(routine.deviceId)) || deviceIds.has(routine.targetId);
    });
  }

  get targetPlaceholder(): string {
    const placeholders: Record<RoutineTargetType, string> = {
      DEVICE: 'routines.placeholders.device',
      GROUP: 'routines.placeholders.group',
      ROOM: 'routines.placeholders.room',
      WORKPLACE: 'routines.placeholders.workplace',
    };

    return this.t(placeholders[this.targetType]);
  }

  get targetLabel(): string {
    return this.t(this.targetLabelKey);
  }

  get targetLabelKey(): string {
    const labels: Record<RoutineTargetType, string> = {
      DEVICE: 'routines.targetTypes.device',
      GROUP: 'routines.targetTypes.group',
      ROOM: 'routines.targetTypes.room',
      WORKPLACE: 'routines.targetTypes.workplace',
    };

    return labels[this.targetType];
  }

  get actionLabel(): string {
    return this.t(this.actionLabelKey);
  }

  get actionLabelKey(): string {
    return this.action === 'TURN_ON'
      ? 'routines.actions.turnOn'
      : 'routines.actions.turnOff';
  }

  getDeviceStatusLabel(device: Device): string {
    const labels: Record<DeviceStatus, string> = {
      ON: 'devices.status.on',
      OFF: 'devices.status.off',
      MAINTENANCE: 'devices.status.maintenance',
      REMOVED: 'devices.status.removed',
    };

    return this.t(labels[device.status] ?? 'devices.status.unknown');
  }

  selectTargetType(value: string): void {
    this.targetType = value as RoutineTargetType;
    this.targetId = null;
  }

  selectTarget(value: string): void {
    this.targetId = Number(value);
  }

  selectAction(value: string): void {
    this.action = value as RoutineAction;
  }

  selectRepeatType(value: string): void {
    this.repeatType = value as RoutineRepeatType;
  }

  selectTime(value: string): void {
    const [hour, minute] = value.split(':').map(Number);

    if (Number.isFinite(hour) && hour >= 0 && hour <= 23) {
      this.selectedHour = hour;
    }

    if (Number.isFinite(minute) && minute >= 0 && minute <= 59) {
      this.selectedMinute = minute;
    }
  }

  onIntervalDaysChange(value: number | null): void {
    this.intervalDays = Math.max(1, value ?? 1);
  }

  toggleDay(day: string): void {
    if (this.selectedDays.has(day)) {
      this.selectedDays.delete(day);
      return;
    }

    this.selectedDays.add(day);
  }

  isDaySelected(day: string): boolean {
    return this.selectedDays.has(day);
  }

  increaseHour(): void {
    this.selectedHour = this.selectedHour === 23 ? 0 : this.selectedHour + 1;
  }

  decreaseHour(): void {
    this.selectedHour = this.selectedHour === 0 ? 23 : this.selectedHour - 1;
  }

  increaseMinute(): void {
    this.selectedMinute = this.selectedMinute === 59 ? 0 : this.selectedMinute + 1;
  }

  decreaseMinute(): void {
    this.selectedMinute = this.selectedMinute === 0 ? 59 : this.selectedMinute - 1;
  }

  handleTimeKeydown(event: KeyboardEvent, part: TimePart): void {
    const allowedNavigationKeys = [
      'Tab',
      'ArrowLeft',
      'ArrowRight',
      'Home',
      'End',
    ];

    if (allowedNavigationKeys.includes(event.key)) {
      return;
    }

    if (event.key === 'ArrowUp') {
      event.preventDefault();
      part === 'hour' ? this.increaseHour() : this.increaseMinute();
      return;
    }

    if (event.key === 'ArrowDown') {
      event.preventDefault();
      part === 'hour' ? this.decreaseHour() : this.decreaseMinute();
      return;
    }

    if (event.key === 'Backspace' || event.key === 'Delete') {
      event.preventDefault();
      return;
    }

    if (!/^\d$/.test(event.key)) {
      event.preventDefault();
      return;
    }

    event.preventDefault();

    this.pushTimeDigit(part, event.key);
  }

  selectTimeInputContent(event: FocusEvent): void {
    const input = event.target as HTMLInputElement;
    input.select();
  }

  async createRoutine(): Promise<void> {
    if (!this.activeLocationId) {
      this.toastService.warning(this.t('routines.validation.activeSiteRequired'));
      return;
    }

    if (!this.targetId || !this.name.trim()) {
      return;
    }

    if (this.repeatType === 'WEEKLY' && this.selectedDays.size === 0) {
      this.toastService.warning(this.t('routines.validation.weeklyDayRequired'));
      return;
    }

    const hasConflict = this.routineConflictChecker.hasConflict(this.displayedRoutines, {
      action: this.action,
      time: this.time,
      targetType: this.targetType,
      targetId: this.targetId,
    });

    if (hasConflict) {
      const accepted = await this.confirmDialog.confirm({
        title: this.t('routines.conflict.title'),
        message: this.t('routines.conflict.message'),
        confirmLabel: this.t('common.continue'),
        cancelLabel: this.t('common.cancel'),
        tone: 'warning',
      });

      if (!accepted) {
        return;
      }
    }

    const success = await this.deviceControlFacade.createRoutine({
      name: this.name.trim(),
      action: this.action,
      time: this.time,
      targetType: this.targetType,
      targetId: this.targetId,
      deviceId: this.targetType === 'DEVICE' ? this.targetId : null,
      groupId: this.targetType === 'GROUP' ? this.targetId : null,
      repeatType: this.repeatType,
      daysOfWeek: this.repeatType === 'WEEKLY'
        ? Array.from(this.selectedDays).join(',')
        : null,
      intervalDays: this.repeatType === 'CUSTOM_INTERVAL'
        ? Math.max(1, Number(this.intervalDays) || 1)
        : 1,
      startsOn: this.repeatType === 'ONCE' ? this.startsOn : null,
    });

    if (success) {
      this.name = '';
      this.targetType = 'DEVICE';
      this.targetId = null;
      this.action = 'TURN_ON';
      this.repeatType = 'DAILY';
      this.selectedDays.clear();
      this.intervalDays = 2;
      this.startsOn = this.toInputDate(new Date());
      this.selectedHour = 8;
      this.selectedMinute = 0;
      this.closeCreateModal();
      this.toastService.success(this.t('routines.createOperationalSuccess'));
    }
  }

  async toggleRoutine(routine: Routine): Promise<void> {
    const viewport = this.captureViewport();
    this.stabilizeViewport(viewport);

    await this.deviceControlFacade.updateRoutineStatus({
      routineId: routine.id,
      enabled: !routine.enabled,
    });

    this.stabilizeViewport(viewport);
    this.toastService.info(
      routine.enabled
        ? this.t('routines.toasts.paused')
        : this.t('routines.toasts.activated')
    );
  }

  async deleteRoutine(routineId: number): Promise<void> {
    if (this.removingRoutineIds.has(routineId)) {
      return;
    }

    const routine = this.deviceControlFacade.routines().find((item) => item.id === routineId);
    const confirmed = await this.confirmDialog.confirm({
      title: this.t('routines.delete.title'),
      message: this.t('routines.delete.message', {
        name: routine?.name ?? this.t('routines.delete.fallbackName'),
      }),
      confirmLabel: this.t('common.delete'),
      cancelLabel: this.t('common.keep'),
      tone: 'danger',
    });

    if (!confirmed) {
      return;
    }

    const viewport = this.captureViewport();
    this.markRoutineAsRemoving(routineId, true);
    this.stabilizeViewport(viewport);
    await this.delay(760);

    const success = await this.deviceControlFacade.deleteRoutine(routineId);
    this.stabilizeViewport(viewport);

    if (success) {
      this.toastService.info(this.t('routines.toasts.deleted'));
      return;
    }

    this.markRoutineAsRemoving(routineId, false);
    this.toastService.error(this.t('routines.deleteError'));
  }

  isRoutineRemoving(routineId: number): boolean {
    return this.removingRoutineIds.has(routineId);
  }

  private pushTimeDigit(part: TimePart, digit: string): void {
    const maxValue = part === 'hour' ? 23 : 59;
    const currentValue = part === 'hour' ? this.selectedHour : this.selectedMinute;

    const currentText = this.formatTimePart(currentValue);
    const candidateText = `${currentText.at(-1) ?? '0'}${digit}`;
    const candidateValue = Number(candidateText);

    const nextValue =
      candidateValue <= maxValue
        ? candidateValue
        : Number(digit);

    if (part === 'hour') {
      this.selectedHour = nextValue;
      return;
    }

    this.selectedMinute = nextValue;
  }

  private formatTimePart(value: number): string {
    return String(value).padStart(2, '0');
  }

  private toInputDate(date: Date): string {
    const year = date.getFullYear();
    const month = String(date.getMonth() + 1).padStart(2, '0');
    const day = String(date.getDate()).padStart(2, '0');

    return `${year}-${month}-${day}`;
  }

  private markRoutineAsRemoving(routineId: number, removing: boolean): void {
    const next = new Set(this.removingRoutineIds);

    if (removing) {
      next.add(routineId);
    } else {
      next.delete(routineId);
    }

    this.removingRoutineIds = next;
  }

  private delay(milliseconds: number): Promise<void> {
    return new Promise((resolve) => window.setTimeout(resolve, milliseconds));
  }

  private captureViewport(): { x: number; y: number } | null {
    if (typeof window === 'undefined') {
      return null;
    }

    return {
      x: window.scrollX,
      y: window.scrollY,
    };
  }

  private stabilizeViewport(viewport: { x: number; y: number } | null): void {
    if (!viewport || typeof window === 'undefined') {
      return;
    }

    const restore = () => {
      if (
        Math.abs(window.scrollX - viewport.x) > 1 ||
        Math.abs(window.scrollY - viewport.y) > 1
      ) {
        window.scrollTo(viewport.x, viewport.y);
      }
    };

    requestAnimationFrame(restore);
    window.setTimeout(restore, 48);
    window.setTimeout(restore, 140);
  }

  private t(key: string, params?: Record<string, unknown>): string {
    return this.translate.instant(key, params);
  }
}


============================================================
FILE: src\app\device-control\presentation\routes\device-control.routes.ts
============================================================

import { Routes } from '@angular/router';

import { activeSubscriptionGuard } from '../../../shared/application/guards/active-subscription.guard';

export const DEVICE_CONTROL_ROUTES: Routes = [
  {
    path: 'operation/devices',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Dispositivos' },
    loadComponent: () =>
      import('../pages/devices/devices-page.component').then(
        (m) => m.DevicesPageComponent
      ),
  },
  {
    path: 'operation/groups',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Grupos' },
    loadComponent: () =>
      import('../pages/device-groups/device-groups-page.component').then(
        (m) => m.DeviceGroupsPageComponent
      ),
  },
  {
    path: 'operation/routines',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Rutinas' },
    loadComponent: () =>
      import('../pages/routines/routines-page.component').then(
        (m) => m.RoutinesPageComponent
      ),
  },
  {
    path: 'operation/modes',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Modos' },
    loadComponent: () =>
      import('../pages/operation-modes/operation-modes-page.component').then(
        (m) => m.OperationModesPageComponent
      ),
  },
  {
    path: 'operation',
    redirectTo: 'operation/devices',
    pathMatch: 'full',
  },
  {
    path: 'devices',
    redirectTo: 'operation/devices',
    pathMatch: 'full',
  },
  {
    path: 'routines',
    redirectTo: 'operation/routines',
    pathMatch: 'full',
  },
  {
    path: 'operation-modes',
    redirectTo: 'operation/modes',
    pathMatch: 'full',
  },
  {
    path: 'device-groups',
    redirectTo: 'operation/groups',
    pathMatch: 'full',
  },
];


============================================================
FILE: src\app\energy-monitoring\application\commands\export-energy-readings.command.ts
============================================================

import { EnergyReading } from '../../domain/model/energy-reading.entity';

export interface ExportEnergyReadingsCommand {
  fileName: string;
  readings?: EnergyReading[];
}


============================================================
FILE: src\app\energy-monitoring\application\criteria\energy-readings-filter.criteria.ts
============================================================

export interface EnergyReadingsFilterCriteria {
  startDate: string;
  endDate: string;
}


============================================================
FILE: src\app\energy-monitoring\application\services\energy-monitoring.facade.ts
============================================================

import { Injectable } from '@angular/core';
import { firstValueFrom } from 'rxjs';

import { BillingFacade } from '../../../billing/application/services/billing.facade';
import { PlanPermissionService } from '../../../billing/domain/services/plan-permission.service';

import { ExportEnergyReadingsCommand } from '../commands/export-energy-readings.command';
import { EnergyReadingsFilterCriteria } from '../criteria/energy-readings-filter.criteria';

import { EnergyReadingsApiService } from '../../infrastructure/api/energy-readings-api.service';
import { EnergyReadingAssembler } from '../../infrastructure/assemblers/energy-reading.assembler';

import { EnergyMonitoringStore } from '../stores/energy-monitoring.store';

@Injectable({
  providedIn: 'root',
})
export class EnergyMonitoringFacade {
  private readonly energyReadingAssembler = new EnergyReadingAssembler();

  private toInputDate(date: Date): string {
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');

  return `${year}-${month}-${day}`;
}

  get readings() {
    return this.store.readings;
  }

  get filteredReadings() {
    return this.store.filteredReadings;
  }

  get visibleReadings() {
    return this.store.visibleReadings;
  }

  get dashboardSummary() {
    return this.store.dashboardSummary;
  }

  get loading() {
    return this.store.loading;
  }

  get error() {
    return this.store.error;
  }

  get totalWatts() {
    return this.store.totalWatts;
  }

  get averageWatts() {
    return this.store.averageWatts;
  }

  get highestReading() {
    return this.store.highestReading;
  }

  get highReadingsCount() {
    return this.store.highReadingsCount;
  }

  get normalReadingsCount() {
    return this.store.normalReadingsCount;
  }

  get recommendationKey() {
    return this.store.recommendationKey;
  }

  get groupedByDevice() {
    return this.store.groupedByDevice;
  }

  constructor(
    private readonly energyReadingsApi: EnergyReadingsApiService,
    private readonly billingFacade: BillingFacade,
    private readonly planPermissionService: PlanPermissionService,
    private readonly store: EnergyMonitoringStore
  ) {}

  async loadReadings(showLoading = true): Promise<void> {
    if (showLoading) {
      this.startRequest();
    } else {
      this.store.setError(null);
    }

    try {
      const responses = await firstValueFrom(
        this.energyReadingsApi.findAllForCurrentUser()
      );
      const dashboardSummary = await firstValueFrom(
        this.energyReadingsApi.getDashboardSummary()
      );

      const readings = responses
        .map((response) => this.energyReadingAssembler.toEntity(response))
        .sort(
          (first, second) =>
            new Date(second.recordedAt).getTime() -
            new Date(first.recordedAt).getTime()
        );

      this.store.setReadings(readings);
      this.store.resetFilter();
      this.store.setDashboardSummary(dashboardSummary);
    } catch (error) {
      console.error(error);
      this.store.setError('energy.loadError');
    } finally {
      if (showLoading) {
        this.finishRequest();
      }
    }
  }

  async loadSamplingSeconds(): Promise<number> {
    const response = await firstValueFrom(
      this.energyReadingsApi.getSamplingSettings()
    );

    return response.sampleSeconds;
  }

  async updateSamplingSeconds(sampleSeconds: number): Promise<number> {
    const response = await firstValueFrom(
      this.energyReadingsApi.updateSamplingSettings(sampleSeconds)
    );

    await this.loadReadings(false);
    return response.sampleSeconds;
  }

  async filterByDateRange(criteria: EnergyReadingsFilterCriteria): Promise<boolean> {
  this.startRequest();

  try {
    if (!criteria.startDate || !criteria.endDate) {
      this.store.setFilteredReadings([]);
      this.store.setError('Selecciona una fecha inicial y una fecha final.');
      return false;
    }

    if (criteria.startDate > criteria.endDate) {
      this.store.setFilteredReadings([]);
      this.store.setError('La fecha inicial no puede ser mayor que la fecha final.');
      return false;
    }

    const today = this.toInputDate(new Date());

    if (criteria.startDate > today || criteria.endDate > today) {
      this.store.setFilteredReadings([]);
      this.store.setError('No puedes filtrar fechas futuras.');
      return false;
    }

    const responses = await firstValueFrom(
      this.energyReadingsApi.findCurrentUserByDateRange(
        criteria.startDate,
        criteria.endDate
      )
    );

    const readings = responses
      .map((response) => this.energyReadingAssembler.toEntity(response))
      .sort(
        (first, second) =>
          new Date(second.recordedAt).getTime() -
          new Date(first.recordedAt).getTime()
      );

    this.store.setFilteredReadings(readings);
    this.store.setError(null);

    return true;
  } catch (error) {
    console.error(error);
    this.store.setFilteredReadings([]);
    this.store.setError('No se pudieron filtrar las lecturas.');
    return false;
  } finally {
    this.finishRequest();
  }
}

  resetFilter(): void {
    this.store.resetFilter();
    this.store.setError(null);
  }

  exportCsv(command: ExportEnergyReadingsCommand): boolean {
    this.store.setError(null);

    const activePlanCode = this.billingFacade.activePlanCode();

    const canExport = this.planPermissionService.canExportCsv(activePlanCode);

    if (!canExport) {
      this.store.setError('energy.exportNotAllowed');
      return false;
    }

    const readings = command.readings ?? this.store.visibleReadings();

    if (readings.length === 0) {
      this.store.setError('energy.emptyReadings');
      return false;
    }

    const header = [
      'id',
      'userId',
      'deviceId',
      'deviceName',
      'watts',
      'kilowattHours',
      'estimatedCost',
      'sampleSeconds',
      'recordedAt',
      'status',
    ];

    const rows = readings.map((reading) => [
      reading.id,
      reading.userId,
      reading.deviceId,
      reading.deviceName,
      reading.watts,
      reading.kilowattHours,
      reading.estimatedCost,
      reading.sampleSeconds,
      reading.recordedAt,
      reading.status,
    ]);

    const csvContent = [header, ...rows]
      .map((row) => row.map((value) => `"${String(value)}"`).join(','))
      .join('\n');

    const blob = new Blob([csvContent], {
      type: 'text/csv;charset=utf-8',
    });

    const url = URL.createObjectURL(blob);
    const anchor = document.createElement('a');

    anchor.href = url;
    anchor.download = command.fileName.endsWith('.csv')
      ? command.fileName
      : `${command.fileName}.csv`;

    anchor.click();
    URL.revokeObjectURL(url);

    return true;
  }

  clearMessages(): void {
    this.store.clearMessages();
  }

  private startRequest(): void {
    this.store.setLoading(true);
    this.store.clearMessages();
  }

  private finishRequest(): void {
    this.store.setLoading(false);
  }
}


============================================================
FILE: src\app\energy-monitoring\application\stores\energy-monitoring.store.ts
============================================================

import { Injectable, computed, signal } from '@angular/core';

import { EnergyReading } from '../../domain/model/energy-reading.entity';
import { EnergyRecommendationService } from '../../domain/services/energy-recommendation.service';
import { EnergyStatisticsService } from '../../domain/services/energy-statistics.service';
import { EnergyDashboardSummaryResponse } from '../../infrastructure/responses/energy-dashboard-summary.response';

@Injectable({
  providedIn: 'root',
})
export class EnergyMonitoringStore {
  private readonly readingsSignal = signal<EnergyReading[]>([]);
  private readonly filteredReadingsSignal = signal<EnergyReading[]>([]);
  private readonly filterActiveSignal = signal<boolean>(false);
  
  private readonly dashboardSummarySignal =
    signal<EnergyDashboardSummaryResponse | null>(null);

  private readonly loadingSignal = signal<boolean>(false);
  private readonly errorSignal = signal<string | null>(null);

  readonly readings = computed(() => this.readingsSignal());
  readonly filteredReadings = computed(() => this.filteredReadingsSignal());
  readonly dashboardSummary = computed(() => this.dashboardSummarySignal());

  readonly loading = computed(() => this.loadingSignal());
  readonly error = computed(() => this.errorSignal());

  readonly visibleReadings = computed(() => {
  return this.filterActiveSignal()
    ? this.filteredReadingsSignal()
    : this.readingsSignal();
});

  readonly totalWatts = computed(() =>
    this.statisticsService.calculateTotalWatts(this.visibleReadings())
  );

  readonly averageWatts = computed(() =>
    this.statisticsService.calculateAverageWatts(this.visibleReadings())
  );

  readonly highestReading = computed(() =>
    this.statisticsService.findHighestReading(this.visibleReadings())
  );

  readonly highReadingsCount = computed(() =>
    this.statisticsService.countHighReadings(this.visibleReadings())
  );

  readonly normalReadingsCount = computed(() =>
    this.statisticsService.countNormalReadings(this.visibleReadings())
  );

  readonly recommendationKey = computed(() =>
    this.recommendationService.getRecommendationKey(this.visibleReadings())
  );

  readonly groupedByDevice = computed(() =>
    this.statisticsService.groupByDevice(this.visibleReadings())
  );

  constructor(
    private readonly statisticsService: EnergyStatisticsService,
    private readonly recommendationService: EnergyRecommendationService
  ) {}

  setReadings(value: EnergyReading[]): void {
    this.readingsSignal.set(value);
  }

  setFilteredReadings(value: EnergyReading[]): void {
  this.filteredReadingsSignal.set(value);
  this.filterActiveSignal.set(true);
}

  setDashboardSummary(value: EnergyDashboardSummaryResponse | null): void {
    this.dashboardSummarySignal.set(value);
  }

  resetFilter(): void {
  this.filteredReadingsSignal.set([]);
  this.filterActiveSignal.set(false);
}
  setLoading(value: boolean): void {
    this.loadingSignal.set(value);
  }

  setError(value: string | null): void {
    this.errorSignal.set(value);
  }

  clearMessages(): void {
    this.errorSignal.set(null);
  }

  reset(): void {
    this.readingsSignal.set([]);
    this.filteredReadingsSignal.set([]);
    this.dashboardSummarySignal.set(null);
    this.loadingSignal.set(false);
    this.errorSignal.set(null);
  }
}


============================================================
FILE: src\app\energy-monitoring\domain\model\energy-reading.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export type EnergyReadingStatus = 'NORMAL' | 'HIGH';

export class EnergyReading extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _deviceId: number;
  private readonly _deviceName: string;
  private readonly _watts: number;
  private readonly _kilowattHours: number;
  private readonly _estimatedCost: number;
  private readonly _sampleSeconds: number;
  private readonly _recordedAt: string;
  private readonly _status: EnergyReadingStatus;

  constructor(props: {
    id: number;
    userId: number;
    deviceId: number;
    deviceName: string;
    watts: number;
    kilowattHours?: number;
    estimatedCost?: number;
    sampleSeconds?: number;
    recordedAt: string;
    status: EnergyReadingStatus;
  }) {
    super(props.id);
    this._userId = props.userId;
    this._deviceId = props.deviceId;
    this._deviceName = props.deviceName;
    this._watts = props.watts;
    this._kilowattHours = props.kilowattHours ?? 0;
    this._estimatedCost = props.estimatedCost ?? 0;
    this._sampleSeconds = props.sampleSeconds ?? 0;
    this._recordedAt = props.recordedAt;
    this._status = props.status;
  }

  get userId(): number {
    return this._userId;
  }

  get deviceId(): number {
    return this._deviceId;
  }

  get deviceName(): string {
    return this._deviceName;
  }

  get watts(): number {
    return this._watts;
  }

  get kilowattHours(): number {
    return this._kilowattHours;
  }

  get estimatedCost(): number {
    return this._estimatedCost;
  }

  get sampleSeconds(): number {
    return this._sampleSeconds;
  }

  get recordedAt(): string {
    return this._recordedAt;
  }

  get status(): EnergyReadingStatus {
    return this._status;
  }

  get isHigh(): boolean {
    return this._status === 'HIGH';
  }

  get isNormal(): boolean {
    return this._status === 'NORMAL';
  }
}

============================================================
FILE: src\app\energy-monitoring\domain\services\energy-recommendation.service.ts
============================================================

import { Injectable } from '@angular/core';

import { EnergyReading } from '../model/energy-reading.entity';
import { EnergyStatisticsService } from './energy-statistics.service';

@Injectable({
  providedIn: 'root',
})
export class EnergyRecommendationService {
  constructor(private readonly statisticsService: EnergyStatisticsService) {}

  getRecommendationKey(readings: EnergyReading[]): string {
    if (readings.length === 0) {
      return 'energy.noDataRecommendation';
    }

    const average = this.statisticsService.calculateAverageWatts(readings);
    const highReadings = this.statisticsService.countHighReadings(readings);

    if (average >= 1000 || highReadings >= 5) {
      return 'energy.highRecommendation';
    }

    if (average >= 500 || highReadings >= 2) {
      return 'energy.moderateRecommendation';
    }

    return 'energy.lowRecommendation';
  }
}

============================================================
FILE: src\app\energy-monitoring\domain\services\energy-statistics.service.ts
============================================================

import { Injectable } from '@angular/core';

import { EnergyReading } from '../model/energy-reading.entity';

@Injectable({
  providedIn: 'root',
})
export class EnergyStatisticsService {
  calculateTotalWatts(readings: EnergyReading[]): number {
    return readings.reduce((total, reading) => total + reading.watts, 0);
  }

  calculateAverageWatts(readings: EnergyReading[]): number {
    if (readings.length === 0) return 0;

    const total = this.calculateTotalWatts(readings);
    return Number((total / readings.length).toFixed(2));
  }

  findHighestReading(readings: EnergyReading[]): EnergyReading | null {
    if (readings.length === 0) return null;

    return readings.reduce((highest, current) =>
      current.watts > highest.watts ? current : highest
    );
  }

  countHighReadings(readings: EnergyReading[]): number {
    return readings.filter((reading) => reading.isHigh).length;
  }

  countNormalReadings(readings: EnergyReading[]): number {
    return readings.filter((reading) => reading.isNormal).length;
  }

  groupByDevice(readings: EnergyReading[]): Record<string, number> {
    return readings.reduce<Record<string, number>>((result, reading) => {
      result[reading.deviceName] = (result[reading.deviceName] ?? 0) + reading.watts;
      return result;
    }, {});
  }
}

============================================================
FILE: src\app\energy-monitoring\infrastructure\api\energy-readings-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { EnergyReading } from '../../domain/model/energy-reading.entity';
import { EnergyReadingAssembler } from '../assemblers/energy-reading.assembler';
import { EnergyReadingResource } from '../resources/energy-reading.resource';
import { EnergyDashboardSummaryResponse } from '../responses/energy-dashboard-summary.response';
import { EnergyReadingResponse } from '../responses/energy-reading.response';
import { EnergySamplingSettingsResponse } from '../responses/energy-sampling-settings.response';

@Injectable({
  providedIn: 'root',
})
export class EnergyReadingsApiService extends BaseApiService<
  EnergyReading,
  EnergyReadingResource,
  EnergyReadingResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(http, apiBaseUrl, 'energy-readings', new EnergyReadingAssembler());
  }

  findAllForCurrentUser(): Observable<EnergyReadingResponse[]> {
    return this.http.get<EnergyReadingResponse[]>(this.resourceEndpoint);
  }

  getDashboardSummary(): Observable<EnergyDashboardSummaryResponse> {
    return this.http.get<EnergyDashboardSummaryResponse>(
      `${this.resourceEndpoint}/dashboard-summary`
    );
  }

  getSamplingSettings(): Observable<EnergySamplingSettingsResponse> {
    return this.http.get<EnergySamplingSettingsResponse>(
      `${this.resourceEndpoint}/sampling-settings`
    );
  }

  updateSamplingSettings(sampleSeconds: number): Observable<EnergySamplingSettingsResponse> {
    return this.http.patch<EnergySamplingSettingsResponse>(
      `${this.resourceEndpoint}/sampling-settings`,
      { sampleSeconds }
    );
  }

  findCurrentUserByDateRange(
    startDate: string,
    endDate: string
  ): Observable<EnergyReadingResponse[]> {
    return this.http.get<EnergyReadingResponse[]>(`${this.resourceEndpoint}?recordedAt_gte=${startDate}&recordedAt_lte=${endDate}`);
  }
}


============================================================
FILE: src\app\energy-monitoring\infrastructure\assemblers\energy-reading.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import {
  EnergyReading,
  EnergyReadingStatus,
} from '../../domain/model/energy-reading.entity';
import { EnergyReadingResource } from '../resources/energy-reading.resource';
import { EnergyReadingResponse } from '../responses/energy-reading.response';

export class EnergyReadingAssembler extends BaseAssembler<
  EnergyReading,
  EnergyReadingResource,
  EnergyReadingResponse
> {
  override toEntity(response: EnergyReadingResponse): EnergyReading {
    const watts = Number(response.watts ?? 0);

    const status: EnergyReadingStatus =
      response.status ?? (watts >= 1800 ? 'HIGH' : 'NORMAL');

    return new EnergyReading({
      id: response.id,
      userId: response.userId,
      deviceId: response.deviceId,
      deviceName: response.deviceName,
      watts,
      kilowattHours: Number(response.kilowattHours ?? 0),
      estimatedCost: Number(response.estimatedCost ?? 0),
      sampleSeconds: Number(response.sampleSeconds ?? 0),
      recordedAt: response.recordedAt,
      status,
    });
  }

  override toResource(entity: EnergyReading): EnergyReadingResource {
    return {
      userId: entity.userId,
      deviceId: entity.deviceId,
      deviceName: entity.deviceName,
      watts: entity.watts,
      kilowattHours: entity.kilowattHours,
      estimatedCost: entity.estimatedCost,
      sampleSeconds: entity.sampleSeconds,
      recordedAt: entity.recordedAt,
      status: entity.status,
    };
  }
}

============================================================
FILE: src\app\energy-monitoring\infrastructure\resources\energy-reading.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';
import { EnergyReadingStatus } from '../../domain/model/energy-reading.entity';

export interface EnergyReadingResource extends BaseResource {
  userId?: number;
  deviceId: number;
  deviceName: string;
  watts: number;
  kilowattHours?: number;
  estimatedCost?: number;
  sampleSeconds?: number;
  recordedAt: string;
  status: EnergyReadingStatus;
}


============================================================
FILE: src\app\energy-monitoring\infrastructure\responses\energy-dashboard-summary.response.ts
============================================================

export interface EnergyTrendPointResponse {
  label: string;
  deviceName: string;
  watts: number;
  kilowattHours: number;
  estimatedCost: number;
  sampleSeconds: number;
  high: boolean;
}

export interface EnergyDeviceConsumptionResponse {
  deviceId: number;
  name: string;
  room: string;
  type: string;
  watts: number;
  kilowattHours: number;
  estimatedCost: number;
  readings: number;
  highReadings: number;
}

export interface EnergyRoomConsumptionResponse {
  room: string;
  watts: number;
  kilowattHours: number;
  estimatedCost: number;
  activeDevices: number;
}

export interface EnergyActiveDeviceResponse {
  deviceId: number;
  name: string;
  room: string;
  type: string;
  watts: number;
  costPerHour: number;
}

export interface EnergyDashboardSummaryResponse {
  currentWatts: number;
  todayKilowattHours: number;
  todayEstimatedCost: number;
  projectedMonthlyCost: number;
  costPerHour: number;
  peakWatts: number;
  averageWatts: number;
  activeDevices: number;
  monitoredDevices: number;
  normalReadings: number;
  highReadings: number;
  efficiencyScore: number;
  operationalStatus: string;
  recommendation: string;
  activeAlerts: number;
  criticalAlerts: number;
  latestAlertLevel: string | null;
  latestAlertTitle: string | null;
  trend: EnergyTrendPointResponse[];
  topDevices: EnergyDeviceConsumptionResponse[];
  rooms: EnergyRoomConsumptionResponse[];
  activeDeviceDetails: EnergyActiveDeviceResponse[];
}


============================================================
FILE: src\app\energy-monitoring\infrastructure\responses\energy-reading.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';
import { EnergyReadingStatus } from '../../domain/model/energy-reading.entity';

export interface EnergyReadingResponse extends BaseResponse<number> {
  userId: number;
  deviceId: number;
  deviceName: string;
  watts: number;
  kilowattHours?: number;
  estimatedCost?: number;
  sampleSeconds?: number;
  recordedAt: string;
  status?: EnergyReadingStatus;
}

============================================================
FILE: src\app\energy-monitoring\infrastructure\responses\energy-sampling-settings.response.ts
============================================================

export interface EnergySamplingSettingsResponse {
  sampleSeconds: number;
}

============================================================
FILE: src\app\energy-monitoring\presentation\components\energy-filter-form\energy-filter-form.component.html
============================================================

<div class="filter-form">
  <app-date-picker
    [label]="'history.startDate' | translate"
    [(value)]="startDate"
    [max]="today"
    (valueChange)="onDateChange()"
  />

  <app-date-picker
    [label]="'history.endDate' | translate"
    [(value)]="endDate"
    [min]="startDate"
    [max]="today"
    (valueChange)="onDateChange()"
  />

  <app-button variant="secondary" size="sm" (pressed)="onReset()">
    {{ 'history.reset' | translate }}
  </app-button>
</div>


============================================================
FILE: src\app\energy-monitoring\presentation\components\energy-filter-form\energy-filter-form.component.scss
============================================================

.filter-form {
  display: grid;
  grid-template-columns: 1fr 1fr auto;
  gap: 12px;
  align-items: end;
  padding: 20px;
  border: 1px solid var(--border-color);
  border-radius: 22px;
  background: var(--surface-color);
}

app-button {
  align-self: end;
  margin-bottom: 0;
}

:host ::ng-deep app-button .app-button {
  min-height: 50px;
  padding-inline: 18px;
}

@media (max-width: 820px) {
  .filter-form {
    grid-template-columns: 1fr;
  }

  app-button {
    margin-bottom: 0;
  }
}


============================================================
FILE: src\app\energy-monitoring\presentation\components\energy-filter-form\energy-filter-form.component.ts
============================================================

import { Component, EventEmitter, Output } from '@angular/core';
import { TranslateModule } from '@ngx-translate/core';

import { EnergyReadingsFilterCriteria } from '../../../application/criteria/energy-readings-filter.criteria';
import { AppDatePickerComponent } from '../../../../shared/presentation/components/app-date-picker/app-date-picker.component';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';

@Component({
  selector: 'app-energy-filter-form',
  standalone: true,
  imports: [TranslateModule, AppDatePickerComponent, AppButtonComponent],
  templateUrl: './energy-filter-form.component.html',
  styleUrls: ['./energy-filter-form.component.scss'],
})
export class EnergyFilterFormComponent {
  @Output() rangeChanged = new EventEmitter<EnergyReadingsFilterCriteria>();
  @Output() reset = new EventEmitter<void>();

  startDate = '';
  endDate = '';
  today = this.toInputDate(new Date());

  get isInvalidRange(): boolean {
    if (!this.startDate || !this.endDate) {
      return true;
    }

    if (this.startDate > this.endDate) {
      return true;
    }

    if (this.startDate > this.today || this.endDate > this.today) {
      return true;
    }

    return false;
  }

  onDateChange(): void {
    if (this.isInvalidRange) {
      return;
    }

    this.rangeChanged.emit({
      startDate: this.startDate,
      endDate: this.endDate,
    });
  }

  onReset(): void {
    this.startDate = '';
    this.endDate = '';
    this.reset.emit();
  }

  private toInputDate(date: Date): string {
    const year = date.getFullYear();
    const month = String(date.getMonth() + 1).padStart(2, '0');
    const day = String(date.getDate()).padStart(2, '0');

    return `${year}-${month}-${day}`;
  }
}


============================================================
FILE: src\app\energy-monitoring\presentation\components\metrics-cards\metrics-cards.component.html
============================================================

<section class="metrics-grid">
  <article>
    <span>{{ 'energy.totalWatts' | translate }}</span>
    <strong>{{ totalWatts }} W</strong>
  </article>

  <article>
    <span>{{ 'energy.averageWatts' | translate }}</span>
    <strong>{{ averageWatts }} W</strong>
  </article>

  <article>
    <span>{{ 'energy.highestReading' | translate }}</span>
    <strong>{{ highestWatts }} W</strong>
  </article>

  <article>
    <span>{{ 'energy.highReadings' | translate }}</span>
    <strong>{{ highReadings }}</strong>
  </article>
</section>

============================================================
FILE: src\app\energy-monitoring\presentation\components\metrics-cards\metrics-cards.component.scss
============================================================

.metrics-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 18px;
}

article {
  padding: 20px;
  border: 1px solid var(--border-color);
  border-radius: 22px;
  background: var(--surface-color);
}

span {
  color: var(--text-secondary);
  font-weight: 800;
}

strong {
  display: block;
  margin-top: 8px;
  font-size: 1.75rem;
}

@media (max-width: 980px) {
  .metrics-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 620px) {
  .metrics-grid {
    grid-template-columns: 1fr;
  }
}

============================================================
FILE: src\app\energy-monitoring\presentation\components\metrics-cards\metrics-cards.component.ts
============================================================

import { Component, Input } from '@angular/core';
import { TranslateModule } from '@ngx-translate/core';

@Component({
  selector: 'app-metrics-cards',
  standalone: true,
  imports: [TranslateModule],
  templateUrl: './metrics-cards.component.html',
  styleUrls: ['./metrics-cards.component.scss'],
})
export class MetricsCardsComponent {
  @Input() totalWatts = 0;
  @Input() averageWatts = 0;
  @Input() highestWatts = 0;
  @Input() highReadings = 0;
}

============================================================
FILE: src\app\energy-monitoring\presentation\components\usage-chart\usage-chart.component.html
============================================================

<section class="chart-card">
  <div class="chart-heading">
    <h2>{{ 'energy.chartTitle' | translate }}</h2>
    <span>{{ readings.length }} {{ 'history.records' | translate }}</span>
  </div>

  @if (chartReadings.length > 0) {
    <div class="chart ec-scrollbar">
      @for (reading of chartReadings; track reading.id) {
        <div class="bar-wrapper">
          <div
            class="bar"
            [class.high]="reading.isHigh"
            [style.height.px]="getHeight(reading.watts)"
            [attr.aria-label]="reading.deviceName + ': ' + reading.watts + 'W'"
          ></div>

          <small>{{ reading.recordedAt | slice: 5 }}</small>
        </div>
      }
    </div>
  } @else {
    <app-empty-state
      [compact]="true"
      [eyebrow]="'energy.chartTitle' | translate"
      [title]="'energy.emptyReadings' | translate"
    />
  }
</section>


============================================================
FILE: src\app\energy-monitoring\presentation\components\usage-chart\usage-chart.component.scss
============================================================

.chart-card {
  padding: 22px;
  border: 1px solid var(--border-color);
  border-radius: 24px;
  background: var(--surface-color);
}

.chart-heading {
  display: flex;
  justify-content: space-between;
  gap: 14px;
  align-items: center;
  margin-bottom: 22px;
}

h2 {
  margin: 0;
}

.chart-heading span {
  color: var(--text-secondary);
  font-weight: 800;
}

.chart {
  min-height: 220px;
  display: flex;
  align-items: end;
  gap: 12px;
  overflow-x: auto;
  padding: 10px 0;
}

.bar-wrapper {
  min-width: 54px;
  display: grid;
  justify-items: center;
  gap: 10px;
}

.bar {
  width: 34px;
  border-radius: 999px 999px 8px 8px;
  background: var(--accent-color);
}

.bar.high {
  background: #dc2626;
}

small {
  color: var(--text-secondary);
  font-size: 0.75rem;
}

.chart-card app-empty-state {
  display: block;
}


============================================================
FILE: src\app\energy-monitoring\presentation\components\usage-chart\usage-chart.component.ts
============================================================

import { SlicePipe } from '@angular/common';
import { Component, Input } from '@angular/core';
import { TranslateModule } from '@ngx-translate/core';

import { EnergyReading } from '../../../domain/model/energy-reading.entity';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';

@Component({
  selector: 'app-usage-chart',
  standalone: true,
  imports: [EmptyStateComponent, TranslateModule, SlicePipe],
  templateUrl: './usage-chart.component.html',
  styleUrls: ['./usage-chart.component.scss'],
})
export class UsageChartComponent {
  @Input() readings: EnergyReading[] = [];

  get maxWatts(): number {
    if (this.readings.length === 0) return 1;

    return Math.max(...this.readings.map((reading) => reading.watts));
  }

  get chartReadings(): EnergyReading[] {
    return [...this.readings].reverse().slice(-12);
  }

  getHeight(watts: number): number {
    return Math.max(8, Math.round((watts / this.maxWatts) * 160));
  }
}


============================================================
FILE: src\app\energy-monitoring\presentation\pages\energy-dashboard\energy-dashboard-page.component.html
============================================================

<section class="energy-dashboard-page ec-page">
  <header class="ec-page-heading">
    <p class="ec-page-eyebrow">{{ 'energyDashboard.eyebrow' | translate }}</p>

    <h1 class="ec-page-title">{{ 'energyDashboard.title' | translate }}</h1>

    <p class="ec-page-description">
      {{ 'energyDashboard.description' | translate }}
    </p>
  </header>

  <app-section-card
    class="dashboard-toolbar"
    [eyebrow]="'energyDashboard.liveConsumptionEyebrow' | translate"
    [title]="'energyDashboard.liveConsumptionTitle' | translate"
    [description]="'energyDashboard.liveConsumptionDescription' | translate"
    size="large"
  >
    <div class="dashboard-toolbar__grid">
      <div class="sampling-panel">
        <span class="live-pill">
          <span class="live-dot"></span>
          {{ 'energyDashboard.automaticRecord' | translate }}
        </span>

        <app-number-stepper
          class="dashboard-sample-stepper"
          label=""
          [value]="sampleSeconds()"
          [min]="5"
          [max]="3600"
          [step]="5"
          [suffix]="'energyDashboard.secondsAbbr' | translate"
          [allowEmpty]="false"
          (valueChange)="onSampleSecondsChange($event)"
        />

        <app-button variant="secondary" size="sm" (pressed)="saveSamplingSeconds()">
          {{ 'energyDashboard.apply' | translate }}
        </app-button>
      </div>

      <div class="toolbar-copy">
        <span>
          {{ 'energyDashboard.sampleEveryPrefix' | translate }}
          <strong>{{ 'energyDashboard.sampleEveryValue' | translate: { seconds: sampleSeconds() } }}</strong>.
        </span>
      </div>

      <app-button variant="primary" (pressed)="exportCsv()">
        {{ 'energyDashboard.exportCsv' | translate }}
      </app-button>
    </div>
  </app-section-card>

  @if (energyMonitoringFacade.loading()) {
    <section class="loading-panel">
      <app-loading-spinner />
      <p>{{ 'energyDashboard.loading' | translate }}</p>
    </section>
  } @else {
    @let currentChartSummary = chartSummary();

    <app-section-card
      class="consumption-chart-card"
      [eyebrow]="'energyDashboard.consumptionEyebrow' | translate"
      [title]="chartTitle()"
      [description]="chartSubtitle()"
      size="large"
    >
      <div class="chart-toolbar">
        <div class="chart-summary-strip">
          <span>
            <strong>{{ currentChartSummary.readings }}</strong>
            {{ 'energyDashboard.readings' | translate }}
          </span>
          <span>
            <strong>{{ currentChartSummary.devices }}</strong>
            {{ 'energyDashboard.equipment' | translate }}
          </span>
          <span>
            <strong>{{ formatEnergy(currentChartSummary.totalKilowattHours) }}</strong>
            {{ 'energyDashboard.energy' | translate }}
          </span>
          <span>
            <strong>S/ {{ formatMoney(currentChartSummary.estimatedCost) }}</strong>
            {{ 'energyDashboard.cost' | translate }}
          </span>
          <span>
            <strong>{{ formatDuration(currentChartSummary.totalSeconds) }}</strong>
            {{ 'energyDashboard.accumulatedUse' | translate }}
          </span>
        </div>

        <app-dropdown
          class="period-dropdown"
          [options]="periodOptions"
          [value]="selectedPeriod()"
          [placeholder]="'energyDashboard.period' | translate"
          (valueChange)="onPeriodChange($event)"
        />
      </div>

      @if (consumptionChart().length > 0) {
        <app-draggable-bar-carousel
          [items]="consumptionChartItems()"
          [followLatest]="true"
        />
      } @else {
        <app-empty-state
          [eyebrow]="'energyDashboard.noReadingsEyebrow' | translate"
          [title]="'energyDashboard.noConsumptionTitle' | translate"
          [description]="'energyDashboard.noConsumptionDescription' | translate"
          [compact]="true"
        />
      }
    </app-section-card>

    <section class="metrics-grid">
      @for (metric of metricCards(); track metric.label) {
        <app-section-card
          class="metric-card"
          [class.metric-card--accent]="metric.tone === 'accent'"
          [class.metric-card--warning]="metric.tone === 'warning'"
          [eyebrow]="metric.label"
          size="small"
        >
          <strong>{{ metric.value }}</strong>
          <p>{{ metric.hint }}</p>
        </app-section-card>
      }
    </section>

    <section class="energy-grid">
      <app-section-card
        class="dashboard-preview-card dashboard-preview-card--health"
        [eyebrow]="'energyDashboard.generalStatusEyebrow' | translate"
        [title]="'energyDashboard.operationalSummaryTitle' | translate"
        [description]="dashboardRecommendation()"
        size="medium"
      >
        <div class="health-block">
          <div>
            <strong>{{ efficiencyScore() }}%</strong>
            <span>{{ operationalStatus() }}</span>
          </div>

          <div class="health-progress">
            <span [style.width.%]="efficiencyScore()"></span>
          </div>
        </div>

        <div class="mini-stats">
          <span>
            <strong>{{ normalReadingsCount() }}</strong>
            {{ 'energyDashboard.normalReadings' | translate }}
          </span>
          <span>
            <strong>{{ highReadingsCount() }}</strong>
            {{ 'energyDashboard.highReadingsShort' | translate }}
          </span>
          <span>
            <strong>{{ activeDevicesCount() }}</strong>
            {{ 'energyDashboard.activeDevicesShort' | translate }}
          </span>
        </div>
      </app-section-card>

      <app-section-card
        class="dashboard-preview-card dashboard-preview-card--list"
        [eyebrow]="'devices.active' | translate"
        [title]="'energyDashboard.activeDevicesNowTitle' | translate"
        size="medium"
      >
        @if (activeDeviceDetails().length > 0) {
          <div
            class="dashboard-list-preview"
            [class.dashboard-list-preview--has-more]="hiddenActiveDeviceDetailsCount() > 0"
            [style.--dashboard-list-fade-opacity]="listFadeOpacity(visibleActiveDeviceDetails().length)"
          >
            <div class="compact-table">
              @for (device of visibleActiveDeviceDetails(); track device.deviceId) {
                <article>
                  <div>
                    <strong>{{ device.name }}</strong>
                    <span>{{ device.room }}</span>
                  </div>

                  <div>
                    <strong>{{ formatNumber(device.watts, 0) }} W</strong>
                    <span>{{ 'energyDashboard.costPerHourValue' | translate: { cost: formatNumber(device.costPerHour) } }}</span>
                  </div>
                </article>
              }
            </div>

            @if (hiddenActiveDeviceDetailsCount() > 0) {
              <p class="dashboard-more-note">{{ 'energyDashboard.moreActiveDevices' | translate: { count: hiddenActiveDeviceDetailsCount() } }}</p>
            }
          </div>
        } @else {
          <app-empty-state
            [eyebrow]="'energyDashboard.noActivityEyebrow' | translate"
            [title]="'energyDashboard.noActiveDevicesTitle' | translate"
            [description]="'energyDashboard.noActiveDevicesDescription' | translate"
            [compact]="true"
          />
        }
      </app-section-card>
    </section>

    <section class="energy-grid">
      <app-section-card
        class="dashboard-preview-card dashboard-preview-card--list"
        [eyebrow]="'energyDashboard.rankingEyebrow' | translate"
        [title]="'energyDashboard.highestCostByDeviceTitle' | translate"
        size="medium"
      >
        @if (topDevices().length > 0) {
          <div
            class="dashboard-list-preview dashboard-list-preview--ranking"
            [class.dashboard-list-preview--has-more]="hiddenTopDevicesCount() > 0"
            [style.--dashboard-list-fade-opacity]="listFadeOpacity(visibleTopDevices().length)"
          >
            <div class="compact-table">
              @for (device of visibleTopDevices(); track device.deviceId; let index = $index) {
                <article class="ranking-row">
                  <span class="ranking-position">{{ index + 1 }}</span>

                  <div>
                    <strong>{{ device.name }}</strong>
                    <span>{{ device.room }}</span>
                  </div>

                  <div>
                    <strong>{{ formatEnergy(device.kilowattHours) }}</strong>
                    <span>S/ {{ formatNumber(device.estimatedCost) }}</span>
                  </div>
                </article>
              }
            </div>

            @if (hiddenTopDevicesCount() > 0) {
              <p class="dashboard-more-note">{{ 'energyDashboard.moreInRanking' | translate: { count: hiddenTopDevicesCount() } }}</p>
            }
          </div>
        } @else {
          <app-empty-state
            [eyebrow]="'energyDashboard.noCostEyebrow' | translate"
            [title]="'energyDashboard.noDeviceConsumptionTitle' | translate"
            [description]="'energyDashboard.noDeviceConsumptionDescription' | translate"
            [compact]="true"
          />
        }
      </app-section-card>

      <app-section-card
        class="dashboard-preview-card dashboard-preview-card--list"
        [eyebrow]="'energyDashboard.roomsEyebrow' | translate"
        [title]="'energyDashboard.consumptionByRoomTitle' | translate"
        size="medium"
      >
        @if (roomConsumption().length > 0) {
          <div
            class="dashboard-list-preview"
            [class.dashboard-list-preview--has-more]="hiddenRoomConsumptionCount() > 0"
            [style.--dashboard-list-fade-opacity]="listFadeOpacity(visibleRoomConsumption().length)"
          >
            <div class="compact-table">
              @for (room of visibleRoomConsumption(); track room.room) {
                <article>
                  <div>
                    <strong>{{ room.room }}</strong>
                    <span>{{ 'energyDashboard.activeDevicesValue' | translate: { count: room.activeDevices } }}</span>
                  </div>

                  <div>
                    <strong>{{ formatEnergy(room.kilowattHours) }}</strong>
                    <span>S/ {{ formatNumber(room.estimatedCost) }}</span>
                  </div>
                </article>
              }
            </div>

            @if (hiddenRoomConsumptionCount() > 0) {
              <p class="dashboard-more-note">{{ 'energyDashboard.moreInRooms' | translate: { count: hiddenRoomConsumptionCount() } }}</p>
            }
          </div>
        } @else {
          <app-empty-state
            [eyebrow]="'energyDashboard.noRoomsEyebrow' | translate"
            [title]="'energyDashboard.noRoomDataTitle' | translate"
            [description]="'energyDashboard.noRoomDataDescription' | translate"
            [compact]="true"
          />
        }
      </app-section-card>
    </section>

    <section class="energy-grid">
      <app-section-card
        class="dashboard-preview-card dashboard-preview-card--list"
        [eyebrow]="'nav.alerts' | translate"
        [title]="'energyDashboard.recentHighConsumptionEventsTitle' | translate"
        size="medium"
      >
        @if (recentAlerts().length > 0) {
          <div
            class="dashboard-list-preview"
            [class.dashboard-list-preview--has-more]="hiddenRecentAlertsCount() > 0"
            [style.--dashboard-list-fade-opacity]="listFadeOpacity(visibleRecentAlerts().length)"
          >
            <div class="alert-list">
              @for (alert of visibleRecentAlerts(); track alert.id) {
                <article>
                  <div>
                    <strong>{{ alert.deviceName }}</strong>
                    <span>{{ formatDateTime(alert.recordedAt) }}</span>
                  </div>

                  <span>{{ formatNumber(alert.watts, 0) }} W</span>
                </article>
              }
            </div>

            @if (hiddenRecentAlertsCount() > 0) {
              <p class="dashboard-more-note">{{ 'energyDashboard.moreInAlerts' | translate: { count: hiddenRecentAlertsCount() } }}</p>
            }
          </div>
        } @else {
          <app-empty-state
            [eyebrow]="'energyDashboard.noAlertsEyebrow' | translate"
            [title]="'energyDashboard.noRecentEventsTitle' | translate"
            [description]="'energyDashboard.noRecentEventsDescription' | translate"
            [compact]="true"
          />
        }
      </app-section-card>

      <app-section-card
        class="dashboard-preview-card dashboard-preview-card--history"
        [eyebrow]="'nav.history' | translate"
        [title]="'energyDashboard.latestReadingsTitle' | translate"
        size="medium"
      >
        @if (latestReadings().length > 0) {
          <div
            class="dashboard-list-preview dashboard-list-preview--table"
            [class.dashboard-list-preview--has-more]="hiddenLatestReadingsCount() > 0"
            [style.--dashboard-list-fade-opacity]="listFadeOpacity(visibleLatestReadings().length)"
          >
            <div class="readings-table-wrapper ec-scrollbar">
              <table class="readings-table">
                <thead>
                  <tr>
                    <th>{{ 'routines.device' | translate }}</th>
                    <th>{{ 'energyDashboard.power' | translate }}</th>
                    <th>{{ 'about.capabilities.items.energy.eyebrow' | translate }}</th>
                    <th>{{ 'energyDashboard.cost' | translate }}</th>
                    <th>{{ 'history.date' | translate }}</th>
                    <th>{{ 'history.status' | translate }}</th>
                  </tr>
                </thead>

                <tbody>
                  @for (reading of visibleLatestReadings(); track reading.id) {
                    <tr>
                      <td>{{ reading.deviceName }}</td>
                      <td>{{ formatNumber(reading.watts, 0) }} W</td>
                      <td>{{ formatEnergy(reading.kilowattHours) }}</td>
                      <td>S/ {{ formatMoney(reading.estimatedCost) }}</td>
                      <td>{{ formatDateTime(reading.recordedAt) }}</td>
                      <td>
                        <span class="status-pill" [class.status-pill--high]="reading.isHigh">
                          {{ readingStatusLabel(reading) }}
                        </span>
                      </td>
                    </tr>
                  }
                </tbody>
              </table>
            </div>

            @if (hiddenLatestReadingsCount() > 0) {
              <p class="dashboard-more-note">{{ 'energyDashboard.moreInHistory' | translate: { count: hiddenLatestReadingsCount() } }}</p>
            }
          </div>
        } @else {
          <app-empty-state
            [eyebrow]="'energyDashboard.noHistoryEyebrow' | translate"
            [title]="'energyDashboard.noReadingsAvailableTitle' | translate"
            [description]="'energyDashboard.noReadingsAvailableDescription' | translate"
            [compact]="true"
          />
        }
      </app-section-card>
    </section>
  }
</section>


============================================================
FILE: src\app\energy-monitoring\presentation\pages\energy-dashboard\energy-dashboard-page.component.scss
============================================================

:host {
  display: block;
}

.energy-dashboard-page {
  gap: 22px;
}

.dashboard-toolbar__grid {
  display: grid;
  grid-template-columns: minmax(360px, 1.2fr) minmax(220px, 0.8fr) auto;
  gap: 18px;
  align-items: center;
}

.sampling-panel {
  display: grid;
  grid-template-columns: auto minmax(190px, 235px) auto;
  gap: 14px;
  align-items: center;
  min-width: 0;
}

.dashboard-sample-stepper {
  align-self: center;
}

:host ::ng-deep .dashboard-sample-stepper .number-stepper {
  gap: 0;
}

:host ::ng-deep .dashboard-sample-stepper .number-stepper__body {
  min-height: 44px;
  border-radius: 18px;
}

.sampling-panel app-button {
  align-self: center;
}

:host ::ng-deep .sampling-panel app-button .app-button {
  min-height: 44px;
}

.live-pill {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  width: fit-content;
  min-height: 44px;
  border: 1px solid var(--ec-status-success-border);
  border-radius: 999px;
  padding: 0 12px;
  color: var(--ec-status-success-text);
  background: var(--ec-status-success-bg);
  font-size: 0.76rem;
  font-weight: 900;
  text-transform: uppercase;
}

.live-dot {
  width: 8px;
  height: 8px;
  border-radius: 999px;
  background: var(--ec-status-success-solid);
  box-shadow: 0 0 0 6px var(--ec-status-success-bg);
}

.toolbar-copy {
  color: var(--ec-copy-color);
  font-size: 0.92rem;
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.toolbar-copy strong {
  color: var(--text-primary);
}

.loading-panel {
  display: grid;
  justify-items: center;
  gap: 12px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  padding: 48px 24px;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
  background: var(--surface-color);
}

.chart-toolbar {
  display: flex;
  justify-content: space-between;
  gap: 18px;
  align-items: center;
  margin-bottom: 20px;
}

.chart-summary-strip {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  min-width: 0;
}

.chart-summary-strip span {
  display: grid;
  gap: 2px;
  min-width: 118px;
  border: 1px solid var(--border-color);
  border-radius: 14px;
  padding: 12px 14px;
  color: var(--ec-copy-color);
  background: color-mix(in srgb, var(--surface-muted) 72%, transparent);
  font-size: 0.76rem;
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.chart-summary-strip strong {
  color: var(--text-primary);
  font-size: 1rem;
}

.period-dropdown {
  width: min(220px, 100%);
}

:host ::ng-deep app-section-card.consumption-chart-card .section-card {
  overflow: visible;
}

:host ::ng-deep app-section-card.consumption-chart-card .section-card > p {
  max-width: min(100%, 920px);
  overflow: visible;
}

.metrics-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  align-items: stretch;
  grid-auto-rows: 1fr;
  gap: 16px;
}

app-section-card.metric-card {
  display: block;
  min-width: 0;
  min-height: 150px;
}

:host ::ng-deep app-section-card.metric-card .section-card {
  box-sizing: border-box;
  height: 100%;
  min-height: 150px;
  grid-template-rows: auto minmax(46px, auto) minmax(42px, 1fr);
  align-content: start;
}

.metric-card strong {
  display: flex;
  align-items: center;
  min-width: 0;
  min-height: 46px;
  margin: 0;
  color: var(--text-primary);
  font-size: clamp(1.35rem, 2vw, 2.1rem);
  font-weight: 950;
  line-height: 1.08;
  overflow-wrap: anywhere;
}

.metric-card p {
  display: -webkit-box;
  overflow: hidden;
  align-self: start;
  margin: 0;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

.metric-card--accent strong {
  color: var(--accent-color);
}

.metric-card--warning strong {
  color: var(--ec-status-warning-text);
}

.energy-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  align-items: stretch;
  grid-auto-rows: 1fr;
  gap: 16px;
}

app-section-card.dashboard-preview-card {
  display: block;
  min-width: 0;
}

:host ::ng-deep app-section-card.dashboard-preview-card .section-card {
  box-sizing: border-box;
  height: 100%;
  min-height: 276px;
  grid-template-rows: auto auto minmax(0, 1fr);
  align-content: start;
}

:host ::ng-deep app-section-card.dashboard-preview-card--described .section-card {
  grid-template-rows: auto auto auto minmax(0, 1fr);
}

:host ::ng-deep app-section-card.dashboard-preview-card--health .section-card {
  min-height: 276px;
}

:host ::ng-deep app-section-card.dashboard-preview-card--list .section-card,
:host ::ng-deep app-section-card.dashboard-preview-card--history .section-card {
  min-height: 276px;
}

.health-block {
  display: grid;
  gap: 14px;
}

.health-block > div:first-child {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  align-items: end;
}

.health-block strong {
  color: var(--text-primary);
  font-size: 2.2rem;
  font-weight: 950;
}

.health-block span {
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
}

.health-progress {
  overflow: hidden;
  height: 12px;
  border-radius: 999px;
  background: color-mix(in srgb, var(--border-color) 55%, transparent);
}

.health-progress span {
  display: block;
  height: 100%;
  border-radius: inherit;
  background: linear-gradient(90deg, var(--ec-status-success-solid), var(--accent-color));
}

.mini-stats {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 10px;
  margin-top: 16px;
}

.mini-stats span {
  display: grid;
  gap: 4px;
  border: 1px solid var(--border-color);
  border-radius: 14px;
  padding: 12px;
  color: var(--ec-copy-color);
  background: color-mix(in srgb, var(--surface-muted) 72%, transparent);
  font-size: 0.8rem;
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.mini-stats strong {
  color: var(--text-primary);
  font-size: 1.2rem;
}

.compact-table,
.alert-list {
  display: grid;
  gap: 10px;
}

.dashboard-list-preview {
  --dashboard-list-fade-opacity: 0.32;
  position: relative;
  display: grid;
  align-content: start;
  min-height: 0;
  height: 100%;
  margin: 0 calc(0px - var(--section-card-padding, 20px)) calc(0px - var(--section-card-padding, 20px));
  padding: 0 var(--section-card-padding, 20px) 34px;
  overflow: visible;
}

.dashboard-list-preview::after {
  content: '';
  position: absolute;
  z-index: 0;
  right: -18px;
  bottom: -26px;
  left: -18px;
  height: 112px;
  border-radius: 999px 999px 0 0 / 82px 82px 0 0;
  background:
    radial-gradient(ellipse 58% 78% at 50% 100%, color-mix(in srgb, var(--accent-color) 32%, transparent), var(--ec-accent-soft) 42%, transparent 74%),
    radial-gradient(ellipse 36% 58% at 28% 100%, var(--ec-accent-soft), transparent 70%),
    radial-gradient(ellipse 36% 58% at 72% 100%, color-mix(in srgb, var(--accent-color) 12%, transparent), transparent 70%);
  filter: blur(16px) saturate(1.18);
  opacity: 0.72;
  mask-image: linear-gradient(0deg, #000 0%, #000 58%, transparent 100%);
  pointer-events: none;
}

.dashboard-list-preview--has-more::after {
  opacity: 0.78;
}

.dashboard-list-preview > .compact-table,
.dashboard-list-preview > .alert-list,
.dashboard-list-preview > .readings-table-wrapper {
  position: relative;
  z-index: 1;
}

.dashboard-list-preview--table {
  padding-bottom: 62px;
}

.dashboard-list-preview--table .dashboard-more-note {
  bottom: 14px;
}

.dashboard-list-preview--ranking.dashboard-list-preview--has-more {
  padding-bottom: 76px;
}

.dashboard-more-note {
  position: absolute;
  z-index: 2;
  right: 10px;
  bottom: 9px;
  margin: 0;
  border: 1px solid var(--border-strong);
  border-radius: 999px;
  padding: 5px 10px;
  color: var(--accent-color);
  background: color-mix(in srgb, var(--surface-elevated-solid) 84%, var(--accent-color) 16%);
  box-shadow: var(--ec-shadow-soft);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.04em;
  text-transform: uppercase;
}

.compact-table article,
.alert-list article {
  display: flex;
  justify-content: space-between;
  gap: 14px;
  align-items: center;
  min-width: 0;
  border: 1px solid var(--border-color);
  border-radius: 14px;
  padding: 12px 14px;
  background: color-mix(in srgb, var(--surface-muted) 68%, transparent);
}

.compact-table article.ranking-row {
  display: grid;
  grid-template-columns: 38px minmax(0, 1fr) auto;
  gap: 12px;
  align-items: stretch;
  padding: 0 14px 0 0;
  overflow: hidden;
}

.ranking-position {
  display: grid;
  place-items: center;
  align-self: stretch;
  min-height: 100%;
  border-right: 1px solid var(--border-strong);
  color: var(--text-on-action);
  background:
    radial-gradient(circle at 50% 24%, var(--ec-accent-soft), transparent 42%),
    var(--ec-action-bg),
    color-mix(in srgb, var(--surface-muted) 78%, transparent);
  box-shadow:
    inset -1px 0 0 var(--ec-accent-soft),
    inset 0 0 20px var(--ec-accent-soft);
  font-size: 1rem;
  font-weight: 950;
  letter-spacing: 0;
  line-height: 1;
  text-shadow: 0 0 14px var(--ec-accent-soft);
  text-transform: none;
}

.ranking-row > div {
  align-self: center;
  padding: 12px 0;
}

.ranking-row > div:last-child {
  justify-items: end;
  text-align: right;
}

.compact-table div,
.alert-list div {
  display: grid;
  gap: 3px;
  min-width: 0;
}

.compact-table strong,
.alert-list strong {
  overflow: hidden;
  color: var(--text-primary);
  font-weight: 950;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.compact-table span,
.alert-list span {
  color: var(--ec-copy-color);
  font-size: 0.82rem;
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.readings-table-wrapper {
  overflow-x: auto;
  overflow-y: hidden;
}

.readings-table {
  width: 100%;
  min-width: 720px;
  margin-bottom: 18px;
  border-spacing: 0;
}

.readings-table th,
.readings-table td {
  border-bottom: 1px solid var(--border-color);
  padding: 12px 10px;
  color: var(--ec-copy-color);
  text-align: left;
  white-space: nowrap;
}

.readings-table th {
  color: var(--text-muted);
  font-size: 0.74rem;
  font-weight: 950;
  text-transform: uppercase;
}

.readings-table td {
  font-size: 0.86rem;
  font-weight: var(--ec-copy-weight);
}

.status-pill {
  display: inline-flex;
  border-radius: 999px;
  padding: 5px 10px;
  color: var(--ec-status-success-text);
  background: var(--ec-status-success-bg);
  font-size: 0.72rem;
  font-weight: 950;
}

.status-pill--high {
  color: var(--ec-status-danger-text);
  background: var(--ec-status-danger-bg);
}

@media (max-width: 1080px) {
  .dashboard-toolbar__grid,
  .energy-grid {
    grid-template-columns: 1fr;
  }

  .metrics-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 760px) {
  .sampling-panel,
  .chart-toolbar,
  .metrics-grid,
  .mini-stats {
    grid-template-columns: 1fr;
  }

  .sampling-panel,
  .chart-toolbar {
    align-items: stretch;
  }

  .chart-summary-strip span,
  .period-dropdown {
    width: 100%;
  }

}


============================================================
FILE: src\app\energy-monitoring\presentation\pages\energy-dashboard\energy-dashboard-page.component.ts
============================================================

import { TranslateModule, TranslateService } from '@ngx-translate/core';
import { Component, OnDestroy, OnInit, computed, inject, signal } from '@angular/core';

import { EnergyMonitoringFacade } from '../../../application/services/energy-monitoring.facade';
import { EnergyReading } from '../../../domain/model/energy-reading.entity';

import { ToastService } from '../../../../shared/application/services/toast.service';
import { ActiveWorkplaceContextService } from '../../../../workplace/application/services/active-workplace-context.service';
import { WorkplaceFacade } from '../../../../workplace/application/services/workplace.facade';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { AppNumberStepperComponent } from '../../../../shared/presentation/components/app-number-stepper/app-number-stepper.component';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { SectionCardComponent } from '../../../../shared/presentation/components/section-card/section-card.component';
import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';
import {
  DraggableBarCarouselComponent,
  DraggableBarCarouselItem,
} from '../../../../shared/presentation/components/draggable-bar-carousel/draggable-bar-carousel.component';

type ConsumptionPeriod = 'DAILY' | 'WEEKLY' | 'MONTHLY';

interface ConsumptionChartPoint {
  key: string;
  deviceId: number;
  deviceName: string;
  deviceEmoji: string;
  recordedAt: Date;
  watts: number;
  kilowattHours: number;
  estimatedCost: number;
  sampleSeconds: number;
  percentage: number;
  isHigh: boolean;
}

interface ConsumptionChartSummary {
  readings: number;
  devices: number;
  totalKilowattHours: number;
  estimatedCost: number;
  averageWatts: number;
  peakWatts: number;
  totalSeconds: number;
}

interface MetricCard {
  label: string;
  value: string;
  hint: string;
  tone: 'accent' | 'plain' | 'warning';
}

@Component({
  selector: 'app-energy-dashboard-page',
  standalone: true,
  imports: [
    TranslateModule,
    AppButtonComponent,
    AppDropdownComponent,
    AppNumberStepperComponent,
    EmptyStateComponent,
    LoadingSpinnerComponent,
    SectionCardComponent,
    DraggableBarCarouselComponent,
  ],
  templateUrl: './energy-dashboard-page.component.html',
  styleUrls: ['./energy-dashboard-page.component.scss'],
})
export class EnergyDashboardPageComponent implements OnInit, OnDestroy {
  readonly energyMonitoringFacade = inject(EnergyMonitoringFacade);
  private readonly activeWorkplaceContext = inject(ActiveWorkplaceContextService);
  private readonly workplaceFacade = inject(WorkplaceFacade);
  private readonly toastService = inject(ToastService);
  private readonly translate = inject(TranslateService);
  private readonly uiPreferences = inject(UiPreferencesService);

  private refreshIntervalId: ReturnType<typeof setInterval> | null = null;
  private readonly maxSegmentsPerReading = 24;
  private readonly maxChartReadings = 220;
  private readonly maxChartPoints = 360;

  readonly sampleSeconds = signal(15);
  readonly selectedPeriod = signal<ConsumptionPeriod>('DAILY');

  readonly periodOptions: DropdownOption[] = [
    {
      label: 'Diario',
      labelKey: 'energyDashboard.periods.daily',
      value: 'DAILY',
      description: 'Lecturas reales registradas hoy.',
      descriptionKey: 'energyDashboard.periodDescriptions.daily',
    },
    {
      label: 'Semanal',
      labelKey: 'energyDashboard.periods.weekly',
      value: 'WEEKLY',
      description: 'Lecturas reales de los ultimos 7 dias.',
      descriptionKey: 'energyDashboard.periodDescriptions.weekly',
    },
    {
      label: 'Mensual',
      labelKey: 'energyDashboard.periods.monthly',
      value: 'MONTHLY',
      description: 'Lecturas reales del mes actual.',
      descriptionKey: 'energyDashboard.periodDescriptions.monthly',
    },
  ];

  readonly activeLocationId = computed(() => this.activeWorkplaceContext.activeLocationId());
  readonly activeLocation = computed(() => this.activeWorkplaceContext.activeLocation());
  readonly activeLocationDeviceIds = computed(() => {
    const activeLocationId = this.activeLocationId();

    if (!activeLocationId) {
      return new Set<number>();
    }

    return new Set(
      this.workplaceFacade
        .deviceAssignments()
        .filter((assignment) => assignment.locationId === activeLocationId)
        .map((assignment) => assignment.deviceId)
    );
  });

  readonly allReadings = computed(() => this.energyMonitoringFacade.readings() ?? []);

  readonly readings = computed(() => {
    const activeLocationId = this.activeLocationId();

    if (!activeLocationId) {
      return [];
    }

    const activeDeviceIds = this.activeLocationDeviceIds();

    return this.allReadings().filter((reading) =>
      activeDeviceIds.has(reading.deviceId)
    );
  });

  readonly summary = computed(() => this.energyMonitoringFacade.dashboardSummary());
  readonly totalReadings = computed(() => this.readings().length);
  readonly todayReadings = computed(() => {
    const range = this.periodRange('DAILY');

    return this.readings().filter((reading) => {
      const timestamp = this.toTimestamp(reading.recordedAt);
      return timestamp >= range.start && timestamp <= range.end;
    });
  });
  readonly currentMonthReadings = computed(() => {
    const range = this.periodRange('MONTHLY');

    return this.readings().filter((reading) => {
      const timestamp = this.toTimestamp(reading.recordedAt);
      return timestamp >= range.start && timestamp <= range.end;
    });
  });
  readonly currentWatts = computed(() =>
    this.activeDeviceDetails().reduce((total, device) => total + device.watts, 0)
  );
  readonly todayKilowattHours = computed(() =>
    this.todayReadings().reduce((total, reading) => total + reading.kilowattHours, 0)
  );
  readonly todayEstimatedCost = computed(() =>
    this.todayReadings().reduce((total, reading) => total + reading.estimatedCost, 0)
  );
  readonly projectedMonthlyCost = computed(() => {
    const now = new Date();
    const elapsedDays = Math.max(1, now.getDate());
    const daysInMonth = new Date(now.getFullYear(), now.getMonth() + 1, 0).getDate();
    const monthCost = this.currentMonthReadings().reduce(
      (total, reading) => total + reading.estimatedCost,
      0
    );

    return monthCost > 0 ? (monthCost / elapsedDays) * daysInMonth : 0;
  });
  readonly costPerHour = computed(() =>
    this.activeDeviceDetails().reduce((total, device) => total + device.costPerHour, 0)
  );
  readonly activeDevicesCount = computed(() => this.activeDeviceDetails().length);
  readonly monitoredDevicesCount = computed(() => this.activeLocationDeviceIds().size);
  readonly normalReadingsCount = computed(
    () => this.readings().filter((reading) => reading.isNormal).length
  );
  readonly highReadingsCount = computed(
    () => this.readings().filter((reading) => reading.isHigh).length
  );
  readonly efficiencyScore = computed(() => {
    const readings = this.readings();

    if (readings.length === 0) {
      return 100;
    }

    return Math.max(
      0,
      Math.round(100 - (this.highReadingsCount() / readings.length) * 100)
    );
  });
  readonly operationalStatus = computed(() => {
    const score = this.efficiencyScore();

    if (score < 70) {
      return this.t('energyDashboard.operationalStatus.review');
    }

    if (score < 90) {
      return this.t('energyDashboard.operationalStatus.attention');
    }

    return this.t('energyDashboard.operationalStatus.optimal');
  });
  readonly activeAlertsCount = computed(() => this.highReadingsCount());
  readonly criticalAlertsCount = computed(() => this.highReadingsCount());
  readonly latestAlertLevel = computed(() =>
    this.highReadingsCount() > 0 ? 'HIGH' : 'STABLE'
  );
  readonly latestAlertLevelLabel = computed(() =>
    this.readingStatusLabelByLevel(this.latestAlertLevel())
  );
  readonly latestAlertTitle = computed(() => {
    const latestHighReading = this.recentAlerts()[0];

    return latestHighReading
      ? this.t('energyDashboard.highConsumptionRegistered', {
          device: latestHighReading.deviceName,
        })
      : this.t('energyDashboard.noActiveAlerts');
  });

  readonly dashboardRecommendation = computed(
    () => {
      const activeLocation = this.activeLocation();

      if (!activeLocation) {
        return this.t('energyDashboard.selectActiveSiteRecommendation');
      }

      if (this.readings().length === 0) {
        return this.t('energyDashboard.noReadingsForSiteRecommendation', {
          site: activeLocation.name,
        });
      }

      if (this.highReadingsCount() > 0) {
        return this.t('energyDashboard.highReadingsForSiteRecommendation', {
          site: activeLocation.name,
        });
      }

      return this.t('energyDashboard.stableConsumptionRecommendation', {
        site: activeLocation.name,
      });
    }
  );

  readonly chartTitle = computed(() => {
    const titles: Record<ConsumptionPeriod, string> = {
      DAILY: 'energyDashboard.chartTitles.daily',
      WEEKLY: 'energyDashboard.chartTitles.weekly',
      MONTHLY: 'energyDashboard.chartTitles.monthly',
    };

    return this.t(titles[this.selectedPeriod()]);
  });

  readonly chartSubtitle = computed(() => {
    const subtitles: Record<ConsumptionPeriod, string> = {
      DAILY: 'energyDashboard.chartSubtitles.daily',
      WEEKLY: 'energyDashboard.chartSubtitles.weekly',
      MONTHLY: 'energyDashboard.chartSubtitles.monthly',
    };

    return this.t(subtitles[this.selectedPeriod()]);
  });

  readonly chartReadings = computed(() => {
    const range = this.periodRange(this.selectedPeriod());

    return [...this.readings()]
      .filter((reading) => {
        const timestamp = this.toTimestamp(reading.recordedAt);
        return timestamp >= range.start && timestamp <= range.end;
      })
      .sort(
        (first, second) =>
          this.toTimestamp(first.recordedAt) - this.toTimestamp(second.recordedAt)
      );
  });

  readonly chartVisualReadings = computed(() => {
    const readings = this.chartReadings();

    if (readings.length <= this.maxChartReadings) {
      return readings;
    }

    return readings.slice(readings.length - this.maxChartReadings);
  });

  readonly consumptionChart = computed<ConsumptionChartPoint[]>(() => {
    const readings = this.chartVisualReadings();
    const rawPoints = readings
      .flatMap((reading, index) =>
        this.toConsumptionChartPoints(reading, index, readings.length)
      )
      .sort((first, second) => first.recordedAt.getTime() - second.recordedAt.getTime());
    const visiblePoints =
      rawPoints.length > this.maxChartPoints
        ? rawPoints.slice(rawPoints.length - this.maxChartPoints)
        : rawPoints;

    const maxKilowattHours = visiblePoints.reduce(
      (max, point) => Math.max(max, point.kilowattHours),
      0.000001
    );

    return visiblePoints.map((point) => ({
      ...point,
      percentage: Math.max(
        8,
        Math.round((point.kilowattHours / maxKilowattHours) * 100)
      ),
    }));
  });

  readonly consumptionChartItems = computed<DraggableBarCarouselItem[]>(() =>
    this.consumptionChart().map((point) => ({
      key: point.key,
      value: this.formatEnergy(point.kilowattHours),
      caption: this.formatDuration(point.sampleSeconds),
      percentage: point.percentage,
      tone: point.isHigh ? 'warning' : 'default',
      eyebrow: point.deviceEmoji,
      title: point.deviceName,
      subtitle: `${this.formatNumber(point.watts, 0)} W`,
      detail: this.formatDateTime(point.recordedAt),
      meta: this.t('energyDashboard.intervalValue', {
        duration: this.formatDuration(point.sampleSeconds),
      }),
      ariaLabel: this.t('energyDashboard.chartItemAria', {
        energy: this.formatEnergy(point.kilowattHours),
        emoji: point.deviceEmoji,
        device: point.deviceName,
        watts: this.formatNumber(point.watts, 0),
        date: this.formatDateTime(point.recordedAt),
        duration: this.formatDuration(point.sampleSeconds),
      }),
    }))
  );

  readonly chartSummary = computed<ConsumptionChartSummary>(() => {
    const readings = this.chartReadings();
    const totalKilowattHours = readings.reduce(
      (total, reading) => total + reading.kilowattHours,
      0
    );
    const estimatedCost = readings.reduce(
      (total, reading) => total + reading.estimatedCost,
      0
    );
    const totalWatts = readings.reduce((total, reading) => total + reading.watts, 0);
    const peakWatts = readings.reduce(
      (peak, reading) => Math.max(peak, reading.watts),
      0
    );
    const totalSeconds = readings.reduce(
      (total, reading) => total + reading.sampleSeconds,
      0
    );

    return {
      readings: readings.length,
      devices: new Set(readings.map((reading) => reading.deviceId)).size,
      totalKilowattHours,
      estimatedCost,
      averageWatts: readings.length ? totalWatts / readings.length : 0,
      peakWatts,
      totalSeconds,
    };
  });

  readonly metricCards = computed<MetricCard[]>(() => [
    {
      label: this.t('energyDashboard.metrics.activePower'),
      value: `${this.formatNumber(this.currentWatts())} W`,
      hint: this.t('energyDashboard.metricHints.activePower'),
      tone: 'accent',
    },
    {
      label: this.t('energyDashboard.metrics.energyToday'),
      value: this.formatEnergy(this.todayKilowattHours()),
      hint: this.t('energyDashboard.metricHints.energyToday'),
      tone: 'plain',
    },
    {
      label: this.t('energyDashboard.metrics.costToday'),
      value: `S/ ${this.formatNumber(this.todayEstimatedCost())}`,
      hint: this.t('energyDashboard.metricHints.costToday'),
      tone: 'plain',
    },
    {
      label: this.t('energyDashboard.metrics.costPerHour'),
      value: `S/ ${this.formatNumber(this.costPerHour())}`,
      hint: this.t('energyDashboard.metricHints.costPerHour'),
      tone: 'plain',
    },
    {
      label: this.t('energyDashboard.metrics.monthlyProjection'),
      value: `S/ ${this.formatNumber(this.projectedMonthlyCost())}`,
      hint: this.t('energyDashboard.metricHints.monthlyProjection'),
      tone: 'plain',
    },
    {
      label: this.t('energyDashboard.metrics.activeAlerts'),
      value: `${this.activeAlertsCount()}`,
      hint: this.t('energyDashboard.metricHints.activeAlerts', {
        count: this.criticalAlertsCount(),
        level: this.latestAlertLevelLabel(),
        title: this.latestAlertTitle(),
      }),
      tone: this.activeAlertsCount() > 0 ? 'warning' : 'plain',
    },
  ]);

  readonly activeDeviceDetails = computed(() => {
    const activeDeviceIds = this.activeLocationDeviceIds();

    return (this.summary()?.activeDeviceDetails ?? []).filter((device) =>
      activeDeviceIds.has(device.deviceId)
    );
  });
  readonly visibleActiveDeviceDetails = computed(() =>
    this.activeDeviceDetails().slice(0, 2)
  );
  readonly hiddenActiveDeviceDetailsCount = computed(() =>
    Math.max(0, this.activeDeviceDetails().length - this.visibleActiveDeviceDetails().length)
  );
  readonly topDevices = computed(() => {
    const byDevice = new Map<
      number,
      {
        deviceId: number;
        name: string;
        room: string;
        type: string;
        watts: number;
        kilowattHours: number;
        estimatedCost: number;
        readings: number;
        highReadings: number;
      }
    >();

    for (const reading of this.readings()) {
      const current = byDevice.get(reading.deviceId) ?? {
        deviceId: reading.deviceId,
        name: reading.deviceName,
        room: this.getRoomNameForDevice(reading.deviceId),
        type: this.getDeviceTypeForDevice(reading.deviceId),
        watts: 0,
        kilowattHours: 0,
        estimatedCost: 0,
        readings: 0,
        highReadings: 0,
      };

      current.watts = Math.max(current.watts, reading.watts);
      current.kilowattHours += reading.kilowattHours;
      current.estimatedCost += reading.estimatedCost;
      current.readings += 1;
      current.highReadings += reading.isHigh ? 1 : 0;
      byDevice.set(reading.deviceId, current);
    }

    return [...byDevice.values()].sort(
      (first, second) => second.kilowattHours - first.kilowattHours
    );
  });
  readonly visibleTopDevices = computed(() => this.topDevices().slice(0, 3));
  readonly hiddenTopDevicesCount = computed(() =>
    Math.max(0, this.topDevices().length - this.visibleTopDevices().length)
  );
  readonly roomConsumption = computed(() => {
    const byRoom = new Map<
      string,
      {
        room: string;
        watts: number;
        kilowattHours: number;
        estimatedCost: number;
        deviceIds: Set<number>;
      }
    >();

    for (const reading of this.readings()) {
      const room = this.getRoomNameForDevice(reading.deviceId);
      const current = byRoom.get(room) ?? {
        room,
        watts: 0,
        kilowattHours: 0,
        estimatedCost: 0,
        deviceIds: new Set<number>(),
      };

      current.watts = Math.max(current.watts, reading.watts);
      current.kilowattHours += reading.kilowattHours;
      current.estimatedCost += reading.estimatedCost;
      current.deviceIds.add(reading.deviceId);
      byRoom.set(room, current);
    }

    return [...byRoom.values()]
      .map((room) => ({
        room: room.room,
        watts: room.watts,
        kilowattHours: room.kilowattHours,
        estimatedCost: room.estimatedCost,
        activeDevices: room.deviceIds.size,
      }))
      .sort((first, second) => second.kilowattHours - first.kilowattHours);
  });
  readonly visibleRoomConsumption = computed(() => this.roomConsumption().slice(0, 2));
  readonly hiddenRoomConsumptionCount = computed(() =>
    Math.max(0, this.roomConsumption().length - this.visibleRoomConsumption().length)
  );
  readonly deviceTypeById = computed(() => {
    const map = new Map<number, string>();

    for (const device of this.activeDeviceDetails()) {
      map.set(device.deviceId, device.type);
    }

    for (const device of this.topDevices()) {
      if (!map.has(device.deviceId)) {
        map.set(device.deviceId, device.type);
      }
    }

    return map;
  });

  readonly recentAlerts = computed(() =>
    [...this.readings()]
      .filter((reading) => reading.isHigh)
      .sort(
        (first, second) =>
          this.toTimestamp(second.recordedAt) - this.toTimestamp(first.recordedAt)
      )
  );
  readonly visibleRecentAlerts = computed(() => this.recentAlerts().slice(0, 2));
  readonly hiddenRecentAlertsCount = computed(() =>
    Math.max(0, this.recentAlerts().length - this.visibleRecentAlerts().length)
  );

  readonly latestReadings = computed(() =>
    [...this.readings()]
      .sort(
        (first, second) =>
          this.toTimestamp(second.recordedAt) - this.toTimestamp(first.recordedAt)
      )
  );
  readonly visibleLatestReadings = computed(() => this.latestReadings().slice(0, 4));
  readonly hiddenLatestReadingsCount = computed(() =>
    Math.max(0, this.latestReadings().length - this.visibleLatestReadings().length)
  );

  async ngOnInit(): Promise<void> {
    const workplaceAlreadyLoaded =
      this.workplaceFacade.locations().length > 0 ||
      this.workplaceFacade.rooms().length > 0 ||
      this.workplaceFacade.deviceAssignments().length > 0;
    const readingsAlreadyLoaded = this.energyMonitoringFacade.readings().length > 0;

    await this.loadSamplingSettings();

    await Promise.all([
      workplaceAlreadyLoaded
        ? Promise.resolve()
        : this.workplaceFacade.loadWorkplace(),
      readingsAlreadyLoaded
        ? Promise.resolve()
        : this.energyMonitoringFacade.loadReadings(),
    ]);
    this.activeWorkplaceContext.ensureActiveLocation(this.workplaceFacade.locations());

    if (readingsAlreadyLoaded) {
      void this.energyMonitoringFacade.loadReadings(false);
    }

    if (this.energyMonitoringFacade.error()) {
      this.toastService.error(this.t('energyDashboard.toasts.loadError'));
    }

    this.restartAutoRefresh();
  }

  ngOnDestroy(): void {
    if (this.refreshIntervalId) {
      clearInterval(this.refreshIntervalId);
    }
  }

  onPeriodChange(value: string): void {
    if (!this.isConsumptionPeriod(value) || value === this.selectedPeriod()) {
      return;
    }

    this.selectedPeriod.set(value);
  }

  exportCsv(): void {
    const exported = this.energyMonitoringFacade.exportCsv({
      readings: this.readings(),
      fileName: 'electrocorp-energy-dashboard',
    });

    if (exported) {
      this.toastService.success(this.t('energyDashboard.toasts.exportSuccess'));
      return;
    }

    this.toastService.warning(this.t('energyDashboard.toasts.exportError'));
  }

  async saveSamplingSeconds(): Promise<void> {
    try {
      const nextValue = Math.max(5, Math.min(3600, Number(this.sampleSeconds())));
      const savedValue = await this.energyMonitoringFacade.updateSamplingSeconds(nextValue);

      this.sampleSeconds.set(savedValue);
      this.restartAutoRefresh();

      this.toastService.success(
        this.t('energyDashboard.toasts.intervalUpdated', { seconds: savedValue })
      );
    } catch (error) {
      console.error(error);
      this.toastService.error(this.t('energyDashboard.toasts.intervalUpdateError'));
    }
  }

  onSampleSecondsChange(value: number | null): void {
    const nextValue = Math.max(5, Math.min(3600, value ?? 5));

    this.sampleSeconds.set(nextValue);
  }

  formatDateTime(value: string | Date): string {
    const date = new Date(value);

    if (Number.isNaN(date.getTime())) {
      return typeof value === 'string' ? value : '';
    }

    return new Intl.DateTimeFormat(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
      hour: '2-digit',
      minute: '2-digit',
    }).format(date);
  }

  formatNumber(value: number, digits = 2): string {
    return new Intl.NumberFormat(this.currentLocale(), {
      minimumFractionDigits: digits,
      maximumFractionDigits: digits,
    }).format(value);
  }

  listFadeOpacity(visibleCount: number): string {
    return String(Math.max(0.14, 0.58 - Math.min(visibleCount, 4) * 0.11));
  }

  formatEnergy(value: number): string {
    if (value < 0.001) {
      return `${this.formatNumber(value * 1000, 2)} Wh`;
    }

    return `${this.formatNumber(value, 3)} kWh`;
  }

  formatMoney(value: number): string {
    return this.formatNumber(value < 0.01 ? value : Number(value.toFixed(2)), value < 0.01 ? 4 : 2);
  }

  formatDuration(totalSeconds: number): string {
    const seconds = Math.max(0, Math.floor(totalSeconds || 0));
    const hours = Math.floor(seconds / 3600);
    const minutes = Math.floor((seconds % 3600) / 60);

    if (hours > 0) {
      return `${hours}h ${minutes}m`;
    }

    if (minutes > 0) {
      return `${minutes}m`;
    }

    return `${seconds}s`;
  }

  getDeviceEmoji(deviceId: number, deviceName: string): string {
    const deviceType = this.deviceTypeById().get(deviceId);

    if (deviceType) {
      return this.getDeviceEmojiByType(deviceType);
    }

    return this.getDeviceEmojiByName(deviceName);
  }

  readingStatusLabel(reading: EnergyReading): string {
    return this.readingStatusLabelByLevel(reading.isHigh ? 'HIGH' : 'STABLE');
  }

  private readingStatusLabelByLevel(level: 'HIGH' | 'STABLE'): string {
    return this.t(
      level === 'HIGH'
        ? 'energyDashboard.readingStatuses.high'
        : 'energyDashboard.readingStatuses.normal'
    );
  }

  private t(key: string, params?: Record<string, unknown>): string {
    this.uiPreferences.currentLanguage();

    return this.translate.instant(key, params);
  }

  private currentLocale(): string {
    const localeByLanguage = {
      es: 'es-PE',
      en: 'en-US',
      pt: 'pt-BR',
    };

    return localeByLanguage[this.uiPreferences.currentLanguage()];
  }

  private getRoomNameForDevice(deviceId: number): string {
    const activeLocationId = this.activeLocationId();
    const assignment = this.workplaceFacade
      .deviceAssignments()
      .find(
        (item) =>
          item.deviceId === deviceId &&
          (!activeLocationId || item.locationId === activeLocationId)
      );

    return assignment
      ? this.workplaceFacade.getRoomName(assignment.roomId)
      : this.t('energyDashboard.noRoom');
  }

  private getDeviceTypeForDevice(deviceId: number): string {
    const summary = this.summary();
    const device =
      summary?.activeDeviceDetails.find((item) => item.deviceId === deviceId) ??
      summary?.topDevices.find((item) => item.deviceId === deviceId);

    return device?.type ?? 'OTHER';
  }

  private async loadSamplingSettings(): Promise<void> {
    try {
      this.sampleSeconds.set(await this.energyMonitoringFacade.loadSamplingSeconds());
    } catch (error) {
      console.error(error);
      this.toastService.warning(this.t('energyDashboard.toasts.samplingLoadFallback'));
    }
  }

  private restartAutoRefresh(): void {
    if (this.refreshIntervalId) {
      clearInterval(this.refreshIntervalId);
    }

    this.refreshIntervalId = setInterval(async () => {
      await this.energyMonitoringFacade.loadReadings(false);

      if (this.energyMonitoringFacade.error()) {
        this.toastService.error(this.t('energyDashboard.toasts.refreshError'));
      }
    }, this.sampleSeconds() * 1000);
  }

  private periodRange(period: ConsumptionPeriod): { start: number; end: number } {
    const now = new Date();
    const start = new Date(now);

    if (period === 'DAILY') {
      start.setHours(0, 0, 0, 0);
      return { start: start.getTime(), end: now.getTime() };
    }

    if (period === 'WEEKLY') {
      start.setDate(now.getDate() - 6);
      start.setHours(0, 0, 0, 0);
      return { start: start.getTime(), end: now.getTime() };
    }

    start.setDate(1);
    start.setHours(0, 0, 0, 0);
    return { start: start.getTime(), end: now.getTime() };
  }

  private toTimestamp(value: string): number {
    const time = new Date(value).getTime();

    return Number.isNaN(time) ? 0 : time;
  }

  private isConsumptionPeriod(value: string): value is ConsumptionPeriod {
    return ['DAILY', 'WEEKLY', 'MONTHLY'].includes(value);
  }

  private toConsumptionChartPoints(
    reading: EnergyReading,
    readingIndex: number,
    readingsCount: number
  ): ConsumptionChartPoint[] {
    const recordedAt = new Date(reading.recordedAt);

    if (Number.isNaN(recordedAt.getTime())) {
      return [];
    }

    const totalSeconds = Math.max(0, Number(reading.sampleSeconds ?? 0));
    const configuredSeconds = Math.max(5, Number(this.sampleSeconds() || 15));
    const visualSegmentSeconds = Math.min(configuredSeconds, 15);
    const maxSegmentsForReading = this.maxSegmentsForVisibleReading(readingsCount);
    const sparseMinimumSegments =
      readingsCount <= 1 && totalSeconds >= 5
        ? 7
        : readingsCount <= 2 && totalSeconds >= 5
          ? 4
          : 1;
    const measuredSegmentCount =
      totalSeconds > visualSegmentSeconds
        ? Math.min(
            Math.ceil(totalSeconds / visualSegmentSeconds),
            maxSegmentsForReading
          )
        : 1;
    const segmentCount = Math.min(
      Math.max(measuredSegmentCount, sparseMinimumSegments),
      maxSegmentsForReading
    );
    const segmentSeconds = segmentCount > 1 ? totalSeconds / segmentCount : totalSeconds;
    const segmentRatio =
      totalSeconds > 0 && segmentCount > 1 ? segmentSeconds / totalSeconds : 1;
    const kilowattHours = Number(reading.kilowattHours ?? 0);
    const estimatedCost = Number(reading.estimatedCost ?? 0);

    return Array.from({ length: segmentCount }, (_, segmentIndex) => {
      const segmentOffset = segmentCount - segmentIndex - 1;
      const segmentRecordedAt =
        segmentCount > 1
          ? new Date(recordedAt.getTime() - segmentOffset * segmentSeconds * 1000)
          : recordedAt;

      return {
        key: `${reading.id}-${reading.deviceId}-${recordedAt.getTime()}-${readingIndex}-${segmentIndex}`,
        deviceId: reading.deviceId,
        deviceName: reading.deviceName,
        deviceEmoji: this.getDeviceEmoji(reading.deviceId, reading.deviceName),
        recordedAt: segmentRecordedAt,
        watts: Number(reading.watts ?? 0),
        kilowattHours: kilowattHours * segmentRatio,
        estimatedCost: estimatedCost * segmentRatio,
        sampleSeconds: Math.round(segmentSeconds),
        percentage: 0,
        isHigh: reading.isHigh === true || reading.status === 'HIGH',
      };
    });
  }

  private maxSegmentsForVisibleReading(readingsCount: number): number {
    if (readingsCount > 160) {
      return 1;
    }

    if (readingsCount > 80) {
      return 2;
    }

    if (readingsCount > 40) {
      return 3;
    }

    if (readingsCount > 20) {
      return 6;
    }

    return this.maxSegmentsPerReading;
  }

  private getDeviceEmojiByType(type: string): string {
    const icons: Record<string, string> = {
      PLUG: '🔌',
      LIGHT: '💡',
      SWITCH: '🔘',
      SENSOR: '📡',
      OTHER: '⚡',
    };

    return icons[type] ?? this.getDeviceEmojiByName(type);
  }

  private getDeviceEmojiByName(value: string): string {
    const normalized = this.normalizeText(value);

    if (
      normalized.includes('luz') ||
      normalized.includes('lampara') ||
      normalized.includes('foco') ||
      normalized.includes('bombilla') ||
      normalized.includes('led')
    ) {
      return '💡';
    }

    if (
      normalized.includes('enchufe') ||
      normalized.includes('plug') ||
      normalized.includes('tomacorriente') ||
      normalized.includes('socket')
    ) {
      return '🔌';
    }

    if (
      normalized.includes('sensor') ||
      normalized.includes('movimiento') ||
      normalized.includes('temperatura') ||
      normalized.includes('humedad') ||
      normalized.includes('presencia')
    ) {
      return '📡';
    }

    if (
      normalized.includes('switch') ||
      normalized.includes('interruptor') ||
      normalized.includes('rele') ||
      normalized.includes('relay')
    ) {
      return '🔘';
    }

    if (
      normalized.includes('tv') ||
      normalized.includes('televisor') ||
      normalized.includes('television') ||
      normalized.includes('pantalla')
    ) {
      return '📺';
    }

    if (
      normalized.includes('micro') ||
      normalized.includes('horno') ||
      normalized.includes('cocina')
    ) {
      return '🍳';
    }

    if (
      normalized.includes('frigo') ||
      normalized.includes('nevera') ||
      normalized.includes('refriger') ||
      normalized.includes('congel')
    ) {
      return '❄️';
    }

    if (
      normalized.includes('aire') ||
      normalized.includes('acondicion') ||
      normalized.includes('clima') ||
      normalized.includes('climat')
    ) {
      return '🌀';
    }

    return '⚡';
  }

  private normalizeText(value: string): string {
    return value
      .trim()
      .toLocaleLowerCase()
      .normalize('NFD')
      .replace(/[\u0300-\u036f]/g, '');
  }
}


============================================================
FILE: src\app\energy-monitoring\presentation\pages\energy-history\energy-history-page.component.html
============================================================

<section class="history-page ec-page">
  <header class="ec-page-heading">
    <p class="ec-page-eyebrow">{{ 'history.eyebrow' | translate }}</p>

    <h1 class="ec-page-title">
      {{ 'history.title' | translate }}
    </h1>

    <p class="ec-page-description">
      {{ 'history.subtitle' | translate }}
    </p>
  </header>

  <section class="filter-card">
    <div class="filter-card__heading">
      <div>
        <h2>{{ 'history.filterTitle' | translate }}</h2>
        <p>
          {{ 'history.filterDescription' | translate }}
        </p>
      </div>

      <app-button variant="primary" (pressed)="exportCsv()">
        {{ 'energy.exportCsv' | translate }}
      </app-button>
    </div>

    <app-energy-filter-form
  (rangeChanged)="onDateRangeChange($event)"
  (reset)="resetFilter()"
/>
  </section>

  @if (energyMonitoringFacade.loading()) {
    <div class="loading-wrapper">
      <app-loading-spinner />
    </div>
  } @else {
    <section class="table-card">
  <div class="table-heading">
    <div>
      <h2>{{ 'history.registeredReadings' | translate }}</h2>
      <p>{{ 'history.registeredReadingsDescription' | translate }}</p>
    </div>

    <span class="count-pill" aria-live="polite">
      <strong>{{ filteredHistoryReadings().length }}</strong>
      <span>{{ 'history.records' | translate }}</span>
    </span>
  </div>

  <section class="advanced-filters" [attr.aria-label]="'history.advancedFiltersAria' | translate">
  <div class="filter-field scope-field">
    <span>{{ 'history.scope.label' | translate }}</span>

    <app-dropdown
      [options]="historyScopeOptions()"
      [value]="historyScope()"
      [placeholder]="'history.scope.label' | translate"
      (valueChange)="onHistoryScopeChange($event)"
    />
  </div>

  <label class="filter-field search-field">
    {{ 'history.device' | translate }}
    <input
      type="search"
      [value]="deviceNameFilter()"
      [placeholder]="'history.deviceNamePlaceholder' | translate"
      (input)="onDeviceNameFilterChange($any($event.target).value)"
    />
  </label>

  <app-number-stepper
    [label]="'history.deviceId' | translate"
    [value]="deviceIdFilter()"
    [min]="1"
    [step]="1"
    [placeholder]="'history.deviceIdPlaceholder' | translate"
    (valueChange)="onDeviceIdFilterChange($event)"
  />

  <app-number-stepper
    [label]="'history.readingId' | translate"
    [value]="readingIdFilter()"
    [min]="1"
    [step]="1"
    [placeholder]="'history.readingIdPlaceholder' | translate"
    (valueChange)="onReadingIdFilterChange($event)"
  />

  <div class="filter-field">
  <span>{{ 'history.status' | translate }}</span>

  <app-dropdown
    [options]="statusOptions"
    [value]="statusFilter()"
    [placeholder]="'history.status' | translate"
    (valueChange)="onStatusFilterChange($event)"
  />
</div>

  <app-number-stepper
    [label]="'history.minWatts' | translate"
    [value]="minWatts()"
    [min]="0"
    [step]="5"
    suffix="W"
    placeholder="0"
    (valueChange)="onMinWattsChange($event)"
  />

  <app-number-stepper
    [label]="'history.maxWatts' | translate"
    [value]="maxWatts()"
    [min]="0"
    [step]="5"
    suffix="W"
    placeholder="1000"
    (valueChange)="onMaxWattsChange($event)"
  />

  <div class="filter-field">
  <span>{{ 'history.sortBy' | translate }}</span>

  <app-dropdown
    [options]="sortOptions"
    [value]="sortOption()"
    [placeholder]="'history.sortBy' | translate"
    (valueChange)="onSortOptionChange($event)"
  />
</div>

  <app-button variant="secondary" (pressed)="clearAdvancedFilters()">
    {{ 'history.clearFilters' | translate }}
  </app-button>
</section>

  <p class="scope-summary">{{ historyScopeSummary() }}</p>

  @if (energyMonitoringFacade.loading()) {
    <div class="loading-wrapper">
      <app-loading-spinner />
    </div>
  } @else {
    @if (paginatedReadings().length === 0) {
      <app-empty-state
        [eyebrow]="'history.emptyEyebrow' | translate"
        [title]="'history.emptyTitle' | translate"
        [description]="'history.emptyDescription' | translate"
        [compact]="true"
      />
    } @else {
    <div class="table-wrapper ec-scrollbar">
      <table>
        <thead>
          <tr>
            <th>{{ 'history.device' | translate }}</th>
            <th>{{ 'history.watts' | translate }}</th>
            <th>{{ 'history.energy' | translate }}</th>
            <th>{{ 'history.estimatedCost' | translate }}</th>
            <th>{{ 'history.date' | translate }}</th>
            <th>{{ 'history.time' | translate }}</th>
            <th>{{ 'history.interval' | translate }}</th>
            <th>{{ 'history.status' | translate }}</th>
          </tr>
        </thead>

        <tbody>
          @for (reading of paginatedReadings(); track trackReading($index, reading)) {
            <tr>
              <td>
                <div class="device-cell">
                  <strong>{{ reading.deviceName }}</strong>
                  <small>{{ readingMeta(reading) }}</small>
                </div>
              </td>

              <td>
                <strong>{{ formatWatts(reading.watts) }}</strong>
              </td>

              <td>{{ formatKilowattHours(reading.kilowattHours) }}</td>

              <td>{{ formatEstimatedCost(reading.estimatedCost) }}</td>

              <td>{{ formatDate(reading.recordedAt) }}</td>

              <td>{{ formatTime(reading.recordedAt) }}</td>

              <td>{{ formatSampleSeconds(reading.sampleSeconds) }}</td>

              <td>
                <span class="status" [class.high]="reading.isHigh">
                  {{ readingStatusLabel(reading) }}
                </span>
              </td>
            </tr>
          }
        </tbody>
      </table>
    </div>
    }

    <footer class="pagination-bar">
      <p>
        {{ 'history.showing' | translate }}
        <strong>{{ pageStart() }}-{{ pageEnd() }}</strong>
        {{ 'history.of' | translate }}
        <strong>{{ filteredHistoryReadings().length }}</strong>
        {{ 'history.readings' | translate }}
      </p>

      <div class="pagination-actions">
        <app-button
          variant="secondary"
          size="sm"
          [disabled]="currentPage() === 1"
          (pressed)="previousPage()"
        >
          {{ 'history.previous' | translate }}
        </app-button>

        <span>{{ 'history.pageOf' | translate: { page: currentPage(), total: totalPages() } }}</span>

        <app-button
          variant="secondary"
          size="sm"
          [disabled]="currentPage() === totalPages()"
          (pressed)="nextPage()"
        >
          {{ 'history.next' | translate }}
        </app-button>
      </div>
    </footer>
  }
</section>
  }
</section>


============================================================
FILE: src\app\energy-monitoring\presentation\pages\energy-history\energy-history-page.component.scss
============================================================

:host {
  display: block;
}

.history-page {
  display: grid;
  gap: 40px;
}

.history-page .ec-page-heading {
  max-width: 980px;
}

.filter-card,
.table-card {
  border: 1px solid var(--border-color);
  border-radius: 30px;
  background: var(--surface-color);
  box-shadow: var(--ec-shadow-soft);
}

.filter-card {
  display: grid;
  gap: 24px;
  padding: 32px;
}

.filter-card__heading,
.table-heading {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 18px;
}

.filter-card h2,
.table-heading h2 {
  margin: 0 0 10px;
  color: var(--text-primary);
  font-size: 2rem;
  line-height: 1.1;
  letter-spacing: -0.025em;
}

.filter-card p,
.table-heading p {
  margin: 0;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.table-card {
  display: grid;
  gap: 26px;
  padding: 32px;
}

.count-pill {
  flex: 0 0 auto;
  display: inline-flex;
  align-items: center;
  gap: 7px;
  width: fit-content;
  min-height: 36px;
  padding: 0 13px;
  border: 1px solid rgba(79, 195, 255, 0.26);
  border-radius: 999px;
  background:
    linear-gradient(135deg, rgba(79, 195, 255, 0.16), rgba(255, 255, 255, 0.04)),
    var(--ec-accent-soft);
  color: var(--accent-color);
  font-size: 0.8rem;
  font-weight: 900;
  box-shadow: var(--ec-shadow-soft);
}

.count-pill::before {
  content: '';
  width: 8px;
  height: 8px;
  border-radius: 999px;
  background: var(--accent-color);
  box-shadow: 0 0 14px rgba(14, 165, 233, 0.42);
}

.count-pill strong {
  color: var(--text-primary);
  font-size: 0.92rem;
  font-variant-numeric: tabular-nums;
}

.count-pill span {
  color: var(--ec-copy-color);
  font-size: 0.78rem;
}

.table-wrapper {
  overflow-x: auto;
}

table {
  width: 100%;
  min-width: 760px;
  border-collapse: collapse;
}

th,
td {
  padding: 16px 14px;
  border-bottom: 1px solid var(--border-color);
  text-align: left;
}

th {
  color: var(--text-muted);
  font-size: 0.78rem;
  font-weight: 900;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

td {
  color: var(--text-secondary);
  font-weight: 750;
}

td strong {
  color: var(--text-primary);
}

.status {
  display: inline-flex;
  align-items: center;
  min-height: 30px;
  padding: 0 11px;
  border-radius: 999px;
  color: var(--ec-status-success-text);
  background: var(--ec-status-success-bg);
  font-size: 0.76rem;
  font-weight: 900;
}

.status.high {
  color: var(--ec-status-danger-text);
  background: var(--ec-status-danger-bg-strong);
}

.empty {
  padding: 26px 12px;
  text-align: center;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.loading-wrapper {
  display: grid;
  place-items: center;
  min-height: 220px;
}

.device-cell {
  display: grid;
  gap: 4px;
}

.device-cell strong {
  color: var(--text-primary);
  font-weight: 900;
}

.device-cell small {
  color: var(--ec-copy-color);
  font-size: 0.75rem;
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.table-wrapper {
  overflow-x: auto;
}

table {
  min-width: 980px;
}

td {
  vertical-align: middle;
}

td:nth-child(3),
td:nth-child(4),
td:nth-child(7) {
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
}

.advanced-filters {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  gap: 12px;
  align-items: end;
  margin: 24px 0 18px;
  padding: 16px;
  border: 1px solid var(--border-color);
  border-radius: 22px;
  background: var(--ec-surface-subtle);
}

.advanced-filters app-number-stepper,
.advanced-filters app-dropdown,
.advanced-filters app-button {
  min-width: 0;
}

.advanced-filters app-button {
  align-self: end;
}

:host ::ng-deep .advanced-filters app-button .app-button {
  min-height: 46px;
  padding-inline: 18px;
}

.filter-field {
  display: grid;
  gap: 8px;
  color: var(--text-primary);
  font-size: 0.82rem;
  font-weight: 900;
}

.filter-field > span {
  color: var(--text-primary);
  font-size: 0.82rem;
  font-weight: 900;
}

.filter-field input,
.filter-field select {
  width: 100%;
  min-height: 46px;
  padding: 0 14px;
  border: 1px solid var(--border-color);
  border-radius: 15px;
  color: var(--text-primary);
  background: var(--field-bg);
  font: inherit;
  font-weight: 850;
  outline: none;
}

.filter-field input::placeholder {
  color: var(--text-muted);
}

.filter-field input:focus,
.filter-field select:focus {
  border-color: var(--border-strong);
  box-shadow: var(--ec-focus-ring);
}

.filter-field app-dropdown {
  width: 100%;
  min-width: 0;
}

.search-field {
  grid-column: span 2;
}

.scope-summary {
  margin: -8px 0 2px;
  color: var(--accent-color);
  font-size: 0.82rem;
  font-weight: 900;
}

.pagination-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  margin-top: 18px;
  padding-top: 18px;
  border-top: 1px solid var(--border-color);
}

.pagination-bar p {
  margin: 0;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.pagination-actions {
  display: flex;
  align-items: center;
  gap: 10px;
}

.pagination-actions span {
  color: var(--text-primary);
  font-weight: 900;
}

@media (max-width: 1280px) {
  .search-field {
    grid-column: span 2;
  }
}

@media (max-width: 860px) {
  .advanced-filters {
    grid-template-columns: 1fr 1fr;
  }

  .search-field {
    grid-column: 1 / -1;
  }
}

@media (max-width: 620px) {
  .advanced-filters {
    grid-template-columns: 1fr;
  }

  .search-field {
    grid-column: auto;
  }
}


============================================================
FILE: src\app\energy-monitoring\presentation\pages\energy-history\energy-history-page.component.ts
============================================================

import { Component, OnInit, computed, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { EnergyMonitoringFacade } from '../../../application/services/energy-monitoring.facade';
import { EnergyReadingsFilterCriteria } from '../../../application/criteria/energy-readings-filter.criteria';
import { EnergyReading } from '../../../domain/model/energy-reading.entity';

import { EnergyFilterFormComponent } from '../../components/energy-filter-form/energy-filter-form.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';

import { AppNumberStepperComponent } from '../../../../shared/presentation/components/app-number-stepper/app-number-stepper.component';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';

import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { WorkplaceFacade } from '../../../../workplace/application/services/workplace.facade';
import { ActiveWorkplaceContextService } from '../../../../workplace/application/services/active-workplace-context.service';
import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';

type HistoryStatusFilter = 'ALL' | 'NORMAL' | 'HIGH';
type HistorySortOption = 'NEWEST' | 'OLDEST' | 'WATTS_DESC' | 'WATTS_ASC' | 'COST_DESC';
type HistoryScope = 'ACTIVE_SEDE' | 'GLOBAL';

@Component({
  selector: 'app-energy-history-page',
  standalone: true,
  imports: [
  FormsModule,
  TranslateModule,
  EnergyFilterFormComponent,
  LoadingSpinnerComponent,
  AppNumberStepperComponent,
  AppButtonComponent,
  AppDropdownComponent,
  EmptyStateComponent,
],
  templateUrl: './energy-history-page.component.html',
  styleUrls: ['./energy-history-page.component.scss'],
})
export class EnergyHistoryPageComponent implements OnInit {
  readonly pageSize = 20;

  readonly deviceNameFilter = signal('');
  readonly deviceIdFilter = signal<number | null>(null);
  readonly readingIdFilter = signal<number | null>(null);
  readonly currentPage = signal(1);
  readonly searchTerm = signal('');
  readonly statusFilter = signal<HistoryStatusFilter>('ALL');
  readonly minWatts = signal<number | null>(null);
  readonly maxWatts = signal<number | null>(null);
  readonly sortOption = signal<HistorySortOption>('NEWEST');
  readonly historyScope = signal<HistoryScope>('ACTIVE_SEDE');

  readonly startDateFilter = signal('');
  readonly endDateFilter = signal('');

  readonly activeLocation = computed(() => {
    const activeLocationId = this.activeWorkplaceContext.activeLocationId();

    if (!activeLocationId) {
      return null;
    }

    return (
      this.workplaceFacade
        .locations()
        .find((location) => location.id === activeLocationId) ?? null
    );
  });

  readonly activeLocationDeviceIds = computed(() => {
    const activeLocationId = this.activeWorkplaceContext.activeLocationId();

    if (!activeLocationId) {
      return new Set<number>();
    }

    return new Set(
      this.workplaceFacade
        .deviceAssignments()
        .filter((assignment) => assignment.locationId === activeLocationId)
        .map((assignment) => assignment.deviceId)
    );
  });

  readonly historyScopeOptions = computed<DropdownOption[]>(() => [
    {
      label: 'Sede activa',
      labelKey: 'history.scope.active',
      value: 'ACTIVE_SEDE',
      description: this.activeLocation()
        ? this.t('history.scope.activeDescription', {
            site: this.activeLocation()?.name,
          })
        : this.t('history.scope.activeFallbackDescription'),
    },
    {
      label: 'Global',
      labelKey: 'history.scope.global',
      value: 'GLOBAL',
      description: this.t('history.scope.globalDescription'),
    },
  ]);

  readonly historyScopeSummary = computed(() => {
    if (this.historyScope() === 'GLOBAL') {
      return this.t('history.scopeSummary.global');
    }

    const activeLocation = this.activeLocation();

    if (!activeLocation) {
      return this.t('history.scopeSummary.noActiveSite');
    }

    return this.t('history.scopeSummary.activeSite', {
      site: activeLocation.name,
    });
  });

  readonly statusOptions: DropdownOption[] = [
  { label: 'Todos', labelKey: 'history.statusOptions.all', value: 'ALL' },
  { label: 'Normal', labelKey: 'history.statusOptions.normal', value: 'NORMAL' },
  { label: 'Alto', labelKey: 'history.statusOptions.high', value: 'HIGH' },
];

readonly sortOptions: DropdownOption[] = [
  { label: 'Mas recientes', labelKey: 'history.sortOptions.newest', value: 'NEWEST' },
  { label: 'Mas antiguas', labelKey: 'history.sortOptions.oldest', value: 'OLDEST' },
  { label: 'Mayor consumo', labelKey: 'history.sortOptions.wattsDesc', value: 'WATTS_DESC' },
  { label: 'Menor consumo', labelKey: 'history.sortOptions.wattsAsc', value: 'WATTS_ASC' },
  { label: 'Mayor costo', labelKey: 'history.sortOptions.costDesc', value: 'COST_DESC' },
];

  readonly filteredHistoryReadings = computed(() => {
  const startDate = this.startDateFilter();
  const endDate = this.endDateFilter();

  const deviceName = this.normalize(this.deviceNameFilter());
  const deviceId = this.deviceIdFilter();
  const readingId = this.readingIdFilter();
  const status = this.statusFilter();
  const minWatts = this.minWatts();
  const maxWatts = this.maxWatts();
  const sort = this.sortOption();
  const scope = this.historyScope();
  const activeLocationId = this.activeWorkplaceContext.activeLocationId();
  const activeLocationDeviceIds = this.activeLocationDeviceIds();

  return [...this.energyMonitoringFacade.readings()]
    .filter((reading) => {
      const readingDate = this.toInputDate(new Date(reading.recordedAt));

      const matchesStartDate = !startDate || readingDate >= startDate;
      const matchesEndDate = !endDate || readingDate <= endDate;

      const matchesDeviceName =
        !deviceName || this.normalize(reading.deviceName).includes(deviceName);

      const matchesDeviceId =
        deviceId === null || reading.deviceId === deviceId;

      const matchesReadingId =
        readingId === null || reading.id === readingId;

      const matchesStatus =
        status === 'ALL' ||
        (status === 'HIGH' && reading.isHigh) ||
        (status === 'NORMAL' && reading.isNormal);

      const matchesMinWatts =
        minWatts === null || reading.watts >= minWatts;

      const matchesMaxWatts =
        maxWatts === null || reading.watts <= maxWatts;

      const matchesScope =
        scope === 'GLOBAL' ||
        (activeLocationId !== null && activeLocationDeviceIds.has(reading.deviceId));

      return (
        matchesScope &&
        matchesStartDate &&
        matchesEndDate &&
        matchesDeviceName &&
        matchesDeviceId &&
        matchesReadingId &&
        matchesStatus &&
        matchesMinWatts &&
        matchesMaxWatts
      );
    })
    .sort((first, second) => {
      switch (sort) {
        case 'OLDEST':
          return new Date(first.recordedAt).getTime() - new Date(second.recordedAt).getTime();

        case 'WATTS_DESC':
          return second.watts - first.watts;

        case 'WATTS_ASC':
          return first.watts - second.watts;

        case 'COST_DESC':
          return second.estimatedCost - first.estimatedCost;

        case 'NEWEST':
        default:
          return new Date(second.recordedAt).getTime() - new Date(first.recordedAt).getTime();
      }
    });
});

  readonly totalPages = computed(() =>
    Math.max(1, Math.ceil(this.filteredHistoryReadings().length / this.pageSize))
  );

  readonly paginatedReadings = computed(() => {
    const page = Math.min(this.currentPage(), this.totalPages());
    const start = (page - 1) * this.pageSize;
    const end = start + this.pageSize;

    return this.filteredHistoryReadings().slice(start, end);
  });

  readonly pageStart = computed(() => {
    if (this.filteredHistoryReadings().length === 0) {
      return 0;
    }

    return (this.currentPage() - 1) * this.pageSize + 1;
  });

  readonly pageEnd = computed(() =>
    Math.min(this.currentPage() * this.pageSize, this.filteredHistoryReadings().length)
  );

  constructor(
    readonly energyMonitoringFacade: EnergyMonitoringFacade,
    readonly workplaceFacade: WorkplaceFacade,
    private readonly activeWorkplaceContext: ActiveWorkplaceContextService,
    private readonly toastService: ToastService,
    private readonly translate: TranslateService,
    private readonly uiPreferences: UiPreferencesService
  ) {}

  async ngOnInit(): Promise<void> {
    await Promise.all([
      this.energyMonitoringFacade.loadReadings(),
      this.workplaceFacade.loadWorkplace(),
    ]);

    this.activeWorkplaceContext.ensureActiveLocation(
      this.workplaceFacade.locations()
    );
  }


resetFilter(): void {
  this.startDateFilter.set('');
  this.endDateFilter.set('');
  this.energyMonitoringFacade.resetFilter();
  this.clearAdvancedFilters();
  this.goToFirstPage();
}

  exportCsv(): void {
    const success = this.energyMonitoringFacade.exportCsv({
      fileName: this.historyExportFileName(),
      readings: this.filteredHistoryReadings(),
    });

    if (success) {
      this.toastService.success(this.t('history.toasts.exportSuccess'));
    } else {
      this.toastService.error(this.t('history.toasts.exportError'));
    }
  }

  onSearchTermChange(value: string): void {
    this.searchTerm.set(value);
    this.goToFirstPage();
  }

  onStatusFilterChange(value: string): void {
  if (!['ALL', 'NORMAL', 'HIGH'].includes(value)) {
    return;
  }

  this.statusFilter.set(value as HistoryStatusFilter);
  this.goToFirstPage();
}

  onMinWattsChange(value: number | null): void {
  this.minWatts.set(value);
  this.goToFirstPage();
}

  onMaxWattsChange(value: number | null): void {
  this.maxWatts.set(value);
  this.goToFirstPage();
}

  onSortOptionChange(value: string): void {
  if (!['NEWEST', 'OLDEST', 'WATTS_DESC', 'WATTS_ASC', 'COST_DESC'].includes(value)) {
    return;
  }

  this.sortOption.set(value as HistorySortOption);
  this.goToFirstPage();
}

  onHistoryScopeChange(value: string): void {
  if (!['ACTIVE_SEDE', 'GLOBAL'].includes(value)) {
    return;
  }

  this.historyScope.set(value as HistoryScope);
  this.goToFirstPage();
}

  clearAdvancedFilters(): void {
  this.deviceNameFilter.set('');
  this.deviceIdFilter.set(null);
  this.readingIdFilter.set(null);
  this.statusFilter.set('ALL');
  this.minWatts.set(null);
  this.maxWatts.set(null);
  this.sortOption.set('NEWEST');
  this.goToFirstPage();
}

  previousPage(): void {
    if (this.currentPage() > 1) {
      this.currentPage.update((page) => page - 1);
    }
  }

  nextPage(): void {
    if (this.currentPage() < this.totalPages()) {
      this.currentPage.update((page) => page + 1);
    }
  }

  goToFirstPage(): void {
    this.currentPage.set(1);
  }

  onDeviceNameFilterChange(value: string): void {
  this.deviceNameFilter.set(value);
  this.goToFirstPage();
}

onDeviceIdFilterChange(value: number | null): void {
  this.deviceIdFilter.set(value);
  this.goToFirstPage();
}

onReadingIdFilterChange(value: number | null): void {
  this.readingIdFilter.set(value);
  this.goToFirstPage();
}

  formatDate(value: string): string {
    const date = new Date(value);

    if (Number.isNaN(date.getTime())) {
      return this.t('history.emptyValues.noDate');
    }

    return new Intl.DateTimeFormat(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
    }).format(date);
  }

  formatTime(value: string): string {
    const date = new Date(value);

    if (Number.isNaN(date.getTime())) {
      return this.t('history.emptyValues.noTime');
    }

    return new Intl.DateTimeFormat(this.currentLocale(), {
      hour: '2-digit',
      minute: '2-digit',
      hour12: true,
    }).format(date);
  }

  formatWatts(value: number): string {
    return `${new Intl.NumberFormat(this.currentLocale(), {
      maximumFractionDigits: 2,
    }).format(value)} W`;
  }

  formatKilowattHours(value: number): string {
    return `${new Intl.NumberFormat(this.currentLocale(), {
      minimumFractionDigits: 6,
      maximumFractionDigits: 6,
    }).format(value)} kWh`;
  }

  formatEstimatedCost(value: number): string {
    return `S/ ${new Intl.NumberFormat(this.currentLocale(), {
      minimumFractionDigits: 4,
      maximumFractionDigits: 4,
    }).format(value)}`;
  }

  formatSampleSeconds(value: number): string {
    if (!value || value <= 0) {
      return this.t('history.emptyValues.undefined');
    }

    if (value < 60) {
      return `${value} s`;
    }

    const minutes = value / 60;

    return `${new Intl.NumberFormat(this.currentLocale(), {
      maximumFractionDigits: 1,
    }).format(minutes)} min`;
  }

  readingMeta(reading: EnergyReading): string {
    return this.t('history.readingMeta', {
      deviceId: reading.deviceId,
      readingId: reading.id,
    });
  }

  readingStatusLabel(reading: EnergyReading): string {
    return reading.isHigh
      ? this.t('history.statusOptions.high')
      : this.t('history.statusOptions.normal');
  }

  trackReading(_: number, reading: EnergyReading): number {
    return reading.id;
  }

  private normalize(value: string): string {
    return value.trim().toLowerCase();
  }

  onDateRangeChange(criteria: EnergyReadingsFilterCriteria): void {
  this.startDateFilter.set(criteria.startDate);
  this.endDateFilter.set(criteria.endDate);
  this.goToFirstPage();
}

private toInputDate(date: Date): string {
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');

  return `${year}-${month}-${day}`;
}

private historyExportFileName(): string {
  if (this.historyScope() === 'GLOBAL') {
    return 'electrocorp-energy-history-global';
  }

  const locationName = this.activeLocation()?.name ?? 'sede-activa';

  return `electrocorp-energy-history-${this.normalizeForFileName(locationName)}`;
}

private normalizeForFileName(value: string): string {
  return value
    .trim()
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '');
}

private t(key: string, params?: Record<string, unknown>): string {
  this.uiPreferences.currentLanguage();

  return this.translate.instant(key, params);
}

private currentLocale(): string {
  const localeByLanguage = {
    es: 'es-PE',
    en: 'en-US',
    pt: 'pt-BR',
  };

  return localeByLanguage[this.uiPreferences.currentLanguage()];
}
}


============================================================
FILE: src\app\energy-monitoring\presentation\routes\energy-monitoring.routes.ts
============================================================

import { Routes } from '@angular/router';

import { activeSubscriptionGuard } from '../../../shared/application/guards/active-subscription.guard';

export const ENERGY_MONITORING_ROUTES: Routes = [
  {
    path: 'energy/consumption',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Consumo' },
    loadComponent: () =>
      import('../pages/energy-dashboard/energy-dashboard-page.component').then(
        (m) => m.EnergyDashboardPageComponent
      ),
  },
  {
    path: 'energy',
    redirectTo: 'energy/consumption',
    pathMatch: 'full',
  },
  {
    path: 'energy/history',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Historial' },
    loadComponent: () =>
      import('../pages/energy-history/energy-history-page.component').then(
        (m) => m.EnergyHistoryPageComponent
      ),
  },
];


============================================================
FILE: src\app\iam\application\commands\delete-account.command.ts
============================================================

export interface DeleteAccountCommand {
  confirmation: string;
}


============================================================
FILE: src\app\iam\application\commands\recover-password.command.ts
============================================================

export interface RecoverPasswordCommand {
  email: string;
}


============================================================
FILE: src\app\iam\application\commands\sign-in.command.ts
============================================================

export interface SignInCommand {
  email: string;
  password: string;
}


============================================================
FILE: src\app\iam\application\commands\sign-up.command.ts
============================================================

export interface SignUpCommand {
  fullName: string;
  email: string;
  password: string;
}


============================================================
FILE: src\app\iam\application\commands\update-profile.command.ts
============================================================

export interface UpdateProfileCommand {
  fullName: string;
  email: string;
}


============================================================
FILE: src\app\iam\application\services\iam.facade.ts
============================================================

import { Injectable } from '@angular/core';
import { Router } from '@angular/router';
import { firstValueFrom } from 'rxjs';

import {
  AuthenticatedUserSession,
  AuthSessionService,
} from '../../../shared/application/services/auth-session.service';
import { ROUTE_PATHS } from '../../../shared/infrastructure/constants/route-paths';

import { User } from '../../domain/model/user.entity';

import { DeleteAccountCommand } from '../commands/delete-account.command';
import { RecoverPasswordCommand } from '../commands/recover-password.command';
import { SignInCommand } from '../commands/sign-in.command';
import { SignUpCommand } from '../commands/sign-up.command';
import { UpdateProfileCommand } from '../commands/update-profile.command';

import { AuthApiService } from '../../infrastructure/api/auth-api.service';
import { UsersApiService } from '../../infrastructure/api/users-api.service';
import { UserAssembler } from '../../infrastructure/assemblers/user.assembler';

import { IamStore } from '../stores/iam.store';

@Injectable({
  providedIn: 'root',
})
export class IamFacade {
  private readonly userAssembler = new UserAssembler();

  get currentUser() {
    return this.store.currentUser;
  }

  get loading() {
    return this.store.loading;
  }

  get error() {
    return this.store.error;
  }

  get isAuthenticated() {
    return this.store.isAuthenticated;
  }

  constructor(
    private readonly authApi: AuthApiService,
    private readonly usersApi: UsersApiService,
    private readonly authSession: AuthSessionService,
    private readonly router: Router,
    private readonly store: IamStore
  ) {}

  async signIn(command: SignInCommand): Promise<boolean> {
    this.startRequest();

    try {
      if (!this.isValidEmail(command.email)) {
        this.store.setError('auth.invalidEmail');
        return false;
      }

      if (!command.password.trim()) {
        this.store.setError('auth.signInError');
        return false;
      }

      const response = await firstValueFrom(
        this.authApi.signIn({
          email: command.email.trim().toLowerCase(),
          password: command.password,
        })
      );

      const user = this.userAssembler.toEntity(response.user);
      this.setAuthenticatedUser(user, response.token);

      await this.router.navigateByUrl(ROUTE_PATHS.HOME);

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('auth.signInError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async signUp(command: SignUpCommand): Promise<boolean> {
    this.startRequest();

    try {
      if (!command.fullName.trim()) {
        this.store.setError('auth.fullNameRequired');
        return false;
      }

      if (!this.isValidEmail(command.email)) {
        this.store.setError('auth.invalidEmail');
        return false;
      }

      if (!this.isAllowedEmailProvider(command.email)) {
        this.store.setError('auth.invalidEmailProvider');
        return false;
      }

      if (command.password.length < 8) {
        this.store.setError('auth.passwordTooShort');
        return false;
      }

      const response = await firstValueFrom(
        this.authApi.signUp({
          fullName: command.fullName.trim(),
          email: command.email.trim().toLowerCase(),
          password: command.password,
        })
      );

      const user = this.userAssembler.toEntity(response.user);
      this.setAuthenticatedUser(user, response.token);

      await this.router.navigateByUrl(ROUTE_PATHS.BILLING.PLANS);

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('auth.signUpError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async restoreSession(): Promise<boolean> {
    this.store.setLoading(true);
    this.authSession.setLoading(true);

    try {
      const storedSession = this.authSession.restoreStoredSession();

      if (!storedSession) {
        return false;
      }

      const response = await firstValueFrom(this.authApi.me());
      const user = this.userAssembler.toEntity(response);

      this.setAuthenticatedUser(user, storedSession.token);
      return true;
    } catch {
      this.store.setCurrentUser(null);
      this.authSession.clearSession();
      return false;
    } finally {
      this.store.setLoading(false);
      this.authSession.setLoading(false);
    }
  }

  async signOut(): Promise<void> {
    this.store.setLoading(true);

    try {
      await firstValueFrom(this.authApi.signOut());
    } catch (error) {
      console.error(error);
    } finally {
      this.store.reset();
      this.authSession.clearSession();
      await this.router.navigateByUrl(ROUTE_PATHS.IAM.LOGIN);
    }
  }

  async recoverPassword(command: RecoverPasswordCommand): Promise<boolean> {
    this.startRequest();

    try {
      if (!this.isValidEmail(command.email)) {
        this.store.setError('auth.invalidEmail');
        return false;
      }

      await firstValueFrom(
        this.authApi.recoverPassword(command.email.trim().toLowerCase())
      );

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('auth.recoverPasswordError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async updateProfile(command: UpdateProfileCommand): Promise<boolean> {
    this.startRequest();

    try {
      const currentUser = this.store.currentUser();

      if (!currentUser) {
        this.store.setError('auth.signInError');
        return false;
      }

      if (!command.fullName.trim()) {
        this.store.setError('auth.fullNameRequired');
        return false;
      }

      if (!this.isValidEmail(command.email)) {
        this.store.setError('auth.invalidEmail');
        return false;
      }

      const response = await firstValueFrom(
        this.usersApi.updateCurrentProfile({
          fullName: command.fullName.trim(),
          email: command.email.trim().toLowerCase(),
          accessProfileId: currentUser.accessProfileId,
        })
      );

      const updatedUser = this.userAssembler.toEntity(response);
      const token = this.authSession.token();

      if (!token) {
        this.store.setError('auth.signInError');
        return false;
      }

      this.setAuthenticatedUser(updatedUser, token);

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('auth.profileUpdateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async deleteAccount(command: DeleteAccountCommand): Promise<boolean> {
    this.startRequest();

    try {
      const currentUser = this.store.currentUser();

      if (!currentUser) {
        this.store.setError('auth.signInError');
        return false;
      }

      if (command.confirmation !== 'ELIMINAR') {
        this.store.setError('auth.deleteAccountConfirmationError');
        return false;
      }

      await firstValueFrom(this.usersApi.deleteCurrentAccount());

      this.store.reset();
      this.authSession.clearSession();

      await this.router.navigateByUrl(ROUTE_PATHS.IAM.LOGIN);

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('auth.deleteAccountError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  clearMessages(): void {
    this.store.clearMessages();
  }

  private setAuthenticatedUser(user: User, token: string): void {
    this.store.setCurrentUser(user);

    const session: AuthenticatedUserSession = {
      id: user.id,
      fullName: user.fullName,
      email: user.email,
      token,
      accessProfileId: user.accessProfileId,
      accessProfileName: user.accessProfileName,
    };

    this.authSession.setCurrentUser(session);
  }

  private startRequest(): void {
    this.store.setLoading(true);
    this.store.clearMessages();
  }

  private finishRequest(): void {
    this.store.setLoading(false);
  }

  private isValidEmail(email: string): boolean {
    return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.trim());
  }

  private isAllowedEmailProvider(email: string): boolean {
    const normalizedEmail = email.trim().toLowerCase();

    return (
      normalizedEmail.endsWith('@gmail.com') ||
      normalizedEmail.endsWith('@outlook.com') ||
      normalizedEmail.endsWith('@hotmail.com')
    );
  }
}


============================================================
FILE: src\app\iam\application\stores\iam.store.ts
============================================================

import { Injectable, computed, signal } from '@angular/core';

import { User } from '../../domain/model/user.entity';

@Injectable({
  providedIn: 'root',
})
export class IamStore {
  private readonly currentUserSignal = signal<User | null>(null);
  private readonly loadingSignal = signal<boolean>(false);
  private readonly errorSignal = signal<string | null>(null);

  readonly currentUser = computed(() => this.currentUserSignal());
  readonly loading = computed(() => this.loadingSignal());
  readonly error = computed(() => this.errorSignal());

  readonly isAuthenticated = computed(() => this.currentUserSignal() !== null);

  setCurrentUser(user: User | null): void {
    this.currentUserSignal.set(user);
  }

  setLoading(value: boolean): void {
    this.loadingSignal.set(value);
  }

  setError(value: string | null): void {
    this.errorSignal.set(value);
  }

  clearMessages(): void {
    this.errorSignal.set(null);
  }

  reset(): void {
    this.currentUserSignal.set(null);
    this.loadingSignal.set(false);
    this.errorSignal.set(null);
  }
}


============================================================
FILE: src\app\iam\domain\model\access-profile.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export type AccessProfileName = 'OWNER' | 'ADMIN' | 'MEMBER' | 'GUEST';

export class AccessProfile extends BaseEntity<number> {
  private readonly _name: AccessProfileName;
  private readonly _description: string;

  constructor(props: {
    id: number;
    name: AccessProfileName;
    description: string;
  }) {
    super(props.id);
    this._name = props.name;
    this._description = props.description;
  }

  get name(): AccessProfileName {
    return this._name;
  }

  get description(): string {
    return this._description;
  }

  get isOwner(): boolean {
    return this._name === 'OWNER';
  }

  get isAdmin(): boolean {
    return this._name === 'ADMIN';
  }

  get isMember(): boolean {
    return this._name === 'MEMBER';
  }

  get isGuest(): boolean {
    return this._name === 'GUEST';
  }
}

============================================================
FILE: src\app\iam\domain\model\user.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export class User extends BaseEntity<number> {
  private readonly _fullName: string;
  private readonly _email: string;
  private readonly _accessProfileId: number;
  private readonly _accessProfileName: string;
  private readonly _createdAt: string;

  constructor(props: {
    id: number;
    fullName: string;
    email: string;
    accessProfileId: number;
    accessProfileName: string;
    createdAt: string;
  }) {
    super(props.id);
    this._fullName = props.fullName;
    this._email = props.email;
    this._accessProfileId = props.accessProfileId;
    this._accessProfileName = props.accessProfileName;
    this._createdAt = props.createdAt;
  }

  get fullName(): string {
    return this._fullName;
  }

  get email(): string {
    return this._email;
  }

  get accessProfileId(): number {
    return this._accessProfileId;
  }

  get accessProfileName(): string {
    return this._accessProfileName;
  }

  get createdAt(): string {
    return this._createdAt;
  }

  get initials(): string {
    return this._fullName
      .split(' ')
      .filter(Boolean)
      .slice(0, 2)
      .map((part) => part.charAt(0).toUpperCase())
      .join('');
  }
}

============================================================
FILE: src\app\iam\infrastructure\api\access-profiles-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { AccessProfile } from '../../domain/model/access-profile.entity';
import { AccessProfileAssembler } from '../assemblers/access-profile.assembler';
import { AccessProfileResource } from '../resources/access-profile.resource';
import { AccessProfileResponse } from '../responses/access-profile.response';

@Injectable({
  providedIn: 'root',
})
export class AccessProfilesApiService extends BaseApiService<
  AccessProfile,
  AccessProfileResource,
  AccessProfileResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(http, apiBaseUrl, 'access-profiles', new AccessProfileAssembler());
  }
}

============================================================
FILE: src\app\iam\infrastructure\api\auth-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';

import { SignInResource } from '../resources/sign-in.resource';
import { SignUpResource } from '../resources/sign-up.resource';
import { AuthResponse } from '../responses/auth.response';
import { UserResponse } from '../responses/user.response';

@Injectable({
  providedIn: 'root',
})
export class AuthApiService {
  private readonly authUrl: string;

  constructor(
    private readonly http: HttpClient,
    @Inject(API_BASE_URL) private readonly apiBaseUrl: string
  ) {
    this.authUrl = `${this.apiBaseUrl}/auth`;
  }

  signIn(resource: SignInResource): Observable<AuthResponse> {
    return this.http.post<AuthResponse>(`${this.authUrl}/sign-in`, resource);
  }

  signUp(resource: SignUpResource): Observable<AuthResponse> {
    return this.http.post<AuthResponse>(`${this.authUrl}/sign-up`, resource);
  }

  signOut(): Observable<void> {
    return this.http.post<void>(`${this.authUrl}/sign-out`, {});
  }

  me(): Observable<UserResponse> {
    return this.http.get<UserResponse>(`${this.authUrl}/me`);
  }

  recoverPassword(email: string): Observable<void> {
    return this.http.post<void>(`${this.authUrl}/recover-password`, { email });
  }
}


============================================================
FILE: src\app\iam\infrastructure\api\iam-api.service.ts
============================================================

import { Injectable } from '@angular/core';

import { AuthApiService } from './auth-api.service';

@Injectable({
  providedIn: 'root',
})
export class IamApiService {
  constructor(readonly authApi: AuthApiService) {}
}

============================================================
FILE: src\app\iam\infrastructure\api\users-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { User } from '../../domain/model/user.entity';
import { UserAssembler } from '../assemblers/user.assembler';
import { UserResource } from '../resources/user.resource';
import { UserResponse } from '../responses/user.response';

@Injectable({
  providedIn: 'root',
})
export class UsersApiService extends BaseApiService<
  User,
  UserResource,
  UserResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(http, apiBaseUrl, 'users', new UserAssembler());
  }

  getCurrentProfile(): Observable<UserResponse> {
    return this.http.get<UserResponse>(`${this.resourceEndpoint}/me`);
  }

  updateCurrentProfile(resource: UserResource): Observable<UserResponse> {
    return this.http.put<UserResponse>(`${this.resourceEndpoint}/me`, resource);
  }

  deleteCurrentAccount(): Observable<void> {
    return this.http.delete<void>(`${this.resourceEndpoint}/me`);
  }

  updateProfile(userId: number, resource: UserResource): Observable<UserResponse> {
    return this.http.put<UserResponse>(
      `${this.resourceEndpoint}/${userId}/profile`,
      resource
    );
  }

  deleteAccount(userId: number): Observable<void> {
    return this.delete(userId);
  }
}


============================================================
FILE: src\app\iam\infrastructure\assemblers\access-profile.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { AccessProfile } from '../../domain/model/access-profile.entity';
import { AccessProfileResource } from '../resources/access-profile.resource';
import { AccessProfileResponse } from '../responses/access-profile.response';

export class AccessProfileAssembler extends BaseAssembler<
  AccessProfile,
  AccessProfileResource,
  AccessProfileResponse
> {
  override toEntity(response: AccessProfileResponse): AccessProfile {
    return new AccessProfile({
      id: response.id,
      name: response.name,
      description: response.description,
    });
  }

  override toResource(entity: AccessProfile): AccessProfileResource {
    return {
      name: entity.name,
      description: entity.description,
    };
  }
}

============================================================
FILE: src\app\iam\infrastructure\assemblers\user.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { User } from '../../domain/model/user.entity';
import { UserResource } from '../resources/user.resource';
import { UserResponse } from '../responses/user.response';

export class UserAssembler extends BaseAssembler<
  User,
  UserResource,
  UserResponse
> {
  override toEntity(response: UserResponse): User {
    return new User({
      id: response.id,
      fullName: response.fullName,
      email: response.email,
      accessProfileId: response.accessProfileId,
      accessProfileName: response.accessProfileName,
      createdAt: response.createdAt,
    });
  }

  override toResource(entity: User): UserResource {
    return {
      fullName: entity.fullName,
      email: entity.email,
      accessProfileId: entity.accessProfileId,
    };
  }
}

============================================================
FILE: src\app\iam\infrastructure\resources\access-profile.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';
import { AccessProfileName } from '../../domain/model/access-profile.entity';

export interface AccessProfileResource extends BaseResource {
  name: AccessProfileName;
  description: string;
}

============================================================
FILE: src\app\iam\infrastructure\resources\sign-in.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';

export interface SignInResource extends BaseResource {
  email: string;
  password: string;
}

============================================================
FILE: src\app\iam\infrastructure\resources\sign-up.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';

export interface SignUpResource extends BaseResource {
  fullName: string;
  email: string;
  password: string;
}

============================================================
FILE: src\app\iam\infrastructure\resources\user.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';

export interface UserResource extends BaseResource {
  fullName: string;
  email: string;
  accessProfileId?: number;
}

============================================================
FILE: src\app\iam\infrastructure\responses\access-profile.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';
import { AccessProfileName } from '../../domain/model/access-profile.entity';

export interface AccessProfileResponse extends BaseResponse<number> {
  name: AccessProfileName;
  description: string;
}

============================================================
FILE: src\app\iam\infrastructure\responses\auth.response.ts
============================================================

import { UserResponse } from './user.response';

export interface AuthResponse {
  user: UserResponse;
  token: string;
  message?: string;
}


============================================================
FILE: src\app\iam\infrastructure\responses\user.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';

export interface UserResponse extends BaseResponse<number> {
  fullName: string;
  email: string;
  accessProfileId: number;
  accessProfileName: string;
  createdAt: string;
}

============================================================
FILE: src\app\iam\presentation\pages\account\account-page.component.html
============================================================

<section class="settings-page">
    <header class="ec-page-heading">
      <p class="ec-page-eyebrow">{{ 'settings.nav.account' | translate }}</p>
      <h1 class="ec-page-title">{{ 'settings.account.title' | translate }}</h1>
      <p class="ec-page-description">
        {{ 'settings.account.pageDescription' | translate }}
      </p>
    </header>

    <app-settings-section
      [eyebrow]="'settings.nav.account' | translate"
      [title]="'settings.nav.accountDescription' | translate"
      [description]="'settings.account.identityDescription' | translate"
      [stackedHeader]="true"
    >
      <div class="account-panel">
        <div class="avatar" aria-hidden="true">{{ userInitials }}</div>

        <div class="account-details">
          <article>
            <span>{{ 'devices.name' | translate }}</span>
            <strong>{{ iamFacade.currentUser()?.fullName || ('settings.common.notAvailable' | translate) }}</strong>
          </article>

          <article>
            <span>{{ 'settings.common.email' | translate }}</span>
            <strong>{{ iamFacade.currentUser()?.email || ('settings.common.notAvailable' | translate) }}</strong>
          </article>

          <article>
            <span>{{ 'settings.account.role' | translate }}</span>
            <strong>{{ iamFacade.currentUser()?.accessProfileName || ('settings.account.userFallback' | translate) }}</strong>
          </article>

          <article>
            <span>{{ 'settings.account.createdAt' | translate }}</span>
            <strong>{{ formatDate(iamFacade.currentUser()?.createdAt) }}</strong>
          </article>
        </div>
      </div>
    </app-settings-section>

    <app-settings-section
      [eyebrow]="'settings.account.contextEyebrow' | translate"
      [title]="'settings.account.operationalReadingTitle' | translate"
      [description]="'settings.account.operationalReadingDescription' | translate"
      [stackedHeader]="true"
    >
      <div class="insight-grid">
        <article class="compact-card">
          <span>{{ 'settings.account.session' | translate }}</span>
          <h3>{{ 'settings.account.active' | translate }}</h3>
          <p>
            {{ 'settings.account.sessionDescription' | translate }}
          </p>
        </article>

        <article class="compact-card">
          <span>{{ 'settings.account.editableData' | translate }}</span>
          <h3>{{ 'settings.account.separatedInProfile' | translate }}</h3>
          <p>
            {{ 'settings.account.editableDataDescription' | translate }}
          </p>
        </article>

        <article class="compact-card">
          <span>{{ 'settings.account.sensitiveActions' | translate }}</span>
          <h3>{{ 'settings.account.separatedInSecurity' | translate }}</h3>
          <p>
            {{ 'settings.account.sensitiveActionsDescription' | translate }}
          </p>
        </article>
      </div>
    </app-settings-section>
</section>


============================================================
FILE: src\app\iam\presentation\pages\account\account-page.component.ts
============================================================

import { TranslateModule, TranslateService } from '@ngx-translate/core';
import { Component } from '@angular/core';

import { IamFacade } from '../../../application/services/iam.facade';
import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';
import { SettingsSectionComponent } from '../../../../shared/presentation/components/settings-section/settings-section.component';

@Component({
  selector: 'app-account-page',
  standalone: true,
  imports: [
    TranslateModule,SettingsSectionComponent],
  templateUrl: './account-page.component.html',
  styleUrls: ['../settings-page.shared.scss'],
})
export class AccountPageComponent {
  constructor(
    readonly iamFacade: IamFacade,
    private readonly uiPreferences: UiPreferencesService,
    private readonly translate: TranslateService
  ) {}

  get userInitials(): string {
    return this.iamFacade.currentUser()?.initials || 'EC';
  }

  formatDate(value: string | undefined): string {
    if (!value) {
      return this.t('settings.common.notAvailable');
    }

    const date = new Date(value);

    if (Number.isNaN(date.getTime())) {
      return value;
    }

    return new Intl.DateTimeFormat(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
    }).format(date);
  }

  currentLocale(): string {
    const localeByLanguage: Record<string, string> = {
      es: 'es-PE',
      en: 'en-US',
      pt: 'pt-BR',
    };

    return localeByLanguage[this.uiPreferences.currentLanguage()] ?? localeByLanguage['es'];
  }

  private t(key: string): string {
    return this.translate.instant(key);
  }
}


============================================================
FILE: src\app\iam\presentation\pages\login\login-page.component.html
============================================================

<section class="auth-page">
  <article class="auth-card">
    <p class="eyebrow">{{ 'auth.loginEyebrow' | translate }}</p>

    <h1>{{ 'auth.loginTitle' | translate }}</h1>

    <p class="subtitle">
      {{ 'auth.loginSubtitle' | translate }}
    </p>

    <form class="auth-form" (ngSubmit)="onSubmit()">
      <label>
        {{ 'auth.email' | translate }}
        <input
          type="email"
          name="email"
          [(ngModel)]="email"
          autocomplete="email"
          required
        />
      </label>

      <label>
        {{ 'auth.password' | translate }}
        <input
          type="password"
          name="password"
          [(ngModel)]="password"
          autocomplete="current-password"
          required
        />
      </label>

      <app-button type="submit" variant="create" [disabled]="iamFacade.loading()" [block]="true">
        @if (iamFacade.loading()) {
          {{ 'common.loading' | translate }}
        } @else {
          {{ 'auth.signIn' | translate }}
        }
      </app-button>
    </form>

    <div class="links">
      <a routerLink="/iam/recover-password">
        {{ 'auth.forgotPassword' | translate }}
      </a>

      <a routerLink="/iam/register">
        {{ 'auth.createAccount' | translate }}
      </a>
    </div>
  </article>
</section>


============================================================
FILE: src\app\iam\presentation\pages\login\login-page.component.scss
============================================================

.auth-page {
  min-height: 68vh;
  display: grid;
  place-items: center;
}

.auth-card {
  width: min(460px, 100%);
  padding: 30px;
  border: 1px solid var(--border-color);
  border-radius: 28px;
  background: var(--surface-color);
}

.eyebrow {
  margin: 0 0 10px;
  color: var(--accent-color);
  font-weight: 900;
  text-transform: uppercase;
  letter-spacing: 0.12em;
}

h1 {
  margin: 0;
  font-size: 2.4rem;
}

.subtitle {
  margin: 12px 0 20px;
  color: var(--text-secondary);
  line-height: 1.6;
}

.auth-form {
  display: grid;
  gap: 16px;
  margin-top: 18px;
}

label {
  display: grid;
  gap: 8px;
  color: var(--text-primary);
  font-weight: 800;
}

input {
  width: 100%;
  padding: 13px 14px;
  border: 1px solid var(--border-color);
  border-radius: 14px;
  color: var(--text-primary);
  background: var(--field-bg);
}

button {
  min-height: 46px;
  border: 0;
  border-radius: 999px;
  color: var(--text-on-action);
  background: var(--ec-action-bg);
  cursor: pointer;
  font-weight: 900;
}

button:disabled {
  opacity: 0.65;
  cursor: not-allowed;
}

.links {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  flex-wrap: wrap;
  margin-top: 20px;
}

.links a {
  color: var(--text-secondary);
  font-weight: 800;
  text-decoration: none;
}

.links a:hover {
  color: var(--text-primary);
}


============================================================
FILE: src\app\iam\presentation\pages\login\login-page.component.ts
============================================================

import { Component } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { RouterLink } from '@angular/router';
import { TranslateModule } from '@ngx-translate/core';

import { IamFacade } from '../../../application/services/iam.facade';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';

@Component({
  selector: 'app-login-page',
  standalone: true,
  imports: [FormsModule, RouterLink, TranslateModule, AppButtonComponent],
  templateUrl: './login-page.component.html',
  styleUrls: ['./login-page.component.scss'],
})
export class LoginPageComponent {
  email = '';
  password = '';

  constructor(
    readonly iamFacade: IamFacade,
    private readonly toastService: ToastService
  ) {}

  async onSubmit(): Promise<void> {
    const success = await this.iamFacade.signIn({
      email: this.email,
      password: this.password,
    });

    if (!success) {
      this.toastService.error('No se pudo iniciar sesión.');
    }
  }
}


============================================================
FILE: src\app\iam\presentation\pages\platform\platform-page.component.html
============================================================

<section class="settings-page">
    <header class="ec-page-heading">
      <p class="ec-page-eyebrow">{{ 'settings.nav.platform' | translate }}</p>
      <h1 class="ec-page-title">{{ 'settings.platform.title' | translate }}</h1>
      <p class="ec-page-description">
        {{ 'settings.platform.pageDescription' | translate }}
      </p>
    </header>

    <app-settings-section
      [eyebrow]="'settings.nav.platform' | translate"
      [title]="'settings.platform.browserExperienceTitle' | translate"
      [description]="'settings.platform.browserExperienceDescription' | translate"
      [stackedHeader]="true"
    >
      <div class="platform-grid">
        <app-setting-toggle
          [title]="'settings.platform.darkMode' | translate"
          [description]="'settings.platform.darkModeDescription' | translate"
          [checked]="uiPreferences.isDarkMode()"
          (checkedChange)="setDarkMode($event)"
        />

        <label class="language-field">
          <span>{{ 'settings.platform.language' | translate }}</span>
          <app-dropdown
            [options]="languageOptions"
            [value]="currentLanguage"
            [placeholder]="'settings.platform.language' | translate"
            (valueChange)="selectLanguage($event)"
          />
        </label>
      </div>
    </app-settings-section>

    <app-settings-section
      [eyebrow]="'settings.platform.localStatusEyebrow' | translate"
      [title]="'settings.platform.preferencesSummaryTitle' | translate"
      [description]="'settings.platform.preferencesSummaryDescription' | translate"
      [stackedHeader]="true"
    >
      <div class="insight-grid">
        <article class="compact-card">
          <span>{{ 'settings.platform.appearance' | translate }}</span>
          <h3>{{ currentThemeLabel }}</h3>
          <p>
            {{ 'settings.platform.appearanceDescription' | translate }}
          </p>
        </article>

        <article class="compact-card">
          <span>{{ 'settings.platform.activeLanguage' | translate }}</span>
          <h3>{{ currentLanguageLabel }}</h3>
          <p>
            {{ 'settings.platform.activeLanguageDescription' | translate }}
          </p>
        </article>

        <article class="compact-card">
          <span>{{ 'settings.platform.scope' | translate }}</span>
          <h3>{{ 'settings.platform.local' | translate }}</h3>
          <p>
            {{ 'settings.platform.scopeDescription' | translate }}
          </p>
        </article>
      </div>
    </app-settings-section>
</section>


============================================================
FILE: src\app\iam\presentation\pages\platform\platform-page.component.ts
============================================================

import { TranslateModule, TranslateService } from '@ngx-translate/core';
import { Component } from '@angular/core';

import { ToastService } from '../../../../shared/application/services/toast.service';
import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { SettingsSectionComponent } from '../../../../shared/presentation/components/settings-section/settings-section.component';
import { SettingToggleComponent } from '../../../../shared/presentation/components/setting-toggle/setting-toggle.component';

@Component({
  selector: 'app-platform-page',
  standalone: true,
  imports: [
    TranslateModule,
    AppDropdownComponent,
    SettingsSectionComponent,
    SettingToggleComponent,
  ],
  templateUrl: './platform-page.component.html',
  styleUrls: ['../settings-page.shared.scss'],
})
export class PlatformPageComponent {
  readonly languageOptions: DropdownOption[] = [
    {
      label: 'Espanol',
      labelKey: 'settings.platform.languages.es',
      value: 'es',
      descriptionKey: 'settings.platform.languages.esDescription',
    },
    {
      label: 'English',
      labelKey: 'settings.platform.languages.en',
      value: 'en',
      descriptionKey: 'settings.platform.languages.enDescription',
    },
    {
      label: 'Portugues',
      labelKey: 'settings.platform.languages.pt',
      value: 'pt',
      descriptionKey: 'settings.platform.languages.ptDescription',
    },
  ];

  constructor(
    readonly uiPreferences: UiPreferencesService,
    private readonly toastService: ToastService,
    private readonly translate: TranslateService
  ) {}

  get currentLanguage(): string {
    return this.uiPreferences.currentLanguage();
  }

  get currentLanguageLabel(): string {
    const option = this.languageOptions.find((item) => item.value === this.currentLanguage);

    return option?.labelKey ? this.t(option.labelKey) : option?.label ?? this.currentLanguage;
  }

  get currentThemeLabel(): string {
    return this.uiPreferences.isDarkMode()
      ? this.t('settings.platform.darkMode')
      : this.t('settings.platform.lightMode');
  }

  selectLanguage(value: string): void {
    if (value !== 'es' && value !== 'en' && value !== 'pt') {
      return;
    }

    this.uiPreferences.setLanguage(value);
    this.toastService.info(this.t('settings.platform.languageApplied'));
  }

  setDarkMode(enabled: boolean): void {
    if (this.uiPreferences.isDarkMode() === enabled) {
      return;
    }

    this.uiPreferences.toggleTheme();
    this.toastService.info(this.t('settings.platform.appearanceApplied'));
  }

  private t(key: string): string {
    return this.translate.instant(key);
  }
}


============================================================
FILE: src\app\iam\presentation\pages\profile\profile-page.component.html
============================================================

<section class="settings-page">
    <header class="ec-page-heading">
      <p class="ec-page-eyebrow">{{ 'settings.nav.profile' | translate }}</p>
      <h1 class="ec-page-title">{{ 'settings.profile.title' | translate }}</h1>
      <p class="ec-page-description">
        {{ 'settings.profile.pageDescription' | translate }}
      </p>
    </header>

    <app-settings-section
      id="profile"
      [eyebrow]="'settings.nav.profile' | translate"
      [title]="'settings.profile.basicInfoTitle' | translate"
      [description]="'settings.profile.basicInfoDescription' | translate"
      [stackedHeader]="true"
    >
      <form class="settings-form" (ngSubmit)="onUpdateProfile()">
        <label>
          <span>{{ 'auth.fullName' | translate }}</span>
          <input
            type="text"
            name="fullName"
            [(ngModel)]="fullName"
            autocomplete="name"
            required
          />
        </label>

        <label>
          <span>{{ 'settings.common.email' | translate }}</span>
          <input
            type="email"
            name="email"
            [(ngModel)]="email"
            autocomplete="email"
            required
          />
        </label>

        <div class="form-actions">
          <app-button
            type="submit"
            variant="create"
            [disabled]="iamFacade.loading()"
          >
            {{ 'settings.profile.saveProfile' | translate }}
          </app-button>
        </div>
      </form>
    </app-settings-section>

    <app-settings-section
      [eyebrow]="'settings.profile.previewEyebrow' | translate"
      [title]="'settings.profile.dataUsageTitle' | translate"
      [description]="'settings.profile.dataUsageDescription' | translate"
      [stackedHeader]="true"
    >
      <div class="insight-grid">
        <article class="compact-card">
          <span>{{ 'settings.profile.visibleName' | translate }}</span>
          <h3>{{ fullName || ('settings.common.notAvailable' | translate) }}</h3>
          <p>
            {{ 'settings.profile.visibleNameDescription' | translate }}
          </p>
        </article>

        <article class="compact-card">
          <span>{{ 'settings.profile.accountEmail' | translate }}</span>
          <h3>{{ email || ('settings.common.notAvailable' | translate) }}</h3>
          <p>
            {{ 'settings.profile.accountEmailDescription' | translate }}
          </p>
        </article>

        <article class="compact-card">
          <span>{{ 'history.status' | translate }}</span>
          <h3>
            @if (hasPendingChanges()) {
              {{ 'settings.profile.unsavedChanges' | translate }}
            } @else {
              {{ 'settings.profile.noPendingChanges' | translate }}
            }
          </h3>
          <p>
            {{ 'settings.profile.statusDescription' | translate }}
          </p>
        </article>
      </div>
    </app-settings-section>
</section>


============================================================
FILE: src\app\iam\presentation\pages\profile\profile-page.component.ts
============================================================

import { TranslateModule, TranslateService } from '@ngx-translate/core';
import { Component, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';

import { IamFacade } from '../../../application/services/iam.facade';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { SettingsSectionComponent } from '../../../../shared/presentation/components/settings-section/settings-section.component';

@Component({
  selector: 'app-profile-page',
  standalone: true,
  imports: [
    TranslateModule,
    FormsModule,
    AppButtonComponent,
    SettingsSectionComponent,
  ],
  templateUrl: './profile-page.component.html',
  styleUrls: ['../settings-page.shared.scss'],
})
export class ProfilePageComponent implements OnInit {
  fullName = '';
  email = '';

  constructor(
    readonly iamFacade: IamFacade,
    private readonly toastService: ToastService,
    private readonly translate: TranslateService
  ) {}

  ngOnInit(): void {
    const user = this.iamFacade.currentUser();

    if (!user) return;

    this.fullName = user.fullName;
    this.email = user.email;
  }

  async onUpdateProfile(): Promise<void> {
    const success = await this.iamFacade.updateProfile({
      fullName: this.fullName,
      email: this.email,
    });

    if (success) {
      this.toastService.success(this.t('settings.profile.updateSuccess'));
    } else {
      this.toastService.error(this.t('settings.profile.updateError'));
    }
  }

  hasPendingChanges(): boolean {
    const currentUser = this.iamFacade.currentUser();

    if (!currentUser) {
      return false;
    }

    return (
      currentUser.fullName !== this.fullName ||
      currentUser.email !== this.email
    );
  }

  private t(key: string): string {
    return this.translate.instant(key);
  }
}


============================================================
FILE: src\app\iam\presentation\pages\recover-password\recover-password-page.component.html
============================================================

<section class="auth-page">
  <article class="auth-card">
    <p class="eyebrow">{{ 'auth.recoverPasswordEyebrow' | translate }}</p>

    <h1>{{ 'auth.recoverPasswordTitle' | translate }}</h1>

    <p class="subtitle">
      {{ 'auth.recoverPasswordSubtitle' | translate }}
    </p>

    <form class="auth-form" (ngSubmit)="onSubmit()">
      <label>
        {{ 'auth.email' | translate }}
        <input
          type="email"
          name="email"
          [(ngModel)]="email"
          autocomplete="email"
          required
        />
      </label>

      <app-button type="submit" variant="create" [disabled]="iamFacade.loading()" [block]="true">
        @if (iamFacade.loading()) {
          {{ 'common.loading' | translate }}
        } @else {
          {{ 'auth.sendRecoveryEmail' | translate }}
        }
      </app-button>
    </form>

    <div class="links">
      <a routerLink="/iam/login">
        {{ 'auth.backToLogin' | translate }}
      </a>
    </div>
  </article>
</section>


============================================================
FILE: src\app\iam\presentation\pages\recover-password\recover-password-page.component.scss
============================================================

@use '../login/login-page.component.scss';


============================================================
FILE: src\app\iam\presentation\pages\recover-password\recover-password-page.component.ts
============================================================

import { Component } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { RouterLink } from '@angular/router';
import { TranslateModule } from '@ngx-translate/core';

import { IamFacade } from '../../../application/services/iam.facade';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';

@Component({
  selector: 'app-recover-password-page',
  standalone: true,
  imports: [FormsModule, RouterLink, TranslateModule, AppButtonComponent],
  templateUrl: './recover-password-page.component.html',
  styleUrls: ['./recover-password-page.component.scss'],
})
export class RecoverPasswordPageComponent {
  email = '';

  constructor(
    readonly iamFacade: IamFacade,
    private readonly toastService: ToastService
  ) {}

  async onSubmit(): Promise<void> {
    const success = await this.iamFacade.recoverPassword({
      email: this.email,
    });

    if (success) {
      this.toastService.success('Solicitud de recuperación enviada.');
    } else {
      this.toastService.error('No se pudo enviar la recuperación.');
    }
  }
}


============================================================
FILE: src\app\iam\presentation\pages\register\register-page.component.html
============================================================

<section class="auth-page">
  <article class="auth-card">
    <p class="eyebrow">{{ 'auth.registerEyebrow' | translate }}</p>

    <h1>{{ 'auth.registerTitle' | translate }}</h1>

    <p class="subtitle">
      {{ 'auth.registerSubtitle' | translate }}
    </p>

    <form class="auth-form" (ngSubmit)="onSubmit()">
      <label>
        {{ 'auth.fullName' | translate }}
        <input
          type="text"
          name="fullName"
          [(ngModel)]="fullName"
          autocomplete="name"
          required
        />
      </label>

      <label>
        {{ 'auth.email' | translate }}
        <input
          type="email"
          name="email"
          [(ngModel)]="email"
          autocomplete="email"
          required
        />
      </label>

      <label>
        {{ 'auth.password' | translate }}
        <input
          type="password"
          name="password"
          [(ngModel)]="password"
          autocomplete="new-password"
          required
        />
      </label>

      <div class="password-rules">
        @for (rule of passwordRequirements; track rule.label) {
          <span [class.valid]="rule.valid">
            {{ rule.valid ? '✓' : '•' }} {{ rule.label }}
          </span>
        }
      </div>

      <app-button
        type="submit"
        variant="create"
        [disabled]="iamFacade.loading() || !passwordIsValid"
        [block]="true"
      >
        @if (iamFacade.loading()) {
          {{ 'common.loading' | translate }}
        } @else {
          {{ 'auth.signUp' | translate }}
        }
      </app-button>
    </form>

    <div class="links">
      <a routerLink="/iam/login">
        {{ 'auth.backToLogin' | translate }}
      </a>
    </div>
  </article>
</section>


============================================================
FILE: src\app\iam\presentation\pages\register\register-page.component.scss
============================================================

@use '../login/login-page.component.scss';

.password-rules {
  display: grid;
  gap: 7px;
  margin-top: -6px;
  padding: 12px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  background: var(--ec-surface-subtle);
}

.password-rules span {
  color: var(--text-secondary);
  font-size: 0.84rem;
  font-weight: 800;
}

.password-rules span.valid {
  color: var(--ec-status-success-text);
}


============================================================
FILE: src\app\iam\presentation\pages\register\register-page.component.ts
============================================================

import { Component } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { RouterLink } from '@angular/router';
import { TranslateModule } from '@ngx-translate/core';

import { IamFacade } from '../../../application/services/iam.facade';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';

@Component({
  selector: 'app-register-page',
  standalone: true,
  imports: [FormsModule, RouterLink, TranslateModule, AppButtonComponent],
  templateUrl: './register-page.component.html',
  styleUrls: ['./register-page.component.scss'],
})
export class RegisterPageComponent {
  fullName = '';
  email = '';
  password = '';

  constructor(
    readonly iamFacade: IamFacade,
    private readonly toastService: ToastService
  ) {}

  get passwordRequirements() {
    return [
      {
        label: 'Mínimo 8 caracteres',
        valid: this.password.length >= 8,
      },
      {
        label: 'Al menos una mayúscula',
        valid: /[A-ZÁÉÍÓÚÑ]/.test(this.password),
      },
      {
        label: 'Al menos un número',
        valid: /\d/.test(this.password),
      },
    ];
  }

  get passwordIsValid(): boolean {
    return this.passwordRequirements.every((requirement) => requirement.valid);
  }

  async onSubmit(): Promise<void> {
    if (!this.passwordIsValid) {
      this.toastService.warning('Completa los requisitos de contraseña.');
      return;
    }

    const success = await this.iamFacade.signUp({
      fullName: this.fullName,
      email: this.email,
      password: this.password,
    });

    if (!success) {
      this.toastService.error('No se pudo crear la cuenta.');
    }
  }
}


============================================================
FILE: src\app\iam\presentation\pages\security\security-page.component.html
============================================================

<section class="settings-page">
    <header class="ec-page-heading">
      <p class="ec-page-eyebrow">{{ 'settings.nav.security' | translate }}</p>
      <h1 class="ec-page-title">{{ 'settings.security.title' | translate }}</h1>
      <p class="ec-page-description">
        {{ 'settings.security.pageDescription' | translate }}
      </p>
    </header>

    <app-settings-section
      [eyebrow]="'settings.nav.security' | translate"
      [title]="'settings.security.accessActionsTitle' | translate"
      [description]="'settings.security.accessActionsDescription' | translate"
      [stackedHeader]="true"
    >
      <div class="split-grid">
        <article class="compact-card">
          <span>{{ 'settings.security.recovery' | translate }}</span>
          <h3>{{ 'settings.security.sendRecoveryTitle' | translate }}</h3>
          <p>
            {{ 'settings.security.sendRecoveryDescription' | translate }}
          </p>
          <app-button
            variant="secondary"
            [disabled]="iamFacade.loading() || !email"
            (pressed)="sendRecovery()"
          >
            {{ 'settings.security.sendRecoveryButton' | translate }}
          </app-button>
        </article>

        <article class="compact-card compact-card--danger">
          <span>{{ 'settings.security.dangerZone' | translate }}</span>
          <h3>{{ 'profile.deleteAccount' | translate }}</h3>
          <p>
            {{ 'settings.security.deleteAccountDescription' | translate }}
          </p>

          <app-button
            variant="danger"
            [disabled]="iamFacade.loading()"
            (pressed)="onDeleteAccount()"
          >
            {{ 'security.deleteAccount' | translate }}
          </app-button>
        </article>
      </div>
    </app-settings-section>

    <app-settings-section
      [eyebrow]="'settings.security.protectionsEyebrow' | translate"
      [title]="'settings.security.securityStatusTitle' | translate"
      [description]="'settings.security.securityStatusDescription' | translate"
      [stackedHeader]="true"
    >
      <div class="insight-grid">
        <article class="compact-card">
          <span>{{ 'settings.security.recoveryEmail' | translate }}</span>
          <h3>{{ email || ('settings.common.notAvailable' | translate) }}</h3>
          <p>
            {{ 'settings.security.recoveryEmailDescription' | translate }}
          </p>
        </article>

        <article class="compact-card">
          <span>{{ 'settings.security.writtenConfirmation' | translate }}</span>
          <h3>ELIMINAR</h3>
          <p>
            {{ 'settings.security.writtenConfirmationDescription' | translate }}
          </p>
        </article>

        <article class="compact-card">
          <span>{{ 'settings.security.finalConfirmation' | translate }}</span>
          <h3>{{ 'settings.security.dialogRequired' | translate }}</h3>
          <p>
            {{ 'settings.security.finalConfirmationDescription' | translate }}
          </p>
        </article>
      </div>
    </app-settings-section>
</section>


============================================================
FILE: src\app\iam\presentation\pages\security\security-page.component.ts
============================================================

import { TranslateModule, TranslateService } from '@ngx-translate/core';
import { Component } from '@angular/core';

import { ConfirmDialogService } from '../../../../shared/application/services/confirm-dialog.service';
import { IamFacade } from '../../../application/services/iam.facade';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { SettingsSectionComponent } from '../../../../shared/presentation/components/settings-section/settings-section.component';

@Component({
  selector: 'app-security-page',
  standalone: true,
  imports: [
    TranslateModule,
    AppButtonComponent,
    SettingsSectionComponent,
  ],
  templateUrl: './security-page.component.html',
  styleUrls: ['../settings-page.shared.scss'],
})
export class SecurityPageComponent {
  constructor(
    readonly iamFacade: IamFacade,
    private readonly toastService: ToastService,
    private readonly confirmDialog: ConfirmDialogService,
    private readonly translate: TranslateService
  ) {}

  get email(): string {
    return this.iamFacade.currentUser()?.email ?? '';
  }

  async sendRecovery(): Promise<void> {
    if (!this.email) {
      this.toastService.error(this.t('settings.security.noRecoveryEmail'));
      return;
    }

    const success = await this.iamFacade.recoverPassword({
      email: this.email,
    });

    if (success) {
      this.toastService.success(this.t('settings.security.recoverySent'));
      return;
    }

    this.toastService.error(this.t('settings.security.recoveryError'));
  }

  async onDeleteAccount(): Promise<void> {
    const confirmed = await this.confirmDialog.confirm({
      title: this.t('settings.security.deleteDialogTitle'),
      message: this.t('settings.security.deleteDialogMessage'),
      detail: this.t('settings.security.deleteDialogDetail'),
      confirmLabel: this.t('settings.security.deleteDialogConfirm'),
      cancelLabel: this.t('settings.security.deleteDialogCancel'),
      tone: 'danger',
      requiredText: 'ELIMINAR',
      requiredTextLabel: this.t('settings.security.requiredTextLabel'),
      requiredTextHint: this.t('settings.security.requiredTextHint'),
    });

    if (!confirmed) {
      return;
    }

    const success = await this.iamFacade.deleteAccount({
      confirmation: 'ELIMINAR',
    });

    if (!success) {
      this.toastService.error(this.t('settings.security.deleteAccountError'));
    }
  }

  private t(key: string): string {
    return this.translate.instant(key);
  }
}


============================================================
FILE: src\app\iam\presentation\pages\settings-page.shared.scss
============================================================

.settings-page {
  display: grid;
  gap: 24px;
}

h3,
p {
  margin: 0;
}

.compact-card p {
  color: var(--text-secondary);
  line-height: 1.6;
}

.settings-form,
.danger-form {
  display: grid;
  gap: 16px;
}

.settings-form {
  width: 100%;
}

label {
  display: grid;
  gap: 8px;
}

label span,
.compact-card > span,
.account-details span {
  color: var(--accent-color);
  font-size: 0.75rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

input {
  width: 100%;
  min-height: 46px;
  padding: 0 14px;
  border: 1px solid var(--border-color);
  border-radius: 14px;
  color: var(--text-primary);
  background: var(--field-bg);
  font: inherit;
  transition:
    border-color 0.18s ease,
    box-shadow 0.18s ease,
    background 0.18s ease;
}

input:focus {
  outline: none;
  border-color: var(--border-strong);
  box-shadow: var(--ec-focus-ring);
}

.form-actions {
  display: flex;
  justify-content: flex-end;
}

.account-panel {
  display: grid;
  grid-template-columns: auto minmax(0, 1fr);
  gap: 18px;
  align-items: start;
}

.avatar {
  width: 78px;
  height: 78px;
  display: grid;
  place-items: center;
  border: 1px solid var(--border-strong);
  border-radius: 22px;
  color: var(--text-on-action);
  background: var(--ec-create-bg);
  box-shadow: var(--ec-create-shadow);
  font-size: 1.25rem;
  font-weight: 950;
}

.account-details,
.insight-grid,
.split-grid,
.platform-grid {
  display: grid;
  gap: 14px;
}

.account-details {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}

.account-details article,
.compact-card,
.language-field {
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background: var(--surface-card);
}

.account-details article {
  display: grid;
  gap: 6px;
  padding: 16px;
}

.account-details strong,
.compact-card h3 {
  min-width: 0;
  color: var(--text-primary);
  overflow-wrap: anywhere;
}

.compact-card h3 {
  font-size: 1.1rem;
  line-height: 1.25;
}

.split-grid {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}

.insight-grid {
  grid-template-columns: repeat(3, minmax(0, 1fr));
}

.compact-card {
  display: grid;
  grid-template-rows: auto auto minmax(0, 1fr) auto;
  gap: 14px;
  padding: 18px;
}

.compact-card > app-button,
.danger-form {
  align-self: end;
}

.compact-card > app-button,
.danger-form app-button {
  justify-self: start;
}

.compact-card--danger {
  border-color: var(--ec-status-danger-border);
}

.platform-grid {
  grid-template-columns: minmax(0, 1fr) minmax(260px, 360px);
  align-items: start;
}

.language-field {
  padding: 16px;
}

@media (max-width: 860px) {
  .account-panel,
  .account-details,
  .insight-grid,
  .split-grid,
  .platform-grid {
    grid-template-columns: 1fr;
  }
}


============================================================
FILE: src\app\iam\presentation\routes\iam.routes.ts
============================================================

import { Routes } from '@angular/router';

export const IAM_ROUTES: Routes = [
  {
    path: 'iam/login',
    data: { title: 'Login' },
    loadComponent: () =>
      import('../pages/login/login-page.component').then(
        (m) => m.LoginPageComponent
      ),
  },
  {
    path: 'iam/register',
    data: { title: 'Register' },
    loadComponent: () =>
      import('../pages/register/register-page.component').then(
        (m) => m.RegisterPageComponent
      ),
  },
  {
    path: 'iam/recover-password',
    data: { title: 'Recover Password' },
    loadComponent: () =>
      import('../pages/recover-password/recover-password-page.component').then(
        (m) => m.RecoverPasswordPageComponent
      ),
  },
  {
    path: 'iam/profile',
    redirectTo: 'settings/profile',
    pathMatch: 'full',
  },
];


============================================================
FILE: src\app\notifications\application\commands\create-alert.command.ts
============================================================

import {
  AlertEventType,
  AlertLevel,
  AlertSourceType,
} from '../../domain/model/alert.entity';

export interface CreateAlertCommand {
  title: string;
  message: string;
  level: AlertLevel;
  sourceType?: AlertSourceType;
  sourceId?: string | null;
  sourceLabel?: string | null;
  eventType?: AlertEventType;
  threadKey?: string;
  evidence?: string | null;
  explanation?: string | null;
  recommendedAction?: string | null;
  severityScore?: number;
  expiresAt?: string | null;
}


============================================================
FILE: src\app\notifications\application\commands\create-alert-rule.command.ts
============================================================

import { AlertLevel } from '../../domain/model/alert.entity';
import {
  AlertRuleCondition,
  AlertRuleMetric,
  RuleEvaluatorType,
  RuleScopeType,
} from '../../domain/model/alert-rule.entity';

export interface CreateAlertRuleCommand {
  name: string;
  metric: AlertRuleMetric;
  condition: AlertRuleCondition;
  threshold: number;
  level: AlertLevel;
  scopeType?: RuleScopeType;
  scopeId?: string | null;
  evaluatorType?: RuleEvaluatorType;
  weight?: number;
  profileName?: string;
}


============================================================
FILE: src\app\notifications\application\commands\mark-alert-as-read.command.ts
============================================================

export interface MarkAlertAsReadCommand {
  alertId: number;
}


============================================================
FILE: src\app\notifications\application\commands\update-alert-rule-status.command.ts
============================================================

export interface UpdateAlertRuleStatusCommand {
  alertRuleId: number;
  enabled: boolean;
}


============================================================
FILE: src\app\notifications\application\commands\update-notification-preference.command.ts
============================================================

import { AlertLevel } from '../../domain/model/alert.entity';
import {
  NotificationDeliveryMode,
  NotificationPreferenceScope,
} from '../../domain/model/notification-preference.entity';

export interface UpdateNotificationPreferenceCommand {
  emailEnabled: boolean;
  pushEnabled: boolean;
  inAppEnabled?: boolean;
  toastEnabled?: boolean;
  dashboardEnabled?: boolean;
  criticalOnly: boolean;
  minimumLevel: AlertLevel;
  scopeType?: NotificationPreferenceScope;
  scopeId?: string | null;
  allowedLevels?: string;
  allowedSourceTypes?: string;
  quietHoursEnabled?: boolean;
  quietHoursStart?: string;
  quietHoursEnd?: string;
  criticalBreaksQuietHours?: boolean;
  groupSimilarAlerts?: boolean;
  remindersEnabled?: boolean;
  cooldownMinutes?: number;
  maxAlertsPerHour?: number;
  routineNightSilence?: boolean;
  goalDeadlineAlerts?: boolean;
  maintenanceDeviceAlerts?: boolean;
  systemRecommendations?: boolean;
  dailySummaryEnabled?: boolean;
  weeklySummaryEnabled?: boolean;
  defaultDeliveryMode?: NotificationDeliveryMode;
}


============================================================
FILE: src\app\notifications\application\services\notifications.facade.ts
============================================================

import { Injectable } from '@angular/core';
import { firstValueFrom } from 'rxjs';

import { AlertLevel } from '../../domain/model/alert.entity';
import { AlertRuleProfile } from '../../domain/model/alert-rule-profile.entity';
import { RuleEvaluationResult } from '../../domain/model/rule-evaluation-result.entity';

import { CreateAlertCommand } from '../commands/create-alert.command';
import { CreateAlertRuleCommand } from '../commands/create-alert-rule.command';
import { MarkAlertAsReadCommand } from '../commands/mark-alert-as-read.command';
import { UpdateAlertRuleStatusCommand } from '../commands/update-alert-rule-status.command';
import { UpdateNotificationPreferenceCommand } from '../commands/update-notification-preference.command';

import { AlertRulesApiService } from '../../infrastructure/api/alert-rules-api.service';
import { AlertRuleProfilesApiService } from '../../infrastructure/api/alert-rule-profiles-api.service';
import { AlertsApiService } from '../../infrastructure/api/alerts-api.service';
import { NotificationPreferencesApiService } from '../../infrastructure/api/notification-preferences-api.service';

import { AlertRuleAssembler } from '../../infrastructure/assemblers/alert-rule.assembler';
import { AlertAssembler } from '../../infrastructure/assemblers/alert.assembler';
import { NotificationPreferenceAssembler } from '../../infrastructure/assemblers/notification-preference.assembler';

import { NotificationChannelPolicyService } from '../../domain/services/notification-channel-policy.service';
import { NotificationClassificationPolicy } from '../../domain/model/notification-classification-policy.model';

import { NotificationsStore } from '../stores/notifications.store';

@Injectable({
  providedIn: 'root',
})
export class NotificationsFacade {
  private readonly alertAssembler = new AlertAssembler();
  private readonly alertRuleAssembler = new AlertRuleAssembler();
  private readonly notificationPreferenceAssembler =
    new NotificationPreferenceAssembler();

  get alerts() {
    return this.store.alerts;
  }

  get alertRules() {
    return this.store.alertRules;
  }

  get alertRuleProfiles() {
    return this.store.alertRuleProfiles;
  }

  get ruleEvaluationResult() {
    return this.store.ruleEvaluationResult;
  }

  get notificationPreference() {
    return this.store.notificationPreference;
  }

  get notificationClassificationPolicy() {
    return this.store.notificationClassificationPolicy;
  }

  get loading() {
    return this.store.loading;
  }

  get error() {
    return this.store.error;
  }

  get unreadAlerts() {
    return this.store.unreadAlerts;
  }

  get criticalAlerts() {
    return this.store.criticalAlerts;
  }

  get enabledAlertRules() {
    return this.store.enabledAlertRules;
  }

  get sortedAlerts() {
    return this.store.sortedAlerts;
  }

  constructor(
    private readonly alertsApi: AlertsApiService,
    private readonly alertRulesApi: AlertRulesApiService,
    private readonly alertRuleProfilesApi: AlertRuleProfilesApiService,
    private readonly notificationPreferencesApi: NotificationPreferencesApiService,
    private readonly notificationChannelPolicy: NotificationChannelPolicyService,
    private readonly store: NotificationsStore
  ) {}

  async loadNotifications(): Promise<void> {
    this.startRequest();

    try {
      await Promise.all([
        this.loadAlerts(),
        this.loadAlertRules(),
        this.loadAlertRuleProfiles(),
        this.loadNotificationPreference(),
      ]);
    } catch (error) {
      console.error(error);
      this.store.setError('alerts.loadError');
    } finally {
      this.finishRequest();
    }
  }

  async loadAlerts(): Promise<void> {
    const responses = await firstValueFrom(this.alertsApi.findAllForCurrentUser());

    const alerts = responses
      .map((response) => this.alertAssembler.toEntity(response))
      .sort(
        (first, second) =>
          new Date(second.createdAt).getTime() -
          new Date(first.createdAt).getTime()
      );

    this.store.setAlerts(alerts);
  }

  async createAlert(command: CreateAlertCommand): Promise<boolean> {
    this.store.clearMessages();

    try {
      if (!command.title.trim() || !command.message.trim()) {
        this.store.setError('alerts.createError');
        return false;
      }

      const preference = this.store.notificationPreference();

      const canNotify = this.notificationChannelPolicy.canNotify(
        preference,
        command.level
      );

      void canNotify;

      const response = await firstValueFrom(
        this.alertsApi.createAlert({
          title: command.title.trim(),
          message: command.message.trim(),
          level: command.level,
          sourceType: command.sourceType ?? 'SYSTEM',
          sourceId: command.sourceId ?? null,
          sourceLabel: command.sourceLabel ?? null,
          eventType: command.eventType ?? 'MANUAL',
          threadKey: command.threadKey,
          evidence: command.evidence ?? null,
          explanation: command.explanation ?? null,
          recommendedAction: command.recommendedAction ?? null,
          severityScore: command.severityScore,
          expiresAt: command.expiresAt ?? null,
        })
      );

      const alert = this.alertAssembler.toEntity(response);
      this.store.prependAlert(alert);

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('alerts.createError');
      return false;
    }
  }

  async markAsRead(command: MarkAlertAsReadCommand): Promise<boolean> {
    this.store.clearMessages();

    try {
      const response = await firstValueFrom(
        this.alertsApi.markAsRead(command.alertId)
      );

      const updatedAlert = this.alertAssembler.toEntity(response);

      this.store.updateAlert(updatedAlert);

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('alerts.markAsReadError');
      return false;
    }
  }

  async dismissAlert(alertId: number, minutes = 10): Promise<boolean> {
    this.store.clearMessages();

    try {
      const response = await firstValueFrom(
        this.alertsApi.dismiss(alertId, minutes)
      );

      const updatedAlert = this.alertAssembler.toEntity(response);
      this.store.updateAlert(updatedAlert);

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('alerts.dismissError');
      return false;
    }
  }

  async resolveAlert(alertId: number): Promise<boolean> {
    this.store.clearMessages();

    try {
      const response = await firstValueFrom(
        this.alertsApi.resolve(alertId)
      );

      const updatedAlert = this.alertAssembler.toEntity(response);
      this.store.updateAlert(updatedAlert);

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('alerts.resolveError');
      return false;
    }
  }

  async deleteAlert(alertId: number): Promise<boolean> {
    this.store.clearMessages();

    try {
      await firstValueFrom(this.alertsApi.delete(alertId));
      this.store.removeAlert(alertId);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('alerts.deleteError');
      return false;
    }
  }

  async loadAlertRules(): Promise<void> {
    const responses = await firstValueFrom(
      this.alertRulesApi.findAllForCurrentUser()
    );

    const alertRules = responses.map((response) =>
      this.alertRuleAssembler.toEntity(response)
    );

    this.store.setAlertRules(alertRules);
  }

  async createAlertRule(command: CreateAlertRuleCommand): Promise<boolean> {
    this.startRequest();

    try {
      if (!command.name.trim() || command.threshold <= 0) {
        this.store.setError('alertRules.createError');
        return false;
      }

      const response = await firstValueFrom(
        this.alertRulesApi.create({
          name: command.name.trim(),
          metric: command.metric,
          conditionType: command.condition,
          threshold: Number(command.threshold),
          level: command.level,
          scopeType: command.scopeType ?? 'GENERAL',
          scopeId: command.scopeId ?? null,
          evaluatorType: command.evaluatorType ?? 'ACTIVE_POWER',
          weight: command.weight ?? 10,
          profileName: command.profileName ?? 'General',
        })
      );

      const rule = this.alertRuleAssembler.toEntity(response);
      this.store.prependAlertRule(rule);

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('alertRules.createError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async updateAlertRuleStatus(
    command: UpdateAlertRuleStatusCommand
  ): Promise<boolean> {
    this.startRequest();

    try {
      const response = await firstValueFrom(
        this.alertRulesApi.toggle(command.alertRuleId)
      );

      const updatedRule = this.alertRuleAssembler.toEntity(response);

      this.store.updateAlertRule(updatedRule);

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('alertRules.updateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async deleteAlertRule(alertRuleId: number): Promise<boolean> {
    this.startRequest();

    try {
      await firstValueFrom(this.alertRulesApi.delete(alertRuleId));
      this.store.removeAlertRule(alertRuleId);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('alertRules.deleteError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async loadAlertRuleProfiles(): Promise<void> {
    const responses = await firstValueFrom(
      this.alertRuleProfilesApi.findAllForCurrentUser()
    );

    const profiles = responses.map((response) => new AlertRuleProfile({
      id: response.id,
      userId: response.userId,
      name: response.name,
      description: response.description,
      scopeType: response.scopeType,
      scopeId: response.scopeId ?? null,
      mode: response.mode,
      sensitivity: response.sensitivity,
      active: response.active,
    }));

    this.store.setAlertRuleProfiles(profiles);
  }

  async createAlertRuleProfile(ruleProfile: {
    name: string;
    description?: string;
    scopeType?: 'GENERAL' | 'WORKPLACE' | 'ROOM' | 'DEVICE' | 'GROUP' | 'ROUTINE' | 'GOAL';
    scopeId?: string | null;
    mode?: 'BALANCED' | 'SAVINGS' | 'PROTECTION';
    sensitivity?: 'LOW' | 'NORMAL' | 'HIGH' | 'STRICT';
  }): Promise<boolean> {
    this.startRequest();

    try {
      const response = await firstValueFrom(
        this.alertRuleProfilesApi.create({
          name: ruleProfile.name.trim(),
          description: ruleProfile.description ?? '',
          scopeType: ruleProfile.scopeType ?? 'GENERAL',
          scopeId: ruleProfile.scopeId ?? null,
          mode: ruleProfile.mode ?? 'BALANCED',
          sensitivity: ruleProfile.sensitivity ?? 'NORMAL',
        })
      );

      const profile = new AlertRuleProfile({
        id: response.id,
        userId: response.userId,
        name: response.name,
        description: response.description,
        scopeType: response.scopeType,
        scopeId: response.scopeId ?? null,
        mode: response.mode,
        sensitivity: response.sensitivity,
        active: response.active,
      });

      this.store.setAlertRuleProfiles([profile, ...this.store.alertRuleProfiles()]);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('alertRules.profileCreateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async evaluateRules(evaluationRequest: {
    scopeType?: 'GENERAL' | 'WORKPLACE' | 'ROOM' | 'DEVICE' | 'GROUP' | 'ROUTINE' | 'GOAL';
    scopeId?: string | null;
    observedValue?: number;
  } = {}): Promise<RuleEvaluationResult | null> {
    try {
      const response = await firstValueFrom(
        this.alertRulesApi.evaluate({
          scopeType: evaluationRequest.scopeType ?? 'GENERAL',
          scopeId: evaluationRequest.scopeId ?? null,
          observedValue: evaluationRequest.observedValue ?? 0,
        })
      );

      const result: RuleEvaluationResult = {
        userId: response.userId,
        scopeType: response.scopeType,
        scopeId: response.scopeId ?? null,
        level: response.level,
        severityScore: response.severityScore,
        evidence: response.evidence,
        explanation: response.explanation,
        recommendedAction: response.recommendedAction,
        sourceType: response.sourceType,
        sourceId: response.sourceId ?? null,
        eventType: response.eventType,
        threadKey: response.threadKey,
        activeEvaluatorCount: response.activeEvaluatorCount,
        totalWeight: response.totalWeight,
      };

      this.store.setRuleEvaluationResult(result);
      return result;
    } catch (error) {
      console.error(error);
      this.store.setError('alertRules.evaluateError');
      return null;
    }
  }

  clearRuleEvaluationResult(): void {
    this.store.setRuleEvaluationResult(null);
  }

  async loadNotificationPreference(): Promise<void> {
    const response = await firstValueFrom(
      this.notificationPreferencesApi.findCurrent()
    );

    this.store.setNotificationPreference(
      this.notificationPreferenceAssembler.toEntity(response)
    );
  }

  async updateNotificationPreference(
    command: UpdateNotificationPreferenceCommand
  ): Promise<boolean> {
    this.startRequest();

    try {
      const response = await firstValueFrom(
        this.notificationPreferencesApi.save({
          emailEnabled: command.emailEnabled,
          pushEnabled: command.pushEnabled,
          inAppEnabled: command.inAppEnabled,
          toastEnabled: command.toastEnabled,
          dashboardEnabled: command.dashboardEnabled,
          criticalOnly: command.criticalOnly,
          minimumLevel: command.minimumLevel,
          scopeType: command.scopeType,
          scopeId: command.scopeId,
          allowedLevels: command.allowedLevels,
          allowedSourceTypes: command.allowedSourceTypes,
          quietHoursEnabled: command.quietHoursEnabled,
          quietHoursStart: command.quietHoursStart,
          quietHoursEnd: command.quietHoursEnd,
          criticalBreaksQuietHours: command.criticalBreaksQuietHours,
          groupSimilarAlerts: command.groupSimilarAlerts,
          remindersEnabled: command.remindersEnabled,
          cooldownMinutes: command.cooldownMinutes,
          maxAlertsPerHour: command.maxAlertsPerHour,
          routineNightSilence: command.routineNightSilence,
          goalDeadlineAlerts: command.goalDeadlineAlerts,
          maintenanceDeviceAlerts: command.maintenanceDeviceAlerts,
          systemRecommendations: command.systemRecommendations,
          dailySummaryEnabled: command.dailySummaryEnabled,
          weeklySummaryEnabled: command.weeklySummaryEnabled,
          defaultDeliveryMode: command.defaultDeliveryMode,
        })
      );

      this.store.setNotificationPreference(
        this.notificationPreferenceAssembler.toEntity(response)
      );

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('notificationPreferences.updateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  clearMessages(): void {
    this.store.clearMessages();
  }

  updateNotificationClassificationPolicy(
    policy: NotificationClassificationPolicy
  ): void {
    this.store.setNotificationClassificationPolicy(policy);
  }

  getPriorityValue(level: AlertLevel): number {
    return this.store.getPriorityValue(level);
  }

  private startRequest(): void {
    this.store.setLoading(true);
    this.store.clearMessages();
  }

  private finishRequest(): void {
    this.store.setLoading(false);
  }
}


============================================================
FILE: src\app\notifications\application\stores\notifications.store.ts
============================================================

import { Injectable, computed, signal } from '@angular/core';

import { Alert, AlertLevel } from '../../domain/model/alert.entity';
import { AlertRule } from '../../domain/model/alert-rule.entity';
import { AlertRuleProfile } from '../../domain/model/alert-rule-profile.entity';
import { RuleEvaluationResult } from '../../domain/model/rule-evaluation-result.entity';
import { NotificationPreference } from '../../domain/model/notification-preference.entity';
import {
  DEFAULT_NOTIFICATION_CLASSIFICATION_POLICY,
  NotificationClassificationPolicy,
  normalizeNotificationClassificationPolicy,
} from '../../domain/model/notification-classification-policy.model';
import { AlertPriorityService } from '../../domain/services/alert-priority.service';
import { STORAGE_KEYS } from '../../../shared/infrastructure/constants/storage-keys';

@Injectable({
  providedIn: 'root',
})
export class NotificationsStore {
  private readonly alertsSignal = signal<Alert[]>([]);
  private readonly alertRulesSignal = signal<AlertRule[]>([]);
  private readonly alertRuleProfilesSignal = signal<AlertRuleProfile[]>([]);
  private readonly ruleEvaluationResultSignal = signal<RuleEvaluationResult | null>(null);
  private readonly notificationPreferenceSignal =
    signal<NotificationPreference | null>(null);
  private readonly notificationClassificationPolicySignal =
    signal<NotificationClassificationPolicy>(
      this.restoreNotificationClassificationPolicy()
    );

  private readonly loadingSignal = signal<boolean>(false);
  private readonly errorSignal = signal<string | null>(null);

  readonly alerts = computed(() => this.alertsSignal());
  readonly alertRules = computed(() => this.alertRulesSignal());
  readonly alertRuleProfiles = computed(() => this.alertRuleProfilesSignal());
  readonly ruleEvaluationResult = computed(() => this.ruleEvaluationResultSignal());
  readonly notificationPreference = computed(() =>
    this.notificationPreferenceSignal()
  );
  readonly notificationClassificationPolicy = computed(() =>
    this.notificationClassificationPolicySignal()
  );

  readonly loading = computed(() => this.loadingSignal());
  readonly error = computed(() => this.errorSignal());

  readonly unreadAlerts = computed(() =>
    this.alertsSignal().filter((alert) => alert.isUnread)
  );

  readonly criticalAlerts = computed(() =>
    this.alertsSignal().filter((alert) => alert.isCritical)
  );

  readonly enabledAlertRules = computed(() =>
    this.alertRulesSignal().filter((rule) => rule.isEnabled)
  );

  readonly sortedAlerts = computed(() =>
    this.alertPriorityService.sortByPriority(this.alertsSignal())
  );

  constructor(private readonly alertPriorityService: AlertPriorityService) {}

  setAlerts(value: Alert[]): void {
    this.alertsSignal.set(value);
  }

  prependAlert(value: Alert): void {
    this.alertsSignal.update((alerts) => [value, ...alerts]);
  }

  updateAlert(value: Alert): void {
    this.alertsSignal.update((alerts) =>
      alerts.map((alert) => (alert.id === value.id ? value : alert))
    );
  }

  removeAlert(alertId: number): void {
    this.alertsSignal.update((alerts) =>
      alerts.filter((alert) => alert.id !== alertId)
    );
  }

  setAlertRules(value: AlertRule[]): void {
    this.alertRulesSignal.set(value);
  }

  setAlertRuleProfiles(value: AlertRuleProfile[]): void {
    this.alertRuleProfilesSignal.set(value);
  }

  setRuleEvaluationResult(value: RuleEvaluationResult | null): void {
    this.ruleEvaluationResultSignal.set(value);
  }

  prependAlertRule(value: AlertRule): void {
    this.alertRulesSignal.update((rules) => [value, ...rules]);
  }

  updateAlertRule(value: AlertRule): void {
    this.alertRulesSignal.update((rules) =>
      rules.map((rule) => (rule.id === value.id ? value : rule))
    );
  }

  removeAlertRule(alertRuleId: number): void {
    this.alertRulesSignal.update((rules) =>
      rules.filter((rule) => rule.id !== alertRuleId)
    );
  }

  setNotificationPreference(value: NotificationPreference | null): void {
    this.notificationPreferenceSignal.set(value);
  }

  setNotificationClassificationPolicy(value: NotificationClassificationPolicy): void {
    const normalized = normalizeNotificationClassificationPolicy(value);

    this.notificationClassificationPolicySignal.set(normalized);
    this.persistNotificationClassificationPolicy(normalized);
  }

  setLoading(value: boolean): void {
    this.loadingSignal.set(value);
  }

  setError(value: string | null): void {
    this.errorSignal.set(value);
  }

  clearMessages(): void {
    this.errorSignal.set(null);
  }

  getPriorityValue(level: AlertLevel): number {
    return this.alertPriorityService.getPriorityValue(level);
  }

  reset(): void {
    this.alertsSignal.set([]);
    this.alertRulesSignal.set([]);
    this.alertRuleProfilesSignal.set([]);
    this.ruleEvaluationResultSignal.set(null);
    this.notificationPreferenceSignal.set(null);
    this.notificationClassificationPolicySignal.set(DEFAULT_NOTIFICATION_CLASSIFICATION_POLICY);
    this.persistNotificationClassificationPolicy(DEFAULT_NOTIFICATION_CLASSIFICATION_POLICY);
    this.loadingSignal.set(false);
    this.errorSignal.set(null);
  }

  private restoreNotificationClassificationPolicy(): NotificationClassificationPolicy {
    if (!this.canUseLocalStorage()) {
      return DEFAULT_NOTIFICATION_CLASSIFICATION_POLICY;
    }

    try {
      const rawValue = localStorage.getItem(STORAGE_KEYS.notificationClassificationPolicy);

      if (!rawValue) {
        return DEFAULT_NOTIFICATION_CLASSIFICATION_POLICY;
      }

      return normalizeNotificationClassificationPolicy(JSON.parse(rawValue));
    } catch {
      return DEFAULT_NOTIFICATION_CLASSIFICATION_POLICY;
    }
  }

  private persistNotificationClassificationPolicy(
    value: NotificationClassificationPolicy
  ): void {
    if (!this.canUseLocalStorage()) {
      return;
    }

    try {
      localStorage.setItem(
        STORAGE_KEYS.notificationClassificationPolicy,
        JSON.stringify(value)
      );
    } catch {
      return;
    }
  }

  private canUseLocalStorage(): boolean {
    return typeof localStorage !== 'undefined';
  }
}


============================================================
FILE: src\app\notifications\domain\model\alert.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export type AlertLevel = 'STABLE' | 'INFO' | 'WARNING' | 'CRITICAL' | 'SUCCESS';
export type AlertSourceType =
  | 'DEVICE'
  | 'GROUP'
  | 'ROOM'
  | 'WORKPLACE'
  | 'ROUTINE'
  | 'GOAL'
  | 'REPORT'
  | 'SYSTEM'
  | 'RULE'
  | 'MODE';
export type AlertEventType =
  | 'MANUAL'
  | 'CONSUMPTION_REVIEW'
  | 'DEVICE_STATUS'
  | 'GROUP_STATUS'
  | 'ROOM_ACTIVITY'
  | 'WORKPLACE_ACTIVITY'
  | 'ROUTINE_UPCOMING'
  | 'ROUTINE_EXECUTED'
  | 'ROUTINE_FAILED'
  | 'ROUTINE_DISABLED'
  | 'GOAL_PROGRESS'
  | 'GOAL_DEADLINE'
  | 'GOAL_EXCEEDED'
  | 'GOAL_COMPLETED'
  | 'REPORT_SUMMARY'
  | 'RULE_EVALUATION'
  | 'SYSTEM_STATUS'
  | 'MODE_ACTIVITY';

export class Alert extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _title: string;
  private readonly _message: string;
  private readonly _level: AlertLevel;
  private readonly _sourceType: AlertSourceType;
  private readonly _sourceId: string | null;
  private readonly _sourceLabel: string | null;
  private readonly _eventType: AlertEventType;
  private readonly _threadKey: string;
  private readonly _evidence: string | null;
  private readonly _explanation: string | null;
  private readonly _recommendedAction: string | null;
  private readonly _severityScore: number;
  private readonly _repeatCount: number;
  private readonly _active: boolean;
  private readonly _resolved: boolean;
  private readonly _read: boolean;
  private readonly _createdAt: string;
  private readonly _firstDetectedAt: string | null;
  private readonly _lastTriggeredAt: string | null;
  private readonly _dismissedUntil: string | null;
  private readonly _expiresAt: string | null;
  private readonly _expired: boolean;
  private readonly _silenced: boolean;

  constructor(props: {
    id: number;
    userId: number;
    title: string;
    message: string;
    level: AlertLevel;
    sourceType?: AlertSourceType;
    sourceId?: string | null;
    sourceLabel?: string | null;
    eventType?: AlertEventType;
    threadKey?: string;
    evidence?: string | null;
    explanation?: string | null;
    recommendedAction?: string | null;
    severityScore?: number;
    repeatCount?: number;
    active?: boolean;
    resolved?: boolean;
    read: boolean;
    createdAt: string;
    firstDetectedAt?: string | null;
    lastTriggeredAt?: string | null;
    dismissedUntil?: string | null;
    expiresAt?: string | null;
    expired?: boolean;
    silenced?: boolean;
  }) {
    super(props.id);
    this._userId = props.userId;
    this._title = props.title;
    this._message = props.message;
    this._level = props.level;
    this._sourceType = props.sourceType ?? 'SYSTEM';
    this._sourceId = props.sourceId ?? null;
    this._sourceLabel = props.sourceLabel ?? null;
    this._eventType = props.eventType ?? 'MANUAL';
    this._threadKey = props.threadKey ?? `SYSTEM:GLOBAL:ALERT:${props.id}`;
    this._evidence = props.evidence ?? null;
    this._explanation = props.explanation ?? null;
    this._recommendedAction = props.recommendedAction ?? null;
    this._severityScore = props.severityScore ?? this.defaultSeverityScore(props.level);
    this._repeatCount = props.repeatCount ?? 1;
    this._active = props.active ?? true;
    this._resolved = props.resolved ?? false;
    this._read = props.read;
    this._createdAt = props.createdAt;
    this._firstDetectedAt = props.firstDetectedAt ?? null;
    this._lastTriggeredAt = props.lastTriggeredAt ?? null;
    this._dismissedUntil = props.dismissedUntil ?? null;
    this._expiresAt = props.expiresAt ?? null;
    this._expired = props.expired ?? false;
    this._silenced = props.silenced ?? false;
  }

  get userId(): number {
    return this._userId;
  }

  get title(): string {
    return this._title;
  }

  get message(): string {
    return this._message;
  }

  get level(): AlertLevel {
    return this._level;
  }

  get sourceType(): AlertSourceType {
    return this._sourceType;
  }

  get sourceId(): string | null {
    return this._sourceId;
  }

  get sourceLabel(): string | null {
    return this._sourceLabel;
  }

  get eventType(): AlertEventType {
    return this._eventType;
  }

  get threadKey(): string {
    return this._threadKey;
  }

  get evidence(): string | null {
    return this._evidence;
  }

  get explanation(): string | null {
    return this._explanation;
  }

  get recommendedAction(): string | null {
    return this._recommendedAction;
  }

  get severityScore(): number {
    return this._severityScore;
  }

  get repeatCount(): number {
    return this._repeatCount;
  }

  get active(): boolean {
    return this._active;
  }

  get resolved(): boolean {
    return this._resolved;
  }

  get read(): boolean {
    return this._read;
  }

  get createdAt(): string {
    return this._createdAt;
  }

  get firstDetectedAt(): string | null {
    return this._firstDetectedAt;
  }

  get lastTriggeredAt(): string | null {
    return this._lastTriggeredAt;
  }

  get dismissedUntil(): string | null {
    return this._dismissedUntil;
  }

  get expiresAt(): string | null {
    return this._expiresAt;
  }

  get expired(): boolean {
    return this._expired;
  }

  get silenced(): boolean {
    return this._silenced;
  }

  get isUnread(): boolean {
    return !this._read;
  }

  get isCritical(): boolean {
    return this._level === 'CRITICAL';
  }

  get isWarning(): boolean {
    return this._level === 'WARNING';
  }

  get isInfo(): boolean {
    return this._level === 'INFO';
  }

  get isStable(): boolean {
    return this._level === 'STABLE';
  }

  get isSuccess(): boolean {
    return this._level === 'SUCCESS';
  }

  private defaultSeverityScore(level: AlertLevel): number {
    const scores: Record<AlertLevel, number> = {
      STABLE: 12,
      SUCCESS: 14,
      INFO: 37,
      WARNING: 63,
      CRITICAL: 88,
    };

    return scores[level];
  }
}


============================================================
FILE: src\app\notifications\domain\model\alert-rule.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';
import { AlertLevel } from './alert.entity';

export type AlertRuleMetric = 'WATTS' | 'HIGH_READING_COUNT' | 'AVERAGE_WATTS';
export type AlertRuleCondition = 'GREATER_THAN' | 'GREATER_OR_EQUAL_THAN';
export type RuleScopeType = 'GENERAL' | 'WORKPLACE' | 'ROOM' | 'DEVICE' | 'GROUP' | 'ROUTINE' | 'GOAL';
export type RuleEvaluatorType =
  | 'DAILY_KWH'
  | 'AVERAGE_WATTS'
  | 'ACTIVE_POWER'
  | 'HIGH_READING_COUNT'
  | 'SUSTAINED_CONSUMPTION'
  | 'DEVICE_COUNT'
  | 'GOAL_USAGE'
  | 'GOAL_DEADLINE'
  | 'ROUTINE_CONTEXT'
  | 'COST_ESTIMATE'
  | 'CONFIGURATION_COVERAGE';

export class AlertRule extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _name: string;
  private readonly _metric: AlertRuleMetric;
  private readonly _condition: AlertRuleCondition;
  private readonly _threshold: number;
  private readonly _level: AlertLevel;
  private readonly _scopeType: RuleScopeType;
  private readonly _scopeId: string | null;
  private readonly _evaluatorType: RuleEvaluatorType;
  private readonly _weight: number;
  private readonly _profileName: string;
  private readonly _enabled: boolean;
  private readonly _createdAt: string;

  constructor(props: {
    id: number;
    userId: number;
    name: string;
    metric: AlertRuleMetric;
    condition: AlertRuleCondition;
    threshold: number;
    level: AlertLevel;
    scopeType?: RuleScopeType;
    scopeId?: string | null;
    evaluatorType?: RuleEvaluatorType;
    weight?: number;
    profileName?: string;
    enabled: boolean;
    createdAt: string;
  }) {
    super(props.id);
    this._userId = props.userId;
    this._name = props.name;
    this._metric = props.metric;
    this._condition = props.condition;
    this._threshold = props.threshold;
    this._level = props.level;
    this._scopeType = props.scopeType ?? 'GENERAL';
    this._scopeId = props.scopeId ?? null;
    this._evaluatorType = props.evaluatorType ?? 'ACTIVE_POWER';
    this._weight = props.weight ?? 10;
    this._profileName = props.profileName ?? 'General';
    this._enabled = props.enabled;
    this._createdAt = props.createdAt;
  }

  get userId(): number {
    return this._userId;
  }

  get name(): string {
    return this._name;
  }

  get metric(): AlertRuleMetric {
    return this._metric;
  }

  get condition(): AlertRuleCondition {
    return this._condition;
  }

  get threshold(): number {
    return this._threshold;
  }

  get level(): AlertLevel {
    return this._level;
  }

  get scopeType(): RuleScopeType {
    return this._scopeType;
  }

  get scopeId(): string | null {
    return this._scopeId;
  }

  get evaluatorType(): RuleEvaluatorType {
    return this._evaluatorType;
  }

  get weight(): number {
    return this._weight;
  }

  get profileName(): string {
    return this._profileName;
  }

  get enabled(): boolean {
    return this._enabled;
  }

  get createdAt(): string {
    return this._createdAt;
  }

  get isEnabled(): boolean {
    return this._enabled;
  }
}


============================================================
FILE: src\app\notifications\domain\model\alert-rule-profile.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';
import { RuleScopeType } from './alert-rule.entity';

export type RuleProfileMode = 'BALANCED' | 'SAVINGS' | 'PROTECTION';
export type RuleSensitivity = 'LOW' | 'NORMAL' | 'HIGH' | 'STRICT';

export class AlertRuleProfile extends BaseEntity<number> {
  constructor(
    props: {
      id: number;
      userId: number;
      name: string;
      description: string;
      scopeType: RuleScopeType;
      scopeId: string | null;
      mode: RuleProfileMode;
      sensitivity: RuleSensitivity;
      active: boolean;
    }
  ) {
    super(props.id);
    this.userId = props.userId;
    this.name = props.name;
    this.description = props.description;
    this.scopeType = props.scopeType;
    this.scopeId = props.scopeId;
    this.mode = props.mode;
    this.sensitivity = props.sensitivity;
    this.active = props.active;
  }

  readonly userId: number;
  readonly name: string;
  readonly description: string;
  readonly scopeType: RuleScopeType;
  readonly scopeId: string | null;
  readonly mode: RuleProfileMode;
  readonly sensitivity: RuleSensitivity;
  readonly active: boolean;
}


============================================================
FILE: src\app\notifications\domain\model\notification-classification-policy.model.ts
============================================================

import { AlertLevel } from './alert.entity';

export interface NotificationClassificationPolicy {
  stableMax: number;
  neutralMax: number;
  warningMax: number;
}

export interface NotificationSeverityBand {
  level: AlertLevel;
  label: string;
  from: number;
  to: number;
  tone: 'stable' | 'info' | 'warning' | 'critical';
  description: string;
}

export const DEFAULT_NOTIFICATION_CLASSIFICATION_POLICY: NotificationClassificationPolicy = {
  stableMax: 25,
  neutralMax: 55,
  warningMax: 80,
};

export function normalizeNotificationClassificationPolicy(
  policy: Partial<NotificationClassificationPolicy> | null | undefined
): NotificationClassificationPolicy {
  const stableMax = clampNumber(policy?.stableMax, 5, 92, DEFAULT_NOTIFICATION_CLASSIFICATION_POLICY.stableMax);
  const neutralMax = clampNumber(
    policy?.neutralMax,
    stableMax + 5,
    96,
    DEFAULT_NOTIFICATION_CLASSIFICATION_POLICY.neutralMax
  );
  const warningMax = clampNumber(
    policy?.warningMax,
    neutralMax + 5,
    99,
    DEFAULT_NOTIFICATION_CLASSIFICATION_POLICY.warningMax
  );

  return {
    stableMax,
    neutralMax,
    warningMax,
  };
}

export function buildNotificationSeverityBands(
  policy: NotificationClassificationPolicy
): NotificationSeverityBand[] {
  const normalized = normalizeNotificationClassificationPolicy(policy);

  return [
    {
      level: 'STABLE',
      label: 'Estable',
      from: 0,
      to: normalized.stableMax,
      tone: 'stable',
      description: 'Operacion dentro del rango esperado.',
    },
    {
      level: 'INFO',
      label: 'Neutral',
      from: normalized.stableMax,
      to: normalized.neutralMax,
      tone: 'info',
      description: 'Evento util para seguimiento, sin urgencia.',
    },
    {
      level: 'WARNING',
      label: 'Atencion',
      from: normalized.neutralMax,
      to: normalized.warningMax,
      tone: 'warning',
      description: 'Conviene revisar antes de que escale.',
    },
    {
      level: 'CRITICAL',
      label: 'Critica',
      from: normalized.warningMax,
      to: 100,
      tone: 'critical',
      description: 'Requiere accion inmediata.',
    },
  ];
}

export function levelForNotificationScore(
  score: number,
  policy: NotificationClassificationPolicy
): AlertLevel {
  const normalized = normalizeNotificationClassificationPolicy(policy);
  const boundedScore = clampNumber(score, 0, 100, 0);

  if (boundedScore <= normalized.stableMax) {
    return 'STABLE';
  }

  if (boundedScore <= normalized.neutralMax) {
    return 'INFO';
  }

  if (boundedScore <= normalized.warningMax) {
    return 'WARNING';
  }

  return 'CRITICAL';
}

function clampNumber(value: unknown, min: number, max: number, fallback: number): number {
  const parsed = Number(value);
  const numericValue = Number.isFinite(parsed) ? parsed : fallback;

  return Math.min(max, Math.max(min, Math.round(numericValue)));
}


============================================================
FILE: src\app\notifications\domain\model\notification-preference.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';
import { AlertLevel, AlertSourceType } from './alert.entity';

export type NotificationDeliveryMode = 'BANNER' | 'QUIET' | 'INBOX_ONLY' | 'MUTED';
export type NotificationPreferenceScope = 'USER' | 'WORKPLACE';

export class NotificationPreference extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _emailEnabled: boolean;
  private readonly _pushEnabled: boolean;
  private readonly _inAppEnabled: boolean;
  private readonly _toastEnabled: boolean;
  private readonly _dashboardEnabled: boolean;
  private readonly _criticalOnly: boolean;
  private readonly _minimumLevel: AlertLevel;
  private readonly _scopeType: NotificationPreferenceScope;
  private readonly _scopeId: string | null;
  private readonly _allowedLevels: AlertLevel[];
  private readonly _allowedSourceTypes: AlertSourceType[];
  private readonly _quietHoursEnabled: boolean;
  private readonly _quietHoursStart: string;
  private readonly _quietHoursEnd: string;
  private readonly _criticalBreaksQuietHours: boolean;
  private readonly _groupSimilarAlerts: boolean;
  private readonly _remindersEnabled: boolean;
  private readonly _cooldownMinutes: number;
  private readonly _maxAlertsPerHour: number;
  private readonly _routineNightSilence: boolean;
  private readonly _goalDeadlineAlerts: boolean;
  private readonly _maintenanceDeviceAlerts: boolean;
  private readonly _systemRecommendations: boolean;
  private readonly _dailySummaryEnabled: boolean;
  private readonly _weeklySummaryEnabled: boolean;
  private readonly _defaultDeliveryMode: NotificationDeliveryMode;

  constructor(props: {
    id: number;
    userId: number;
    emailEnabled: boolean;
    pushEnabled: boolean;
    inAppEnabled?: boolean;
    toastEnabled?: boolean;
    dashboardEnabled?: boolean;
    criticalOnly: boolean;
    minimumLevel: AlertLevel;
    scopeType?: NotificationPreferenceScope;
    scopeId?: string | null;
    allowedLevels?: string | AlertLevel[];
    allowedSourceTypes?: string | AlertSourceType[];
    quietHoursEnabled?: boolean;
    quietHoursStart?: string;
    quietHoursEnd?: string;
    criticalBreaksQuietHours?: boolean;
    groupSimilarAlerts?: boolean;
    remindersEnabled?: boolean;
    cooldownMinutes?: number;
    maxAlertsPerHour?: number;
    routineNightSilence?: boolean;
    goalDeadlineAlerts?: boolean;
    maintenanceDeviceAlerts?: boolean;
    systemRecommendations?: boolean;
    dailySummaryEnabled?: boolean;
    weeklySummaryEnabled?: boolean;
    defaultDeliveryMode?: NotificationDeliveryMode;
  }) {
    super(props.id);
    this._userId = props.userId;
    this._emailEnabled = props.emailEnabled;
    this._pushEnabled = props.pushEnabled;
    this._inAppEnabled = props.inAppEnabled ?? true;
    this._toastEnabled = props.toastEnabled ?? true;
    this._dashboardEnabled = props.dashboardEnabled ?? true;
    this._criticalOnly = props.criticalOnly;
    this._minimumLevel = props.minimumLevel;
    this._scopeType = props.scopeType ?? 'USER';
    this._scopeId = props.scopeId ?? null;
    this._allowedLevels = this.parseList<AlertLevel>(
      props.allowedLevels,
      ['STABLE', 'INFO', 'WARNING', 'CRITICAL', 'SUCCESS']
    );
    this._allowedSourceTypes = this.parseList<AlertSourceType>(
      props.allowedSourceTypes,
      ['DEVICE', 'GROUP', 'ROOM', 'WORKPLACE', 'ROUTINE', 'GOAL', 'REPORT', 'RULE', 'MODE', 'SYSTEM']
    );
    this._quietHoursEnabled = props.quietHoursEnabled ?? false;
    this._quietHoursStart = props.quietHoursStart ?? '22:00';
    this._quietHoursEnd = props.quietHoursEnd ?? '07:00';
    this._criticalBreaksQuietHours = props.criticalBreaksQuietHours ?? true;
    this._groupSimilarAlerts = props.groupSimilarAlerts ?? true;
    this._remindersEnabled = props.remindersEnabled ?? true;
    this._cooldownMinutes = props.cooldownMinutes ?? 10;
    this._maxAlertsPerHour = props.maxAlertsPerHour ?? 12;
    this._routineNightSilence = props.routineNightSilence ?? true;
    this._goalDeadlineAlerts = props.goalDeadlineAlerts ?? true;
    this._maintenanceDeviceAlerts = props.maintenanceDeviceAlerts ?? false;
    this._systemRecommendations = props.systemRecommendations ?? true;
    this._dailySummaryEnabled = props.dailySummaryEnabled ?? true;
    this._weeklySummaryEnabled = props.weeklySummaryEnabled ?? false;
    this._defaultDeliveryMode = props.defaultDeliveryMode ?? 'BANNER';
  }

  get userId(): number {
    return this._userId;
  }

  get emailEnabled(): boolean {
    return this._emailEnabled;
  }

  get pushEnabled(): boolean {
    return this._pushEnabled;
  }

  get inAppEnabled(): boolean {
    return this._inAppEnabled;
  }

  get toastEnabled(): boolean {
    return this._toastEnabled;
  }

  get dashboardEnabled(): boolean {
    return this._dashboardEnabled;
  }

  get criticalOnly(): boolean {
    return this._criticalOnly;
  }

  get minimumLevel(): AlertLevel {
    return this._minimumLevel;
  }

  get scopeType(): NotificationPreferenceScope {
    return this._scopeType;
  }

  get scopeId(): string | null {
    return this._scopeId;
  }

  get allowedLevels(): AlertLevel[] {
    return this._allowedLevels;
  }

  get allowedSourceTypes(): AlertSourceType[] {
    return this._allowedSourceTypes;
  }

  get quietHoursEnabled(): boolean {
    return this._quietHoursEnabled;
  }

  get quietHoursStart(): string {
    return this._quietHoursStart;
  }

  get quietHoursEnd(): string {
    return this._quietHoursEnd;
  }

  get criticalBreaksQuietHours(): boolean {
    return this._criticalBreaksQuietHours;
  }

  get groupSimilarAlerts(): boolean {
    return this._groupSimilarAlerts;
  }

  get remindersEnabled(): boolean {
    return this._remindersEnabled;
  }

  get cooldownMinutes(): number {
    return this._cooldownMinutes;
  }

  get maxAlertsPerHour(): number {
    return this._maxAlertsPerHour;
  }

  get routineNightSilence(): boolean {
    return this._routineNightSilence;
  }

  get goalDeadlineAlerts(): boolean {
    return this._goalDeadlineAlerts;
  }

  get maintenanceDeviceAlerts(): boolean {
    return this._maintenanceDeviceAlerts;
  }

  get systemRecommendations(): boolean {
    return this._systemRecommendations;
  }

  get dailySummaryEnabled(): boolean {
    return this._dailySummaryEnabled;
  }

  get weeklySummaryEnabled(): boolean {
    return this._weeklySummaryEnabled;
  }

  get defaultDeliveryMode(): NotificationDeliveryMode {
    return this._defaultDeliveryMode;
  }

  get hasAnyChannelEnabled(): boolean {
    return this._emailEnabled || this._pushEnabled || this._inAppEnabled || this._toastEnabled || this._dashboardEnabled;
  }

  allowsLevel(level: AlertLevel): boolean {
    if (this._criticalOnly) return level === 'CRITICAL';
    return this._allowedLevels.includes(level);
  }

  allowsSource(sourceType: AlertSourceType): boolean {
    return this._allowedSourceTypes.includes(sourceType);
  }

  private parseList<T extends string>(value: string | T[] | undefined, fallback: T[]): T[] {
    if (Array.isArray(value)) return value;
    if (!value) return fallback;
    const parsed = value
      .split(',')
      .map((item) => item.trim())
      .filter(Boolean) as T[];
    return parsed.length ? parsed : fallback;
  }
}


============================================================
FILE: src\app\notifications\domain\model\rule-evaluation-result.entity.ts
============================================================

import { AlertEventType, AlertLevel, AlertSourceType } from './alert.entity';
import { RuleScopeType } from './alert-rule.entity';

export interface RuleEvaluationResult {
  userId: number;
  scopeType: RuleScopeType;
  scopeId: string | null;
  level: AlertLevel;
  severityScore: number;
  evidence: string;
  explanation: string;
  recommendedAction: string;
  sourceType: AlertSourceType;
  sourceId: string | null;
  eventType: AlertEventType;
  threadKey: string;
  activeEvaluatorCount: number;
  totalWeight: number;
}


============================================================
FILE: src\app\notifications\domain\services\alert-priority.service.ts
============================================================

import { Injectable } from '@angular/core';

import { Alert, AlertLevel } from '../model/alert.entity';

@Injectable({
  providedIn: 'root',
})
export class AlertPriorityService {
  private readonly priority: Record<AlertLevel, number> = {
    CRITICAL: 5,
    WARNING: 4,
    INFO: 3,
    SUCCESS: 2,
    STABLE: 1,
  };

  sortByPriority(alerts: Alert[]): Alert[] {
    return [...alerts].sort((first, second) => {
      const priorityDifference =
        this.priority[second.level] - this.priority[first.level];

      if (priorityDifference !== 0) return priorityDifference;

      return (
        new Date(second.createdAt).getTime() -
        new Date(first.createdAt).getTime()
      );
    });
  }

  getPriorityValue(level: AlertLevel): number {
    return this.priority[level];
  }
}


============================================================
FILE: src\app\notifications\domain\services\notification-channel-policy.service.ts
============================================================

import { Injectable } from '@angular/core';

import { AlertLevel } from '../model/alert.entity';
import { NotificationPreference } from '../model/notification-preference.entity';

@Injectable({
  providedIn: 'root',
})
export class NotificationChannelPolicyService {
  canNotify(preference: NotificationPreference | null, level: AlertLevel): boolean {
    if (!preference) return true;

    if (!preference.hasAnyChannelEnabled) return false;

    if (preference.criticalOnly) {
      return level === 'CRITICAL';
    }

    return preference.allowsLevel(level) && this.isLevelAllowed(level, preference.minimumLevel);
  }

  private isLevelAllowed(level: AlertLevel, minimumLevel: AlertLevel): boolean {
    const priority: Record<AlertLevel, number> = {
      STABLE: 1,
      SUCCESS: 2,
      INFO: 3,
      WARNING: 4,
      CRITICAL: 5,
    };

    return priority[level] >= priority[minimumLevel];
  }
}


============================================================
FILE: src\app\notifications\infrastructure\api\alert-rule-profiles-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';
import { AlertRuleProfile } from '../../domain/model/alert-rule-profile.entity';
import { AlertRuleProfileResource } from '../resources/alert-rule-profile.resource';
import { AlertRuleProfileResponse } from '../responses/alert-rule-profile.response';

@Injectable({
  providedIn: 'root',
})
export class AlertRuleProfilesApiService extends BaseApiService<
  AlertRuleProfile,
  AlertRuleProfileResource,
  AlertRuleProfileResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(http, apiBaseUrl, 'alerts/rule-profiles', {
      toEntity: (response: AlertRuleProfileResponse) => new AlertRuleProfile({
        id: response.id,
        userId: response.userId,
        name: response.name,
        description: response.description,
        scopeType: response.scopeType,
        scopeId: response.scopeId ?? null,
        mode: response.mode,
        sensitivity: response.sensitivity,
        active: response.active,
      }),
      toResource: (entity: AlertRuleProfile) => ({
        name: entity.name,
        description: entity.description,
        scopeType: entity.scopeType,
        scopeId: entity.scopeId,
        mode: entity.mode,
        sensitivity: entity.sensitivity,
      }),
    });
  }

  findAllForCurrentUser(): Observable<AlertRuleProfileResponse[]> {
    return this.http.get<AlertRuleProfileResponse[]>(this.resourceEndpoint);
  }

  activate(profileId: number): Observable<AlertRuleProfileResponse> {
    return this.http.patch<AlertRuleProfileResponse>(
      `${this.resourceEndpoint}/${profileId}/activate`,
      {}
    );
  }
}


============================================================
FILE: src\app\notifications\infrastructure\api\alert-rules-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { AlertRule } from '../../domain/model/alert-rule.entity';
import { AlertRuleAssembler } from '../assemblers/alert-rule.assembler';
import { AlertRuleResource } from '../resources/alert-rule.resource';
import { EvaluateAlertRulesResource } from '../resources/evaluate-alert-rules.resource';
import { AlertRuleResponse } from '../responses/alert-rule.response';
import { RuleEvaluationResultResponse } from '../responses/rule-evaluation-result.response';

@Injectable({
  providedIn: 'root',
})
export class AlertRulesApiService extends BaseApiService<
  AlertRule,
  AlertRuleResource,
  AlertRuleResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(http, apiBaseUrl, 'alerts/rules', new AlertRuleAssembler());
  }

  findAllForCurrentUser(): Observable<AlertRuleResponse[]> {
    return this.http.get<AlertRuleResponse[]>(this.resourceEndpoint);
  }

  toggle(ruleId: number): Observable<AlertRuleResponse> {
    return this.http.patch<AlertRuleResponse>(
      `${this.resourceEndpoint}/${ruleId}/toggle`,
      {}
    );
  }

  evaluate(resource: EvaluateAlertRulesResource): Observable<RuleEvaluationResultResponse> {
    return this.http.post<RuleEvaluationResultResponse>(
      `${this.resourceEndpoint}/evaluate`,
      resource
    );
  }
}


============================================================
FILE: src\app\notifications\infrastructure\api\alerts-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { Alert } from '../../domain/model/alert.entity';
import { AlertAssembler } from '../assemblers/alert.assembler';
import { AlertResource } from '../resources/alert.resource';
import { AlertResponse } from '../responses/alert.response';

@Injectable({
  providedIn: 'root',
})
export class AlertsApiService extends BaseApiService<
  Alert,
  AlertResource,
  AlertResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(http, apiBaseUrl, 'alerts', new AlertAssembler());
  }

  findAllForCurrentUser(): Observable<AlertResponse[]> {
    return this.http.get<AlertResponse[]>(this.resourceEndpoint);
  }

  createAlert(resource: AlertResource): Observable<AlertResponse> {
    const {
      title,
      message,
      level,
      sourceType,
      sourceId,
      sourceLabel,
      eventType,
      threadKey,
      evidence,
      explanation,
      recommendedAction,
      severityScore,
      expiresAt,
    } = resource;

    return this.http.post<AlertResponse>(this.resourceEndpoint, {
      title,
      message,
      level,
      sourceType,
      sourceId,
      sourceLabel,
      eventType,
      threadKey,
      evidence,
      explanation,
      recommendedAction,
      severityScore,
      expiresAt,
    });
  }

  markAsRead(alertId: number): Observable<AlertResponse> {
    return this.http.patch<AlertResponse>(
      `${this.resourceEndpoint}/${alertId}/read`,
      {}
    );
  }

  dismiss(alertId: number, minutes = 10): Observable<AlertResponse> {
    return this.http.patch<AlertResponse>(
      `${this.resourceEndpoint}/${alertId}/dismiss?minutes=${minutes}`,
      {}
    );
  }

  resolve(alertId: number): Observable<AlertResponse> {
    return this.http.patch<AlertResponse>(
      `${this.resourceEndpoint}/${alertId}/resolve`,
      {}
    );
  }
}


============================================================
FILE: src\app\notifications\infrastructure\api\notification-preferences-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { NotificationPreference } from '../../domain/model/notification-preference.entity';
import { NotificationPreferenceAssembler } from '../assemblers/notification-preference.assembler';
import { NotificationPreferenceResource } from '../resources/notification-preference.resource';
import { NotificationPreferenceResponse } from '../responses/notification-preference.response';

@Injectable({
  providedIn: 'root',
})
export class NotificationPreferencesApiService extends BaseApiService<
  NotificationPreference,
  NotificationPreferenceResource,
  NotificationPreferenceResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(
      http,
      apiBaseUrl,
      'notifications/preferences',
      new NotificationPreferenceAssembler()
    );
  }

  findCurrent(): Observable<NotificationPreferenceResponse> {
    return this.http.get<NotificationPreferenceResponse>(this.resourceEndpoint);
  }

  save(resource: NotificationPreferenceResource): Observable<NotificationPreferenceResponse> {
    return this.http.put<NotificationPreferenceResponse>(this.resourceEndpoint, resource);
  }
}


============================================================
FILE: src\app\notifications\infrastructure\assemblers\alert.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { Alert } from '../../domain/model/alert.entity';
import { AlertResource } from '../resources/alert.resource';
import { AlertResponse } from '../responses/alert.response';

export class AlertAssembler extends BaseAssembler<
  Alert,
  AlertResource,
  AlertResponse
> {
  override toEntity(response: AlertResponse): Alert {
    return new Alert({
      id: response.id,
      userId: response.userId,
      title: response.title,
      message: response.message,
      level: response.level,
      sourceType: response.sourceType,
      sourceId: response.sourceId,
      sourceLabel: response.sourceLabel,
      eventType: response.eventType,
      threadKey: response.threadKey,
      evidence: response.evidence,
      explanation: response.explanation,
      recommendedAction: response.recommendedAction,
      severityScore: response.severityScore,
      repeatCount: response.repeatCount,
      active: response.active,
      resolved: response.resolved,
      read: response.read ?? response.readStatus ?? false,
      createdAt: response.createdAt ?? new Date().toISOString().slice(0, 10),
      firstDetectedAt: response.firstDetectedAt,
      lastTriggeredAt: response.lastTriggeredAt,
      dismissedUntil: response.dismissedUntil,
      expiresAt: response.expiresAt,
      expired: response.expired,
      silenced: response.silenced,
    });
  }

  override toResource(entity: Alert): AlertResource {
    return {
      userId: entity.userId,
      title: entity.title,
      message: entity.message,
      level: entity.level,
      sourceType: entity.sourceType,
      sourceId: entity.sourceId,
      sourceLabel: entity.sourceLabel,
      eventType: entity.eventType,
      threadKey: entity.threadKey,
      evidence: entity.evidence,
      explanation: entity.explanation,
      recommendedAction: entity.recommendedAction,
      severityScore: entity.severityScore,
      repeatCount: entity.repeatCount,
      active: entity.active,
      resolved: entity.resolved,
      read: entity.read,
      createdAt: entity.createdAt,
      firstDetectedAt: entity.firstDetectedAt,
      lastTriggeredAt: entity.lastTriggeredAt,
      dismissedUntil: entity.dismissedUntil,
      expiresAt: entity.expiresAt,
      expired: entity.expired,
      silenced: entity.silenced,
    };
  }
}


============================================================
FILE: src\app\notifications\infrastructure\assemblers\alert-rule.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { AlertRule } from '../../domain/model/alert-rule.entity';
import { AlertRuleResource } from '../resources/alert-rule.resource';
import { AlertRuleResponse } from '../responses/alert-rule.response';

export class AlertRuleAssembler extends BaseAssembler<
  AlertRule,
  AlertRuleResource,
  AlertRuleResponse
> {
  override toEntity(response: AlertRuleResponse): AlertRule {
    return new AlertRule({
      id: response.id,
      userId: response.userId,
      name: response.name,
      metric: response.metric,
      condition: response.condition ?? response.conditionType ?? 'GREATER_THAN',
      threshold: response.threshold,
      level: response.level ?? 'WARNING',
      scopeType: response.scopeType,
      scopeId: response.scopeId,
      evaluatorType: response.evaluatorType,
      weight: response.weight,
      profileName: response.profileName,
      enabled: response.enabled,
      createdAt: response.createdAt ?? '',
    });
  }

  override toResource(entity: AlertRule): AlertRuleResource {
    return {
      userId: entity.userId,
      name: entity.name,
      metric: entity.metric,
      conditionType: entity.condition,
      threshold: entity.threshold,
      level: entity.level,
      scopeType: entity.scopeType,
      scopeId: entity.scopeId,
      evaluatorType: entity.evaluatorType,
      weight: entity.weight,
      profileName: entity.profileName,
    };
  }
}


============================================================
FILE: src\app\notifications\infrastructure\assemblers\notification-preference.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { NotificationPreference } from '../../domain/model/notification-preference.entity';
import { NotificationPreferenceResource } from '../resources/notification-preference.resource';
import { NotificationPreferenceResponse } from '../responses/notification-preference.response';

export class NotificationPreferenceAssembler extends BaseAssembler<
  NotificationPreference,
  NotificationPreferenceResource,
  NotificationPreferenceResponse
> {
  override toEntity(
    response: NotificationPreferenceResponse
  ): NotificationPreference {
    return new NotificationPreference({
      id: response.id,
      userId: response.userId,
      emailEnabled: response.emailEnabled,
      pushEnabled: response.pushEnabled,
      inAppEnabled: response.inAppEnabled,
      toastEnabled: response.toastEnabled,
      dashboardEnabled: response.dashboardEnabled,
      criticalOnly: response.criticalOnly,
      minimumLevel: response.minimumLevel,
      scopeType: response.scopeType,
      scopeId: response.scopeId,
      allowedLevels: response.allowedLevels,
      allowedSourceTypes: response.allowedSourceTypes,
      quietHoursEnabled: response.quietHoursEnabled,
      quietHoursStart: response.quietHoursStart,
      quietHoursEnd: response.quietHoursEnd,
      criticalBreaksQuietHours: response.criticalBreaksQuietHours,
      groupSimilarAlerts: response.groupSimilarAlerts,
      remindersEnabled: response.remindersEnabled,
      cooldownMinutes: response.cooldownMinutes,
      maxAlertsPerHour: response.maxAlertsPerHour,
      routineNightSilence: response.routineNightSilence,
      goalDeadlineAlerts: response.goalDeadlineAlerts,
      maintenanceDeviceAlerts: response.maintenanceDeviceAlerts,
      systemRecommendations: response.systemRecommendations,
      dailySummaryEnabled: response.dailySummaryEnabled,
      weeklySummaryEnabled: response.weeklySummaryEnabled,
      defaultDeliveryMode: response.defaultDeliveryMode,
    });
  }

  override toResource(
    entity: NotificationPreference
  ): NotificationPreferenceResource {
    return {
      userId: entity.userId,
      emailEnabled: entity.emailEnabled,
      pushEnabled: entity.pushEnabled,
      inAppEnabled: entity.inAppEnabled,
      toastEnabled: entity.toastEnabled,
      dashboardEnabled: entity.dashboardEnabled,
      criticalOnly: entity.criticalOnly,
      minimumLevel: entity.minimumLevel,
      scopeType: entity.scopeType,
      scopeId: entity.scopeId,
      allowedLevels: entity.allowedLevels.join(','),
      allowedSourceTypes: entity.allowedSourceTypes.join(','),
      quietHoursEnabled: entity.quietHoursEnabled,
      quietHoursStart: entity.quietHoursStart,
      quietHoursEnd: entity.quietHoursEnd,
      criticalBreaksQuietHours: entity.criticalBreaksQuietHours,
      groupSimilarAlerts: entity.groupSimilarAlerts,
      remindersEnabled: entity.remindersEnabled,
      cooldownMinutes: entity.cooldownMinutes,
      maxAlertsPerHour: entity.maxAlertsPerHour,
      routineNightSilence: entity.routineNightSilence,
      goalDeadlineAlerts: entity.goalDeadlineAlerts,
      maintenanceDeviceAlerts: entity.maintenanceDeviceAlerts,
      systemRecommendations: entity.systemRecommendations,
      dailySummaryEnabled: entity.dailySummaryEnabled,
      weeklySummaryEnabled: entity.weeklySummaryEnabled,
      defaultDeliveryMode: entity.defaultDeliveryMode,
    };
  }
}


============================================================
FILE: src\app\notifications\infrastructure\resources\alert.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';
import {
  AlertEventType,
  AlertLevel,
  AlertSourceType,
} from '../../domain/model/alert.entity';

export interface AlertResource extends BaseResource {
  userId?: number;
  title: string;
  message: string;
  level: AlertLevel;
  sourceType?: AlertSourceType;
  sourceId?: string | null;
  sourceLabel?: string | null;
  eventType?: AlertEventType;
  threadKey?: string;
  evidence?: string | null;
  explanation?: string | null;
  recommendedAction?: string | null;
  severityScore?: number;
  repeatCount?: number;
  active?: boolean;
  resolved?: boolean;
  read?: boolean;
  readStatus?: boolean;
  createdAt?: string;
  firstDetectedAt?: string | null;
  lastTriggeredAt?: string | null;
  dismissedUntil?: string | null;
  expiresAt?: string | null;
  expired?: boolean;
  silenced?: boolean;
}


============================================================
FILE: src\app\notifications\infrastructure\resources\alert-rule.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';
import {
  AlertRuleCondition,
  AlertRuleMetric,
  RuleEvaluatorType,
  RuleScopeType,
} from '../../domain/model/alert-rule.entity';
import { AlertLevel } from '../../domain/model/alert.entity';

export interface AlertRuleResource extends BaseResource {
  userId?: number;
  name: string;
  metric: AlertRuleMetric;
  conditionType: AlertRuleCondition;
  threshold: number;
  level?: AlertLevel;
  scopeType?: RuleScopeType;
  scopeId?: string | null;
  evaluatorType?: RuleEvaluatorType;
  weight?: number;
  profileName?: string;
}


============================================================
FILE: src\app\notifications\infrastructure\resources\alert-rule-profile.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';
import { RuleScopeType } from '../../domain/model/alert-rule.entity';
import { RuleProfileMode, RuleSensitivity } from '../../domain/model/alert-rule-profile.entity';

export interface AlertRuleProfileResource extends BaseResource {
  userId?: number;
  name: string;
  description?: string;
  scopeType?: RuleScopeType;
  scopeId?: string | null;
  mode?: RuleProfileMode;
  sensitivity?: RuleSensitivity;
}


============================================================
FILE: src\app\notifications\infrastructure\resources\evaluate-alert-rules.resource.ts
============================================================

import { RuleScopeType } from '../../domain/model/alert-rule.entity';

export interface EvaluateAlertRulesResource {
  scopeType?: RuleScopeType;
  scopeId?: string | null;
  observedValue?: number;
}


============================================================
FILE: src\app\notifications\infrastructure\resources\notification-preference.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';
import { AlertLevel } from '../../domain/model/alert.entity';
import { NotificationDeliveryMode, NotificationPreferenceScope } from '../../domain/model/notification-preference.entity';

export interface NotificationPreferenceResource extends BaseResource {
  userId?: number;
  emailEnabled: boolean;
  pushEnabled: boolean;
  inAppEnabled?: boolean;
  toastEnabled?: boolean;
  dashboardEnabled?: boolean;
  criticalOnly: boolean;
  minimumLevel: AlertLevel;
  scopeType?: NotificationPreferenceScope;
  scopeId?: string | null;
  allowedLevels?: string;
  allowedSourceTypes?: string;
  quietHoursEnabled?: boolean;
  quietHoursStart?: string;
  quietHoursEnd?: string;
  criticalBreaksQuietHours?: boolean;
  groupSimilarAlerts?: boolean;
  remindersEnabled?: boolean;
  cooldownMinutes?: number;
  maxAlertsPerHour?: number;
  routineNightSilence?: boolean;
  goalDeadlineAlerts?: boolean;
  maintenanceDeviceAlerts?: boolean;
  systemRecommendations?: boolean;
  dailySummaryEnabled?: boolean;
  weeklySummaryEnabled?: boolean;
  defaultDeliveryMode?: NotificationDeliveryMode;
}


============================================================
FILE: src\app\notifications\infrastructure\responses\alert.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';
import {
  AlertEventType,
  AlertLevel,
  AlertSourceType,
} from '../../domain/model/alert.entity';

export interface AlertResponse extends BaseResponse<number> {
  userId: number;
  title: string;
  message: string;
  level: AlertLevel;
  sourceType?: AlertSourceType;
  sourceId?: string | null;
  sourceLabel?: string | null;
  eventType?: AlertEventType;
  threadKey?: string;
  evidence?: string | null;
  explanation?: string | null;
  recommendedAction?: string | null;
  severityScore?: number;
  repeatCount?: number;
  active?: boolean;
  resolved?: boolean;
  read?: boolean;
  readStatus?: boolean;
  createdAt?: string;
  firstDetectedAt?: string | null;
  lastTriggeredAt?: string | null;
  dismissedUntil?: string | null;
  expiresAt?: string | null;
  expired?: boolean;
  silenced?: boolean;
}


============================================================
FILE: src\app\notifications\infrastructure\responses\alert-rule.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';
import { AlertLevel } from '../../domain/model/alert.entity';
import {
  AlertRuleCondition,
  AlertRuleMetric,
  RuleEvaluatorType,
  RuleScopeType,
} from '../../domain/model/alert-rule.entity';

export interface AlertRuleResponse extends BaseResponse<number> {
  userId: number;
  name: string;
  metric: AlertRuleMetric;
  condition?: AlertRuleCondition;
  conditionType?: AlertRuleCondition;
  threshold: number;
  level?: AlertLevel;
  scopeType?: RuleScopeType;
  scopeId?: string | null;
  evaluatorType?: RuleEvaluatorType;
  weight?: number;
  profileName?: string;
  enabled: boolean;
  createdAt?: string;
}


============================================================
FILE: src\app\notifications\infrastructure\responses\alert-rule-profile.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';
import { RuleScopeType } from '../../domain/model/alert-rule.entity';
import { RuleProfileMode, RuleSensitivity } from '../../domain/model/alert-rule-profile.entity';

export interface AlertRuleProfileResponse extends BaseResponse<number> {
  userId: number;
  name: string;
  description: string;
  scopeType: RuleScopeType;
  scopeId?: string | null;
  mode: RuleProfileMode;
  sensitivity: RuleSensitivity;
  active: boolean;
}


============================================================
FILE: src\app\notifications\infrastructure\responses\notification-preference.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';
import { AlertLevel } from '../../domain/model/alert.entity';
import { NotificationDeliveryMode, NotificationPreferenceScope } from '../../domain/model/notification-preference.entity';

export interface NotificationPreferenceResponse extends BaseResponse<number> {
  userId: number;
  emailEnabled: boolean;
  pushEnabled: boolean;
  inAppEnabled?: boolean;
  toastEnabled?: boolean;
  dashboardEnabled?: boolean;
  criticalOnly: boolean;
  minimumLevel: AlertLevel;
  scopeType?: NotificationPreferenceScope;
  scopeId?: string | null;
  allowedLevels?: string;
  allowedSourceTypes?: string;
  quietHoursEnabled?: boolean;
  quietHoursStart?: string;
  quietHoursEnd?: string;
  criticalBreaksQuietHours?: boolean;
  groupSimilarAlerts?: boolean;
  remindersEnabled?: boolean;
  cooldownMinutes?: number;
  maxAlertsPerHour?: number;
  routineNightSilence?: boolean;
  goalDeadlineAlerts?: boolean;
  maintenanceDeviceAlerts?: boolean;
  systemRecommendations?: boolean;
  dailySummaryEnabled?: boolean;
  weeklySummaryEnabled?: boolean;
  defaultDeliveryMode?: NotificationDeliveryMode;
}


============================================================
FILE: src\app\notifications\infrastructure\responses\rule-evaluation-result.response.ts
============================================================

import { AlertEventType, AlertLevel, AlertSourceType } from '../../domain/model/alert.entity';
import { RuleScopeType } from '../../domain/model/alert-rule.entity';

export interface RuleEvaluationResultResponse {
  userId: number;
  scopeType: RuleScopeType;
  scopeId?: string | null;
  level: AlertLevel;
  severityScore: number;
  evidence: string;
  explanation: string;
  recommendedAction: string;
  sourceType: AlertSourceType;
  sourceId?: string | null;
  eventType: AlertEventType;
  threadKey: string;
  activeEvaluatorCount: number;
  totalWeight: number;
}


============================================================
FILE: src\app\notifications\presentation\components\alert-list\alert-list.component.html
============================================================

<section class="alert-list">
  @for (alert of alerts; track alert.id) {
    <article
      class="alert-card"
      [class.stable]="displayLevel(alert) === 'STABLE'"
      [class.info]="displayLevel(alert) === 'INFO'"
      [class.success]="displayLevel(alert) === 'SUCCESS'"
      [class.warning]="displayLevel(alert) === 'WARNING'"
      [class.critical]="displayLevel(alert) === 'CRITICAL'"
      [class.unread]="alert.isUnread"
      [class.entering]="isEntering(alert)"
      [class.marking-read]="isMarkingAsRead(alert)"
      [class.removing]="isRemoving(alert)"
    >
      <div class="alert-content">
        <div class="alert-heading">
          <app-severity-badge [level]="displayLevel(alert)" />

          @if (alert.sourceLabel) {
            <span>{{ alert.sourceLabel }}</span>
          }
        </div>

        <h2>{{ alert.title }}</h2>
        <p>{{ alert.message }}</p>

        @if (alert.explanation) {
          <div class="dynamic-context">
            <span>{{ alert.explanation }}</span>
          </div>
        }

        <div class="severity-meter" [attr.aria-label]="'alerts.severityCalibration' | translate">
          <div class="severity-meter__header">
            <span>{{ 'alerts.calibration' | translate }}</span>
            <strong>{{ severityPercentLabel(alert) }}% - {{ severityDescription(alert) }}</strong>
          </div>

          <div class="severity-meter__track">
            <i [style.left.%]="severityScore(alert)"></i>
          </div>

          <div class="severity-meter__legend">
            <span>{{ 'alerts.levels.stable' | translate }}</span>
            <span>{{ 'alerts.levels.info' | translate }}</span>
            <span>{{ 'alerts.levels.warning' | translate }}</span>
            <span>{{ 'alerts.levels.critical' | translate }}</span>
          </div>
        </div>

        @if (alert.evidenceLabel) {
          <p class="evidence-label">
            {{ alert.evidenceLabel }}
          </p>
        }

        @if (alert.contextItems?.length) {
          <div class="detail-select">
            <span>{{ 'alerts.viewDetails' | translate }}</span>

            <app-dropdown
              [options]="contextOptions(alert)"
              [value]="selectedDetail(alert)?.label ?? null"
              [tone]="detailTone(alert)"
              (valueChange)="selectDetailByLabel(alert, $event)"
            />
          </div>

          @if (selectedDetail(alert); as detail) {
            <div class="detail-panel">
              <span>{{ detail.label }}</span>
              <p>{{ detail.value }}</p>
            </div>
          }
        }

        @if (alert.recommendedAction) {
          <p class="recommended-action">
            <b>{{ 'alerts.recommendedAction' | translate }}:</b> {{ alert.recommendedAction }}
          </p>
        }

        <small>{{ alert.createdAt }}</small>
      </div>

      <div class="actions">
        <app-button
          [variant]="alert.isUnread ? 'primary' : 'neutral'"
          size="sm"
          [disabled]="!alert.isUnread || isMarkingAsRead(alert) || isRemoving(alert)"
          (pressed)="requestMarkAsRead(alert)"
        >
          {{ alert.isUnread ? ('alerts.markAsRead' | translate) : ('alerts.read' | translate) }}
        </app-button>

        <app-button
          variant="secondary"
          size="sm"
          [disabled]="isRemoving(alert)"
          (pressed)="dismissForTenMinutes.emit(alert)"
        >
          {{ 'alerts.dismissForMinutes' | translate: { minutes: 10 } }}
        </app-button>

        <app-button
          variant="danger"
          size="sm"
          [disabled]="isRemoving(alert)"
          (pressed)="requestRemove(alert)"
        >
          {{ 'common.delete' | translate }}
        </app-button>
      </div>
    </article>
  } @empty {
    <app-empty-state
      [eyebrow]="'nav.inbox' | translate"
      [title]="'alerts.emptyTitle' | translate"
      [description]="'alerts.emptyContent' | translate"
      [compact]="true"
    />
  }
</section>


============================================================
FILE: src\app\notifications\presentation\components\alert-list\alert-list.component.scss
============================================================

.alert-list {
  display: grid;
  gap: 14px;
}

.alert-card {
  position: relative;
  isolation: isolate;
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 18px;
  align-items: center;
  max-height: 720px;
  padding: 20px;
  border: 1px solid var(--border-color);
  border-radius: 22px;
  background: var(--surface-color);
  transform-origin: center;
  transition:
    border-color 0.58s ease,
    box-shadow 0.72s ease,
    max-height 0.3s ease,
    margin 0.3s ease,
    opacity 0.22s ease,
    padding 0.3s ease,
    transform 0.46s ease;
}

.alert-card > * {
  position: relative;
  z-index: 1;
}

.alert-card.unread {
  border-color: var(--border-strong);
}

.alert-card.entering {
  overflow: hidden;
  animation: alertEnter 0.52s cubic-bezier(0.18, 0.74, 0.28, 1) both;
}

.alert-card.marking-read {
  overflow: hidden;
  border-color: rgba(125, 211, 252, 0.74);
  box-shadow:
    0 0 0 1px rgba(186, 230, 253, 0.16),
    0 0 28px rgba(56, 189, 248, 0.22),
    0 0 52px rgba(34, 197, 94, 0.12),
    0 18px 42px rgba(3, 7, 18, 0.22);
  animation: gemGlow 1.18s ease both;
}

.alert-card.marking-read::before {
  content: '';
  position: absolute;
  inset: 0;
  z-index: 0;
  pointer-events: none;
  background:
    radial-gradient(circle at 12% 0%, rgba(186, 230, 253, 0.2), transparent 34%),
    radial-gradient(circle at 88% 18%, rgba(167, 139, 250, 0.14), transparent 30%),
    linear-gradient(135deg, rgba(34, 197, 94, 0.1), rgba(56, 189, 248, 0.08), transparent 62%);
  animation: gemAura 1.16s ease both;
}

.alert-card.marking-read::after {
  content: '';
  position: absolute;
  top: -72%;
  left: -34%;
  z-index: 2;
  width: 28%;
  height: 230%;
  pointer-events: none;
  background:
    linear-gradient(
      115deg,
      transparent 0%,
      rgba(255, 255, 255, 0.03) 28%,
      rgba(255, 255, 255, 0.46) 48%,
      rgba(186, 230, 253, 0.72) 50%,
      rgba(255, 255, 255, 0.34) 54%,
      transparent 76%
  );
  filter: blur(0.3px);
  transform: translate3d(-145%, -12%, 0) rotate(24deg);
  animation: glassSweep 0.96s cubic-bezier(0.18, 0.74, 0.28, 1) 0.08s both;
  mix-blend-mode: screen;
}

.alert-card.marking-read .level {
  animation: badgeSettle 0.5s ease both;
}

.alert-card.removing {
  max-height: 0;
  margin: 0;
  overflow: hidden;
  padding-top: 0;
  padding-bottom: 0;
  pointer-events: none;
  animation: removeSlide 0.42s cubic-bezier(0.2, 0.7, 0.25, 1) forwards;
}

.alert-card.info {
  border-color: var(--ec-status-info-border);
}

.alert-card.stable {
  border-color: var(--ec-status-stable-border);
  background:
    linear-gradient(180deg, var(--ec-status-stable-bg), transparent),
    var(--surface-color);
}

.alert-card.success {
  border-color: var(--ec-status-success-border);
  background:
    linear-gradient(180deg, var(--ec-status-success-bg), transparent),
    var(--surface-color);
}

.alert-card.warning {
  border-color: var(--ec-status-warning-border);
  background:
    radial-gradient(circle at top left, var(--ec-status-warning-bg), transparent 32%),
    var(--surface-color);
}

.alert-card.critical {
  border-color: var(--ec-status-danger-border);
  background:
    radial-gradient(circle at top left, var(--ec-status-danger-bg), transparent 32%),
    var(--surface-color);
}

.alert-content {
  display: grid;
  gap: 10px;
  min-width: 0;
}

.alert-heading {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 10px;
}

.alert-heading span {
  color: var(--accent-color);
  font-size: 0.76rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.level {
  width: fit-content;
  margin: 0;
  padding: 6px 10px;
  border-radius: 999px;
  color: var(--text-on-action);
  background: var(--ec-action-bg);
  font-size: 0.75rem;
  font-weight: 950;
}

.warning .level {
  color: var(--ec-status-warning-text);
  background: var(--ec-status-warning-bg);
}

.critical .level {
  color: var(--text-on-action);
  background: var(--ec-danger-bg);
}

.success .level {
  color: var(--text-on-action);
  background: var(--ec-status-success-solid);
}

h2 {
  margin: 0;
  color: var(--text-primary);
  font-size: 1.22rem;
  font-weight: 950;
}

p {
  margin: 0;
  color: var(--text-secondary);
  line-height: 1.45;
}

.evidence-label {
  color: var(--text-primary);
  font-weight: 850;
}

.dynamic-context {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  align-items: center;
}

.dynamic-context span,
.dynamic-context small {
  width: fit-content;
  padding: 7px 10px;
  border: 1px solid var(--border-color);
  border-radius: 999px;
  color: var(--ec-status-info-text);
  background: var(--ec-status-info-bg);
  font-size: 0.75rem;
  font-weight: 900;
}

.severity-meter {
  display: grid;
  gap: 7px;
  width: min(100%, 520px);
  margin: 2px 0 4px;
  padding: 11px 12px 10px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  background: var(--ec-surface-subtle);
  box-shadow: var(--ec-inset-highlight);
}

.severity-meter__header,
.severity-meter__legend {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
}

.severity-meter__header span {
  color: var(--text-muted);
  font-size: 0.7rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.severity-meter__header strong {
  color: var(--text-primary);
  font-size: 0.82rem;
  font-weight: 950;
}

.severity-meter__track {
  position: relative;
  height: 10px;
  border-radius: 999px;
  background:
    linear-gradient(
      90deg,
      var(--ec-status-success-solid) 0%,
      var(--ec-status-success-solid) 24%,
      var(--accent-color) 25%,
      var(--accent-color) 49%,
      var(--ec-status-warning-text) 50%,
      var(--ec-status-warning-text) 74%,
      var(--ec-status-danger-solid) 75%,
      var(--ec-status-danger-solid) 100%
    );
  box-shadow:
    var(--ec-inset-highlight),
    var(--ec-shadow-soft);
}

.severity-meter__track i {
  position: absolute;
  top: 50%;
  width: 18px;
  height: 18px;
  border: 2px solid var(--surface-elevated-solid);
  border-radius: 999px;
  background: var(--bg-primary);
  box-shadow:
    0 0 0 4px var(--ec-surface-muted),
    var(--ec-shadow-soft);
  transform: translate(-50%, -50%);
}

.severity-meter__legend {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  color: var(--text-muted);
  font-size: 0.66rem;
  font-weight: 900;
}

.severity-meter__legend span:nth-child(1) {
  color: var(--ec-status-success-text);
}

.severity-meter__legend span:nth-child(2) {
  color: var(--ec-status-info-text);
  text-align: center;
}

.severity-meter__legend span:nth-child(3) {
  color: var(--ec-status-warning-text);
  text-align: center;
}

.severity-meter__legend span:nth-child(4) {
  color: var(--ec-status-danger-text);
  text-align: right;
}

.warning .evidence-label {
  color: var(--ec-status-warning-text);
}

.critical .evidence-label {
  color: var(--ec-status-danger-text);
}

.success .evidence-label {
  color: var(--ec-status-success-text);
}

.stable .evidence-label {
  color: var(--ec-status-stable-text);
}

.detail-select {
  display: grid;
  gap: 6px;
  width: min(100%, 260px);
}

.detail-select span {
  color: var(--text-muted);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.detail-select app-dropdown {
  width: 100%;
}

.detail-panel {
  display: grid;
  gap: 4px;
  width: fit-content;
  max-width: min(100%, 520px);
  padding: 12px 14px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  background: var(--ec-surface-subtle);
  box-shadow: var(--ec-inset-highlight);
}

.detail-panel span {
  color: var(--accent-color);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.detail-panel p {
  color: var(--text-primary);
  font-weight: 850;
}

.recommended-action {
  width: fit-content;
  max-width: 100%;
  padding: 10px 12px;
  border-radius: 14px;
  color: var(--ec-status-info-text);
  background: var(--ec-status-info-bg);
  font-size: 0.9rem;
}

.warning .recommended-action {
  color: var(--ec-status-warning-text);
  background: var(--ec-status-warning-bg);
}

.critical .recommended-action {
  color: var(--ec-status-danger-text);
  background: var(--ec-status-danger-bg);
}

.success .recommended-action {
  color: var(--ec-status-success-text);
  background: var(--ec-status-success-bg);
}

small {
  display: block;
  color: var(--text-muted);
  font-weight: 700;
}

.actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  flex-wrap: wrap;
}

:host ::ng-deep .actions .app-button {
  min-width: 112px;
}

@media (max-width: 980px) {
  .alert-card {
    grid-template-columns: 1fr;
  }

  .actions {
    justify-content: flex-start;
  }
}

@media (max-width: 560px) {
  .detail-panel {
    width: 100%;
  }
}

@keyframes gemGlow {
  0% {
    filter: saturate(1);
    transform: scale(1);
  }

  38% {
    filter: saturate(1.12);
    transform: scale(1.006);
  }

  100% {
    filter: saturate(1);
    transform: scale(1);
  }
}

@keyframes gemAura {
  0% {
    opacity: 0;
  }

  16% {
    opacity: 1;
  }

  68% {
    opacity: 0.72;
  }

  100% {
    opacity: 0;
  }
}

@keyframes glassSweep {
  0% {
    opacity: 0;
    transform: translate3d(-145%, -12%, 0) rotate(24deg);
  }

  12% {
    opacity: 0.92;
  }

  100% {
    opacity: 0;
    transform: translate3d(560%, 10%, 0) rotate(24deg);
  }
}

@keyframes badgeSettle {
  0% {
    transform: translateY(0);
  }

  50% {
    transform: translateY(-2px);
  }

  100% {
    transform: translateY(0);
  }
}

@keyframes removeSlide {
  0% {
    opacity: 1;
    max-height: 720px;
    transform: translateX(0) scale(1);
  }

  45% {
    opacity: 0.66;
    transform: translateX(22px) scale(0.992);
  }

  100% {
    opacity: 0;
    max-height: 0;
    transform: translateX(44px) scale(0.985);
  }
}

@keyframes alertEnter {
  0% {
    max-height: 0;
    opacity: 0;
    padding-top: 0;
    padding-bottom: 0;
    transform: translateY(-16px) scale(0.992);
  }

  62% {
    opacity: 1;
    transform: translateY(2px) scale(1);
  }

  100% {
    max-height: 720px;
    opacity: 1;
    padding-top: 20px;
    padding-bottom: 20px;
    transform: translateY(0) scale(1);
  }
}


============================================================
FILE: src\app\notifications\presentation\components\alert-list\alert-list.component.ts
============================================================

import {
  ChangeDetectorRef,
  Component,
  EventEmitter,
  Input,
  OnChanges,
  Output,
} from '@angular/core';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import {
  AppDropdownComponent,
  DropdownTone,
} from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { SeverityBadgeComponent } from '../../../../shared/presentation/components/severity-badge/severity-badge.component';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { Alert } from '../../../domain/model/alert.entity';

export interface AlertContextItem {
  label: string;
  value: string;
}

export type AlertListItem = Alert & {
  visualLevel?: string;
  severityScore?: number;
  kind?: string;
  sourceLabel?: string;
  evidenceLabel?: string;
  recommendedAction?: string;
  contextItems?: AlertContextItem[];
  explanation?: string;
  threadKey?: string;
  sourceType?: string;
  firstDetectedAt?: string;
  lastUpdatedAt?: string;
};

@Component({
  selector: 'app-alert-list',
  standalone: true,
  imports: [
    TranslateModule,
    AppDropdownComponent,
    SeverityBadgeComponent,
    AppButtonComponent,
    EmptyStateComponent,
  ],
  templateUrl: './alert-list.component.html',
  styleUrls: ['./alert-list.component.scss'],
})
export class AlertListComponent implements OnChanges {
  @Input() alerts: AlertListItem[] = [];

  @Output() markAsRead = new EventEmitter<number>();
  @Output() remove = new EventEmitter<number>();
  @Output() dismissForTenMinutes = new EventEmitter<AlertListItem>();

  private readonly actionDelayMs = 420;
  private readonly enterAnimationMs = 560;
  private readonly removeRecoveryMs = 1800;
  private readonly readGlowMs = 1180;
  private hasRenderedAlerts = false;
  private knownAlertIds = new Set<number>();
  private readonly enteringAlerts = new Set<number>();
  private readonly selectedDetailByAlert = new Map<number, string>();
  private readonly markingAsRead = new Set<number>();
  private readonly removingAlerts = new Set<number>();

  constructor(
    private readonly changeDetectorRef: ChangeDetectorRef,
    private readonly translate: TranslateService
  ) {}

  ngOnChanges(): void {
    const visibleAlertIds = new Set(this.alerts.map((alert) => alert.id));

    if (this.hasRenderedAlerts) {
      for (const alert of this.alerts) {
        if (!this.knownAlertIds.has(alert.id) && !this.removingAlerts.has(alert.id)) {
          this.enteringAlerts.add(alert.id);
          window.setTimeout(() => this.finishEnterAnimation(alert.id), this.enterAnimationMs);
        }
      }
    }

    for (const alert of this.alerts) {
      if (!alert.isUnread && this.markingAsRead.has(alert.id)) {
        window.setTimeout(() => this.finishReadAnimation(alert.id), this.readGlowMs);
      }
    }

    for (const alertId of Array.from(this.removingAlerts)) {
      if (!visibleAlertIds.has(alertId)) {
        this.removingAlerts.delete(alertId);
        this.selectedDetailByAlert.delete(alertId);
      }
    }

    this.knownAlertIds = visibleAlertIds;
    this.hasRenderedAlerts = true;
  }

  selectedDetail(alert: AlertListItem): AlertContextItem | null {
    const items = alert.contextItems ?? [];

    if (!items.length) {
      return null;
    }

    const selectedLabel = this.selectedDetailByAlert.get(alert.id) ?? items[0].label;

    return items.find((item) => item.label === selectedLabel) ?? items[0];
  }

  selectDetailByLabel(alert: AlertListItem, label: string): void {
    this.selectedDetailByAlert.set(alert.id, label);
  }

  requestMarkAsRead(alert: AlertListItem): void {
    if (this.markingAsRead.has(alert.id) || this.removingAlerts.has(alert.id)) {
      return;
    }

    this.markingAsRead.add(alert.id);
    window.setTimeout(() => {
      this.markAsRead.emit(alert.id);
      window.setTimeout(() => this.finishReadAnimation(alert.id), this.readGlowMs);
    }, this.actionDelayMs);
  }

  requestRemove(alert: AlertListItem): void {
    if (this.removingAlerts.has(alert.id)) {
      return;
    }

    this.removingAlerts.add(alert.id);
    window.setTimeout(() => this.remove.emit(alert.id), this.actionDelayMs);

    window.setTimeout(() => {
      if (this.alerts.some((currentAlert) => currentAlert.id === alert.id)) {
        this.removingAlerts.delete(alert.id);
        this.changeDetectorRef.detectChanges();
      }
    }, this.removeRecoveryMs);
  }

  isMarkingAsRead(alert: AlertListItem): boolean {
    return this.markingAsRead.has(alert.id);
  }

  isRemoving(alert: AlertListItem): boolean {
    return this.removingAlerts.has(alert.id);
  }

  isEntering(alert: AlertListItem): boolean {
    return this.enteringAlerts.has(alert.id);
  }

  contextOptions(alert: AlertListItem): DropdownOption[] {
    return (alert.contextItems ?? []).map((item) => ({
      label: item.label,
      value: item.label,
    }));
  }

  detailTone(alert: AlertListItem): DropdownTone {
    const tones: Record<string, DropdownTone> = {
      STABLE: 'success',
      SUCCESS: 'success',
      INFO: 'info',
      WARNING: 'warning',
      CRITICAL: 'critical',
    };

    return tones[this.displayLevel(alert)] ?? 'default';
  }

  displayLevel(alert: AlertListItem): string {
    return alert.visualLevel ?? alert.level;
  }

  displayLevelLabel(alert: AlertListItem): string {
    const labels: Record<string, string> = {
      STABLE: 'alerts.levels.stable',
      SUCCESS: 'alerts.levels.success',
      INFO: 'alerts.levels.info',
      WARNING: 'alerts.levels.warning',
      CRITICAL: 'alerts.levels.critical',
    };

    const key = labels[this.displayLevel(alert)];

    return key ? this.t(key) : this.displayLevel(alert);
  }

  severityScore(alert: AlertListItem): number {
    const score = alert.severityScore ?? this.defaultSeverityScore(alert);

    return Number(Math.max(0, Math.min(100, score)).toFixed(4));
  }

  severityPercentLabel(alert: AlertListItem): string {
    return this.severityScore(alert).toFixed(2);
  }

  severityDescription(alert: AlertListItem): string {
    const labels: Record<string, string> = {
      STABLE: 'alerts.severityDescriptions.stable',
      SUCCESS: 'alerts.severityDescriptions.success',
      INFO: 'alerts.severityDescriptions.info',
      WARNING: 'alerts.severityDescriptions.warning',
      CRITICAL: 'alerts.severityDescriptions.critical',
    };

    return this.t(labels[this.displayLevel(alert)] ?? 'alerts.severityDescriptions.default');
  }

  private finishReadAnimation(alertId: number): void {
    this.markingAsRead.delete(alertId);
    this.changeDetectorRef.detectChanges();
  }

  private finishEnterAnimation(alertId: number): void {
    this.enteringAlerts.delete(alertId);
    this.changeDetectorRef.detectChanges();
  }

  private defaultSeverityScore(alert: AlertListItem): number {
    const scores: Record<string, number> = {
      STABLE: 12,
      SUCCESS: 14,
      INFO: 37,
      WARNING: 63,
      CRITICAL: 88,
    };

    return scores[this.displayLevel(alert)] ?? 37;
  }

  private t(key: string, params?: Record<string, unknown>): string {
    return this.translate.instant(key, params);
  }
}


============================================================
FILE: src\app\notifications\presentation\components\alert-rule-card\alert-rule-card.component.html
============================================================

<article class="rule-card" [class.disabled]="!rule.enabled">
  <div class="rule-main">
    <div class="rule-heading">
      <p class="level">{{ levelLabel() }}</p>
      <span>{{ statusLabelKey | translate }}</span>
    </div>

    <h2>{{ rule.name }}</h2>
    <p>{{ metricLabel() }} {{ conditionLabel() }} {{ thresholdLabel() }}</p>

    <div class="rule-details">
      <span>{{ rule.metric }}</span>
      <span>{{ rule.condition }}</span>
      <span>{{ thresholdLabel() }}</span>
    </div>
  </div>

  <div class="actions">
    <app-button
      [variant]="rule.enabled ? 'secondary' : 'primary'"
      size="sm"
      (pressed)="toggle.emit(rule)"
    >
      @if (rule.enabled) {
        {{ 'alertRules.disable' | translate }}
      } @else {
        {{ 'alertRules.enable' | translate }}
      }
    </app-button>

    <app-button
      variant="danger"
      size="sm"
      (pressed)="remove.emit(rule.id)"
    >
      {{ 'common.delete' | translate }}
    </app-button>
  </div>
</article>


============================================================
FILE: src\app\notifications\presentation\components\alert-rule-card\alert-rule-card.component.scss
============================================================

.rule-card {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 16px;
  align-items: center;
  padding: 18px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background:
    radial-gradient(circle at top left, var(--ec-accent-soft), transparent 34%),
    var(--surface-card);
  transition:
    border-color 0.18s ease,
    opacity 0.18s ease,
    transform 0.18s ease;
}

.rule-card:hover {
  border-color: var(--border-strong);
  transform: translateY(-1px);
}

.rule-card.disabled {
  opacity: 0.58;
}

.rule-main {
  display: grid;
  gap: 8px;
}

.rule-heading,
.actions,
.rule-details {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 10px;
}

.rule-heading span {
  color: var(--accent-color);
  font-size: 0.75rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.level {
  width: fit-content;
  margin: 0;
  padding: 6px 10px;
  border-radius: 999px;
  color: #422006;
  background: #facc15;
  font-size: 0.75rem;
  font-weight: 950;
}

h2 {
  margin: 0;
  color: var(--text-primary);
  font-size: 1.2rem;
}

p {
  margin: 0;
  color: var(--text-secondary);
  font-weight: 850;
}

.rule-details span {
  padding: 7px 10px;
  border-radius: 999px;
  color: var(--ec-status-info-text);
  background: var(--ec-status-info-bg);
  font-size: 0.76rem;
  font-weight: 900;
}

:host ::ng-deep .actions .app-button {
  min-width: 112px;
}

@media (max-width: 720px) {
  .rule-card {
    grid-template-columns: 1fr;
  }

  .actions {
    justify-content: flex-start;
  }
}


============================================================
FILE: src\app\notifications\presentation\components\alert-rule-card\alert-rule-card.component.ts
============================================================

import { Component, EventEmitter, Input, Output } from '@angular/core';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { AlertRule } from '../../../domain/model/alert-rule.entity';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';

@Component({
  selector: 'app-alert-rule-card',
  standalone: true,
  imports: [TranslateModule, AppButtonComponent],
  templateUrl: './alert-rule-card.component.html',
  styleUrls: ['./alert-rule-card.component.scss'],
})
export class AlertRuleCardComponent {
  @Input({ required: true }) rule!: AlertRule;

  @Output() toggle = new EventEmitter<AlertRule>();
  @Output() remove = new EventEmitter<number>();

  constructor(private readonly translate: TranslateService) {}

  metricLabel(): string {
    const labels: Record<string, string> = {
      WATTS: 'alertRules.metrics.watts',
      HIGH_READING_COUNT: 'alertRules.metrics.highReadingCount',
      AVERAGE_WATTS: 'alertRules.metrics.averageWatts',
    };

    return this.t(labels[this.rule.metric] ?? this.rule.metric);
  }

  conditionLabel(): string {
    const labels: Record<string, string> = {
      GREATER_THAN: 'alertRules.conditions.greaterThanLower',
      GREATER_OR_EQUAL_THAN: 'alertRules.conditions.greaterOrEqualLower',
    };

    return this.t(labels[this.rule.condition] ?? this.rule.condition);
  }

  levelLabel(): string {
    const labels: Record<string, string> = {
      STABLE: 'alerts.levels.stable',
      SUCCESS: 'alerts.levels.success',
      INFO: 'alerts.levels.info',
      WARNING: 'alerts.levels.warning',
      CRITICAL: 'alerts.levels.critical',
    };

    return this.t(labels[this.rule.level] ?? this.rule.level);
  }

  thresholdLabel(): string {
    return this.rule.metric === 'HIGH_READING_COUNT'
      ? this.t('alertRules.readingsValue', { count: this.rule.threshold })
      : `${this.rule.threshold} W`;
  }

  get statusLabelKey(): string {
    return this.rule.enabled ? 'routines.status.active' : 'routines.status.paused';
  }

  private t(key: string, params?: Record<string, unknown>): string {
    return this.translate.instant(key, params);
  }
}


============================================================
FILE: src\app\notifications\presentation\components\notification-preference-form\notification-preference-form.component.html
============================================================

<form class="preference-form" (ngSubmit)="onSubmit()">
  <section class="preference-block preference-block--hero">
    <div class="block-heading">
      <span>{{ 'notificationPreferences.sensitivity' | translate }}</span>
      <h3>{{ 'notificationPreferences.weightedClassification' | translate }}</h3>
      <p>{{ 'notificationPreferences.weightedClassificationDescription' | translate }}</p>
    </div>

    <div class="severity-workbench">
      <div class="severity-rail-card">
        <div
          class="severity-rail"
          [style.--stable-stop]="stableMax + '%'"
          [style.--neutral-stop]="neutralMax + '%'"
          [style.--warning-stop]="warningMax + '%'"
        >
          <span class="rail-marker rail-marker--stable" [style.bottom.%]="stableMax"></span>
          <span class="rail-marker rail-marker--neutral" [style.bottom.%]="neutralMax"></span>
          <span class="rail-marker rail-marker--warning" [style.bottom.%]="warningMax"></span>
        </div>

        <div class="severity-readout">
          @for (band of visualSeverityBands; track band.level) {
            <article [class]="'band-card ' + levelClass(band.level)">
              <span>{{ bandLabel(band) }}</span>
              <strong>{{ bandRangeLabel(band) }}</strong>
            </article>
          }
        </div>
      </div>

      <div class="threshold-controls">
        <label>
          <span>{{ 'notificationPreferences.warningCeiling' | translate }}</span>
          <strong>{{ warningMax }}%</strong>
          <input
            type="range"
            min="15"
            max="99"
            [value]="warningMax"
            (input)="updateWarningMax($any($event.target).value)"
          />
        </label>

        <label>
          <span>{{ 'notificationPreferences.neutralCeiling' | translate }}</span>
          <strong>{{ neutralMax }}%</strong>
          <input
            type="range"
            min="10"
            max="96"
            [value]="neutralMax"
            (input)="updateNeutralMax($any($event.target).value)"
          />
        </label>

        <label>
          <span>{{ 'notificationPreferences.stableCeiling' | translate }}</span>
          <strong>{{ stableMax }}%</strong>
          <input
            type="range"
            min="5"
            max="92"
            [value]="stableMax"
            (input)="updateStableMax($any($event.target).value)"
          />
        </label>
      </div>
    </div>
  </section>

  <section class="preference-block">
    <div class="block-heading">
      <span>{{ 'notificationPreferences.preview' | translate }}</span>
      <h3>{{ 'notificationPreferences.sensitiveResult' | translate }}</h3>
      <p>{{ 'notificationPreferences.previewDescription' | translate }}</p>
    </div>

    <div class="preview-grid">
      <label class="preview-field">
        <span>{{ 'notificationPreferences.context' | translate }}</span>
        <app-dropdown
          [options]="previewSourceOptions"
          [value]="previewSource"
          (valueChange)="updatePreviewSource($event)"
        />
      </label>

      <label class="preview-slider">
        <span>{{ 'notificationPreferences.baseSignal' | translate }}</span>
        <strong>{{ previewScore }}%</strong>
        <input
          type="range"
          min="0"
          max="100"
          [value]="previewScore"
          (input)="updatePreviewScore($any($event.target).value)"
        />
      </label>

      <article [class]="'preview-result ' + levelClass(previewLevel)">
        <span>{{ 'notificationPreferences.finalCategory' | translate }}</span>
        <strong>{{ previewLabel }}</strong>
        <p>{{ 'notificationPreferences.weightedPercent' | translate: { value: previewWeightedScore } }}</p>
      </article>
    </div>
  </section>

  <section class="preference-block">
    <div class="block-heading">
      <span>{{ 'alerts.levels.success' | translate }}</span>
      <h3>{{ 'notificationPreferences.positiveEvents' | translate }}</h3>
      <p>{{ 'notificationPreferences.positiveEventsDescription' | translate }}</p>
    </div>

    <div class="toggle-grid toggle-grid--single">
      <app-setting-toggle
        [title]="'notificationPreferences.showSuccess' | translate"
        [description]="'notificationPreferences.showSuccessDescription' | translate"
        [checked]="allowedLevels.has('SUCCESS')"
        (checkedChange)="toggleLevel('SUCCESS')"
      />
    </div>
  </section>

  <section class="preference-block">
    <div class="block-heading">
      <span>{{ 'notificationPreferences.visibleCategories' | translate }}</span>
      <h3>{{ 'notificationPreferences.allowedLevels' | translate }}</h3>
      <p>{{ 'notificationPreferences.allowedLevelsDescription' | translate }}</p>
    </div>

    <div class="toggle-grid toggle-grid--single">
      <app-setting-toggle
        [title]="'notificationPreferences.criticalOnlyLabel' | translate"
        [description]="'notificationPreferences.criticalOnlyDescription' | translate"
        [(checked)]="criticalOnly"
      />
    </div>

    <label class="level-field">
      <span>{{ 'notificationPreferences.minimumLevel' | translate }}</span>
      <app-dropdown
        [options]="levelOptions"
        [value]="minimumLevel"
        (valueChange)="updateMinimumLevel($event)"
      />
    </label>

    <div class="chip-grid">
      @for (level of riskLevels; track level) {
        <app-button
          [variant]="chipVariant(allowedLevels.has(level))"
          size="sm"
          (pressed)="toggleLevel(level)"
        >
          {{ levelLabel(level) }}
        </app-button>
      }
    </div>
  </section>

  <section class="preference-block">
    <div class="block-heading">
      <span>{{ 'notificationPreferences.weighting' | translate }}</span>
      <h3>{{ 'notificationPreferences.contextParameters' | translate }}</h3>
      <p>{{ 'notificationPreferences.contextParametersDescription' | translate }}</p>
    </div>

    <div class="weight-list">
      @for (signal of signalWeights; track signal.source) {
        <article class="weight-row" [class.weight-row--disabled]="!sourceEnabled(signal.source)">
          <button
            type="button"
            class="weight-row__toggle"
            [class.weight-row__toggle--active]="sourceEnabled(signal.source)"
            (click)="toggleSource(signal.source)"
          >
            <span>{{ signalLabel(signal) }}</span>
            <small>{{ signalDescription(signal) }}</small>
          </button>

          <label>
            <strong>{{ signal.weight }}%</strong>
            <input
              type="range"
              min="0"
              max="100"
              [value]="signal.weight"
              (input)="updateSignalWeight(signal, $any($event.target).value)"
            />
          </label>
        </article>
      }
    </div>
  </section>

  <section class="preference-block">
    <div class="block-heading">
      <span>{{ 'notificationPreferences.output' | translate }}</span>
      <h3>{{ 'notificationPreferences.channelsAndNoise' | translate }}</h3>
      <p>{{ 'notificationPreferences.channelsAndNoiseDescription' | translate }}</p>
    </div>

    <div class="toggle-grid">
      <app-setting-toggle [title]="'nav.inbox' | translate" [description]="'notificationPreferences.toggles.inAppDescription' | translate" [(checked)]="inAppEnabled" />
      <app-setting-toggle [title]="'notificationPreferences.toggles.toastTitle' | translate" [description]="'notificationPreferences.toggles.toastDescription' | translate" [(checked)]="toastEnabled" />
      <app-setting-toggle [title]="'notificationPreferences.toggles.dashboardTitle' | translate" [description]="'notificationPreferences.toggles.dashboardDescription' | translate" [(checked)]="dashboardEnabled" />
      <app-setting-toggle [title]="'notificationPreferences.toggles.emailTitle' | translate" [description]="'notificationPreferences.toggles.emailDescription' | translate" [(checked)]="emailEnabled" />
      <app-setting-toggle [title]="'notificationPreferences.toggles.pushTitle' | translate" [description]="'notificationPreferences.toggles.pushDescription' | translate" [(checked)]="pushEnabled" />
      <app-setting-toggle [title]="'notificationPreferences.toggles.groupSimilarTitle' | translate" [description]="'notificationPreferences.toggles.groupSimilarDescription' | translate" [(checked)]="groupSimilarAlerts" />
    </div>

    <div class="control-grid">
      <label class="level-field">
        <span>{{ 'notificationPreferences.deliveryMode' | translate }}</span>
        <app-dropdown
          [options]="deliveryModeOptions"
          [value]="defaultDeliveryMode"
          (valueChange)="updateDeliveryMode($event)"
        />
      </label>

      <label class="number-field">
        <span>{{ 'notificationPreferences.cooldown' | translate }}</span>
        <input name="cooldownMinutes" type="number" min="0" max="1440" [(ngModel)]="cooldownMinutes" />
        <small>{{ 'notificationPreferences.minutes' | translate }}</small>
      </label>

      <label class="number-field">
        <span>{{ 'notificationPreferences.maxPerHour' | translate }}</span>
        <input name="maxAlertsPerHour" type="number" min="1" max="200" [(ngModel)]="maxAlertsPerHour" />
        <small>{{ 'notificationPreferences.alertsUnit' | translate }}</small>
      </label>
    </div>
  </section>

  <section class="preference-block">
    <div class="block-heading">
      <span>{{ 'notificationPreferences.silences' | translate }}</span>
      <h3>{{ 'notificationPreferences.scheduleAndExceptions' | translate }}</h3>
      <p>{{ 'notificationPreferences.scheduleAndExceptionsDescription' | translate }}</p>
    </div>

    <div class="toggle-grid">
      <app-setting-toggle [title]="'notificationPreferences.toggles.quietHoursTitle' | translate" [description]="'notificationPreferences.toggles.quietHoursDescription' | translate" [(checked)]="quietHoursEnabled" />
      <app-setting-toggle [title]="'notificationPreferences.toggles.criticalBreaksTitle' | translate" [description]="'notificationPreferences.toggles.criticalBreaksDescription' | translate" [(checked)]="criticalBreaksQuietHours" />
      <app-setting-toggle [title]="'notificationPreferences.toggles.remindersTitle' | translate" [description]="'notificationPreferences.toggles.remindersDescription' | translate" [(checked)]="remindersEnabled" />
      <app-setting-toggle [title]="'notificationPreferences.toggles.nightRoutinesTitle' | translate" [description]="'notificationPreferences.toggles.nightRoutinesDescription' | translate" [(checked)]="routineNightSilence" />
      <app-setting-toggle [title]="'notificationPreferences.toggles.goalDeadlineTitle' | translate" [description]="'notificationPreferences.toggles.goalDeadlineDescription' | translate" [(checked)]="goalDeadlineAlerts" />
      <app-setting-toggle [title]="'nav.maintenanceTickets' | translate" [description]="'notificationPreferences.toggles.maintenanceDescription' | translate" [(checked)]="maintenanceDeviceAlerts" />
      <app-setting-toggle [title]="'notificationPreferences.toggles.recommendationsTitle' | translate" [description]="'notificationPreferences.toggles.recommendationsDescription' | translate" [(checked)]="systemRecommendations" />
      <app-setting-toggle [title]="'notificationPreferences.toggles.dailySummaryTitle' | translate" [description]="'notificationPreferences.toggles.dailySummaryDescription' | translate" [(checked)]="dailySummaryEnabled" />
      <app-setting-toggle [title]="'notificationPreferences.toggles.weeklySummaryTitle' | translate" [description]="'notificationPreferences.toggles.weeklySummaryDescription' | translate" [(checked)]="weeklySummaryEnabled" />
    </div>

    <div class="control-grid control-grid--compact">
      <app-time-picker [label]="'notificationPreferences.quietStart' | translate" [(value)]="quietHoursStart" />

      <app-time-picker [label]="'notificationPreferences.quietEnd' | translate" [(value)]="quietHoursEnd" />
    </div>
  </section>

  <app-button type="submit" variant="create" size="lg" [block]="true">
    {{ 'notificationPreferences.save' | translate }}
  </app-button>
</form>


============================================================
FILE: src\app\notifications\presentation\components\notification-preference-form\notification-preference-form.component.scss
============================================================

.preference-form {
  display: grid;
  gap: 18px;
}

.preference-block {
  display: grid;
  gap: 16px;
  padding: 18px;
  border: 1px solid var(--border-color);
  border-radius: 22px;
  background:
    var(--ec-card-highlight),
    var(--surface-card);
  box-shadow:
    var(--ec-inset-highlight),
    var(--ec-shadow-soft);
}

.preference-block--hero {
  gap: 20px;
}

.block-heading {
  display: grid;
  gap: 6px;
}

.block-heading span,
.level-field > span,
.number-field > span,
.preview-field > span,
.preview-slider > span,
.threshold-controls span,
.summary-label {
  color: var(--accent-color);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.block-heading h3 {
  margin: 0;
  color: var(--text-primary);
  font-size: 1.2rem;
  line-height: 1.1;
}

.block-heading p,
.number-field small,
.weight-row small,
.preview-result p {
  margin: 0;
  color: var(--text-secondary);
  font-weight: 780;
  line-height: 1.45;
}

.severity-workbench {
  display: grid;
  grid-template-columns: minmax(260px, 0.85fr) minmax(0, 1.15fr);
  gap: 16px;
  align-items: stretch;
}

.severity-rail-card {
  display: grid;
  grid-template-columns: 54px minmax(0, 1fr);
  gap: 14px;
  min-height: 360px;
  padding: 14px;
  border: 1px solid var(--border-color);
  border-radius: 20px;
  background: var(--ec-surface-subtle);
}

.severity-rail {
  --stable-stop: 25%;
  --neutral-stop: 55%;
  --warning-stop: 80%;

  position: relative;
  width: 34px;
  min-height: 100%;
  border: 1px solid var(--border-color);
  border-radius: 999px;
  background:
    linear-gradient(
      to top,
      var(--ec-status-stable-bg) 0 var(--stable-stop),
      var(--ec-status-info-bg) var(--stable-stop) var(--neutral-stop),
      var(--ec-status-warning-bg) var(--neutral-stop) var(--warning-stop),
      var(--ec-status-danger-bg) var(--warning-stop) 100%
    ),
    var(--surface-card);
  box-shadow: var(--ec-inset-highlight);
}

.rail-marker {
  position: absolute;
  left: 50%;
  width: 46px;
  height: 12px;
  border: 2px solid var(--surface-elevated-solid);
  border-radius: 999px;
  background: var(--accent-color);
  box-shadow: 0 8px 18px color-mix(in srgb, var(--accent-color) 28%, transparent);
  transform: translate(-50%, 50%);
}

.rail-marker--stable {
  background: var(--ec-status-stable-text);
}

.rail-marker--neutral {
  background: var(--ec-status-info-text);
}

.rail-marker--warning {
  background: var(--ec-status-warning-text);
}

.severity-readout,
.threshold-controls,
.weight-list,
.record-stack {
  display: grid;
  gap: 10px;
}

.band-card {
  display: grid;
  gap: 4px;
  padding: 12px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  background: var(--ec-surface-subtle);
}

.band-card span,
.preview-result span {
  font-size: 0.78rem;
  font-weight: 950;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.band-card strong,
.preview-result strong,
.threshold-controls strong,
.weight-row strong {
  color: var(--text-primary);
  font-weight: 950;
}

.level-stable span {
  color: var(--ec-status-stable-text);
}

.level-info span {
  color: var(--ec-status-info-text);
}

.level-warning span {
  color: var(--ec-status-warning-text);
}

.level-critical span {
  color: var(--ec-status-danger-text);
}

.level-success span {
  color: var(--ec-status-success-text);
}

.threshold-controls label,
.preview-slider,
.weight-row label {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 8px 12px;
  align-items: center;
  padding: 14px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background: var(--ec-surface-subtle);
}

.threshold-controls input,
.preview-slider input,
.weight-row input {
  grid-column: 1 / -1;
  width: 100%;
  accent-color: var(--accent-color);
}

.preview-grid {
  display: grid;
  grid-template-columns: minmax(220px, 0.8fr) minmax(220px, 1fr) minmax(180px, 0.7fr);
  gap: 14px;
  align-items: stretch;
}

.preview-field,
.level-field,
.number-field {
  display: grid;
  gap: 8px;
  min-width: 0;
}

.preview-result {
  display: grid;
  gap: 7px;
  align-content: center;
  padding: 16px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background: var(--ec-surface-subtle);
}

.preview-result strong {
  font-size: 1.45rem;
}

.toggle-grid,
.control-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 14px;
}

.toggle-grid--single {
  grid-template-columns: minmax(0, 420px);
}

.control-grid--compact {
  grid-template-columns: repeat(2, minmax(0, 220px));
}

.chip-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
}

:host ::ng-deep .chip-grid .app-button {
  min-width: 112px;
}

.weight-list {
  display: grid;
  gap: 12px;
}

.weight-row {
  display: grid;
  grid-template-columns: minmax(220px, 0.8fr) minmax(220px, 1fr);
  gap: 12px;
  align-items: stretch;
}

.weight-row__toggle {
  display: grid;
  gap: 5px;
  align-content: center;
  width: 100%;
  min-height: 82px;
  padding: 14px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  color: var(--text-primary);
  background: var(--ec-surface-subtle);
  cursor: pointer;
  text-align: left;
}

.weight-row__toggle span {
  font-weight: 950;
}

.weight-row__toggle--active {
  border-color: var(--border-strong);
  background:
    radial-gradient(circle at top left, var(--ec-accent-soft), transparent 46%),
    var(--ec-surface-muted);
}

.weight-row--disabled {
  opacity: 0.72;
}

.number-field input {
  width: 100%;
  min-width: 0;
  border: 1px solid var(--border-color);
  border-radius: 14px;
  background: var(--field-bg);
  color: var(--text-primary);
  font: inherit;
  font-weight: 850;
  padding: 12px;
  outline: none;
}

.number-field input:focus {
  border-color: var(--border-strong);
  box-shadow: var(--ec-focus-ring);
}

@media (max-width: 1040px) {
  .severity-workbench,
  .preview-grid,
  .weight-row {
    grid-template-columns: 1fr;
  }

  .severity-rail-card {
    min-height: 320px;
  }
}

@media (max-width: 920px) {
  .toggle-grid,
  .control-grid,
  .control-grid--compact {
    grid-template-columns: 1fr;
  }
}


============================================================
FILE: src\app\notifications\presentation\components\notification-preference-form\notification-preference-form.component.ts
============================================================

import { CommonModule } from '@angular/common';
import { Component, EventEmitter, Input, OnChanges, Output, SimpleChanges } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { AlertLevel, AlertSourceType } from '../../../domain/model/alert.entity';
import {
  NotificationDeliveryMode,
  NotificationPreference,
} from '../../../domain/model/notification-preference.entity';
import { UpdateNotificationPreferenceCommand } from '../../../application/commands/update-notification-preference.command';
import { SettingToggleComponent } from '../../../../shared/presentation/components/setting-toggle/setting-toggle.component';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import {
  AppButtonComponent,
  AppButtonVariant,
} from '../../../../shared/presentation/components/app-button/app-button.component';
import { AppTimePickerComponent } from '../../../../shared/presentation/components/app-time-picker/app-time-picker.component';
import {
  buildNotificationSeverityBands,
  DEFAULT_NOTIFICATION_CLASSIFICATION_POLICY,
  levelForNotificationScore,
  NotificationClassificationPolicy,
  NotificationSeverityBand,
} from '../../../domain/model/notification-classification-policy.model';

interface SignalWeight {
  source: AlertSourceType;
  label: string;
  labelKey: string;
  description: string;
  descriptionKey: string;
  weight: number;
}

@Component({
  selector: 'app-notification-preference-form',
  standalone: true,
  imports: [
    CommonModule,
    FormsModule,
    TranslateModule,
    SettingToggleComponent,
    AppDropdownComponent,
    AppButtonComponent,
    AppTimePickerComponent,
  ],
  templateUrl: './notification-preference-form.component.html',
  styleUrls: ['./notification-preference-form.component.scss'],
})
export class NotificationPreferenceFormComponent implements OnChanges {
  @Input() preference: NotificationPreference | null = null;
  @Input() classificationPolicy: NotificationClassificationPolicy =
    DEFAULT_NOTIFICATION_CLASSIFICATION_POLICY;

  @Output() saved = new EventEmitter<UpdateNotificationPreferenceCommand>();
  @Output() classificationPolicyChanged =
    new EventEmitter<NotificationClassificationPolicy>();

  stableMax = 25;
  neutralMax = 55;
  warningMax = 80;
  previewScore = 62;
  previewSource: AlertSourceType = 'GOAL';

  emailEnabled = true;
  pushEnabled = true;
  inAppEnabled = true;
  toastEnabled = true;
  dashboardEnabled = true;
  criticalOnly = false;
  minimumLevel: AlertLevel = 'INFO';
  allowedLevels = new Set<AlertLevel>(['STABLE', 'INFO', 'WARNING', 'CRITICAL', 'SUCCESS']);
  allowedSourceTypes = new Set<AlertSourceType>([
    'DEVICE',
    'GROUP',
    'ROOM',
    'WORKPLACE',
    'ROUTINE',
    'GOAL',
    'REPORT',
    'RULE',
    'MODE',
    'SYSTEM',
  ]);
  quietHoursEnabled = false;
  quietHoursStart = '22:00';
  quietHoursEnd = '07:00';
  criticalBreaksQuietHours = true;
  groupSimilarAlerts = true;
  remindersEnabled = true;
  cooldownMinutes = 10;
  maxAlertsPerHour = 12;
  routineNightSilence = true;
  goalDeadlineAlerts = true;
  maintenanceDeviceAlerts = false;
  systemRecommendations = true;
  dailySummaryEnabled = true;
  weeklySummaryEnabled = false;
  defaultDeliveryMode: NotificationDeliveryMode = 'BANNER';

  readonly levels: AlertLevel[] = ['SUCCESS', 'STABLE', 'INFO', 'WARNING', 'CRITICAL'];
  readonly riskLevels: AlertLevel[] = ['STABLE', 'INFO', 'WARNING', 'CRITICAL'];
  readonly sources: AlertSourceType[] = [
    'DEVICE',
    'GROUP',
    'ROOM',
    'WORKPLACE',
    'ROUTINE',
    'GOAL',
    'REPORT',
    'RULE',
    'MODE',
    'SYSTEM',
  ];
  readonly levelOptions: DropdownOption[] = [
    { label: '', labelKey: 'alerts.levels.stable', value: 'STABLE', descriptionKey: 'notificationPreferences.levelDescriptions.stable' },
    { label: '', labelKey: 'alerts.levels.info', value: 'INFO', descriptionKey: 'notificationPreferences.levelDescriptions.info' },
    { label: '', labelKey: 'alerts.levels.warning', value: 'WARNING', descriptionKey: 'notificationPreferences.levelDescriptions.warning' },
    { label: '', labelKey: 'alerts.levels.critical', value: 'CRITICAL', descriptionKey: 'notificationPreferences.levelDescriptions.critical' },
  ];
  readonly deliveryModeOptions: DropdownOption[] = [
    { label: '', labelKey: 'notificationPreferences.deliveryModes.banner', value: 'BANNER', descriptionKey: 'notificationPreferences.deliveryModeDescriptions.banner' },
    { label: '', labelKey: 'notificationPreferences.deliveryModes.quiet', value: 'QUIET', descriptionKey: 'notificationPreferences.deliveryModeDescriptions.quiet' },
    { label: '', labelKey: 'notificationPreferences.deliveryModes.inboxOnly', value: 'INBOX_ONLY', descriptionKey: 'notificationPreferences.deliveryModeDescriptions.inboxOnly' },
    { label: '', labelKey: 'notificationPreferences.deliveryModes.muted', value: 'MUTED', descriptionKey: 'notificationPreferences.deliveryModeDescriptions.muted' },
  ];
  readonly signalWeights: SignalWeight[] = [
    {
      source: 'DEVICE',
      label: '',
      labelKey: 'notificationPreferences.sources.device',
      description: '',
      descriptionKey: 'notificationPreferences.sourceDescriptions.device',
      weight: 54,
    },
    {
      source: 'GROUP',
      label: '',
      labelKey: 'notificationPreferences.sources.group',
      description: '',
      descriptionKey: 'notificationPreferences.sourceDescriptions.group',
      weight: 46,
    },
    {
      source: 'ROUTINE',
      label: '',
      labelKey: 'notificationPreferences.sources.routine',
      description: '',
      descriptionKey: 'notificationPreferences.sourceDescriptions.routine',
      weight: 62,
    },
    {
      source: 'GOAL',
      label: '',
      labelKey: 'notificationPreferences.sources.goal',
      description: '',
      descriptionKey: 'notificationPreferences.sourceDescriptions.goal',
      weight: 68,
    },
    {
      source: 'RULE',
      label: '',
      labelKey: 'notificationPreferences.sources.rule',
      description: '',
      descriptionKey: 'notificationPreferences.sourceDescriptions.rule',
      weight: 72,
    },
    {
      source: 'MODE',
      label: '',
      labelKey: 'notificationPreferences.sources.mode',
      description: '',
      descriptionKey: 'notificationPreferences.sourceDescriptions.mode',
      weight: 50,
    },
    {
      source: 'SYSTEM',
      label: '',
      labelKey: 'notificationPreferences.sources.system',
      description: '',
      descriptionKey: 'notificationPreferences.sourceDescriptions.system',
      weight: 36,
    },
  ];

  constructor(private readonly translate: TranslateService) {}

  get severityBands(): NotificationSeverityBand[] {
    return buildNotificationSeverityBands(this.currentClassificationPolicy());
  }

  get visualSeverityBands(): NotificationSeverityBand[] {
    return [...this.severityBands].reverse();
  }

  get previewWeightedScore(): number {
    const signal = this.signalWeights.find((item) => item.source === this.previewSource);
    const weight = signal?.weight ?? 50;
    const enabledAdjustment = this.allowedSourceTypes.has(this.previewSource) ? 0 : -18;

    return this.clamp(Math.round(this.previewScore + (weight - 50) / 2 + enabledAdjustment), 0, 100);
  }

  get previewLevel(): AlertLevel {
    return this.levelForScore(this.previewWeightedScore);
  }

  get previewLabel(): string {
    return this.levelLabel(this.previewLevel);
  }

  get previewSourceOptions(): DropdownOption[] {
    return this.signalWeights.map((signal) => ({
      label: this.signalLabel(signal),
      value: signal.source,
      description: this.t('notificationPreferences.weightDescription', { weight: signal.weight }),
    }));
  }

  updateMinimumLevel(value: string): void {
    this.minimumLevel = value as AlertLevel;
  }

  updateDeliveryMode(value: string): void {
    this.defaultDeliveryMode = value as NotificationDeliveryMode;
  }

  toggleLevel(level: AlertLevel): void {
    this.toggleSetValue(this.allowedLevels, level);
  }

  toggleSource(source: AlertSourceType): void {
    this.toggleSetValue(this.allowedSourceTypes, source);
  }

  updateStableMax(value: string | number): void {
    this.stableMax = this.clamp(this.toNumber(value), 5, 92);
    this.pushThresholdsForward();
    this.emitClassificationPolicy();
  }

  updateNeutralMax(value: string | number): void {
    this.neutralMax = this.clamp(this.toNumber(value), this.stableMax + 5, 96);
    this.pushThresholdsForward();
    this.emitClassificationPolicy();
  }

  updateWarningMax(value: string | number): void {
    this.warningMax = this.clamp(this.toNumber(value), this.neutralMax + 5, 99);
    this.emitClassificationPolicy();
  }

  updatePreviewScore(value: string | number): void {
    this.previewScore = this.clamp(this.toNumber(value), 0, 100);
  }

  updatePreviewSource(value: string): void {
    this.previewSource = value as AlertSourceType;
  }

  updateSignalWeight(signal: SignalWeight, value: string | number): void {
    signal.weight = this.clamp(this.toNumber(value), 0, 100);
  }

  sourceEnabled(source: AlertSourceType): boolean {
    return this.allowedSourceTypes.has(source);
  }

  bandRangeLabel(band: NotificationSeverityBand): string {
    return `${band.from}% - ${band.to}%`;
  }

  levelLabel(level: AlertLevel): string {
    const labels: Record<AlertLevel, string> = {
      STABLE: 'alerts.levels.stable',
      SUCCESS: 'alerts.levels.success',
      INFO: 'alerts.levels.info',
      WARNING: 'alerts.levels.warning',
      CRITICAL: 'alerts.levels.critical',
    };

    return this.t(labels[level]);
  }

  sourceLabel(source: AlertSourceType): string {
    const labels: Record<AlertSourceType, string> = {
      DEVICE: 'notificationPreferences.sources.device',
      GROUP: 'notificationPreferences.sources.group',
      ROOM: 'notificationPreferences.sources.room',
      WORKPLACE: 'notificationPreferences.sources.workplace',
      ROUTINE: 'notificationPreferences.sources.routine',
      GOAL: 'notificationPreferences.sources.goal',
      REPORT: 'notificationPreferences.sources.report',
      RULE: 'notificationPreferences.sources.rule',
      MODE: 'notificationPreferences.sources.mode',
      SYSTEM: 'notificationPreferences.sources.system',
    };

    return this.t(labels[source]);
  }

  signalLabel(signal: SignalWeight): string {
    return this.t(signal.labelKey);
  }

  signalDescription(signal: SignalWeight): string {
    return this.t(signal.descriptionKey);
  }

  bandLabel(band: NotificationSeverityBand): string {
    return this.levelLabel(band.level);
  }

  chipVariant(active: boolean): AppButtonVariant {
    return active ? 'primary' : 'secondary';
  }

  levelClass(level: AlertLevel): string {
    return `level-${level.toLowerCase()}`;
  }

  ngOnChanges(changes: SimpleChanges): void {
    if (changes['classificationPolicy']) {
      this.stableMax = this.classificationPolicy.stableMax;
      this.neutralMax = this.classificationPolicy.neutralMax;
      this.warningMax = this.classificationPolicy.warningMax;
    }

    if (!this.preference) return;

    this.emailEnabled = this.preference.emailEnabled;
    this.pushEnabled = this.preference.pushEnabled;
    this.inAppEnabled = this.preference.inAppEnabled;
    this.toastEnabled = this.preference.toastEnabled;
    this.dashboardEnabled = this.preference.dashboardEnabled;
    this.criticalOnly = this.preference.criticalOnly;
    this.minimumLevel = this.preference.minimumLevel === 'SUCCESS'
      ? 'INFO'
      : this.preference.minimumLevel;
    this.allowedLevels = new Set(this.preference.allowedLevels);
    this.allowedSourceTypes = new Set(this.preference.allowedSourceTypes);
    this.quietHoursEnabled = this.preference.quietHoursEnabled;
    this.quietHoursStart = this.preference.quietHoursStart;
    this.quietHoursEnd = this.preference.quietHoursEnd;
    this.criticalBreaksQuietHours = this.preference.criticalBreaksQuietHours;
    this.groupSimilarAlerts = this.preference.groupSimilarAlerts;
    this.remindersEnabled = this.preference.remindersEnabled;
    this.cooldownMinutes = this.preference.cooldownMinutes;
    this.maxAlertsPerHour = this.preference.maxAlertsPerHour;
    this.routineNightSilence = this.preference.routineNightSilence;
    this.goalDeadlineAlerts = this.preference.goalDeadlineAlerts;
    this.maintenanceDeviceAlerts = this.preference.maintenanceDeviceAlerts;
    this.systemRecommendations = this.preference.systemRecommendations;
    this.dailySummaryEnabled = this.preference.dailySummaryEnabled;
    this.weeklySummaryEnabled = this.preference.weeklySummaryEnabled;
    this.defaultDeliveryMode = this.preference.defaultDeliveryMode;
  }

  onSubmit(): void {
    this.saved.emit({
      emailEnabled: this.emailEnabled,
      pushEnabled: this.pushEnabled,
      inAppEnabled: this.inAppEnabled,
      toastEnabled: this.toastEnabled,
      dashboardEnabled: this.dashboardEnabled,
      criticalOnly: this.criticalOnly,
      minimumLevel: this.minimumLevel,
      scopeType: 'USER',
      allowedLevels: Array.from(this.allowedLevels).join(','),
      allowedSourceTypes: Array.from(this.allowedSourceTypes).join(','),
      quietHoursEnabled: this.quietHoursEnabled,
      quietHoursStart: this.quietHoursStart,
      quietHoursEnd: this.quietHoursEnd,
      criticalBreaksQuietHours: this.criticalBreaksQuietHours,
      groupSimilarAlerts: this.groupSimilarAlerts,
      remindersEnabled: this.remindersEnabled,
      cooldownMinutes: this.cooldownMinutes,
      maxAlertsPerHour: this.maxAlertsPerHour,
      routineNightSilence: this.routineNightSilence,
      goalDeadlineAlerts: this.goalDeadlineAlerts,
      maintenanceDeviceAlerts: this.maintenanceDeviceAlerts,
      systemRecommendations: this.systemRecommendations,
      dailySummaryEnabled: this.dailySummaryEnabled,
      weeklySummaryEnabled: this.weeklySummaryEnabled,
      defaultDeliveryMode: this.defaultDeliveryMode,
    });
  }

  private levelForScore(score: number): AlertLevel {
    return levelForNotificationScore(score, this.currentClassificationPolicy());
  }

  private pushThresholdsForward(): void {
    if (this.neutralMax < this.stableMax + 5) {
      this.neutralMax = this.clamp(this.stableMax + 5, 10, 96);
    }

    if (this.warningMax < this.neutralMax + 5) {
      this.warningMax = this.clamp(this.neutralMax + 5, 15, 99);
    }
  }

  private toNumber(value: string | number): number {
    const parsed = Number(value);
    return Number.isFinite(parsed) ? parsed : 0;
  }

  private clamp(value: number, min: number, max: number): number {
    return Math.min(max, Math.max(min, value));
  }

  private toggleSetValue<T>(set: Set<T>, value: T): void {
    if (set.has(value) && set.size > 1) {
      set.delete(value);
      return;
    }

    set.add(value);
  }

  private currentClassificationPolicy(): NotificationClassificationPolicy {
    return {
      stableMax: this.stableMax,
      neutralMax: this.neutralMax,
      warningMax: this.warningMax,
    };
  }

  private emitClassificationPolicy(): void {
    this.classificationPolicyChanged.emit(this.currentClassificationPolicy());
  }

  private t(key: string, params?: Record<string, unknown>): string {
    return this.translate.instant(key, params);
  }
}


============================================================
FILE: src\app\notifications\presentation\pages\alert-rules\alert-rules-page.component.html
============================================================

<section class="alert-rules-page ec-page">
  <header class="ec-page-heading">
    <p class="ec-page-eyebrow">{{ 'nav.rules' | translate }}</p>
    <h1 class="ec-page-title">{{ 'alertRules.title' | translate }}</h1>
    <p class="ec-page-description">
      {{ 'alertRules.pageDescription' | translate }}
    </p>
  </header>

  <section class="rules-summary" [attr.aria-label]="'alertRules.summaryAria' | translate">
    <article>
      <span>{{ 'devices.total' | translate }}</span>
      <strong>{{ rules.length }}</strong>
      <small>{{ 'alertRules.savedRules' | translate }}</small>
    </article>

    <article>
      <span>{{ 'routines.active' | translate }}</span>
      <strong>{{ activeRules.length }}</strong>
      <small>{{ 'alertRules.pausedCount' | translate: { count: inactiveRules.length } }}</small>
    </article>

    <article>
      <span>{{ 'alertRules.activeWeight' | translate }}</span>
      <strong>{{ totalWeight }}</strong>
      <small>{{ 'alertRules.usedWhenEvaluating' | translate }}</small>
    </article>
  </section>

  <app-settings-section
    [eyebrow]="'alerts.classification' | translate"
    [title]="'alertRules.realScoreRanges' | translate"
    [description]="'alertRules.realScoreRangesDescription' | translate"
  >
    <div class="severity-stack">
      @for (range of severityRanges; track range.label) {
        <article class="severity-row" [class]="'severity-row severity-row--' + range.tone">
          <div>
            <strong>{{ severityRangeLabel(range) }}</strong>
            <p>{{ severityRangeDescription(range) }}</p>
          </div>
          <span>{{ range.from }}% - {{ range.to }}%</span>
        </article>
      }
    </div>
  </app-settings-section>

  <app-settings-section
    [eyebrow]="'alertRules.newRule' | translate"
    [title]="'alertRules.createPersistentRule' | translate"
    [description]="'alertRules.createPersistentRuleDescription' | translate"
  >
    <form class="rule-form" (ngSubmit)="createRule()">
      <label class="text-field">
        <span>{{ 'devices.name' | translate }}</span>
        <input
          type="text"
          name="newRuleName"
          [(ngModel)]="newRuleName"
          [placeholder]="'alertRules.namePlaceholder' | translate"
        />
      </label>

      <label class="setting-field">
        <span>{{ 'alertRules.metric' | translate }}</span>
        <app-dropdown
          [options]="metricOptions"
          [value]="newRuleMetric"
          (valueChange)="updateMetric($event)"
        />
      </label>

      <label class="setting-field">
        <span>{{ 'alertRules.condition' | translate }}</span>
        <app-dropdown
          [options]="conditionOptions"
          [value]="newRuleCondition"
          (valueChange)="updateCondition($event)"
        />
      </label>

      <label class="setting-field">
        <span>{{ 'alerts.level' | translate }}</span>
        <app-dropdown
          [options]="levelOptions"
          [value]="newRuleLevel"
          (valueChange)="updateLevel($event)"
        />
      </label>

      <label class="setting-field">
        <span>{{ 'alertRules.scope' | translate }}</span>
        <app-dropdown
          [options]="scopeOptions"
          [value]="newRuleScope"
          (valueChange)="updateScope($event)"
        />
      </label>

      <label class="setting-field">
        <span>{{ 'alertRules.criterion' | translate }}</span>
        <app-dropdown
          [options]="evaluatorOptions"
          [value]="newRuleEvaluator"
          (valueChange)="updateEvaluator($event)"
        />
      </label>

      <app-number-stepper
        [label]="'alertRules.threshold' | translate"
        [value]="newRuleThreshold"
        [min]="0"
        [step]="newRuleMetric === 'HIGH_READING_COUNT' ? 1 : 25"
        [decimals]="newRuleMetric === 'HIGH_READING_COUNT' ? 0 : 2"
        [allowEmpty]="false"
        (valueChange)="newRuleThreshold = $event ?? 0"
      />

      <app-number-stepper
        [label]="'alertRules.weight' | translate"
        [value]="newRuleWeight"
        [min]="1"
        [max]="100"
        [step]="1"
        [allowEmpty]="false"
        (valueChange)="newRuleWeight = $event ?? 1"
      />

      <app-button type="submit" variant="create" size="lg">
        {{ 'alertRules.create' | translate }}
      </app-button>
    </form>
  </app-settings-section>

  <app-settings-section
    [eyebrow]="'nav.rules' | translate"
    [title]="'alertRules.savedRules' | translate"
    [description]="'alertRules.savedRulesDescription' | translate"
  >
    @if (notificationsFacade.loading()) {
      <div class="loading-wrapper">
        <app-loading-spinner />
      </div>
    } @else {
      <div class="rules-list">
        @for (rule of rules; track rule.id) {
          <app-alert-rule-card
            [rule]="rule"
            (toggle)="toggleRule($event)"
            (remove)="deleteRule($event)"
          />
        } @empty {
          <app-empty-state
            [eyebrow]="'nav.rules' | translate"
            [title]="'alertRules.emptyTitle' | translate"
            [description]="'alertRules.emptyDescription' | translate"
            [compact]="true"
          />
        }
      </div>
    }
  </app-settings-section>

  <app-settings-section
    [eyebrow]="'alertRules.evaluation' | translate"
    [title]="'alertRules.testActiveRules' | translate"
    [description]="'alertRules.testActiveRulesDescription' | translate"
  >
    <div class="evaluation-panel">
      <app-number-stepper
        [label]="'alertRules.observedValue' | translate"
        [value]="observedValue"
        [min]="0"
        [step]="1"
        [allowEmpty]="false"
        (valueChange)="observedValue = $event ?? 0"
      />

      <app-button
        variant="primary"
        size="lg"
        [disabled]="!activeRules.length"
        (pressed)="evaluateRules()"
      >
        {{ 'alertRules.evaluate' | translate }}
      </app-button>
    </div>

    @if (evaluationResult; as result) {
      <article
        class="evaluation-result"
        [class]="'evaluation-result evaluation-result--' + result.level.toLowerCase()"
        [class.evaluation-result--dismissing]="isEvaluationResultDismissing"
      >
        <div class="evaluation-result__body">
          <div class="evaluation-result__header">
            <div class="evaluation-result__mark" aria-hidden="true">
              <span></span>
            </div>
            <div>
              <span class="evaluation-result__eyebrow">{{ 'alertRules.testedNotification' | translate }}</span>
              <h3>{{ levelLabel(result.level) }}</h3>
              <p>{{ result.explanation }}</p>
            </div>
            <div class="evaluation-result__score">
              <span>{{ 'alertRules.score' | translate }}</span>
              <strong>{{ result.severityScore }}</strong>
            </div>
          </div>

          <div class="result-metrics">
            <span>
              <span>{{ 'alertRules.criteria' | translate }}</span>
              <strong>{{ result.activeEvaluatorCount }}</strong>
            </span>
            <span>
              <span>{{ 'alertRules.weight' | translate }}</span>
              <strong>{{ result.totalWeight }}</strong>
            </span>
            <span>
              <span>{{ 'alertRules.scope' | translate }}</span>
              <strong>{{ result.scopeType }}</strong>
            </span>
          </div>

          <p class="result-evidence">{{ result.evidence }}</p>
        </div>

        <footer class="evaluation-result__footer">
          <p class="result-action">{{ result.recommendedAction }}</p>
          <app-button
            type="button"
            variant="ghost"
            size="sm"
            (pressed)="dismissEvaluationResult()"
          >
            {{ 'operationModes.actions.remove' | translate }}
          </app-button>
        </footer>
      </article>
    }
  </app-settings-section>
</section>


============================================================
FILE: src\app\notifications\presentation\pages\alert-rules\alert-rules-page.component.scss
============================================================

:host {
  display: block;
}

.alert-rules-page {
  display: grid;
  gap: 24px;
  animation: rules-in 0.24s ease;
}

.rules-summary {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 14px;
}

.rules-summary article,
.evaluation-result {
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background: var(--surface-card);
  box-shadow: var(--shadow-soft);
}

.rules-summary article {
  display: grid;
  gap: 8px;
  min-height: 126px;
  padding: 18px;
  transition:
    border-color 0.18s ease,
    transform 0.18s ease,
    background 0.18s ease;
}

.rules-summary article:hover {
  border-color: var(--border-strong);
  transform: translateY(-1px);
}

.rules-summary span,
.severity-row span,
.evaluation-result__eyebrow,
.evaluation-result__score span,
.result-metrics span span,
.text-field span,
.setting-field span {
  color: var(--text-muted);
  font-size: 0.76rem;
  font-weight: 900;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.rules-summary strong {
  color: var(--text-primary);
  font-size: 2rem;
  font-weight: 950;
  line-height: 1;
}

.rules-summary small {
  color: var(--text-secondary);
  font-weight: 750;
}

.severity-stack,
.rules-list {
  display: grid;
  gap: 12px;
}

.severity-row {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 14px;
  align-items: center;
  padding: 16px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  background: var(--surface-color);
  transition:
    border-color 0.18s ease,
    transform 0.18s ease;
}

.severity-row:hover {
  border-color: var(--border-strong);
  transform: translateY(-1px);
}

.severity-row strong,
.evaluation-result strong {
  color: var(--text-primary);
  font-size: 1.12rem;
  font-weight: 950;
}

.severity-row p,
.evaluation-result p {
  margin: 4px 0 0;
  color: var(--text-secondary);
  font-weight: 750;
  line-height: 1.45;
}

.severity-row > span {
  min-width: 86px;
  padding: 8px 10px;
  border-radius: 999px;
  text-align: center;
  background: rgba(148, 163, 184, 0.12);
}

.severity-row--stable {
  border-color: rgba(34, 197, 94, 0.34);
}

.severity-row--info {
  border-color: rgba(56, 189, 248, 0.34);
}

.severity-row--warning {
  border-color: rgba(250, 204, 21, 0.46);
}

.severity-row--critical {
  border-color: rgba(248, 113, 113, 0.46);
}

.rule-form {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 14px;
  align-items: end;
}

.text-field,
.setting-field {
  display: grid;
  gap: 8px;
  min-width: 0;
}

.text-field input {
  width: 100%;
  min-height: 46px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  padding: 0 14px;
  color: var(--text-primary);
  background: var(--field-bg);
  font: inherit;
  font-weight: 850;
  outline: none;
  transition:
    border-color 0.18s ease,
    box-shadow 0.18s ease,
    background 0.18s ease;
}

.text-field input:focus {
  border-color: var(--border-strong);
  box-shadow: var(--ec-focus-ring);
}

.loading-wrapper {
  display: grid;
  min-height: 220px;
  place-items: center;
}

.evaluation-panel {
  display: grid;
  grid-template-columns: minmax(0, 260px) auto;
  gap: 14px;
  align-items: end;
}

.evaluation-result {
  overflow: hidden;
  max-height: 420px;
  margin-top: 18px;
  padding: 18px;
  background:
    linear-gradient(135deg, rgba(56, 189, 248, 0.1), transparent 42%),
    rgba(8, 17, 35, 0.74);
  box-shadow:
    0 18px 44px rgba(2, 8, 23, 0.16),
    inset 3px 0 0 rgba(56, 189, 248, 0.46);
  transition:
    border-color 0.18s ease,
    background 0.18s ease,
    transform 0.18s ease;
}

.evaluation-result:hover {
  transform: translateY(-1px);
}

.evaluation-result__body {
  display: grid;
  gap: 14px;
}

.evaluation-result__header {
  display: grid;
  grid-template-columns: auto minmax(0, 1fr) auto;
  gap: 14px;
  align-items: center;
}

.evaluation-result__mark {
  width: 34px;
  height: 34px;
  display: grid;
  place-items: center;
  border: 1px solid rgba(125, 211, 252, 0.28);
  border-radius: 14px;
  background: rgba(56, 189, 248, 0.1);
}

.evaluation-result__mark span {
  width: 9px;
  height: 9px;
  border-radius: 999px;
  background: var(--accent-color);
  box-shadow:
    0 0 0 5px rgba(56, 189, 248, 0.11),
    0 0 18px rgba(56, 189, 248, 0.28);
}

.evaluation-result h3,
.evaluation-result p {
  margin: 0;
}

.evaluation-result h3 {
  color: var(--text-primary);
  font-size: 1.18rem;
  font-weight: 950;
  line-height: 1.15;
}

.evaluation-result__header p {
  margin-top: 5px;
}

.evaluation-result__score {
  min-width: 56px;
  display: grid;
  gap: 2px;
  padding: 8px 10px;
  border: 1px solid rgba(148, 163, 184, 0.16);
  border-radius: 16px;
  text-align: center;
  background: rgba(148, 163, 184, 0.12);
}

.evaluation-result--stable,
.evaluation-result--success {
  border-color: var(--ec-status-success-border);
}

.evaluation-result--info {
  border-color: var(--ec-status-info-border);
}

.evaluation-result--warning {
  border-color: var(--ec-status-warning-border);
}

.evaluation-result--critical {
  border-color: var(--ec-status-danger-border);
}

.result-metrics {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.result-metrics > span {
  display: inline-flex;
  gap: 8px;
  align-items: center;
  min-height: 34px;
  padding: 0 11px;
  border: 1px solid var(--border-color);
  border-radius: 999px;
  background: var(--ec-surface-subtle);
}

.result-evidence,
.result-action {
  margin: 0;
}

.result-evidence {
  width: fit-content;
  max-width: 100%;
  padding: 9px 11px;
  border-radius: 12px;
  background: var(--ec-status-info-bg);
}

.evaluation-result__footer {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  align-items: center;
  margin-top: 14px;
  padding-top: 12px;
  border-top: 1px solid var(--border-color);
}

.result-action {
  max-width: 68%;
  color: var(--text-secondary);
  font-size: 0.9rem;
}

.evaluation-result--dismissing {
  pointer-events: none;
  animation: result-shell-dismiss 0.52s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

.evaluation-result--dismissing .evaluation-result__body {
  animation: result-body-dismiss 0.28s ease forwards;
}

.evaluation-result--dismissing .evaluation-result__footer {
  animation: result-footer-dismiss 0.52s ease forwards;
}

@keyframes rules-in {
  from {
    opacity: 0;
    transform: translateY(6px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes result-body-dismiss {
  to {
    opacity: 0;
    transform: translateX(-36px);
  }
}

@keyframes result-footer-dismiss {
  0%,
  48% {
    opacity: 1;
    transform: translateY(0);
  }

  100% {
    opacity: 0;
    transform: translateY(-8px);
  }
}

@keyframes result-shell-dismiss {
  0%,
  50% {
    max-height: 420px;
    margin-top: 18px;
    padding-top: 18px;
    padding-bottom: 18px;
    opacity: 1;
  }

  100% {
    max-height: 0;
    margin-top: 0;
    padding-top: 0;
    padding-bottom: 0;
    opacity: 0;
    transform: translateX(-10px);
  }
}

@media (max-width: 1080px) {
  .rule-form {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 780px) {
  .rules-summary,
  .severity-row,
  .rule-form,
  .evaluation-panel,
  .result-metrics,
  .evaluation-result__header {
    grid-template-columns: 1fr;
  }

  .evaluation-result__footer {
    align-items: stretch;
    flex-direction: column;
  }

  .result-action {
    max-width: 100%;
  }
}


============================================================
FILE: src\app\notifications\presentation\pages\alert-rules\alert-rules-page.component.ts
============================================================

import { Component, OnDestroy, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { NotificationsFacade } from '../../../application/services/notifications.facade';
import { AlertLevel } from '../../../domain/model/alert.entity';
import {
  AlertRule,
  AlertRuleCondition,
  AlertRuleMetric,
  RuleEvaluatorType,
  RuleScopeType,
} from '../../../domain/model/alert-rule.entity';
import { RuleEvaluationResult } from '../../../domain/model/rule-evaluation-result.entity';

import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { AppNumberStepperComponent } from '../../../../shared/presentation/components/app-number-stepper/app-number-stepper.component';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { SettingsSectionComponent } from '../../../../shared/presentation/components/settings-section/settings-section.component';
import { AlertRuleCardComponent } from '../../components/alert-rule-card/alert-rule-card.component';
import {
  buildNotificationSeverityBands,
  NotificationSeverityBand,
} from '../../../domain/model/notification-classification-policy.model';

@Component({
  selector: 'app-alert-rules-page',
  standalone: true,
  imports: [
    FormsModule,
    TranslateModule,
    AppButtonComponent,
    AppDropdownComponent,
    AppNumberStepperComponent,
    EmptyStateComponent,
    LoadingSpinnerComponent,
    SettingsSectionComponent,
    AlertRuleCardComponent,
  ],
  templateUrl: './alert-rules-page.component.html',
  styleUrls: ['./alert-rules-page.component.scss'],
})
export class AlertRulesPageComponent implements OnInit, OnDestroy {
  newRuleName = '';
  newRuleMetric: AlertRuleMetric = 'WATTS';
  newRuleCondition: AlertRuleCondition = 'GREATER_OR_EQUAL_THAN';
  newRuleLevel: AlertLevel = 'WARNING';
  newRuleScope: RuleScopeType = 'GENERAL';
  newRuleEvaluator: RuleEvaluatorType = 'ACTIVE_POWER';
  newRuleThreshold = 750;
  newRuleWeight = 10;
  observedValue = 0;
  isEvaluationResultDismissing = false;
  private evaluationResultDismissTimeout: ReturnType<typeof setTimeout> | null = null;
  private readonly evaluationResultDismissMs = 520;

  readonly metricOptions: DropdownOption[] = [
    { label: '', labelKey: 'alertRules.metrics.watts', value: 'WATTS', descriptionKey: 'alertRules.metricDescriptions.watts' },
    { label: '', labelKey: 'alertRules.metrics.highReadingCount', value: 'HIGH_READING_COUNT', descriptionKey: 'alertRules.metricDescriptions.highReadingCount' },
    { label: '', labelKey: 'alertRules.metrics.averageWatts', value: 'AVERAGE_WATTS', descriptionKey: 'alertRules.metricDescriptions.averageWatts' },
  ];

  readonly conditionOptions: DropdownOption[] = [
    { label: '', labelKey: 'alertRules.conditions.greaterOrEqual', value: 'GREATER_OR_EQUAL_THAN' },
    { label: '', labelKey: 'alertRules.conditions.greaterThan', value: 'GREATER_THAN' },
  ];

  readonly levelOptions: DropdownOption[] = [
    { label: '', labelKey: 'alerts.levels.info', value: 'INFO' },
    { label: '', labelKey: 'alerts.levels.warning', value: 'WARNING' },
    { label: '', labelKey: 'alerts.levels.critical', value: 'CRITICAL' },
  ];

  readonly scopeOptions: DropdownOption[] = [
    { label: '', labelKey: 'alertRules.scopes.general', value: 'GENERAL' },
    { label: '', labelKey: 'operationModes.site', value: 'WORKPLACE' },
    { label: '', labelKey: 'devices.room', value: 'ROOM' },
    { label: '', labelKey: 'operationModes.routineTargetTypes.group', value: 'GROUP' },
    { label: '', labelKey: 'operationModes.routineTargetTypes.device', value: 'DEVICE' },
    { label: '', labelKey: 'nav.routines', value: 'ROUTINE' },
    { label: '', labelKey: 'reporting.goals', value: 'GOAL' },
  ];

  readonly evaluatorOptions: DropdownOption[] = [
    { label: '', labelKey: 'alertRules.evaluators.activePower', value: 'ACTIVE_POWER' },
    { label: '', labelKey: 'alertRules.evaluators.dailyKwh', value: 'DAILY_KWH' },
    { label: '', labelKey: 'alertRules.evaluators.averageWatts', value: 'AVERAGE_WATTS' },
    { label: '', labelKey: 'alertRules.evaluators.highReadingCount', value: 'HIGH_READING_COUNT' },
    { label: '', labelKey: 'alertRules.evaluators.sustainedConsumption', value: 'SUSTAINED_CONSUMPTION' },
    { label: '', labelKey: 'alertRules.evaluators.deviceCount', value: 'DEVICE_COUNT' },
    { label: '', labelKey: 'alertRules.evaluators.goalUsage', value: 'GOAL_USAGE' },
    { label: '', labelKey: 'alertRules.evaluators.goalDeadline', value: 'GOAL_DEADLINE' },
    { label: '', labelKey: 'alertRules.evaluators.routineContext', value: 'ROUTINE_CONTEXT' },
    { label: '', labelKey: 'alertRules.evaluators.costEstimate', value: 'COST_ESTIMATE' },
    { label: '', labelKey: 'alertRules.evaluators.configurationCoverage', value: 'CONFIGURATION_COVERAGE' },
  ];

  constructor(
    readonly notificationsFacade: NotificationsFacade,
    private readonly toastService: ToastService,
    private readonly translate: TranslateService
  ) {}

  async ngOnInit(): Promise<void> {
    await this.notificationsFacade.loadNotifications();
  }

  ngOnDestroy(): void {
    this.clearEvaluationResultDismissTimeout();
  }

  updateMetric(value: string): void {
    this.newRuleMetric = value as AlertRuleMetric;
  }

  updateCondition(value: string): void {
    this.newRuleCondition = value as AlertRuleCondition;
  }

  updateLevel(value: string): void {
    this.newRuleLevel = value as AlertLevel;
  }

  updateScope(value: string): void {
    this.newRuleScope = value as RuleScopeType;
  }

  updateEvaluator(value: string): void {
    this.newRuleEvaluator = value as RuleEvaluatorType;
  }

  async createRule(): Promise<void> {
    const name = this.newRuleName.trim();

    if (!name) {
      this.toastService.warning(this.t('alertRules.validation.nameRequired'));
      return;
    }

    const created = await this.notificationsFacade.createAlertRule({
      name,
      metric: this.newRuleMetric,
      condition: this.newRuleCondition,
      threshold: this.newRuleThreshold,
      level: this.newRuleLevel,
      scopeType: this.newRuleScope,
      scopeId: null,
      evaluatorType: this.newRuleEvaluator,
      weight: this.newRuleWeight,
    });

    if (created) {
      this.newRuleName = '';
      this.toastService.success(this.t('alertRules.createSuccess'));
      return;
    }

    this.toastService.error(this.t('alertRules.createError'));
  }

  async toggleRule(rule: AlertRule): Promise<void> {
    const updated = await this.notificationsFacade.updateAlertRuleStatus({
      alertRuleId: rule.id,
      enabled: !rule.enabled,
    });

    if (updated) {
      this.toastService.info(
        rule.enabled
          ? this.t('alertRules.toasts.paused')
          : this.t('alertRules.toasts.activated')
      );
      return;
    }

    this.toastService.error(this.t('alertRules.updateError'));
  }

  async deleteRule(ruleId: number): Promise<void> {
    const deleted = await this.notificationsFacade.deleteAlertRule(ruleId);

    if (deleted) {
      this.toastService.info(this.t('alertRules.toasts.deleted'));
      return;
    }

    this.toastService.error(this.t('alertRules.deleteError'));
  }

  async evaluateRules(): Promise<void> {
    this.clearEvaluationResultDismissTimeout();
    this.isEvaluationResultDismissing = false;

    const result = await this.notificationsFacade.evaluateRules({
      scopeType: this.newRuleScope,
      scopeId: null,
      observedValue: this.observedValue,
    });

    if (!result) {
      this.toastService.error(this.t('alertRules.toasts.evaluateError'));
      return;
    }

    this.toastService.success(this.t('alertRules.toasts.evaluated'));
  }

  dismissEvaluationResult(): void {
    if (!this.evaluationResult || this.isEvaluationResultDismissing) {
      return;
    }

    this.isEvaluationResultDismissing = true;
    this.clearEvaluationResultDismissTimeout();
    this.evaluationResultDismissTimeout = setTimeout(() => {
      this.notificationsFacade.clearRuleEvaluationResult();
      this.isEvaluationResultDismissing = false;
      this.evaluationResultDismissTimeout = null;
    }, this.evaluationResultDismissMs);
  }

  get rules(): AlertRule[] {
    return this.notificationsFacade.alertRules();
  }

  get activeRules(): AlertRule[] {
    return this.rules.filter((rule) => rule.enabled);
  }

  get inactiveRules(): AlertRule[] {
    return this.rules.filter((rule) => !rule.enabled);
  }

  get totalWeight(): number {
    return this.activeRules.reduce((total, rule) => total + rule.weight, 0);
  }

  get severityRanges(): NotificationSeverityBand[] {
    return buildNotificationSeverityBands(
      this.notificationsFacade.notificationClassificationPolicy()
    );
  }

  get evaluationResult(): RuleEvaluationResult | null {
    return this.notificationsFacade.ruleEvaluationResult();
  }

  private clearEvaluationResultDismissTimeout(): void {
    if (this.evaluationResultDismissTimeout === null) {
      return;
    }

    clearTimeout(this.evaluationResultDismissTimeout);
    this.evaluationResultDismissTimeout = null;
  }

  levelLabel(level: AlertLevel): string {
    const labels: Record<AlertLevel, string> = {
      STABLE: 'alerts.levels.stable',
      SUCCESS: 'alerts.levels.success',
      INFO: 'alerts.levels.info',
      WARNING: 'alerts.levels.warning',
      CRITICAL: 'alerts.levels.critical',
    };

    return this.t(labels[level] ?? level);
  }

  severityRangeLabel(range: NotificationSeverityBand): string {
    return this.levelLabel(range.level);
  }

  severityRangeDescription(range: NotificationSeverityBand): string {
    const descriptions: Record<AlertLevel, string> = {
      STABLE: 'alertRules.severityRanges.stableDescription',
      SUCCESS: 'alerts.severityDescriptions.success',
      INFO: 'alertRules.severityRanges.infoDescription',
      WARNING: 'alertRules.severityRanges.warningDescription',
      CRITICAL: 'alertRules.severityRanges.criticalDescription',
    };

    return this.t(descriptions[range.level] ?? 'alerts.severityDescriptions.default');
  }

  private t(key: string, params?: Record<string, unknown>): string {
    return this.translate.instant(key, params);
  }
}


============================================================
FILE: src\app\notifications\presentation\pages\alerts\alerts-page.component.html
============================================================

<section class="alerts-page">
  <header class="ec-page-heading alert-hero">
    <p class="ec-page-eyebrow">{{ 'alerts.eyebrow' | translate }}</p>
    <h1 class="ec-page-title">{{ 'nav.inbox' | translate }}</h1>
    <p class="ec-page-description">{{ 'alerts.inboxSubtitle' | translate }}</p>
  </header>

  <section class="summary-grid" [attr.aria-label]="'alerts.summaryAria' | translate">
    <article>
      <span>{{ 'alerts.total' | translate }}</span>
      <strong>{{ visibleAlertsCount }}</strong>
      <small>{{ activeFilterLabel }}</small>
    </article>

    <article>
      <span>{{ 'alerts.unreadCount' | translate }}</span>
      <strong>{{ visibleUnreadCount }}</strong>
      <small>{{ 'alerts.summary.visibleInboxOnly' | translate }}</small>
    </article>

    <article>
      <span>{{ 'alerts.criticalCount' | translate }}</span>
      <strong>{{ visibleCriticalCount }}</strong>
      <small>{{ 'alerts.summary.withoutSilenced' | translate }}</small>
    </article>
  </section>

  <section class="alerts-toolbar">
    <div>
      <span>{{ 'nav.inbox' | translate }}</span>
      <h2>{{ 'alerts.toolbar.title' | translate }}</h2>
      <p>
        {{ 'alerts.toolbar.description' | translate }}
      </p>
    </div>

    <div class="alert-filters">
      <app-dropdown
        [options]="filterOptions"
        [value]="selectedFilter"
        tone="info"
        (valueChange)="selectFilter($event)"
      />

      <app-button variant="secondary" size="sm" (pressed)="refresh()">
        {{ 'alerts.refresh' | translate }}
      </app-button>
    </div>
  </section>

  @if (hiddenByPreferencesCount > 0) {
    <p class="preference-note">
      {{ 'alerts.hiddenByPreferences' | translate: { count: hiddenByPreferencesCount } }}
    </p>
  }

  @if (notificationsFacade.loading()) {
    <div class="loading-wrapper">
      <app-loading-spinner />
    </div>
  } @else {
    <app-alert-list
      [alerts]="enrichedFilteredAlerts"
      (markAsRead)="markAsRead($event)"
      (remove)="deleteAlert($event)"
      (dismissForTenMinutes)="dismissAlert($event)"
    />
  }
</section>


============================================================
FILE: src\app\notifications\presentation\pages\alerts\alerts-page.component.scss
============================================================

:host {
  display: block;
}

.alerts-page {
  display: grid;
  gap: 24px;
  animation: page-in 0.24s ease;
}

.alert-hero {
  margin-bottom: 0;
}

.summary-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 14px;
}

.summary-grid article,
.alerts-toolbar,
.preference-note {
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background: var(--surface-card);
  box-shadow: var(--shadow-soft);
}

.summary-grid article {
  display: grid;
  gap: 8px;
  min-height: 126px;
  padding: 18px;
  transition:
    border-color 0.18s ease,
    transform 0.18s ease,
    background 0.18s ease;
}

.summary-grid article:hover {
  border-color: var(--border-strong);
  transform: translateY(-1px);
}

.summary-grid span,
.alerts-toolbar span {
  color: var(--text-muted);
  font-size: 0.76rem;
  font-weight: 900;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.summary-grid strong {
  color: var(--text-primary);
  font-size: 2rem;
  font-weight: 950;
  line-height: 1;
}

.summary-grid small,
.alerts-toolbar p,
.preference-note {
  color: var(--text-secondary);
  font-size: 0.86rem;
  font-weight: 700;
  line-height: 1.45;
}

.alerts-toolbar {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 18px;
  align-items: end;
  padding: 20px;
}

.alerts-toolbar h2 {
  margin: 4px 0 6px;
  color: var(--text-primary);
  font-size: 1.35rem;
}

.alerts-toolbar p {
  max-width: 720px;
  margin: 0;
}

.alert-filters {
  display: grid;
  grid-template-columns: minmax(210px, 260px) auto;
  gap: 10px;
  align-items: center;
}

.preference-note {
  margin: 0;
  padding: 14px 16px;
}

.loading-wrapper {
  display: grid;
  min-height: 260px;
  place-items: center;
}

@keyframes page-in {
  from {
    opacity: 0;
    transform: translateY(6px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@media (max-width: 840px) {
  .summary-grid,
  .alerts-toolbar,
  .alert-filters {
    grid-template-columns: 1fr;
  }
}


============================================================
FILE: src\app\notifications\presentation\pages\alerts\alerts-page.component.ts
============================================================

import { Component, OnInit } from '@angular/core';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { NotificationsFacade } from '../../../application/services/notifications.facade';
import { Alert, AlertLevel } from '../../../domain/model/alert.entity';
import { NotificationChannelPolicyService } from '../../../domain/services/notification-channel-policy.service';

import {
  AlertListComponent,
  AlertListItem,
} from '../../components/alert-list/alert-list.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';

type AlertFilter = 'ALL' | 'UNREAD' | AlertLevel;

@Component({
  selector: 'app-alerts-page',
  standalone: true,
  imports: [
    TranslateModule,
    AlertListComponent,
    LoadingSpinnerComponent,
    AppDropdownComponent,
    AppButtonComponent,
  ],
  templateUrl: './alerts-page.component.html',
  styleUrls: ['./alerts-page.component.scss'],
})
export class AlertsPageComponent implements OnInit {
  selectedFilter: AlertFilter = 'ALL';

  readonly filterOptions: DropdownOption[] = [
    { label: '', labelKey: 'alerts.filters.all', value: 'ALL' },
    { label: '', labelKey: 'alerts.filters.unread', value: 'UNREAD' },
    { label: '', labelKey: 'alerts.filters.stable', value: 'STABLE' },
    { label: '', labelKey: 'alerts.filters.success', value: 'SUCCESS' },
    { label: '', labelKey: 'alerts.filters.info', value: 'INFO' },
    { label: '', labelKey: 'alerts.filters.warning', value: 'WARNING' },
    { label: '', labelKey: 'alerts.filters.critical', value: 'CRITICAL' },
  ];

  constructor(
    readonly notificationsFacade: NotificationsFacade,
    private readonly notificationChannelPolicy: NotificationChannelPolicyService,
    private readonly toastService: ToastService,
    private readonly translate: TranslateService
  ) {}

  async ngOnInit(): Promise<void> {
    await this.refresh();
  }

  async refresh(): Promise<void> {
    await this.notificationsFacade.loadNotifications();
  }

  selectFilter(value: string): void {
    this.selectedFilter = value as AlertFilter;
  }

  async markAsRead(alertId: number): Promise<void> {
    const success = await this.notificationsFacade.markAsRead({ alertId });

    if (success) {
      this.toastService.success(this.t('alerts.markAsReadSuccess'));
      return;
    }

    this.toastService.error(this.t('alerts.markAsReadError'));
  }

  async deleteAlert(alertId: number): Promise<void> {
    const success = await this.notificationsFacade.deleteAlert(alertId);

    if (success) {
      this.toastService.info(this.t('alerts.deleteSuccess'));
      return;
    }

    this.toastService.error(this.t('alerts.deleteError'));
  }

  async dismissAlert(alert: AlertListItem): Promise<void> {
    const dismissed = await this.notificationsFacade.dismissAlert(alert.id, 10);

    if (dismissed) {
      this.toastService.info(this.t('alerts.dismissSuccess', { minutes: 10 }));
      return;
    }

    this.toastService.error(this.t('alerts.dismissError'));
  }

  get inboxAlerts(): AlertListItem[] {
    return this.notificationsFacade
      .sortedAlerts()
      .filter((alert) => this.isVisibleInboxAlert(alert))
      .filter((alert) => this.isAllowedByPreference(alert))
      .map((alert) => alert as AlertListItem);
  }

  get enrichedFilteredAlerts(): AlertListItem[] {
    return this.inboxAlerts.filter((alert) => {
      if (this.selectedFilter === 'ALL') {
        return true;
      }

      if (this.selectedFilter === 'UNREAD') {
        return alert.isUnread;
      }

      return alert.level === this.selectedFilter;
    });
  }

  get visibleAlertsCount(): number {
    return this.enrichedFilteredAlerts.length;
  }

  get visibleUnreadCount(): number {
    return this.enrichedFilteredAlerts.filter((alert) => alert.isUnread).length;
  }

  get visibleCriticalCount(): number {
    return this.enrichedFilteredAlerts.filter((alert) => alert.isCritical).length;
  }

  get hiddenByPreferencesCount(): number {
    const candidates = this.notificationsFacade
      .sortedAlerts()
      .filter((alert) => this.isVisibleInboxAlert(alert));

    return candidates.filter((alert) => !this.isAllowedByPreference(alert)).length;
  }

  get activeFilterLabel(): string {
    const option = this.filterOptions.find((item) => item.value === this.selectedFilter);

    return this.t(option?.labelKey ?? 'alerts.filters.all');
  }

  private isVisibleInboxAlert(alert: Alert): boolean {
    return alert.active && !alert.expired && !alert.silenced;
  }

  private isAllowedByPreference(alert: Alert): boolean {
    const preference = this.notificationsFacade.notificationPreference();

    if (!preference) {
      return true;
    }

    if (!preference.inAppEnabled) {
      return false;
    }

    return (
      preference.allowsSource(alert.sourceType) &&
      this.notificationChannelPolicy.canNotify(preference, alert.level)
    );
  }

  private t(key: string, params?: Record<string, unknown>): string {
    return this.translate.instant(key, params);
  }
}


============================================================
FILE: src\app\notifications\presentation\pages\notification-preferences\notification-preferences-page.component.html
============================================================

<section class="preferences-page ec-page">
    <header class="ec-page-heading">
      <p class="ec-page-eyebrow">{{ 'nav.preferences' | translate }}</p>
      <h1 class="ec-page-title">{{ 'notificationPreferences.sensitivityTitle' | translate }}</h1>
      <p class="ec-page-description">
        {{ 'notificationPreferences.sensitivityDescription' | translate }}
      </p>
    </header>

    <section class="preference-summary">
      <app-section-card
        [eyebrow]="'notificationPreferences.summary.rangesEyebrow' | translate"
        [title]="'notificationPreferences.summary.rangesTitle' | translate"
        [description]="'notificationPreferences.summary.rangesDescription' | translate"
      />
      <app-section-card
        [eyebrow]="'alerts.levels.success' | translate"
        [title]="'notificationPreferences.summary.successTitle' | translate"
        [description]="'notificationPreferences.summary.successDescription' | translate"
      />
      <app-section-card
        [eyebrow]="'notificationPreferences.summary.parametersEyebrow' | translate"
        [title]="'notificationPreferences.summary.parametersTitle' | translate"
        [description]="'notificationPreferences.summary.parametersDescription' | translate"
      />
    </section>

    @if (notificationsFacade.loading()) {
      <div class="loading-wrapper">
        <app-loading-spinner />
      </div>
    } @else {
      <app-settings-section
        [eyebrow]="'notificationPreferences.currentPreference' | translate"
        [title]="'notificationPreferences.editorTitle' | translate"
        [description]="'notificationPreferences.editorDescription' | translate"
        [stackedHeader]="true"
      >
        @if (notificationsFacade.notificationPreference()) {
          <app-notification-preference-form
            [preference]="notificationsFacade.notificationPreference()"
            [classificationPolicy]="notificationsFacade.notificationClassificationPolicy()"
            (classificationPolicyChanged)="notificationsFacade.updateNotificationClassificationPolicy($event)"
            (saved)="savePreference($event)"
          />
        } @else {
          <app-empty-state
            [eyebrow]="'nav.notifications' | translate"
            [title]="'notificationPreferences.emptyTitle' | translate"
            [description]="'notificationPreferences.emptyDescription' | translate"
          />
        }
      </app-settings-section>
    }
</section>


============================================================
FILE: src\app\notifications\presentation\pages\notification-preferences\notification-preferences-page.component.scss
============================================================

.preferences-page {
  display: grid;
  gap: 24px;
}

h1,
p {
  margin: 0;
}

.preference-summary {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;
}

.loading-wrapper {
  display: grid;
  place-items: center;
  min-height: 220px;
}

@media (max-width: 920px) {
  .preference-summary {
    grid-template-columns: 1fr;
  }
}


============================================================
FILE: src\app\notifications\presentation\pages\notification-preferences\notification-preferences-page.component.ts
============================================================

import { Component, OnInit } from '@angular/core';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { NotificationsFacade } from '../../../application/services/notifications.facade';
import { UpdateNotificationPreferenceCommand } from '../../../application/commands/update-notification-preference.command';

import { NotificationPreferenceFormComponent } from '../../components/notification-preference-form/notification-preference-form.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { SettingsSectionComponent } from '../../../../shared/presentation/components/settings-section/settings-section.component';
import { SectionCardComponent } from '../../../../shared/presentation/components/section-card/section-card.component';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';

@Component({
  selector: 'app-notification-preferences-page',
  standalone: true,
  imports: [
    TranslateModule,
    EmptyStateComponent,
    NotificationPreferenceFormComponent,
    LoadingSpinnerComponent,
    SettingsSectionComponent,
    SectionCardComponent,
  ],
  templateUrl: './notification-preferences-page.component.html',
  styleUrls: ['./notification-preferences-page.component.scss'],
})
export class NotificationPreferencesPageComponent implements OnInit {
  constructor(
    readonly notificationsFacade: NotificationsFacade,
    private readonly toastService: ToastService,
    private readonly translate: TranslateService
  ) {}

  async ngOnInit(): Promise<void> {
    await this.notificationsFacade.loadNotifications();
  }

  async savePreference(command: UpdateNotificationPreferenceCommand): Promise<void> {
    const success = await this.notificationsFacade.updateNotificationPreference(command);

    if (success) {
      this.toastService.success(this.translate.instant('notificationPreferences.updateSuccess'));
    } else {
      this.toastService.error(this.translate.instant('notificationPreferences.updateError'));
    }
  }
}


============================================================
FILE: src\app\notifications\presentation\routes\notifications.routes.ts
============================================================

import { Routes } from '@angular/router';

import { activeSubscriptionGuard } from '../../../shared/application/guards/active-subscription.guard';

export const NOTIFICATIONS_ROUTES: Routes = [
  {
    path: 'alerts/inbox',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Alertas' },
    loadComponent: () =>
      import('../pages/alerts/alerts-page.component').then(
        (m) => m.AlertsPageComponent
      ),
  },
  {
    path: 'alerts',
    redirectTo: 'alerts/inbox',
    pathMatch: 'full',
  },
  {
    path: 'alerts/rules',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Reglas' },
    loadComponent: () =>
      import('../pages/alert-rules/alert-rules-page.component').then(
        (m) => m.AlertRulesPageComponent
      ),
  },
  {
    path: 'alerts/preferences',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Preferencias' },
    loadComponent: () =>
      import('../pages/notification-preferences/notification-preferences-page.component').then(
        (m) => m.NotificationPreferencesPageComponent
      ),
  },
  {
    path: 'notifications/preferences',
    redirectTo: '/alerts/preferences',
    pathMatch: 'full',
  },
];


============================================================
FILE: src\app\reporting\application\commands\create-consumption-report.command.ts
============================================================

import { ConsumptionReportPeriod } from '../../domain/model/consumption-report.entity';

export interface CreateConsumptionReportCommand {
  title: string;
  period: ConsumptionReportPeriod;
  startDate: string;
  endDate: string;
}


============================================================
FILE: src\app\reporting\application\commands\create-energy-goal.command.ts
============================================================

export interface CreateEnergyGoalCommand {
  title: string;
  targetKilowattHours: number;
  deadline: string;
  scopeType?: 'GENERAL' | 'WORKPLACE' | 'ROOM' | 'DEVICE' | 'GROUP';
  scopeId?: number | null;
  scopeName?: string | null;
  activeFrom?: string | null;
  activeTo?: string | null;
}


============================================================
FILE: src\app\reporting\application\commands\export-report.command.ts
============================================================

export interface ExportReportCommand {
  reportId: number;
}


============================================================
FILE: src\app\reporting\application\commands\generate-consumption-report.command.ts
============================================================

export interface GenerateConsumptionReportCommand {
  startDate: string;
  endDate: string;
}


============================================================
FILE: src\app\reporting\application\commands\update-energy-goal.command.ts
============================================================

export interface UpdateEnergyGoalCommand {
  goalId: number;
  title: string;
  targetKilowattHours: number;
  currentKilowattHours: number;
  deadline: string;
  scopeType?: 'GENERAL' | 'WORKPLACE' | 'ROOM' | 'DEVICE' | 'GROUP';
  scopeId?: number | null;
  scopeName?: string | null;
  activeFrom?: string | null;
  activeTo?: string | null;
}


============================================================
FILE: src\app\reporting\application\services\reporting.facade.ts
============================================================

import { Injectable } from '@angular/core';
import { firstValueFrom } from 'rxjs';

import { BillingFacade } from '../../../billing/application/services/billing.facade';
import { PlanPermissionService } from '../../../billing/domain/services/plan-permission.service';
import { EnergyMonitoringFacade } from '../../../energy-monitoring/application/services/energy-monitoring.facade';

import { ConsumptionReport } from '../../domain/model/consumption-report.entity';
import { EnergyGoalPolicyService } from '../../domain/services/energy-goal-policy.service';

import { CreateConsumptionReportCommand } from '../commands/create-consumption-report.command';
import { CreateEnergyGoalCommand } from '../commands/create-energy-goal.command';
import { ExportReportCommand } from '../commands/export-report.command';
import { UpdateEnergyGoalCommand } from '../commands/update-energy-goal.command';

import { ConsumptionReportsApiService } from '../../infrastructure/api/consumption-reports-api.service';
import { EnergyGoalsApiService } from '../../infrastructure/api/energy-goals-api.service';

import { ConsumptionReportAssembler } from '../../infrastructure/assemblers/consumption-report.assembler';
import { EnergyGoalAssembler } from '../../infrastructure/assemblers/energy-goal.assembler';

import { ReportingStore } from '../stores/reporting.store';

export type CreateConsumptionReportResult =
  | 'CREATED'
  | 'UPDATED'
  | 'UNCHANGED'
  | 'FAILED';

@Injectable({
  providedIn: 'root',
})
export class ReportingFacade {
  private readonly consumptionReportAssembler = new ConsumptionReportAssembler();
  private readonly energyGoalAssembler = new EnergyGoalAssembler();

  get consumptionReports() {
    return this.store.consumptionReports;
  }

  get energyGoals() {
    return this.store.energyGoals;
  }

  get loading() {
    return this.store.loading;
  }

  get error() {
    return this.store.error;
  }

  get activeGoals() {
    return this.store.activeGoals;
  }

  get completedGoals() {
    return this.store.completedGoals;
  }

  get failedGoals() {
    return this.store.failedGoals;
  }

  get totalReportedWatts() {
    return this.store.totalReportedWatts;
  }

  get averageReportedWatts() {
    return this.store.averageReportedWatts;
  }

  get highestReport() {
    return this.store.highestReport;
  }

  get energyReadings() {
    return this.energyMonitoringFacade.readings;
  }

  constructor(
    private readonly consumptionReportsApi: ConsumptionReportsApiService,
    private readonly energyGoalsApi: EnergyGoalsApiService,
    private readonly billingFacade: BillingFacade,
    private readonly energyMonitoringFacade: EnergyMonitoringFacade,
    private readonly energyGoalPolicyService: EnergyGoalPolicyService,
    private readonly planPermissionService: PlanPermissionService,
    private readonly store: ReportingStore
  ) {}

  async loadReporting(): Promise<void> {
    this.startRequest();

    try {
      await Promise.all([
        this.loadConsumptionReports(),
        this.loadEnergyGoals(),
        this.energyMonitoringFacade.loadReadings(),
      ]);
    } catch (error) {
      console.error(error);
      this.store.setError('reporting.loadError');
    } finally {
      this.finishRequest();
    }
  }

  async loadConsumptionReports(): Promise<void> {
    const responses = await firstValueFrom(
      this.consumptionReportsApi.findAllForCurrentUser()
    );

    const reports = responses.map((response) =>
      this.consumptionReportAssembler.toEntity(response)
    );

    this.store.setConsumptionReports(reports);
  }

  async loadEnergyGoals(): Promise<void> {
    const responses = await firstValueFrom(
      this.energyGoalsApi.findAllForCurrentUser()
    );

    const goals = responses.map((response) =>
      this.energyGoalAssembler.toEntity(response)
    );

    this.store.setEnergyGoals(goals);
  }

  async createConsumptionReport(
    command: CreateConsumptionReportCommand
  ): Promise<CreateConsumptionReportResult> {
    this.startRequest();

    try {
      const existingReport = this.store
        .consumptionReports()
        .find(
          (report) =>
            report.startDate === command.startDate &&
            report.endDate === command.endDate
        );
      const response = await firstValueFrom(
        this.consumptionReportsApi.create({
          startDate: command.startDate,
          endDate: command.endDate,
        })
      );

      const report = this.consumptionReportAssembler.toEntity(response);

      this.store.upsertConsumptionReport(report);
      this.store.setError(null);

      if (!existingReport) {
        return 'CREATED';
      }

      return this.isClosedReportCycle(command.endDate)
        ? 'UNCHANGED'
        : 'UPDATED';
    } catch (error) {
      console.error(error);
      this.store.setError('reporting.reportCreateError');
      return 'FAILED';
    } finally {
      this.finishRequest();
    }
  }

  async deleteConsumptionReport(reportId: number): Promise<boolean> {
    this.startRequest();

    try {
      await firstValueFrom(this.consumptionReportsApi.delete(reportId));
      this.store.removeConsumptionReport(reportId);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('reporting.reportDeleteError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  exportReport(command: ExportReportCommand): boolean {
    this.store.setError(null);

    const canExport = this.planPermissionService.canExportCsv(
      this.billingFacade.activePlanCode()
    );

    if (!canExport) {
      this.store.setError('reporting.exportNotAllowed');
      return false;
    }

    const report: ConsumptionReport | undefined = this.store
      .consumptionReports()
      .find((item) => item.id === command.reportId);

    if (!report) {
      this.store.setError('reporting.reportNotFound');
      return false;
    }

    const content = [
      'ELECTROCORP CONSUMPTION REPORT',
      `Report ID: ${report.id}`,
      `Title: ${report.title}`,
      `Period: ${report.period}`,
      `Start Date: ${report.startDate}`,
      `End Date: ${report.endDate}`,
      `Total Watts: ${report.totalWatts}`,
      `Average Watts: ${report.averageWatts}`,
      `Highest Watts: ${report.highestWatts}`,
    ].join('\n');

    const blob = new Blob([content], { type: 'text/plain;charset=utf-8' });
    const url = URL.createObjectURL(blob);

    const anchor = document.createElement('a');
    anchor.href = url;
    anchor.download = `electrocorp-report-${report.id}.txt`;
    anchor.click();

    URL.revokeObjectURL(url);
    return true;
  }

  async createEnergyGoal(command: CreateEnergyGoalCommand): Promise<boolean> {
    this.startRequest();

    try {
      const status = this.energyGoalPolicyService.resolveStatus({
        targetKilowattHours: Number(command.targetKilowattHours),
        currentKilowattHours: 0,
        deadline: command.deadline,
      });
      const currentKilowattHours = this.energyMonitoringFacade
        .readings()
        .reduce((total, reading) => total + reading.kilowattHours, 0);

      const response = await firstValueFrom(
        this.energyGoalsApi.create({
          title: command.title.trim(),
          targetKilowattHours: Number(command.targetKilowattHours),
          currentKilowattHours,
          deadline: command.deadline,
          status,
          createdAt: new Date().toISOString().slice(0, 10),
          scopeType: command.scopeType ?? 'GENERAL',
          scopeId: command.scopeId ?? null,
          scopeName: command.scopeName ?? null,
          activeFrom: command.activeFrom ?? null,
          activeTo: command.activeTo ?? null,
        })
      );

      const goal = this.energyGoalAssembler.toEntity(response);
      this.store.prependEnergyGoal(goal);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('reporting.goalCreateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async updateEnergyGoal(command: UpdateEnergyGoalCommand): Promise<boolean> {
    this.startRequest();

    try {
      const status = this.energyGoalPolicyService.resolveStatus({
        targetKilowattHours: Number(command.targetKilowattHours),
        currentKilowattHours: Number(command.currentKilowattHours),
        deadline: command.deadline,
      });

      const response = await firstValueFrom(
        this.energyGoalsApi.patch(command.goalId, {
          title: command.title.trim(),
          targetKilowattHours: Number(command.targetKilowattHours),
          currentKilowattHours: Number(command.currentKilowattHours),
          deadline: command.deadline,
          status,
          scopeType: command.scopeType ?? 'GENERAL',
          scopeId: command.scopeId ?? null,
          scopeName: command.scopeName ?? null,
          activeFrom: command.activeFrom ?? null,
          activeTo: command.activeTo ?? null,
        })
      );

      const goal = this.energyGoalAssembler.toEntity(response);
      this.store.updateEnergyGoal(goal);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('reporting.goalUpdateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async deleteEnergyGoal(goalId: number): Promise<boolean> {
    this.startRequest();

    try {
      await firstValueFrom(this.energyGoalsApi.delete(goalId));
      this.store.removeEnergyGoal(goalId);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('reporting.goalDeleteError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  clearMessages(): void {
    this.store.clearMessages();
  }

  private startRequest(): void {
    this.store.setLoading(true);
    this.store.clearMessages();
  }

  private finishRequest(): void {
    this.store.setLoading(false);
  }

  private isClosedReportCycle(endDate: string): boolean {
    const today = new Date();
    const todayAtMidnight = new Date(
      today.getFullYear(),
      today.getMonth(),
      today.getDate()
    );

    return new Date(`${endDate}T00:00:00`).getTime() < todayAtMidnight.getTime();
  }
}


============================================================
FILE: src\app\reporting\application\stores\reporting.store.ts
============================================================

import { Injectable, computed, signal } from '@angular/core';

import { ConsumptionReport } from '../../domain/model/consumption-report.entity';
import { EnergyGoal } from '../../domain/model/energy-goal.entity';
import { ReportSummaryService } from '../../domain/services/report-summary.service';

@Injectable({
  providedIn: 'root',
})
export class ReportingStore {
  private readonly consumptionReportsSignal = signal<ConsumptionReport[]>([]);
  private readonly energyGoalsSignal = signal<EnergyGoal[]>([]);

  private readonly loadingSignal = signal<boolean>(false);
  private readonly errorSignal = signal<string | null>(null);

  readonly consumptionReports = computed(() => this.consumptionReportsSignal());
  readonly energyGoals = computed(() => this.energyGoalsSignal());

  readonly loading = computed(() => this.loadingSignal());
  readonly error = computed(() => this.errorSignal());

  readonly activeGoals = computed(() =>
    this.energyGoalsSignal().filter((goal) => goal.isActive)
  );

  readonly completedGoals = computed(() =>
    this.energyGoalsSignal().filter((goal) => goal.isCompleted)
  );

  readonly failedGoals = computed(() =>
    this.energyGoalsSignal().filter((goal) => goal.isFailed)
  );

  readonly totalReportedWatts = computed(() =>
    this.reportSummaryService.calculateTotalReportedWatts(
      this.consumptionReportsSignal()
    )
  );

  readonly averageReportedWatts = computed(() =>
    this.reportSummaryService.calculateAverageReportedWatts(
      this.consumptionReportsSignal()
    )
  );

  readonly highestReport = computed(() =>
    this.reportSummaryService.findHighestReport(this.consumptionReportsSignal())
  );

  constructor(private readonly reportSummaryService: ReportSummaryService) {}

  setConsumptionReports(value: ConsumptionReport[]): void {
    this.consumptionReportsSignal.set(this.sortConsumptionReports(value));
  }

  upsertConsumptionReport(value: ConsumptionReport): void {
    this.consumptionReportsSignal.update((reports) =>
      this.sortConsumptionReports([
        value,
        ...reports.filter((report) => report.id !== value.id),
      ])
    );
  }

  removeConsumptionReport(reportId: number): void {
    this.consumptionReportsSignal.update((reports) =>
      reports.filter((report) => report.id !== reportId)
    );
  }

  setEnergyGoals(value: EnergyGoal[]): void {
    this.energyGoalsSignal.set(value);
  }

  prependEnergyGoal(value: EnergyGoal): void {
    this.energyGoalsSignal.update((goals) => [value, ...goals]);
  }

  updateEnergyGoal(value: EnergyGoal): void {
    this.energyGoalsSignal.update((goals) =>
      goals.map((goal) => (goal.id === value.id ? value : goal))
    );
  }

  removeEnergyGoal(goalId: number): void {
    this.energyGoalsSignal.update((goals) =>
      goals.filter((goal) => goal.id !== goalId)
    );
  }

  setLoading(value: boolean): void {
    this.loadingSignal.set(value);
  }

  setError(value: string | null): void {
    this.errorSignal.set(value);
  }

  clearMessages(): void {
    this.errorSignal.set(null);
  }

  reset(): void {
    this.consumptionReportsSignal.set([]);
    this.energyGoalsSignal.set([]);
    this.loadingSignal.set(false);
    this.errorSignal.set(null);
  }

  private sortConsumptionReports(reports: ConsumptionReport[]): ConsumptionReport[] {
    return [...reports].sort((first, second) => {
      const startDiff =
        new Date(first.startDate).getTime() - new Date(second.startDate).getTime();

      if (startDiff !== 0) {
        return startDiff;
      }

      const endDiff =
        new Date(first.endDate).getTime() - new Date(second.endDate).getTime();

      return endDiff !== 0 ? endDiff : first.id - second.id;
    });
  }
}


============================================================
FILE: src\app\reporting\domain\model\consumption-report.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export type ConsumptionReportPeriod = 'DAILY' | 'WEEKLY' | 'MONTHLY' | 'YEARLY';

export class ConsumptionReport extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _title: string;
  private readonly _period: ConsumptionReportPeriod;
  private readonly _totalWatts: number;
  private readonly _averageWatts: number;
  private readonly _highestWatts: number;
  private readonly _startDate: string;
  private readonly _endDate: string;
  private readonly _generatedAt: string;

  constructor(props: {
    id: number;
    userId: number;
    title: string;
    period: ConsumptionReportPeriod;
    totalWatts: number;
    averageWatts: number;
    highestWatts: number;
    startDate: string;
    endDate: string;
    generatedAt: string;
  }) {
    super(props.id);
    this._userId = props.userId;
    this._title = props.title;
    this._period = props.period;
    this._totalWatts = props.totalWatts;
    this._averageWatts = props.averageWatts;
    this._highestWatts = props.highestWatts;
    this._startDate = props.startDate;
    this._endDate = props.endDate;
    this._generatedAt = props.generatedAt;
  }

  get userId(): number {
    return this._userId;
  }

  get title(): string {
    return this._title;
  }

  get period(): ConsumptionReportPeriod {
    return this._period;
  }

  get totalWatts(): number {
    return this._totalWatts;
  }

  get averageWatts(): number {
    return this._averageWatts;
  }

  get highestWatts(): number {
    return this._highestWatts;
  }

  get startDate(): string {
    return this._startDate;
  }

  get endDate(): string {
    return this._endDate;
  }

  get generatedAt(): string {
    return this._generatedAt;
  }
}

============================================================
FILE: src\app\reporting\domain\model\energy-goal.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export type EnergyGoalStatus = 'ACTIVE' | 'COMPLETED' | 'FAILED';
export type EnergyGoalScopeType = 'GENERAL' | 'WORKPLACE' | 'ROOM' | 'DEVICE' | 'GROUP';

export class EnergyGoal extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _title: string;
  private readonly _targetKilowattHours: number;
  private readonly _currentKilowattHours: number;
  private readonly _deadline: string;
  private readonly _status: EnergyGoalStatus;
  private readonly _createdAt: string;
  private readonly _scopeType: EnergyGoalScopeType;
  private readonly _scopeId: number | null;
  private readonly _scopeName: string;
  private readonly _activeFrom: string | null;
  private readonly _activeTo: string | null;

  constructor(props: {
    id: number;
    userId: number;
    title: string;
    targetKilowattHours: number;
    currentKilowattHours: number;
    deadline: string;
    status: EnergyGoalStatus;
    createdAt: string;
    scopeType?: EnergyGoalScopeType | null;
    scopeId?: number | null;
    scopeName?: string | null;
    activeFrom?: string | null;
    activeTo?: string | null;
  }) {
    super(props.id);
    this._userId = props.userId;
    this._title = props.title;
    this._targetKilowattHours = props.targetKilowattHours;
    this._currentKilowattHours = props.currentKilowattHours;
    this._deadline = props.deadline;
    this._status = props.status;
    this._createdAt = props.createdAt;
    this._scopeType = props.scopeType ?? 'GENERAL';
    this._scopeId = props.scopeId ?? null;
    this._scopeName = props.scopeName ?? 'Toda la operacion';
    this._activeFrom = props.activeFrom ?? null;
    this._activeTo = props.activeTo ?? null;
  }

  get userId(): number {
    return this._userId;
  }

  get title(): string {
    return this._title;
  }

  get targetKilowattHours(): number {
    return this._targetKilowattHours;
  }

  get currentKilowattHours(): number {
    return this._currentKilowattHours;
  }

  get deadline(): string {
    return this._deadline;
  }

  get status(): EnergyGoalStatus {
    return this._status;
  }

  get createdAt(): string {
    return this._createdAt;
  }

  get scopeType(): EnergyGoalScopeType {
    return this._scopeType;
  }

  get scopeId(): number | null {
    return this._scopeId;
  }

  get scopeName(): string {
    return this._scopeName;
  }

  get activeFrom(): string | null {
    return this._activeFrom;
  }

  get activeTo(): string | null {
    return this._activeTo;
  }

  get activeWindowLabel(): string {
    if (!this._activeFrom || !this._activeTo) {
      return 'Todo el dia';
    }

    return `${this._activeFrom} - ${this._activeTo}`;
  }

  get progressPercentage(): number {
    if (this.targetKilowattHours <= 0) return 0;

    const progress = (this.currentKilowattHours / this.targetKilowattHours) * 100;
    return Math.min(100, Number(progress.toFixed(2)));
  }

  get isActive(): boolean {
    return this._status === 'ACTIVE';
  }

  get isCompleted(): boolean {
    return this._status === 'COMPLETED';
  }

  get isFailed(): boolean {
    return this._status === 'FAILED';
  }
}


============================================================
FILE: src\app\reporting\domain\services\energy-goal-policy.service.ts
============================================================

import { Injectable } from '@angular/core';

import { EnergyGoal } from '../model/energy-goal.entity';

type EnergyGoalProgress = Pick<
  EnergyGoal,
  'currentKilowattHours' | 'targetKilowattHours' | 'deadline'
>;

@Injectable({
  providedIn: 'root',
})
export class EnergyGoalPolicyService {
  canCreateGoal(title: string, targetKilowattHours: number, deadline: string): boolean {
    return title.trim().length >= 3 && targetKilowattHours > 0 && deadline.trim().length > 0;
  }

  resolveStatus(goal: EnergyGoalProgress): 'ACTIVE' | 'COMPLETED' | 'FAILED' {
    if (goal.currentKilowattHours > goal.targetKilowattHours) {
      return 'FAILED';
    }

    const today = new Date().toISOString().slice(0, 10);

    if (goal.deadline < today) {
      return goal.currentKilowattHours > goal.targetKilowattHours ? 'FAILED' : 'ACTIVE';
    }

    return 'ACTIVE';
  }
}


============================================================
FILE: src\app\reporting\domain\services\report-summary.service.ts
============================================================

import { Injectable } from '@angular/core';

import { ConsumptionReport } from '../model/consumption-report.entity';

@Injectable({
  providedIn: 'root',
})
export class ReportSummaryService {
  calculateTotalReportedWatts(reports: ConsumptionReport[]): number {
    return reports.reduce((total, report) => total + report.totalWatts, 0);
  }

  calculateAverageReportedWatts(reports: ConsumptionReport[]): number {
    if (reports.length === 0) return 0;

    const total = reports.reduce((sum, report) => sum + report.averageWatts, 0);
    return Number((total / reports.length).toFixed(2));
  }

  findHighestReport(reports: ConsumptionReport[]): ConsumptionReport | null {
    if (reports.length === 0) return null;

    return reports.reduce((highest, current) =>
      current.highestWatts > highest.highestWatts ? current : highest
    );
  }
}

============================================================
FILE: src\app\reporting\infrastructure\api\consumption-reports-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { ConsumptionReport } from '../../domain/model/consumption-report.entity';
import { ConsumptionReportAssembler } from '../assemblers/consumption-report.assembler';
import { ConsumptionReportResource } from '../resources/consumption-report.resource';
import { ConsumptionReportResponse } from '../responses/consumption-report.response';

@Injectable({
  providedIn: 'root',
})
export class ConsumptionReportsApiService extends BaseApiService<
  ConsumptionReport,
  ConsumptionReportResource,
  ConsumptionReportResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(
      http,
      apiBaseUrl,
      'reports',
      new ConsumptionReportAssembler()
    );
  }

  findAllForCurrentUser(): Observable<ConsumptionReportResponse[]> {
    return this.http.get<ConsumptionReportResponse[]>(this.resourceEndpoint);
  }
}


============================================================
FILE: src\app\reporting\infrastructure\api\energy-goals-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { EnergyGoal } from '../../domain/model/energy-goal.entity';
import { EnergyGoalAssembler } from '../assemblers/energy-goal.assembler';
import { EnergyGoalResource } from '../resources/energy-goal.resource';
import { EnergyGoalResponse } from '../responses/energy-goal.response';

@Injectable({
  providedIn: 'root',
})
export class EnergyGoalsApiService extends BaseApiService<
  EnergyGoal,
  EnergyGoalResource,
  EnergyGoalResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(
      http,
      apiBaseUrl,
      'reports/energy-goals',
      new EnergyGoalAssembler()
    );
  }

  findAllForCurrentUser(): Observable<EnergyGoalResponse[]> {
    return this.http.get<EnergyGoalResponse[]>(this.resourceEndpoint);
  }
}


============================================================
FILE: src\app\reporting\infrastructure\assemblers\consumption-report.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { ConsumptionReport } from '../../domain/model/consumption-report.entity';
import { ConsumptionReportResource } from '../resources/consumption-report.resource';
import { ConsumptionReportResponse } from '../responses/consumption-report.response';

export class ConsumptionReportAssembler extends BaseAssembler<
  ConsumptionReport,
  ConsumptionReportResource,
  ConsumptionReportResponse
> {
  override toEntity(response: ConsumptionReportResponse): ConsumptionReport {
    const startDate = response.startDate;
    const endDate = response.endDate;
    const period = this.resolvePeriod(startDate, endDate);

    return new ConsumptionReport({
      id: response.id,
      userId: response.userId,
      title: `Reporte ${startDate} / ${endDate}`,
      period,
      totalWatts: response.totalWatts,
      averageWatts: response.averageWatts,
      highestWatts: response.highestWatts,
      startDate,
      endDate,
      generatedAt: endDate,
    });
  }

  override toResource(entity: ConsumptionReport): ConsumptionReportResource {
    return {
      userId: entity.userId,
      startDate: entity.startDate,
      endDate: entity.endDate,
    };
  }

  private resolvePeriod(
    startDate: string,
    endDate: string
  ): ConsumptionReport['period'] {
    const start = new Date(`${startDate}T00:00:00`);
    const end = new Date(`${endDate}T00:00:00`);

    const days =
      Math.round(
        (end.getTime() - start.getTime()) / (1000 * 60 * 60 * 24)
      ) + 1;

    if (days <= 1) return 'DAILY';
    if (days <= 7) return 'WEEKLY';
    if (days >= 365) return 'YEARLY';

    return 'MONTHLY';
  }
}

============================================================
FILE: src\app\reporting\infrastructure\assemblers\energy-goal.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { EnergyGoal } from '../../domain/model/energy-goal.entity';
import { EnergyGoalResource } from '../resources/energy-goal.resource';
import { EnergyGoalResponse } from '../responses/energy-goal.response';

export class EnergyGoalAssembler extends BaseAssembler<
  EnergyGoal,
  EnergyGoalResource,
  EnergyGoalResponse
> {
  override toEntity(response: EnergyGoalResponse): EnergyGoal {
    return new EnergyGoal({
      id: response.id,
      userId: response.userId,
      title: response.title,
      targetKilowattHours: this.resolveTargetKilowattHours(response.targetKilowattHours, response.targetWatts),
      currentKilowattHours: this.resolveCurrentKilowattHours(response.currentKilowattHours, response.currentWatts),
      deadline: response.deadline,
      status: response.status,
      createdAt: response.createdAt,
      scopeType: response.scopeType ?? 'GENERAL',
      scopeId: response.scopeId ?? null,
      scopeName: response.scopeName,
      activeFrom: response.activeFrom,
      activeTo: response.activeTo,
    });
  }

  override toResource(entity: EnergyGoal): EnergyGoalResource {
    return {
      userId: entity.userId,
      title: entity.title,
      targetKilowattHours: entity.targetKilowattHours,
      currentKilowattHours: entity.currentKilowattHours,
      deadline: entity.deadline,
      status: entity.status,
      createdAt: entity.createdAt,
      scopeType: entity.scopeType,
      scopeId: entity.scopeId,
      scopeName: entity.scopeName,
      activeFrom: entity.activeFrom,
      activeTo: entity.activeTo,
    };
  }

  private resolveTargetKilowattHours(primary: number | undefined, legacyMisnamedKilowattHours: number | undefined): number {
    const value = Number(primary ?? legacyMisnamedKilowattHours ?? 0);
    return this.normalizeLegacyTarget(value);
  }

  private resolveCurrentKilowattHours(primary: number | undefined, legacyMisnamedKilowattHours: number | undefined): number {
    return Number(primary ?? legacyMisnamedKilowattHours ?? 0);
  }

  private normalizeLegacyTarget(value: number): number {
    if (Number.isInteger(value) && value >= 100 && value <= 10000) {
      return Number((value / 1000).toFixed(6));
    }

    return value;
  }
}


============================================================
FILE: src\app\reporting\infrastructure\resources\consumption-report.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';

export interface ConsumptionReportResource extends BaseResource {
  userId?: number;
  startDate: string;
  endDate: string;
}


============================================================
FILE: src\app\reporting\infrastructure\resources\energy-goal.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';
import { EnergyGoalScopeType, EnergyGoalStatus } from '../../domain/model/energy-goal.entity';

export interface EnergyGoalResource extends BaseResource {
  userId?: number;
  title: string;
  targetKilowattHours: number;
  currentKilowattHours: number;
  deadline: string;
  status: EnergyGoalStatus;
  createdAt?: string;
  scopeType?: EnergyGoalScopeType;
  scopeId?: number | null;
  scopeName?: string | null;
  activeFrom?: string | null;
  activeTo?: string | null;
}


============================================================
FILE: src\app\reporting\infrastructure\responses\consumption-report.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';

export interface ConsumptionReportResponse extends BaseResponse<number> {
  userId: number;
  totalWatts: number;
  averageWatts: number;
  highestWatts: number;
  startDate: string;
  endDate: string;
}

============================================================
FILE: src\app\reporting\infrastructure\responses\energy-goal.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';
import { EnergyGoalScopeType, EnergyGoalStatus } from '../../domain/model/energy-goal.entity';

export interface EnergyGoalResponse extends BaseResponse<number> {
  userId: number;
  title: string;
  targetKilowattHours: number;
  currentKilowattHours: number;
  /** Legacy API fields that were historically misnamed kWh values. */
  targetWatts?: number;
  currentWatts?: number;
  deadline: string;
  status: EnergyGoalStatus;
  createdAt: string;
  scopeType?: EnergyGoalScopeType | null;
  scopeId?: number | null;
  scopeName?: string | null;
  activeFrom?: string | null;
  activeTo?: string | null;
}


============================================================
FILE: src\app\reporting\presentation\components\goal-progress-card\goal-progress-card.component.html
============================================================

<article
  class="goal-card"
  [class.completed]="goal.isCompleted"
  [class.failed]="goal.isFailed"
  [class.warning]="insight.statusTone === 'warning'"
  [class.danger]="insight.statusTone === 'danger'"
>
  <header class="goal-card__header">
    <p class="badge">{{ insight.statusLabel }}</p>

    <h2>{{ goal.title }}</h2>

    <div class="goal-meta">
      <span>{{ goal.scopeName }}</span>
      <span>{{ activeWindowLabel() }}</span>
      <span>{{ 'reporting.goals.limitLabel' | translate: { value: formatNumber(goal.targetKilowattHours, 2) } }}</span>
      <span>{{ 'reporting.goals.deadlineLabel' | translate: { date: formatDate(goal.deadline) } }}</span>
      <span>{{ daysRemainingLabel() }}</span>
    </div>
  </header>

  <section class="goal-progress">
    <div class="progress-text">
      <span>{{ 'reporting.goals.measured' | translate }}</span>
      <strong>{{ formatNumber(insight.measuredKilowattHours, 4) }} kWh</strong>
    </div>

    <div class="progress-bar">
      <span class="progress-bar__actual" [style.width.%]="progressPercentage"></span>
      <i [style.left.%]="insight.expectedProgressPercentage"></i>
    </div>

    <div class="progress-scale">
      <span>{{ 'reporting.goals.usedPercentage' | translate: { value: formatNumber(progressPercentage, 1) } }}</span>
      <strong>{{ 'reporting.goals.availableKwh' | translate: { value: formatNumber(remainingKilowattHours, 2) } }}</strong>
    </div>
  </section>

  <section class="goal-facts">
    <div>
      <span>{{ 'about.capabilities.items.energy.eyebrow' | translate }}</span>
      <strong>{{ formatNumber(insight.measuredKilowattHours, 4) }} kWh</strong>
    </div>

    <div>
      <span>{{ 'energyDashboard.cost' | translate }}</span>
      <strong>S/ {{ formatNumber(insight.estimatedCost, 2) }}</strong>
    </div>

    <div>
      <span>{{ 'reporting.goals.devices' | translate }}</span>
      <strong>{{ insight.devicesMeasured }}</strong>
    </div>

    <div>
      <span>{{ 'reporting.goals.projection' | translate }}</span>
      <strong>{{ formatNumber(projectedPercentage, 1) }}%</strong>
    </div>
  </section>

  <footer class="goal-card__footer">
    <p>{{ insight.recommendation }}</p>

    <app-button
      class="danger-action"
      variant="danger"
      size="sm"
      (pressed)="onRemove()"
    >
      {{ 'common.delete' | translate }}
    </app-button>
  </footer>
</article>


============================================================
FILE: src\app\reporting\presentation\components\goal-progress-card\goal-progress-card.component.scss
============================================================

.goal-card {
  display: grid;
  gap: 18px;
  padding: 22px;
  border: 1px solid var(--border-color);
  border-radius: 22px;
  background:
    radial-gradient(circle at top left, var(--ec-accent-soft), transparent 34%),
    var(--surface-card);
  box-shadow: var(--ec-inset-highlight);
}

.goal-card.completed {
  border-color: var(--ec-status-success-border);
}

.goal-card.warning {
  border-color: var(--ec-status-warning-border);
}

.goal-card.failed,
.goal-card.danger {
  border-color: var(--ec-status-danger-border);
}

.goal-card__header {
  display: grid;
  gap: 10px;
}

.badge {
  width: fit-content;
  margin: 0;
  padding: 7px 11px;
  border-radius: 999px;
  color: var(--ec-status-info-text);
  background: var(--ec-status-info-bg);
  border: 1px solid var(--ec-status-info-border);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.05em;
  text-transform: uppercase;
}

.completed .badge {
  color: var(--ec-status-success-text);
  background: var(--ec-status-success-bg);
  border-color: var(--ec-status-success-border);
}

.warning .badge {
  color: var(--ec-status-warning-text);
  background: var(--ec-status-warning-bg);
  border-color: var(--ec-status-warning-border);
}

.danger .badge,
.failed .badge {
  color: var(--ec-status-danger-text);
  background: var(--ec-status-danger-bg);
  border-color: var(--ec-status-danger-border);
}

h2 {
  margin: 0;
  color: var(--text-primary);
  font-size: 1.24rem;
  font-weight: 950;
}

.goal-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.goal-meta span {
  padding: 7px 10px;
  border-radius: 999px;
  color: var(--ec-status-info-text);
  background: var(--ec-surface-subtle);
  border: 1px solid var(--border-color);
  font-size: 0.78rem;
  font-weight: 850;
}

.goal-progress {
  display: grid;
  gap: 10px;
}

.progress-text,
.progress-scale {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  color: var(--text-secondary);
  font-size: 0.9rem;
  font-weight: 850;
}

.progress-text strong,
.progress-scale strong {
  color: var(--text-primary);
}

.progress-bar {
  position: relative;
  height: 16px;
  overflow: hidden;
  border-radius: 999px;
  background: var(--ec-surface-strong);
  border: 1px solid var(--border-color);
}

.progress-bar__actual {
  display: block;
  height: 100%;
  border-radius: inherit;
  background: var(--ec-slider-track);
  box-shadow: 0 0 24px var(--ec-accent-soft);
}

.danger .progress-bar__actual,
.failed .progress-bar__actual {
  background: var(--ec-danger-bg);
}

.warning .progress-bar__actual {
  background: linear-gradient(90deg, var(--ec-status-warning-bg), var(--ec-status-danger-bg));
}

.progress-bar i {
  position: absolute;
  top: 1px;
  bottom: 1px;
  width: 2px;
  border-radius: 999px;
  background: var(--text-primary);
  box-shadow: 0 0 12px var(--ec-accent-soft);
}

.goal-facts {
  display: grid;
  grid-template-columns: repeat(4, minmax(118px, 1fr));
  gap: 10px;
}

.goal-facts div {
  display: grid;
  gap: 6px;
  min-height: 72px;
  padding: 12px;
  border-radius: 16px;
  background: var(--ec-surface-subtle);
  border: 1px solid var(--border-color);
}

.goal-facts span {
  color: var(--text-muted);
  font-size: 0.76rem;
  font-weight: 900;
}

.goal-facts strong {
  color: var(--text-primary);
  font-size: 0.96rem;
  font-weight: 950;
}

.goal-card__footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
}

.goal-card__footer p {
  margin: 0;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.danger-action {
  flex: 0 0 auto;
}

@media (max-width: 860px) {
  .goal-facts {
    grid-template-columns: repeat(2, minmax(120px, 1fr));
  }

  .goal-card__footer,
  .progress-text,
  .progress-scale {
    align-items: flex-start;
    flex-direction: column;
  }
}


============================================================
FILE: src\app\reporting\presentation\components\goal-progress-card\goal-progress-card.component.ts
============================================================

import { Component, EventEmitter, Input, Output } from '@angular/core';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { EnergyGoal } from '../../../domain/model/energy-goal.entity';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';

export interface EnergyGoalInsight {
  measuredKilowattHours: number;
  estimatedCost: number;
  devicesMeasured: number;
  daysRemaining: number;
  expectedProgressPercentage: number;
  projectedKilowattHours: number;
  statusLabel: string;
  statusTone: 'good' | 'warning' | 'danger';
  recommendation: string;
}

@Component({
  selector: 'app-goal-progress-card',
  standalone: true,
  imports: [TranslateModule, AppButtonComponent],
  templateUrl: './goal-progress-card.component.html',
  styleUrls: ['./goal-progress-card.component.scss'],
})
export class GoalProgressCardComponent {
  @Input({ required: true }) goal!: EnergyGoal;
  @Input({ required: true }) insight!: EnergyGoalInsight;

  @Output() remove = new EventEmitter<number>();

  constructor(
    private readonly translate: TranslateService,
    private readonly uiPreferences: UiPreferencesService
  ) {}

  get progressPercentage(): number {
    if (this.goal.targetKilowattHours <= 0) {
      return 0;
    }

    const progress = (this.insight.measuredKilowattHours / this.goal.targetKilowattHours) * 100;
    return Math.min(100, Number(progress.toFixed(1)));
  }

  get remainingKilowattHours(): number {
    return Math.max(0, this.goal.targetKilowattHours - this.insight.measuredKilowattHours);
  }

  get projectedPercentage(): number {
    if (this.goal.targetKilowattHours <= 0) {
      return 0;
    }

    return Math.min(
      160,
      Number(((this.insight.projectedKilowattHours / this.goal.targetKilowattHours) * 100).toFixed(1))
    );
  }

  onRemove(): void {
    this.remove.emit(this.goal.id);
  }

  formatNumber(value: number, decimals = 2): string {
    return Number(value || 0).toLocaleString(this.currentLocale(), {
      minimumFractionDigits: decimals,
      maximumFractionDigits: decimals,
    });
  }

  formatDate(value: string): string {
    if (!value) {
      return this.t('reporting.emptyValue');
    }

    const [datePart] = value.split('T');
    const [year, month, day] = datePart.split('-').map(Number);
    const date = year && month && day
      ? new Date(year, month - 1, day)
      : new Date(value);

    if (Number.isNaN(date.getTime())) {
      return value;
    }

    return new Intl.DateTimeFormat(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
    }).format(date);
  }

  activeWindowLabel(): string {
    if (!this.goal.activeFrom || !this.goal.activeTo) {
      return this.t('reporting.goals.fullDay');
    }

    return `${this.goal.activeFrom} - ${this.goal.activeTo}`;
  }

  daysRemainingLabel(): string {
    return this.t('reporting.goals.daysRemaining', {
      count: this.insight.daysRemaining,
    });
  }

  private t(key: string, params?: Record<string, unknown>): string {
    this.uiPreferences.currentLanguage();

    return this.translate.instant(key, params);
  }

  private currentLocale(): string {
    const localeByLanguage = {
      es: 'es-PE',
      en: 'en-US',
      pt: 'pt-BR',
    };

    return localeByLanguage[this.uiPreferences.currentLanguage()];
  }
}


============================================================
FILE: src\app\reporting\presentation\components\monthly-comparison-chart\monthly-comparison-chart.component.html
============================================================

<app-section-card
  class="report-comparison-card"
  [title]="'reporting.monthlyComparison' | translate"
  size="large"
>
  <p class="chart-description">
    {{ 'reporting.monthlyComparisonSubtitle' | translate }}
    <strong>{{ selectedPeriodLabel() }}</strong>.
  </p>

  <div class="chart-controls">
    <span>
      {{ chartReports.length }}
      {{ 'history.records' | translate }}
    </span>

    <app-dropdown
      class="chart-period-dropdown"
      [options]="periodOptions"
      [value]="selectedPeriod"
      [placeholder]="'reporting.period' | translate"
      (valueChange)="onPeriodChange($event)"
    />
  </div>

  @if (chartReports.length > 0) {
    <app-draggable-bar-carousel
      class="report-carousel"
      [items]="chartItems"
      [selectable]="true"
      [followLatest]="true"
      (itemSelected)="selectCarouselItem($event)"
    />
  } @else {
    <app-empty-state
      [compact]="true"
      [eyebrow]="'reporting.monthlyComparison' | translate"
      [title]="'reporting.emptyReportsTitle' | translate"
      [description]="'reporting.emptyReportsContent' | translate"
    />
  }
</app-section-card>


============================================================
FILE: src\app\reporting\presentation\components\monthly-comparison-chart\monthly-comparison-chart.component.scss
============================================================

:host {
  display: block;
}

.report-comparison-card {
  display: block;
}

:host ::ng-deep .report-comparison-card .section-card {
  position: relative;
  overflow: visible;
}

:host ::ng-deep .report-comparison-card .section-card > h3 {
  max-width: min(100%, 620px);
}

.chart-description {
  max-width: min(100%, 620px);
  margin: -4px 0 20px;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.chart-description strong {
  color: var(--text-primary);
  font-weight: 900;
}

.chart-controls {
  position: absolute;
  top: 20px;
  right: 20px;
  display: flex;
  align-items: center;
  gap: 12px;
  z-index: 3;
}

.chart-controls span {
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  white-space: nowrap;
}

.chart-period-dropdown {
  width: 170px;
}

.report-carousel {
  display: block;
}

.report-comparison-card app-empty-state {
  display: block;
  margin-top: 12px;
}

@media (max-width: 760px) {
  .chart-controls {
    position: static;
    align-items: stretch;
    flex-direction: column;
    margin: 0 0 18px;
  }

  .chart-period-dropdown {
    width: 100%;
  }
}


============================================================
FILE: src\app\reporting\presentation\components\monthly-comparison-chart\monthly-comparison-chart.component.ts
============================================================

import { Component, EventEmitter, Input, OnDestroy, Output } from '@angular/core';
import { TranslateModule, TranslateService } from '@ngx-translate/core';
import { Subscription } from 'rxjs';

import {
  ConsumptionReport,
  ConsumptionReportPeriod,
} from '../../../domain/model/consumption-report.entity';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { SectionCardComponent } from '../../../../shared/presentation/components/section-card/section-card.component';
import {
  DraggableBarCarouselComponent,
  DraggableBarCarouselItem,
} from '../../../../shared/presentation/components/draggable-bar-carousel/draggable-bar-carousel.component';
import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';

@Component({
  selector: 'app-monthly-comparison-chart',
  standalone: true,
  imports: [
    EmptyStateComponent,
    TranslateModule,
    AppDropdownComponent,
    SectionCardComponent,
    DraggableBarCarouselComponent,
  ],
  templateUrl: './monthly-comparison-chart.component.html',
  styleUrls: ['./monthly-comparison-chart.component.scss'],
})
export class MonthlyComparisonChartComponent implements OnDestroy {
  private _reports: ConsumptionReport[] = [];

  @Input()
  set reports(value: ConsumptionReport[] | null) {
    this._reports = value ?? [];
    this.syncChart();
  }

  get reports(): ConsumptionReport[] {
    return this._reports;
  }

  @Output() reportSelected = new EventEmitter<ConsumptionReport>();

  selectedPeriod: ConsumptionReportPeriod = 'MONTHLY';
  chartReports: ConsumptionReport[] = [];
  chartItems: DraggableBarCarouselItem[] = [];
  private chartMaxWatts = 1;
  private readonly languageChangeSubscription: Subscription;

  readonly periodOptions: DropdownOption[] = [
    { label: 'Diarios', labelKey: 'reporting.periodOptions.daily', value: 'DAILY' },
    { label: 'Semanales', labelKey: 'reporting.periodOptions.weekly', value: 'WEEKLY' },
    { label: 'Mensuales', labelKey: 'reporting.periodOptions.monthly', value: 'MONTHLY' },
    { label: 'Anuales', labelKey: 'reporting.periodOptions.yearly', value: 'YEARLY' },
  ];

  constructor(
    private readonly translate: TranslateService,
    private readonly uiPreferences: UiPreferencesService
  ) {
    this.languageChangeSubscription = this.translate.onLangChange.subscribe(() => {
      this.syncChart();
    });
  }

  ngOnDestroy(): void {
    this.languageChangeSubscription.unsubscribe();
  }

  get filteredReports(): ConsumptionReport[] {
    return this.reports.filter((report) => report.period === this.selectedPeriod);
  }

  getBarPercentage(totalWatts: number): number {
    return Math.max(8, Math.round((totalWatts / this.chartMaxWatts) * 100));
  }

  selectReport(report: ConsumptionReport): void {
    this.reportSelected.emit(report);
  }

  selectCarouselItem(item: DraggableBarCarouselItem): void {
    const report = this.chartReports.find((entry) => String(entry.id) === item.key);

    if (report) {
      this.selectReport(report);
    }
  }

  onPeriodChange(value: string): void {
    if (!this.isReportPeriod(value) || value === this.selectedPeriod) {
      return;
    }

    this.selectedPeriod = value;
    this.syncChart();
  }

  selectedPeriodLabel(): string {
    return this.t(this.periodLabelKey(this.selectedPeriod)).toLocaleLowerCase(
      this.currentLocale()
    );
  }

  formatWatts(value: number): string {
    if (value >= 1000) {
      return `${this.formatNumber(value / 1000, 2)} kW`;
    }

    return `${this.formatNumber(value, 2)} W`;
  }

  private formatReportCaption(report: ConsumptionReport): string {
    if (this.selectedPeriod === 'YEARLY') {
      return report.startDate.slice(0, 4);
    }

    return report.generatedAt.slice(5);
  }

  private formatDate(value: string): string {
    const [datePart] = value.split('T');
    const [year, month, day] = datePart.split('-');

    if (!year || !month || !day) {
      return value;
    }

    return new Intl.DateTimeFormat(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
    }).format(new Date(Number(year), Number(month) - 1, Number(day)));
  }

  private syncChart(): void {
    this.chartReports = [...this.filteredReports].sort(
      (first, second) => this.reportTime(first) - this.reportTime(second)
    );
    this.chartMaxWatts = Math.max(
      ...this.chartReports.map((report) => report.totalWatts),
      1
    );
    this.chartItems = this.chartReports.map((report) => ({
      key: String(report.id),
      value: this.formatWatts(report.totalWatts),
      caption: this.formatReportCaption(report),
      percentage: this.getBarPercentage(report.totalWatts),
      title: report.title || this.t('reporting.reportFallbackTitleWithId', { id: report.id }),
      subtitle: `${this.formatDate(report.startDate)} - ${this.formatDate(report.endDate)}`,
      detail: this.t('reporting.averageDetail', {
        value: this.formatWatts(report.averageWatts),
      }),
      ariaLabel: this.t('reporting.reportChartAria', {
        title: report.title || this.t('reporting.reportFallbackTitleWithId', { id: report.id }),
        total: this.formatWatts(report.totalWatts),
        start: this.formatDate(report.startDate),
        end: this.formatDate(report.endDate),
      }),
    }));
  }

  private periodLabelKey(period: ConsumptionReportPeriod): string {
    return `reporting.periodOptions.${period.toLowerCase()}`;
  }

  private t(key: string, params?: Record<string, unknown>): string {
    this.uiPreferences.currentLanguage();

    return this.translate.instant(key, params);
  }

  private currentLocale(): string {
    const localeByLanguage = {
      es: 'es-PE',
      en: 'en-US',
      pt: 'pt-BR',
    };

    return localeByLanguage[this.uiPreferences.currentLanguage()];
  }

  private formatNumber(value: number, decimals: number): string {
    return Number(value || 0).toLocaleString(this.currentLocale(), {
      minimumFractionDigits: decimals,
      maximumFractionDigits: decimals,
    });
  }

  private reportTime(report: ConsumptionReport): number {
    const startTime = new Date(report.startDate).getTime();
    const generatedTime = new Date(report.generatedAt).getTime();

    if (!Number.isNaN(startTime)) {
      return startTime;
    }

    return Number.isNaN(generatedTime) ? report.id : generatedTime;
  }

  private isReportPeriod(value: string): value is ConsumptionReportPeriod {
    return ['DAILY', 'WEEKLY', 'MONTHLY', 'YEARLY'].includes(value);
  }
}


============================================================
FILE: src\app\reporting\presentation\components\report-card\report-card.component.html
============================================================

<article class="report-card">
  <div class="report-cell report-cell--identity">
    <div class="report-kicker">
      <p class="badge">{{ periodLabel(report.period) }}</p>
      <span class="report-id">#{{
        report.id.toString().padStart(4, '0')
      }}</span>
    </div>

    <h2>{{ getCleanTitle() }}</h2>
  </div>

  <div class="report-cell report-cell--date">
    <span>{{ 'reporting.from' | translate }}</span>
    <strong>{{ formatDate(report.startDate) }}</strong>
  </div>

  <div class="report-cell report-cell--date">
    <span>{{ 'reporting.to' | translate }}</span>
    <strong>{{ formatDate(report.endDate) }}</strong>
  </div>

  <div class="report-cell report-cell--date">
    <span>{{ 'reporting.generated' | translate }}</span>
    <strong>{{ formatDate(report.generatedAt) }}</strong>
  </div>

  <div class="metrics">
    <div class="report-cell">
      <span>{{ 'reporting.totalWatts' | translate }}</span>
      <strong>{{ formatWatts(report.totalWatts) }}</strong>
    </div>

    <div class="report-cell">
      <span>{{ 'reporting.averageWatts' | translate }}</span>
      <strong>{{ formatWatts(report.averageWatts) }}</strong>
    </div>

    <div class="report-cell">
      <span>{{ 'reporting.highestWatts' | translate }}</span>
      <strong>{{ formatWatts(report.highestWatts) }}</strong>
    </div>
  </div>

  <div class="actions">
    <app-button variant="secondary" size="sm" (pressed)="onExport()">
      {{ 'reporting.export' | translate }}
    </app-button>

    <app-button variant="danger" size="sm" (pressed)="onRemove()">
      {{ 'common.delete' | translate }}
    </app-button>
  </div>
</article>


============================================================
FILE: src\app\reporting\presentation\components\report-card\report-card.component.scss
============================================================

.report-card {
  display: grid;
  grid-template-columns:
    minmax(180px, 0.95fr)
    minmax(115px, 0.55fr)
    minmax(115px, 0.55fr)
    minmax(115px, 0.55fr)
    minmax(320px, 1.55fr)
    auto;
  gap: 0;
  align-items: stretch;
  overflow: hidden;
  border: 1px solid var(--border-color);
  border-radius: 24px;
  background: var(--surface-card);
  box-shadow:
    var(--ec-shadow-soft),
    var(--ec-inset-highlight);
}

.report-cell,
.actions {
  min-width: 0;
  display: grid;
  align-content: center;
  gap: 6px;
  padding: 18px;
  border-left: 1px solid var(--border-color);
}

.report-cell--identity {
  border-left: 0;
  gap: 10px;
  background: var(--ec-surface-subtle);
}

.report-cell--date {
  padding-inline: 14px;
}

.report-cell--date strong {
  overflow: visible;
  text-overflow: clip;
  white-space: nowrap;
}

.report-kicker {
  display: flex;
  align-items: center;
  gap: 10px;
  min-width: 0;
}

.badge {
  width: fit-content;
  margin: 0;
  padding: 7px 11px;
  border-radius: 999px;
  color: var(--text-on-action);
  background: var(--ec-action-bg);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.02em;
}

.report-id {
  color: var(--accent-color);
  font-size: 0.78rem;
  font-weight: 950;
}

h2 {
  margin: 0;
  color: var(--text-primary);
  font-size: clamp(1.05rem, 1.25vw, 1.24rem);
  font-weight: 950;
  line-height: 1.2;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.report-cell span {
  color: var(--text-muted);
  font-size: 0.72rem;
  font-weight: 900;
  letter-spacing: 0.02em;
}

.report-cell strong {
  color: var(--text-primary);
  font-size: 0.88rem;
  font-weight: 950;
  line-height: 1.12;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.metrics {
  min-width: 0;
  display: grid;
  grid-template-columns:
    minmax(120px, 1.2fr)
    minmax(100px, 1fr)
    minmax(100px, 1fr);
}

.metrics .report-cell {
  border-left: 1px solid var(--border-color);
  padding-inline: 14px;
}

.metrics .report-cell strong {
  overflow: visible;
  text-overflow: clip;
  white-space: nowrap;
}

.actions {
  grid-template-columns: repeat(2, auto);
  align-content: center;
  align-items: center;
  gap: 10px;
  min-width: 190px;
  background: var(--ec-surface-subtle);
}

.actions app-button {
  min-width: 88px;
}

@media (max-width: 1260px) {
  .report-card {
    grid-template-columns: minmax(260px, 1fr) minmax(360px, 1fr) auto;
  }

  .report-card > .report-cell:not(.report-cell--identity) {
    display: none;
  }

  .metrics {
    border-left: 1px solid var(--border-color);
  }
}

@media (max-width: 900px) {
  .report-card {
    grid-template-columns: 1fr;
  }

  .report-card > .report-cell:not(.report-cell--identity) {
    display: grid;
  }

  .metrics,
  .actions {
    border-left: 0;
    border-top: 1px solid var(--border-color);
  }
}

@media (max-width: 640px) {
  .metrics,
  .actions {
    grid-template-columns: 1fr;
  }
}


============================================================
FILE: src\app\reporting\presentation\components\report-card\report-card.component.ts
============================================================

import { Component, EventEmitter, Input, Output } from '@angular/core';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import {
  ConsumptionReport,
  ConsumptionReportPeriod,
} from '../../../domain/model/consumption-report.entity';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';

@Component({
  selector: 'app-report-card',
  standalone: true,
  imports: [TranslateModule, AppButtonComponent],
  templateUrl: './report-card.component.html',
  styleUrls: ['./report-card.component.scss'],
})
export class ReportCardComponent {
  @Input({ required: true }) report!: ConsumptionReport;

  @Output() exported = new EventEmitter<number>();
  @Output() remove = new EventEmitter<number>();

  constructor(
    private readonly translate: TranslateService,
    private readonly uiPreferences: UiPreferencesService
  ) {}

  onExport(): void {
    this.exported.emit(this.report.id);
  }

  onRemove(): void {
    this.remove.emit(this.report.id);
  }

  getCleanTitle(): string {
    const title = this.report.title?.trim();

    if (!title) {
      return this.t('reporting.reportFallbackTitle');
    }

    return title
      .replace(/\s*\|\s*\d{4}-\d{2}-\d{2}\s*\/\s*\d{4}-\d{2}-\d{2}.*/g, '')
      .replace(/\s*\|\s*\d{2}\/\d{2}\/\d{4}\s*\/\s*\d{2}\/\d{2}\/\d{4}.*/g, '')
      .trim();
  }

  periodLabel(period: ConsumptionReportPeriod): string {
    return this.t(`reporting.periods.${period.toLowerCase()}`);
  }

  formatWatts(value: number): string {
    if (value >= 1000) {
      return `${(value / 1000).toLocaleString(this.currentLocale(), {
        minimumFractionDigits: 2,
        maximumFractionDigits: 2,
      })} kW`;
    }

    return `${value.toLocaleString(this.currentLocale(), {
      minimumFractionDigits: 2,
      maximumFractionDigits: 2,
    })} W`;
  }

  formatDate(value: string): string {
    if (!value) {
      return this.t('reporting.emptyValue');
    }

    const date = this.dateFromInput(value);

    if (Number.isNaN(date.getTime())) {
      return value;
    }

    return new Intl.DateTimeFormat(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
    }).format(date);
  }

  private t(key: string, params?: Record<string, unknown>): string {
    this.uiPreferences.currentLanguage();

    return this.translate.instant(key, params);
  }

  private currentLocale(): string {
    const localeByLanguage = {
      es: 'es-PE',
      en: 'en-US',
      pt: 'pt-BR',
    };

    return localeByLanguage[this.uiPreferences.currentLanguage()];
  }

  private dateFromInput(value: string): Date {
    const [datePart] = value.split('T');
    const [year, month, day] = datePart.split('-').map(Number);

    if (!year || !month || !day) {
      return new Date(value);
    }

    return new Date(year, month - 1, day);
  }
}


============================================================
FILE: src\app\reporting\presentation\pages\energy-goals\energy-goals-page.component.html
============================================================

<section class="reporting-page goals-page">
  <header class="ec-page-heading goals-hero">
    <p class="ec-page-eyebrow">
      {{ 'reporting.goalsEyebrow' | translate }}
    </p>

    <h1 class="ec-page-title">
      {{ 'reporting.goalsTitle' | translate }}
    </h1>

    <p class="ec-page-description">
      {{ 'reporting.goalsSubtitle' | translate }}
    </p>
  </header>

  <section class="summary-grid goals-summary">
    <article>
      <span>{{ 'reporting.totalGoals' | translate }}</span>
      <strong>{{ reportingFacade.energyGoals().length }}</strong>
      <small>{{ 'reporting.goals.summary.created' | translate }}</small>
    </article>

    <article>
      <span>{{ 'reporting.activeGoals' | translate }}</span>
      <strong>{{ reportingFacade.activeGoals().length }}</strong>
      <small>{{ 'reporting.goals.summary.tracking' | translate }}</small>
    </article>

    <article>
      <span>{{ 'reporting.goals.summary.atRisk' | translate }}</span>
      <strong>{{ atRiskGoalsCount }}</strong>
      <small>{{ 'reporting.goals.summary.needAttention' | translate }}</small>
    </article>

    <article>
      <span>{{ 'reporting.goals.summary.averageCompliance' | translate }}</span>
      <strong>{{ averageProgressPercentage | number: '1.0-1' }}%</strong>
      <small>{{ projectedSavingsLabel }}</small>
    </article>
  </section>

  <section class="goal-create-strip">
    <div>
      <h2>{{ 'reporting.goals.createStripTitle' | translate }}</h2>
      <p>{{ 'reporting.goals.createStripDescription' | translate }}</p>
    </div>

    <app-button variant="create" (pressed)="openCreateGoalModal()">
      {{ 'reporting.createGoal' | translate }}
    </app-button>
  </section>

  <app-modal-form-shell
    [eyebrow]="'reporting.goals.modalEyebrow' | translate"
    [title]="'reporting.goals.createStripTitle' | translate"
    [description]="'reporting.goals.modalDescription' | translate"
    [open]="createModalOpen"
    (closed)="closeCreateGoalModal()"
  >
    <form class="goal-form" (ngSubmit)="createGoal()">
      <div class="field-block">
        <span>{{ 'reporting.goals.scope' | translate }}</span>
        <app-dropdown
          [options]="scopeOptions"
          [value]="scopeType"
          [placeholder]="'reporting.goals.scopeTargetPlaceholders.general' | translate"
          (valueChange)="onScopeChange($event)"
        />
      </div>

      <div class="field-block">
        <span>{{ scopeTargetLabel }}</span>
        <app-dropdown
          [options]="scopeTargetOptions"
          [value]="scopeTargetId"
          [placeholder]="scopeTargetPlaceholder"
          (valueChange)="onScopeTargetChange($event)"
        />
      </div>

      <label class="field-block field-block--title">
        <span>{{ 'reporting.goals.goalName' | translate }}</span>
        <input
          type="text"
          name="title"
          [(ngModel)]="title"
          [placeholder]="'reporting.goals.goalNamePlaceholder' | translate"
          required
        />
      </label>

      <div class="field-block">
        <span>{{ 'reporting.goals.consumptionLimit' | translate }}</span>
        <app-number-stepper
          label=""
          suffix="kWh"
          placeholder="kWh"
          [min]="0.1"
          [step]="0.1"
          [decimals]="2"
          [allowEmpty]="true"
          [(value)]="targetKilowattHours"
        />
      </div>

      <app-time-picker
        [label]="'reporting.from' | translate"
        [(value)]="activeFrom"
      />

      <app-time-picker
        [label]="'reporting.to' | translate"
        [(value)]="activeTo"
      />

      <div class="field-block">
        <span>{{ 'reporting.deadline' | translate }}</span>
        <app-date-picker
          label=""
          [min]="minDeadline()"
          [(value)]="deadline"
        />
      </div>
    </form>

    <div modal-actions class="goal-modal-actions">
      <app-button variant="secondary" (pressed)="closeCreateGoalModal()">
        {{ 'common.cancel' | translate }}
      </app-button>

      <app-button
        variant="create"
        [disabled]="!canCreateGoal"
        (pressed)="createGoal()"
      >
        {{ 'reporting.createGoal' | translate }}
      </app-button>
    </div>
  </app-modal-form-shell>

  @if (reportingFacade.loading()) {
    <div class="loading-wrapper">
      <app-loading-spinner />
    </div>
  } @else {
    <section class="list goals-list">
      @for (goal of reportingFacade.energyGoals(); track goal.id) {
        <app-goal-progress-card
          [goal]="goal"
          [insight]="getGoalInsight(goal)"
          (remove)="deleteGoal($event)"
        />
      } @empty {
        <app-empty-state
          [eyebrow]="'reporting.goals.emptyEyebrow' | translate"
          [title]="'reporting.emptyGoalsTitle' | translate"
          [description]="'reporting.emptyGoalsContent' | translate"
        />
      }
    </section>
  }
</section>


============================================================
FILE: src\app\reporting\presentation\pages\energy-goals\energy-goals-page.component.scss
============================================================

.reporting-page,
.list {
  display: grid;
}

.reporting-page {
  gap: 24px;
}

.summary-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 18px;
}

.summary-grid article {
  padding: 20px;
  border: 1px solid var(--border-color);
  border-radius: 22px;
  background: var(--surface-color);
}

.summary-grid span {
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
}

.summary-grid strong {
  display: block;
  margin-top: 8px;
  font-size: 1.8rem;
}

.list {
  gap: 14px;
}

.loading-wrapper {
  min-height: 220px;
  display: grid;
  place-items: center;
}

.goals-page {
  gap: 22px;
}

.goals-hero {
  max-width: 860px;
}

.goals-summary {
  grid-template-columns: repeat(4, minmax(0, 1fr));
}

.goals-summary article {
  min-height: 112px;
}

.goals-summary small {
  color: var(--ec-copy-color);
  font-size: 0.78rem;
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.goal-create-strip {
  display: flex;
  justify-content: space-between;
  gap: 18px;
  align-items: center;
  border: 1px solid var(--border-color);
  border-radius: 20px;
  padding: 20px;
  background: var(--surface-color);
}

.goal-create-strip div {
  display: grid;
  gap: 6px;
  min-width: 0;
}

.goal-create-strip h2,
.goal-create-strip p {
  margin: 0;
}

.goal-create-strip h2 {
  color: var(--text-primary);
  font-size: 1.1rem;
}

.goal-create-strip p {
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.goal-builder {
  gap: 18px;
}

.goal-form {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
  align-items: end;
}

.field-block {
  display: grid;
  gap: 8px;
  min-width: 0;
}

.field-block > span {
  display: block;
  color: var(--text-primary);
  font-size: 0.92rem;
  font-weight: 950;
}

.field-block input {
  width: 100%;
  min-height: 62px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  padding: 0 18px;
  color: var(--text-primary);
  background: var(--field-bg);
  outline: none;
  font: inherit;
  font-weight: 850;
}

.field-block input::placeholder {
  color: var(--text-muted);
}

.goal-form app-dropdown,
.goal-form app-date-picker,
.goal-form app-number-stepper,
.goal-form app-time-picker {
  min-width: 0;
}

:host ::ng-deep .goal-form .dropdown__trigger,
:host ::ng-deep .goal-form .date-picker__trigger,
:host ::ng-deep .goal-form .number-stepper__body {
  min-height: 62px;
  border-radius: 18px;
}

:host ::ng-deep .goal-form .number-stepper input {
  height: 60px;
}

:host ::ng-deep .goal-form .number-stepper__buttons {
  height: 60px;
}

.goals-list {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}

.goals-list app-empty-state {
  grid-column: 1 / -1;
  width: 100%;
}

.goal-modal-actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  width: 100%;
}

@media (max-width: 1180px) {
  .goals-summary,
  .goals-list {
    grid-template-columns: 1fr 1fr;
  }

  .goal-create-strip {
    align-items: flex-start;
    flex-direction: column;
  }
}

@media (max-width: 760px) {
  .goals-summary,
  .goals-list,
  .goal-form {
    grid-template-columns: 1fr;
  }

  .goal-modal-actions {
    flex-direction: column-reverse;
  }
}


============================================================
FILE: src\app\reporting\presentation\pages\energy-goals\energy-goals-page.component.ts
============================================================

import { DecimalPipe } from '@angular/common';
import { Component, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { DeviceControlFacade } from '../../../../device-control/application/services/device-control.facade';
import { EnergyReading } from '../../../../energy-monitoring/domain/model/energy-reading.entity';
import { ConfirmDialogService } from '../../../../shared/application/services/confirm-dialog.service';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';
import { ActiveWorkplaceContextService } from '../../../../workplace/application/services/active-workplace-context.service';
import { WorkplaceFacade } from '../../../../workplace/application/services/workplace.facade';
import { AppDatePickerComponent } from '../../../../shared/presentation/components/app-date-picker/app-date-picker.component';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { AppNumberStepperComponent } from '../../../../shared/presentation/components/app-number-stepper/app-number-stepper.component';
import { AppTimePickerComponent } from '../../../../shared/presentation/components/app-time-picker/app-time-picker.component';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { ModalFormShellComponent } from '../../../../shared/presentation/components/modal-form-shell/modal-form-shell.component';

import { ReportingFacade } from '../../../application/services/reporting.facade';
import { EnergyGoal, EnergyGoalScopeType } from '../../../domain/model/energy-goal.entity';

import {
  EnergyGoalInsight,
  GoalProgressCardComponent,
} from '../../components/goal-progress-card/goal-progress-card.component';

@Component({
  selector: 'app-energy-goals-page',
  standalone: true,
  imports: [
    FormsModule,
    DecimalPipe,
    TranslateModule,
    LoadingSpinnerComponent,
    GoalProgressCardComponent,
    AppDatePickerComponent,
    AppNumberStepperComponent,
    AppDropdownComponent,
    AppTimePickerComponent,
    AppButtonComponent,
    EmptyStateComponent,
    ModalFormShellComponent,
  ],
  templateUrl: './energy-goals-page.component.html',
  styleUrls: ['./energy-goals-page.component.scss'],
})
export class EnergyGoalsPageComponent implements OnInit {
  createModalOpen = false;
  title = '';
  targetKilowattHours: number | null = null;
  deadline = '';
  scopeType: EnergyGoalScopeType = 'WORKPLACE';
  scopeTargetId = '';
  activeFrom = '00:00';
  activeTo = '00:00';

  readonly scopeOptions: DropdownOption[] = [
    {
      label: 'Una sede',
      labelKey: 'reporting.goals.scopeOptions.workplace',
      value: 'WORKPLACE',
      description: 'Mide dispositivos asignados a un centro energetico.',
      descriptionKey: 'reporting.goals.scopeDescriptions.workplace',
    },
    {
      label: 'Una habitacion',
      labelKey: 'reporting.goals.scopeOptions.room',
      value: 'ROOM',
      description: 'Mide dispositivos asignados a una habitacion.',
      descriptionKey: 'reporting.goals.scopeDescriptions.room',
    },
    {
      label: 'Un grupo',
      labelKey: 'reporting.goals.scopeOptions.group',
      value: 'GROUP',
      description: 'Mide los dispositivos incluidos en un grupo.',
      descriptionKey: 'reporting.goals.scopeDescriptions.group',
    },
    {
      label: 'Un dispositivo',
      labelKey: 'reporting.goals.scopeOptions.device',
      value: 'DEVICE',
      description: 'Mide solo las lecturas de un equipo.',
      descriptionKey: 'reporting.goals.scopeDescriptions.device',
    },
  ];

  constructor(
    readonly reportingFacade: ReportingFacade,
    readonly deviceControlFacade: DeviceControlFacade,
    readonly workplaceFacade: WorkplaceFacade,
    private readonly activeWorkplaceContext: ActiveWorkplaceContextService,
    private readonly confirmDialogService: ConfirmDialogService,
    private readonly toastService: ToastService,
    private readonly translate: TranslateService,
    private readonly uiPreferences: UiPreferencesService
  ) {}

  async ngOnInit(): Promise<void> {
    await Promise.all([
      this.reportingFacade.loadReporting(),
      this.workplaceFacade.loadWorkplace(),
      this.deviceControlFacade.loadDeviceControl(),
    ]);

    this.activeWorkplaceContext.ensureActiveLocation(this.workplaceFacade.locations());

    if (this.reportingFacade.error()) {
      this.toastService.error(this.t('reporting.goals.toasts.loadError'));
    }
  }

  async createGoal(): Promise<void> {
  const title = this.title.trim();

  if (!title) {
    this.toastService.warning(this.t('reporting.goals.toasts.nameRequired'));
    return;
  }

  if (title.length < 4) {
    this.toastService.warning(this.t('reporting.goals.toasts.nameMinLength'));
    return;
  }

  if (!this.targetKilowattHours || this.targetKilowattHours <= 0) {
    this.toastService.warning(this.t('reporting.goals.toasts.targetRequired'));
    return;
  }

  if (!this.deadline) {
    this.toastService.warning(this.t('reporting.goals.toasts.deadlineRequired'));
    return;
  }

  if (this.isPastDate(this.deadline)) {
    this.toastService.warning(this.t('reporting.goals.toasts.deadlinePast'));
    return;
  }

  if (this.activeFrom === this.activeTo) {
    this.toastService.warning(this.t('reporting.goals.toasts.timeWindowInvalid'));
    return;
  }

  const scopeId = this.resolveScopeId();
  const scopeName = this.resolveScopeName(scopeId);

  if (this.scopeType !== 'GENERAL' && !scopeId) {
    this.toastService.warning(this.t('reporting.goals.toasts.scopeTargetRequired'));
    return;
  }

  const success = await this.reportingFacade.createEnergyGoal({
    title,
    targetKilowattHours: Number(this.targetKilowattHours),
    deadline: this.deadline,
    scopeType: this.scopeType,
    scopeId,
    scopeName,
    activeFrom: this.activeFrom,
    activeTo: this.activeTo,
  });

  if (success) {
    this.resetGoalForm();
    this.createModalOpen = false;
    this.toastService.success(this.t('reporting.goalCreateSuccess'));
    return;
  }

  this.toastService.error(this.t('reporting.goalCreateError'));
}

  async deleteGoal(goalId: number): Promise<void> {
  const confirmed = await this.confirmDialogService.confirm({
    title: this.t('reporting.goals.deleteDialog.title'),
    message: this.t('reporting.goals.deleteDialog.message'),
    confirmLabel: this.t('reporting.goals.deleteDialog.confirm'),
    cancelLabel: this.t('reporting.goals.deleteDialog.cancel'),
    tone: 'danger',
  });

  if (!confirmed) {
    return;
  }

  const success = await this.reportingFacade.deleteEnergyGoal(goalId);

  if (success) {
    this.toastService.info(this.t('reporting.goals.toasts.deleteSuccess'));
    return;
  }

  this.toastService.error(this.t('reporting.goalDeleteError'));
}

  openCreateGoalModal(): void {
    if (!this.scopeTargetId) {
      this.scopeTargetId = this.defaultScopeTargetId(this.scopeType);
    }

    this.createModalOpen = true;
  }

  closeCreateGoalModal(): void {
    this.createModalOpen = false;
  }

  onScopeChange(value: string): void {
    this.scopeType = value as EnergyGoalScopeType;
    this.scopeTargetId = this.defaultScopeTargetId(this.scopeType);
  }

  onScopeTargetChange(value: string): void {
    this.scopeTargetId = value;
  }

  get scopeTargetLabel(): string {
    const labels: Record<EnergyGoalScopeType, string> = {
      GENERAL: 'reporting.goals.scopeTargetLabels.general',
      WORKPLACE: 'reporting.goals.scopeTargetLabels.workplace',
      ROOM: 'reporting.goals.scopeTargetLabels.room',
      DEVICE: 'reporting.goals.scopeTargetLabels.device',
      GROUP: 'reporting.goals.scopeTargetLabels.group',
    };

    return this.t(labels[this.scopeType]);
  }

  get scopeTargetPlaceholder(): string {
    const placeholders: Record<EnergyGoalScopeType, string> = {
      GENERAL: 'reporting.goals.scopeTargetPlaceholders.general',
      WORKPLACE: 'reporting.goals.scopeTargetPlaceholders.workplace',
      ROOM: 'reporting.goals.scopeTargetPlaceholders.room',
      DEVICE: 'reporting.goals.scopeTargetPlaceholders.device',
      GROUP: 'reporting.goals.scopeTargetPlaceholders.group',
    };

    return this.t(placeholders[this.scopeType]);
  }

  get scopeTargetOptions(): DropdownOption[] {
    const activeLocationId = this.activeWorkplaceContext.activeLocationId();
    const activeDeviceIds = this.deviceIdsForLocation(activeLocationId);

    if (this.scopeType === 'WORKPLACE') {
      return this.workplaceFacade
        .locations()
        .filter((location) => !activeLocationId || location.id === activeLocationId)
        .map((location) => ({
          label: location.name,
          value: String(location.id),
          description: location.address,
        }));
    }

    if (this.scopeType === 'ROOM') {
      return this.workplaceFacade
        .rooms()
        .filter((room) => !activeLocationId || room.locationId === activeLocationId)
        .map((room) => ({
          label: room.name,
          value: String(room.id),
          description: `${this.workplaceFacade.getLocationName(room.locationId)} - ${room.floor}`,
        }));
    }

    if (this.scopeType === 'DEVICE') {
      return this.deviceControlFacade
        .devices()
        .filter((device) => !activeLocationId || activeDeviceIds.has(device.id))
        .map((device) => ({
          label: device.name,
          value: String(device.id),
          description: `${device.room} - ${device.powerWatts} W - ${device.status}`,
        }));
    }

    if (this.scopeType === 'GROUP') {
      return this.deviceControlFacade
        .deviceGroups()
        .filter((group) => !activeLocationId || group.deviceIds.some((deviceId) => activeDeviceIds.has(deviceId)))
        .map((group) => ({
          label: group.name,
          value: String(group.id),
          description: this.t('reporting.goals.deviceCountDescription', {
            count: group.deviceCount,
          }),
          disabled: !group.hasDevices,
        }));
    }

    return [];
  }

  get selectedScopeId(): number | null {
    if (this.scopeType === 'GENERAL' || !this.scopeTargetId) {
      return null;
    }

    return Number(this.scopeTargetId);
  }

  get selectedScopeName(): string {
    if (this.scopeType === 'WORKPLACE') {
      return this.workplaceFacade
        .locations()
        .find((location) => location.id === this.selectedScopeId)?.name ??
        this.t('reporting.goals.scopeTargetLabels.workplace');
    }

    if (this.scopeType === 'ROOM') {
      return this.workplaceFacade
        .rooms()
        .find((room) => room.id === this.selectedScopeId)?.name ??
        this.t('reporting.goals.scopeTargetLabels.room');
    }

    if (this.scopeType === 'DEVICE') {
      return this.deviceControlFacade
            .devices()
        .find((device) => device.id === this.selectedScopeId)?.name ??
        this.t('reporting.goals.scopeTargetLabels.device');
    }

    if (this.scopeType === 'GROUP') {
      return this.deviceControlFacade
        .deviceGroups()
        .find((group) => group.id === this.selectedScopeId)?.name ??
        this.t('reporting.goals.scopeTargetLabels.group');
    }

    return this.t('reporting.goals.generalOperation');
  }

  get canCreateGoal(): boolean {
    const hasScopeTarget = !!this.scopeTargetId;
    return Boolean(
      !this.reportingFacade.loading() &&
      this.title.trim() &&
      this.targetKilowattHours &&
      this.deadline &&
      this.activeFrom &&
      this.activeTo &&
      hasScopeTarget
    );
  }

  get atRiskGoalsCount(): number {
    return this.reportingFacade
      .energyGoals()
      .filter((goal) => this.getGoalInsight(goal).statusTone !== 'good').length;
  }

  get averageProgressPercentage(): number {
    const goals = this.reportingFacade.energyGoals();

    if (!goals.length) {
      return 0;
    }

    const totalProgress = goals.reduce(
      (total, goal) => total + this.getProgressPercentage(goal),
      0
    );

    return Number((totalProgress / goals.length).toFixed(1));
  }

  get projectedSavingsLabel(): string {
    const totalRemainingKilowattHours = this.reportingFacade
      .energyGoals()
      .reduce((total, goal) => {
        const insight = this.getGoalInsight(goal);
        return total + Math.max(0, goal.targetKilowattHours - insight.measuredKilowattHours);
      }, 0);

    return this.t('reporting.goals.availableKwh', {
      value: this.formatNumber(totalRemainingKilowattHours, 2),
    });
  }

  minDeadline(): string {
    return this.toInputDate(new Date());
  }

  getGoalInsight(goal: EnergyGoal): EnergyGoalInsight {
    const readings = this.getGoalReadings(goal);
    const measuredKilowattHours = readings.reduce(
      (total, reading) => total + reading.kilowattHours,
      0
    );
    const estimatedCost = readings.reduce(
      (total, reading) => total + reading.estimatedCost,
      0
    );
    const devicesMeasured = new Set(readings.map((reading) => reading.deviceId)).size;
    const expectedProgressPercentage = this.getExpectedProgressPercentage(goal);
    const projectedKilowattHours = this.getProjectedKilowattHours(goal, measuredKilowattHours);
    const progressPercentage = this.getProgressPercentage(goal, measuredKilowattHours);
    const daysRemaining = this.getDaysRemaining(goal.deadline);

    let statusLabel = this.t('reporting.goals.status.inControl');
    let statusTone: EnergyGoalInsight['statusTone'] = 'good';
    let recommendation = this.t('reporting.goals.recommendations.inControl');

    if (measuredKilowattHours > goal.targetKilowattHours || (daysRemaining <= 0 && progressPercentage > 100)) {
      statusLabel = this.t('reporting.goals.status.outOfGoal');
      statusTone = 'danger';
      recommendation = this.t('reporting.goals.recommendations.outOfGoal');
    } else if (projectedKilowattHours > goal.targetKilowattHours) {
      statusLabel = this.t('reporting.goals.status.atRisk');
      statusTone = 'warning';
      recommendation = this.t('reporting.goals.recommendations.atRisk');
    } else if (projectedKilowattHours <= goal.targetKilowattHours) {
      statusLabel = this.t('reporting.goals.status.goodPace');
      recommendation = this.t('reporting.goals.recommendations.goodPace');
    }

    if (!readings.length) {
      statusLabel = this.t('reporting.goals.status.noReadings');
      statusTone = 'warning';
      recommendation = this.t('reporting.goals.recommendations.noReadings');
    }

    return {
      measuredKilowattHours,
      estimatedCost,
      devicesMeasured,
      daysRemaining,
      expectedProgressPercentage,
      projectedKilowattHours,
      statusLabel,
      statusTone,
      recommendation,
    };
  }

  private getGoalReadings(goal: EnergyGoal): EnergyReading[] {
    const start = new Date(`${goal.createdAt || this.toInputDate(new Date())}T00:00:00`).getTime();
    const end = Math.min(
      new Date(`${goal.deadline}T23:59:59.999`).getTime(),
      Date.now()
    );

    return this.reportingFacade.energyReadings().filter((reading) => {
      const recordedAt = new Date(reading.recordedAt).getTime();
      return recordedAt >= start &&
        recordedAt <= end &&
        this.matchesGoalScope(reading, goal) &&
        this.matchesActiveWindow(reading, goal);
    });
  }

  private matchesActiveWindow(reading: EnergyReading, goal: EnergyGoal): boolean {
    if (!goal.activeFrom || !goal.activeTo) {
      return true;
    }

    const recordedTime = new Date(reading.recordedAt).toTimeString().slice(0, 5);

    if (goal.activeFrom === goal.activeTo) {
      return true;
    }

    if (goal.activeFrom < goal.activeTo) {
      return recordedTime >= goal.activeFrom && recordedTime < goal.activeTo;
    }

    return recordedTime >= goal.activeFrom || recordedTime < goal.activeTo;
  }

  private matchesGoalScope(reading: EnergyReading, goal: EnergyGoal): boolean {
    if (goal.scopeType === 'DEVICE') {
      return goal.scopeId === reading.deviceId;
    }

    if (goal.scopeType === 'GROUP') {
      const group = this.deviceControlFacade
        .deviceGroups()
        .find((item) => item.id === goal.scopeId);

      return group?.containsDevice(reading.deviceId) ?? false;
    }

    if (goal.scopeType === 'WORKPLACE') {
      return this.workplaceFacade
        .deviceAssignments()
        .some(
          (assignment) =>
            assignment.locationId === goal.scopeId &&
            assignment.deviceId === reading.deviceId
        );
    }

    if (goal.scopeType === 'ROOM') {
      return this.workplaceFacade
        .deviceAssignments()
        .some(
          (assignment) =>
            assignment.roomId === goal.scopeId &&
            assignment.deviceId === reading.deviceId
        );
    }

    return true;
  }

  private getProgressPercentage(goal: EnergyGoal, measuredKilowattHours?: number): number {
    if (goal.targetKilowattHours <= 0) {
      return 0;
    }

    const currentKilowattHours = measuredKilowattHours ?? this.getGoalInsight(goal).measuredKilowattHours;
    return Number(((currentKilowattHours / goal.targetKilowattHours) * 100).toFixed(1));
  }

  private getExpectedProgressPercentage(goal: EnergyGoal): number {
    const createdAt = new Date(`${goal.createdAt || this.toInputDate(new Date())}T00:00:00`);
    const deadline = new Date(`${goal.deadline}T23:59:59.999`);
    const now = new Date();
    const totalDuration = deadline.getTime() - createdAt.getTime();
    const elapsed = now.getTime() - createdAt.getTime();

    if (totalDuration <= 0) {
      return 100;
    }

    return Math.min(100, Math.max(0, Number(((elapsed / totalDuration) * 100).toFixed(1))));
  }

  private getProjectedKilowattHours(goal: EnergyGoal, measuredKilowattHours: number): number {
    const createdAt = new Date(`${goal.createdAt || this.toInputDate(new Date())}T00:00:00`);
    const deadline = new Date(`${goal.deadline}T23:59:59.999`);
    const now = new Date();
    const elapsedDays = Math.max(
      1,
      Math.ceil((now.getTime() - createdAt.getTime()) / 86_400_000)
    );
    const totalDays = Math.max(
      1,
      Math.ceil((deadline.getTime() - createdAt.getTime()) / 86_400_000)
    );

    return Number(((measuredKilowattHours / elapsedDays) * totalDays).toFixed(2));
  }

  private getDaysRemaining(deadline: string): number {
    const today = new Date();
    const end = new Date(`${deadline}T23:59:59.999`);

    return Math.max(0, Math.ceil((end.getTime() - today.getTime()) / 86_400_000));
  }

  private formatNumber(value: number, decimals = 2): string {
    return Number(value || 0).toLocaleString(this.currentLocale(), {
      minimumFractionDigits: decimals,
      maximumFractionDigits: decimals,
    });
  }

  private toInputDate(date: Date): string {
    const year = date.getFullYear();
    const month = String(date.getMonth() + 1).padStart(2, '0');
    const day = String(date.getDate()).padStart(2, '0');

    return `${year}-${month}-${day}`;
  }

  private defaultScopeTargetId(scopeType: EnergyGoalScopeType): string {
    const activeLocationId = this.activeWorkplaceContext.activeLocationId();

    if (scopeType === 'WORKPLACE') {
      return String(activeLocationId ?? this.workplaceFacade.locations()[0]?.id ?? '');
    }

    if (scopeType === 'ROOM') {
      const room = activeLocationId
        ? this.workplaceFacade.rooms().find((item) => item.locationId === activeLocationId)
        : this.workplaceFacade.rooms()[0];

      return String(room?.id ?? '');
    }

    if (scopeType === 'DEVICE') {
      const activeDeviceIds = this.deviceIdsForLocation(activeLocationId);
      const device = activeLocationId
        ? this.deviceControlFacade.devices().find((item) => activeDeviceIds.has(item.id))
        : this.deviceControlFacade.devices()[0];

      return String(device?.id ?? '');
    }

    if (scopeType === 'GROUP') {
      const activeDeviceIds = this.deviceIdsForLocation(activeLocationId);
      const group = activeLocationId
        ? this.deviceControlFacade
            .deviceGroups()
            .find((item) => item.hasDevices && item.deviceIds.some((deviceId) => activeDeviceIds.has(deviceId)))
        : this.deviceControlFacade.deviceGroups().find((item) => item.hasDevices);

      return String(
        group?.id ?? ''
      );
    }

    return '';
  }

  private deviceIdsForLocation(locationId: number | null): Set<number> {
    if (!locationId) {
      return new Set<number>();
    }

    return new Set(
      this.workplaceFacade
        .deviceAssignments()
        .filter((assignment) => assignment.locationId === locationId)
        .map((assignment) => assignment.deviceId)
    );
  }

  private resolveScopeId(): number | null {
  if (this.scopeType === 'GENERAL') {
    return null;
  }

  const parsedId = Number(this.scopeTargetId);

  return Number.isFinite(parsedId) && parsedId > 0 ? parsedId : null;
}

private resolveScopeName(scopeId: number | null): string | null {
  if (this.scopeType === 'GENERAL' || !scopeId) {
    return this.t('reporting.goals.generalOperation');
  }

  if (this.scopeType === 'DEVICE') {
    return this.deviceControlFacade.getDeviceName(scopeId);
  }

  if (this.scopeType === 'WORKPLACE') {
    return this.workplaceFacade.getLocationName(scopeId);
  }

  if (this.scopeType === 'ROOM') {
    return this.workplaceFacade.getRoomName(scopeId);
  }

  if (this.scopeType === 'GROUP') {
    return this.deviceControlFacade
      .deviceGroups()
      .find((group) => group.id === scopeId)?.name ?? null;
  }

  return null;
}

private t(key: string, params?: Record<string, unknown>): string {
  this.uiPreferences.currentLanguage();

  return this.translate.instant(key, params);
}

private currentLocale(): string {
  const localeByLanguage = {
    es: 'es-PE',
    en: 'en-US',
    pt: 'pt-BR',
  };

  return localeByLanguage[this.uiPreferences.currentLanguage()];
}

private isPastDate(value: string): boolean {
  const selectedDate = new Date(`${value}T00:00:00`);
  const today = new Date();

  today.setHours(0, 0, 0, 0);

  return selectedDate.getTime() < today.getTime();
}

private resetGoalForm(): void {
  this.title = '';
  this.targetKilowattHours = null;
  this.deadline = '';
  this.scopeType = 'WORKPLACE';
  this.scopeTargetId = this.defaultScopeTargetId(this.scopeType);
  this.activeFrom = '00:00';
  this.activeTo = '00:00';
}

}


============================================================
FILE: src\app\reporting\presentation\pages\reports\reports-page.component.html
============================================================

<section class="reporting-page">
  <header class="ec-page-heading">
    <p class="ec-page-eyebrow">
      {{ 'reporting.reportsEyebrow' | translate }}
    </p>

    <h1 class="ec-page-title">
      {{ 'reporting.reportsTitle' | translate }}
    </h1>

    <p class="ec-page-description">
      {{ 'reporting.reportsSubtitle' | translate }}
    </p>
  </header>

  <section class="summary-grid">
    <article>
      <span>{{ 'reporting.totalReports' | translate }}</span>
      <strong>{{ reportingFacade.consumptionReports().length }}</strong>
    </article>

    <article>
      <span>{{ 'reporting.totalReportedWatts' | translate }}</span>
      <strong>{{ reportingFacade.totalReportedWatts() }} W</strong>
    </article>

    <article>
      <span>{{ 'reporting.averageReportedWatts' | translate }}</span>
      <strong>{{ reportingFacade.averageReportedWatts() }} W</strong>
    </article>
  </section>

  <section class="form-card">
    <div class="form-card__header">
      <div>
        <h2>{{ 'reporting.createReport' | translate }}</h2>
        <p>
          {{ 'reporting.createReportDescription' | translate }}
        </p>
      </div>

      <span class="range-summary">
        {{ formatDate(selectedRangeValue.startDate) }} -
        {{ formatDate(selectedRangeValue.endDate) }}
      </span>
    </div>

    <form class="form" (ngSubmit)="createReport()">
      <input
        type="text"
        name="title"
        [(ngModel)]="title"
        [placeholder]="'reporting.reportTitle' | translate"
        required
      />

      <app-dropdown
        [options]="periodOptions"
        [value]="period"
        [placeholder]="'reporting.period' | translate"
        (valueChange)="onPeriodChange($event)"
      />

      <app-button
        type="submit"
        [disabled]="reportingFacade.loading()"
      >
        {{ 'reporting.generate' | translate }}
      </app-button>
    </form>

    <app-wheel-selector
      [options]="rangeOptions"
      [value]="selectedRangeValue.value"
      [eyebrow]="'reporting.reportRangeEyebrow' | translate"
      [title]="rangeSelectionTitle()"
      [summary]="selectedRangeSummary()"
      [hint]="'reporting.reportRangeHint' | translate"
      (optionChange)="onRangeChange($event)"
    />
  </section>

  @if (reportingFacade.loading()) {
    <div class="loading-wrapper">
      <app-loading-spinner />
    </div>
  } @else {
    <app-monthly-comparison-chart
      [reports]="reportingFacade.consumptionReports()"
      (reportSelected)="openReportDetail($event)"
    />

    <section class="report-list-card">
      <div class="report-list-card__header">
        <div>
          <h2>{{ 'reporting.generatedReportsTitle' | translate }}</h2>
          <p>{{ 'reporting.generatedReportsDescription' | translate }}</p>
        </div>

        <span>
          {{ reportingFacade.consumptionReports().length }}
          {{ 'history.records' | translate }}
        </span>
      </div>

      <div class="list">
        @for (report of reportingFacade.consumptionReports(); track report.id) {
          <app-report-card
            [report]="report"
            (exported)="exportReport($event)"
            (remove)="deleteReport($event)"
          />
        } @empty {
          <app-empty-state
            [eyebrow]="'reporting.emptyReportsEyebrow' | translate"
            [title]="'reporting.emptyReportsTitle' | translate"
            [description]="'reporting.emptyReportsContent' | translate"
          />
        }
      </div>
    </section>
  }

  @if (selectedReport; as report) {
    @let summary = getInvoiceSummary(report);
    @let devices = getDeviceBreakdown(report);
    @let topDevices = getTopDevices(report);

    <div
      class="invoice-backdrop ec-modal-backdrop"
      [class.ec-modal-backdrop--closing]="isReportDetailClosing"
      (click)="closeReportDetail()"
    >
      <section
        class="invoice-modal ec-modal-panel"
        [class.ec-modal-panel--closing]="isReportDetailClosing"
        (click)="$event.stopPropagation()"
      >
        <header class="invoice-header">
          <div>
            <p class="invoice-eyebrow">{{ 'reporting.invoice.eyebrow' | translate }}</p>
            <h2>{{ 'reporting.invoice.title' | translate: { id: report.id } }}</h2>
            <span>
              {{ formatDate(report.startDate) }} - {{ formatDate(report.endDate) }}
            </span>
          </div>

          <button
            type="button"
            class="invoice-close"
            [attr.aria-label]="'reporting.invoice.close' | translate"
            (click)="closeReportDetail()"
          >
            &times;
          </button>
        </header>

        <div class="invoice-scroll ec-scrollbar">
          <section class="invoice-hero">
            <article>
              <span>{{ 'reporting.invoice.totalEnergy' | translate }}</span>
              <strong>{{ formatEnergy(summary.totalKilowattHours) }}</strong>
              <small>{{ 'reporting.invoice.exactReadings' | translate: { count: summary.readings } }}</small>
            </article>

            <article class="invoice-hero__accent">
              <span>{{ 'reporting.invoice.totalToPay' | translate }}</span>
              <strong>S/ {{ formatMoney(summary.totalCost) }}</strong>
              <small>{{ 'reporting.invoice.realKwhCalculation' | translate }}</small>
            </article>

            <article>
              <span>{{ 'nav.devices' | translate }}</span>
              <strong>{{ summary.devices }}</strong>
              <small>{{ 'reporting.invoice.monitoredDuration' | translate: { duration: formatDuration(summary.totalSeconds) } }}</small>
            </article>
          </section>

          <section class="invoice-grid">
            <article class="invoice-panel">
              <div class="invoice-panel__title">
                <span>{{ 'reporting.invoice.technicalSummary' | translate }}</span>
                <strong>{{ periodLabel(report.period) }}</strong>
              </div>

              <div class="invoice-facts">
                <div>
                  <span>{{ 'reporting.invoice.averagePower' | translate }}</span>
                  <strong>{{ formatNumber(summary.averageWatts) }} W</strong>
                </div>

                <div>
                  <span>{{ 'reporting.invoice.detectedPeak' | translate }}</span>
                  <strong>{{ formatNumber(summary.highestWatts) }} W</strong>
                </div>

                <div>
                  <span>{{ 'reporting.invoice.criticalReadings' | translate }}</span>
                  <strong>{{ countHighReadings(report) }}</strong>
                </div>

                <div>
                  <span>{{ 'reporting.invoice.costPerKwh' | translate }}</span>
                  <strong>
                    S/
                    {{
                      summary.totalKilowattHours
                        ? formatNumber(summary.totalCost / summary.totalKilowattHours, 2)
                        : formatNumber(0, 2)
                    }}
                  </strong>
                </div>

                <div>
                  <span>{{ 'reporting.invoice.billedEnergy' | translate }}</span>
                  <strong>{{ formatEnergy(summary.totalKilowattHours) }}</strong>
                </div>

                <div>
                  <span>{{ 'reporting.invoice.energySubtotal' | translate }}</span>
                  <strong>S/ {{ formatMoney(summary.totalCost) }}</strong>
                </div>

                <div>
                  <span>{{ 'reporting.invoice.measuredDevices' | translate }}</span>
                  <strong>{{ summary.devices }}</strong>
                </div>

                <div>
                  <span>{{ 'reporting.invoice.totalOnTime' | translate }}</span>
                  <strong>{{ formatDuration(summary.totalSeconds) }}</strong>
                </div>
              </div>
            </article>

            <article class="invoice-panel">
              <div class="invoice-panel__title">
                <span>{{ 'reporting.invoice.topConsumption' | translate }}</span>
                <strong>{{ 'reporting.invoice.devicesCount' | translate: { count: topDevices.length } }}</strong>
              </div>

              <div class="top-devices">
                @for (device of topDevices; track device.deviceId) {
                  <div class="top-device">
                    <div>
                      <strong>{{ device.deviceName }}</strong>
                      <span>{{ formatEnergy(device.totalKilowattHours) }}</span>
                    </div>

                    <b>{{ formatNumber(device.share, 1) }}%</b>
                  </div>
                } @empty {
                  <p class="invoice-empty">{{ 'reporting.invoice.noDevicesInRange' | translate }}</p>
                }
              </div>
            </article>
          </section>

          <section class="invoice-panel invoice-panel--wide">
            <div class="invoice-panel__title">
              <span>{{ 'reporting.invoice.deviceExactDetail' | translate }}</span>
              <strong>S/ {{ formatMoney(summary.totalCost) }}</strong>
            </div>

            <div class="invoice-table-wrap ec-scrollbar">
              <table class="invoice-table">
                <thead>
                  <tr>
                    <th>{{ 'history.device' | translate }}</th>
                    <th>{{ 'reporting.invoice.readings' | translate }}</th>
                    <th>{{ 'reporting.invoice.onTime' | translate }}</th>
                    <th>{{ 'reporting.invoice.exactKwh' | translate }}</th>
                    <th>{{ 'home.averageConsumption' | translate }}</th>
                    <th>{{ 'reporting.invoice.peak' | translate }}</th>
                    <th>{{ 'reporting.invoice.share' | translate }}</th>
                    <th>{{ 'reporting.invoice.subtotal' | translate }}</th>
                  </tr>
                </thead>

                <tbody>
                  @for (device of devices; track device.deviceId) {
                    <tr>
                      <td>{{ device.deviceName }}</td>
                      <td>{{ device.readings }}</td>
                      <td>{{ formatDuration(device.totalSeconds) }}</td>
                      <td>{{ formatEnergy(device.totalKilowattHours) }}</td>
                      <td>{{ formatNumber(device.averageWatts) }} W</td>
                      <td>{{ formatNumber(device.highestWatts) }} W</td>
                      <td>
                        <span class="share-meter">
                          <i [style.width.%]="device.share"></i>
                        </span>
                        {{ formatNumber(device.share, 1) }}%
                      </td>
                      <td>S/ {{ formatMoney(device.totalCost) }}</td>
                    </tr>
                  } @empty {
                    <tr>
                      <td colspan="8">{{ 'reporting.invoice.noReadingsForReport' | translate }}</td>
                    </tr>
                  }
                </tbody>
              </table>
            </div>
          </section>
        </div>
      </section>
    </div>
  }
</section>


============================================================
FILE: src\app\reporting\presentation\pages\reports\reports-page.component.scss
============================================================

.reporting-page {
  display: grid;
  gap: 24px;
}

.summary-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 18px;
}

.summary-grid article,
.form-card,
.report-list-card,
.empty-state {
  padding: 20px;
  border: 1px solid var(--border-color);
  border-radius: 22px;
  background: var(--surface-color);
}

.summary-grid span {
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
}

.summary-grid strong {
  display: block;
  margin-top: 8px;
  font-size: 1.8rem;
}

.form-card {
  display: grid;
  gap: 18px;
}

.form-card__header {
  display: flex;
  justify-content: space-between;
  gap: 18px;
  align-items: flex-start;
}

.form-card h2 {
  margin: 0 0 6px;
}

.form-card__header p {
  margin: 0;
  color: var(--ec-copy-color);
  font-size: 0.92rem;
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.range-summary {
  flex: 0 0 auto;
  padding: 10px 14px;
  border: 1px solid var(--border-strong);
  border-radius: 999px;
  color: var(--accent-color);
  background: var(--ec-accent-soft);
  font-size: 0.82rem;
  font-weight: 950;
  white-space: nowrap;
}

.form {
  display: grid;
  grid-template-columns: 1fr 180px auto;
  gap: 12px;
}

input {
  width: 100%;
  padding: 13px 14px;
  border: 1px solid var(--border-color);
  border-radius: 14px;
  color: var(--text-primary);
  background: var(--field-bg);
}

.form app-dropdown {
  width: 100%;
  min-width: 0;
}

.form app-button {
  min-width: 146px;
}

.report-list-card {
  display: grid;
  gap: 18px;
  padding: 24px;
  background:
    var(--ec-card-highlight),
    var(--surface-color);
}

.report-list-card__header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 18px;
}

.report-list-card__header h2 {
  margin: 0 0 6px;
  color: var(--text-primary);
  font-size: clamp(1.35rem, 2vw, 1.8rem);
  font-weight: 950;
  line-height: 1.1;
}

.report-list-card__header p {
  max-width: min(100%, 640px);
  margin: 0;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.report-list-card__header > span {
  flex: 0 0 auto;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
  white-space: nowrap;
}

.list {
  display: grid;
  gap: 14px;
}

.loading-wrapper {
  display: grid;
  place-items: center;
  min-height: 220px;
}

.empty-state {
  text-align: center;
}

.empty-state h2 {
  margin: 0 0 8px;
}

.empty-state p {
  margin: 0;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

@media (max-width: 940px) {
  .summary-grid,
  .form {
    grid-template-columns: 1fr;
  }

  .form-card__header,
  .report-list-card__header {
    align-items: flex-start;
    flex-direction: column;
  }

  .range-summary {
    white-space: normal;
  }
}


============================================================
FILE: src\app\reporting\presentation\pages\reports\reports-page.component.ts
============================================================

import {
  ChangeDetectionStrategy,
  ChangeDetectorRef,
  Component,
  Inject,
  OnDestroy,
  OnInit,
} from '@angular/core';
import { DOCUMENT } from '@angular/common';
import { FormsModule } from '@angular/forms';
import { TranslateModule, TranslateService } from '@ngx-translate/core';
import { Subscription } from 'rxjs';

import { EnergyReading } from '../../../../energy-monitoring/domain/model/energy-reading.entity';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { AppWheelSelectorComponent } from '../../../../shared/presentation/components/app-wheel-selector/app-wheel-selector.component';
import { WheelOption } from '../../../../shared/presentation/components/app-wheel-selector/wheel-option.model';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';

import { ReportingFacade } from '../../../application/services/reporting.facade';
import {
  ConsumptionReport,
  ConsumptionReportPeriod,
} from '../../../domain/model/consumption-report.entity';

import { MonthlyComparisonChartComponent } from '../../components/monthly-comparison-chart/monthly-comparison-chart.component';
import { ReportCardComponent } from '../../components/report-card/report-card.component';

interface ReportInvoiceSummary {
  readings: number;
  devices: number;
  totalKilowattHours: number;
  totalCost: number;
  averageWatts: number;
  highestWatts: number;
  totalSeconds: number;
}

interface ReportDeviceBreakdown {
  deviceId: number;
  deviceName: string;
  readings: number;
  totalKilowattHours: number;
  totalCost: number;
  totalSeconds: number;
  averageWatts: number;
  highestWatts: number;
  share: number;
}

interface TimeRangeOption extends WheelOption<string> {
  startDate: string;
  endDate: string;
}

@Component({
  selector: 'app-reports-page',
  standalone: true,
  imports: [
    FormsModule,
    TranslateModule,
    LoadingSpinnerComponent,
    MonthlyComparisonChartComponent,
    ReportCardComponent,
    AppDropdownComponent,
    AppButtonComponent,
    EmptyStateComponent,
    AppWheelSelectorComponent,
  ],
  templateUrl: './reports-page.component.html',
  styleUrls: ['./reports-page.component.scss'],
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class ReportsPageComponent implements OnInit, OnDestroy {
  title = '';
  period: ConsumptionReportPeriod = 'MONTHLY';
  selectedReport: ConsumptionReport | null = null;
  isReportDetailClosing = false;
  rangeOptions: TimeRangeOption[] = [];
  selectedRangeValue: TimeRangeOption = this.emptyRangeOption();
  private lockedScrollY = 0;
  private reportDetailCloseTimeout: ReturnType<typeof setTimeout> | null = null;
  private readonly reportDetailCloseAnimationMs = 220;
  private readonly languageChangeSubscription: Subscription;

  readonly periods: ConsumptionReportPeriod[] = [
    'DAILY',
    'WEEKLY',
    'MONTHLY',
    'YEARLY',
  ];

  readonly periodOptions: DropdownOption[] = this.periods.map((period) => ({
    label: period,
    labelKey: this.periodLabelKey(period),
    value: period,
  }));

  constructor(
    readonly reportingFacade: ReportingFacade,
    private readonly changeDetectorRef: ChangeDetectorRef,
    private readonly toastService: ToastService,
    private readonly translate: TranslateService,
    private readonly uiPreferences: UiPreferencesService,
    @Inject(DOCUMENT) private readonly document: Document
  ) {
    this.rangeOptions = this.buildRangeOptions(this.period);
    this.selectedRangeValue = this.getLatestRangeOption();
    this.languageChangeSubscription = this.translate.onLangChange.subscribe(() => {
      this.refreshRangeOptions();
      this.changeDetectorRef.markForCheck();
    });
  }

  async ngOnInit(): Promise<void> {
  await this.reportingFacade.loadReporting();

  if (this.reportingFacade.error()) {
    this.toastService.error(this.t('reporting.loadError'));
  }

  this.changeDetectorRef.markForCheck();
}

  ngOnDestroy(): void {
    this.clearReportDetailCloseTimeout();
    this.languageChangeSubscription.unsubscribe();
    this.unlockPageScroll();
  }

  async createReport(): Promise<void> {
  const title = this.title.trim();
  const range = this.selectedRangeValue;

  if (!title) {
    this.toastService.warning(this.t('reporting.reportNameRequired'));
    return;
  }

  if (title.length < 4) {
    this.toastService.warning(this.t('reporting.reportNameMinLength'));
    return;
  }

  if (!range.startDate || !range.endDate) {
    this.toastService.warning(this.t('reporting.reportRangeRequired'));
    return;
  }

  if (new Date(range.startDate).getTime() > new Date(range.endDate).getTime()) {
    this.toastService.warning(this.t('reporting.reportRangeInvalid'));
    return;
  }

  const result = await this.reportingFacade.createConsumptionReport({
    title,
    period: this.period,
    startDate: range.startDate,
    endDate: range.endDate,
  });

  if (result !== 'FAILED') {
    this.title = '';
    if (result === 'UPDATED') {
      this.toastService.success(this.t('reporting.reportUpdateSuccess'));
    } else if (result === 'UNCHANGED') {
      this.toastService.info(this.t('reporting.reportUnchanged'));
    } else {
      this.toastService.success(this.t('reporting.reportCreateSuccess'));
    }
  } else {
    this.toastService.error(this.t('reporting.reportCreateError'));
  }

  this.changeDetectorRef.markForCheck();
}

  onPeriodChange(value: string): void {
    this.period = value as ConsumptionReportPeriod;
    this.rangeOptions = this.buildRangeOptions(this.period);
    this.selectedRangeValue = this.getLatestRangeOption();
    this.changeDetectorRef.markForCheck();
  }

  onRangeChange(option: WheelOption): void {
    const range = this.rangeOptions.find((item) => item.value === option.value);

    if (!range) {
      return;
    }

    this.selectedRangeValue = range;
    this.changeDetectorRef.markForCheck();
  }

  rangeSelectionTitle(): string {
    if (this.period === 'DAILY') return this.t('reporting.rangeTitles.daily');
    if (this.period === 'WEEKLY') return this.t('reporting.rangeTitles.weekly');
    if (this.period === 'YEARLY') return this.t('reporting.rangeTitles.yearly');

    return this.t('reporting.rangeTitles.monthly');
  }

  selectedRangeSummary(): string {
    return `${this.formatDate(this.selectedRangeValue.startDate)} - ${this.formatDate(
      this.selectedRangeValue.endDate
    )}`;
  }

  exportReport(reportId: number): void {
  const report = this.reportingFacade
    .consumptionReports()
    .find((item) => item.id === reportId);

  if (!report) {
    this.toastService.error(this.t('reporting.reportNotFound'));
    return;
    }

    try {
    const exported = this.reportingFacade.exportReport({ reportId });

    if (exported) {
      this.toastService.success(this.t('reporting.reportExportSuccess'));
    } else if (this.reportingFacade.error() === 'reporting.exportNotAllowed') {
      this.toastService.error(this.t('reporting.exportNotAllowed'));
    } else {
      this.toastService.error(this.t('reporting.reportExportError'));
    }
  } catch (error) {
    console.error(error);
    this.toastService.error(this.t('reporting.reportExportError'));
  }
}

  async deleteReport(reportId: number): Promise<void> {
  const report = this.reportingFacade
    .consumptionReports()
    .find((item) => item.id === reportId);

  if (!report) {
    this.toastService.error(this.t('reporting.reportNotFound'));
    return;
  }

  const success = await this.reportingFacade.deleteConsumptionReport(reportId);

  if (success) {
    if (this.selectedReport?.id === reportId) {
      this.closeReportDetail();
    }

    this.toastService.info(this.t('reporting.reportDeleteSuccess'));
  } else {
    this.toastService.error(this.t('reporting.reportDeleteError'));
  }

  this.changeDetectorRef.markForCheck();
}

  openReportDetail(report: ConsumptionReport): void {
    this.clearReportDetailCloseTimeout();
    this.isReportDetailClosing = false;
    this.selectedReport = report;
    this.lockPageScroll();
    this.changeDetectorRef.markForCheck();
  }

  closeReportDetail(): void {
    if (!this.selectedReport || this.isReportDetailClosing) {
      return;
    }

    this.isReportDetailClosing = true;
    this.changeDetectorRef.markForCheck();

    this.reportDetailCloseTimeout = setTimeout(() => {
      this.selectedReport = null;
      this.isReportDetailClosing = false;
      this.reportDetailCloseTimeout = null;
      this.unlockPageScroll();
      this.changeDetectorRef.markForCheck();
    }, this.reportDetailCloseAnimationMs);
  }

  private clearReportDetailCloseTimeout(): void {
    if (this.reportDetailCloseTimeout === null) {
      return;
    }

    clearTimeout(this.reportDetailCloseTimeout);
    this.reportDetailCloseTimeout = null;
  }

  private lockPageScroll(): void {
    if (this.document.body.classList.contains('ec-modal-open')) {
      return;
    }

    const windowRef = this.document.defaultView;
    this.lockedScrollY =
      windowRef?.scrollY ?? this.document.documentElement.scrollTop ?? 0;

    this.document.documentElement.classList.add('ec-modal-open');
    this.document.body.classList.add('ec-modal-open');
    this.document.body.style.top = `-${this.lockedScrollY}px`;
  }

  private unlockPageScroll(): void {
    if (!this.document.body.classList.contains('ec-modal-open')) {
      return;
    }

    this.document.body.classList.remove('ec-modal-open');
    this.document.documentElement.classList.remove('ec-modal-open');
    this.document.body.style.top = '';
    this.document.defaultView?.scrollTo({
      top: this.lockedScrollY,
      left: 0,
      behavior: 'auto',
    });
    this.lockedScrollY = 0;
  }

  getReportReadings(report: ConsumptionReport): EnergyReading[] {
    const start = new Date(`${report.startDate}T00:00:00`).getTime();
    const end = new Date(`${report.endDate}T23:59:59.999`).getTime();

    return this.reportingFacade
      .energyReadings()
      .filter((reading) => {
        const recordedAt = new Date(reading.recordedAt).getTime();
        return recordedAt >= start && recordedAt <= end;
      })
      .sort(
        (first, second) =>
          new Date(first.recordedAt).getTime() -
          new Date(second.recordedAt).getTime()
      );
  }

  getInvoiceSummary(report: ConsumptionReport): ReportInvoiceSummary {
    const readings = this.getReportReadings(report);
    const totalKilowattHours = readings.reduce(
      (total, reading) => total + reading.kilowattHours,
      0
    );
    const totalCost = readings.reduce(
      (total, reading) => total + reading.estimatedCost,
      0
    );
    const totalWatts = readings.reduce(
      (total, reading) => total + reading.watts,
      0
    );
    const highestWatts = readings.reduce(
      (highest, reading) => Math.max(highest, reading.watts),
      0
    );
    const totalSeconds = readings.reduce(
      (total, reading) => total + reading.sampleSeconds,
      0
    );

    return {
      readings: readings.length,
      devices: new Set(readings.map((reading) => reading.deviceId)).size,
      totalKilowattHours,
      totalCost,
      averageWatts: readings.length ? totalWatts / readings.length : 0,
      highestWatts,
      totalSeconds,
    };
  }

  getDeviceBreakdown(report: ConsumptionReport): ReportDeviceBreakdown[] {
    const readings = this.getReportReadings(report);
    const totalKilowattHours = readings.reduce(
      (total, reading) => total + reading.kilowattHours,
      0
    );
    const devices = new Map<number, ReportDeviceBreakdown>();

    for (const reading of readings) {
      const current = devices.get(reading.deviceId) ?? {
        deviceId: reading.deviceId,
        deviceName: reading.deviceName,
        readings: 0,
        totalKilowattHours: 0,
        totalCost: 0,
        totalSeconds: 0,
        averageWatts: 0,
        highestWatts: 0,
        share: 0,
      };

      current.readings += 1;
      current.totalKilowattHours += reading.kilowattHours;
      current.totalCost += reading.estimatedCost;
      current.totalSeconds += reading.sampleSeconds;
      current.averageWatts += reading.watts;
      current.highestWatts = Math.max(current.highestWatts, reading.watts);

      devices.set(reading.deviceId, current);
    }

    return Array.from(devices.values())
      .map((device) => ({
        ...device,
        averageWatts: device.readings ? device.averageWatts / device.readings : 0,
        share: totalKilowattHours
          ? (device.totalKilowattHours / totalKilowattHours) * 100
          : 0,
      }))
      .sort(
        (first, second) =>
          second.totalKilowattHours - first.totalKilowattHours
      );
  }

  getTopDevices(report: ConsumptionReport): ReportDeviceBreakdown[] {
    return this.getDeviceBreakdown(report).slice(0, 5);
  }

  countHighReadings(report: ConsumptionReport): number {
    return this.getReportReadings(report).filter((reading) => reading.isHigh)
      .length;
  }

  formatDate(value: string): string {
    if (!value) return this.t('reporting.emptyValue');

    const date = this.dateFromInput(value);

    if (Number.isNaN(date.getTime())) {
      return value;
    }

    return new Intl.DateTimeFormat(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
    }).format(date);
  }

  formatDateTime(value: string): string {
    if (!value) return this.t('reporting.emptyValue');

    const date = new Date(value);

    if (Number.isNaN(date.getTime())) {
      return value;
    }

    return date.toLocaleString(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
      hour: '2-digit',
      minute: '2-digit',
    });
  }

  formatNumber(value: number, decimals = 2): string {
    return Number(value || 0).toLocaleString(this.currentLocale(), {
      minimumFractionDigits: decimals,
      maximumFractionDigits: decimals,
    });
  }

  formatMoney(value: number): string {
    return this.formatNumber(value, 2);
  }

  formatEnergy(value: number): string {
    if (value >= 1) {
      return `${this.formatNumber(value, 3)} kWh`;
    }

    return `${this.formatNumber(value * 1000, 2)} Wh`;
  }

  formatDuration(totalSeconds: number): string {
    const seconds = Math.max(0, Math.floor(totalSeconds || 0));
    const hours = Math.floor(seconds / 3600);
    const minutes = Math.floor((seconds % 3600) / 60);

    if (hours > 0) {
      return this.t('reporting.duration.hoursMinutes', {
        hours,
        minutes,
      });
    }

    return this.t('reporting.duration.minutesOnly', {
      minutes,
    });
  }

  periodLabel(period: ConsumptionReportPeriod): string {
    return this.t(this.periodLabelKey(period));
  }

  private getLatestRangeOption(): TimeRangeOption {
    return (
      this.rangeOptions[this.rangeOptions.length - 1] ?? this.emptyRangeOption()
    );
  }

  private emptyRangeOption(): TimeRangeOption {
    return {
      label: '',
      value: '',
      sublabel: '',
      startDate: '',
      endDate: '',
    };
  }

  private buildRangeOptions(period: ConsumptionReportPeriod): TimeRangeOption[] {
    if (period === 'DAILY') {
      return this.buildDailyOptions();
    }

    if (period === 'WEEKLY') {
      return this.buildWeeklyOptions();
    }

    if (period === 'YEARLY') {
      return this.buildYearlyOptions();
    }

    return this.buildMonthlyOptions();
  }

  private buildDailyOptions(): TimeRangeOption[] {
    const today = this.todayAtMidnight();
    const pastDays = 365;

    return Array.from({ length: pastDays + 1 }, (_, index) => {
      const offset = index - pastDays;
      const date = new Date(today);
      date.setDate(today.getDate() + offset);

      const inputDate = this.toInputDate(date);

      return {
        label: this.formatShortDay(date),
        value: inputDate,
        sublabel: this.formatDate(inputDate),
        startDate: inputDate,
        endDate: inputDate,
      };
    });
  }

  private buildWeeklyOptions(): TimeRangeOption[] {
    const today = this.todayAtMidnight();
    const currentWeekStart = this.startOfWeekOnMonday(today);
    const pastWeeks = 104;

    return Array.from({ length: pastWeeks + 1 }, (_, index) => {
      const weeksBack = pastWeeks - index;
      const start = new Date(currentWeekStart);
      start.setDate(currentWeekStart.getDate() - weeksBack * 7);

      const naturalEnd = new Date(start);
      naturalEnd.setDate(start.getDate() + 6);

      const isCurrentWeek = start.getTime() === currentWeekStart.getTime();

      const startDate = this.toInputDate(start);
      const endDate = this.toInputDate(naturalEnd);

      return {
        label: `${this.formatDayMonth(start)} - ${this.formatDayMonth(naturalEnd)}`,
        value: `${startDate}:${endDate}`,
        sublabel: isCurrentWeek
          ? this.t('reporting.rangeLabels.currentWeek')
          : `${this.formatDate(startDate)} - ${this.formatDate(endDate)}`,
        startDate,
        endDate,
      };
    });
  }

  private buildMonthlyOptions(): TimeRangeOption[] {
    const today = this.todayAtMidnight();
    const pastMonths = 120;

    return Array.from({ length: pastMonths + 1 }, (_, index) => {
      const offset = index - pastMonths;

      const firstDay = new Date(today.getFullYear(), today.getMonth() + offset, 1);
      const naturalLastDay = new Date(
        firstDay.getFullYear(),
        firstDay.getMonth() + 1,
        0
      );

      const isCurrentMonth =
        firstDay.getFullYear() === today.getFullYear() &&
        firstDay.getMonth() === today.getMonth();

      const startDateValue = this.toInputDate(firstDay);
      const endDateValue = this.toInputDate(naturalLastDay);

      return {
        label: this.formatMonth(firstDay),
        value: `${startDateValue}:${endDateValue}`,
        sublabel: isCurrentMonth
          ? this.t('reporting.rangeLabels.currentYearCycle', {
              year: firstDay.getFullYear(),
            })
          : String(firstDay.getFullYear()),
        startDate: startDateValue,
        endDate: endDateValue,
      };
    });
  }

  private buildYearlyOptions(): TimeRangeOption[] {
    const today = this.todayAtMidnight();
    const currentYear = today.getFullYear();
    const pastYears = 30;

    return Array.from({ length: pastYears + 1 }, (_, index) => {
      const year = currentYear - pastYears + index;
      const start = new Date(year, 0, 1);
      const end = new Date(year, 11, 31);
      const startDate = this.toInputDate(start);
      const endDate = this.toInputDate(end);

      return {
        label: String(year),
        value: `${startDate}:${endDate}`,
        sublabel: year === currentYear
          ? this.t('reporting.rangeLabels.currentYear')
          : this.t('reporting.rangeLabels.fullYear'),
        startDate,
        endDate,
      };
    });
  }

  private todayAtMidnight(): Date {
    const today = new Date();
    return new Date(today.getFullYear(), today.getMonth(), today.getDate());
  }

  private startOfWeekOnMonday(date: Date): Date {
    const day = date.getDay();
    const mondayOffset = day === 0 ? -6 : 1 - day;
    const monday = new Date(date);
    monday.setDate(date.getDate() + mondayOffset);

    return monday;
  }

  private toInputDate(date: Date): string {
    const year = date.getFullYear();
    const month = String(date.getMonth() + 1).padStart(2, '0');
    const day = String(date.getDate()).padStart(2, '0');

    return `${year}-${month}-${day}`;
  }

  private formatShortDay(date: Date): string {
    return date.toLocaleDateString(this.currentLocale(), {
      weekday: 'short',
      day: '2-digit',
      month: '2-digit',
    });
  }

  private formatDayMonth(date: Date): string {
    return date.toLocaleDateString(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
    });
  }

  private formatMonth(date: Date): string {
    const value = date.toLocaleDateString(this.currentLocale(), {
      month: 'long',
    });

    return value.charAt(0).toUpperCase() + value.slice(1);
  }

  private refreshRangeOptions(): void {
    const currentValue = this.selectedRangeValue.value;
    this.rangeOptions = this.buildRangeOptions(this.period);
    this.selectedRangeValue =
      this.rangeOptions.find((option) => option.value === currentValue) ??
      this.getLatestRangeOption();
  }

  private periodLabelKey(period: ConsumptionReportPeriod): string {
    return `reporting.periods.${period.toLowerCase()}`;
  }

  private t(key: string, params?: Record<string, unknown>): string {
    this.uiPreferences.currentLanguage();

    return this.translate.instant(key, params);
  }

  private currentLocale(): string {
    const localeByLanguage = {
      es: 'es-PE',
      en: 'en-US',
      pt: 'pt-BR',
    };

    return localeByLanguage[this.uiPreferences.currentLanguage()];
  }

  private dateFromInput(value: string): Date {
    const [datePart] = value.split('T');
    const [year, month, day] = datePart.split('-').map(Number);

    if (!year || !month || !day) {
      return new Date(value);
    }

    return new Date(year, month - 1, day);
  }

}


============================================================
FILE: src\app\reporting\presentation\routes\reporting.routes.ts
============================================================

import { Routes } from '@angular/router';

import { activeSubscriptionGuard } from '../../../shared/application/guards/active-subscription.guard';

export const REPORTING_ROUTES: Routes = [
  {
    path: 'energy/reports',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Reportes' },
    loadComponent: () =>
      import('../pages/reports/reports-page.component').then(
        (m) => m.ReportsPageComponent
      ),
  },
  {
    path: 'energy/goals',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Metas' },
    loadComponent: () =>
      import('../pages/energy-goals/energy-goals-page.component').then(
        (m) => m.EnergyGoalsPageComponent
      ),
  },
  {
    path: 'reports',
    redirectTo: 'energy/reports',
    pathMatch: 'full',
  },
  {
    path: 'reports/energy-goals',
    redirectTo: 'energy/goals',
    pathMatch: 'full',
  },
];


============================================================
FILE: src\app\service-management\application\commands\create-maintenance-ticket.command.ts
============================================================

import { MaintenanceTicketType } from '../../domain/model/maintenance-ticket.entity';

export interface CreateMaintenanceTicketCommand {
  deviceId: number;
  deviceName: string;
  type: MaintenanceTicketType;
  description: string;
  scheduledDate: string;
}


============================================================
FILE: src\app\service-management\application\commands\create-support-ticket.command.ts
============================================================

import { SupportTicketPriority } from '../../domain/model/support-ticket.entity';

export interface CreateSupportTicketCommand {
  subject: string;
  description: string;
  priority: SupportTicketPriority;
}


============================================================
FILE: src\app\service-management\application\commands\update-maintenance-ticket-status.command.ts
============================================================

import { MaintenanceTicketStatus } from '../../domain/model/maintenance-ticket.entity';

export interface UpdateMaintenanceTicketStatusCommand {
  ticketId: number;
  status: MaintenanceTicketStatus;
}


============================================================
FILE: src\app\service-management\application\commands\update-support-ticket-status.command.ts
============================================================

import { SupportTicketStatus } from '../../domain/model/support-ticket.entity';

export interface UpdateSupportTicketStatusCommand {
  ticketId: number;
  status: SupportTicketStatus;
}


============================================================
FILE: src\app\service-management\application\services\service-management.facade.ts
============================================================

import { Injectable } from '@angular/core';
import { firstValueFrom } from 'rxjs';

import { DeviceControlFacade } from '../../../device-control/application/services/device-control.facade';

import { CreateMaintenanceTicketCommand } from '../commands/create-maintenance-ticket.command';
import { CreateSupportTicketCommand } from '../commands/create-support-ticket.command';
import { UpdateMaintenanceTicketStatusCommand } from '../commands/update-maintenance-ticket-status.command';
import { UpdateSupportTicketStatusCommand } from '../commands/update-support-ticket-status.command';

import { MaintenanceTicketsApiService } from '../../infrastructure/api/maintenance-tickets-api.service';
import { SupportTicketsApiService } from '../../infrastructure/api/support-tickets-api.service';

import { MaintenanceTicketAssembler } from '../../infrastructure/assemblers/maintenance-ticket.assembler';
import { SupportTicketAssembler } from '../../infrastructure/assemblers/support-ticket.assembler';

import { ServiceManagementStore } from '../stores/service-management.store';

@Injectable({
  providedIn: 'root',
})
export class ServiceManagementFacade {
  private readonly supportTicketAssembler = new SupportTicketAssembler();
  private readonly maintenanceTicketAssembler = new MaintenanceTicketAssembler();

  get supportTickets() {
    return this.store.supportTickets;
  }

  get maintenanceTickets() {
    return this.store.maintenanceTickets;
  }

  get sortedSupportTickets() {
    return this.store.sortedSupportTickets;
  }

  get sortedMaintenanceTickets() {
    return this.store.sortedMaintenanceTickets;
  }

  get loading() {
    return this.store.loading;
  }

  get error() {
    return this.store.error;
  }

  get openSupportTickets() {
    return this.store.openSupportTickets;
  }

  get urgentSupportTickets() {
    return this.store.urgentSupportTickets;
  }

  get pendingMaintenanceTickets() {
    return this.store.pendingMaintenanceTickets;
  }

  get scheduledMaintenanceTickets() {
    return this.store.scheduledMaintenanceTickets;
  }

  get completedMaintenanceTickets() {
    return this.store.completedMaintenanceTickets;
  }

  constructor(
    private readonly supportTicketsApi: SupportTicketsApiService,
    private readonly maintenanceTicketsApi: MaintenanceTicketsApiService,
    private readonly deviceControlFacade: DeviceControlFacade,
    private readonly store: ServiceManagementStore
  ) {}

  async loadServiceManagement(): Promise<void> {
    this.startRequest();

    try {
      await Promise.all([
        this.loadSupportTickets(),
        this.loadMaintenanceTickets(),
        this.deviceControlFacade.loadDevices(),
      ]);
    } catch (error) {
      console.error(error);
      this.store.setError('serviceManagement.loadError');
    } finally {
      this.finishRequest();
    }
  }

  async loadSupportTickets(): Promise<void> {
    const responses = await firstValueFrom(
      this.supportTicketsApi.findAllForCurrentUser()
    );

    const tickets = responses
      .map((response) => this.supportTicketAssembler.toEntity(response))
      .sort(
        (first, second) =>
          new Date(second.createdAt).getTime() -
          new Date(first.createdAt).getTime()
      );

    this.store.setSupportTickets(tickets);
  }

  async createSupportTicket(command: CreateSupportTicketCommand): Promise<boolean> {
    this.startRequest();

    try {
      if (!command.subject.trim() || !command.description.trim()) {
        this.store.setError('serviceManagement.supportCreateError');
        return false;
      }

      const response = await firstValueFrom(
        this.supportTicketsApi.create({
          subject: command.subject.trim(),
          description: command.description.trim(),
          priority: command.priority,
          status: 'OPEN',
          createdAt: new Date().toISOString().slice(0, 10),
        })
      );

      const ticket = this.supportTicketAssembler.toEntity(response);
      this.store.prependSupportTicket(ticket);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('serviceManagement.supportCreateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async updateSupportTicketStatus(
    command: UpdateSupportTicketStatusCommand
  ): Promise<boolean> {
    this.startRequest();

    try {
      const response = await firstValueFrom(
        this.supportTicketsApi.updateStatus(command.ticketId, {
          status: command.status,
        })
      );

      const ticket = this.supportTicketAssembler.toEntity(response);
      this.store.updateSupportTicket(ticket);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('serviceManagement.supportUpdateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async deleteSupportTicket(ticketId: number): Promise<boolean> {
    this.startRequest();

    try {
      await firstValueFrom(this.supportTicketsApi.delete(ticketId));
      this.store.removeSupportTicket(ticketId);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('serviceManagement.supportDeleteError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async loadMaintenanceTickets(): Promise<void> {
    const responses = await firstValueFrom(
      this.maintenanceTicketsApi.findAllForCurrentUser()
    );

    const tickets = responses
      .map((response) => this.maintenanceTicketAssembler.toEntity(response))
      .sort(
        (first, second) =>
          new Date(second.createdAt).getTime() -
          new Date(first.createdAt).getTime()
      );

    this.store.setMaintenanceTickets(tickets);
  }

  async createMaintenanceTicket(
    command: CreateMaintenanceTicketCommand
  ): Promise<boolean> {
    this.startRequest();

    try {
      if (
        !command.deviceId ||
        !command.deviceName.trim() ||
        !command.description.trim() ||
        !command.scheduledDate
      ) {
        this.store.setError('serviceManagement.maintenanceCreateError');
        return false;
      }

      const response = await firstValueFrom(
        this.maintenanceTicketsApi.create({
          deviceId: Number(command.deviceId),
          deviceName: command.deviceName.trim(),
          type: command.type,
          description: command.description.trim(),
          scheduledDate: command.scheduledDate,
          status: 'PENDING',
          createdAt: new Date().toISOString().slice(0, 10),
        })
      );

      const ticket = this.maintenanceTicketAssembler.toEntity(response);
      this.store.prependMaintenanceTicket(ticket);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('serviceManagement.maintenanceCreateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async updateMaintenanceTicketStatus(
    command: UpdateMaintenanceTicketStatusCommand
  ): Promise<boolean> {
    this.startRequest();

    try {
      const response = await firstValueFrom(
        this.maintenanceTicketsApi.updateStatus(command.ticketId, {
          status: command.status,
        })
      );

      const ticket = this.maintenanceTicketAssembler.toEntity(response);
      this.store.updateMaintenanceTicket(ticket);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('serviceManagement.maintenanceUpdateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async deleteMaintenanceTicket(ticketId: number): Promise<boolean> {
    this.startRequest();

    try {
      await firstValueFrom(this.maintenanceTicketsApi.delete(ticketId));
      this.store.removeMaintenanceTicket(ticketId);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('serviceManagement.maintenanceDeleteError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  getDeviceName(deviceId: number): string {
    return this.deviceControlFacade.getDeviceName(deviceId);
  }

  clearMessages(): void {
    this.store.clearMessages();
  }

  private startRequest(): void {
    this.store.setLoading(true);
    this.store.clearMessages();
  }

  private finishRequest(): void {
    this.store.setLoading(false);
  }
}


============================================================
FILE: src\app\service-management\application\stores\service-management.store.ts
============================================================

import { Injectable, computed, signal } from '@angular/core';

import { MaintenanceTicket } from '../../domain/model/maintenance-ticket.entity';
import { SupportTicket } from '../../domain/model/support-ticket.entity';
import { TicketPriorityService } from '../../domain/services/ticket-priority.service';

@Injectable({
  providedIn: 'root',
})
export class ServiceManagementStore {
  private readonly supportTicketsSignal = signal<SupportTicket[]>([]);
  private readonly maintenanceTicketsSignal = signal<MaintenanceTicket[]>([]);

  private readonly loadingSignal = signal<boolean>(false);
  private readonly errorSignal = signal<string | null>(null);

  readonly supportTickets = computed(() => this.supportTicketsSignal());
  readonly maintenanceTickets = computed(() => this.maintenanceTicketsSignal());

  readonly sortedSupportTickets = computed(() =>
    this.ticketPriorityService.sortSupportTickets(this.supportTicketsSignal())
  );

  readonly sortedMaintenanceTickets = computed(() =>
    this.ticketPriorityService.sortMaintenanceTickets(
      this.maintenanceTicketsSignal()
    )
  );

  readonly loading = computed(() => this.loadingSignal());
  readonly error = computed(() => this.errorSignal());

  readonly openSupportTickets = computed(() =>
    this.supportTicketsSignal().filter((ticket) => ticket.isOpen)
  );

  readonly urgentSupportTickets = computed(() =>
    this.supportTicketsSignal().filter((ticket) => ticket.isUrgent)
  );

  readonly pendingMaintenanceTickets = computed(() =>
    this.maintenanceTicketsSignal().filter((ticket) => ticket.isPending)
  );

  readonly scheduledMaintenanceTickets = computed(() =>
    this.maintenanceTicketsSignal().filter((ticket) => ticket.isScheduled)
  );

  readonly completedMaintenanceTickets = computed(() =>
    this.maintenanceTicketsSignal().filter((ticket) => ticket.isCompleted)
  );

  constructor(private readonly ticketPriorityService: TicketPriorityService) {}

  setSupportTickets(value: SupportTicket[]): void {
    this.supportTicketsSignal.set(value);
  }

  prependSupportTicket(value: SupportTicket): void {
    this.supportTicketsSignal.update((tickets) => [value, ...tickets]);
  }

  updateSupportTicket(value: SupportTicket): void {
    this.supportTicketsSignal.update((tickets) =>
      tickets.map((ticket) => (ticket.id === value.id ? value : ticket))
    );
  }

  removeSupportTicket(ticketId: number): void {
    this.supportTicketsSignal.update((tickets) =>
      tickets.filter((ticket) => ticket.id !== ticketId)
    );
  }

  setMaintenanceTickets(value: MaintenanceTicket[]): void {
    this.maintenanceTicketsSignal.set(value);
  }

  prependMaintenanceTicket(value: MaintenanceTicket): void {
    this.maintenanceTicketsSignal.update((tickets) => [value, ...tickets]);
  }

  updateMaintenanceTicket(value: MaintenanceTicket): void {
    this.maintenanceTicketsSignal.update((tickets) =>
      tickets.map((ticket) => (ticket.id === value.id ? value : ticket))
    );
  }

  removeMaintenanceTicket(ticketId: number): void {
    this.maintenanceTicketsSignal.update((tickets) =>
      tickets.filter((ticket) => ticket.id !== ticketId)
    );
  }

  setLoading(value: boolean): void {
    this.loadingSignal.set(value);
  }

  setError(value: string | null): void {
    this.errorSignal.set(value);
  }

  clearMessages(): void {
    this.errorSignal.set(null);
  }

  reset(): void {
    this.supportTicketsSignal.set([]);
    this.maintenanceTicketsSignal.set([]);
    this.loadingSignal.set(false);
    this.errorSignal.set(null);
  }
}


============================================================
FILE: src\app\service-management\domain\model\maintenance-ticket.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export type MaintenanceTicketStatus = 'PENDING' | 'SCHEDULED' | 'COMPLETED' | 'CANCELED';
export type MaintenanceTicketType = 'INSPECTION' | 'REPAIR' | 'REPLACEMENT' | 'INSTALLATION';

export class MaintenanceTicket extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _deviceId: number;
  private readonly _deviceName: string;
  private readonly _type: MaintenanceTicketType;
  private readonly _description: string;
  private readonly _scheduledDate: string;
  private readonly _status: MaintenanceTicketStatus;
  private readonly _createdAt: string;

  constructor(props: {
    id: number;
    userId: number;
    deviceId: number;
    deviceName: string;
    type: MaintenanceTicketType;
    description: string;
    scheduledDate: string;
    status: MaintenanceTicketStatus;
    createdAt: string;
  }) {
    super(props.id);
    this._userId = props.userId;
    this._deviceId = props.deviceId;
    this._deviceName = props.deviceName;
    this._type = props.type;
    this._description = props.description;
    this._scheduledDate = props.scheduledDate;
    this._status = props.status;
    this._createdAt = props.createdAt;
  }

  get userId(): number {
    return this._userId;
  }

  get deviceId(): number {
    return this._deviceId;
  }

  get deviceName(): string {
    return this._deviceName;
  }

  get type(): MaintenanceTicketType {
    return this._type;
  }

  get description(): string {
    return this._description;
  }

  get scheduledDate(): string {
    return this._scheduledDate;
  }

  get status(): MaintenanceTicketStatus {
    return this._status;
  }

  get createdAt(): string {
    return this._createdAt;
  }

  get isPending(): boolean {
    return this._status === 'PENDING';
  }

  get isScheduled(): boolean {
    return this._status === 'SCHEDULED';
  }

  get isCompleted(): boolean {
    return this._status === 'COMPLETED';
  }

  get isCanceled(): boolean {
    return this._status === 'CANCELED';
  }
}

============================================================
FILE: src\app\service-management\domain\model\support-ticket.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export type SupportTicketStatus = 'OPEN' | 'IN_PROGRESS' | 'RESOLVED' | 'CLOSED';
export type SupportTicketPriority = 'LOW' | 'MEDIUM' | 'HIGH' | 'URGENT';

export class SupportTicket extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _subject: string;
  private readonly _description: string;
  private readonly _priority: SupportTicketPriority;
  private readonly _status: SupportTicketStatus;
  private readonly _createdAt: string;

  constructor(props: {
    id: number;
    userId: number;
    subject: string;
    description: string;
    priority: SupportTicketPriority;
    status: SupportTicketStatus;
    createdAt: string;
  }) {
    super(props.id);
    this._userId = props.userId;
    this._subject = props.subject;
    this._description = props.description;
    this._priority = props.priority;
    this._status = props.status;
    this._createdAt = props.createdAt;
  }

  get userId(): number {
    return this._userId;
  }

  get subject(): string {
    return this._subject;
  }

  get description(): string {
    return this._description;
  }

  get priority(): SupportTicketPriority {
    return this._priority;
  }

  get status(): SupportTicketStatus {
    return this._status;
  }

  get createdAt(): string {
    return this._createdAt;
  }

  get isOpen(): boolean {
    return this._status === 'OPEN';
  }

  get isInProgress(): boolean {
    return this._status === 'IN_PROGRESS';
  }

  get isResolved(): boolean {
    return this._status === 'RESOLVED';
  }

  get isClosed(): boolean {
    return this._status === 'CLOSED';
  }

  get isUrgent(): boolean {
    return this._priority === 'URGENT';
  }
}

============================================================
FILE: src\app\service-management\domain\services\ticket-priority.service.ts
============================================================

import { Injectable } from '@angular/core';

import {
  SupportTicket,
  SupportTicketPriority,
} from '../model/support-ticket.entity';
import {
  MaintenanceTicket,
  MaintenanceTicketStatus,
} from '../model/maintenance-ticket.entity';

@Injectable({
  providedIn: 'root',
})
export class TicketPriorityService {
  private readonly supportPriorityWeight: Record<SupportTicketPriority, number> = {
    URGENT: 4,
    HIGH: 3,
    MEDIUM: 2,
    LOW: 1,
  };

  private readonly maintenanceStatusWeight: Record<MaintenanceTicketStatus, number> = {
    PENDING: 4,
    SCHEDULED: 3,
    COMPLETED: 2,
    CANCELED: 1,
  };

  sortSupportTickets(tickets: SupportTicket[]): SupportTicket[] {
    return [...tickets].sort((first, second) => {
      const priorityDifference =
        this.supportPriorityWeight[second.priority] -
        this.supportPriorityWeight[first.priority];

      if (priorityDifference !== 0) return priorityDifference;

      return (
        new Date(second.createdAt).getTime() -
        new Date(first.createdAt).getTime()
      );
    });
  }

  sortMaintenanceTickets(tickets: MaintenanceTicket[]): MaintenanceTicket[] {
    return [...tickets].sort((first, second) => {
      const statusDifference =
        this.maintenanceStatusWeight[second.status] -
        this.maintenanceStatusWeight[first.status];

      if (statusDifference !== 0) return statusDifference;

      return (
        new Date(first.scheduledDate).getTime() -
        new Date(second.scheduledDate).getTime()
      );
    });
  }

  getSupportPriorityWeight(priority: SupportTicketPriority): number {
    return this.supportPriorityWeight[priority];
  }

  getMaintenanceStatusWeight(status: MaintenanceTicketStatus): number {
    return this.maintenanceStatusWeight[status];
  }
}

============================================================
FILE: src\app\service-management\infrastructure\api\maintenance-tickets-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { MaintenanceTicket } from '../../domain/model/maintenance-ticket.entity';
import { MaintenanceTicketAssembler } from '../assemblers/maintenance-ticket.assembler';
import { MaintenanceTicketResource } from '../resources/maintenance-ticket.resource';
import { MaintenanceTicketResponse } from '../responses/maintenance-ticket.response';

@Injectable({
  providedIn: 'root',
})
export class MaintenanceTicketsApiService extends BaseApiService<
  MaintenanceTicket,
  MaintenanceTicketResource,
  MaintenanceTicketResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(
      http,
      apiBaseUrl,
      'maintenance-tickets',
      new MaintenanceTicketAssembler()
    );
  }

  findAllForCurrentUser(): Observable<MaintenanceTicketResponse[]> {
    return this.http.get<MaintenanceTicketResponse[]>(this.resourceEndpoint);
  }

  updateStatus(
    ticketId: number,
    resource: Partial<MaintenanceTicketResource>
  ): Observable<MaintenanceTicketResponse> {
    return this.http.patch<MaintenanceTicketResponse>(
      `${this.resourceEndpoint}/${ticketId}/status`,
      resource
    );
  }
}


============================================================
FILE: src\app\service-management\infrastructure\api\support-tickets-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { SupportTicket } from '../../domain/model/support-ticket.entity';
import { SupportTicketAssembler } from '../assemblers/support-ticket.assembler';
import { SupportTicketResource } from '../resources/support-ticket.resource';
import { SupportTicketResponse } from '../responses/support-ticket.response';

@Injectable({
  providedIn: 'root',
})
export class SupportTicketsApiService extends BaseApiService<
  SupportTicket,
  SupportTicketResource,
  SupportTicketResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(http, apiBaseUrl, 'support-tickets', new SupportTicketAssembler());
  }

  findAllForCurrentUser(): Observable<SupportTicketResponse[]> {
    return this.http.get<SupportTicketResponse[]>(this.resourceEndpoint);
  }

  updateStatus(
    ticketId: number,
    resource: Partial<SupportTicketResource>
  ): Observable<SupportTicketResponse> {
    return this.http.patch<SupportTicketResponse>(
      `${this.resourceEndpoint}/${ticketId}/status`,
      resource
    );
  }
}


============================================================
FILE: src\app\service-management\infrastructure\assemblers\maintenance-ticket.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { MaintenanceTicket } from '../../domain/model/maintenance-ticket.entity';
import { MaintenanceTicketResource } from '../resources/maintenance-ticket.resource';
import { MaintenanceTicketResponse } from '../responses/maintenance-ticket.response';

export class MaintenanceTicketAssembler extends BaseAssembler<
  MaintenanceTicket,
  MaintenanceTicketResource,
  MaintenanceTicketResponse
> {
  override toEntity(response: MaintenanceTicketResponse): MaintenanceTicket {
    return new MaintenanceTicket({
      id: response.id,
      userId: response.userId,
      deviceId: response.deviceId,
      deviceName: response.deviceName,
      type: response.type,
      description: response.description,
      scheduledDate: response.scheduledDate,
      status: response.status,
      createdAt: response.createdAt,
    });
  }

  override toResource(entity: MaintenanceTicket): MaintenanceTicketResource {
    return {
      userId: entity.userId,
      deviceId: entity.deviceId,
      deviceName: entity.deviceName,
      type: entity.type,
      description: entity.description,
      scheduledDate: entity.scheduledDate,
      status: entity.status,
      createdAt: entity.createdAt,
    };
  }
}

============================================================
FILE: src\app\service-management\infrastructure\assemblers\support-ticket.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { SupportTicket } from '../../domain/model/support-ticket.entity';
import { SupportTicketResource } from '../resources/support-ticket.resource';
import { SupportTicketResponse } from '../responses/support-ticket.response';

export class SupportTicketAssembler extends BaseAssembler<
  SupportTicket,
  SupportTicketResource,
  SupportTicketResponse
> {
  override toEntity(response: SupportTicketResponse): SupportTicket {
    return new SupportTicket({
      id: response.id,
      userId: response.userId,
      subject: response.subject,
      description: response.description,
      priority: response.priority,
      status: response.status,
      createdAt: this.resolveCreatedAt(response.createdAt),
    });
  }

  override toResource(entity: SupportTicket): SupportTicketResource {
    return {
      userId: entity.userId,
      subject: entity.subject,
      description: entity.description,
      priority: entity.priority,
      status: entity.status,
      createdAt: entity.createdAt,
    };
  }

  private resolveCreatedAt(value?: string | null): string {
    return value?.trim() || new Date().toISOString().slice(0, 10);
  }
}


============================================================
FILE: src\app\service-management\infrastructure\resources\maintenance-ticket.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';
import {
  MaintenanceTicketStatus,
  MaintenanceTicketType,
} from '../../domain/model/maintenance-ticket.entity';

export interface MaintenanceTicketResource extends BaseResource {
  userId?: number;
  deviceId: number;
  deviceName: string;
  type: MaintenanceTicketType;
  description: string;
  scheduledDate: string;
  status: MaintenanceTicketStatus;
  createdAt: string;
}


============================================================
FILE: src\app\service-management\infrastructure\resources\support-ticket.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';
import {
  SupportTicketPriority,
  SupportTicketStatus,
} from '../../domain/model/support-ticket.entity';

export interface SupportTicketResource extends BaseResource {
  userId?: number;
  subject: string;
  description: string;
  priority: SupportTicketPriority;
  status: SupportTicketStatus;
  createdAt?: string | null;
}


============================================================
FILE: src\app\service-management\infrastructure\responses\maintenance-ticket.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';
import {
  MaintenanceTicketStatus,
  MaintenanceTicketType,
} from '../../domain/model/maintenance-ticket.entity';

export interface MaintenanceTicketResponse extends BaseResponse<number> {
  userId: number;
  deviceId: number;
  deviceName: string;
  type: MaintenanceTicketType;
  description: string;
  scheduledDate: string;
  status: MaintenanceTicketStatus;
  createdAt: string;
}

============================================================
FILE: src\app\service-management\infrastructure\responses\support-ticket.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';
import {
  SupportTicketPriority,
  SupportTicketStatus,
} from '../../domain/model/support-ticket.entity';

export interface SupportTicketResponse extends BaseResponse<number> {
  userId: number;
  subject: string;
  description: string;
  priority: SupportTicketPriority;
  status: SupportTicketStatus;
  createdAt?: string | null;
}


============================================================
FILE: src\app\service-management\presentation\components\maintenance-ticket-card\maintenance-ticket-card.component.html
============================================================

<article
  class="ticket-card"
  [class.completed]="ticket.isCompleted"
  [class.canceled]="ticket.isCanceled"
>
  <div class="ticket-card__content">
    <div class="badges">
      <app-ticket-status-badge
        [value]="ticket.type"
        [label]="maintenanceTypeLabel(ticket.type)"
      />
      <app-ticket-status-badge
        [value]="ticket.status"
        [label]="statusLabel(ticket.status)"
      />
    </div>

    <h2>{{ ticket.deviceName }}</h2>

    <p>{{ ticket.description }}</p>

    <small>{{ 'serviceManagement.scheduledFor' | translate: { date: formatDate(ticket.scheduledDate) } }}</small>
  </div>

  <div class="actions">
    <app-dropdown
      [options]="statusOptions"
      [value]="ticket.status"
      [placeholder]="'history.status' | translate"
      (valueChange)="changeStatus($event)"
    />

    <app-button variant="danger" size="sm" (pressed)="onRemove()">
      {{ 'common.delete' | translate }}
    </app-button>
  </div>
</article>


============================================================
FILE: src\app\service-management\presentation\components\maintenance-ticket-card\maintenance-ticket-card.component.scss
============================================================

@use '../support-ticket-card/support-ticket-card.component.scss';

.ticket-card.completed {
  border-color: rgba(34, 197, 94, 0.48);
}

.ticket-card.canceled {
  opacity: 0.72;
}


============================================================
FILE: src\app\service-management\presentation\components\maintenance-ticket-card\maintenance-ticket-card.component.ts
============================================================

import { TranslateModule, TranslateService } from '@ngx-translate/core';
import { Component, EventEmitter, Input, Output } from '@angular/core';

import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import {
  MaintenanceTicket,
  MaintenanceTicketStatus,
  MaintenanceTicketType,
} from '../../../domain/model/maintenance-ticket.entity';
import { TicketStatusBadgeComponent } from '../ticket-status-badge/ticket-status-badge.component';

@Component({
  selector: 'app-maintenance-ticket-card',
  standalone: true,
  imports: [
    TranslateModule,
    AppButtonComponent,
    AppDropdownComponent,
    TicketStatusBadgeComponent,
  ],
  templateUrl: './maintenance-ticket-card.component.html',
  styleUrls: ['./maintenance-ticket-card.component.scss'],
})
export class MaintenanceTicketCardComponent {
  @Input({ required: true }) ticket!: MaintenanceTicket;

  @Output() statusChanged = new EventEmitter<{
    ticketId: number;
    status: MaintenanceTicketStatus;
  }>();

  @Output() remove = new EventEmitter<number>();

  readonly statuses: MaintenanceTicketStatus[] = [
    'PENDING',
    'SCHEDULED',
    'COMPLETED',
    'CANCELED',
  ];

  get statusOptions(): DropdownOption[] {
    return this.statuses.map((status) => ({
      label: status,
      labelKey: this.statusLabelKey(status),
      value: status,
    }));
  }

  constructor(
    private readonly translate: TranslateService,
    private readonly uiPreferences: UiPreferencesService
  ) {}

  changeStatus(status: string): void {
    if (status === this.ticket.status) {
      return;
    }

    this.statusChanged.emit({
      ticketId: this.ticket.id,
      status: status as MaintenanceTicketStatus,
    });
  }

  onRemove(): void {
    this.remove.emit(this.ticket.id);
  }

  maintenanceTypeLabel(type: MaintenanceTicketType): string {
    return this.t(this.maintenanceTypeLabelKey(type));
  }

  private maintenanceTypeLabelKey(type: MaintenanceTicketType): string {
    const labels: Record<MaintenanceTicketType, string> = {
      INSPECTION: 'serviceManagement.maintenanceTypes.inspection',
      REPAIR: 'serviceManagement.maintenanceTypes.repair',
      REPLACEMENT: 'serviceManagement.maintenanceTypes.replacement',
      INSTALLATION: 'serviceManagement.maintenanceTypes.installation',
    };

    return labels[type] ?? type;
  }

  statusLabel(status: MaintenanceTicketStatus): string {
    return this.t(this.statusLabelKey(status));
  }

  private statusLabelKey(status: MaintenanceTicketStatus): string {
    const labels: Record<MaintenanceTicketStatus, string> = {
      PENDING: 'serviceManagement.maintenanceStatuses.pending',
      SCHEDULED: 'serviceManagement.maintenanceStatuses.scheduled',
      COMPLETED: 'serviceManagement.maintenanceStatuses.completed',
      CANCELED: 'serviceManagement.maintenanceStatuses.canceled',
    };

    return labels[status] ?? status;
  }

  formatDate(value: string): string {
    const date = new Date(value);

    if (Number.isNaN(date.getTime())) {
      return value;
    }

    return new Intl.DateTimeFormat(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
    }).format(date);
  }

  private t(key: string): string {
    return this.translate.instant(key);
  }

  private currentLocale(): string {
    const localeByLanguage: Record<string, string> = {
      es: 'es-PE',
      en: 'en-US',
      pt: 'pt-BR',
    };

    return localeByLanguage[this.uiPreferences.currentLanguage()] ?? localeByLanguage['es'];
  }
}


============================================================
FILE: src\app\service-management\presentation\components\support-ticket-card\support-ticket-card.component.html
============================================================

<article
  class="ticket-card"
  [class.urgent]="ticket.isUrgent"
  [class.resolved]="ticket.isResolved"
  [class.closed]="ticket.isClosed"
>
  <div class="ticket-card__content">
    <div class="badges">
      <app-ticket-status-badge
        [value]="ticket.priority"
        [label]="priorityLabel(ticket.priority)"
      />
      <app-ticket-status-badge
        [value]="ticket.status"
        [label]="statusLabel(ticket.status)"
      />
    </div>

    <h2>{{ ticket.subject }}</h2>

    <p>{{ ticket.description }}</p>

    <small>{{ 'serviceManagement.createdOn' | translate: { date: formatDate(ticket.createdAt) } }}</small>
  </div>

  <div class="actions">
    <app-dropdown
      [options]="statusOptions"
      [value]="ticket.status"
      [placeholder]="'history.status' | translate"
      (valueChange)="changeStatus($event)"
    />

    <app-button variant="danger" size="sm" (pressed)="onRemove()">
      {{ 'common.delete' | translate }}
    </app-button>
  </div>
</article>


============================================================
FILE: src\app\service-management\presentation\components\support-ticket-card\support-ticket-card.component.scss
============================================================

.ticket-card {
  position: relative;
  overflow: hidden;
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(210px, auto);
  gap: 20px;
  align-items: center;
  padding: 20px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background:
    var(--ec-card-highlight),
    color-mix(in srgb, var(--surface-color) 88%, var(--app-bg));
  box-shadow: var(--ec-inset-highlight);
  transition:
    border-color 0.18s ease,
    opacity 0.18s ease,
    transform 0.18s ease,
    box-shadow 0.18s ease;
}

.ticket-card::before {
  content: '';
  position: absolute;
  inset: 0 auto 0 0;
  width: 3px;
  background: var(--accent-color);
  opacity: 0.55;
}

.ticket-card:hover {
  border-color: var(--border-strong);
  box-shadow: var(--shadow-soft);
  transform: translateY(-1px);
}

.ticket-card.urgent {
  border-color: var(--ec-status-danger-border);
}

.ticket-card.urgent::before {
  background: var(--ec-status-danger-solid);
  opacity: 0.75;
}

.ticket-card.resolved {
  border-color: var(--ec-status-success-border);
}

.ticket-card.resolved::before {
  background: var(--ec-status-success-solid);
}

.ticket-card.closed {
  opacity: 0.72;
}

.ticket-card__content {
  display: grid;
  gap: 8px;
  min-width: 0;
}

.badges {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

h2 {
  margin: 0;
  color: var(--text-primary);
  font-size: clamp(1.02rem, 2vw, 1.28rem);
  line-height: 1.25;
  overflow-wrap: anywhere;
}

p {
  display: -webkit-box;
  margin: 0;
  color: var(--text-secondary);
  line-height: 1.55;
  overflow: hidden;
  overflow-wrap: anywhere;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 3;
}

small {
  display: block;
  color: var(--text-muted);
  font-weight: 800;
}

.actions {
  display: grid;
  grid-template-columns: minmax(170px, 1fr) auto;
  gap: 10px;
  align-items: center;
  padding: 10px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  background: var(--ec-surface-subtle);
}

app-dropdown {
  min-width: 170px;
}

@media (max-width: 780px) {
  .ticket-card,
  .actions {
    grid-template-columns: 1fr;
  }

  .actions {
    padding: 0;
    border: 0;
    background: transparent;
  }
}


============================================================
FILE: src\app\service-management\presentation\components\support-ticket-card\support-ticket-card.component.ts
============================================================

import { TranslateModule, TranslateService } from '@ngx-translate/core';
import { Component, EventEmitter, Input, Output } from '@angular/core';

import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import {
  SupportTicket,
  SupportTicketPriority,
  SupportTicketStatus,
} from '../../../domain/model/support-ticket.entity';
import { TicketStatusBadgeComponent } from '../ticket-status-badge/ticket-status-badge.component';

@Component({
  selector: 'app-support-ticket-card',
  standalone: true,
  imports: [
    TranslateModule,
    AppButtonComponent,
    AppDropdownComponent,
    TicketStatusBadgeComponent,
  ],
  templateUrl: './support-ticket-card.component.html',
  styleUrls: ['./support-ticket-card.component.scss'],
})
export class SupportTicketCardComponent {
  @Input({ required: true }) ticket!: SupportTicket;

  @Output() statusChanged = new EventEmitter<{
    ticketId: number;
    status: SupportTicketStatus;
  }>();

  @Output() remove = new EventEmitter<number>();

  readonly statuses: SupportTicketStatus[] = [
    'OPEN',
    'IN_PROGRESS',
    'RESOLVED',
    'CLOSED',
  ];

  get statusOptions(): DropdownOption[] {
    return this.statuses.map((status) => ({
      label: status,
      labelKey: this.statusLabelKey(status),
      value: status,
    }));
  }

  constructor(
    private readonly translate: TranslateService,
    private readonly uiPreferences: UiPreferencesService
  ) {}

  changeStatus(status: string): void {
    if (status === this.ticket.status) {
      return;
    }

    this.statusChanged.emit({
      ticketId: this.ticket.id,
      status: status as SupportTicketStatus,
    });
  }

  onRemove(): void {
    this.remove.emit(this.ticket.id);
  }

  priorityLabel(priority: SupportTicketPriority): string {
    return this.t(this.priorityLabelKey(priority));
  }

  private priorityLabelKey(priority: SupportTicketPriority): string {
    const labels: Record<SupportTicketPriority, string> = {
      LOW: 'serviceManagement.priorities.low',
      MEDIUM: 'serviceManagement.priorities.medium',
      HIGH: 'serviceManagement.priorities.high',
      URGENT: 'serviceManagement.priorities.urgent',
    };

    return labels[priority] ?? priority;
  }

  statusLabel(status: SupportTicketStatus): string {
    return this.t(this.statusLabelKey(status));
  }

  private statusLabelKey(status: SupportTicketStatus): string {
    const labels: Record<SupportTicketStatus, string> = {
      OPEN: 'serviceManagement.supportStatuses.open',
      IN_PROGRESS: 'serviceManagement.supportStatuses.inProgress',
      RESOLVED: 'serviceManagement.supportStatuses.resolved',
      CLOSED: 'serviceManagement.supportStatuses.closed',
    };

    return labels[status] ?? status;
  }

  formatDate(value: string): string {
    const date = new Date(value);

    if (Number.isNaN(date.getTime())) {
      return value;
    }

    return new Intl.DateTimeFormat(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
    }).format(date);
  }

  private t(key: string): string {
    return this.translate.instant(key);
  }

  private currentLocale(): string {
    const localeByLanguage: Record<string, string> = {
      es: 'es-PE',
      en: 'en-US',
      pt: 'pt-BR',
    };

    return localeByLanguage[this.uiPreferences.currentLanguage()] ?? localeByLanguage['es'];
  }
}


============================================================
FILE: src\app\service-management\presentation\components\ticket-status-badge\ticket-status-badge.component.html
============================================================

<span class="ticket-status-badge" [class]="normalizedValue">
  {{ displayLabel }}
</span>

============================================================
FILE: src\app\service-management\presentation\components\ticket-status-badge\ticket-status-badge.component.scss
============================================================

.ticket-status-badge {
  width: fit-content;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 6px 10px;
  border-radius: 999px;
  color: var(--text-on-action);
  background: var(--accent-color);
  font-size: 0.75rem;
  font-weight: 900;
  line-height: 1;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

.open,
.pending,
.medium {
  background: var(--ec-status-warning-bg);
  color: var(--ec-status-warning-text);
}

.in-progress,
.scheduled,
.high {
  background: var(--ec-action-bg);
}

.resolved,
.completed,
.low {
  background: var(--ec-status-success-solid);
}

.closed,
.canceled {
  background: var(--text-muted);
}

.urgent {
  background: var(--ec-danger-bg);
}


============================================================
FILE: src\app\service-management\presentation\components\ticket-status-badge\ticket-status-badge.component.ts
============================================================

import { Component, Input } from '@angular/core';

type BadgeVariant =
  | 'OPEN'
  | 'IN_PROGRESS'
  | 'RESOLVED'
  | 'CLOSED'
  | 'PENDING'
  | 'SCHEDULED'
  | 'COMPLETED'
  | 'CANCELED'
  | 'LOW'
  | 'MEDIUM'
  | 'HIGH'
  | 'URGENT'
  | string;

@Component({
  selector: 'app-ticket-status-badge',
  standalone: true,
  templateUrl: './ticket-status-badge.component.html',
  styleUrls: ['./ticket-status-badge.component.scss'],
})
export class TicketStatusBadgeComponent {
  @Input({ required: true }) value!: BadgeVariant;
  @Input() label: string | null = null;

  get normalizedValue(): string {
    return String(this.value).toLowerCase().replaceAll('_', '-');
  }

  get displayLabel(): string {
    return this.label ?? String(this.value).replaceAll('_', ' ');
  }
}

============================================================
FILE: src\app\service-management\presentation\pages\maintenance-tickets\maintenance-tickets-page.component.html
============================================================

<section class="service-page ec-page">
  <header class="ec-page-heading">
    <p class="ec-page-eyebrow">{{ 'about.capabilities.items.service.eyebrow' | translate }}</p>
    <h1 class="ec-page-title">{{ 'nav.maintenanceTickets' | translate }}</h1>
    <p class="ec-page-description">
      {{ 'serviceManagement.maintenancePageDescription' | translate }}
    </p>
  </header>

  <section class="service-summary">
    <article class="summary-card">
      <span>{{ 'serviceManagement.maintenanceSummaryTitle' | translate }}</span>
      <strong>{{ serviceManagementFacade.maintenanceTickets().length }}</strong>
      <p>{{ 'serviceManagement.maintenanceSummaryDescription' | translate }}</p>
    </article>

    <article class="summary-card">
      <span>{{ 'serviceManagement.pendingMaintenanceShort' | translate }}</span>
      <strong>{{ serviceManagementFacade.pendingMaintenanceTickets().length }}</strong>
    </article>

    <article class="summary-card">
      <span>{{ 'serviceManagement.completedMaintenanceShort' | translate }}</span>
      <strong>{{ serviceManagementFacade.completedMaintenanceTickets().length }}</strong>
    </article>
  </section>

  <section class="operation-toolbar">
    <div>
      <p class="section-eyebrow">{{ 'serviceManagement.scheduleEyebrow' | translate }}</p>
      <h2>{{ 'serviceManagement.technicalScheduleTitle' | translate }}</h2>
      <p>{{ 'serviceManagement.technicalScheduleDescription' | translate }}</p>
    </div>

    @if (hasDevices) {
      <app-button variant="create" (pressed)="openMaintenanceModal()">
        {{ 'serviceManagement.scheduleMaintenance' | translate }}
      </app-button>
    } @else {
      <app-button variant="create" link="/operation/devices">
        {{ 'serviceManagement.goToDevices' | translate }}
      </app-button>
    }
  </section>

  @if (serviceManagementFacade.loading()) {
    <div class="loading-wrapper">
      <app-loading-spinner />
    </div>
  } @else {
    <section class="ticket-section">
      <header class="section-heading">
        <div>
          <span>{{ 'serviceManagement.orders' | translate }}</span>
          <h2>{{ 'serviceManagement.maintenanceAgendaTitle' | translate }}</h2>
        </div>
      </header>

      <div class="list">
        @for (ticket of serviceManagementFacade.sortedMaintenanceTickets(); track ticket.id) {
          <app-maintenance-ticket-card
            [ticket]="ticket"
            (statusChanged)="updateStatus($event)"
            (remove)="deleteTicket($event)"
          />
        } @empty {
          <app-empty-state
            [eyebrow]="'nav.maintenanceTickets' | translate"
            [title]="'serviceManagement.emptyMaintenanceTitle' | translate"
            [description]="'serviceManagement.emptyMaintenanceDescription' | translate"
          >
            @if (hasDevices) {
              <app-button variant="create" size="sm" (pressed)="openMaintenanceModal()">
                {{ 'serviceManagement.scheduleMaintenance' | translate }}
              </app-button>
            } @else {
              <app-button variant="create" size="sm" link="/operation/devices">
                {{ 'serviceManagement.goToDevices' | translate }}
              </app-button>
            }
          </app-empty-state>
        }
      </div>
    </section>
  }

  <app-modal-form-shell
    [open]="isMaintenanceModalOpen"
    [eyebrow]="'about.capabilities.items.service.eyebrow' | translate"
    [title]="'serviceManagement.scheduleMaintenance' | translate"
    [description]="'serviceManagement.maintenanceModalDescription' | translate"
    (closed)="closeMaintenanceModal()"
  >
    <form class="modal-form" (ngSubmit)="createTicket()">
      <label>
        <span>{{ 'routines.device' | translate }}</span>
        <app-dropdown
          [options]="deviceOptions"
          [value]="deviceValue"
          [placeholder]="'routines.selectDevice' | translate"
          (valueChange)="selectDevice($event)"
        />
      </label>

      <label>
        <span>{{ 'devices.type' | translate }}</span>
        <app-dropdown
          [options]="typeOptions"
          [value]="type"
          [placeholder]="'serviceManagement.maintenanceType' | translate"
          (valueChange)="selectType($event)"
        />
      </label>

      <label>
        <span>{{ 'serviceManagement.reason' | translate }}</span>
        <textarea
          name="description"
          [(ngModel)]="description"
          rows="5"
          [placeholder]="'serviceManagement.maintenanceDescriptionPlaceholder' | translate"
          required
        ></textarea>
      </label>

      <app-date-picker
        [label]="'serviceManagement.scheduledDate' | translate"
        [value]="scheduledDate"
        [min]="todayInput"
        (valueChange)="selectDate($event)"
      />

      <div class="form-actions">
        <app-button
          variant="secondary"
          type="button"
          (pressed)="closeMaintenanceModal()"
        >
          {{ 'common.cancel' | translate }}
        </app-button>

        <app-button
          variant="create"
          type="submit"
          [disabled]="serviceManagementFacade.loading()"
        >
          {{ 'serviceManagement.saveMaintenance' | translate }}
        </app-button>
      </div>
    </form>
  </app-modal-form-shell>
</section>


============================================================
FILE: src\app\service-management\presentation\pages\maintenance-tickets\maintenance-tickets-page.component.scss
============================================================

@use '../support-tickets/support-tickets-page.component.scss';

:host {
  display: block;
}


============================================================
FILE: src\app\service-management\presentation\pages\maintenance-tickets\maintenance-tickets-page.component.ts
============================================================

import { TranslateModule, TranslateService } from '@ngx-translate/core';
import { Component, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';

import { DeviceControlFacade } from '../../../../device-control/application/services/device-control.facade';
import { ConfirmDialogService } from '../../../../shared/application/services/confirm-dialog.service';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { AppDatePickerComponent } from '../../../../shared/presentation/components/app-date-picker/app-date-picker.component';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { ModalFormShellComponent } from '../../../../shared/presentation/components/modal-form-shell/modal-form-shell.component';

import { ServiceManagementFacade } from '../../../application/services/service-management.facade';
import {
  MaintenanceTicketStatus,
  MaintenanceTicketType,
} from '../../../domain/model/maintenance-ticket.entity';
import { MaintenanceTicketCardComponent } from '../../components/maintenance-ticket-card/maintenance-ticket-card.component';

@Component({
  selector: 'app-maintenance-tickets-page',
  standalone: true,
  imports: [
    TranslateModule,
    FormsModule,
    AppButtonComponent,
    AppDatePickerComponent,
    AppDropdownComponent,
    EmptyStateComponent,
    LoadingSpinnerComponent,
    MaintenanceTicketCardComponent,
    ModalFormShellComponent,
  ],
  templateUrl: './maintenance-tickets-page.component.html',
  styleUrls: ['./maintenance-tickets-page.component.scss'],
})
export class MaintenanceTicketsPageComponent implements OnInit {
  deviceId: number | null = null;
  type: MaintenanceTicketType = 'INSPECTION';
  description = '';
  scheduledDate = '';
  isMaintenanceModalOpen = false;

  readonly types: MaintenanceTicketType[] = [
    'INSPECTION',
    'REPAIR',
    'REPLACEMENT',
    'INSTALLATION',
  ];

  get deviceValue(): string | null {
    return this.deviceId ? String(this.deviceId) : null;
  }

  get deviceOptions(): DropdownOption[] {
    return this.deviceControlFacade.devices().map((device) => ({
      label: device.name,
      value: String(device.id),
      description: device.room
        ? `${device.room} - ${device.powerWatts} W`
        : `${device.powerWatts} W`,
    }));
  }

  get hasDevices(): boolean {
    return this.deviceControlFacade.devices().length > 0;
  }

  get todayInput(): string {
    const today = new Date();
    const month = String(today.getMonth() + 1).padStart(2, '0');
    const day = String(today.getDate()).padStart(2, '0');

    return `${today.getFullYear()}-${month}-${day}`;
  }

  get typeOptions(): DropdownOption[] {
    return this.types.map((type) => ({
      label: type,
      labelKey: this.maintenanceTypeLabelKey(type),
      value: type,
    }));
  }

  constructor(
    readonly serviceManagementFacade: ServiceManagementFacade,
    readonly deviceControlFacade: DeviceControlFacade,
    private readonly toastService: ToastService,
    private readonly confirmDialog: ConfirmDialogService,
    private readonly translate: TranslateService
  ) {}

  async ngOnInit(): Promise<void> {
    await this.serviceManagementFacade.loadServiceManagement();

    if (this.serviceManagementFacade.error()) {
      this.toastService.error(this.t('serviceManagement.maintenanceLoadError'));
    }
  }

  openMaintenanceModal(): void {
    if (!this.hasDevices) {
      this.toastService.info(this.t('serviceManagement.validation.deviceRequiredBeforeMaintenance'));
      return;
    }

    this.isMaintenanceModalOpen = true;
  }

  closeMaintenanceModal(): void {
    this.isMaintenanceModalOpen = false;
  }

  async createTicket(): Promise<void> {
    if (!this.deviceId) {
      this.toastService.warning(this.t('serviceManagement.validation.maintenanceDeviceRequired'));
      return;
    }

    const description = this.description.trim();

    if (!description) {
      this.toastService.warning(this.t('serviceManagement.validation.maintenanceDescriptionRequired'));
      return;
    }

    if (description.length < 10) {
      this.toastService.warning(this.t('serviceManagement.validation.descriptionMinLength'));
      return;
    }

    if (!this.scheduledDate) {
      this.toastService.warning(this.t('serviceManagement.validation.maintenanceDateRequired'));
      return;
    }

    if (this.isPastDate(this.scheduledDate)) {
      this.toastService.warning(this.t('serviceManagement.validation.maintenanceDatePast'));
      return;
    }

    const deviceName = this.deviceControlFacade.getDeviceName(Number(this.deviceId));

    if (!deviceName || deviceName === 'Unknown device') {
      this.toastService.error(this.t('serviceManagement.validation.maintenanceDeviceUnknown'));
      return;
    }

    const success = await this.serviceManagementFacade.createMaintenanceTicket({
      deviceId: Number(this.deviceId),
      deviceName,
      type: this.type,
      description,
      scheduledDate: this.scheduledDate,
    });

    if (success) {
      this.deviceId = null;
      this.type = 'INSPECTION';
      this.description = '';
      this.scheduledDate = '';
      this.closeMaintenanceModal();
      this.toastService.success(this.t('serviceManagement.maintenanceCreateSuccess'));
      return;
    }

    this.toastService.error(this.t('serviceManagement.maintenanceCreateError'));
  }

  selectDevice(value: string): void {
    const deviceId = Number(value);
    this.deviceId = Number.isFinite(deviceId) ? deviceId : null;
  }

  selectType(value: string): void {
    this.type = value as MaintenanceTicketType;
  }

  selectDate(value: string): void {
    this.scheduledDate = value;
  }

  maintenanceTypeLabel(type: MaintenanceTicketType): string {
    return this.t(this.maintenanceTypeLabelKey(type));
  }

  maintenanceTypeLabelKey(type: MaintenanceTicketType): string {
    const labels: Record<MaintenanceTicketType, string> = {
      INSPECTION: 'serviceManagement.maintenanceTypes.inspection',
      REPAIR: 'serviceManagement.maintenanceTypes.repair',
      REPLACEMENT: 'serviceManagement.maintenanceTypes.replacement',
      INSTALLATION: 'serviceManagement.maintenanceTypes.installation',
    };

    return labels[type] ?? type;
  }

  async updateStatus(statusChange: {
    ticketId: number;
    status: MaintenanceTicketStatus;
  }): Promise<void> {
    if (statusChange.status === 'CANCELED') {
      const confirmed = await this.confirmDialog.confirm({
        title: this.t('serviceManagement.confirmCancelMaintenanceTitle'),
        message: this.t('serviceManagement.confirmCancelMaintenanceMessage'),
        confirmLabel: this.t('serviceManagement.cancelMaintenance'),
        cancelLabel: this.t('serviceManagement.back'),
        tone: 'warning',
      });

      if (!confirmed) {
        return;
      }
    }

    const success =
      await this.serviceManagementFacade.updateMaintenanceTicketStatus(statusChange);

    if (success) {
      this.toastService.info(this.t('serviceManagement.maintenanceUpdateSuccess'));
      return;
    }

    this.toastService.error(this.t('serviceManagement.maintenanceUpdateError'));
  }

  async deleteTicket(ticketId: number): Promise<void> {
    const confirmed = await this.confirmDialog.confirm({
      title: this.t('serviceManagement.confirmDeleteMaintenanceTitle'),
      message: this.t('serviceManagement.confirmDeleteMaintenanceMessage'),
      confirmLabel: this.t('common.delete'),
      cancelLabel: this.t('common.keep'),
      tone: 'danger',
    });

    if (!confirmed) {
      return;
    }

    const success =
      await this.serviceManagementFacade.deleteMaintenanceTicket(ticketId);

    if (success) {
      this.toastService.info(this.t('serviceManagement.maintenanceDeleteSuccess'));
      return;
    }

    this.toastService.error(this.t('serviceManagement.maintenanceDeleteError'));
  }

  private isPastDate(value: string): boolean {
    const selectedDate = new Date(`${value}T00:00:00`);
    const today = new Date();

    today.setHours(0, 0, 0, 0);

    return selectedDate.getTime() < today.getTime();
  }

  private t(key: string): string {
    return this.translate.instant(key);
  }
}


============================================================
FILE: src\app\service-management\presentation\pages\support-tickets\support-tickets-page.component.html
============================================================

<section class="service-page ec-page">
  <header class="ec-page-heading">
    <p class="ec-page-eyebrow">{{ 'about.capabilities.items.service.eyebrow' | translate }}</p>
    <h1 class="ec-page-title">{{ 'nav.supportTickets' | translate }}</h1>
    <p class="ec-page-description">
      {{ 'serviceManagement.supportPageDescription' | translate }}
    </p>
  </header>

  <section class="service-summary">
    <article class="summary-card">
      <span>{{ 'serviceManagement.supportSummaryTitle' | translate }}</span>
      <strong>{{ serviceManagementFacade.supportTickets().length }}</strong>
      <p>{{ 'serviceManagement.supportSummaryDescription' | translate }}</p>
    </article>

    <article class="summary-card">
      <span>{{ 'serviceManagement.openTicketsShort' | translate }}</span>
      <strong>{{ serviceManagementFacade.openSupportTickets().length }}</strong>
    </article>

    <article class="summary-card">
      <span>{{ 'serviceManagement.urgentTicketsShort' | translate }}</span>
      <strong>{{ serviceManagementFacade.urgentSupportTickets().length }}</strong>
    </article>
  </section>

  <section class="operation-toolbar">
    <div>
      <p class="section-eyebrow">{{ 'serviceManagement.newSupportRequest' | translate }}</p>
      <h2>{{ 'serviceManagement.supportAttentionTitle' | translate }}</h2>
      <p>{{ 'serviceManagement.supportAttentionDescription' | translate }}</p>
    </div>

    <app-button variant="create" (pressed)="openSupportModal()">
      {{ 'serviceManagement.createSupportRequest' | translate }}
    </app-button>
  </section>

  @if (serviceManagementFacade.loading()) {
    <div class="loading-wrapper">
      <app-loading-spinner />
    </div>
  } @else {
    <section class="ticket-section">
      <header class="section-heading">
        <div>
          <span>{{ 'serviceManagement.cases' | translate }}</span>
          <h2>{{ 'serviceManagement.supportCasesTitle' | translate }}</h2>
        </div>
      </header>

      <div class="list">
        @for (ticket of serviceManagementFacade.sortedSupportTickets(); track ticket.id) {
          <app-support-ticket-card
            [ticket]="ticket"
            (statusChanged)="updateStatus($event)"
            (remove)="deleteTicket($event)"
          />
        } @empty {
          <app-empty-state
            [eyebrow]="'nav.supportTickets' | translate"
            [title]="'serviceManagement.emptySupportRequestsTitle' | translate"
            [description]="'serviceManagement.emptySupportRequestsDescription' | translate"
          >
            <app-button variant="create" size="sm" (pressed)="openSupportModal()">
              {{ 'serviceManagement.createSupportRequest' | translate }}
            </app-button>
          </app-empty-state>
        }
      </div>
    </section>
  }

  <app-modal-form-shell
    [open]="isSupportModalOpen"
    [eyebrow]="'about.capabilities.items.service.eyebrow' | translate"
    [title]="'serviceManagement.createSupportRequest' | translate"
    [description]="'serviceManagement.supportModalDescription' | translate"
    (closed)="closeSupportModal()"
  >
    <form class="modal-form" (ngSubmit)="createTicket()">
      <label>
        <span>{{ 'serviceManagement.subject' | translate }}</span>
        <input
          type="text"
          name="subject"
          [(ngModel)]="subject"
          [placeholder]="'serviceManagement.supportSubjectPlaceholder' | translate"
          required
        />
      </label>

      <label>
        <span>{{ 'serviceManagement.description' | translate }}</span>
        <textarea
          name="description"
          [(ngModel)]="description"
          rows="5"
          [placeholder]="'serviceManagement.supportDescriptionPlaceholder' | translate"
          required
        ></textarea>
      </label>

      <label>
        <span>{{ 'serviceManagement.priority' | translate }}</span>
        <app-dropdown
          [options]="priorityOptions"
          [value]="priority"
          [placeholder]="'serviceManagement.priority' | translate"
          (valueChange)="selectPriority($event)"
        />
      </label>

      <div class="form-actions">
        <app-button
          variant="secondary"
          type="button"
          (pressed)="closeSupportModal()"
        >
          {{ 'common.cancel' | translate }}
        </app-button>

        <app-button
          variant="create"
          type="submit"
          [disabled]="serviceManagementFacade.loading()"
        >
          {{ 'serviceManagement.saveSupportRequest' | translate }}
        </app-button>
      </div>
    </form>
  </app-modal-form-shell>
</section>


============================================================
FILE: src\app\service-management\presentation\pages\support-tickets\support-tickets-page.component.scss
============================================================

.service-page {
  display: grid;
  gap: 24px;
}

.service-summary {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;
}

.summary-grid {
  display: grid;
  grid-template-columns: minmax(280px, 1.4fr) repeat(2, minmax(170px, 0.8fr));
  gap: 16px;
}

.service-hero {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(280px, 360px);
  gap: 20px;
  align-items: stretch;
}

.service-hero__copy,
.service-hero__action {
  border: 1px solid var(--border-color);
  border-radius: 24px;
  background: var(--surface-card);
  box-shadow: var(--shadow-soft);
}

.service-hero__copy {
  padding: 30px;
}

.service-hero__action {
  display: grid;
  align-content: space-between;
  gap: 18px;
  padding: 24px;
}

.service-hero__copy h1 {
  margin: 0 0 12px;
  color: var(--text-primary);
  font-size: clamp(2.6rem, 5vw, 4.7rem);
  line-height: 1;
}

.service-hero__copy p:not(.eyebrow),
.service-hero__action p {
  margin: 0;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: 1.7;
}

.eyebrow {
  width: fit-content;
  margin: 0 0 16px;
  padding: 9px 15px;
  border: 1px solid var(--border-color);
  border-radius: 999px;
  color: var(--accent-color);
  background: var(--surface-color);
  font-size: 0.88rem;
  font-weight: 950;
  letter-spacing: 0.12em;
  line-height: 1;
  text-transform: uppercase;
}

.summary-card,
.ticket-section {
  border: 1px solid var(--border-color);
  background: var(--surface-card);
  box-shadow: var(--shadow-soft);
}

.summary-card span,
.section-heading span,
.section-eyebrow,
.service-hero__action span,
.modal-form span {
  color: var(--accent-color);
  font-size: 0.74rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  line-height: 1;
  text-transform: uppercase;
}

.summary-card {
  display: grid;
  gap: 9px;
  min-height: 132px;
  padding: 18px;
  border-radius: 18px;
  background:
    linear-gradient(135deg, rgba(56, 189, 248, 0.09), transparent 42%),
    var(--surface-card);
}

.summary-card--wide {
  grid-template-columns: auto 1fr;
  gap: 8px 18px;
  align-items: center;
}

.summary-card--wide p {
  grid-column: 1 / -1;
}

.summary-card strong {
  display: block;
  color: var(--text-primary);
  font-size: clamp(2rem, 5vw, 3rem);
  line-height: 1;
}

.summary-card p {
  margin: 0;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: 1.55;
}

.operation-toolbar {
  display: flex;
  justify-content: space-between;
  gap: 18px;
  align-items: center;
  border: 1px solid var(--border-color);
  border-radius: 24px;
  padding: 22px;
  background: var(--surface-color);
}

.operation-toolbar h2,
.operation-toolbar p {
  margin: 0;
}

.operation-toolbar h2 {
  color: var(--text-color);
  font-size: 1.3rem;
}

.operation-toolbar p:not(.section-eyebrow) {
  margin-top: 6px;
  color: var(--text-secondary);
  font-weight: 700;
}

.ticket-section {
  display: grid;
  gap: 20px;
  padding: 24px;
  border-radius: 24px;
}

.section-heading {
  display: flex;
  justify-content: space-between;
  gap: 16px;
  align-items: center;
}

.section-heading h2 {
  margin: 4px 0 0;
  color: var(--text-primary);
  font-size: clamp(1.25rem, 3vw, 1.65rem);
}

.list {
  display: grid;
  gap: 14px;
}

.modal-form {
  display: grid;
  gap: 18px;
}

.modal-form label {
  display: grid;
  gap: 8px;
}

.modal-form input,
.modal-form textarea {
  width: 100%;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  padding: 14px 16px;
  color: var(--text-primary);
  background: var(--field-bg);
  font: inherit;
  font-weight: 700;
  outline: none;
  transition:
    border-color 0.18s ease,
    box-shadow 0.18s ease,
    background 0.18s ease;
}

.modal-form textarea {
  min-height: 132px;
  resize: vertical;
}

.modal-form input:focus,
.modal-form textarea:focus {
  border-color: var(--border-strong);
  box-shadow: var(--ec-focus-ring);
}

.form-actions {
  display: flex;
  justify-content: flex-end;
  gap: 12px;
  flex-wrap: wrap;
  margin-top: 4px;
}

app-dropdown,
app-date-picker {
  width: 100%;
}

.loading-wrapper {
  display: grid;
  place-items: center;
  min-height: 220px;
}

@media (max-width: 960px) {
  .service-hero,
  .service-summary,
  .summary-grid,
  .summary-card--wide {
    grid-template-columns: 1fr;
  }

  .operation-toolbar {
    align-items: stretch;
    flex-direction: column;
  }
}

@media (max-width: 620px) {
  .service-hero__copy,
  .service-hero__action,
  .ticket-section,
  .summary-card {
    border-radius: 20px;
    padding: 18px;
  }

  .form-actions {
    flex-direction: column-reverse;
  }

  .form-actions app-button {
    width: 100%;
  }
}


============================================================
FILE: src\app\service-management\presentation\pages\support-tickets\support-tickets-page.component.ts
============================================================

import { TranslateModule, TranslateService } from '@ngx-translate/core';
import { Component, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';

import { ConfirmDialogService } from '../../../../shared/application/services/confirm-dialog.service';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { ModalFormShellComponent } from '../../../../shared/presentation/components/modal-form-shell/modal-form-shell.component';

import { ServiceManagementFacade } from '../../../application/services/service-management.facade';
import {
  SupportTicketPriority,
  SupportTicketStatus,
} from '../../../domain/model/support-ticket.entity';
import { SupportTicketCardComponent } from '../../components/support-ticket-card/support-ticket-card.component';

@Component({
  selector: 'app-support-tickets-page',
  standalone: true,
  imports: [
    TranslateModule,
    FormsModule,
    AppButtonComponent,
    AppDropdownComponent,
    EmptyStateComponent,
    LoadingSpinnerComponent,
    ModalFormShellComponent,
    SupportTicketCardComponent,
  ],
  templateUrl: './support-tickets-page.component.html',
  styleUrls: ['./support-tickets-page.component.scss'],
})
export class SupportTicketsPageComponent implements OnInit {
  subject = '';
  description = '';
  priority: SupportTicketPriority = 'MEDIUM';
  isSupportModalOpen = false;

  readonly priorities: SupportTicketPriority[] = [
    'LOW',
    'MEDIUM',
    'HIGH',
    'URGENT',
  ];

  get priorityOptions(): DropdownOption[] {
    return this.priorities.map((priority) => ({
      label: priority,
      labelKey: this.priorityLabelKey(priority),
      value: priority,
    }));
  }

  constructor(
    readonly serviceManagementFacade: ServiceManagementFacade,
    private readonly toastService: ToastService,
    private readonly confirmDialog: ConfirmDialogService,
    private readonly translate: TranslateService
  ) {}

  async ngOnInit(): Promise<void> {
    await this.serviceManagementFacade.loadServiceManagement();

    if (this.serviceManagementFacade.error()) {
      this.toastService.error(this.t('serviceManagement.supportLoadError'));
    }
  }

  openSupportModal(): void {
    this.isSupportModalOpen = true;
  }

  closeSupportModal(): void {
    this.isSupportModalOpen = false;
  }

  async createTicket(): Promise<void> {
    const subject = this.subject.trim();
    const description = this.description.trim();

    if (!subject) {
      this.toastService.warning(this.t('serviceManagement.validation.supportSubjectRequired'));
      return;
    }

    if (subject.length < 4) {
      this.toastService.warning(this.t('serviceManagement.validation.supportSubjectMinLength'));
      return;
    }

    if (!description) {
      this.toastService.warning(this.t('serviceManagement.validation.supportDescriptionRequired'));
      return;
    }

    if (description.length < 10) {
      this.toastService.warning(this.t('serviceManagement.validation.descriptionMinLength'));
      return;
    }

    const success = await this.serviceManagementFacade.createSupportTicket({
      subject,
      description,
      priority: this.priority,
    });

    if (success) {
      this.subject = '';
      this.description = '';
      this.priority = 'MEDIUM';
      this.closeSupportModal();
      this.toastService.success(this.t('serviceManagement.supportCreateSuccess'));
      return;
    }

    this.toastService.error(this.t('serviceManagement.supportCreateError'));
  }

  selectPriority(value: string): void {
    this.priority = value as SupportTicketPriority;
  }

  priorityLabel(priority: SupportTicketPriority): string {
    return this.t(this.priorityLabelKey(priority));
  }

  priorityLabelKey(priority: SupportTicketPriority): string {
    const labels: Record<SupportTicketPriority, string> = {
      LOW: 'serviceManagement.priorities.low',
      MEDIUM: 'serviceManagement.priorities.medium',
      HIGH: 'serviceManagement.priorities.high',
      URGENT: 'serviceManagement.priorities.urgent',
    };

    return labels[priority] ?? priority;
  }

  async updateStatus(statusChange: {
    ticketId: number;
    status: SupportTicketStatus;
  }): Promise<void> {
    const success = await this.serviceManagementFacade.updateSupportTicketStatus(statusChange);

    if (success) {
      this.toastService.info(this.t('serviceManagement.supportUpdateSuccess'));
      return;
    }

    this.toastService.error(this.t('serviceManagement.supportUpdateError'));
  }

  async deleteTicket(ticketId: number): Promise<void> {
    const confirmed = await this.confirmDialog.confirm({
      title: this.t('serviceManagement.confirmDeleteSupportTitle'),
      message: this.t('serviceManagement.confirmDeleteSupportMessage'),
      confirmLabel: this.t('common.delete'),
      cancelLabel: this.t('common.keep'),
      tone: 'danger',
    });

    if (!confirmed) {
      return;
    }

    const success = await this.serviceManagementFacade.deleteSupportTicket(ticketId);

    if (success) {
      this.toastService.info(this.t('serviceManagement.supportDeleteSuccess'));
      return;
    }

    this.toastService.error(this.t('serviceManagement.supportDeleteError'));
  }

  private t(key: string): string {
    return this.translate.instant(key);
  }
}


============================================================
FILE: src\app\service-management\presentation\routes\service-management.routes.ts
============================================================

import { Routes } from '@angular/router';

import { activeSubscriptionGuard } from '../../../shared/application/guards/active-subscription.guard';

export const SERVICE_MANAGEMENT_ROUTES: Routes = [
  {
    path: 'service/support',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Soporte' },
    loadComponent: () =>
      import('../pages/support-tickets/support-tickets-page.component').then(
        (m) => m.SupportTicketsPageComponent
      ),
  },
  {
    path: 'service/maintenance',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Mantenimiento' },
    loadComponent: () =>
      import(
        '../pages/maintenance-tickets/maintenance-tickets-page.component'
        ).then((m) => m.MaintenanceTicketsPageComponent),
  },
  {
    path: 'support-tickets',
    redirectTo: 'service/support',
    pathMatch: 'full',
  },
  {
    path: 'maintenance-tickets',
    redirectTo: 'service/maintenance',
    pathMatch: 'full',
  },
];


============================================================
FILE: src\app\shared\application\guards\active-subscription.guard.ts
============================================================

import { inject } from '@angular/core';
import { CanActivateFn, Router } from '@angular/router';

import { AuthSessionService } from '../services/auth-session.service';
import { ROUTE_PATHS } from '../../infrastructure/constants/route-paths';
import { BillingFacade } from '../../../billing/application/services/billing.facade';

export const activeSubscriptionGuard: CanActivateFn = () => {
  const authSession = inject(AuthSessionService);
  const billingFacade = inject(BillingFacade);
  const router = inject(Router);

  if (!authSession.isAuthenticated()) {
    return router.createUrlTree([ROUTE_PATHS.IAM.LOGIN]);
  }

  return billingFacade.loadActiveSubscription().then(() => {
    if (billingFacade.hasActiveSubscription()) {
      return true;
    }

    return router.createUrlTree([ROUTE_PATHS.BILLING.PLANS]);
  }).catch((error) => {
    console.error(error);
    billingFacade.clearActiveSubscription();
    return router.createUrlTree([ROUTE_PATHS.BILLING.PLANS]);
  });
};


============================================================
FILE: src\app\shared\application\guards\auth.guard.ts
============================================================

import { inject } from '@angular/core';
import { CanActivateFn, Router } from '@angular/router';

import { AuthSessionService } from '../services/auth-session.service';

export const authGuard: CanActivateFn = () => {
  const authSession = inject(AuthSessionService);
  const router = inject(Router);

  if (authSession.isAuthenticated()) {
    return true;
  }

  return router.createUrlTree(['/iam/login']);
};

============================================================
FILE: src\app\shared\application\interceptors\auth.interceptor.ts
============================================================

// src/app/shared/application/interceptors/auth.interceptor.ts

import { HttpInterceptorFn } from '@angular/common/http';
import { inject } from '@angular/core';

import { AuthSessionService } from '../services/auth-session.service';

export const authInterceptor: HttpInterceptorFn = (request, next) => {
  const authSession = inject(AuthSessionService);
  const token = authSession.token();

  const authenticatedRequest = request.clone({
    withCredentials: true,
    ...(token
      ? {
          setHeaders: {
            Authorization: `Bearer ${token}`,
          },
        }
      : {}),
  });

  return next(authenticatedRequest);
};


============================================================
FILE: src\app\shared\application\interceptors\error.interceptor.ts
============================================================

// src/app/shared/application/interceptors/error.interceptor.ts

import { HttpErrorResponse, HttpInterceptorFn } from '@angular/common/http';
import { catchError, throwError } from 'rxjs';

export const errorInterceptor: HttpInterceptorFn = (request, next) => {
  return next(request).pipe(
    catchError((error: HttpErrorResponse) => {
      console.error('HTTP Error:', {
        status: error.status,
        message: error.message,
        url: error.url,
      });

      return throwError(() => error);
    })
  );
};

============================================================
FILE: src\app\shared\application\services\auth-session.service.ts
============================================================

import { computed, Injectable, signal } from '@angular/core';

export interface AuthenticatedUserSession {
  id: number;
  fullName: string;
  email: string;
  token: string;
  accessProfileId?: number;
  accessProfileName?: string;
}

@Injectable({
  providedIn: 'root',
})
export class AuthSessionService {
  private readonly storageKey = 'electrocorp_auth_session';
  private readonly currentUserSignal = signal<AuthenticatedUserSession | null>(null);
  private readonly loadingSignal = signal<boolean>(false);

  readonly currentUser = computed(() => this.currentUserSignal());
  readonly loading = computed(() => this.loadingSignal());

  readonly isAuthenticated = computed(() => this.currentUserSignal() !== null);

  readonly userId = computed(() => this.currentUserSignal()?.id ?? null);
  readonly token = computed(() => this.currentUserSignal()?.token ?? null);
  readonly userEmail = computed(() => this.currentUserSignal()?.email ?? null);
  readonly userFullName = computed(() => this.currentUserSignal()?.fullName ?? null);
  readonly accessProfileName = computed(
    () => this.currentUserSignal()?.accessProfileName ?? null
  );

  setLoading(value: boolean): void {
    this.loadingSignal.set(value);
  }

  setCurrentUser(user: AuthenticatedUserSession): void {
    this.currentUserSignal.set(user);
    localStorage.setItem(this.storageKey, JSON.stringify(user));
  }

  clearSession(): void {
    this.currentUserSignal.set(null);
    localStorage.removeItem(this.storageKey);
  }

  restoreStoredSession(): AuthenticatedUserSession | null {
    const rawSession = localStorage.getItem(this.storageKey);

    if (!rawSession) {
      return null;
    }

    try {
      const session = JSON.parse(rawSession) as AuthenticatedUserSession;

      if (!session.id || !session.email || !session.fullName || !session.token) {
        this.clearSession();
        return null;
      }

      this.currentUserSignal.set(session);
      return session;
    } catch {
      this.clearSession();
      return null;
    }
  }
}


============================================================
FILE: src\app\shared\application\services\confirm-dialog.service.ts
============================================================

import { computed, Injectable, signal } from '@angular/core';

export interface ConfirmDialogOptions {
  title?: string;
  message: string;
  detail?: string;
  confirmLabel?: string;
  cancelLabel?: string;
  tone?: ConfirmDialogTone;
  requiredText?: string;
  requiredTextLabel?: string;
  requiredTextHint?: string;
}

export interface ConfirmDialogState {
  title: string;
  message: string;
  detail?: string;
  confirmLabel: string;
  cancelLabel: string;
  tone: ConfirmDialogTone;
  requiredText?: string;
  requiredTextLabel?: string;
  requiredTextHint?: string;
}

export type ConfirmDialogTone = 'warning' | 'danger' | 'info';

@Injectable({
  providedIn: 'root',
})
export class ConfirmDialogService {
  private readonly activeDialog = signal<ConfirmDialogState | null>(null);
  private resolveCurrent?: (confirmed: boolean) => void;

  readonly dialog = computed(() => this.activeDialog());

  confirm(options: ConfirmDialogOptions): Promise<boolean> {
    this.close(false);

    return new Promise<boolean>((resolve) => {
      this.resolveCurrent = resolve;
      this.activeDialog.set({
        title: options.title ?? 'Confirmar accion',
        message: options.message,
        detail: options.detail,
        confirmLabel: options.confirmLabel ?? 'Confirmar',
        cancelLabel: options.cancelLabel ?? 'Cancelar',
        tone: options.tone ?? 'warning',
        requiredText: options.requiredText,
        requiredTextLabel: options.requiredTextLabel,
        requiredTextHint: options.requiredTextHint,
      });
    });
  }

  async confirmSequence(options: ConfirmDialogOptions[]): Promise<boolean> {
    for (const option of options) {
      const accepted = await this.confirm(option);

      if (!accepted) {
        return false;
      }
    }

    return true;
  }

  confirmCurrent(): void {
    this.close(true);
  }

  cancelCurrent(): void {
    this.close(false);
  }

  private close(confirmed: boolean): void {
    if (!this.activeDialog()) {
      return;
    }

    const resolve = this.resolveCurrent;
    this.resolveCurrent = undefined;
    this.activeDialog.set(null);
    resolve?.(confirmed);
  }
}


============================================================
FILE: src\app\shared\application\services\theme.service.ts
============================================================

import { Injectable, computed, inject } from '@angular/core';

import { AppTheme, UiPreferencesService } from './ui-preferences.service';

export type { AppTheme };

@Injectable({
  providedIn: 'root',
})
export class ThemeService {
  private readonly uiPreferences = inject(UiPreferencesService);

  readonly currentTheme = computed<AppTheme>(() => this.uiPreferences.isDarkMode() ? 'dark' : 'light');
  readonly isDarkMode = computed(() => this.uiPreferences.isDarkMode());

  toggleTheme(): void {
    this.uiPreferences.toggleTheme();
  }

  setTheme(theme: AppTheme): void {
    this.uiPreferences.setTheme(theme);
  }
}


============================================================
FILE: src\app\shared\application\services\toast.service.ts
============================================================

import { Injectable, signal } from '@angular/core';

export type ToastType = 'success' | 'info' | 'warning' | 'critical' | 'error';

export interface ToastMessage {
  id: number;
  type: ToastType;
  message: string;
  leaving?: boolean;
}

@Injectable({ providedIn: 'root' })
export class ToastService {
  readonly toasts = signal<ToastMessage[]>([]);

  private nextId = 1;
  private readonly defaultDurationMs = 3600;
  private readonly exitDurationMs = 320;

  success(message: string): void {
    this.show('success', message);
  }

  info(message: string): void {
    this.show('info', message);
  }

  warning(message: string): void {
    this.show('warning', message);
  }

  critical(message: string): void {
    this.show('critical', message, 5200);
  }

  error(message: string): void {
    this.show('error', message, 5200);
  }

  dismiss(id: number): void {
    const target = this.toasts().find((toast) => toast.id === id);

    if (!target || target.leaving) {
      return;
    }

    this.toasts.update((toasts) =>
      toasts.map((toast) =>
        toast.id === id ? { ...toast, leaving: true } : toast
      )
    );

    window.setTimeout(() => {
      this.toasts.update((toasts) => toasts.filter((toast) => toast.id !== id));
    }, this.exitDurationMs);
  }

  private show(type: ToastType, message: string, durationMs = this.defaultDurationMs): void {
    const toast: ToastMessage = {
      id: this.nextId++,
      type,
      message,
    };

    this.toasts.update((toasts) => [toast, ...toasts].slice(0, 5));
    window.setTimeout(() => this.dismiss(toast.id), durationMs);
  }
}


============================================================
FILE: src\app\shared\application\services\translation.service.ts
============================================================

// src/app/shared/application/services/translation.service.ts

import { Injectable, computed, signal } from '@angular/core';
import { TranslateService } from '@ngx-translate/core';

import {
  APP_LANGUAGES,
  AppLanguageCode,
  DEFAULT_LANGUAGE,
  SUPPORTED_LANGUAGES,
} from '../../infrastructure/constants/app-language';

export type AppLanguage = AppLanguageCode;

@Injectable({
  providedIn: 'root',
})
export class TranslationService {
  private readonly currentLanguageSignal = signal<AppLanguage>(DEFAULT_LANGUAGE);

  readonly currentLanguage = computed(() => this.currentLanguageSignal());

  readonly availableLanguages = SUPPORTED_LANGUAGES;

  constructor(private readonly translate: TranslateService) {
    this.translate.addLangs(SUPPORTED_LANGUAGES);
    this.translate.setDefaultLang(DEFAULT_LANGUAGE);
    this.use(DEFAULT_LANGUAGE);
  }

  use(language: AppLanguage): void {
    const nextLanguage = this.availableLanguages.includes(language)
      ? language
      : DEFAULT_LANGUAGE;

    this.currentLanguageSignal.set(nextLanguage);
    this.translate.use(nextLanguage);
  }

  toggleLanguage(): void {
    const current = this.currentLanguageSignal();

    if (current === APP_LANGUAGES.ES) {
      this.use(APP_LANGUAGES.EN);
      return;
    }

    if (current === APP_LANGUAGES.EN) {
      this.use(APP_LANGUAGES.PT);
      return;
    }

    this.use(APP_LANGUAGES.ES);
  }
}

============================================================
FILE: src\app\shared\application\services\ui-preferences.service.ts
============================================================

import { DOCUMENT } from '@angular/common';
import { Injectable, effect, inject, signal } from '@angular/core';
import { TranslateService } from '@ngx-translate/core';

import { DEFAULT_LANGUAGE, SUPPORTED_LANGUAGES } from '../../infrastructure/constants/app-language';
import { STORAGE_KEYS } from '../../infrastructure/constants/storage-keys';

type LanguageCode = 'es' | 'en' | 'pt';
export type AppTheme = 'light' | 'dark';

const LEGACY_LANGUAGE_KEY = 'electrocorp_language';
const LEGACY_THEME_KEY = 'electrocorp_theme';

@Injectable({
  providedIn: 'root'
})
export class UiPreferencesService {
  private readonly document = inject(DOCUMENT);
  private readonly translate = inject(TranslateService);
  private readonly storage = this.document.defaultView?.localStorage;

  readonly currentLanguage = signal<LanguageCode>(this.readStoredLanguage());

  readonly isDarkMode = signal(this.readStoredTheme() === 'dark');

  constructor() {
    this.translate.addLangs(SUPPORTED_LANGUAGES);
    this.translate.setFallbackLang(DEFAULT_LANGUAGE);
    this.translate.use(this.currentLanguage());

    effect(() => {
      const language = this.currentLanguage();
      this.writeStorage(STORAGE_KEYS.language, language);
      this.writeStorage(LEGACY_LANGUAGE_KEY, language);
      this.translate.use(language);
    });

    effect(() => {
      const darkMode = this.isDarkMode();
      this.applyTheme(darkMode ? 'dark' : 'light');
    });
  }

  setLanguage(language: LanguageCode): void {
    this.currentLanguage.set(language);
  }

  toggleLanguage(): void {
    const current = this.currentLanguage();

    const nextLanguage: LanguageCode =
      current === 'es' ? 'en' :
        current === 'en' ? 'pt' :
          'es';

    this.setLanguage(nextLanguage);
  }

  toggleTheme(): void {
    this.isDarkMode.update((value) => !value);
  }

  setTheme(theme: AppTheme): void {
    this.isDarkMode.set(theme === 'dark');
  }

  private applyTheme(theme: AppTheme): void {
    const root = this.document.documentElement;

    root.classList.remove('light-theme', 'dark-theme');
    root.classList.add(`${theme}-theme`);
    root.dataset['theme'] = theme;
    root.style.colorScheme = theme;

    this.writeStorage(LEGACY_THEME_KEY, theme);
    this.writeStorage(STORAGE_KEYS.darkMode, String(theme === 'dark'));
  }

  private readStoredLanguage(): LanguageCode {
    const storedLanguage =
      this.readStorage(STORAGE_KEYS.language) ??
      this.readStorage(LEGACY_LANGUAGE_KEY);

    return this.isSupportedLanguage(storedLanguage) ? storedLanguage : DEFAULT_LANGUAGE;
  }

  private readStoredTheme(): AppTheme {
    const storedTheme = this.readStorage(LEGACY_THEME_KEY);

    if (storedTheme === 'light' || storedTheme === 'dark') {
      return storedTheme;
    }

    const storedDarkMode = this.readStorage(STORAGE_KEYS.darkMode);

    if (storedDarkMode === 'false') {
      return 'light';
    }

    return 'dark';
  }

  private isSupportedLanguage(language: string | null): language is LanguageCode {
    return SUPPORTED_LANGUAGES.includes(language as LanguageCode);
  }

  private readStorage(key: string): string | null {
    try {
      return this.storage?.getItem(key) ?? null;
    } catch {
      return null;
    }
  }

  private writeStorage(key: string, value: string): void {
    try {
      this.storage?.setItem(key, value);
    } catch {
      return;
    }
  }
}


============================================================
FILE: src\app\shared\application\services\ui-state.service.ts
============================================================

// src/app/shared/application/services/ui-state.service.ts

import { Injectable } from '@angular/core';

import { SharedUiStore } from '../stores/shared-ui.store';

@Injectable({
  providedIn: 'root',
})
export class UiStateService {
  constructor(private readonly store: SharedUiStore) {}

  get sidebarOpen() {
    return this.store.sidebarOpen;
  }

  get globalLoading() {
    return this.store.globalLoading;
  }

  openSidebar(): void {
    this.store.setSidebarOpen(true);
  }

  closeSidebar(): void {
    this.store.setSidebarOpen(false);
  }

  toggleSidebar(): void {
    this.store.toggleSidebar();
  }

  setGlobalLoading(value: boolean): void {
    this.store.setGlobalLoading(value);
  }

  reset(): void {
    this.store.reset();
  }
}

============================================================
FILE: src\app\shared\application\stores\shared-ui.store.ts
============================================================

import { Injectable, computed, signal } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class SharedUiStore {
  private readonly sidebarOpenSignal = signal<boolean>(false);
  private readonly globalLoadingSignal = signal<boolean>(false);

  readonly sidebarOpen = computed(() => this.sidebarOpenSignal());
  readonly globalLoading = computed(() => this.globalLoadingSignal());

  setSidebarOpen(value: boolean): void {
    this.sidebarOpenSignal.set(value);
  }

  toggleSidebar(): void {
    this.sidebarOpenSignal.update((value) => !value);
  }

  setGlobalLoading(value: boolean): void {
    this.globalLoadingSignal.set(value);
  }

  reset(): void {
    this.sidebarOpenSignal.set(false);
    this.globalLoadingSignal.set(false);
  }
}

============================================================
FILE: src\app\shared\domain\model\base.entity.ts
============================================================

export abstract class BaseEntity<TId> {
  protected constructor(private readonly _id: TId) {}

  get id(): TId {
    return this._id;
  }
}

============================================================
FILE: src\app\shared\infrastructure\api\api-config.ts
============================================================

import { InjectionToken } from '@angular/core';

export const API_BASE_URL = new InjectionToken<string>('API_BASE_URL');

============================================================
FILE: src\app\shared\infrastructure\api\base-api.service.ts
============================================================

// src/app/shared/infrastructure/api/base-api.service.ts

import { Inject } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from './api-config';
import { BaseAssembler } from '../assemblers/base.assembler';

export abstract class BaseApiService<TEntity, TResource, TResponse> {
  protected readonly resourceEndpoint: string;

  protected constructor(
    protected readonly http: HttpClient,
    @Inject(API_BASE_URL) protected readonly apiBaseUrl: string,
    endpoint: string,
    protected readonly assembler: BaseAssembler<TEntity, TResource, TResponse>
  ) {
    this.resourceEndpoint = `${this.apiBaseUrl}/${endpoint}`;
  }

  findAll(): Observable<TResponse[]> {
    return this.http.get<TResponse[]>(this.resourceEndpoint);
  }

  findById(id: number | string): Observable<TResponse> {
    return this.http.get<TResponse>(`${this.resourceEndpoint}/${id}`);
  }

  create(resource: TResource): Observable<TResponse> {
    return this.http.post<TResponse>(this.resourceEndpoint, resource);
  }

  update(id: number | string, resource: Partial<TResource>): Observable<TResponse> {
    return this.http.put<TResponse>(`${this.resourceEndpoint}/${id}`, resource);
  }

  patch(id: number | string, resource: Partial<TResource>): Observable<TResponse> {
    return this.http.patch<TResponse>(`${this.resourceEndpoint}/${id}`, resource);
  }

  delete(id: number | string): Observable<void> {
    return this.http.delete<void>(`${this.resourceEndpoint}/${id}`);
  }
}

============================================================
FILE: src\app\shared\infrastructure\assemblers\base.assembler.ts
============================================================

export abstract class BaseAssembler<TEntity, TResource, TResponse> {
  abstract toEntity(response: TResponse): TEntity;

  abstract toResource(entity: TEntity): TResource;
}

============================================================
FILE: src\app\shared\infrastructure\constants\app-language.ts
============================================================

export const APP_LANGUAGES = {
  ES: 'es',
  EN: 'en',
  PT: 'pt',
} as const;

export type AppLanguageCode =
  (typeof APP_LANGUAGES)[keyof typeof APP_LANGUAGES];

export const DEFAULT_LANGUAGE: AppLanguageCode = APP_LANGUAGES.ES;

export const SUPPORTED_LANGUAGES: AppLanguageCode[] = [
  APP_LANGUAGES.ES,
  APP_LANGUAGES.EN,
  APP_LANGUAGES.PT,
];

export const LANGUAGE_LABELS: Record<AppLanguageCode, string> = {
  es: 'Español',
  en: 'English',
  pt: 'Português',
};

============================================================
FILE: src\app\shared\infrastructure\constants\http-status-codes.ts
============================================================

export const HTTP_STATUS_CODES = {
  OK: 200,
  CREATED: 201,
  NO_CONTENT: 204,

  BAD_REQUEST: 400,
  UNAUTHORIZED: 401,
  FORBIDDEN: 403,
  NOT_FOUND: 404,
  CONFLICT: 409,
  UNPROCESSABLE_ENTITY: 422,

  INTERNAL_SERVER_ERROR: 500,
  BAD_GATEWAY: 502,
  SERVICE_UNAVAILABLE: 503,
} as const;

export type HttpStatusCode =
  (typeof HTTP_STATUS_CODES)[keyof typeof HTTP_STATUS_CODES];

============================================================
FILE: src\app\shared\infrastructure\constants\route-paths.ts
============================================================

export const ROUTE_PATHS = {
  IAM: {
    LOGIN: '/iam/login',
    REGISTER: '/iam/register',
    RECOVER_PASSWORD: '/iam/recover-password',
    PROFILE: '/settings/profile',
    ACCOUNT: '/settings/account',
    SECURITY: '/settings/security',
    PLATFORM: '/settings/platform',
  },

  HOME: '/home',
  ABOUT: '/about',

  BILLING: {
    SETTINGS: '/settings/billing',
    PLANS: '/plans',
    HISTORY: '/settings/billing',
  },

  DEVICE_CONTROL: {
    DEVICES: '/operation/devices',
    ROUTINES: '/operation/routines',
    DEVICE_GROUPS: '/operation/groups',
    OPERATION_MODES: '/operation/modes',
  },

  ENERGY_MONITORING: {
    DASHBOARD: '/energy/consumption',
    HISTORY: '/energy/history',
  },

  NOTIFICATIONS: {
    ALERTS: '/alerts/inbox',
    ALERT_RULES: '/alerts/rules',
    PREFERENCES: '/alerts/preferences',
  },

  WORKPLACE: {
    OVERVIEW: '/spaces/sites',
    LOCATIONS: '/spaces/sites',
    ROOMS: '/spaces/rooms',
    DEVICE_ASSIGNMENTS: '/spaces/assignments',
  },

  REPORTING: {
    REPORTS: '/energy/reports',
    ENERGY_GOALS: '/energy/goals',
  },

  SERVICE_MANAGEMENT: {
    SUPPORT_TICKETS: '/service/support',
    MAINTENANCE_TICKETS: '/service/maintenance',
  },

  LEGACY: {
    IAM_PROFILE: '/iam/profile',
    BILLING_PLANS: '/plans',
    BILLING_HISTORY: '/billing/history',
    ENERGY_DASHBOARD: '/energy',
    WORKPLACE: '/workplace',
    WORKPLACE_LOCATIONS: '/workplace/locations',
    WORKPLACE_ROOMS: '/workplace/rooms',
    WORKPLACE_DEVICE_ASSIGNMENTS: '/workplace/device-assignments',
    DEVICES: '/devices',
    DEVICE_GROUPS: '/device-groups',
    ROUTINES: '/routines',
    OPERATION_MODES: '/operation-modes',
    ALERTS: '/alerts',
    NOTIFICATION_PREFERENCES: '/notifications/preferences',
    SUPPORT_TICKETS: '/support-tickets',
    MAINTENANCE_TICKETS: '/maintenance-tickets',
    REPORTS: '/reports',
    ENERGY_GOALS: '/reports/energy-goals',
  },
} as const;


============================================================
FILE: src\app\shared\infrastructure\constants\storage-keys.ts
============================================================

export const STORAGE_KEYS = {
  auth: 'ec.auth',
  userId: 'ec.user.id',
  userEmail: 'ec.user.email',
  userName: 'ec.user.name',
  language: 'ec.language',
  darkMode: 'ec.darkMode',
  activeWorkplaceId: 'ec.activeWorkplaceId',
  notificationClassificationPolicy: 'ec.notifications.classificationPolicy',
} as const;


============================================================
FILE: src\app\shared\infrastructure\resources\base.resource.ts
============================================================

export interface BaseResource {}

============================================================
FILE: src\app\shared\infrastructure\responses\base.response.ts
============================================================

export interface BaseResponse<TId = number> {
  id: TId;
}

============================================================
FILE: src\app\shared\infrastructure\storage\local-storage.service.ts
============================================================

import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class LocalStorageService {
  getItem(key: string): string | null {
    return localStorage.getItem(key);
  }

  setItem(key: string, value: string): void {
    localStorage.setItem(key, value);
  }

  removeItem(key: string): void {
    localStorage.removeItem(key);
  }

  clear(): void {
    localStorage.clear();
  }
}


============================================================
FILE: src\app\shared\presentation\components\app-button\app-button.component.html
============================================================

@if (link) {
  <a
    [routerLink]="link"
    [class]="className"
    [attr.aria-label]="ariaLabel || null"
    [attr.aria-disabled]="disabled"
    (click)="onPressed($event)"
  >
    <span class="app-button__content">
      @if (label) {
        {{ label }}
      } @else {
        <ng-content></ng-content>
      }
    </span>
  </a>
} @else {
  <button
    [type]="type"
    [disabled]="disabled"
    [class]="className"
    [attr.aria-label]="ariaLabel || null"
    (click)="onPressed($event)"
  >
    <span class="app-button__content">
      @if (label) {
        {{ label }}
      } @else {
        <ng-content></ng-content>
      }
    </span>
  </button>
}


============================================================
FILE: src\app\shared\presentation\components\app-button\app-button.component.scss
============================================================

:host {
  display: inline-flex;
  max-width: 100%;
  min-width: 0;
}

.app-button {
  --app-button-glow-rgb: 125, 211, 252;

  position: relative;
  isolation: isolate;
  overflow: hidden;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  min-width: max-content;
  border-radius: 999px;
  border: 1px solid transparent;
  color: var(--text-on-action);
  cursor: pointer;
  font: inherit;
  font-weight: 950;
  line-height: 1;
  text-decoration: none;
  white-space: nowrap;
  box-shadow: var(--ec-action-shadow);
  transition:
    border-color 0.2s ease,
    box-shadow 0.2s ease,
    background 0.2s ease,
    color 0.2s ease,
    filter 0.2s ease,
    opacity 0.2s ease;
}

.app-button::before {
  content: '';
  position: absolute;
  inset: -72% -54% -72% auto;
  z-index: 0;
  width: 46%;
  transform: translateX(140%) skewX(-18deg);
  background:
    linear-gradient(
      105deg,
      transparent 0%,
      rgba(255, 255, 255, 0.12) 36%,
      rgba(255, 255, 255, 0.34) 50%,
      rgba(255, 255, 255, 0.12) 64%,
      transparent 100%
    );
  opacity: 0;
  transition:
    transform 0.56s cubic-bezier(0.16, 1, 0.3, 1),
    opacity 0.28s ease;
}

.app-button::after {
  content: '';
  position: absolute;
  inset: -1px;
  z-index: 0;
  border-radius: inherit;
  background:
    radial-gradient(circle at 50% 0%, rgba(var(--app-button-glow-rgb), 0.34), transparent 48%),
    linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.12), transparent);
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.22s ease;
}

.app-button__content {
  position: relative;
  z-index: 1;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: inherit;
  min-width: max-content;
}

.app-button:hover:not(:disabled):not([aria-disabled='true'])::before {
  opacity: 1;
  transform: translateX(-430%) skewX(-18deg);
}

.app-button:hover:not(:disabled):not([aria-disabled='true'])::after {
  opacity: 1;
}

.app-button:active:not(:disabled):not([aria-disabled='true']) {
  filter: brightness(0.94) saturate(1.04);
}

.app-button--sm {
  min-height: 36px;
  padding: 0 14px;
  font-size: 0.78rem;
}

.app-button--md {
  min-height: 44px;
  padding: 0 20px;
  font-size: 0.88rem;
}

.app-button--lg {
  min-height: 52px;
  padding: 0 26px;
  font-size: 0.96rem;
}

.app-button--block {
  width: 100%;
  min-width: 0;
}

.app-button--primary,
.app-button--action {
  --app-button-glow-rgb: 56, 189, 248;

  background: var(--ec-action-bg);
  border-color: rgba(125, 211, 252, 0.18);
}

.app-button--create {
  --app-button-glow-rgb: 34, 211, 238;

  background: var(--ec-create-bg);
  border-color: rgba(125, 211, 252, 0.24);
  box-shadow: var(--ec-create-shadow);
}

.app-button--secondary,
.app-button--neutral {
  --app-button-glow-rgb: 125, 211, 252;

  color: var(--text-primary);
  background: var(--ec-neutral-bg);
  border-color: var(--border-color);
  box-shadow: var(--ec-shadow-soft);
}

.app-button--danger {
  --app-button-glow-rgb: 248, 113, 113;

  background: var(--ec-danger-bg);
  border-color: rgba(254, 202, 202, 0.16);
  box-shadow: var(--ec-danger-shadow);
}

.app-button--ghost {
  --app-button-glow-rgb: 125, 211, 252;

  color: var(--text-primary);
  background: var(--ec-ghost-bg);
  border-color: var(--border-color);
  box-shadow: none;
}

.app-button--primary:hover:not(:disabled):not([aria-disabled='true']),
.app-button--action:hover:not(:disabled):not([aria-disabled='true']) {
  background: var(--ec-action-bg-hover);
  box-shadow: var(--ec-action-shadow-hover);
}

.app-button--create:hover:not(:disabled):not([aria-disabled='true']) {
  background: var(--ec-create-bg-hover);
  box-shadow: var(--ec-create-shadow-hover);
}

.app-button--secondary:hover:not(:disabled):not([aria-disabled='true']),
.app-button--neutral:hover:not(:disabled):not([aria-disabled='true']),
.app-button--ghost:hover:not(:disabled):not([aria-disabled='true']) {
  background: var(--ec-neutral-bg-hover);
  border-color: var(--border-strong);
  box-shadow:
    0 0 0 1px rgba(var(--app-button-glow-rgb), 0.1),
    0 0 20px rgba(var(--app-button-glow-rgb), 0.16);
}

.app-button--danger:hover:not(:disabled):not([aria-disabled='true']) {
  background: var(--ec-danger-bg-hover);
  box-shadow: var(--ec-danger-shadow-hover);
}

.app-button:disabled,
.app-button[aria-disabled='true'] {
  cursor: not-allowed;
  opacity: 0.5;
  filter: grayscale(0.2);
}


============================================================
FILE: src\app\shared\presentation\components\app-button\app-button.component.ts
============================================================

import { Component, EventEmitter, Input, Output } from '@angular/core';
import { RouterLink } from '@angular/router';

export type AppButtonVariant = 'primary' | 'secondary' | 'danger' | 'ghost' | 'create' | 'action' | 'neutral';
export type AppButtonSize = 'sm' | 'md' | 'lg';

@Component({
  selector: 'app-button',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './app-button.component.html',
  styleUrls: ['./app-button.component.scss'],
})
export class AppButtonComponent {
  @Input() variant: AppButtonVariant = 'primary';
  @Input() size: AppButtonSize = 'md';
  @Input() type: 'button' | 'submit' | 'reset' = 'button';
  @Input() disabled = false;
  @Input() block = false;
  @Input() link: string | unknown[] | null = null;
  @Input() ariaLabel = '';
  @Input() label = '';

  @Output() pressed = new EventEmitter<MouseEvent>();

  get className(): string {
    return [
      'app-button',
      `app-button--${this.variant}`,
      `app-button--${this.size}`,
      this.block ? 'app-button--block' : '',
    ]
      .filter(Boolean)
      .join(' ');
  }

  onPressed(event: MouseEvent): void {
    if (this.disabled) {
      event.preventDefault();
      event.stopPropagation();
      return;
    }

    this.pressed.emit(event);
  }
}


============================================================
FILE: src\app\shared\presentation\components\app-date-picker\app-date-picker.component.html
============================================================

<div class="date-picker">
  @if (label) {
    <span class="date-picker__label">{{ label }}</span>
  }

  <button
    #dateTrigger
    type="button"
    class="date-picker__trigger"
    aria-haspopup="dialog"
    [attr.aria-expanded]="shouldRenderCalendar"
    (click)="toggleCalendar()"
  >
    <span [class.placeholder]="!value">
      @if (value) {
        {{ displayValue }}
      } @else {
        {{ 'shared.datePicker.placeholder' | translate }}
      }
    </span>

    <span class="date-picker__icon">v</span>
  </button>

  @if (shouldRenderCalendar) {
    <section
      #calendarPanel
      class="calendar-panel"
      [class.closing]="isClosing"
      [attr.aria-label]="'shared.datePicker.calendarAria' | translate"
      popover="manual"
    >
      <header class="calendar-panel__header">
        <button
          type="button"
          class="nav-btn"
          [attr.aria-label]="'shared.datePicker.previousMonth' | translate"
          (click)="previousMonth()"
        >
          <span aria-hidden="true">&#8249;</span>
        </button>

        <strong>{{ currentMonthLabel }}</strong>

        <button
          type="button"
          class="nav-btn"
          [attr.aria-label]="'shared.datePicker.nextMonth' | translate"
          (click)="nextMonth()"
          [disabled]="!canGoNextMonth()"
        >
          <span aria-hidden="true">&#8250;</span>
        </button>
      </header>

      <div class="calendar-weekdays">
        @for (weekDay of weekDays; track weekDay) {
          <span>{{ weekDay }}</span>
        }
      </div>

      <div class="calendar-grid">
        @for (day of days; track day.value) {
          <button
            type="button"
            class="day-btn"
            [class.outside]="!day.isCurrentMonth"
            [class.today]="day.isToday"
            [class.selected]="day.isSelected"
            [disabled]="day.isDisabled"
            (click)="selectDate(day)"
          >
            {{ day.day }}
          </button>
        }
      </div>

      <footer class="calendar-panel__footer">
        <button type="button" class="footer-btn" (click)="clear()">
          {{ 'shared.datePicker.clear' | translate }}
        </button>
        <button type="button" class="footer-btn primary" (click)="selectToday()">
          {{ 'shared.datePicker.today' | translate }}
        </button>
      </footer>
    </section>
  }
</div>


============================================================
FILE: src\app\shared\presentation\components\app-date-picker\app-date-picker.component.scss
============================================================

.date-picker {
  position: relative;
  display: grid;
  gap: 8px;
}

.date-picker__label {
  color: var(--text-primary);
  font-size: 0.82rem;
  font-weight: 900;
}

.date-picker__trigger {
  width: 100%;
  min-height: 50px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 0 16px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  color: var(--text-primary);
  background: var(--field-bg);
  cursor: pointer;
  font: inherit;
  font-weight: 900;
  text-align: left;
}

.date-picker__trigger:hover {
  border-color: var(--border-strong);
  background: var(--field-bg-hover);
}

.date-picker__trigger:focus-visible {
  outline: 0;
  border-color: var(--accent-color);
  box-shadow: var(--ec-focus-ring);
}

.placeholder {
  color: var(--text-muted);
}

.date-picker__icon {
  color: var(--accent-color);
  font-size: 0.76rem;
  font-weight: 950;
  line-height: 1;
}

.calendar-panel {
  position: fixed;
  inset: auto;
  z-index: 9999;
  width: min(330px, 88vw);
  padding: 16px;
  border: 1px solid var(--border-strong);
  border-radius: 20px;
  margin: 0;
  background: var(--menu-bg);
  box-shadow:
    var(--ec-shadow-panel),
    var(--ec-inset-highlight);
  backdrop-filter: blur(12px);
  overflow: auto;
  overscroll-behavior: contain;
  animation: calendarIn 0.22s cubic-bezier(0.16, 1, 0.3, 1) both;
}

.calendar-panel::backdrop {
  background: transparent;
}

.calendar-panel.closing {
  pointer-events: none;
  animation: calendarOut 0.17s ease both;
}

.calendar-panel__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 14px;
}

.calendar-panel__header strong {
  min-width: 0;
  color: var(--text-primary);
  font-size: 0.95rem;
  font-weight: 950;
  text-align: center;
}

.nav-btn,
.footer-btn,
.day-btn {
  border: 0;
  font: inherit;
  cursor: pointer;
}

.nav-btn {
  width: 36px;
  height: 36px;
  display: grid;
  place-items: center;
  border: 1px solid var(--border-color);
  border-radius: 12px;
  color: var(--text-primary);
  background: var(--ec-ghost-bg);
  font-size: 1rem;
  font-weight: 950;
}

.nav-btn:hover:not(:disabled) {
  border-color: var(--border-strong);
  background: var(--ec-ghost-bg-hover);
}

.nav-btn:disabled {
  opacity: 0.35;
  cursor: not-allowed;
}

.calendar-weekdays,
.calendar-grid {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
}

.calendar-weekdays {
  margin-bottom: 8px;
}

.calendar-weekdays span {
  display: grid;
  place-items: center;
  min-height: 28px;
  color: var(--accent-color);
  font-size: 0.7rem;
  font-weight: 950;
  letter-spacing: 0.08em;
}

.calendar-grid {
  gap: 6px;
}

.day-btn {
  height: 36px;
  display: grid;
  place-items: center;
  border-radius: 10px;
  color: var(--text-primary);
  background: transparent;
  font-size: 0.84rem;
  font-weight: 900;
}

.day-btn:hover:not(:disabled) {
  background: var(--ec-accent-soft);
}

.day-btn.outside {
  color: var(--text-muted);
  opacity: 0.48;
}

.day-btn.today {
  border: 1px solid var(--border-strong);
  color: var(--accent-color);
}

.day-btn.selected {
  color: var(--text-on-action);
  background: var(--accent-color);
  box-shadow: 0 10px 22px rgba(14, 165, 233, 0.24);
}

.day-btn:disabled {
  color: var(--text-muted);
  cursor: not-allowed;
  opacity: 0.35;
}

.calendar-panel__footer {
  display: flex;
  justify-content: space-between;
  gap: 10px;
  margin-top: 14px;
  padding-top: 12px;
  border-top: 1px solid var(--border-color);
}

.footer-btn {
  min-height: 36px;
  padding: 0 13px;
  border-radius: 999px;
  color: var(--text-secondary);
  background: var(--ec-ghost-bg);
  font-size: 0.82rem;
  font-weight: 900;
}

.footer-btn:hover {
  color: var(--text-primary);
  background: var(--ec-ghost-bg-hover);
}

.footer-btn.primary {
  color: var(--text-on-action);
  background: var(--accent-color);
}

@keyframes calendarIn {
  from {
    opacity: 0;
    transform: translateY(-8px) scale(0.97);
    filter: blur(3px);
  }

  to {
    opacity: 1;
    transform: translateY(0) scale(1);
    filter: blur(0);
  }
}

@keyframes calendarOut {
  from {
    opacity: 1;
    transform: translateY(0) scale(1);
    filter: blur(0);
  }

  to {
    opacity: 0;
    transform: translateY(-6px) scale(0.98);
    filter: blur(2px);
  }
}


============================================================
FILE: src\app\shared\presentation\components\app-date-picker\app-date-picker.component.ts
============================================================

import { TranslateModule } from '@ngx-translate/core';
import {
  Component,
  ElementRef,
  EventEmitter,
  HostListener,
  Input,
  OnDestroy,
  Output,
  ViewChild,
} from '@angular/core';

import {
  hideFloatingPopover,
  positionFloatingPanel,
  showFloatingPopover,
} from '../../utils/floating-panel-position';
import { UiPreferencesService } from '../../../application/services/ui-preferences.service';

interface CalendarDay {
  day: number;
  value: string;
  isCurrentMonth: boolean;
  isToday: boolean;
  isSelected: boolean;
  isDisabled: boolean;
}

@Component({
  selector: 'app-date-picker',
  standalone: true,
  imports: [TranslateModule],
  templateUrl: './app-date-picker.component.html',
  styleUrls: ['./app-date-picker.component.scss'],
})
export class AppDatePickerComponent implements OnDestroy {
  @ViewChild('dateTrigger') private readonly dateTrigger?: ElementRef<HTMLElement>;
  @ViewChild('calendarPanel') private readonly calendarPanel?: ElementRef<HTMLElement>;

  @Input() label = '';
  @Input() value = '';
  @Input() min = '';
  @Input() max = '';

  @Output() valueChange = new EventEmitter<string>();

  isOpen = false;
  isClosing = false;

  visibleYear = new Date().getFullYear();
  visibleMonth = new Date().getMonth();

  private closeTimer: ReturnType<typeof setTimeout> | null = null;
  private syncTimer: ReturnType<typeof setTimeout> | null = null;
  private readonly repositionCalendar = () => this.positionCalendar();

  constructor(
    private readonly elementRef: ElementRef<HTMLElement>,
    private readonly uiPreferences: UiPreferencesService
  ) {}

  get displayValue(): string {
    if (!this.value) {
      return '';
    }

    const date = this.fromInputDate(this.value);

    return new Intl.DateTimeFormat(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
    }).format(date);
  }

  get currentMonthLabel(): string {
    const label = new Intl.DateTimeFormat(this.currentLocale(), {
      month: 'long',
      year: 'numeric',
    }).format(new Date(this.visibleYear, this.visibleMonth, 1));

    return this.capitalize(label);
  }

  get weekDays(): string[] {
    const formatter = new Intl.DateTimeFormat(this.currentLocale(), {
      weekday: 'short',
    });
    const sunday = new Date(2026, 5, 7);

    return Array.from({ length: 7 }, (_, index) => {
      const date = new Date(sunday);
      date.setDate(sunday.getDate() + index);

      return formatter
        .format(date)
        .replace('.', '')
        .normalize('NFD')
        .replace(/[\u0300-\u036f]/g, '')
        .slice(0, 2)
        .toUpperCase();
    });
  }

  get shouldRenderCalendar(): boolean {
    return this.isOpen || this.isClosing;
  }

  get days(): CalendarDay[] {
    const firstDayOfMonth = new Date(this.visibleYear, this.visibleMonth, 1);
    const calendarStart = new Date(firstDayOfMonth);

    calendarStart.setDate(firstDayOfMonth.getDate() - firstDayOfMonth.getDay());

    return Array.from({ length: 42 }, (_, index) => {
      const date = new Date(calendarStart);
      date.setDate(calendarStart.getDate() + index);

      const value = this.toInputDate(date);

      return {
        day: date.getDate(),
        value,
        isCurrentMonth: date.getMonth() === this.visibleMonth,
        isToday: value === this.toInputDate(new Date()),
        isSelected: value === this.value,
        isDisabled: this.isDisabled(value),
      };
    });
  }

  toggleCalendar(): void {
    if (this.isOpen) {
      this.closeCalendar();
      return;
    }

    this.openCalendar();
  }

  openCalendar(): void {
    this.clearCloseTimer();
    this.isClosing = false;
    this.isOpen = true;
    this.syncVisibleMonth();
    this.scheduleCalendarSync();
  }

  closeCalendar(): void {
    if (!this.isOpen || this.isClosing) {
      return;
    }

    this.isClosing = true;
    this.clearCloseTimer();
    this.closeTimer = setTimeout(() => {
      hideFloatingPopover(this.calendarPanel?.nativeElement);
      this.unbindFloatingListeners();
      this.isOpen = false;
      this.isClosing = false;
      this.closeTimer = null;
    }, 170);
  }

  previousMonth(): void {
    if (this.visibleMonth === 0) {
      this.visibleMonth = 11;
      this.visibleYear--;
      return;
    }

    this.visibleMonth--;
  }

  nextMonth(): void {
    if (!this.canGoNextMonth()) {
      return;
    }

    if (this.visibleMonth === 11) {
      this.visibleMonth = 0;
      this.visibleYear++;
      return;
    }

    this.visibleMonth++;
  }

  selectDate(day: CalendarDay): void {
    if (day.isDisabled) {
      return;
    }

    this.value = day.value;
    this.valueChange.emit(day.value);
    this.closeCalendar();
  }

  selectToday(): void {
    const today = this.toInputDate(new Date());

    if (this.isDisabled(today)) {
      return;
    }

    this.value = today;
    this.valueChange.emit(today);
    this.syncVisibleMonth();
    this.closeCalendar();
  }

  clear(): void {
    this.value = '';
    this.valueChange.emit('');
    this.closeCalendar();
  }

  canGoNextMonth(): boolean {
    if (!this.max) {
      return true;
    }

    const nextMonth = this.visibleMonth === 11 ? 0 : this.visibleMonth + 1;
    const nextYear = this.visibleMonth === 11 ? this.visibleYear + 1 : this.visibleYear;
    const firstDayOfNextMonth = this.toInputDate(new Date(nextYear, nextMonth, 1));

    return firstDayOfNextMonth <= this.max;
  }

  @HostListener('document:click', ['$event'])
  onDocumentClick(event: MouseEvent): void {
    const target = event.target as Node;

    if (!this.elementRef.nativeElement.contains(target)) {
      this.closeCalendar();
    }
  }

  ngOnDestroy(): void {
    this.clearCloseTimer();
    this.clearSyncTimer();
    hideFloatingPopover(this.calendarPanel?.nativeElement);
    this.unbindFloatingListeners();
  }

  private syncVisibleMonth(): void {
    const baseDate = this.value ? this.fromInputDate(this.value) : new Date();

    this.visibleYear = baseDate.getFullYear();
    this.visibleMonth = baseDate.getMonth();
  }

  private isDisabled(value: string): boolean {
    if (this.min && value < this.min) {
      return true;
    }

    if (this.max && value > this.max) {
      return true;
    }

    return false;
  }

  private toInputDate(date: Date): string {
    const year = date.getFullYear();
    const month = String(date.getMonth() + 1).padStart(2, '0');
    const day = String(date.getDate()).padStart(2, '0');

    return `${year}-${month}-${day}`;
  }

  private fromInputDate(value: string): Date {
    const [year, month, day] = value.split('-').map(Number);

    return new Date(year, month - 1, day);
  }

  private currentLocale(): string {
    const localeByLanguage = {
      es: 'es-PE',
      en: 'en-US',
      pt: 'pt-BR',
    };

    return localeByLanguage[this.uiPreferences.currentLanguage()];
  }

  private capitalize(value: string): string {
    if (!value) {
      return value;
    }

    return value.charAt(0).toUpperCase() + value.slice(1);
  }

  private clearCloseTimer(): void {
    if (this.closeTimer) {
      clearTimeout(this.closeTimer);
      this.closeTimer = null;
    }
  }

  private scheduleCalendarSync(): void {
    this.clearSyncTimer();
    this.syncTimer = setTimeout(() => {
      this.syncTimer = null;
      this.positionCalendar();
    });
  }

  private positionCalendar(): void {
    if (!this.shouldRenderCalendar) {
      return;
    }

    const trigger = this.dateTrigger?.nativeElement;
    const panel = this.calendarPanel?.nativeElement;

    if (!trigger || !panel) {
      return;
    }

    positionFloatingPanel(trigger, panel, {
      maxHeight: 420,
      minHeight: 320,
      minWidth: 292,
      panelHeight: 386,
      preferredWidth: 330,
    });
    showFloatingPopover(panel);
    this.bindFloatingListeners();
  }

  private bindFloatingListeners(): void {
    window.addEventListener('resize', this.repositionCalendar);
    document.addEventListener('scroll', this.repositionCalendar, true);
  }

  private unbindFloatingListeners(): void {
    window.removeEventListener('resize', this.repositionCalendar);
    document.removeEventListener('scroll', this.repositionCalendar, true);
  }

  private clearSyncTimer(): void {
    if (this.syncTimer) {
      clearTimeout(this.syncTimer);
      this.syncTimer = null;
    }
  }
}


============================================================
FILE: src\app\shared\presentation\components\app-dropdown\app-dropdown.component.html
============================================================

<div
  class="dropdown"
  [class.open]="isOpen"
  [class.closing]="isClosing"
  [class.has-selection]="selectedOption"
  [class.nav]="variant === 'nav'"
  [class.info]="tone === 'info'"
  [class.success]="tone === 'success'"
  [class.warning]="tone === 'warning'"
  [class.critical]="tone === 'critical'"
  (mouseenter)="onNavMouseEnter()"
  (mouseleave)="onNavMouseLeave($event)"
>
  <button
    #dropdownTrigger
    type="button"
    class="dropdown__trigger"
    aria-haspopup="listbox"
    [attr.aria-expanded]="shouldRenderMenu"
    (click)="toggleDropdown()"
  >
    <span>
      @if (selectedLabelKey) {
        {{ selectedLabelKey | translate }}
      } @else {
        {{ labelToShow }}
      }
    </span>
    <span class="dropdown__arrow"></span>
  </button>

  @if (shouldRenderMenu) {
    <div
      #dropdownMenu
      class="dropdown__menu"
      role="listbox"
      [attr.popover]="isNavVariant ? null : 'manual'"
    >
      @for (option of options; track option.value) {
        @if (option.route) {
          <a
            class="dropdown__option"
            role="option"
            [class.selected]="option.value === value"
            [class.disabled]="option.disabled"
            [class.dropdown__option--empty-state]="option.emptyState"
            [attr.aria-selected]="option.value === value"
            [routerLink]="option.route"
            (click)="selectOption(option)"
          >
            <span>
              @if (option.labelKey) {
                {{ option.labelKey | translate }}
              } @else {
                {{ option.label }}
              }
            </span>

            @if ((option.description || option.descriptionKey) && variant !== 'nav') {
              <small>
                @if (option.descriptionKey) {
                  {{ option.descriptionKey | translate }}
                } @else {
                  {{ option.description }}
                }
              </small>
            }
          </a>
        } @else {
          <button
            type="button"
            class="dropdown__option"
            role="option"
            [class.selected]="option.value === value"
            [class.disabled]="option.disabled"
            [class.dropdown__option--empty-state]="option.emptyState"
            [attr.aria-selected]="option.value === value"
            (click)="selectOption(option)"
          >
            <span>
              @if (option.labelKey) {
                {{ option.labelKey | translate }}
              } @else {
                {{ option.label }}
              }
            </span>

            @if ((option.description || option.descriptionKey) && variant !== 'nav') {
              <small>
                @if (option.descriptionKey) {
                  {{ option.descriptionKey | translate }}
                } @else {
                  {{ option.description }}
                }
              </small>
            }
          </button>
        }
      }
    </div>
  }
</div>


============================================================
FILE: src\app\shared\presentation\components\app-dropdown\app-dropdown.component.scss
============================================================

:host {
  display: block;
  width: 100%;
  overflow: visible;
}

:host-context(.nav) {
  width: fit-content;
  flex: 0 0 auto;
}

.dropdown {
  position: relative;
  width: 100%;
  overflow: visible;
}

.dropdown__trigger {
  width: 100%;
  min-height: 48px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 0 16px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  color: var(--text-primary);
  background: var(--field-bg);
  cursor: pointer;
  font: inherit;
  font-weight: 850;
  outline: none;
  box-shadow:
    var(--ec-inset-highlight),
    0 12px 24px rgba(3, 7, 18, 0.12);
  transition:
    border-color 0.22s ease,
    box-shadow 0.22s ease,
    background 0.22s ease,
    filter 0.22s ease;
}

.dropdown__trigger:hover,
.dropdown.open .dropdown__trigger {
  border-color: rgba(79, 195, 255, 0.72);
  box-shadow:
    0 0 0 4px rgba(79, 195, 255, 0.1),
    var(--ec-shadow-soft);
}

.dropdown__arrow {
  flex: 0 0 auto;
  width: 8px;
  height: 8px;
  border-right: 2px solid var(--text-secondary);
  border-bottom: 2px solid var(--text-secondary);
  transform: translateY(-2px) rotate(45deg);
  transition: transform 0.22s ease;
}

.dropdown.open .dropdown__arrow {
  transform: translateY(2px) rotate(225deg);
}

.dropdown__menu {
  position: fixed;
  inset: auto;
  z-index: 10000;
  width: 100%;
  min-width: 180px;
  max-width: calc(100vw - 24px);
  padding: 8px;
  border: 1px solid var(--border-strong);
  border-radius: 18px;
  margin: 0;
  background: var(--menu-bg);
  box-shadow:
    var(--ec-shadow-panel),
    0 0 0 1px rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(14px);
  overflow: auto;
  overscroll-behavior: contain;
  animation: dropdownIn 0.2s cubic-bezier(0.16, 1, 0.3, 1) both;
}

.dropdown__menu::backdrop {
  background: transparent;
}

.dropdown.nav .dropdown__menu {
  position: absolute;
  top: calc(100% + 8px);
  left: 50%;
  z-index: 100000;
  width: max-content;
  min-width: 200px;
  max-width: min(280px, calc(100vw - 24px));
  max-height: none;
  translate: -50% 0;
  overflow: visible;
}

.dropdown__option {
  width: 100%;
  min-height: 42px;
  display: grid;
  gap: 3px;
  align-items: center;
  justify-content: flex-start;
  padding: 9px 12px;
  border: 0;
  border-radius: 12px;
  color: var(--text-secondary);
  background: transparent;
  cursor: pointer;
  font: inherit;
  font-weight: 800;
  text-align: left;
  text-decoration: none;
  transition:
    background 0.18s ease,
    color 0.18s ease,
    filter 0.18s ease;
}

.dropdown__option:hover {
  color: var(--text-primary);
  background: var(--ec-accent-soft);
  filter: brightness(1.08);
}

.dropdown__option.selected {
  color: var(--accent-color);
  background: var(--ec-accent-soft);
}

.dropdown__option.disabled {
  opacity: 0.5;
  cursor: not-allowed;
  pointer-events: none;
}

.dropdown__option--empty-state {
  justify-content: center;
  text-align: center;
}

.dropdown__option--empty-state span {
  width: 100%;
}

.dropdown__option small {
  color: var(--text-muted);
  font-size: 0.76rem;
  font-weight: 700;
  line-height: 1.35;
}

.dropdown.info {
  --tone: 56, 189, 248;
  --tone-text: var(--ec-status-info-text);
}

.dropdown.success {
  --tone: 34, 197, 94;
  --tone-text: var(--ec-status-success-text);
}

.dropdown.warning {
  --tone: 250, 204, 21;
  --tone-text: var(--ec-status-warning-text);
}

.dropdown.critical {
  --tone: 248, 113, 113;
  --tone-text: var(--ec-status-danger-text);
}

.dropdown:is(.info, .success, .warning, .critical) .dropdown__trigger {
  border-color: rgba(var(--tone), 0.5);
  background:
    linear-gradient(180deg, rgba(var(--tone), 0.16), transparent),
    var(--field-bg);
}

.dropdown:is(.info, .success, .warning, .critical) .dropdown__trigger:hover,
.dropdown:is(.info, .success, .warning, .critical).open .dropdown__trigger {
  border-color: rgba(var(--tone), 0.82);
  box-shadow:
    0 0 0 4px rgba(var(--tone), 0.1),
    0 16px 34px rgba(3, 7, 18, 0.24);
}

.dropdown:is(.info, .success, .warning, .critical) .dropdown__menu {
  border-color: rgba(var(--tone), 0.36);
}

.dropdown:is(.info, .success, .warning, .critical) .dropdown__option.selected {
  color: var(--tone-text);
  background: rgba(var(--tone), 0.15);
}

@keyframes dropdownIn {
  from {
    opacity: 0;
    transform: translateY(-8px) scale(0.97);
  }

  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.dropdown.closing .dropdown__menu {
  pointer-events: none;
  animation: dropdownOut 0.16s ease both;
}

@keyframes dropdownOut {
  from {
    opacity: 1;
    transform: translateY(0) scale(1);
  }

  to {
    opacity: 0;
    transform: translateY(-6px) scale(0.98);
  }
}


============================================================
FILE: src\app\shared\presentation\components\app-dropdown\app-dropdown.component.ts
============================================================

import {
  Component,
  ElementRef,
  EventEmitter,
  HostListener,
  Input,
  OnDestroy,
  Output,
  ViewChild,
} from '@angular/core';
import { RouterLink } from '@angular/router';
import { TranslateModule } from '@ngx-translate/core';

import {
  hideFloatingPopover,
  positionFloatingPanel,
  showFloatingPopover,
} from '../../utils/floating-panel-position';
import { DropdownOption } from './dropdown-option.model';

export type DropdownTone = 'default' | 'info' | 'success' | 'warning' | 'critical';

@Component({
  selector: 'app-dropdown',
  standalone: true,
  imports: [RouterLink, TranslateModule],
  templateUrl: './app-dropdown.component.html',
  styleUrls: ['./app-dropdown.component.scss'],
})
export class AppDropdownComponent implements OnDestroy {
  @ViewChild('dropdownTrigger') private readonly dropdownTrigger?: ElementRef<HTMLElement>;
  @ViewChild('dropdownMenu') private readonly dropdownMenu?: ElementRef<HTMLElement>;

  @Input() options: DropdownOption[] = [];
  @Input() value: string | null = null;
  @Input() placeholder = 'Seleccionar';
  @Input() triggerLabel = '';
  @Input() variant: 'field' | 'nav' = 'field';
  @Input() tone: DropdownTone = 'default';

  @Output() valueChange = new EventEmitter<string>();
  @Output() selected = new EventEmitter<DropdownOption>();

  isOpen = false;
  isClosing = false;

  private closeTimer: ReturnType<typeof setTimeout> | null = null;
  private syncTimer: ReturnType<typeof setTimeout> | null = null;
  private readonly repositionFloatingMenu = () => this.positionFloatingMenu();

  constructor(private readonly elementRef: ElementRef<HTMLElement>) {}

  get isNavVariant(): boolean {
    return this.variant === 'nav';
  }

  get selectedOption(): DropdownOption | undefined {
    return this.options.find((option) => option.value === this.value);
  }

  get labelToShow(): string {
    if (this.triggerLabel) {
      return this.triggerLabel;
    }

    return this.selectedOption?.label ?? this.placeholder;
  }

  get selectedLabelKey(): string | null {
    if (this.triggerLabel) {
      return null;
    }

    return this.selectedOption?.labelKey ?? null;
  }

  get shouldRenderMenu(): boolean {
    return this.isOpen || this.isClosing;
  }

  toggleDropdown(): void {
    if (this.isNavVariant) {
      return;
    }

    if (this.isOpen) {
      this.closeDropdown();
      return;
    }

    this.openDropdown();
  }

  openDropdown(): void {
    this.clearCloseTimer();
    this.isClosing = false;
    this.isOpen = true;
    this.scheduleFloatingMenuSync();
  }

  closeDropdown(): void {
    if (!this.isOpen || this.isClosing) {
      return;
    }

    this.isClosing = true;
    this.isOpen = false;
    this.clearCloseTimer();
    this.closeTimer = setTimeout(() => {
      hideFloatingPopover(this.dropdownMenu?.nativeElement);
      this.unbindFloatingListeners();
      this.isClosing = false;
      this.closeTimer = null;
    }, 180);
  }

  selectOption(option: DropdownOption): void {
    if (option.disabled) {
      return;
    }

    this.valueChange.emit(option.value);
    this.selected.emit(option);
    this.closeDropdown();
  }

  onNavMouseEnter(): void {
    if (this.isNavVariant) {
      this.openDropdown();
    }
  }

  onNavMouseLeave(event: MouseEvent): void {
    const nextTarget = event.relatedTarget as Node | null;

    if (this.isNavVariant && (!nextTarget || !this.elementRef.nativeElement.contains(nextTarget))) {
      this.closeDropdown();
    }
  }

  @HostListener('document:click', ['$event'])
  closeOnOutsideClick(event: MouseEvent): void {
    if (this.isNavVariant) {
      return;
    }

    const target = event.target as Node;

    if (!this.elementRef.nativeElement.contains(target)) {
      this.closeDropdown();
    }
  }

  @HostListener('document:keydown.escape')
  closeOnEscape(): void {
    this.closeDropdown();
  }

  ngOnDestroy(): void {
    this.clearCloseTimer();
    this.clearSyncTimer();
    hideFloatingPopover(this.dropdownMenu?.nativeElement);
    this.unbindFloatingListeners();
  }

  private clearCloseTimer(): void {
    if (this.closeTimer) {
      clearTimeout(this.closeTimer);
      this.closeTimer = null;
    }
  }

  private scheduleFloatingMenuSync(): void {
    this.clearSyncTimer();
    this.syncTimer = setTimeout(() => {
      this.syncTimer = null;
      this.positionFloatingMenu();
    });
  }

  private positionFloatingMenu(): void {
    if (!this.shouldRenderMenu) {
      return;
    }

    const trigger = this.dropdownTrigger?.nativeElement;
    const panel = this.dropdownMenu?.nativeElement;

    if (!trigger || !panel) {
      return;
    }

    if (this.isNavVariant) {
      return;
    }

    const estimatedHeight = Math.min(360, 16 + this.options.length * (this.isNavVariant ? 46 : 58));

    positionFloatingPanel(trigger, panel, {
      maxHeight: 360,
      minHeight: 120,
      minWidth: 180,
      panelHeight: estimatedHeight,
    });
    showFloatingPopover(panel);
    this.bindFloatingListeners();
  }

  private bindFloatingListeners(): void {
    window.addEventListener('resize', this.repositionFloatingMenu);
    document.addEventListener('scroll', this.repositionFloatingMenu, true);
  }

  private unbindFloatingListeners(): void {
    window.removeEventListener('resize', this.repositionFloatingMenu);
    document.removeEventListener('scroll', this.repositionFloatingMenu, true);
  }

  private clearSyncTimer(): void {
    if (this.syncTimer) {
      clearTimeout(this.syncTimer);
      this.syncTimer = null;
    }
  }
}


============================================================
FILE: src\app\shared\presentation\components\app-dropdown\dropdown-option.model.ts
============================================================

export interface DropdownOption {
  label: string;
  labelKey?: string;
  value: string;
  route?: string;
  aliases?: string[];
  description?: string;
  descriptionKey?: string;
  disabled?: boolean;
  emptyState?: boolean;
}


============================================================
FILE: src\app\shared\presentation\components\app-layout\app-layout.component.html
============================================================

<div class="app-shell">
  <app-header
    [showPrivateNavigation]="!isAuthScreen() && isAuthenticated()"
    [isDarkMode]="uiPreferences.isDarkMode()"
    [userInitial]="currentUserInitial()"
    (themeToggled)="toggleTheme()"
    (userMenuToggled)="toggleUserSidebar()"
  />

  @if (!isAuthScreen() && isAuthenticated()) {
    <aside class="user-sidebar ec-scrollbar" [class.open]="isUserSidebarOpen()">
      <div class="user-sidebar-header">
        <div class="profile-avatar">
          @if (currentUserPhotoUrl()) {
            <img [src]="currentUserPhotoUrl()" alt="User profile photo" />
          } @else {
            <span>{{ currentUserInitial() }}</span>
          }
        </div>

        <div class="profile-info">
          <strong>{{ currentUserName() }}</strong>
          <span>{{ currentUserEmail() }}</span>
          <small>{{ currentUserRole() }}</small>
        </div>
      </div>

      <nav class="private-nav">
        <a routerLink="/settings/profile" routerLinkActive="active" (click)="closeUserSidebar()">
          <span aria-hidden="true">&#9881;&#65039;</span>
          {{ 'nav.userSettings' | translate }}
        </a>

        <a routerLink="/about" routerLinkActive="active" (click)="closeUserSidebar()">
          <span aria-hidden="true">&#8505;&#65039;</span>
          {{ 'nav.about' | translate }}
        </a>

        <a routerLink="/plans" routerLinkActive="active" (click)="closeUserSidebar()">
          <span aria-hidden="true">&#128179;</span>
          {{ 'nav.plans' | translate }}
        </a>
      </nav>

      <div class="user-sidebar-footer">
        <button type="button" class="logout-button" (click)="signOut()">
          {{ 'auth.logout' | translate }}
        </button>
      </div>
    </aside>

    @if (isUserSidebarOpen()) {
      <button
        type="button"
        class="sidebar-backdrop"
        aria-label="Close user panel"
        (click)="closeUserSidebar()"
      ></button>
    }
  }

  <main class="content ec-scrollbar">
    @if (requiresLocationSetup()) {
      <section class="location-required-panel">
        <p class="location-required-panel__eyebrow">
          {{ 'onboarding.locationRequiredEyebrow' | translate }}
        </p>
        <h1>{{ 'onboarding.locationRequiredTitle' | translate }}</h1>
        <p>{{ 'onboarding.locationRequiredDescription' | translate }}</p>
        <a routerLink="/spaces/sites">
          {{ 'onboarding.createLocationAction' | translate }}
        </a>
      </section>
    } @else {
      <router-outlet />
    }
  </main>

  <app-footer />

  @if (confirmDialog.dialog(); as dialog) {
    <app-confirm-dialog
      [open]="true"
      [title]="dialog.title"
      [message]="dialog.message"
      [detail]="dialog.detail"
      [confirmLabel]="dialog.confirmLabel"
      [cancelLabel]="dialog.cancelLabel"
      [tone]="dialog.tone"
      [requiredText]="dialog.requiredText"
      [requiredTextLabel]="dialog.requiredTextLabel"
      [requiredTextHint]="dialog.requiredTextHint"
      (confirmed)="confirmDialog.confirmCurrent()"
      (cancelled)="confirmDialog.cancelCurrent()"
    />
  }

  <app-toast-container />
</div>


============================================================
FILE: src\app\shared\presentation\components\app-layout\app-layout.component.scss
============================================================

.app-shell {
  --bar-h: 70px;

  min-height: 100vh;
  display: flex;
  flex-direction: column;
  background: var(--ec-shell-bg);
  color: var(--text-primary);
}

.user-sidebar {
  position: fixed;
  top: var(--bar-h);
  right: 0;
  z-index: 90;
  width: min(360px, calc(100vw - 24px));
  height: calc(100dvh - var(--bar-h));
  display: flex;
  flex-direction: column;
  gap: 20px;
  padding: 24px;
  border-left: 1px solid var(--border-color);
  background: var(--ec-sidebar-bg);
  backdrop-filter: blur(18px);
  box-shadow: var(--ec-sidebar-shadow);
  transform: translateX(100%);
  transition: transform 0.28s ease;
  overflow-y: auto;
}

.user-sidebar.open {
  transform: translateX(0);
}

.sidebar-backdrop {
  position: fixed;
  inset: var(--bar-h) 0 0;
  z-index: 80;
  border: 0;
  background: var(--ec-overlay-bg);
  cursor: pointer;
}

.user-sidebar-header {
  display: grid;
  justify-items: center;
  gap: 12px;
  padding: 28px 16px 20px;
  border: 1px solid var(--border-color);
  border-radius: 24px;
  background:
    radial-gradient(circle at top, var(--ec-accent-soft), transparent 55%),
    var(--surface-color);
}

.profile-avatar {
  display: grid;
  place-items: center;
  width: 92px;
  height: 92px;
  border-radius: 50%;
  background: var(--accent-color);
  color: var(--text-on-action);
  overflow: hidden;
  font-size: 2rem;
  font-weight: 900;
  box-shadow: 0 18px 34px rgba(99, 102, 241, 0.28);
}

.profile-avatar img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.profile-info {
  display: grid;
  justify-items: center;
  gap: 4px;
  text-align: center;
}

.profile-info strong {
  color: var(--text-primary);
  font-size: 1.05rem;
}

.profile-info span,
.profile-info small {
  color: var(--text-secondary);
}

.profile-info span {
  max-width: 260px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.private-nav {
  display: grid;
  gap: 10px;
}

.private-nav a {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 14px 16px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  color: var(--text-secondary);
  background: var(--ec-surface-subtle);
  text-decoration: none;
  font-weight: 800;
  transition:
    background 0.2s ease,
    color 0.2s ease,
    border-color 0.2s ease;
}

.private-nav a:hover,
.private-nav a.active {
  background: var(--surface-color);
  color: var(--text-primary);
  border-color: var(--accent-color);
}

.private-nav a span {
  display: grid;
  place-items: center;
  width: 24px;
  height: 24px;
}

.user-sidebar-footer {
  margin-top: auto;
  display: grid;
  gap: 10px;
}

.logout-button {
  width: 100%;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  padding: 14px 16px;
  background: transparent;
  color: var(--text-primary);
  font-weight: 900;
  cursor: pointer;
  transition:
    background 0.2s ease,
    border-color 0.2s ease;
}

.logout-button:hover {
  background: var(--ec-ghost-bg-hover);
  border-color: var(--accent-color);
}

.content {
  flex: 1;
  width: min(1180px, calc(100% - 32px));
  margin: 0 auto;
  padding: 34px 0;
}

.location-required-panel {
  width: min(880px, 100%);
  margin: 42px auto;
  display: grid;
  gap: 16px;
  padding: 34px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background:
    var(--ec-card-highlight),
    var(--surface-card);
  box-shadow: var(--ec-shadow-soft);
}

.location-required-panel__eyebrow {
  margin: 0;
  color: var(--accent-color);
  font-size: 0.78rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.location-required-panel h1,
.location-required-panel p {
  margin: 0;
}

.location-required-panel h1 {
  max-width: 680px;
  font-size: clamp(1.8rem, 4vw, 2.8rem);
  line-height: 1.08;
}

.location-required-panel p {
  max-width: 720px;
  color: var(--text-secondary);
  line-height: 1.65;
}

.location-required-panel a {
  width: fit-content;
  min-height: 42px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0 16px;
  border-radius: 8px;
  color: var(--text-on-action);
  background: var(--accent-color);
  font-weight: 950;
  text-decoration: none;
}

@media (max-width: 760px) {
  .app-shell {
    --bar-h: 64px;
  }

  .content {
    width: min(100% - 24px, 1180px);
    padding: 24px 0;
  }

  .user-sidebar {
    width: min(330px, calc(100vw - 18px));
    padding: 20px;
  }
}


============================================================
FILE: src\app\shared\presentation\components\app-layout\app-layout.component.ts
============================================================

import { Component, computed, inject, signal } from '@angular/core';
import { NavigationEnd, Router, RouterLink, RouterLinkActive, RouterOutlet } from '@angular/router';
import { filter } from 'rxjs';

import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { AuthSessionService } from '../../../application/services/auth-session.service';
import { UiPreferencesService } from '../../../application/services/ui-preferences.service';
import { IamFacade } from '../../../../iam/application/services/iam.facade';
import { FooterComponent } from '../footer/footer.component';
import { HeaderComponent } from '../header/header.component';
import { ToastContainerComponent } from '../toast-container/toast-container.component';
import { WorkplaceFacade } from '../../../../workplace/application/services/workplace.facade';
import { ConfirmDialogService } from '../../../application/services/confirm-dialog.service';
import { ConfirmDialogComponent } from '../confirm-dialog/confirm-dialog.component';

@Component({
  selector: 'app-layout',
  standalone: true,
  imports: [
    RouterOutlet,
    RouterLink,
    RouterLinkActive,
    TranslateModule,
    FooterComponent,
    HeaderComponent,
    ToastContainerComponent,
    ConfirmDialogComponent,
  ],
  templateUrl: './app-layout.component.html',
  styleUrls: ['./app-layout.component.scss'],
})
export class AppLayoutComponent {
  readonly router = inject(Router);
  readonly translate = inject(TranslateService);
  readonly uiPreferences = inject(UiPreferencesService);
  readonly authSession = inject(AuthSessionService);
  readonly iamFacade = inject(IamFacade);
  readonly workplaceFacade = inject(WorkplaceFacade);
  readonly confirmDialog = inject(ConfirmDialogService);

  readonly currentUrl = signal(this.router.url);
  readonly isDrawerOpen = signal(false);

  readonly isAuthScreen = computed(() => {
    const url = this.currentUrl();
    return (
      url.startsWith('/iam/login') ||
      url.startsWith('/iam/register') ||
      url.startsWith('/iam/recover-password')
    );
  });

  readonly currentUser = computed(() => this.authSession.currentUser?.() ?? null);
  readonly isAuthenticated = computed(() => this.authSession.isAuthenticated());
  readonly isUserSidebarOpen = signal(false);
  readonly requiresLocationSetup = computed(() => {
    if (!this.isAuthenticated() || this.isAuthScreen()) {
      return false;
    }

    if (this.workplaceFacade.loading() || this.workplaceFacade.locations().length > 0) {
      return false;
    }

    const url = this.currentUrl().split('?')[0];
    const allowedPrefixes = [
      '/home',
      '/spaces/sites',
      '/workplace/locations',
      '/workplace',
      '/plans',
      '/alerts/preferences',
      '/notifications/preferences',
      '/settings/billing',
      '/settings/account',
      '/settings/notifications',
      '/settings/platform',
      '/billing/history',
      '/settings/profile',
      '/settings/security',
      '/iam/profile',
      '/about',
    ];

    return !allowedPrefixes.some((prefix) => url === prefix || url.startsWith(`${prefix}/`));
  });

  private workplaceLoadRequested = false;

  constructor() {
    this.router.events
      .pipe(filter((event) => event instanceof NavigationEnd))
      .subscribe((event) => {
        const navigation = event as NavigationEnd;
        this.currentUrl.set(navigation.urlAfterRedirects);
        this.ensureWorkplaceLoaded();
      });

    this.ensureWorkplaceLoaded();
  }

  openDrawer(): void {
    this.isDrawerOpen.set(true);
  }

  closeDrawer(): void {
    this.isDrawerOpen.set(false);
  }

  toggleTheme(): void {
    this.uiPreferences.toggleTheme();
  }

  signOut(): void {
    this.iamFacade.signOut();
  }

  currentUserName(): string {
    return this.currentUser()?.fullName || 'Usuario';
  }

  currentUserEmail(): string {
    return this.currentUser()?.email || 'Sin correo registrado';
  }

  currentUserRole(): string {
    return this.currentUser()?.accessProfileName || 'Member';
  }

  currentUserInitial(): string {
    return this.currentUserName().trim().charAt(0).toUpperCase() || 'U';
  }

  currentUserPhotoUrl(): string | null {
    return null;
  }

  toggleUserSidebar(): void {
    this.isUserSidebarOpen.update((value) => !value);
  }

  closeUserSidebar(): void {
    this.isUserSidebarOpen.set(false);
  }

  private ensureWorkplaceLoaded(): void {
    if (!this.isAuthenticated()) {
      this.workplaceLoadRequested = false;
      return;
    }

    if (this.isAuthScreen() || this.workplaceLoadRequested) {
      return;
    }

    this.workplaceLoadRequested = true;
    void this.workplaceFacade.loadWorkplace();
  }
}


============================================================
FILE: src\app\shared\presentation\components\app-number-stepper\app-number-stepper.component.html
============================================================

<label class="number-stepper">
  @if (label) {
    <span class="number-stepper__label">{{ label }}</span>
  }

  <span class="number-stepper__body">
    <input
      type="text"
      [inputMode]="decimals > 0 ? 'decimal' : 'numeric'"
      [value]="inputValue"
      [placeholder]="placeholder"
      (input)="onInput($event)"
      (blur)="onBlur($event)"
      (paste)="onPaste($event)"
    />

    <span class="number-stepper__buttons">
      <button type="button" [attr.aria-label]="'shared.numberStepper.increase' | translate" (click)="increase()">+</button>
      <button type="button" [attr.aria-label]="'shared.numberStepper.decrease' | translate" (click)="decrease()">-</button>
    </span>

    @if (suffix) {
      <small>{{ suffix }}</small>
    }
  </span>
</label>


============================================================
FILE: src\app\shared\presentation\components\app-number-stepper\app-number-stepper.component.scss
============================================================

:host {
  display: block;
  min-width: 0;
}

.number-stepper {
  display: grid;
  gap: 8px;
  min-width: 0;
}

.number-stepper__label {
  color: var(--text-primary);
  font-size: 0.82rem;
  font-weight: 900;
}

.number-stepper__body {
  min-height: 46px;
  display: grid;
  grid-template-columns: minmax(0, 1fr) 34px auto;
  align-items: center;
  min-width: 0;
  overflow: hidden;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background: var(--field-bg);
  box-shadow: var(--ec-inset-highlight);
}

.number-stepper__body:focus-within {
  border-color: var(--border-strong);
  box-shadow: var(--ec-focus-ring);
}

.number-stepper input {
  width: 100%;
  min-width: 0;
  height: 44px;
  border: 0;
  padding: 0 12px;
  color: var(--text-primary);
  background: transparent;
  font: inherit;
  font-weight: 900;
  outline: none;
  text-align: center;
  font-variant-numeric: tabular-nums;
}

.number-stepper input::placeholder {
  color: var(--text-muted);
}

.number-stepper input::-webkit-outer-spin-button,
.number-stepper input::-webkit-inner-spin-button {
  appearance: none;
  margin: 0;
}

.number-stepper__buttons {
  height: 44px;
  display: grid;
  grid-template-rows: repeat(2, minmax(0, 1fr));
  align-self: stretch;
  border-left: 1px solid var(--border-color);
  border-right: 1px solid var(--border-color);
}

.number-stepper__buttons button {
  display: grid;
  place-items: center;
  width: 34px;
  min-height: 22px;
  height: auto;
  border: 0;
  padding: 0;
  color: var(--accent-color);
  background: var(--ec-ghost-bg);
  cursor: pointer;
  font-size: 0.62rem;
  line-height: 1;
}

.number-stepper__buttons button:hover {
  color: var(--text-primary);
  background: var(--ec-ghost-bg-hover);
}

.number-stepper small {
  min-width: 0;
  padding: 0 10px;
  color: var(--text-primary);
  font-size: 0.72rem;
  font-weight: 900;
  white-space: nowrap;
}


============================================================
FILE: src\app\shared\presentation\components\app-number-stepper\app-number-stepper.component.ts
============================================================

import { TranslateModule } from '@ngx-translate/core';
import { Component, EventEmitter, Input, Output } from '@angular/core';

@Component({
  selector: 'app-number-stepper',
  standalone: true,
  imports: [TranslateModule],
  templateUrl: './app-number-stepper.component.html',
  styleUrls: ['./app-number-stepper.component.scss'],
})
export class AppNumberStepperComponent {
  @Input() label = '';
  @Input() value: number | null = null;
  @Input() min: number | null = null;
  @Input() max: number | null = null;
  @Input() step = 1;
  @Input() decimals = 0;
  @Input() suffix = '';
  @Input() placeholder = '';
  @Input() allowEmpty = true;

  @Output() valueChange = new EventEmitter<number | null>();

  get inputValue(): string {
    if (this.value === null) {
      return this.allowEmpty ? '' : String(this.fallbackValue);
    }

    return String(this.clampValue(this.value));
  }

  onInput(event: Event): void {
    const input = event.target as HTMLInputElement;
    const sanitizedValue = this.sanitizeInput(input.value);

    if (sanitizedValue === '') {
      if (this.allowEmpty) {
        this.valueChange.emit(null);
        input.value = '';
        return;
      }

      input.value = String(this.fallbackValue);
      this.valueChange.emit(this.fallbackValue);
      return;
    }

    const nextValue = this.clampValue(Number(sanitizedValue));

    input.value = String(nextValue);
    this.valueChange.emit(nextValue);
  }

  onBlur(event: Event): void {
    const input = event.target as HTMLInputElement;

    if (input.value.trim() === '') {
      const nextValue = this.allowEmpty ? null : this.fallbackValue;

      input.value = nextValue === null ? '' : String(nextValue);
      this.valueChange.emit(nextValue);
      return;
    }

    const nextValue = this.clampValue(Number(input.value));

    input.value = String(nextValue);
    this.valueChange.emit(nextValue);
  }

  onPaste(event: ClipboardEvent): void {
    event.preventDefault();

    const input = event.target as HTMLInputElement;
    const pastedValue = event.clipboardData?.getData('text') ?? '';
    const sanitizedValue = this.sanitizeInput(pastedValue);

    if (sanitizedValue === '') {
      if (!this.allowEmpty) {
        input.value = String(this.fallbackValue);
        this.valueChange.emit(this.fallbackValue);
      }

      return;
    }

    const nextValue = this.clampValue(Number(sanitizedValue));

    input.value = String(nextValue);
    this.valueChange.emit(nextValue);
  }

  increase(): void {
    const currentValue = this.value ?? this.fallbackValue;
    const nextValue = this.clampValue(currentValue + this.step);

    this.valueChange.emit(nextValue);
  }

  decrease(): void {
    const currentValue = this.value ?? this.fallbackValue;
    const nextValue = this.clampValue(currentValue - this.step);

    this.valueChange.emit(nextValue);
  }

  private get fallbackValue(): number {
    return this.min ?? 0;
  }

  private clampValue(value: number): number {
    let nextValue = Number.isFinite(value) ? value : this.fallbackValue;

    if (this.decimals <= 0) {
      nextValue = Math.trunc(nextValue);
    } else {
      nextValue = Number(nextValue.toFixed(this.decimals));
    }

    if (this.min !== null) {
      nextValue = Math.max(this.min, nextValue);
    }

    if (this.max !== null) {
      nextValue = Math.min(this.max, nextValue);
    }

    return nextValue;
  }

  private sanitizeInput(value: string): string {
    if (this.decimals <= 0) {
      return value.replace(/\D/g, '');
    }

    const normalizedValue = value.replace(',', '.');
    const [integerPart, ...decimalParts] = normalizedValue.split('.');
    const integerDigits = integerPart.replace(/\D/g, '');
    const decimalDigits = decimalParts.join('').replace(/\D/g, '').slice(0, this.decimals);

    return decimalDigits.length ? `${integerDigits}.${decimalDigits}` : integerDigits;
  }
}


============================================================
FILE: src\app\shared\presentation\components\app-time-picker\app-time-picker.component.html
============================================================

<div class="time-picker">
  @if (label) {
    <span class="time-picker__label">{{ label }}</span>
  }

  <button #timeTrigger type="button" class="time-picker__trigger" (click)="togglePanel()">
    <span>{{ displayValue }}</span>
    <span class="time-picker__icon" aria-hidden="true"></span>
  </button>

  @if (isOpen) {
    <section #timePanel class="time-panel" [attr.aria-label]="'shared.timePicker.panelAria' | translate" popover="manual">
      <div class="time-panel__column" [attr.aria-label]="'shared.timePicker.hoursAria' | translate">
        @for (hour of hours; track hour) {
          <button
            type="button"
            [class.selected]="hour === selectedHour"
            (click)="selectHour(hour)"
          >
            {{ hour }}
          </button>
        }
      </div>

      <div class="time-panel__column" [attr.aria-label]="'shared.timePicker.minutesAria' | translate">
        @for (minute of minutes; track minute) {
          <button
            type="button"
            [class.selected]="minute === selectedMinute"
            (click)="selectMinute(minute)"
          >
            {{ minute }}
          </button>
        }
      </div>
    </section>
  }
</div>


============================================================
FILE: src\app\shared\presentation\components\app-time-picker\app-time-picker.component.scss
============================================================

:host {
  display: block;
  width: 100%;
}

.time-picker {
  position: relative;
  display: grid;
  gap: 8px;
}

.time-picker__label {
  color: var(--text-primary);
  font-size: 0.92rem;
  font-weight: 950;
}

.time-picker__trigger {
  width: 100%;
  min-height: var(--time-picker-trigger-height, 62px);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 0 18px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  color: var(--text-primary);
  background: var(--field-bg);
  cursor: pointer;
  font: inherit;
  font-weight: 900;
  text-align: left;
  outline: none;
}

.time-picker__trigger:hover,
.time-picker__trigger:focus-visible {
  border-color: var(--border-strong);
  box-shadow: var(--ec-focus-ring);
}

.time-picker__icon {
  position: relative;
  flex: 0 0 auto;
  width: 18px;
  height: 18px;
  border: 2px solid var(--accent-color);
  border-radius: 50%;
}

.time-picker__icon::before,
.time-picker__icon::after {
  content: '';
  position: absolute;
  left: 50%;
  top: 50%;
  width: 2px;
  border-radius: 999px;
  background: var(--accent-color);
  transform-origin: bottom center;
}

.time-picker__icon::before {
  height: 5px;
  transform: translate(-50%, -100%);
}

.time-picker__icon::after {
  height: 6px;
  transform: translate(-50%, -100%) rotate(90deg);
}

.time-panel {
  position: fixed;
  inset: auto;
  z-index: 1000;
  width: 172px;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px;
  padding: 8px;
  border: 1px solid var(--border-strong);
  border-radius: 18px;
  margin: 0;
  background: var(--menu-bg);
  box-shadow:
    var(--ec-shadow-panel),
    var(--ec-inset-highlight);
  overflow: hidden;
  overscroll-behavior: contain;
}

.time-panel::backdrop {
  background: transparent;
}

.time-panel__column {
  max-height: 220px;
  display: grid;
  gap: 4px;
  overflow-y: auto;
  padding-right: 2px;
}

.time-panel button {
  min-height: 34px;
  border: 0;
  border-radius: 10px;
  color: var(--text-secondary);
  background: transparent;
  cursor: pointer;
  font: inherit;
  font-size: 0.86rem;
  font-weight: 900;
}

.time-panel button:hover {
  color: var(--text-primary);
  background: var(--ec-accent-soft);
}

.time-panel button.selected {
  color: var(--accent-color);
  background: var(--ec-accent-soft);
}


============================================================
FILE: src\app\shared\presentation\components\app-time-picker\app-time-picker.component.ts
============================================================

import { TranslateModule } from '@ngx-translate/core';
import {
  Component,
  ElementRef,
  EventEmitter,
  HostListener,
  Input,
  OnDestroy,
  Output,
  ViewChild,
} from '@angular/core';

import {
  hideFloatingPopover,
  positionFloatingPanel,
  showFloatingPopover,
} from '../../utils/floating-panel-position';

@Component({
  selector: 'app-time-picker',
  standalone: true,
  imports: [TranslateModule],
  templateUrl: './app-time-picker.component.html',
  styleUrls: ['./app-time-picker.component.scss'],
})
export class AppTimePickerComponent implements OnDestroy {
  @ViewChild('timeTrigger') private readonly timeTrigger?: ElementRef<HTMLElement>;
  @ViewChild('timePanel') private readonly timePanel?: ElementRef<HTMLElement>;

  @Input() label = '';
  @Input() value = '00:00';

  @Output() valueChange = new EventEmitter<string>();

  isOpen = false;

  private syncTimer: ReturnType<typeof setTimeout> | null = null;
  private readonly repositionTimePanel = () => this.positionTimePanel();

  readonly hours = Array.from({ length: 24 }, (_, index) =>
    String(index).padStart(2, '0')
  );

  readonly minutes = Array.from({ length: 12 }, (_, index) =>
    String(index * 5).padStart(2, '0')
  );

  constructor(private readonly elementRef: ElementRef<HTMLElement>) {}

  get displayValue(): string {
    return this.normalizedValue;
  }

  get selectedHour(): string {
    return this.normalizedValue.slice(0, 2);
  }

  get selectedMinute(): string {
    return this.normalizedValue.slice(3, 5);
  }

  togglePanel(): void {
    if (this.isOpen) {
      this.closePanel();
      return;
    }

    this.openPanel();
  }

  openPanel(): void {
    this.isOpen = true;
    this.scheduleTimePanelSync();
  }

  closePanel(): void {
    this.clearSyncTimer();
    hideFloatingPopover(this.timePanel?.nativeElement);
    this.unbindFloatingListeners();
    this.isOpen = false;
  }

  selectHour(hour: string): void {
    this.emitValue(hour, this.selectedMinute);
  }

  selectMinute(minute: string): void {
    this.emitValue(this.selectedHour, minute);
    this.closePanel();
  }

  @HostListener('document:click', ['$event'])
  onDocumentClick(event: MouseEvent): void {
    const target = event.target as Node;

    if (!this.elementRef.nativeElement.contains(target)) {
      this.closePanel();
    }
  }

  @HostListener('document:keydown.escape')
  onEscape(): void {
    this.closePanel();
  }

  ngOnDestroy(): void {
    this.clearSyncTimer();
    hideFloatingPopover(this.timePanel?.nativeElement);
    this.unbindFloatingListeners();
  }

  private get normalizedValue(): string {
    if (/^\d{2}:\d{2}$/.test(this.value)) {
      return this.value;
    }

    return '00:00';
  }

  private emitValue(hour: string, minute: string): void {
    const nextValue = `${hour}:${minute}`;

    this.value = nextValue;
    this.valueChange.emit(nextValue);
  }

  private scheduleTimePanelSync(): void {
    this.clearSyncTimer();
    this.syncTimer = setTimeout(() => {
      this.syncTimer = null;
      this.positionTimePanel();
    });
  }

  private positionTimePanel(): void {
    if (!this.isOpen) {
      return;
    }

    const trigger = this.timeTrigger?.nativeElement;
    const panel = this.timePanel?.nativeElement;

    if (!trigger || !panel) {
      return;
    }

    positionFloatingPanel(trigger, panel, {
      maxHeight: 250,
      minHeight: 180,
      minWidth: 172,
      panelHeight: 236,
      preferredWidth: 172,
    });
    showFloatingPopover(panel);
    this.bindFloatingListeners();
  }

  private bindFloatingListeners(): void {
    window.addEventListener('resize', this.repositionTimePanel);
    document.addEventListener('scroll', this.repositionTimePanel, true);
  }

  private unbindFloatingListeners(): void {
    window.removeEventListener('resize', this.repositionTimePanel);
    document.removeEventListener('scroll', this.repositionTimePanel, true);
  }

  private clearSyncTimer(): void {
    if (this.syncTimer) {
      clearTimeout(this.syncTimer);
      this.syncTimer = null;
    }
  }
}


============================================================
FILE: src\app\shared\presentation\components\app-wheel-selector\app-wheel-selector.component.html
============================================================

<section class="wheel-selector">
  @if (eyebrow || title || summary) {
    <div class="wheel-selector__heading">
      <div>
        @if (eyebrow) {
          <span>{{ eyebrow }}</span>
        }

        @if (title) {
          <strong>{{ title }}</strong>
        }
      </div>

      @if (summary) {
        <p>{{ summary }}</p>
      }
    </div>
  }

  <div class="wheel-selector__wrap">
    <div
      class="wheel-selector__wheel"
      [class.wheel-selector__wheel--dragging]="isWheelDragging"
      (pointerdown)="onWheelPointerDown($event)"
      (pointermove)="onWheelPointerMove($event)"
      (pointerup)="onWheelPointerUp($event)"
      (pointercancel)="onWheelPointerCancel($event)"
      (pointerleave)="onWheelPointerCancel($event)"
    >
      <div class="wheel-selector__glow"></div>
      <div class="wheel-selector__line"></div>

      @for (option of visibleOptionsList; track option.index) {
        <button
          type="button"
          class="wheel-selector-option"
          [class.wheel-selector-option--active]="isOptionActive(option.index)"
          [style.transform]="optionTransform(option.index)"
          [style.opacity]="optionOpacity(option.index)"
          [style.z-index]="optionZIndex(option.index)"
          (click)="onOptionClick(option.index, $event)"
        >
          <span
            class="wheel-selector-option__shade"
            [style.opacity]="optionShade(option.index)"
          ></span>

          <strong>{{ option.label }}</strong>

          @if (option.sublabel) {
            <small>{{ option.sublabel }}</small>
          }
        </button>
      }
    </div>
  </div>

  @if (hint) {
    <p class="wheel-selector__hint">{{ hint }}</p>
  }
</section>


============================================================
FILE: src\app\shared\presentation\components\app-wheel-selector\app-wheel-selector.component.scss
============================================================

.wheel-selector {
  display: grid;
  gap: 14px;
  padding: 18px;
  border: 1px solid var(--border-color);
  border-radius: 22px;
  background:
    radial-gradient(circle at center, var(--ec-accent-soft), transparent 34%),
    var(--ec-surface-muted);
  overflow: hidden;
}

.wheel-selector__heading {
  display: flex;
  justify-content: space-between;
  gap: 18px;
  align-items: flex-end;
}

.wheel-selector__heading div {
  display: grid;
  gap: 4px;
}

.wheel-selector__heading span {
  color: var(--accent-color);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.wheel-selector__heading strong {
  color: var(--text-primary);
  font-size: 1.05rem;
  font-weight: 950;
}

.wheel-selector__heading p {
  margin: 0;
  color: var(--text-secondary);
  font-size: 0.86rem;
  font-weight: 850;
  white-space: nowrap;
}

.wheel-selector__wrap {
  position: relative;
  display: grid;
  grid-template-columns: minmax(0, 1fr);
  align-items: center;
  min-height: 176px;
}

.wheel-selector__wheel {
  position: relative;
  height: 160px;
  min-width: 0;
  overflow: hidden;
  border: 1px solid color-mix(in srgb, var(--border-strong) 42%, transparent);
  border-radius: 26px;
  background:
    radial-gradient(circle at center, var(--ec-accent-soft), transparent 38%),
    var(--field-bg);
  box-shadow:
    var(--ec-inset-highlight),
    inset 0 -24px 52px color-mix(in srgb, var(--bg-primary) 24%, transparent);
  cursor: grab;
  perspective: 900px;
  touch-action: none;
  user-select: none;
}

.wheel-selector__wheel--dragging {
  cursor: grabbing;
}

.wheel-selector__glow {
  position: absolute;
  left: 50%;
  top: 50%;
  width: 230px;
  height: 116px;
  border-radius: 999px;
  transform: translate(-50%, -50%);
  background: var(--ec-accent-soft);
  filter: blur(24px);
  pointer-events: none;
}

.wheel-selector__line {
  position: absolute;
  left: 50%;
  top: 16px;
  bottom: 16px;
  width: 188px;
  border: 1px solid var(--border-strong);
  border-radius: 22px;
  transform: translateX(-50%);
  background: var(--ec-accent-soft);
  box-shadow:
    0 0 28px var(--ec-accent-soft),
    var(--ec-inset-highlight);
  pointer-events: none;
}

.wheel-selector-option {
  position: absolute;
  left: calc(50% - 72px);
  top: 34px;
  width: 144px;
  height: 90px;
  display: grid;
  place-items: center;
  gap: 5px;
  overflow: hidden;
  border: 1px solid var(--border-color);
  border-radius: 20px;
  padding: 12px;
  color: var(--text-primary);
  background:
    radial-gradient(circle at top, var(--ec-accent-soft), transparent 44%),
    var(--surface-card);
  box-shadow:
    var(--ec-shadow-soft),
    var(--ec-inset-highlight);
  transform-origin: center;
  transition:
    border-color 160ms ease,
    background 160ms ease,
    box-shadow 160ms ease;
  will-change: transform, opacity;
}

.wheel-selector-option__shade {
  position: absolute;
  inset: 0;
  background: var(--bg-primary);
  pointer-events: none;
}

.wheel-selector-option strong,
.wheel-selector-option small {
  position: relative;
  z-index: 1;
  text-align: center;
  pointer-events: none;
}

.wheel-selector-option strong {
  color: var(--text-primary);
  font-size: 0.92rem;
  font-weight: 950;
  line-height: 1.05;
}

.wheel-selector-option small {
  color: var(--accent-color);
  font-size: 0.74rem;
  font-weight: 850;
  line-height: 1.15;
}

.wheel-selector-option--active {
  border-color: var(--border-strong);
  background:
    radial-gradient(circle at top, var(--ec-accent-soft), transparent 48%),
    var(--ec-action-bg);
  box-shadow:
    0 0 0 1px var(--ec-accent-soft),
    var(--ec-action-shadow),
    var(--ec-inset-highlight);
}

.wheel-selector-option--active strong {
  color: var(--text-on-action);
  font-size: 0.98rem;
}

.wheel-selector-option--active small {
  color: var(--text-on-action);
}

.wheel-selector__hint {
  margin: -2px 0 0;
  color: var(--text-secondary);
  font-size: 0.82rem;
  font-weight: 750;
  text-align: center;
}

@media (max-width: 940px) {
  .wheel-selector__heading {
    align-items: flex-start;
    flex-direction: column;
  }

  .wheel-selector__heading p {
    white-space: normal;
  }

  .wheel-selector__wrap {
    grid-template-columns: 1fr;
  }

  .wheel-selector__wheel {
    height: 154px;
  }

  .wheel-selector-option {
    left: calc(50% - 70px);
    width: 140px;
  }
}


============================================================
FILE: src\app\shared\presentation\components\app-wheel-selector\app-wheel-selector.component.ts
============================================================

import {
  ChangeDetectionStrategy,
  ChangeDetectorRef,
  Component,
  EventEmitter,
  Input,
  OnChanges,
  OnDestroy,
  Output,
  SimpleChanges,
} from '@angular/core';

import { VisibleWheelOption, WheelOption, WheelOptionValue } from './wheel-option.model';

@Component({
  selector: 'app-wheel-selector',
  standalone: true,
  templateUrl: './app-wheel-selector.component.html',
  styleUrls: ['./app-wheel-selector.component.scss'],
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class AppWheelSelectorComponent implements OnChanges, OnDestroy {
  @Input() options: WheelOption[] = [];
  @Input() value: WheelOptionValue | null = null;
  @Input() eyebrow = '';
  @Input() title = 'Selecciona una opcion';
  @Input() summary = '';
  @Input() hint = '';

  @Output() valueChange = new EventEmitter<WheelOptionValue>();
  @Output() optionChange = new EventEmitter<WheelOption>();

  selectedIndex = 0;
  isWheelDragging = false;
  visibleOptionsList: VisibleWheelOption[] = [];
  selectedOption: WheelOption | null = null;

  private readonly wheelStepPx = 200;
  private wheelOffsetPx = 0;
  private wheelVelocity = 0;
  private wheelAnimationFrame: number | null = null;

  private activePointerId: number | null = null;
  private lastPointerX = 0;
  private lastPointerTime = 0;
  private totalDragDistance = 0;
  private suppressNextClick = false;

  constructor(private readonly changeDetectorRef: ChangeDetectorRef) {}

  ngOnChanges(changes: SimpleChanges): void {
    if (changes['options'] || changes['value']) {
      this.syncSelectedIndexFromValue();
      this.resetWheelMotion(false);
    }
  }

  ngOnDestroy(): void {
    this.cancelWheelAnimation();
  }

  selectOption(index: number): void {
    this.selectedIndex = this.clampIndex(index);
    this.resetWheelMotion(true);
  }

  onOptionClick(index: number, event: MouseEvent): void {
    if (this.suppressNextClick) {
      event.preventDefault();
      event.stopPropagation();
      return;
    }

    this.selectOption(index);
  }

  onWheelPointerDown(event: PointerEvent): void {
    if (event.button !== 0 || this.options.length <= 1) {
      return;
    }

    this.cancelWheelAnimation();

    this.isWheelDragging = true;
    this.activePointerId = event.pointerId;
    this.lastPointerX = event.clientX;
    this.lastPointerTime = performance.now();
    this.totalDragDistance = 0;
    this.wheelVelocity = 0;

    const target = event.currentTarget as HTMLElement;
    target.setPointerCapture(event.pointerId);

    this.changeDetectorRef.markForCheck();
  }

  onWheelPointerMove(event: PointerEvent): void {
    if (!this.isWheelDragging || event.pointerId !== this.activePointerId) {
      return;
    }

    const now = performance.now();
    const deltaX = event.clientX - this.lastPointerX;
    const deltaTime = Math.max(1, now - this.lastPointerTime);

    this.totalDragDistance += Math.abs(deltaX);
    this.wheelVelocity = deltaX / deltaTime;
    this.wheelOffsetPx += this.applyEdgeResistance(deltaX);

    this.lastPointerX = event.clientX;
    this.lastPointerTime = now;

    this.consumeWheelOffset();
    this.changeDetectorRef.markForCheck();
  }

  onWheelPointerUp(event: PointerEvent): void {
    if (!this.isWheelDragging || event.pointerId !== this.activePointerId) {
      return;
    }

    this.isWheelDragging = false;
    this.activePointerId = null;

    const target = event.currentTarget as HTMLElement;

    if (target.hasPointerCapture(event.pointerId)) {
      target.releasePointerCapture(event.pointerId);
    }

    if (this.totalDragDistance > 8) {
      this.suppressNextClick = true;
      window.setTimeout(() => {
        this.suppressNextClick = false;
      }, 120);
    }

    this.startWheelInertia();
    this.changeDetectorRef.markForCheck();
  }

  onWheelPointerCancel(event: PointerEvent): void {
    if (event.pointerId !== this.activePointerId) {
      return;
    }

    this.isWheelDragging = false;
    this.activePointerId = null;
    this.startWheelInertia();
    this.changeDetectorRef.markForCheck();
  }

  optionTransform(index: number): string {
    const position = this.optionPosition(index);
    const distance = Math.abs(position);
    const translateX = position * this.wheelStepPx;
    const translateY = distance * 11;
    const scale = Math.max(0.52, 1 - distance * 0.1);
    const rotateY = Math.max(-30, Math.min(30, position * -8));

    return `translateX(${translateX}px) translateY(${translateY}px) scale(${scale}) rotateY(${rotateY}deg)`;
  }

  optionOpacity(index: number): number {
    const distance = Math.abs(this.optionPosition(index));

    if (distance > 4.8) {
      return 0;
    }

    return Math.max(0.14, 1 - distance * 0.16);
  }

  optionZIndex(index: number): number {
    const distance = Math.abs(this.optionPosition(index));
    return Math.round(100 - distance * 10);
  }

  optionShade(index: number): number {
    const distance = Math.abs(this.optionPosition(index));
    return Math.min(0.78, distance * 0.17);
  }

  isOptionActive(index: number): boolean {
    return index === this.selectedIndex && Math.abs(this.wheelOffsetPx) < 32;
  }

  private syncSelectedIndexFromValue(): void {
    if (this.options.length === 0) {
      this.selectedIndex = 0;
      this.selectedOption = null;
      this.visibleOptionsList = [];
      return;
    }

    const selectedIndex = this.options.findIndex((option) => option.value === this.value);
    this.selectedIndex = selectedIndex >= 0 ? selectedIndex : this.clampIndex(this.selectedIndex);
  }

  private updateSelectedState(emitChange: boolean): void {
    if (this.options.length === 0) {
      this.selectedOption = null;
      this.visibleOptionsList = [];
      this.changeDetectorRef.markForCheck();
      return;
    }

    this.selectedIndex = this.clampIndex(this.selectedIndex);
    this.selectedOption = this.options[this.selectedIndex] ?? this.options[0];

    const radius = 4;
    const start = Math.max(0, this.selectedIndex - radius);
    const end = Math.min(this.options.length, this.selectedIndex + radius + 1);

    this.visibleOptionsList = this.options
      .slice(start, end)
      .map((option, offset) => ({
        ...option,
        index: start + offset,
      }));

    if (emitChange && this.selectedOption) {
      this.valueChange.emit(this.selectedOption.value);
      this.optionChange.emit(this.selectedOption);
    }

    this.changeDetectorRef.markForCheck();
  }

  private clampIndex(index: number): number {
    return Math.min(Math.max(index, 0), Math.max(0, this.options.length - 1));
  }

  private optionPosition(index: number): number {
    return index - this.selectedIndex + this.wheelOffsetPx / this.wheelStepPx;
  }

  private consumeWheelOffset(): void {
    let changed = false;

    while (this.wheelOffsetPx >= this.wheelStepPx / 2) {
      if (this.selectedIndex <= 0) {
        this.wheelOffsetPx = this.wheelStepPx / 2;
        this.wheelVelocity = 0;
        break;
      }

      this.selectedIndex -= 1;
      this.wheelOffsetPx -= this.wheelStepPx;
      changed = true;
    }

    while (this.wheelOffsetPx <= -this.wheelStepPx / 2) {
      if (this.selectedIndex >= this.options.length - 1) {
        this.wheelOffsetPx = -this.wheelStepPx / 2;
        this.wheelVelocity = 0;
        break;
      }

      this.selectedIndex += 1;
      this.wheelOffsetPx += this.wheelStepPx;
      changed = true;
    }

    if (changed) {
      this.updateSelectedState(true);
    }
  }

  private applyEdgeResistance(deltaX: number): number {
    const isPullingBeforeFirst = this.selectedIndex === 0 && deltaX > 0;
    const isPullingAfterLast =
      this.selectedIndex === this.options.length - 1 && deltaX < 0;

    if (isPullingBeforeFirst || isPullingAfterLast) {
      return deltaX * 0.2;
    }

    return deltaX;
  }

  private startWheelInertia(): void {
    const minVelocity = 0.02;
    const friction = 0.9;
    let lastTime = performance.now();

    const animate = () => {
      const now = performance.now();
      const deltaTime = Math.max(1, now - lastTime);
      lastTime = now;

      this.wheelOffsetPx += this.applyEdgeResistance(
        this.wheelVelocity * deltaTime
      );
      this.wheelVelocity *= friction;

      this.consumeWheelOffset();

      if (Math.abs(this.wheelVelocity) > minVelocity) {
        this.wheelAnimationFrame = requestAnimationFrame(animate);
        this.changeDetectorRef.markForCheck();
        return;
      }

      this.snapWheelToCenter();
    };

    this.wheelAnimationFrame = requestAnimationFrame(animate);
  }

  private snapWheelToCenter(): void {
    const startOffset = this.wheelOffsetPx;
    const duration = 160;
    const startedAt = performance.now();

    const animate = () => {
      const elapsed = performance.now() - startedAt;
      const progress = Math.min(1, elapsed / duration);
      const easedProgress = 1 - Math.pow(1 - progress, 3);

      this.wheelOffsetPx = startOffset * (1 - easedProgress);

      if (progress < 1) {
        this.wheelAnimationFrame = requestAnimationFrame(animate);
        this.changeDetectorRef.markForCheck();
        return;
      }

      this.wheelOffsetPx = 0;
      this.wheelVelocity = 0;
      this.wheelAnimationFrame = null;
      this.updateSelectedState(true);
    };

    this.wheelAnimationFrame = requestAnimationFrame(animate);
  }

  private resetWheelMotion(emitChange: boolean): void {
    this.cancelWheelAnimation();
    this.wheelOffsetPx = 0;
    this.wheelVelocity = 0;
    this.updateSelectedState(emitChange);
  }

  private cancelWheelAnimation(): void {
    if (this.wheelAnimationFrame !== null) {
      cancelAnimationFrame(this.wheelAnimationFrame);
      this.wheelAnimationFrame = null;
    }
  }
}


============================================================
FILE: src\app\shared\presentation\components\app-wheel-selector\wheel-option.model.ts
============================================================

export type WheelOptionValue = string | number;

export interface WheelOption<TValue extends WheelOptionValue = WheelOptionValue> {
  label: string;
  value: TValue;
  sublabel?: string;
}

export interface VisibleWheelOption<TValue extends WheelOptionValue = WheelOptionValue> extends WheelOption<TValue> {
  index: number;
}


============================================================
FILE: src\app\shared\presentation\components\calibration-card\calibration-card.component.html
============================================================

<article class="calibration-card" [class.calibration-card--off]="!enabled">
  <div class="calibration-card__top">
    <div>
      <span>{{ source }}</span>
      <h3>{{ title }}</h3>
    </div>

    <app-setting-toggle
      class="calibration-card__toggle"
      [title]="enabled ? 'Activo' : 'Apagado'"
      [checked]="enabled"
      (checkedChange)="enabledChange.emit($event)"
    />
  </div>

  <p>{{ description }}</p>

  <div class="calibration-card__meta">
    <small>{{ metric }}</small>
  </div>

  <app-weight-slider
    label="Peso operativo"
    [value]="weight"
    [percent]="percent"
    [active]="enabled"
    (valueChange)="weightChange.emit($event)"
  />
</article>


============================================================
FILE: src\app\shared\presentation\components\calibration-card\calibration-card.component.scss
============================================================

.calibration-card {
  display: grid;
  gap: 14px;
  min-height: 230px;
  padding: 18px;
  border: 1px solid var(--border-color);
  border-radius: 20px;
  background:
    radial-gradient(circle at top right, var(--ec-accent-soft), transparent 34%),
    var(--ec-surface-muted);
  box-shadow: var(--ec-inset-highlight);
}

.calibration-card--off {
  opacity: 0.62;
}

.calibration-card__top {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 14px;
  align-items: start;
}

.calibration-card__top span {
  color: var(--accent-color);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.calibration-card h3 {
  margin: 7px 0 0;
  color: var(--text-primary);
  font-size: 1.05rem;
  font-weight: 950;
  line-height: 1.15;
}

.calibration-card p {
  margin: 0;
  color: var(--text-secondary);
  line-height: 1.45;
}

.calibration-card__meta {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.calibration-card__meta small {
  padding: 7px 10px;
  border-radius: 999px;
  color: var(--ec-status-info-text);
  background: var(--ec-status-info-bg);
  font-size: 0.75rem;
  font-weight: 900;
}

.calibration-card__toggle {
  width: 106px;
}

.calibration-card__toggle ::ng-deep .setting-toggle {
  min-height: 38px;
  gap: 8px;
  padding: 5px 7px 5px 10px;
  border-radius: 999px;
}

.calibration-card__toggle ::ng-deep .setting-toggle small {
  display: none;
}

.calibration-card__toggle ::ng-deep .setting-toggle strong {
  font-size: 0.72rem;
}

.calibration-card__toggle ::ng-deep .setting-toggle__switch {
  width: 34px;
  height: 20px;
  padding: 2px;
}

.calibration-card__toggle ::ng-deep .setting-toggle__switch span {
  width: 16px;
  height: 16px;
}

.calibration-card__toggle ::ng-deep .setting-toggle--active .setting-toggle__switch span {
  transform: translateX(14px);
}


============================================================
FILE: src\app\shared\presentation\components\calibration-card\calibration-card.component.ts
============================================================

import { TranslateModule } from '@ngx-translate/core';
import { Component, EventEmitter, Input, Output } from '@angular/core';

import { SettingToggleComponent } from '../setting-toggle/setting-toggle.component';
import { WeightSliderComponent } from '../weight-slider/weight-slider.component';

@Component({
  selector: 'app-calibration-card',
  standalone: true,
  imports: [
    TranslateModule,SettingToggleComponent, WeightSliderComponent],
  templateUrl: './calibration-card.component.html',
  styleUrls: ['./calibration-card.component.scss'],
})
export class CalibrationCardComponent {
  @Input() source = '';
  @Input() title = '';
  @Input() description = '';
  @Input() metric = '';
  @Input() enabled = false;
  @Input() weight = 0;
  @Input() percent = 0;

  @Output() enabledChange = new EventEmitter<boolean>();
  @Output() weightChange = new EventEmitter<number>();
}


============================================================
FILE: src\app\shared\presentation\components\confirm-dialog\confirm-dialog.component.html
============================================================

@if (open) {
  <div class="confirm-backdrop" [class.closing]="closing" (click)="cancel()">
    <section
      class="confirm-dialog"
      [class.confirm-dialog--danger]="tone === 'danger'"
      [class.confirm-dialog--info]="tone === 'info'"
      [class.closing]="closing"
      (click)="$event.stopPropagation()"
    >
      <span class="confirm-dialog__mark" aria-hidden="true">!</span>

      <div class="confirm-dialog__content">
        <h2>{{ title }}</h2>
        <p>{{ message }}</p>
        @if (detail) {
          <p class="confirm-dialog__detail">{{ detail }}</p>
        }
      </div>

      @if (requiresTypedConfirmation) {
        <label class="confirm-dialog__typed-check">
          <span>{{ requiredTextLabel || 'Escribe la confirmacion exacta' }}</span>
          <strong>{{ requiredText }}</strong>
          <input
            type="text"
            name="confirmDialogText"
            [value]="confirmationText"
            (input)="onConfirmationTextInput($event)"
            autocomplete="off"
            [attr.aria-label]="requiredTextLabel || 'Confirmacion requerida'"
          />
          @if (requiredTextHint) {
            <small>{{ requiredTextHint }}</small>
          }
        </label>
      }

      <div class="confirm-dialog__actions">
        <app-button type="button" variant="ghost" size="sm" (pressed)="cancel()">
          {{ cancelLabel }}
        </app-button>
        <app-button
          type="button"
          [variant]="confirmVariant"
          size="sm"
          [disabled]="!canConfirm"
          (pressed)="confirm()"
        >
          {{ confirmLabel }}
        </app-button>
      </div>
    </section>
  </div>
}


============================================================
FILE: src\app\shared\presentation\components\confirm-dialog\confirm-dialog.component.scss
============================================================

:host {
  position: fixed;
  inset: 0;
  z-index: 1000;
  display: block;
  pointer-events: none;
}

.confirm-backdrop {
  position: fixed;
  inset: 0;
  display: grid;
  place-items: center;
  min-height: 100dvh;
  padding: clamp(18px, 4vw, 40px);
  background:
    var(--ec-overlay-glow),
    var(--ec-overlay-bg);
  backdrop-filter: blur(12px);
  pointer-events: auto;
  animation: confirmBackdropEnter 0.24s ease both;
}

.confirm-dialog {
  width: min(420px, calc(100vw - 36px));
  max-height: min(84dvh, 560px);
  display: grid;
  gap: 16px;
  padding: 22px;
  border: 1px solid var(--border-strong);
  border-radius: 20px;
  background: var(--ec-modal-bg);
  box-shadow:
    var(--ec-shadow-modal),
    0 0 0 1px rgba(255, 255, 255, 0.02) inset;
  overflow: auto;
  animation: confirmDialogEnter 0.3s cubic-bezier(0.16, 1, 0.3, 1) both;
}

.confirm-backdrop.closing {
  animation: confirmBackdropExit 0.18s ease both;
}

.confirm-dialog.closing {
  animation: confirmDialogExit 0.18s ease both;
}

.confirm-dialog__mark {
  width: 42px;
  height: 42px;
  display: grid;
  place-items: center;
  border-radius: 16px;
  color: var(--text-on-action);
  background:
    linear-gradient(145deg, rgba(250, 204, 21, 0.88), rgba(180, 83, 9, 0.88));
  box-shadow:
    0 14px 34px rgba(180, 83, 9, 0.18),
    0 1px 0 rgba(255, 255, 255, 0.08) inset;
  font-size: 1.1rem;
  font-weight: 950;
}

.confirm-dialog__content {
  display: grid;
  gap: 10px;
}

.confirm-dialog h2,
.confirm-dialog p {
  margin: 0;
}

.confirm-dialog h2 {
  color: var(--text-primary);
  font-size: 1.18rem;
  line-height: 1.22;
}

.confirm-dialog p {
  color: var(--text-secondary);
  font-size: 0.96rem;
  line-height: 1.55;
}

.confirm-dialog__detail {
  padding: 14px;
  border: 1px solid var(--ec-status-danger-border);
  border-radius: 14px;
  color: var(--text-primary);
  background: var(--ec-status-danger-bg);
}

.confirm-dialog--danger {
  border-color: var(--ec-status-danger-border);
}

.confirm-dialog--danger:has(.confirm-dialog__typed-check) {
  width: min(560px, calc(100vw - 36px));
  border-color: var(--ec-status-danger-border);
  box-shadow:
    var(--ec-shadow-modal),
    0 0 0 1px var(--ec-status-danger-bg) inset,
    0 24px 70px color-mix(in srgb, var(--ec-status-danger-solid) 22%, transparent);
}

.confirm-dialog--danger .confirm-dialog__mark {
  background: var(--ec-danger-bg);
  box-shadow:
    var(--ec-danger-shadow),
    0 1px 0 rgba(255, 255, 255, 0.08) inset;
}

.confirm-dialog--info .confirm-dialog__mark {
  background: var(--ec-action-bg);
  box-shadow:
    var(--ec-action-shadow),
    0 1px 0 rgba(255, 255, 255, 0.08) inset;
}

.confirm-dialog__actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  flex-wrap: wrap;
}

.confirm-dialog__typed-check {
  display: grid;
  gap: 10px;
  padding: 16px;
  border: 1px solid var(--ec-status-danger-border);
  border-radius: 16px;
  background:
    linear-gradient(180deg, var(--ec-status-danger-bg), transparent),
    var(--surface-card);
}

.confirm-dialog__typed-check span,
.confirm-dialog__typed-check small {
  color: var(--text-secondary);
  line-height: 1.45;
}

.confirm-dialog__typed-check span {
  font-size: 0.86rem;
  font-weight: 800;
}

.confirm-dialog__typed-check strong {
  width: fit-content;
  padding: 5px 9px;
  border: 1px solid var(--ec-status-danger-border);
  border-radius: 10px;
  color: var(--text-primary);
  background: var(--ec-status-danger-bg);
  font-size: 0.9rem;
  letter-spacing: 0.08em;
}

.confirm-dialog__typed-check input {
  width: 100%;
  min-height: 46px;
  padding: 0 14px;
  border: 1px solid var(--ec-status-danger-border);
  border-radius: 12px;
  color: var(--text-primary);
  background: var(--field-bg);
  font: inherit;
  transition:
    border-color 0.18s ease,
    box-shadow 0.18s ease,
    background 0.18s ease;
}

.confirm-dialog__typed-check input:focus {
  outline: none;
  border-color: var(--ec-status-danger-border);
  box-shadow: 0 0 0 4px var(--ec-status-danger-bg);
}

@keyframes confirmDialogEnter {
  from {
    opacity: 0;
    transform: translateY(18px) scale(0.96);
    filter: blur(4px);
  }

  to {
    opacity: 1;
    transform: translateY(0) scale(1);
    filter: blur(0);
  }
}

@keyframes confirmDialogExit {
  from {
    opacity: 1;
    transform: translateY(0) scale(1);
    filter: blur(0);
  }

  to {
    opacity: 0;
    transform: translateY(10px) scale(0.97);
    filter: blur(3px);
  }
}

@keyframes confirmBackdropEnter {
  from {
    opacity: 0;
    backdrop-filter: blur(0);
  }

  to {
    opacity: 1;
    backdrop-filter: blur(10px);
  }
}

@keyframes confirmBackdropExit {
  from {
    opacity: 1;
    backdrop-filter: blur(10px);
  }

  to {
    opacity: 0;
    backdrop-filter: blur(0);
  }
}

@media (max-width: 520px) {
  .confirm-dialog__actions {
    justify-content: stretch;
  }

  .confirm-dialog__actions app-button {
    flex: 1 1 140px;
  }
}


============================================================
FILE: src\app\shared\presentation\components\confirm-dialog\confirm-dialog.component.ts
============================================================

import { Component, EventEmitter, Input, OnDestroy, Output } from '@angular/core';

import { ConfirmDialogTone } from '../../../application/services/confirm-dialog.service';
import { AppButtonComponent, AppButtonVariant } from '../app-button/app-button.component';

@Component({
  selector: 'app-confirm-dialog',
  standalone: true,
  imports: [AppButtonComponent],
  templateUrl: './confirm-dialog.component.html',
  styleUrls: ['./confirm-dialog.component.scss'],
})
export class ConfirmDialogComponent implements OnDestroy {
  @Input() open = false;
  @Input() title = 'Confirmar accion';
  @Input() message = '';
  @Input() detail?: string;
  @Input() confirmLabel = 'Confirmar';
  @Input() cancelLabel = 'Cancelar';
  @Input() tone: ConfirmDialogTone = 'warning';
  @Input() requiredText?: string;
  @Input() requiredTextLabel?: string;
  @Input() requiredTextHint?: string;

  @Output() confirmed = new EventEmitter<void>();
  @Output() cancelled = new EventEmitter<void>();

  closing = false;
  confirmationText = '';

  private closeTimer: ReturnType<typeof setTimeout> | null = null;

  get confirmVariant(): AppButtonVariant {
    return this.tone === 'danger' ? 'danger' : 'action';
  }

  get requiresTypedConfirmation(): boolean {
    return Boolean(this.requiredText);
  }

  get canConfirm(): boolean {
    return !this.requiredText || this.confirmationText === this.requiredText;
  }

  ngOnDestroy(): void {
    if (this.closeTimer) {
      clearTimeout(this.closeTimer);
    }
  }

  confirm(): void {
    if (!this.canConfirm) {
      return;
    }

    this.close(() => this.confirmed.emit());
  }

  onConfirmationTextInput(event: Event): void {
    this.confirmationText = (event.target as HTMLInputElement).value;
  }

  cancel(): void {
    this.close(() => this.cancelled.emit());
  }

  private close(emit: () => void): void {
    if (this.closing) {
      return;
    }

    this.closing = true;
    this.closeTimer = setTimeout(() => {
      emit();
      this.closeTimer = null;
    }, 180);
  }
}


============================================================
FILE: src\app\shared\presentation\components\draggable-bar-carousel\draggable-bar-carousel.component.html
============================================================

<div
  class="draggable-bar-carousel"
  [class.draggable-bar-carousel--dragging]="dragging()"
  [class.draggable-bar-carousel--momentum]="momentum()"
  [class.draggable-bar-carousel--auto-advancing]="autoAdvancing()"
  (pointerdown)="onPointerDown($event)"
  (pointermove)="onPointerMove($event)"
  (pointerup)="onPointerUp($event)"
  (pointercancel)="onPointerCancel($event)"
  (pointerleave)="onPointerCancel($event)"
>
  <div class="draggable-bar-carousel__glow"></div>

  <div class="draggable-bar-carousel__rail" [style.transform]="railTransform()">
    @for (item of visibleItems(); track item.key) {
      <article
        class="draggable-bar-carousel__item"
        [class.draggable-bar-carousel__item--active]="isActive(item.index)"
        [style.transform]="itemTransform(item.index)"
        [style.opacity]="itemOpacity(item.index)"
        [style.z-index]="itemZIndex(item.index)"
      >
        <span class="draggable-bar-carousel__track">
          <span
            class="draggable-bar-carousel__bar"
            [class.draggable-bar-carousel__bar--high]="item.tone === 'warning'"
            [style.height.%]="item.percentage"
          ></span>
        </span>

        @if (selectable) {
          <button
            class="draggable-bar-carousel__chip"
            type="button"
            [attr.aria-label]="item.ariaLabel || item.title || item.value"
            (pointerdown)="onChipPointerDown($event)"
            (click)="selectItem(item, $event)"
          >
            <strong>{{ item.value }}</strong>
            <small>{{ item.caption }}</small>
          </button>
        } @else {
          <div
            class="draggable-bar-carousel__chip"
            tabindex="0"
            role="group"
            [attr.aria-label]="item.ariaLabel || item.title || item.value"
          >
            <strong>{{ item.value }}</strong>
            <small>{{ item.caption }}</small>

            @if (item.title || item.subtitle || item.detail || item.meta) {
              <span class="draggable-bar-carousel__tooltip" aria-hidden="true">
                @if (item.eyebrow) {
                  <span class="draggable-bar-carousel__tooltip-emoji">
                    {{ item.eyebrow }}
                  </span>
                }

                @if (item.title) {
                  <strong>{{ item.title }}</strong>
                }

                @if (item.subtitle) {
                  <small>{{ item.subtitle }}</small>
                }

                @if (item.detail) {
                  <small>{{ item.detail }}</small>
                }

                @if (item.meta) {
                  <small>{{ item.meta }}</small>
                }
              </span>
            }
          </div>
        }
      </article>
    }
  </div>
</div>


============================================================
FILE: src\app\shared\presentation\components\draggable-bar-carousel\draggable-bar-carousel.component.scss
============================================================

:host {
  display: block;
}

.draggable-bar-carousel {
  position: relative;
  min-height: 440px;
  overflow: hidden;
  border: 1px solid var(--border-color);
  border-radius: 24px;
  padding: 16px 14px 18px;
  background:
    var(--ec-card-highlight),
    var(--ec-surface-muted);
  box-shadow: var(--ec-inset-highlight), var(--ec-shadow-panel);
  cursor: grab;
  perspective: 980px;
  touch-action: none;
  user-select: none;
}

.draggable-bar-carousel--dragging {
  cursor: grabbing;
}

.draggable-bar-carousel__glow {
  position: absolute;
  left: 50%;
  top: 44%;
  width: min(360px, 62%);
  height: 230px;
  border-radius: 999px;
  background:
    radial-gradient(circle at center, var(--ec-status-stable-bg), transparent 42%),
    radial-gradient(circle at center, var(--ec-accent-soft), transparent 64%);
  filter: blur(28px);
  pointer-events: none;
  transform: translate(-50%, -50%);
}

.draggable-bar-carousel__rail {
  position: absolute;
  z-index: 1;
  left: 50%;
  bottom: 66px;
  display: flex;
  align-items: end;
  gap: 42px;
  width: max-content;
  height: 304px;
  transition: transform 360ms cubic-bezier(0.18, 0.9, 0.2, 1);
  will-change: transform;
}

.draggable-bar-carousel--dragging .draggable-bar-carousel__rail,
.draggable-bar-carousel--momentum .draggable-bar-carousel__rail,
.draggable-bar-carousel--auto-advancing .draggable-bar-carousel__rail {
  transition: none;
}

.draggable-bar-carousel__item {
  position: relative;
  flex: 0 0 126px;
  width: 126px;
  display: grid;
  grid-template-rows: minmax(220px, 1fr) auto;
  gap: 12px;
  min-width: 0;
  transform-origin: center;
  transition: opacity 180ms ease, transform 300ms cubic-bezier(0.18, 0.9, 0.2, 1);
  will-change: transform;
}

.draggable-bar-carousel--dragging .draggable-bar-carousel__item,
.draggable-bar-carousel--momentum .draggable-bar-carousel__item,
.draggable-bar-carousel--auto-advancing .draggable-bar-carousel__item {
  transition: none;
}

.draggable-bar-carousel__track {
  display: flex;
  align-items: end;
  justify-content: center;
  width: 100%;
  min-height: 224px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  padding: 14px 12px 12px;
  background:
    var(--ec-card-highlight),
    color-mix(in srgb, var(--surface-muted) 74%, transparent);
  transition: transform 180ms ease, border-color 180ms ease, background 180ms ease, box-shadow 180ms ease;
}

.draggable-bar-carousel__bar {
  display: block;
  width: min(46px, 68%);
  min-height: 18px;
  border-radius: 12px 12px 5px 5px;
  background: var(--ec-action-bg);
  box-shadow: 0 18px 34px rgba(37, 99, 235, 0.2);
  transition: height 260ms ease;
}

.draggable-bar-carousel__bar--high {
  background: var(--ec-danger-bg);
  box-shadow: 0 18px 34px rgba(239, 68, 68, 0.18);
}

.draggable-bar-carousel__chip {
  position: relative;
  appearance: none;
  display: grid;
  place-items: center;
  justify-self: center;
  width: max-content;
  margin: 0;
  min-width: 78px;
  min-height: 44px;
  border: 1px solid var(--border-color);
  border-radius: 15px;
  padding: 8px 12px 7px;
  color: var(--text-primary);
  background:
    var(--ec-card-highlight),
    color-mix(in srgb, var(--surface-color) 86%, transparent);
  cursor: pointer;
  font: inherit;
  box-shadow: var(--ec-shadow-soft);
  line-height: 1.05;
  text-align: center;
  touch-action: none;
  transition: transform 180ms ease, border-color 180ms ease, box-shadow 180ms ease;
}

.draggable-bar-carousel__chip strong,
.draggable-bar-carousel__chip small {
  display: block;
  max-width: 90px;
  overflow: hidden;
  text-align: center;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.draggable-bar-carousel__chip strong {
  color: var(--text-primary);
  font-size: 0.74rem;
  font-weight: 950;
}

.draggable-bar-carousel__chip small {
  margin-top: 4px;
  color: var(--text-muted);
  font-size: 0.66rem;
  font-weight: 900;
}

.draggable-bar-carousel__item--active .draggable-bar-carousel__track {
  transform: translateY(-4px) scale(1.02);
  border-color: var(--border-strong);
  background:
    radial-gradient(circle at 50% 100%, var(--ec-accent-soft), transparent 48%),
    color-mix(in srgb, var(--surface-muted) 80%, transparent);
  box-shadow: var(--ec-action-shadow-hover);
}

.draggable-bar-carousel__item--active .draggable-bar-carousel__chip {
  transform: translateY(-4px);
  border-color: var(--border-strong);
}

.draggable-bar-carousel__chip:hover,
.draggable-bar-carousel__chip:focus-visible {
  transform: translateY(-2px);
  border-color: var(--border-strong);
  box-shadow:
    0 0 0 1px var(--ec-accent-soft),
    var(--ec-shadow-soft);
  outline: none;
}

.draggable-bar-carousel__tooltip {
  position: absolute;
  z-index: 30;
  bottom: calc(100% + 12px);
  left: 50%;
  display: grid;
  gap: 4px;
  width: min(220px, 76vw);
  padding: 14px 15px;
  border: 1px solid var(--border-strong);
  border-radius: 18px;
  background: var(--ec-modal-bg);
  box-shadow:
    var(--ec-shadow-panel),
    var(--ec-inset-highlight);
  opacity: 0;
  pointer-events: none;
  justify-items: center;
  text-align: center;
  transform: translateX(-50%) translateY(10px) scale(0.98);
  transition: opacity 180ms ease, transform 180ms ease;
}

.draggable-bar-carousel__chip:hover .draggable-bar-carousel__tooltip,
.draggable-bar-carousel__chip:focus-visible .draggable-bar-carousel__tooltip {
  opacity: 1;
  transform: translateX(-50%) translateY(0) scale(1);
}

.draggable-bar-carousel__tooltip::after {
  content: '';
  position: absolute;
  bottom: -7px;
  left: 50%;
  width: 12px;
  height: 12px;
  border-right: 1px solid var(--border-strong);
  border-bottom: 1px solid var(--border-strong);
  background: var(--surface-elevated-solid);
  transform: translateX(-50%) rotate(45deg);
}

.draggable-bar-carousel__tooltip-emoji {
  display: inline-grid;
  place-items: center;
  width: 34px;
  height: 34px;
  margin-bottom: 2px;
  border: 1px solid var(--border-color);
  border-radius: 12px;
  background: var(--ec-accent-soft);
  font-size: 1.05rem;
}

.draggable-bar-carousel__tooltip strong,
.draggable-bar-carousel__tooltip small {
  display: block;
}

.draggable-bar-carousel__tooltip strong {
  color: var(--text-primary);
  font-size: 0.84rem;
  font-weight: 950;
  line-height: 1.15;
  overflow-wrap: anywhere;
}

.draggable-bar-carousel__tooltip small {
  color: var(--text-secondary);
  font-size: 0.74rem;
  font-weight: 800;
  overflow-wrap: anywhere;
}

.draggable-bar-carousel__item:hover,
.draggable-bar-carousel__item:focus-within {
  z-index: 200 !important;
}


============================================================
FILE: src\app\shared\presentation\components\draggable-bar-carousel\draggable-bar-carousel.component.ts
============================================================

import {
  Component,
  EventEmitter,
  Input,
  OnChanges,
  Output,
  SimpleChanges,
  signal,
} from '@angular/core';

export interface DraggableBarCarouselItem {
  key: string;
  value: string;
  caption: string;
  percentage: number;
  ariaLabel?: string;
  eyebrow?: string;
  title?: string;
  subtitle?: string;
  detail?: string;
  meta?: string;
  tone?: 'default' | 'warning';
}

interface VisibleDraggableBarCarouselItem extends DraggableBarCarouselItem {
  index: number;
}

@Component({
  selector: 'app-draggable-bar-carousel',
  standalone: true,
  templateUrl: './draggable-bar-carousel.component.html',
  styleUrls: ['./draggable-bar-carousel.component.scss'],
})
export class DraggableBarCarouselComponent implements OnChanges {
  @Input() items: DraggableBarCarouselItem[] = [];
  @Input() visibleRadius = 5;
  @Input() selectable = false;
  @Input() followLatest = false;
  @Output() itemSelected = new EventEmitter<DraggableBarCarouselItem>();

  readonly dragging = signal(false);
  readonly momentum = signal(false);
  readonly autoAdvancing = signal(false);
  readonly motionTick = signal(0);

  private readonly stepPx = 168;
  private readonly dragSensitivity = 1.55;
  private readonly throwPower = 1.9;
  private offsetPx = 0;
  private velocity = 0;
  private selectedIndex = 0;
  private didDrag = false;
  private activePointerId: number | null = null;
  private lastPointerX = 0;
  private lastPointerTime = 0;
  private animationFrame: number | null = null;

  ngOnChanges(changes: SimpleChanges): void {
    if (!changes['items']) {
      return;
    }

    const previousItems = (changes['items'].previousValue ??
      []) as DraggableBarCarouselItem[];
    const nextLatestIndex = Math.max(0, this.items.length - 1);
    const wasAtLatest =
      this.selectedIndex >= Math.max(0, previousItems.length - 1);

    if (
      this.followLatest &&
      wasAtLatest &&
      previousItems.length > 0 &&
      this.items.length > previousItems.length
    ) {
      this.advanceToLatest(Math.max(0, previousItems.length - 1), nextLatestIndex);
      return;
    }

    this.selectedIndex = this.followLatest && wasAtLatest
      ? nextLatestIndex
      : this.clampIndex(this.selectedIndex);
    this.offsetPx = 0;
    this.markMotionChanged();
  }

  visibleItems(): VisibleDraggableBarCarouselItem[] {
    const selectedIndex = this.activeIndex();
    const start = Math.max(0, selectedIndex - this.visibleRadius);
    const end = Math.min(this.items.length, selectedIndex + this.visibleRadius + 1);

    return this.items.slice(start, end).map((item, offset) => ({
      ...item,
      index: start + offset,
    }));
  }

  railTransform(): string {
    this.motionTick();

    if (this.items.length === 0) {
      return 'translateX(0)';
    }

    const start = this.visibleStartIndex();
    const selectedOffset = this.activeIndex() - start;
    const translateX = -selectedOffset * this.stepPx + this.offsetPx - 63;

    return `translateX(${translateX}px)`;
  }

  itemTransform(index: number): string {
    const position = this.itemPosition(index);
    const distance = Math.abs(position);
    const focus = Math.max(0, 1 - Math.min(distance, 1));
    const magneticX = -Math.sign(position) * Math.min(22, distance * 10);
    const translateY = Math.min(distance, 4.5) * 14 - focus * 34;
    const scale = Math.max(0.72, 1.08 - distance * 0.06);
    const rotateY = Math.max(-24, Math.min(24, position * -7));

    return `translateX(${magneticX}px) translateY(${translateY}px) scale(${scale}) rotateY(${rotateY}deg)`;
  }

  itemOpacity(index: number): number {
    const distance = Math.abs(this.itemPosition(index));
    return distance > 5.4 ? 0 : Math.max(0.18, 1 - distance * 0.13);
  }

  itemZIndex(index: number): number {
    const distance = Math.abs(this.itemPosition(index));
    return Math.round(120 - distance * 12);
  }

  isActive(index: number): boolean {
    this.motionTick();
    return index === this.activeIndex() && Math.abs(this.offsetPx) < 32;
  }

  onPointerDown(event: PointerEvent): void {
    if (this.items.length <= 1 || event.button !== 0) {
      return;
    }

    this.cancelAnimation();
    this.dragging.set(true);
    this.activePointerId = event.pointerId;
    this.lastPointerX = event.clientX;
    this.lastPointerTime = performance.now();
    this.velocity = 0;
    this.didDrag = false;
    (event.currentTarget as HTMLElement).setPointerCapture(event.pointerId);
  }

  onPointerMove(event: PointerEvent): void {
    if (!this.dragging() || event.pointerId !== this.activePointerId) {
      return;
    }

    const now = performance.now();
    const deltaX = event.clientX - this.lastPointerX;
    const deltaTime = Math.max(1, now - this.lastPointerTime);

    if (Math.abs(deltaX) > 2) {
      this.didDrag = true;
    }

    this.velocity = (deltaX / deltaTime) * this.throwPower;
    this.offsetPx += this.applyEdgeResistance(deltaX * this.dragSensitivity);
    this.lastPointerX = event.clientX;
    this.lastPointerTime = now;
    this.consumeOffset();
    this.markMotionChanged();
    event.preventDefault();
  }

  onPointerUp(event: PointerEvent): void {
    if (!this.dragging() || event.pointerId !== this.activePointerId) {
      return;
    }

    this.dragging.set(false);
    this.activePointerId = null;

    const target = event.currentTarget as HTMLElement;

    if (target.hasPointerCapture(event.pointerId)) {
      target.releasePointerCapture(event.pointerId);
    }

    this.startInertia();
  }

  onPointerCancel(event: PointerEvent): void {
    if (event.pointerId !== this.activePointerId) {
      return;
    }

    this.dragging.set(false);
    this.activePointerId = null;
    this.startInertia();
  }

  onChipPointerDown(event: PointerEvent): void {
    this.didDrag = false;
    event.stopPropagation();
  }

  selectItem(item: DraggableBarCarouselItem, event?: MouseEvent): void {
    event?.stopPropagation();

    if (this.didDrag) {
      this.didDrag = false;
      return;
    }

    this.itemSelected.emit(item);
  }

  private activeIndex(): number {
    return this.clampIndex(this.selectedIndex);
  }

  private visibleStartIndex(): number {
    return Math.max(0, this.activeIndex() - this.visibleRadius);
  }

  private clampIndex(index: number): number {
    return Math.min(Math.max(index, 0), Math.max(0, this.items.length - 1));
  }

  private itemPosition(index: number): number {
    this.motionTick();
    return index - this.activeIndex() + this.offsetPx / this.stepPx;
  }

  private consumeOffset(): void {
    let nextIndex = this.clampIndex(this.selectedIndex);

    while (this.offsetPx >= this.stepPx / 2) {
      if (nextIndex <= 0) {
        this.offsetPx = this.stepPx / 2;
        this.velocity = 0;
        break;
      }

      nextIndex -= 1;
      this.offsetPx -= this.stepPx;
    }

    while (this.offsetPx <= -this.stepPx / 2) {
      if (nextIndex >= this.items.length - 1) {
        this.offsetPx = -this.stepPx / 2;
        this.velocity = 0;
        break;
      }

      nextIndex += 1;
      this.offsetPx += this.stepPx;
    }

    this.selectedIndex = nextIndex;
  }

  private applyEdgeResistance(deltaX: number): number {
    const index = this.activeIndex();
    const isPullingBeforeFirst = index === 0 && deltaX > 0;
    const isPullingAfterLast = index === this.items.length - 1 && deltaX < 0;

    return isPullingBeforeFirst || isPullingAfterLast ? deltaX * 0.22 : deltaX;
  }

  private startInertia(): void {
    const minVelocity = 0.014;
    const friction = 0.935;
    let lastTime = performance.now();

    this.momentum.set(true);

    const animate = () => {
      const now = performance.now();
      const deltaTime = Math.max(1, now - lastTime);
      lastTime = now;

      this.offsetPx += this.applyEdgeResistance(this.velocity * deltaTime);
      this.velocity *= friction;
      this.consumeOffset();
      this.markMotionChanged();

      if (Math.abs(this.velocity) > minVelocity) {
        this.animationFrame = requestAnimationFrame(animate);
        return;
      }

      this.snapToCenter();
    };

    this.animationFrame = requestAnimationFrame(animate);
  }

  private snapToCenter(): void {
    const startOffset = this.offsetPx;
    const duration = 160;
    const startedAt = performance.now();

    const animate = () => {
      const progress = Math.min(1, (performance.now() - startedAt) / duration);
      const easedProgress = 1 - Math.pow(1 - progress, 3);

      this.offsetPx = startOffset * (1 - easedProgress);
      this.markMotionChanged();

      if (progress < 1) {
        this.animationFrame = requestAnimationFrame(animate);
        return;
      }

      this.offsetPx = 0;
      this.velocity = 0;
      this.momentum.set(false);
      this.animationFrame = null;
      this.markMotionChanged();
    };

    this.animationFrame = requestAnimationFrame(animate);
  }

  private cancelAnimation(): void {
    if (this.animationFrame !== null) {
      cancelAnimationFrame(this.animationFrame);
      this.animationFrame = null;
    }

    this.momentum.set(false);
    this.autoAdvancing.set(false);
  }

  private advanceToLatest(previousLatestIndex: number, latestIndex: number): void {
    const startIndex = this.clampIndex(previousLatestIndex);
    const targetIndex = this.clampIndex(latestIndex);
    const steps = Math.max(0, targetIndex - startIndex);

    if (steps === 0) {
      this.selectedIndex = targetIndex;
      this.offsetPx = 0;
      this.markMotionChanged();
      return;
    }

    this.cancelAnimation();
    this.autoAdvancing.set(true);
    this.selectedIndex = startIndex;
    this.offsetPx = 0;
    this.velocity = 0;
    this.markMotionChanged();

    const travelDistance = -steps * this.stepPx;
    const duration = Math.min(1400, 720 + steps * 150);
    const startedAt = performance.now();

    const animate = () => {
      const progress = Math.min(1, (performance.now() - startedAt) / duration);
      const easedProgress = this.easeInOutCubic(progress);

      this.offsetPx = travelDistance * easedProgress;
      this.markMotionChanged();

      if (progress < 1) {
        this.animationFrame = requestAnimationFrame(animate);
        return;
      }

      this.selectedIndex = targetIndex;
      this.offsetPx = 0;
      this.velocity = 0;
      this.autoAdvancing.set(false);
      this.animationFrame = null;
      this.markMotionChanged();
    };

    this.animationFrame = requestAnimationFrame(animate);
  }

  private easeInOutCubic(progress: number): number {
    return progress < 0.5
      ? 4 * progress * progress * progress
      : 1 - Math.pow(-2 * progress + 2, 3) / 2;
  }

  private markMotionChanged(): void {
    this.motionTick.update((value) => (value + 1) % 100000);
  }
}


============================================================
FILE: src\app\shared\presentation\components\empty-state\empty-state.component.html
============================================================

<article class="empty-state" [class.empty-state--compact]="compact">
  @if (eyebrow) {
    <span class="empty-state__eyebrow">{{ eyebrow }}</span>
  }
  <h2>{{ title }}</h2>

  @if (description) {
    <p>{{ description }}</p>
  }

  @if (actionLabel && actionLink) {
    <app-button
      [variant]="actionVariant"
      size="sm"
      [link]="actionLink"
      [label]="actionLabel"
    />
  }

  <ng-content />
</article>


============================================================
FILE: src\app\shared\presentation\components\empty-state\empty-state.component.scss
============================================================

:host {
  display: block;
  min-width: 0;
}

.empty-state {
  position: relative;
  overflow: hidden;
  display: grid;
  justify-items: start;
  gap: 12px;
  min-height: 150px;
  padding: clamp(18px, 3vw, 24px);
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background:
    var(--ec-card-highlight),
    var(--surface-card);
  box-shadow:
    var(--ec-inset-highlight),
    var(--ec-shadow-soft);
}

.empty-state::before {
  content: '';
  position: absolute;
  top: 0;
  right: 0;
  width: 34%;
  height: 3px;
  background: linear-gradient(90deg, transparent, var(--accent-color));
  opacity: 0.7;
  pointer-events: none;
}

.empty-state--compact {
  min-height: 0;
  padding: 16px;
  border-radius: 16px;
}

.empty-state__eyebrow {
  width: fit-content;
  padding: 7px 10px;
  border-radius: 999px;
  color: var(--accent-color);
  background: var(--ec-accent-soft);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.04em;
  line-height: 1;
  text-transform: uppercase;
}

.empty-state h2 {
  margin: 0;
  color: var(--text-primary);
  font-size: clamp(1.05rem, 2vw, 1.35rem);
  font-weight: 950;
  line-height: 1.15;
}

.empty-state p {
  max-width: 58ch;
  margin: 0;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

:host-context(.home-card) .empty-state--compact {
  --home-empty-description-slot: calc(4.65em + 14px);
  --home-empty-title-slot: 2.42em;

  align-content: start;
  grid-template-rows: var(--home-empty-title-slot) var(--home-empty-description-slot) auto;
  gap: 0;
  min-height: 0;
  height: auto;
  padding: 12px;
}

:host-context(.home-card--insight-empty) .empty-state--compact {
  --home-empty-description-slot: calc(3.05em + 6px);
}

:host-context(.home-card--devices) .empty-state--compact,
:host-context(.home-card--action-empty) .empty-state--compact,
:host-context(.home-card--devices-peer) .empty-state--compact {
  --home-empty-description-slot: calc(3.1em + 6px);
  --home-empty-title-slot: 3.7em;
}

:host-context(.home-card--compact-action-empty) .empty-state--compact {
  --home-empty-title-slot: 2.7em;
}

:host-context(.home-card) .empty-state--compact h2 {
  box-sizing: border-box;
  height: var(--home-empty-title-slot);
  max-width: 100%;
  overflow: hidden;
}

:host-context(.home-card) .empty-state--compact p {
  display: -webkit-box;
  box-sizing: border-box;
  width: 100%;
  min-height: var(--home-empty-description-slot);
  padding-top: 7px;
  overflow: hidden;
  border-top: 1px solid var(--border-color);
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 3;
}

:host-context(.home-card--service) .empty-state--compact p {
  max-width: none;
}

:host-context(.home-card--service) .empty-state--compact {
  --home-empty-description-slot: calc(2.1em + 2px);
}

:host-context(.home-card) .empty-state--compact app-button {
  display: flex;
  justify-self: stretch;
  width: 100%;
  margin-top: 10px;
  padding-top: 10px;
  border-top: 1px solid var(--border-color);
}


============================================================
FILE: src\app\shared\presentation\components\empty-state\empty-state.component.ts
============================================================

import { Component, Input } from '@angular/core';

import { AppButtonComponent, AppButtonVariant } from '../app-button/app-button.component';

@Component({
  selector: 'app-empty-state',
  standalone: true,
  imports: [AppButtonComponent],
  templateUrl: './empty-state.component.html',
  styleUrls: ['./empty-state.component.scss'],
})
export class EmptyStateComponent {
  @Input() eyebrow = '';
  @Input() title = '';
  @Input() description = '';
  @Input() actionLabel = '';
  @Input() actionLink: string | unknown[] | null = null;
  @Input() actionVariant: AppButtonVariant = 'create';
  @Input() compact = false;
}


============================================================
FILE: src\app\shared\presentation\components\error-message\error-message.component.html
============================================================

@if (message) {
  <p class="error-message">
    {{ message | translate }}
  </p>
}

============================================================
FILE: src\app\shared\presentation\components\error-message\error-message.component.scss
============================================================

.error-message {
  margin: 0;
  padding: 12px 14px;
  border: 1px solid var(--ec-status-danger-border);
  border-radius: 14px;
  color: var(--ec-status-danger-text);
  background: var(--ec-status-danger-bg-strong);
  font-weight: 700;
}


============================================================
FILE: src\app\shared\presentation\components\error-message\error-message.component.ts
============================================================

import { Component, Input } from '@angular/core';
import { TranslateModule } from '@ngx-translate/core';

@Component({
  selector: 'app-error-message',
  standalone: true,
  imports: [TranslateModule],
  templateUrl: './error-message.component.html',
  styleUrls: ['./error-message.component.scss'],
})
export class ErrorMessageComponent {
  @Input() message: string | null = null;
}

============================================================
FILE: src\app\shared\presentation\components\footer\footer.component.html
============================================================

<footer class="footer">
  <span>© {{ currentYear }} ElectroCorp.</span>
  <span>{{ 'footer.rights' | translate }}</span>
</footer>

============================================================
FILE: src\app\shared\presentation\components\footer\footer.component.scss
============================================================

.footer {
  display: flex;
  justify-content: center;
  gap: 8px;
  flex-wrap: wrap;
  padding: 24px;
  color: var(--text-muted);
  font-size: 0.9rem;
}

============================================================
FILE: src\app\shared\presentation\components\footer\footer.component.ts
============================================================

import { Component } from '@angular/core';
import { TranslateModule } from '@ngx-translate/core';

@Component({
  selector: 'app-footer',
  standalone: true,
  imports: [TranslateModule],
  templateUrl: './footer.component.html',
  styleUrls: ['./footer.component.scss'],
})
export class FooterComponent {
  readonly currentYear = new Date().getFullYear();
}

============================================================
FILE: src\app\shared\presentation\components\header\header.component.html
============================================================

<header class="topbar">
  <a class="brand" routerLink="/home">
    <span class="brand-icon">&#9889;</span>
    <span>{{ 'app.name' | translate }}</span>
  </a>

  @if (showPrivateNavigation) {
    <nav class="nav">
      <a routerLink="/home" routerLinkActive="active">
        {{ 'nav.home' | translate }}
      </a>

      <app-dropdown
        variant="nav"
        [triggerLabel]="'nav.energySection' | translate"
        [options]="energyOptions"
        [value]="activeValue(energyOptions)"
      />

      <app-dropdown
        variant="nav"
        [triggerLabel]="'nav.spaces' | translate"
        [options]="workplaceOptions"
        [value]="activeValue(workplaceOptions)"
      />

      <app-dropdown
        variant="nav"
        [triggerLabel]="'nav.operation' | translate"
        [options]="operationOptions"
        [value]="activeValue(operationOptions)"
      />

      <app-dropdown
        variant="nav"
        [triggerLabel]="'nav.alertsSection' | translate"
        [options]="alertsOptions"
        [value]="activeValue(alertsOptions)"
      />

      <app-dropdown
        variant="nav"
        [triggerLabel]="'nav.support' | translate"
        [options]="supportOptions"
        [value]="activeValue(supportOptions)"
      />
    </nav>
  }

  <div class="topbar-actions">
    <app-language-switcher />

    <button type="button" class="theme-button" (click)="toggleTheme()">
      @if (isDarkMode) {
        &#9728;&#65039;
      } @else {
        &#127769;
      }
    </button>

    @if (showPrivateNavigation) {
      <button
        type="button"
        class="user-menu-button"
        (click)="toggleUserMenu()"
      >
        <span class="user-menu-avatar">
          {{ userInitial }}
        </span>
      </button>
    }
  </div>
</header>


============================================================
FILE: src\app\shared\presentation\components\header\header.component.scss
============================================================

:host {
  display: block;
  width: 100%;
  position: sticky;
  top: 0;
  z-index: 99999;
  overflow: visible;
}

.topbar {
  position: relative;
  z-index: 99999;
  min-height: 70px;
  display: grid;
  grid-template-columns: auto 1fr auto;
  align-items: center;
  gap: 24px;
  padding: 0 28px;
  border-bottom: 1px solid var(--border-color);
  background: var(--ec-topbar-bg);
  backdrop-filter: blur(16px);
  overflow: visible;
}

.brand {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  color: var(--text-primary);
  text-decoration: none;
  font-weight: 950;
  white-space: nowrap;
}

.brand-icon {
  width: 34px;
  height: 34px;
  display: grid;
  place-items: center;
  border-radius: 12px;
  background: var(--accent-color);
  box-shadow: 0 14px 28px rgba(14, 165, 233, 0.2);
}

.nav {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
  min-width: 0;
  overflow: visible;
}

.nav > a,
.nav app-dropdown {
  flex: 0 0 auto;
}

.nav > a {
  min-height: 40px;
  display: inline-flex;
  align-items: center;
  padding: 0 16px;
  border-radius: 999px;
  color: var(--text-primary);
  background: var(--ec-ghost-bg);
  text-decoration: none;
  font-weight: 900;
  white-space: nowrap;
  transition:
    color 0.2s ease,
    background 0.2s ease,
    box-shadow 0.2s ease;
}

.nav > a:hover,
.nav > a.active {
  color: var(--text-primary);
  background: var(--ec-ghost-bg-hover);
  box-shadow:
    inset 0 0 0 1px rgba(125, 211, 252, 0.1),
    0 0 0 3px rgba(79, 195, 255, 0.055);
}

.topbar-actions {
  display: inline-flex;
  align-items: center;
  justify-content: flex-end;
  gap: 10px;
  white-space: nowrap;
}

.theme-button,
.user-menu-button {
  border: 1px solid var(--border-color);
  cursor: pointer;
  color: var(--text-primary);
  background: var(--ec-ghost-bg);
  transition:
    background 0.2s ease,
    border-color 0.2s ease;
}

.theme-button:hover,
.user-menu-button:hover {
  border-color: var(--border-strong);
  background: var(--ec-ghost-bg-hover);
}

.theme-button {
  width: 42px;
  height: 42px;
  display: grid;
  place-items: center;
  border-radius: 50%;
}

.user-menu-button {
  width: 44px;
  height: 44px;
  padding: 0;
  display: grid;
  place-items: center;
  border-radius: 50%;
}

.user-menu-avatar {
  width: 34px;
  height: 34px;
  display: grid;
  place-items: center;
  border-radius: 50%;
  background: var(--accent-color);
  color: var(--text-on-action);
  font-weight: 950;
  box-shadow: 0 12px 24px rgba(14, 165, 233, 0.22);
}

@media (max-width: 1080px) {
  .topbar {
    grid-template-columns: 1fr auto;
  }

  .nav {
    display: none;
  }
}

@media (max-width: 640px) {
  .topbar {
    min-height: 64px;
    padding: 0 16px;
    gap: 14px;
  }

  .brand span:last-child {
    display: none;
  }

  .theme-button {
    width: 38px;
    height: 38px;
  }

  .user-menu-button {
    width: 40px;
    height: 40px;
  }

  .user-menu-avatar {
    width: 31px;
    height: 31px;
  }
}


============================================================
FILE: src\app\shared\presentation\components\header\header.component.ts
============================================================

import { Component, EventEmitter, Input, Output, signal } from '@angular/core';
import { NavigationEnd, Router, RouterLink, RouterLinkActive } from '@angular/router';
import { TranslateModule } from '@ngx-translate/core';
import { filter } from 'rxjs';

import { AppDropdownComponent } from '../app-dropdown/app-dropdown.component';
import { DropdownOption } from '../app-dropdown/dropdown-option.model';
import { LanguageSwitcherComponent } from '../language-switcher/language-switcher.component';

@Component({
  selector: 'app-header',
  standalone: true,
  imports: [
    RouterLink,
    RouterLinkActive,
    TranslateModule,
    LanguageSwitcherComponent,
    AppDropdownComponent,
  ],
  templateUrl: './header.component.html',
  styleUrls: ['./header.component.scss'],
})
export class HeaderComponent {
  @Input() showPrivateNavigation = false;
  @Input() isDarkMode = true;
  @Input() userInitial = '';

  @Output() themeToggled = new EventEmitter<void>();
  @Output() userMenuToggled = new EventEmitter<void>();

  readonly currentUrl = signal('');

  readonly energyOptions: DropdownOption[] = [
    { label: 'Consumo', labelKey: 'nav.consumption', value: 'energy', route: '/energy/consumption', description: 'Consumo actual' },
    { label: 'Historial', labelKey: 'nav.history', value: 'energy-history', route: '/energy/history', description: 'Lecturas anteriores' },
    { label: 'Reportes', labelKey: 'nav.reports', value: 'reports', route: '/energy/reports', aliases: ['/reports'], description: 'Analisis energetico' },
    { label: 'Metas', labelKey: 'nav.energyGoals', value: 'energy-goals', route: '/energy/goals', aliases: ['/reports/energy-goals'], description: 'Objetivos de consumo' },
  ];

  readonly workplaceOptions: DropdownOption[] = [
    { label: 'Sedes', labelKey: 'nav.locations', value: 'locations', route: '/spaces/sites', aliases: ['/workplace/locations'], description: 'Centros operativos' },
    { label: 'Habitaciones', labelKey: 'nav.rooms', value: 'rooms', route: '/spaces/rooms', aliases: ['/workplace/rooms'], description: 'Espacios internos' },
    { label: 'Asignaciones', labelKey: 'nav.assignments', value: 'assignments', route: '/spaces/assignments', aliases: ['/workplace/device-assignments'], description: 'Dispositivos por habitacion' },
  ];

  readonly operationOptions: DropdownOption[] = [
    { label: 'Dispositivos', labelKey: 'nav.devices', value: 'devices', route: '/operation/devices', aliases: ['/devices'], description: 'Control de equipos' },
    { label: 'Grupos', labelKey: 'nav.deviceGroups', value: 'device-groups', route: '/operation/groups', aliases: ['/device-groups'], description: 'Agrupacion de dispositivos' },
    { label: 'Rutinas', labelKey: 'nav.routines', value: 'routines', route: '/operation/routines', aliases: ['/routines'], description: 'Automatizacion' },
    { label: 'Modos', labelKey: 'nav.operationModes', value: 'operation-modes', route: '/operation/modes', aliases: ['/operation-modes'], description: 'Planes operativos' },
  ];

  readonly alertsOptions: DropdownOption[] = [
    { label: 'Bandeja', labelKey: 'nav.inbox', value: 'alerts', route: '/alerts/inbox', aliases: ['/alerts'], description: 'Eventos detectados' },
    { label: 'Reglas', labelKey: 'nav.rules', value: 'alert-rules', route: '/alerts/rules', description: 'Umbrales automaticos' },
    { label: 'Preferencias', labelKey: 'nav.preferences', value: 'alert-preferences', route: '/alerts/preferences', aliases: ['/notifications/preferences', '/settings/notifications'], description: 'Sensibilidad y ruido' },
  ];

  readonly supportOptions: DropdownOption[] = [
    { label: 'Soporte', labelKey: 'nav.supportTickets', value: 'support-tickets', route: '/service/support', aliases: ['/support-tickets'], description: 'Atencion al usuario' },
    { label: 'Mantenimiento', labelKey: 'nav.maintenanceTickets', value: 'maintenance-tickets', route: '/service/maintenance', aliases: ['/maintenance-tickets'], description: 'Revision tecnica' },
  ];

  constructor(private readonly router: Router) {
    this.currentUrl.set(this.router.url.split('?')[0]);

    this.router.events
      .pipe(filter((event): event is NavigationEnd => event instanceof NavigationEnd))
      .subscribe((event) => this.currentUrl.set(event.urlAfterRedirects.split('?')[0]));
  }

  activeValue(options: DropdownOption[]): string | null {
    const url = this.currentUrl();
    const match = options
      .map((option) => ({
        option,
        matchedLength: this.matchedRouteLength(url, option),
      }))
      .filter((match) => match.matchedLength >= 0)
      .sort((left, right) => right.matchedLength - left.matchedLength)[0];

    return match?.option.value ?? null;
  }

  toggleTheme(): void {
    this.themeToggled.emit();
  }

  toggleUserMenu(): void {
    this.userMenuToggled.emit();
  }

  private matchedRouteLength(url: string, option: DropdownOption): number {
    const routes = [option.route, ...(option.aliases ?? [])].filter(
      (route): route is string => Boolean(route)
    );

    return routes.reduce((bestMatch, route) => {
      const matches = url === route || url.startsWith(`${route}/`);

      return matches ? Math.max(bestMatch, route.length) : bestMatch;
    }, -1);
  }
}


============================================================
FILE: src\app\shared\presentation\components\language-switcher\language-switcher.component.html
============================================================

<div class="language-switcher" aria-label="Language selector">
  @for (language of languages; track language.code) {
    <button
      type="button"
      [class.active]="uiPreferences.currentLanguage() === language.code"
      (click)="changeLanguage(language.code)">
      {{ language.label }}
    </button>
  }
</div>

============================================================
FILE: src\app\shared\presentation\components\language-switcher\language-switcher.component.scss
============================================================

.language-switcher {
  display: inline-flex;
  align-items: center;
  padding: 4px;
  border: 1px solid var(--border-color);
  border-radius: 999px;
  background: var(--ec-surface-subtle);
  overflow: hidden;
}

.language-switcher button {
  min-width: 46px;
  height: 34px;
  border: 0;
  border-radius: 0;
  background: transparent;
  color: var(--text-secondary);
  font: inherit;
  font-size: 0.85rem;
  font-weight: 900;
  cursor: pointer;
  transition:
    background 0.2s ease,
    color 0.2s ease;
}

.language-switcher button + button {
  border-left: 1px solid var(--border-color);
}

.language-switcher button:first-child {
  border-top-left-radius: 999px;
  border-bottom-left-radius: 999px;
}

.language-switcher button:last-child {
  border-top-right-radius: 999px;
  border-bottom-right-radius: 999px;
}

.language-switcher button:hover {
  color: var(--text-primary);
}

.language-switcher button.active {
  background: var(--surface-color);
  color: var(--text-primary);
}


============================================================
FILE: src\app\shared\presentation\components\language-switcher\language-switcher.component.ts
============================================================

import { TranslateModule } from '@ngx-translate/core';
import { Component } from '@angular/core';

import { UiPreferencesService } from '../../../application/services/ui-preferences.service';

type LanguageOption = {
  code: 'es' | 'en' | 'pt';
  label: string;
};

@Component({
  selector: 'app-language-switcher',
  standalone: true,
  imports: [
    TranslateModule,],
  templateUrl: './language-switcher.component.html',
  styleUrls: ['./language-switcher.component.scss'],
})
export class LanguageSwitcherComponent {
  readonly languages: LanguageOption[] = [
    { code: 'es', label: 'ES' },
    { code: 'en', label: 'EN' },
    { code: 'pt', label: 'PT' },
  ];

  constructor(readonly uiPreferences: UiPreferencesService) {}

  changeLanguage(language: LanguageOption['code']): void {
    this.uiPreferences.setLanguage(language);
  }
}

============================================================
FILE: src\app\shared\presentation\components\loading-spinner\loading-spinner.component.html
============================================================

<div class="loading-spinner" aria-label="Loading">
  <span></span>
</div>

============================================================
FILE: src\app\shared\presentation\components\loading-spinner\loading-spinner.component.scss
============================================================

.loading-spinner {
  display: inline-grid;
  place-items: center;
  width: 36px;
  height: 36px;
}

.loading-spinner span {
  width: 28px;
  height: 28px;
  border: 3px solid var(--ec-accent-soft);
  border-top-color: var(--accent-color);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}


============================================================
FILE: src\app\shared\presentation\components\loading-spinner\loading-spinner.component.ts
============================================================

import { TranslateModule } from '@ngx-translate/core';
import { Component } from '@angular/core';

@Component({
  selector: 'app-loading-spinner',
  standalone: true,
  imports: [TranslateModule],
  templateUrl: './loading-spinner.component.html',
  styleUrls: ['./loading-spinner.component.scss'],
})
export class LoadingSpinnerComponent {}

============================================================
FILE: src\app\shared\presentation\components\modal-form-shell\modal-form-shell.component.html
============================================================

@if (open) {
  <section class="modal-shell" [class.closing]="closing" role="dialog" aria-modal="true">
    <button
      type="button"
      class="modal-shell__backdrop"
      [attr.aria-label]="'shared.modal.closeForm' | translate"
      (click)="close()"
    ></button>

    <article class="modal-shell__panel" [class.closing]="closing">
      <header class="modal-shell__header">
        <div>
          @if (eyebrow) {
            <p>{{ eyebrow }}</p>
          }
          <h2>{{ title }}</h2>
          @if (description) {
            <span>{{ description }}</span>
          }
        </div>

        <button type="button" class="modal-shell__close" (click)="close()">
          &times;
        </button>
      </header>

      <div class="modal-shell__body">
        <ng-content />
      </div>

      <footer class="modal-shell__actions">
        <ng-content select="[modal-actions]" />
      </footer>
    </article>
  </section>
}


============================================================
FILE: src\app\shared\presentation\components\modal-form-shell\modal-form-shell.component.scss
============================================================

:host {
  position: fixed;
  inset: 0;
  z-index: 980;
  display: block;
  pointer-events: none;
}

.modal-shell {
  position: fixed;
  inset: 0;
  display: grid;
  place-items: center;
  min-height: 100dvh;
  padding: clamp(18px, 4vw, 40px);
  pointer-events: auto;
  animation: modalShellEnter 0.22s ease both;
}

.modal-shell.closing {
  animation: modalShellExit 0.18s ease both;
}

.modal-shell__backdrop {
  position: absolute;
  inset: 0;
  border: 0;
  background:
    var(--ec-overlay-glow),
    var(--ec-overlay-bg);
  backdrop-filter: blur(12px);
  cursor: pointer;
}

.modal-shell__panel {
  position: relative;
  z-index: 1;
  width: min(680px, calc(100vw - 36px));
  max-height: min(86dvh, 760px);
  display: grid;
  grid-template-rows: auto minmax(0, 1fr) auto;
  overflow: hidden;
  border: 1px solid var(--border-strong);
  border-radius: 20px;
  color: var(--text-primary);
  background: var(--ec-modal-bg);
  box-shadow:
    var(--ec-shadow-modal),
    0 0 0 1px rgba(255, 255, 255, 0.02) inset;
  animation: modalPanelEnter 0.28s cubic-bezier(0.16, 1, 0.3, 1) both;
}

.modal-shell__panel.closing {
  animation: modalPanelExit 0.18s ease both;
}

.modal-shell__header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 16px;
  padding: 22px 22px 18px;
  border-bottom: 1px solid var(--border-color);
}

.modal-shell__header div {
  min-width: 0;
  display: grid;
  gap: 8px;
}

.modal-shell__header p,
.modal-shell__header h2,
.modal-shell__header span {
  margin: 0;
}

.modal-shell__header p {
  width: fit-content;
  padding: 7px 10px;
  border-radius: 999px;
  color: var(--accent-color);
  background: var(--ec-accent-soft);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.06em;
  line-height: 1;
  text-transform: uppercase;
}

.modal-shell__header h2 {
  color: var(--text-primary);
  font-size: clamp(1.16rem, 3vw, 1.55rem);
  line-height: 1.14;
}

.modal-shell__header span {
  max-width: 62ch;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.modal-shell__close {
  flex: 0 0 auto;
  width: 36px;
  height: 36px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border: 1px solid var(--border-color);
  border-radius: 12px;
  padding: 0;
  color: var(--text-primary);
  background: var(--ec-ghost-bg);
  cursor: pointer;
  font-family: Arial, sans-serif;
  font-size: 1.05rem;
  font-weight: 950;
  line-height: 1;
}

.modal-shell__close:hover {
  border-color: var(--border-strong);
  background: var(--ec-ghost-bg-hover);
}

.modal-shell__body {
  min-height: 0;
  overflow: auto;
  padding: 22px;
}

.modal-shell__actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  padding: 16px 22px 22px;
  border-top: 1px solid var(--border-color);
}

.modal-shell__actions:empty {
  display: none;
}

@keyframes modalShellEnter {
  from {
    opacity: 0;
  }

  to {
    opacity: 1;
  }
}

@keyframes modalShellExit {
  from {
    opacity: 1;
  }

  to {
    opacity: 0;
  }
}

@keyframes modalPanelEnter {
  from {
    opacity: 0;
    transform: translateY(18px) scale(0.97);
    filter: blur(4px);
  }

  to {
    opacity: 1;
    transform: translateY(0) scale(1);
    filter: blur(0);
  }
}

@keyframes modalPanelExit {
  from {
    opacity: 1;
    transform: translateY(0) scale(1);
    filter: blur(0);
  }

  to {
    opacity: 0;
    transform: translateY(10px) scale(0.98);
    filter: blur(3px);
  }
}

@media (max-width: 560px) {
  .modal-shell__header,
  .modal-shell__body,
  .modal-shell__actions {
    padding-right: 18px;
    padding-left: 18px;
  }

  .modal-shell__actions {
    flex-direction: column-reverse;
  }
}


============================================================
FILE: src\app\shared\presentation\components\modal-form-shell\modal-form-shell.component.ts
============================================================

import { TranslateModule } from '@ngx-translate/core';
import { Component, EventEmitter, Input, OnDestroy, Output } from '@angular/core';

@Component({
  selector: 'app-modal-form-shell',
  standalone: true,
  imports: [TranslateModule],
  templateUrl: './modal-form-shell.component.html',
  styleUrls: ['./modal-form-shell.component.scss'],
})
export class ModalFormShellComponent implements OnDestroy {
  @Input() eyebrow = '';
  @Input() title = '';
  @Input() description = '';

  @Output() closed = new EventEmitter<void>();

  closing = false;

  private modalOpen = false;
  private closeTimer: ReturnType<typeof setTimeout> | null = null;

  @Input()
  set open(value: boolean) {
    this.modalOpen = value;

    if (value) {
      this.clearCloseTimer();
      this.closing = false;
    }
  }

  get open(): boolean {
    return this.modalOpen || this.closing;
  }

  ngOnDestroy(): void {
    this.clearCloseTimer();
  }

  close(): void {
    if (this.closing) {
      return;
    }

    this.closing = true;
    this.clearCloseTimer();
    this.closeTimer = setTimeout(() => {
      this.closed.emit();
      this.closing = false;
      this.closeTimer = null;
    }, 180);
  }

  private clearCloseTimer(): void {
    if (this.closeTimer) {
      clearTimeout(this.closeTimer);
      this.closeTimer = null;
    }
  }
}


============================================================
FILE: src\app\shared\presentation\components\section-card\section-card.component.html
============================================================

<article [class]="className">
  @if (eyebrow) {
    <span>{{ eyebrow }}</span>
  }
  @if (title) {
    <h3>{{ title }}</h3>
  }
  @if (description) {
    <p>{{ description }}</p>
  }
  <ng-content />
</article>


============================================================
FILE: src\app\shared\presentation\components\section-card\section-card.component.scss
============================================================

.section-card {
  position: relative;
  overflow: hidden;
  display: grid;
  gap: 12px;
  min-width: 0;
  padding: var(--section-card-padding, 18px);
  border: 1px solid var(--border-color);
  border-radius: var(--section-card-radius, 18px);
  background:
    var(--ec-card-highlight),
    var(--ec-section-bg);
  box-shadow:
    var(--ec-inset-highlight),
    var(--ec-shadow-soft);
}

.section-card::before {
  content: '';
  position: absolute;
  top: 0;
  right: 0;
  width: 30%;
  height: 3px;
  background: linear-gradient(90deg, transparent, var(--accent-color));
  opacity: 0.62;
  pointer-events: none;
}

.section-card--large {
  --section-card-padding: clamp(22px, 4vw, 30px);
  --section-card-radius: 20px;
  gap: 16px;
}

.section-card--medium {
  --section-card-padding: 20px;
}

.section-card--small {
  --section-card-padding: 16px;
  --section-card-radius: 16px;
  gap: 9px;
}

.section-card--description {
  --section-card-padding: 14px 16px;
  --section-card-radius: 14px;
  gap: 7px;
  background: var(--surface-card);
  box-shadow: var(--ec-inset-highlight);
}

.section-card span {
  width: fit-content;
  color: var(--accent-color);
  font-size: 0.74rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  line-height: 1;
  text-transform: uppercase;
}

.section-card h3 {
  margin: 0;
  color: var(--text-primary);
  font-size: var(--section-card-title-size, 1.08rem);
  font-weight: 950;
  line-height: 1.16;
}

.section-card--large h3 {
  --section-card-title-size: clamp(1.25rem, 3vw, 1.72rem);
}

.section-card--small h3,
.section-card--description h3 {
  --section-card-title-size: 0.98rem;
}

.section-card p {
  max-width: 68ch;
  margin: 0;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}


============================================================
FILE: src\app\shared\presentation\components\section-card\section-card.component.ts
============================================================

import { Component, Input } from '@angular/core';

export type SectionCardSize = 'large' | 'medium' | 'small' | 'description';

@Component({
  selector: 'app-section-card',
  standalone: true,
  templateUrl: './section-card.component.html',
  styleUrls: ['./section-card.component.scss'],
})
export class SectionCardComponent {
  @Input() eyebrow = '';
  @Input() title = '';
  @Input() description = '';
  @Input() size: SectionCardSize = 'medium';

  get className(): string {
    return `section-card section-card--${this.size}`;
  }
}


============================================================
FILE: src\app\shared\presentation\components\settings-section\settings-section.component.html
============================================================

<section class="settings-section" [class.settings-section--stacked]="stackedHeader">
  <header class="settings-section__heading">
    @if (eyebrow) {
      <span>{{ eyebrow }}</span>
    }

    <h2>{{ title }}</h2>

    @if (description) {
      <p>{{ description }}</p>
    }
  </header>

  <div class="settings-section__content">
    <ng-content />
  </div>
</section>


============================================================
FILE: src\app\shared\presentation\components\settings-section\settings-section.component.scss
============================================================

.settings-section {
  display: grid;
  gap: 24px;
  padding: 28px;
  border: 1px solid var(--border-color);
  border-radius: 30px;
  background:
    radial-gradient(circle at top left, var(--ec-accent-soft), transparent 42%),
    var(--surface-card);
  box-shadow:
    var(--shadow-soft),
    var(--ec-inset-highlight);
}

.settings-section__heading {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(280px, 520px);
  gap: 24px;
  align-items: end;
}

.settings-section__heading span {
  grid-column: 1;
  color: var(--accent-color);
  font-size: 0.76rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.settings-section__heading h2 {
  grid-column: 1;
  margin: 8px 0 0;
  color: var(--text-primary);
  font-size: 1.45rem;
  font-weight: 950;
  line-height: 1.15;
}

.settings-section__heading p {
  grid-column: 2;
  grid-row: 1 / span 2;
  align-self: end;
  margin: 0;
  color: var(--text-secondary);
  line-height: 1.55;
  text-align: right;
}

.settings-section--stacked {
  gap: 20px;
}

.settings-section--stacked .settings-section__heading {
  grid-template-columns: 1fr;
  gap: 12px;
  align-items: stretch;
}

.settings-section--stacked .settings-section__heading span,
.settings-section--stacked .settings-section__heading h2,
.settings-section--stacked .settings-section__heading p {
  grid-column: 1;
  grid-row: auto;
}

.settings-section--stacked .settings-section__heading h2 {
  padding-bottom: 14px;
  border-bottom: 1px solid var(--border-color);
}

.settings-section--stacked .settings-section__heading p {
  max-width: 720px;
  text-align: left;
}

.settings-section--stacked .settings-section__content {
  padding-top: 20px;
  border-top: 1px solid var(--border-color);
}

@media (max-width: 1180px) {
  .settings-section__heading {
    grid-template-columns: 1fr;
  }

  .settings-section__heading p {
    grid-column: 1;
    grid-row: auto;
    text-align: left;
  }
}

@media (max-width: 640px) {
  .settings-section {
    padding: 22px;
    border-radius: 24px;
  }
}


============================================================
FILE: src\app\shared\presentation\components\settings-section\settings-section.component.ts
============================================================

import { Component, Input } from '@angular/core';

@Component({
  selector: 'app-settings-section',
  standalone: true,
  templateUrl: './settings-section.component.html',
  styleUrls: ['./settings-section.component.scss'],
})
export class SettingsSectionComponent {
  @Input() eyebrow = '';
  @Input() title = '';
  @Input() description = '';
  @Input() stackedHeader = false;
}


============================================================
FILE: src\app\shared\presentation\components\settings-shell\settings-shell.component.html
============================================================

<section class="settings-shell">
  <aside class="settings-sidebar" [attr.aria-label]="'settings.shell.ariaLabel' | translate">
    <div class="settings-sidebar__brand">
      <span>{{ 'settings.shell.eyebrow' | translate }}</span>
      <strong>{{ 'app.name' | translate }}</strong>
      <small>{{ 'settings.shell.description' | translate }}</small>
    </div>

    <nav class="settings-nav">
      @for (item of navItems; track item.id) {
        <a
          [routerLink]="item.link"
          routerLinkActive="is-active"
          [routerLinkActiveOptions]="{ exact: true }"
          ariaCurrentWhenActive="page"
        >
          <span class="settings-nav__marker" aria-hidden="true"></span>
          <span class="settings-nav__content">
            <strong class="settings-nav__title">{{ item.labelKey | translate }}</strong>
            <span class="settings-nav__description">{{ item.descriptionKey | translate }}</span>
          </span>
        </a>
      }
    </nav>
  </aside>

  <main class="settings-content">
    <ng-content />
    <router-outlet />
  </main>
</section>


============================================================
FILE: src\app\shared\presentation\components\settings-shell\settings-shell.component.scss
============================================================

:host {
  display: block;
}

.settings-shell {
  display: grid;
  grid-template-columns: minmax(220px, 280px) minmax(0, 1fr);
  gap: 26px;
  align-items: start;
}

.settings-sidebar {
  position: sticky;
  top: 24px;
  display: grid;
  gap: 18px;
}

.settings-sidebar__brand,
.settings-nav {
  border: 1px solid var(--border-color);
  background: var(--surface-card);
  box-shadow: var(--shadow-soft);
}

.settings-sidebar__brand {
  display: grid;
  gap: 8px;
  padding: 20px;
  border-radius: 22px;
}

.settings-sidebar__brand span {
  width: fit-content;
  padding: 6px 10px;
  border-radius: 999px;
  color: var(--accent-color);
  background: var(--surface-color);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.settings-sidebar__brand strong {
  color: var(--text-primary);
  font-size: 1.25rem;
  line-height: 1.15;
}

.settings-sidebar__brand small {
  color: var(--text-secondary);
  font-weight: 700;
  line-height: 1.45;
}

.settings-nav {
  display: grid;
  gap: 8px;
  padding: 8px;
  border-radius: 22px;
}

.settings-nav a {
  --marker-width: 0px;
  --content-left: 14px;

  position: relative;
  display: block;
  height: 58px;
  padding: 0;
  overflow: hidden;
  border: 1px solid color-mix(in srgb, var(--border-strong) 58%, transparent);
  border-radius: 16px;
  color: var(--text-primary);
  background: var(--ec-settings-nav-item-bg);
  box-shadow: var(--ec-inset-highlight);
  text-decoration: none;
  transition:
    border-color 0.18s ease,
    box-shadow 0.18s ease,
    background 0.18s ease,
    color 0.18s ease,
    transform 0.18s ease;
}

.settings-nav a:not(.is-active) {
  --marker-width: 42px;
  --content-left: 52px;
}

.settings-nav__marker {
  position: absolute;
  inset: 0 auto 0 0;
  display: block;
  width: var(--marker-width);
  min-width: 0;
  overflow: hidden;
  pointer-events: none;
  border-radius: 15px 0 0 15px;
  background: var(--ec-settings-nav-marker-bg);
  box-shadow:
    var(--ec-inset-highlight),
    inset -1px 0 0 color-mix(in srgb, var(--accent-color) 24%, transparent);
  transition: width 0.24s cubic-bezier(0.22, 1, 0.36, 1);
}

.settings-nav__content {
  position: absolute;
  top: 0;
  right: 14px;
  bottom: 0;
  left: var(--content-left);
  display: grid;
  gap: 4px;
  align-content: center;
  justify-items: start;
  min-width: 0;
  text-align: left;
  transition: left 0.24s cubic-bezier(0.22, 1, 0.36, 1);
}

.settings-nav a:hover {
  color: var(--text-primary);
  border-color: var(--border-strong);
  background: var(--ec-settings-nav-item-hover-bg);
  transform: translateX(2px);
}

.settings-nav a:not(.is-active):hover {
  --marker-width: 0px;
  --content-left: 14px;

  border-color: color-mix(in srgb, var(--border-strong) 82%, var(--accent-color));
  background: var(--ec-settings-nav-active-bg);
  box-shadow:
    var(--ec-inset-highlight),
    0 0 0 1px var(--ec-accent-soft);
}

.settings-nav a.is-active {
  border-color: color-mix(in srgb, var(--border-strong) 82%, var(--accent-color));
  color: var(--text-primary);
  background: var(--ec-settings-nav-active-bg);
  box-shadow:
    var(--ec-inset-highlight),
    0 0 0 1px var(--ec-accent-soft);
}

.settings-nav__title {
  font-size: 0.95rem;
  font-weight: 950;
}

.settings-nav__description {
  font-size: 0.78rem;
  font-weight: 750;
  line-height: 1.35;
}

.settings-content {
  min-width: 0;
}

@media (max-width: 980px) {
  .settings-shell {
    grid-template-columns: 1fr;
  }

  .settings-sidebar {
    position: static;
  }

  .settings-nav {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }
}

@media (max-width: 680px) {
  .settings-nav {
    grid-template-columns: 1fr;
  }
}


============================================================
FILE: src\app\shared\presentation\components\settings-shell\settings-shell.component.ts
============================================================

import { TranslateModule } from '@ngx-translate/core';
import { Component } from '@angular/core';
import { RouterLink, RouterLinkActive, RouterOutlet } from '@angular/router';

import { ROUTE_PATHS } from '../../../infrastructure/constants/route-paths';

type SettingsSectionId =
  | 'profile'
  | 'account'
  | 'security'
  | 'billing'
  | 'platform';

interface SettingsNavItem {
  id: SettingsSectionId;
  labelKey: string;
  descriptionKey: string;
  link: string;
}

@Component({
  selector: 'app-settings-shell',
  standalone: true,
  imports: [
    TranslateModule,
    RouterLink,
    RouterLinkActive,
    RouterOutlet,
  ],
  templateUrl: './settings-shell.component.html',
  styleUrls: ['./settings-shell.component.scss'],
})
export class SettingsShellComponent {
  readonly navItems: SettingsNavItem[] = [
    {
      id: 'profile',
      labelKey: 'settings.nav.profile',
      descriptionKey: 'settings.nav.profileDescription',
      link: ROUTE_PATHS.IAM.PROFILE,
    },
    {
      id: 'account',
      labelKey: 'settings.nav.account',
      descriptionKey: 'settings.nav.accountDescription',
      link: ROUTE_PATHS.IAM.ACCOUNT,
    },
    {
      id: 'security',
      labelKey: 'settings.nav.security',
      descriptionKey: 'settings.nav.securityDescription',
      link: ROUTE_PATHS.IAM.SECURITY,
    },
    {
      id: 'billing',
      labelKey: 'settings.nav.billing',
      descriptionKey: 'settings.nav.billingDescription',
      link: ROUTE_PATHS.BILLING.SETTINGS,
    },
    {
      id: 'platform',
      labelKey: 'settings.nav.platform',
      descriptionKey: 'settings.nav.platformDescription',
      link: ROUTE_PATHS.IAM.PLATFORM,
    },
  ];

}


============================================================
FILE: src\app\shared\presentation\components\setting-toggle\setting-toggle.component.html
============================================================

<button
  type="button"
  class="setting-toggle"
  [class.setting-toggle--active]="checked"
  [disabled]="disabled"
  (click)="toggle()"
>
  <span class="setting-toggle__content">
    <strong>{{ title }}</strong>
    @if (description) {
      <small>{{ description }}</small>
    }
  </span>

  <span class="setting-toggle__switch" aria-hidden="true">
    <span></span>
  </span>
</button>


============================================================
FILE: src\app\shared\presentation\components\setting-toggle\setting-toggle.component.scss
============================================================

:host {
  display: block;
  height: 100%;
}

.setting-toggle {
  width: 100%;
  height: 100%;
  min-height: 78px;
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 14px;
  align-items: center;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  padding: 14px;
  color: var(--text-primary);
  background:
    linear-gradient(135deg, var(--ec-status-danger-bg), transparent 46%),
    var(--ec-surface-strong);
  cursor: pointer;
  text-align: left;
  transition:
    border-color 0.18s ease,
    background 0.18s ease,
    opacity 0.18s ease;
}

.setting-toggle:hover:not(:disabled) {
  border-color: var(--ec-status-danger-border);
  background:
    linear-gradient(135deg, var(--ec-status-danger-bg), transparent 46%),
    var(--ec-surface-hover);
}

.setting-toggle:disabled {
  cursor: default;
  opacity: 0.58;
}

.setting-toggle--active {
  border-color: var(--ec-status-success-border);
  background:
    radial-gradient(circle at top left, var(--ec-status-success-bg), transparent 42%),
    var(--ec-surface-muted);
}

.setting-toggle__content {
  display: grid;
  gap: 5px;
  min-width: 0;
}

.setting-toggle strong {
  color: var(--text-primary);
  font-size: 0.95rem;
  font-weight: 950;
  line-height: 1.2;
}

.setting-toggle small {
  color: var(--text-secondary);
  font-size: 0.82rem;
  font-weight: 700;
  line-height: 1.4;
}

.setting-toggle__switch {
  align-self: center;
  width: 48px;
  height: 28px;
  display: flex;
  align-items: center;
  padding: 3px;
  border-radius: 999px;
  background: var(--ec-status-danger-solid);
  transition:
    background 0.18s ease,
    box-shadow 0.18s ease;
}

.setting-toggle__switch span {
  width: 22px;
  height: 22px;
  border-radius: 50%;
  background: var(--ec-control-thumb-bg);
  box-shadow: var(--ec-control-thumb-shadow);
  transition: transform 0.18s ease;
}

.setting-toggle--active .setting-toggle__switch {
  background: var(--ec-status-success-solid);
  box-shadow: 0 0 0 4px var(--ec-status-success-bg);
}

.setting-toggle--active .setting-toggle__switch span {
  transform: translateX(20px);
}


============================================================
FILE: src\app\shared\presentation\components\setting-toggle\setting-toggle.component.ts
============================================================

import { Component, EventEmitter, Input, Output } from '@angular/core';

@Component({
  selector: 'app-setting-toggle',
  standalone: true,
  templateUrl: './setting-toggle.component.html',
  styleUrls: ['./setting-toggle.component.scss'],
})
export class SettingToggleComponent {
  @Input() title = '';
  @Input() description = '';
  @Input() checked = false;
  @Input() disabled = false;

  @Output() checkedChange = new EventEmitter<boolean>();

  toggle(): void {
    if (this.disabled) {
      return;
    }

    this.checkedChange.emit(!this.checked);
  }
}


============================================================
FILE: src\app\shared\presentation\components\severity-badge\severity-badge.component.html
============================================================

<span class="severity-badge" [class]="'severity-badge severity-badge--' + normalizedLevel.toLowerCase()">
  {{ label }}
</span>


============================================================
FILE: src\app\shared\presentation\components\severity-badge\severity-badge.component.scss
============================================================

.severity-badge {
  width: fit-content;
  min-height: 28px;
  display: inline-flex;
  align-items: center;
  border: 1px solid transparent;
  border-radius: 999px;
  padding: 0 10px;
  font-size: 0.72rem;
  font-weight: 950;
}

.severity-badge--stable {
  border-color: var(--ec-status-stable-border);
  color: var(--ec-status-stable-text);
  background: var(--ec-status-stable-bg);
}

.severity-badge--success {
  border-color: var(--ec-status-success-border);
  color: var(--ec-status-success-text);
  background: var(--ec-status-success-bg);
}

.severity-badge--info {
  border-color: var(--ec-status-info-border);
  color: var(--ec-status-info-text);
  background: var(--ec-status-info-bg);
}

.severity-badge--warning {
  border-color: var(--ec-status-warning-border);
  color: var(--ec-status-warning-text);
  background: var(--ec-status-warning-bg);
}

.severity-badge--critical {
  border-color: var(--ec-status-danger-border);
  color: var(--ec-status-danger-text);
  background: var(--ec-status-danger-bg);
}


============================================================
FILE: src\app\shared\presentation\components\severity-badge\severity-badge.component.ts
============================================================

import { Component, Input } from '@angular/core';

export type SeverityBadgeLevel =
  | 'STABLE'
  | 'SUCCESS'
  | 'INFO'
  | 'WARNING'
  | 'CRITICAL';

@Component({
  selector: 'app-severity-badge',
  standalone: true,
  templateUrl: './severity-badge.component.html',
  styleUrls: ['./severity-badge.component.scss'],
})
export class SeverityBadgeComponent {
  @Input() level: SeverityBadgeLevel | string = 'INFO';

  get normalizedLevel(): SeverityBadgeLevel {
    const level = String(this.level || 'INFO').toUpperCase();

    if (['STABLE', 'SUCCESS', 'INFO', 'WARNING', 'CRITICAL'].includes(level)) {
      return level as SeverityBadgeLevel;
    }

    return 'INFO';
  }

  get label(): string {
    const labels: Record<SeverityBadgeLevel, string> = {
      STABLE: 'Estable',
      SUCCESS: 'Exito',
      INFO: 'Info',
      WARNING: 'Atencion',
      CRITICAL: 'Critico',
    };

    return labels[this.normalizedLevel];
  }
}


============================================================
FILE: src\app\shared\presentation\components\toast-container\toast-container.component.html
============================================================

<section class="toast-stack" aria-live="polite" aria-label="Notificaciones del sistema">
  @for (toast of toastService.toasts(); track toast.id) {
    <div class="toast-slot" [class.toast-slot--leaving]="toast.leaving">
      <article
        class="toast"
        [class]="'toast toast--' + toast.type"
        [class.toast--leaving]="toast.leaving"
      >
        <div>
          <span>{{ labelFor(toast.type) }}</span>
          <p>{{ toast.message }}</p>
        </div>

        <button type="button" aria-label="Cerrar notificacion" (click)="toastService.dismiss(toast.id)">
          x
        </button>
      </article>
    </div>
  }
</section>


============================================================
FILE: src\app\shared\presentation\components\toast-container\toast-container.component.scss
============================================================

.toast-stack {
  position: fixed;
  right: 22px;
  bottom: 22px;
  z-index: 120;
  width: min(360px, calc(100vw - 28px));
  display: flex;
  flex-direction: column;
  pointer-events: none;
}

.toast-slot {
  max-height: 140px;
  margin-top: 12px;
  opacity: 1;
  pointer-events: none;
  transition:
    max-height 0.32s cubic-bezier(0.22, 1, 0.36, 1),
    margin-top 0.32s cubic-bezier(0.22, 1, 0.36, 1),
    opacity 0.24s ease;
}

.toast-slot:first-child {
  margin-top: 0;
}

.toast-slot--leaving {
  max-height: 0;
  margin-top: 0;
  opacity: 0;
}

.toast {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 14px;
  align-items: start;
  padding: 14px 14px 14px 16px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  background:
    var(--ec-card-highlight),
    var(--surface-elevated);
  box-shadow:
    var(--ec-shadow-soft),
    var(--ec-inset-highlight);
  pointer-events: auto;
  transform-origin: right center;
  will-change: transform, opacity;
  animation: toast-in 0.3s cubic-bezier(0.22, 1, 0.36, 1) both;
}

.toast--leaving {
  pointer-events: none;
  animation: toast-out-right 0.32s cubic-bezier(0.55, 0, 0.1, 1) both;
}

.toast--success {
  border-color: var(--ec-status-success-border);
  --toast-label-color: var(--ec-status-success-text);
}

.toast--info {
  border-color: var(--ec-status-info-border);
  --toast-label-color: var(--accent-color);
}

.toast--warning {
  border-color: var(--ec-status-warning-border);
  --toast-label-color: var(--ec-status-warning-text);
}

.toast--critical,
.toast--error {
  border-color: var(--ec-status-danger-border);
  --toast-label-color: var(--ec-status-danger-text);
}

.toast span {
  color: var(--toast-label-color, var(--accent-color));
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.toast p {
  margin: 5px 0 0;
  color: var(--text-primary);
  font-weight: 850;
  line-height: 1.4;
}

.toast button {
  width: 28px;
  height: 28px;
  border: 1px solid var(--border-color);
  border-radius: 10px;
  color: var(--text-secondary);
  background: var(--ec-ghost-bg);
  cursor: pointer;
  font-weight: 950;
}

.toast button:hover {
  color: var(--text-primary);
  border-color: var(--border-strong);
  background: var(--ec-ghost-bg-hover);
}

@keyframes toast-in {
  from {
    opacity: 0;
    transform: translateX(32px) translateY(8px) scale(0.98);
  }

  to {
    opacity: 1;
    transform: translateX(0) translateY(0) scale(1);
  }
}

@keyframes toast-out-right {
  from {
    opacity: 1;
    transform: translateX(0) scale(1);
  }

  to {
    opacity: 0;
    transform: translateX(120%) scale(0.96);
  }
}


============================================================
FILE: src\app\shared\presentation\components\toast-container\toast-container.component.ts
============================================================

import { TranslateModule } from '@ngx-translate/core';
import { Component, inject } from '@angular/core';

import { ToastService, ToastType } from '../../../application/services/toast.service';

@Component({
  selector: 'app-toast-container',
  standalone: true,
  imports: [TranslateModule],
  templateUrl: './toast-container.component.html',
  styleUrls: ['./toast-container.component.scss'],
})
export class ToastContainerComponent {
  readonly toastService = inject(ToastService);

  labelFor(type: ToastType): string {
    const labels: Record<ToastType, string> = {
      success: 'Exito',
      info: 'Informacion',
      warning: 'Atencion',
      critical: 'Critico',
      error: 'Error',
    };

    return labels[type];
  }
}


============================================================
FILE: src\app\shared\presentation\components\weight-slider\weight-slider.component.html
============================================================

<label class="weight-slider" [class.weight-slider--off]="!active">
  <span class="weight-slider__header">
    <strong>{{ label }}</strong>
    <small>{{ value }} pts · {{ percent }}%</small>
  </span>

  <input
    type="range"
    [value]="value"
    [min]="min"
    [max]="max"
    [step]="step"
    [disabled]="!active"
    (input)="update($event)"
  />
</label>


============================================================
FILE: src\app\shared\presentation\components\weight-slider\weight-slider.component.scss
============================================================

.weight-slider {
  display: grid;
  gap: 10px;
}

.weight-slider__header {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  color: var(--text-primary);
  font-size: 0.82rem;
  font-weight: 900;
}

.weight-slider__header small {
  color: var(--accent-color);
  font-weight: 950;
  white-space: nowrap;
}

.weight-slider input {
  width: 100%;
  height: 8px;
  appearance: none;
  border-radius: 999px;
  background: var(--ec-slider-track);
  cursor: pointer;
}

.weight-slider input::-webkit-slider-thumb {
  width: 18px;
  height: 18px;
  appearance: none;
  border: 2px solid var(--ec-slider-thumb-border);
  border-radius: 50%;
  background: var(--ec-slider-thumb-bg);
  box-shadow: 0 0 0 4px var(--ec-accent-soft);
}

.weight-slider input::-moz-range-thumb {
  width: 18px;
  height: 18px;
  border: 2px solid var(--ec-slider-thumb-border);
  border-radius: 50%;
  background: var(--ec-slider-thumb-bg);
  box-shadow: 0 0 0 4px var(--ec-accent-soft);
}

.weight-slider--off {
  opacity: 0.58;
}

.weight-slider--off input {
  cursor: default;
  background: var(--ec-disabled-track-bg);
}


============================================================
FILE: src\app\shared\presentation\components\weight-slider\weight-slider.component.ts
============================================================

import { Component, EventEmitter, Input, Output } from '@angular/core';

@Component({
  selector: 'app-weight-slider',
  standalone: true,
  templateUrl: './weight-slider.component.html',
  styleUrls: ['./weight-slider.component.scss'],
})
export class WeightSliderComponent {
  @Input() label = 'Peso';
  @Input() value = 0;
  @Input() percent = 0;
  @Input() active = true;
  @Input() min = 0;
  @Input() max = 30;
  @Input() step = 1;

  @Output() valueChange = new EventEmitter<number>();

  update(event: Event): void {
    const input = event.target as HTMLInputElement;
    this.valueChange.emit(Number(input.value));
  }
}


============================================================
FILE: src\app\shared\presentation\pages\about\about-page.component.html
============================================================

<section class="about-page">
  <header class="about-hero">
    <div class="about-hero__copy">
      <p class="eyebrow">{{ 'about.eyebrow' | translate }}</p>
      <h1>{{ 'about.title' | translate }}</h1>
      <p class="about-hero__subtitle">{{ 'about.subtitle' | translate }}</p>

      <div class="about-actions" aria-label="About navigation">
        <app-button variant="primary" size="lg" link="/home">
          {{ 'about.actions.home' | translate }}
        </app-button>
        <app-button variant="secondary" size="lg" link="/spaces/sites">
          {{ 'about.actions.spaces' | translate }}
        </app-button>
        <app-button variant="ghost" size="lg" link="/energy/consumption">
          {{ 'about.actions.energy' | translate }}
        </app-button>
      </div>
    </div>

    <aside class="about-hero__panel" aria-label="ElectroCorp focus">
      @for (signalKey of heroSignalKeys; track signalKey) {
        <div class="signal-card">
          <span>{{ 'about.heroSignals.' + signalKey + '.label' | translate }}</span>
          <strong>{{ 'about.heroSignals.' + signalKey + '.title' | translate }}</strong>
          <p>{{ 'about.heroSignals.' + signalKey + '.description' | translate }}</p>
        </div>
      }
    </aside>
  </header>

  <app-section-card
    class="about-intro"
    size="large"
    [eyebrow]="'about.what.eyebrow' | translate"
    [title]="'about.what.title' | translate"
    [description]="'about.what.description' | translate"
  >
    <div class="about-intro__grid">
      <span>{{ 'about.what.points.spaces' | translate }}</span>
      <span>{{ 'about.what.points.control' | translate }}</span>
      <span>{{ 'about.what.points.energy' | translate }}</span>
      <span>{{ 'about.what.points.care' | translate }}</span>
    </div>
  </app-section-card>

  <div class="value-grid">
    @for (valueKey of valueKeys; track valueKey) {
      <app-section-card
        size="medium"
        [eyebrow]="'about.values.' + valueKey + '.eyebrow' | translate"
        [title]="'about.values.' + valueKey + '.title' | translate"
        [description]="'about.values.' + valueKey + '.description' | translate"
      />
    }
  </div>

  <section class="about-band">
    <div class="section-heading">
      <p class="eyebrow">{{ 'about.daily.eyebrow' | translate }}</p>
      <h2>{{ 'about.daily.title' | translate }}</h2>
      <p>{{ 'about.daily.description' | translate }}</p>
    </div>

    <div class="daily-grid">
      @for (itemKey of dailyUseKeys; track itemKey) {
        <article class="daily-step">
          <span>{{ $index + 1 }}</span>
          <div>
            <h3>{{ 'about.daily.items.' + itemKey + '.title' | translate }}</h3>
            <p>{{ 'about.daily.items.' + itemKey + '.description' | translate }}</p>
          </div>
        </article>
      }
    </div>
  </section>

  <section class="about-band about-band--split">
    <div class="section-heading">
      <p class="eyebrow">{{ 'about.recommendations.eyebrow' | translate }}</p>
      <h2>{{ 'about.recommendations.title' | translate }}</h2>
      <p>{{ 'about.recommendations.description' | translate }}</p>
    </div>

    <div class="recommendation-list">
      @for (itemKey of recommendationKeys; track itemKey) {
        <article>
          <strong>{{ 'about.recommendations.items.' + itemKey + '.title' | translate }}</strong>
          <p>{{ 'about.recommendations.items.' + itemKey + '.description' | translate }}</p>
        </article>
      }
    </div>
  </section>

  <section class="about-band">
    <div class="section-heading">
      <p class="eyebrow">{{ 'about.capabilities.eyebrow' | translate }}</p>
      <h2>{{ 'about.capabilities.title' | translate }}</h2>
      <p>{{ 'about.capabilities.description' | translate }}</p>
    </div>

    <div class="capability-grid">
      @for (itemKey of capabilityKeys; track itemKey) {
        <app-section-card
          size="small"
          [eyebrow]="'about.capabilities.items.' + itemKey + '.eyebrow' | translate"
          [title]="'about.capabilities.items.' + itemKey + '.title' | translate"
          [description]="'about.capabilities.items.' + itemKey + '.description' | translate"
        />
      }
    </div>
  </section>

  <section class="about-band about-band--careful">
    <div class="section-heading">
      <p class="eyebrow">{{ 'about.careful.eyebrow' | translate }}</p>
      <h2>{{ 'about.careful.title' | translate }}</h2>
      <p>{{ 'about.careful.description' | translate }}</p>
    </div>

    <div class="careful-grid">
      @for (itemKey of carefulUseKeys; track itemKey) {
        <article>
          <strong>{{ 'about.careful.items.' + itemKey + '.title' | translate }}</strong>
          <p>{{ 'about.careful.items.' + itemKey + '.description' | translate }}</p>
        </article>
      }
    </div>
  </section>
</section>


============================================================
FILE: src\app\shared\presentation\pages\about\about-page.component.scss
============================================================

.about-page {
  display: grid;
  gap: 32px;
  padding: 48px 0 72px;
}

.about-hero {
  display: grid;
  grid-template-columns: minmax(0, 1.35fr) minmax(280px, 0.65fr);
  gap: 28px;
  align-items: stretch;
}

.about-hero__copy,
.about-hero__panel,
.about-band {
  border: 1px solid var(--border-color);
  background: var(--surface-card);
  box-shadow: var(--ec-shadow-soft);
}

.about-hero__copy {
  display: grid;
  align-content: center;
  min-height: 420px;
  padding: 42px;
  border-radius: 28px;
}

.eyebrow {
  width: fit-content;
  margin: 0 0 14px;
  padding: 8px 14px;
  border: 1px solid var(--border-color);
  border-radius: 999px;
  color: var(--accent-color);
  background: var(--surface-color);
  font-size: 0.78rem;
  font-weight: 900;
  letter-spacing: 0;
  text-transform: uppercase;
}

h1,
h2,
h3,
p {
  margin: 0;
}

h1 {
  max-width: 860px;
  color: var(--text-color);
  font-size: 3.35rem;
  line-height: 1.04;
}

.about-hero__subtitle {
  max-width: 760px;
  margin-top: 18px;
  color: var(--text-secondary);
  font-size: 1.08rem;
  line-height: 1.75;
}

.about-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  margin-top: 28px;
}

.about-hero__panel {
  display: grid;
  gap: 14px;
  padding: 20px;
  border-radius: 24px;
}

.signal-card,
.daily-step,
.recommendation-list article,
.careful-grid article {
  border: 1px solid var(--border-color);
  background: var(--surface-color);
}

.signal-card {
  display: grid;
  align-content: center;
  gap: 8px;
  min-height: 124px;
  padding: 20px;
  border-radius: 20px;
}

.signal-card span {
  color: var(--accent-color);
  font-size: 0.76rem;
  font-weight: 900;
  letter-spacing: 0;
  text-transform: uppercase;
}

.signal-card strong,
.recommendation-list strong,
.careful-grid strong {
  color: var(--text-color);
  font-size: 1rem;
}

.signal-card p,
.section-heading p,
.daily-step p,
.recommendation-list p,
.careful-grid p {
  color: var(--text-secondary);
  line-height: 1.65;
}

app-section-card.about-intro {
  display: block;
}

.about-intro__grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 12px;
  margin-top: 22px;
}

.about-intro__grid span {
  min-height: 76px;
  padding: 16px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  color: var(--text-color);
  background: var(--surface-color);
  font-weight: 800;
  line-height: 1.45;
}

.value-grid,
.capability-grid,
.careful-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 18px;
}

.about-band {
  display: grid;
  gap: 24px;
  padding: 28px;
  border-radius: 28px;
}

.about-band--split {
  grid-template-columns: minmax(260px, 0.55fr) minmax(0, 1fr);
  align-items: start;
}

.about-band--careful {
  border-color: var(--border-strong);
  background: var(--surface-card);
}

.section-heading {
  display: grid;
  gap: 12px;
}

.section-heading h2 {
  color: var(--text-color);
  font-size: 2rem;
  line-height: 1.12;
}

.section-heading p:not(.eyebrow) {
  max-width: 860px;
  font-size: 1rem;
}

.daily-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14px;
}

.daily-step {
  display: grid;
  grid-template-columns: 42px minmax(0, 1fr);
  gap: 14px;
  min-height: 126px;
  padding: 18px;
  border-radius: 18px;
}

.daily-step span {
  display: grid;
  place-items: center;
  width: 42px;
  height: 42px;
  border-radius: 14px;
  color: var(--text-on-action);
  background: var(--accent-color);
  font-weight: 900;
}

.daily-step h3 {
  margin-bottom: 6px;
  color: var(--text-color);
  font-size: 1rem;
}

.recommendation-list {
  display: grid;
  gap: 12px;
}

.recommendation-list article,
.careful-grid article {
  display: grid;
  gap: 6px;
  min-height: 96px;
  padding: 16px;
  border-radius: 18px;
}

.capability-grid {
  grid-template-columns: repeat(3, minmax(0, 1fr));
}

@media (max-width: 1100px) {
  .about-hero,
  .about-band--split,
  .capability-grid {
    grid-template-columns: 1fr;
  }

  .about-hero__copy {
    min-height: auto;
  }
}

@media (max-width: 780px) {
  .about-page {
    gap: 22px;
    padding: 28px 0 52px;
  }

  .about-hero__copy,
  .about-band {
    padding: 22px;
    border-radius: 22px;
  }

  h1 {
    font-size: 2.35rem;
  }

  .section-heading h2 {
    font-size: 1.55rem;
  }

  .about-intro__grid,
  .value-grid,
  .daily-grid,
  .careful-grid {
    grid-template-columns: 1fr;
  }

  .about-actions {
    flex-direction: column;
  }
}


============================================================
FILE: src\app\shared\presentation\pages\about\about-page.component.ts
============================================================

import { Component } from '@angular/core';
import { TranslateModule } from '@ngx-translate/core';
import { AppButtonComponent } from '../../components/app-button/app-button.component';
import { SectionCardComponent } from '../../components/section-card/section-card.component';

@Component({
  selector: 'app-about-page',
  standalone: true,
  imports: [AppButtonComponent, SectionCardComponent, TranslateModule],
  templateUrl: './about-page.component.html',
  styleUrls: ['./about-page.component.scss'],
})
export class AboutPageComponent {
  readonly heroSignalKeys = ['order', 'control', 'clarity'];
  readonly valueKeys = ['mission', 'vision'];
  readonly dailyUseKeys = [
    'site',
    'rooms',
    'devices',
    'groups',
    'routines',
    'mode',
    'energy',
    'goals',
    'alerts',
    'service',
    'billing',
  ];
  readonly recommendationKeys = [
    'site',
    'rooms',
    'assignments',
    'groups',
    'routines',
    'modes',
    'energy',
    'alerts',
    'rules',
    'names',
  ];
  readonly capabilityKeys = ['spaces', 'control', 'modes', 'energy', 'alerts', 'service', 'settings'];
  readonly carefulUseKeys = ['automation', 'alerts', 'reports', 'billing'];
}


============================================================
FILE: src\app\shared\presentation\pages\home\home-page.component.html
============================================================

<section class="home-dashboard ec-page">
  <header class="ec-page-heading home-heading">
    <p class="ec-page-eyebrow">{{ 'nav.home' | translate }}</p>
    <h1 class="ec-page-title">{{ 'home.operationalCenterTitle' | translate }}</h1>
    <p class="ec-page-description">
      {{ 'home.operationalCenterDescription' | translate }}
    </p>
  </header>

  @if (loading()) {
    <app-section-card class="home-status-card" size="large">
      <p class="home-eyebrow">{{ 'home.loadingEyebrow' | translate }}</p>
      <h2>{{ 'home.loadingTitle' | translate }}</h2>
      <p>{{ 'home.loadingDescription' | translate }}</p>
    </app-section-card>
  } @else {
    <section class="home-top-grid" [attr.aria-label]="'home.mainSummaryAria' | translate">
      <app-section-card class="home-card home-card--site" size="large">
        <div class="home-card__header">
          <div>
            <p class="home-eyebrow">{{ 'home.activeSiteEyebrow' | translate }}</p>
            <h2>{{ activeLocation()?.name ?? ('home.noActiveSite' | translate) }}</h2>
            <p>
              @if (activeLocation()) {
                {{ getLocationSubtitle(activeLocation()!) || ('home.noRegisteredLocation' | translate) }}
              } @else {
                {{ 'home.setupHint' | translate }}
              }
            </p>
          </div>
          <app-button variant="ghost" size="sm" [link]="'/spaces/sites'" [label]="'home.go' | translate" />
        </div>

        @if (workplaceFacade.locations().length === 0) {
            <app-empty-state
            [compact]="true"
            [title]="'home.noSitesTitle' | translate"
            [description]="'home.noSitesDescription' | translate"
            [actionLabel]="'onboarding.createLocationAction' | translate"
            actionVariant="create"
            actionLink="/spaces/sites"
          />
        } @else {
          <div class="site-selector">
            <app-wheel-selector
              [options]="locationWheelOptions()"
              [value]="activeLocationValue()"
              title=""
              (valueChange)="onActiveLocationChange($event)"
            />
          </div>

          <dl class="home-metrics home-metrics--four">
            <div>
              <dt>{{ 'nav.rooms' | translate }}</dt>
              <dd>{{ activeRooms().length }}</dd>
            </div>
            <div>
              <dt>{{ 'nav.devices' | translate }}</dt>
              <dd>{{ activeDevices().length }}</dd>
            </div>
            <div>
              <dt>{{ 'devices.active' | translate }}</dt>
              <dd>{{ activeOnDevices().length }}</dd>
            </div>
            <div>
              <dt>{{ 'home.consumption' | translate }}</dt>
              <dd>{{ formatNumber(currentWatts(), 0) }} W</dd>
            </div>
          </dl>
        }
      </app-section-card>

      <app-section-card class="home-card home-card--status" size="medium">
        <div class="home-card__header">
          <div>
            <p class="home-eyebrow">{{ 'home.generalStatusEyebrow' | translate }}</p>
            <h2>{{ (activeLocation() ? 'home.siteSummaryTitle' : 'home.noOperationalContextTitle') | translate }}</h2>
            <p>
              @if (activeLocation()) {
                {{ 'home.siteSummaryDescription' | translate }}
              } @else {
                {{ 'home.setupEmpty' | translate }}
              }
            </p>
          </div>
        </div>

        @if (!activeLocation()) {
          <app-empty-state
            [compact]="true"
            [title]="'home.missingSiteTitle' | translate"
            [description]="'home.missingSiteDescription' | translate"
            [actionLabel]="'onboarding.createLocationAction' | translate"
            actionVariant="create"
            actionLink="/spaces/sites"
          />
        } @else {
          <div class="status-list">
            <div>
              <span>{{ 'home.criticalAlerts' | translate }}</span>
              <strong>{{ criticalAlerts().length }}</strong>
            </div>
            <div>
              <span>{{ 'reporting.activeGoals' | translate }}</span>
              <strong>{{ activeGoals().length }}</strong>
            </div>
            <div>
              <span>{{ 'home.openSupport' | translate }}</span>
              <strong>{{ openSupportTickets().length }}</strong>
            </div>
          </div>
        }
      </app-section-card>
    </section>

    <section class="home-zone">
      <div class="home-card-grid home-card-grid--operations">
        <app-section-card class="home-card home-card--rooms home-card--action-empty" size="medium">
          <div class="home-card__header">
            <div>
              <p class="home-eyebrow">{{ 'nav.rooms' | translate }}</p>
              <h3>{{ 'home.siteRoomsTitle' | translate }}</h3>
              <p>{{ 'home.roomsRegisteredDescription' | translate: { count: activeRooms().length } }}</p>
            </div>
            <app-button variant="ghost" size="sm" [link]="'/spaces/rooms'" [label]="'home.go' | translate" />
          </div>

          @if (!activeLocation()) {
            <app-empty-state [compact]="true" [title]="'home.noActiveSite' | translate" [description]="'home.noActiveSiteRoomsDescription' | translate" [actionLabel]="'onboarding.createLocationAction' | translate" actionVariant="create" actionLink="/spaces/sites" />
          } @else if (activeRooms().length === 0) {
            <app-empty-state [compact]="true" [title]="'home.noRoomsTitle' | translate" [description]="'home.noRoomsDescription' | translate" [actionLabel]="'workplace.createRoom' | translate" actionVariant="create" actionLink="/spaces/rooms" />
          } @else {
            <dl class="home-metrics home-metrics--four">
              <div><dt>{{ 'devices.total' | translate }}</dt><dd>{{ activeRooms().length }}</dd></div>
              <div><dt>{{ 'home.withDevices' | translate }}</dt><dd>{{ roomsWithDevicesCount() }}</dd></div>
              <div><dt>{{ 'home.withoutDevices' | translate }}</dt><dd>{{ roomsWithoutDevicesCount() }}</dd></div>
              <div><dt>{{ 'home.averageDevicesShort' | translate }}</dt><dd>{{ formatNumber(averageDevicesPerRoom(), 1) }}</dd></div>
            </dl>
          }
        </app-section-card>

        <app-section-card class="home-card home-card--devices home-card--action-empty" size="medium">
          <div class="home-card__header">
            <div>
              <p class="home-eyebrow">{{ 'nav.devices' | translate }}</p>
              <h3>{{ 'home.assignedEquipmentTitle' | translate }}</h3>
              <p>{{ 'home.assignedEquipmentDescription' | translate }}</p>
            </div>
            <app-button variant="ghost" size="sm" [link]="'/operation/devices'" [label]="'home.go' | translate" />
          </div>

          @if (!activeLocation() || activeDevices().length === 0) {
            <app-empty-state [compact]="true" [title]="'home.noAssignedDevicesTitle' | translate" [description]="'home.noAssignedDevicesDescription' | translate" [actionLabel]="'devices.create' | translate" actionVariant="create" actionLink="/operation/devices" />
          } @else {
            <dl class="home-metrics home-metrics--three">
              <div><dt>{{ 'devices.total' | translate }}</dt><dd>{{ activeDevices().length }}</dd></div>
              <div><dt>{{ 'devices.active' | translate }}</dt><dd>{{ activeOnDevices().length }}</dd></div>
              <div><dt>{{ 'history.watts' | translate }}</dt><dd>{{ formatNumber(currentWatts(), 0) }}</dd></div>
            </dl>
          }
        </app-section-card>

        <app-section-card class="home-card home-card--action-empty home-card--compact-action-empty" size="small">
          <div class="home-card__header">
            <div>
              <p class="home-eyebrow">{{ 'nav.deviceGroups' | translate }}</p>
              <h3>{{ 'home.groupSetsTitle' | translate }}</h3>
              <p>{{ 'home.groupsDescription' | translate }}</p>
            </div>
            <app-button variant="ghost" size="sm" [link]="'/operation/groups'" [label]="'home.go' | translate" />
          </div>

          @if (activeGroups().length === 0) {
            <app-empty-state [compact]="true" [title]="'home.noGroupsTitle' | translate" [description]="'home.noGroupsDescription' | translate" [actionLabel]="'deviceGroups.create' | translate" actionVariant="create" actionLink="/operation/groups" />
          } @else {
            <div
              class="home-list-wrap"
              [class.home-list-wrap--has-more]="hiddenGroupCount() > 0"
              [style.--home-list-fade-opacity]="listFadeOpacity(visibleGroups().length)"
            >
              <ul class="home-list">
                @for (group of visibleGroups(); track group.id) {
                  <li><span>{{ group.name }}</span><strong>{{ 'home.deviceCountShort' | translate: { count: group.deviceCount } }}</strong></li>
                }
              </ul>
              @if (hiddenGroupCount() > 0) {
                <p class="home-more-note">{{ 'home.moreGroups' | translate: { count: hiddenGroupCount() } }}</p>
              }
            </div>
          }
        </app-section-card>

        <app-section-card class="home-card home-card--action-empty home-card--compact-action-empty" size="small">
          <div class="home-card__header">
            <div>
              <p class="home-eyebrow">{{ 'nav.routines' | translate }}</p>
              <h3>{{ 'home.scheduledAlarmsTitle' | translate }}</h3>
              <p>{{ 'home.scheduledAlarmsDescription' | translate }}</p>
            </div>
            <app-button variant="ghost" size="sm" [link]="'/operation/routines'" [label]="'home.go' | translate" />
          </div>

          @if (enabledRoutines().length === 0) {
            <app-empty-state [compact]="true" [title]="'home.noActiveRoutinesTitle' | translate" [description]="'home.noActiveRoutinesDescription' | translate" [actionLabel]="'routines.addTitle' | translate" actionVariant="create" actionLink="/operation/routines" />
          } @else {
            <div
              class="home-list-wrap"
              [class.home-list-wrap--has-more]="hiddenRoutineCount() > 0"
              [style.--home-list-fade-opacity]="listFadeOpacity(visibleRoutines().length)"
            >
              <ul class="home-list">
                @for (routine of visibleRoutines(); track routine.id) {
                  <li><span>{{ routine.name }}</span><strong>{{ routine.time }}</strong></li>
                }
              </ul>
              @if (hiddenRoutineCount() > 0) {
                <p class="home-more-note">{{ 'home.moreRoutines' | translate: { count: hiddenRoutineCount() } }}</p>
              }
            </div>
          }
        </app-section-card>

        <app-section-card class="home-card home-card--action-empty home-card--compact-action-empty" size="small">
          <div class="home-card__header">
            <div>
              <p class="home-eyebrow">{{ 'about.capabilities.items.modes.eyebrow' | translate }}</p>
              <h3>{{ 'home.activeModeTitle' | translate }}</h3>
              <p>{{ 'home.activeModeDescription' | translate }}</p>
            </div>
            <app-button variant="ghost" size="sm" [link]="'/operation/modes'" [label]="'home.go' | translate" />
          </div>

          @if (activeMode()) {
            <div class="single-metric">
              <strong>{{ activeMode()?.name }}</strong>
              <span>{{ 'home.affectedDevices' | translate: { count: activeModeAffectedDeviceIds().length } }}</span>
            </div>
          } @else {
            <app-empty-state [compact]="true" [title]="'home.noActiveModeTitle' | translate" [description]="'home.noActiveModeDescription' | translate" [actionLabel]="'home.createMode' | translate" actionVariant="create" actionLink="/operation/modes" />
          }
        </app-section-card>
      </div>
    </section>

    <section class="home-zone">
      <div class="home-card-grid home-card-grid--insight">
        <app-section-card class="home-card home-card--insight-empty" size="medium">
          <div class="home-card__header">
            <div>
              <p class="home-eyebrow">{{ 'home.consumption' | translate }}</p>
              <h3>{{ 'home.energyRecordedTitle' | translate }}</h3>
              <p>{{ 'home.energyRecordedDescription' | translate }}</p>
            </div>
            <app-button variant="ghost" size="sm" [link]="'/energy/consumption'" [label]="'home.go' | translate" />
          </div>

          @if (workplaceReadings().length === 0) {
            <app-empty-state [compact]="true" [title]="'home.noReadingsTitle' | translate" [description]="'home.noReadingsDescription' | translate" />
          } @else {
            <dl class="home-metrics home-metrics--two home-metrics--matrix">
              <div><dt>{{ 'home.today' | translate }}</dt><dd>{{ formatNumber(todayKilowattHours(), 3) }} kWh</dd></div>
              <div><dt>{{ 'home.readings' | translate }}</dt><dd>{{ workplaceReadings().length }}</dd></div>
              <div><dt>{{ 'home.averageConsumption' | translate }}</dt><dd>{{ formatNumber(averageReadingWatts(), 0) }} W</dd></div>
              <div><dt>{{ 'home.peak' | translate }}</dt><dd>{{ formatNumber(peakReadingWatts(), 0) }} W</dd></div>
            </dl>
          }
        </app-section-card>

        <app-section-card class="home-card home-card--insight-empty" size="medium">
          <div class="home-card__header">
            <div>
              <p class="home-eyebrow">{{ 'home.goalsEyebrow' | translate }}</p>
              <h3>{{ 'home.activeGoalsTitle' | translate }}</h3>
              <p>{{ 'home.activeGoalsDescription' | translate }}</p>
            </div>
            <app-button variant="ghost" size="sm" [link]="'/energy/goals'" [label]="'home.go' | translate" />
          </div>

          @if (activeGoals().length === 0) {
            <app-empty-state [compact]="true" [title]="'home.noActiveGoalsTitle' | translate" [description]="'home.noActiveGoalsDescription' | translate" [actionLabel]="'reporting.createGoal' | translate" actionVariant="create" actionLink="/energy/goals" />
          } @else {
            <ul class="home-list">
              @for (goal of activeGoals().slice(0, 3); track goal.id) {
                <li><span>{{ goal.title }}</span><strong>{{ formatNumber(goal.progressPercentage, 1) }}%</strong></li>
              }
            </ul>
          }
        </app-section-card>

        <app-section-card class="home-card" size="medium">
          <div class="home-card__header">
            <div>
              <p class="home-eyebrow">{{ 'nav.alerts' | translate }}</p>
              <h3>{{ 'home.recentInboxTitle' | translate }}</h3>
              <p>{{ 'home.recentInboxDescription' | translate }}</p>
            </div>
            <app-button variant="ghost" size="sm" [link]="'/alerts/inbox'" [label]="'home.go' | translate" />
          </div>

          @if (workplaceAlerts().length === 0) {
            <app-empty-state [compact]="true" [title]="'home.noRecentAlertsTitle' | translate" [description]="'home.noRecentAlertsDescription' | translate" [actionLabel]="'home.openAlerts' | translate" actionLink="/alerts/inbox" />
          } @else {
            <div class="alert-summary">
              <span>{{ 'home.criticalCount' | translate: { count: criticalAlerts().length } }}</span>
              <ul class="home-list home-list--alerts">
                @for (alert of workplaceAlerts().slice(0, 3); track alert.id) {
                  <li><span>{{ alert.title }}</span><strong>{{ alertLevelLabel(alert.level) }}</strong></li>
                }
              </ul>
            </div>
          }
        </app-section-card>
      </div>
    </section>

    <section class="home-zone">
      <app-section-card class="home-card home-card--service" size="description">
          <div class="home-card__header">
            <div>
              <p class="home-eyebrow">{{ 'about.capabilities.items.service.eyebrow' | translate }}</p>
            <h3>{{ 'home.accountSupportTitle' | translate }}</h3>
            <p>{{ 'home.accountSupportDescription' | translate }}</p>
          </div>
          <app-button variant="ghost" size="sm" [link]="'/service/support'" [label]="'home.go' | translate" />
        </div>

        @if (openSupportTickets().length === 0 && pendingMaintenanceForActiveDevices().length === 0) {
          <app-empty-state [compact]="true" [title]="'home.noOpenTicketsTitle' | translate" [description]="'home.noOpenTicketsDescription' | translate" [actionLabel]="'home.createSupport' | translate" actionVariant="create" actionLink="/service/support" />
        } @else {
          <dl class="home-metrics home-metrics--two">
            <div><dt>{{ 'nav.supportTickets' | translate }}</dt><dd>{{ openSupportTickets().length }}</dd></div>
            <div><dt>{{ 'nav.maintenanceTickets' | translate }}</dt><dd>{{ pendingMaintenanceForActiveDevices().length }}</dd></div>
          </dl>
        }
      </app-section-card>
    </section>
  }
</section>



============================================================
FILE: src\app\shared\presentation\pages\home\home-page.component.scss
============================================================

:host {
  display: block;
}

.home-dashboard,
.home-zone {
  display: grid;
  gap: 22px;
}

.home-top-grid,
.home-card-grid {
  display: grid;
  gap: 16px;
  align-items: stretch;
}

.home-top-grid {
  grid-template-columns: minmax(0, 1.45fr) minmax(280px, 0.8fr);
}

.home-card-grid--operations {
  grid-template-columns: repeat(3, minmax(0, 1fr));
  grid-auto-rows: minmax(152px, auto);
}

.home-card-grid--insight {
  grid-template-columns: repeat(3, minmax(0, 1fr));
  grid-auto-rows: minmax(176px, auto);
}

app-section-card.home-card,
app-section-card.home-status-card {
  --section-card-padding: 20px;
  display: block;
  height: 100%;
}

app-section-card.home-card--site {
  --section-card-padding: clamp(22px, 3vw, 28px);
}

app-section-card.home-card ::ng-deep .section-card,
app-section-card.home-status-card ::ng-deep .section-card {
  height: 100%;
  box-sizing: border-box;
}

app-section-card.home-card:not(.home-card--site) ::ng-deep .section-card {
  grid-template-rows: auto minmax(max-content, 1fr);
}

.home-card--rooms,
.home-card--service {
  grid-column: span 2;
}

.home-card__header {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 12px;
  align-items: start;
  height: 116px;
  min-height: 0;
  padding-bottom: 10px;
  overflow: hidden;
  border-bottom: 1px solid var(--border-color);
}

.home-card__header > div {
  min-height: 0;
  overflow: hidden;
}

.home-card--site .home-card__header {
  min-height: 0;
  padding: 0;
  border: 0;
}

.home-card--status .home-card__header {
  height: auto;
  min-height: 128px;
  overflow: visible;
}

.home-card--status .home-card__header > div {
  overflow: visible;
}

.home-card__header h2,
.home-card__header h3,
.home-status-card h2 {
  margin: 0;
  color: var(--text-primary);
  font-weight: 950;
  line-height: 1.12;
}

.home-card__header h2 {
  font-size: clamp(1.45rem, 3vw, 2rem);
}

.home-card__header p:not(.home-eyebrow),
.home-status-card p {
  margin: 0;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.home-card__header p:not(.home-eyebrow) {
  display: -webkit-box;
  overflow: hidden;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

.home-card--site .home-card__header p:not(.home-eyebrow) {
  display: block;
}

.home-card--status .home-card__header p:not(.home-eyebrow) {
  display: block;
  overflow: visible;
}

.home-eyebrow {
  width: fit-content;
  margin: 0 0 8px;
  padding: 7px 10px;
  border-radius: 999px;
  background: var(--ec-accent-soft);
  color: var(--accent-color);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  line-height: 1;
  text-transform: uppercase;
}

.home-card__header app-button {
  justify-self: end;
}

.site-selector ::ng-deep .wheel-selector {
  gap: 0;
  padding: 0;
  border: 0;
  border-radius: 18px;
  background: transparent;
}

.site-selector ::ng-deep .wheel-selector__wrap {
  min-height: 128px;
}

.site-selector ::ng-deep .wheel-selector__wheel {
  height: 120px;
  border-radius: 20px;
}

.site-selector ::ng-deep .wheel-selector__line {
  top: 12px;
  bottom: 12px;
  width: 164px;
  border-radius: 18px;
}

.site-selector ::ng-deep .wheel-selector-option {
  left: calc(50% - 66px);
  top: 25px;
  width: 132px;
  height: 68px;
  border-radius: 16px;
  padding: 10px;
}

.site-selector ::ng-deep .wheel-selector-option strong {
  font-size: 0.88rem;
}

.home-metrics {
  display: grid;
  gap: 10px;
  margin: 0;
}

.home-metrics--four {
  grid-template-columns: repeat(4, minmax(0, 1fr));
}

.home-metrics--three {
  grid-template-columns: repeat(3, minmax(0, 1fr));
}

.home-metrics--two {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}

.home-metrics--matrix div:nth-child(n + 3) {
  border-top: 1px solid var(--border-color);
}

.home-metrics div,
.status-list div,
.single-metric {
  display: grid;
  gap: 5px;
  min-width: 0;
  padding: 12px 0;
}

.home-card--site .home-metrics div,
.status-list div + div {
  border-top: 1px solid var(--border-color);
}

.home-metrics dt,
.status-list span,
.single-metric span {
  color: var(--text-muted);
  font-size: 0.72rem;
  font-weight: 950;
  text-transform: uppercase;
}

.home-metrics dd,
.status-list strong,
.single-metric strong {
  margin: 0;
  color: var(--text-primary);
  font-size: 1.35rem;
  font-weight: 950;
}

.status-list,
.alert-summary {
  display: grid;
  gap: 10px;
}

.home-list-wrap {
  --home-list-fade-opacity: 0.3;
  position: relative;
  display: grid;
  align-content: start;
  gap: 4px;
  min-height: 0;
  height: 100%;
  margin: 0 calc(0px - var(--section-card-padding, 20px)) calc(0px - var(--section-card-padding, 20px));
  padding: 0 var(--section-card-padding, 20px) 16px;
  overflow: visible;
}

.home-list-wrap::after {
  content: '';
  position: absolute;
  z-index: 0;
  right: -8px;
  bottom: -58px;
  left: -8px;
  height: 150px;
  border-radius: 50% 50% 0 0 / 76% 76% 0 0;
  background:
    radial-gradient(ellipse at 50% 100%, rgba(56, 189, 248, var(--home-list-fade-opacity)), transparent 62%),
    radial-gradient(ellipse at 28% 78%, rgba(37, 99, 235, 0.16), transparent 58%),
    radial-gradient(ellipse at 74% 84%, rgba(14, 165, 233, 0.12), transparent 60%);
  filter: blur(18px) saturate(1.2);
  opacity: 0.76;
  pointer-events: none;
}

.home-list-wrap--has-more::after {
  opacity: 0.82;
}

.home-list {
  position: relative;
  z-index: 1;
  display: grid;
  gap: 0;
  margin: 0;
  padding: 0;
  list-style: none;
}

.home-list li {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 12px;
  align-items: center;
  min-height: 29px;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
}

.home-list span {
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.home-list strong,
.alert-summary > span {
  color: var(--text-primary);
  font-weight: 950;
  white-space: nowrap;
}

.home-more-note {
  position: relative;
  z-index: 1;
  margin: 0;
  color: var(--accent-color);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.home-list--alerts {
  gap: 0;
}

.home-list--alerts li {
  padding: 12px 0;
}

.home-list--alerts li + li {
  border-top: 1px solid var(--border-color);
}

@media (max-width: 1120px) {
  .home-top-grid,
  .home-card-grid--operations,
  .home-card-grid--insight {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .home-card--site,
  .home-card--rooms,
  .home-card--service {
    grid-column: span 2;
  }
}

@media (max-width: 720px) {
  .home-top-grid,
  .home-card-grid--operations,
  .home-card-grid--insight,
  .home-metrics--four,
  .home-metrics--three,
  .home-metrics--two,
  .home-card__header {
    grid-template-columns: 1fr;
    height: auto;
    overflow: visible;
  }

  .home-card--site,
  .home-card--rooms,
  .home-card--service {
    grid-column: auto;
  }
}


============================================================
FILE: src\app\shared\presentation\pages\home\home-page.component.ts
============================================================

import { TranslateModule, TranslateService } from '@ngx-translate/core';
import { CommonModule } from '@angular/common';
import { Component, OnInit, computed, inject } from '@angular/core';

import { DeviceControlFacade } from '../../../../device-control/application/services/device-control.facade';
import { Device } from '../../../../device-control/domain/model/device.entity';
import { Routine } from '../../../../device-control/domain/model/routine.entity';
import { EnergyMonitoringFacade } from '../../../../energy-monitoring/application/services/energy-monitoring.facade';
import { NotificationsFacade } from '../../../../notifications/application/services/notifications.facade';
import { Alert } from '../../../../notifications/domain/model/alert.entity';
import { ReportingFacade } from '../../../../reporting/application/services/reporting.facade';
import { EnergyGoal } from '../../../../reporting/domain/model/energy-goal.entity';
import { ServiceManagementFacade } from '../../../../service-management/application/services/service-management.facade';
import { AppButtonComponent } from '../../components/app-button/app-button.component';
import { AppWheelSelectorComponent } from '../../components/app-wheel-selector/app-wheel-selector.component';
import { WheelOption, WheelOptionValue } from '../../components/app-wheel-selector/wheel-option.model';
import { EmptyStateComponent } from '../../components/empty-state/empty-state.component';
import { SectionCardComponent } from '../../components/section-card/section-card.component';
import { ActiveWorkplaceContextService } from '../../../../workplace/application/services/active-workplace-context.service';
import { WorkplaceFacade } from '../../../../workplace/application/services/workplace.facade';
import { DeviceAssignment } from '../../../../workplace/domain/model/device-assignment.entity';
import { Location, LocationType } from '../../../../workplace/domain/model/location.entity';
import { UiPreferencesService } from '../../../application/services/ui-preferences.service';

@Component({
  selector: 'app-home-page',
  standalone: true,
  imports: [
    TranslateModule,
    CommonModule,
    AppButtonComponent,
    AppWheelSelectorComponent,
    EmptyStateComponent,
    SectionCardComponent,
  ],
  templateUrl: './home-page.component.html',
  styleUrls: ['./home-page.component.scss'],
})
export class HomePageComponent implements OnInit {
  readonly workplaceFacade = inject(WorkplaceFacade);
  readonly deviceControlFacade = inject(DeviceControlFacade);
  readonly energyMonitoringFacade = inject(EnergyMonitoringFacade);
  readonly reportingFacade = inject(ReportingFacade);
  readonly notificationsFacade = inject(NotificationsFacade);
  readonly serviceManagementFacade = inject(ServiceManagementFacade);
  readonly activeWorkplaceContext = inject(ActiveWorkplaceContextService);
  readonly uiPreferences = inject(UiPreferencesService);
  private readonly translate = inject(TranslateService);

  readonly activeLocation = this.activeWorkplaceContext.activeLocation;
  readonly activeLocationId = this.activeWorkplaceContext.activeLocationId;
  readonly activeLocationValue = computed(() => {
    const id = this.activeLocationId();
    return id ? String(id) : null;
  });
  readonly locationWheelOptions = computed<WheelOption[]>(() => {
    this.uiPreferences.currentLanguage();

    return this.workplaceFacade.locations().map((location) => ({
      label: location.name,
      value: String(location.id),
      sublabel: this.getLocationSubtitle(location),
    }));
  });

  readonly activeRooms = computed(() => {
    const activeId = this.activeLocationId();
    return activeId
      ? this.workplaceFacade.rooms().filter((room) => room.locationId === activeId)
      : [];
  });

  readonly activeAssignments = computed(() => {
    const activeId = this.activeLocationId();
    return activeId
      ? this.workplaceFacade
          .deviceAssignments()
          .filter((assignment) => assignment.locationId === activeId)
      : [];
  });

  readonly roomsWithDevicesCount = computed(() => {
    const roomIds = new Set(this.activeRooms().map((room) => room.id));
    const occupiedRoomIds = new Set(
      this.activeAssignments()
        .map((assignment) => assignment.roomId)
        .filter((roomId): roomId is number => roomId !== null && roomIds.has(roomId))
    );

    return occupiedRoomIds.size;
  });

  readonly roomsWithoutDevicesCount = computed(() =>
    Math.max(this.activeRooms().length - this.roomsWithDevicesCount(), 0)
  );

  readonly averageDevicesPerRoom = computed(() => {
    const roomIds = new Set(this.activeRooms().map((room) => room.id));
    const assignedToRooms = this.activeAssignments()
      .filter((assignment) => assignment.roomId !== null && roomIds.has(assignment.roomId))
      .length;

    return this.activeRooms().length === 0 ? 0 : assignedToRooms / this.activeRooms().length;
  });

  readonly assignedDeviceIds = computed(
    () => new Set(this.activeAssignments().map((assignment) => assignment.deviceId))
  );

  readonly activeDevices = computed(() => {
    const assignedIds = this.assignedDeviceIds();
    return this.deviceControlFacade
      .devices()
      .filter((device) => assignedIds.has(device.id));
  });

  readonly activeOnDevices = computed(() =>
    this.activeDevices().filter((device) => device.isOn)
  );

  readonly unassignedDevices = computed(() => {
    const allAssignedIds = new Set(
      this.workplaceFacade.deviceAssignments().map((assignment) => assignment.deviceId)
    );

    return this.deviceControlFacade
      .devices()
      .filter((device) => !allAssignedIds.has(device.id));
  });

  readonly activeGroups = computed(() => {
    const assignedIds = this.assignedDeviceIds();
    return this.deviceControlFacade
      .deviceGroups()
      .filter((group) => group.deviceIds.some((deviceId) => assignedIds.has(deviceId)));
  });

  readonly visibleGroups = computed(() => this.activeGroups().slice(0, 3));

  readonly hiddenGroupCount = computed(() =>
    Math.max(this.activeGroups().length - this.visibleGroups().length, 0)
  );

  readonly activeRoutines = computed(() => {
    const assignedIds = this.assignedDeviceIds();
    const roomIds = new Set(this.activeRooms().map((room) => room.id));
    const groupIds = new Set(this.activeGroups().map((group) => group.id));
    const activeId = this.activeLocationId();

    return this.deviceControlFacade
      .routines()
      .filter((routine) =>
        this.routineBelongsToActiveWorkplace(routine, assignedIds, roomIds, groupIds, activeId)
      );
  });

  readonly enabledRoutines = computed(() =>
    this.activeRoutines().filter((routine) => routine.enabled)
  );

  readonly visibleRoutines = computed(() => this.enabledRoutines().slice(0, 3));

  readonly hiddenRoutineCount = computed(() =>
    Math.max(this.enabledRoutines().length - this.visibleRoutines().length, 0)
  );

  readonly activeModes = computed(() => {
    const activeId = this.activeLocationId();
    return activeId
      ? this.deviceControlFacade
          .operationModes()
          .filter((mode) => mode.locationId === activeId)
      : [];
  });

  readonly activeMode = computed(
    () => this.activeModes().find((mode) => mode.status === 'ACTIVE') ?? null
  );

  readonly activeModeAffectedDeviceIds = computed(() => {
    const mode = this.activeMode();
    if (!mode) {
      return [];
    }

    return Array.from(new Set([
      ...mode.deviceIds,
      ...mode.turnOnDeviceIds,
      ...mode.turnOffDeviceIds,
      ...mode.keepOnDeviceIds,
    ]));
  });

  readonly activeGoals = computed(() => {
    const assignedIds = this.assignedDeviceIds();
    const roomIds = new Set(this.activeRooms().map((room) => room.id));
    const groupIds = new Set(this.activeGroups().map((group) => group.id));
    const activeId = this.activeLocationId();

    return this.reportingFacade
      .energyGoals()
      .filter((goal) => goal.isActive)
      .filter((goal) =>
        this.goalBelongsToActiveWorkplace(goal, assignedIds, roomIds, groupIds, activeId)
      );
  });

  readonly workplaceAlerts = computed(() => {
    const assignedIds = this.assignedDeviceIds();
    const roomIds = new Set(this.activeRooms().map((room) => room.id));
    const groupIds = new Set(this.activeGroups().map((group) => group.id));
    const activeId = this.activeLocationId();

    return this.notificationsFacade
      .sortedAlerts()
      .filter((alert) =>
        this.alertBelongsToActiveWorkplace(alert, assignedIds, roomIds, groupIds, activeId)
      )
      .slice(0, 5);
  });

  readonly criticalAlerts = computed(() =>
    this.workplaceAlerts().filter((alert) => alert.isCritical)
  );

  readonly workplaceReadings = computed(() => {
    const assignedIds = this.assignedDeviceIds();
    return this.energyMonitoringFacade
      .readings()
      .filter((reading) => assignedIds.has(reading.deviceId));
  });

  readonly currentWatts = computed(() =>
    this.activeDevices()
      .filter((device) => device.isOn)
      .reduce((total, device) => total + device.powerWatts, 0)
  );

  readonly todayKilowattHours = computed(() => {
    const today = new Date().toISOString().slice(0, 10);
    return this.workplaceReadings()
      .filter((reading) => reading.recordedAt.slice(0, 10) === today)
      .reduce((total, reading) => total + reading.kilowattHours, 0);
  });

  readonly averageReadingWatts = computed(() => {
    const readings = this.workplaceReadings();
    if (readings.length === 0) {
      return 0;
    }

    return readings.reduce((total, reading) => total + reading.watts, 0) / readings.length;
  });

  readonly peakReadingWatts = computed(() =>
    Math.max(0, ...this.workplaceReadings().map((reading) => reading.watts))
  );

  readonly recentReports = computed(() =>
    this.reportingFacade.consumptionReports().slice(0, 3)
  );

  readonly maintenanceForActiveDevices = computed(() => {
    const assignedIds = this.assignedDeviceIds();
    return this.serviceManagementFacade
      .maintenanceTickets()
      .filter((ticket) => assignedIds.has(ticket.deviceId))
      .slice(0, 3);
  });

  readonly openSupportTickets = computed(() =>
    this.serviceManagementFacade
      .supportTickets()
      .filter((ticket) => ticket.isOpen || ticket.isInProgress)
  );

  readonly pendingMaintenanceForActiveDevices = computed(() => {
    const assignedIds = this.assignedDeviceIds();
    return this.serviceManagementFacade
      .maintenanceTickets()
      .filter((ticket) => assignedIds.has(ticket.deviceId))
      .filter((ticket) => ticket.isPending || ticket.isScheduled);
  });

  readonly loading = computed(
    () =>
      this.workplaceFacade.loading() ||
      this.deviceControlFacade.loading() ||
      this.energyMonitoringFacade.loading() ||
      this.reportingFacade.loading() ||
      this.notificationsFacade.loading() ||
      this.serviceManagementFacade.loading()
  );

  async ngOnInit(): Promise<void> {
    await Promise.all([
      this.workplaceFacade.loadWorkplace(),
      this.deviceControlFacade.loadDeviceControl(),
      this.energyMonitoringFacade.loadReadings(),
      this.reportingFacade.loadReporting(),
      this.notificationsFacade.loadNotifications(),
      this.serviceManagementFacade.loadServiceManagement(),
    ]);

    this.activeWorkplaceContext.ensureActiveLocation(this.workplaceFacade.locations());
  }

  onActiveLocationChange(value: WheelOptionValue): void {
    this.activeWorkplaceContext.setActiveLocation(Number(value));
    this.activeWorkplaceContext.ensureActiveLocation(this.workplaceFacade.locations());
  }

  getLocationTypeLabel(location: Location): string {
    const labelKeys: Record<LocationType, string> = {
      HOME: 'home.locationTypes.home',
      BUSINESS: 'home.locationTypes.business',
      BRANCH: 'home.locationTypes.branch',
    };

    return this.translate.instant(labelKeys[location.type]);
  }

  getLocationSubtitle(location: Location): string {
    return [this.getLocationTypeLabel(location), location.address].filter(Boolean).join(' - ');
  }

  getRoomName(assignment: DeviceAssignment): string {
    if (!assignment.roomId) {
      return this.translate.instant('home.noRoom');
    }

    return (
      this.activeRooms().find((room) => room.id === assignment.roomId)?.name ??
      this.translate.instant('home.roomNumber', { id: assignment.roomId })
    );
  }

  getDeviceName(deviceId: number): string {
    return this.deviceControlFacade.getDeviceName(deviceId);
  }

  getDeviceAssignment(device: Device): DeviceAssignment | null {
    return this.activeAssignments().find((assignment) => assignment.deviceId === device.id) ?? null;
  }

  deviceStatusLine(device: Device): string {
    const status = this.translate.instant(device.isOn ? 'home.deviceStatusOn' : 'home.deviceStatusOff');

    return `${status} - ${device.powerWatts} W`;
  }

  alertLevelLabel(level: Alert['level']): string {
    const labelKeys: Record<Alert['level'], string> = {
      SUCCESS: 'home.alertLevels.success',
      STABLE: 'home.alertLevels.stable',
      INFO: 'home.alertLevels.info',
      WARNING: 'home.alertLevels.warning',
      CRITICAL: 'home.alertLevels.critical',
    };

    return this.translate.instant(labelKeys[level]);
  }

  formatNumber(value: number, decimals = 2): string {
    const localeByLanguage = {
      es: 'es-PE',
      en: 'en-US',
      pt: 'pt-BR',
    };

    return Number(value || 0).toLocaleString(localeByLanguage[this.uiPreferences.currentLanguage()], {
      minimumFractionDigits: decimals,
      maximumFractionDigits: decimals,
    });
  }

  listFadeOpacity(visibleCount: number): string {
    return String(Math.max(0.14, 0.58 - Math.min(visibleCount, 3) * 0.14));
  }

  private routineBelongsToActiveWorkplace(
    routine: Routine,
    assignedIds: Set<number>,
    roomIds: Set<number>,
    groupIds: Set<number>,
    activeId: number | null
  ): boolean {
    if (!activeId) {
      return false;
    }

    if (routine.targetType === 'WORKPLACE') {
      return routine.targetId === activeId;
    }

    if (routine.targetType === 'ROOM') {
      return roomIds.has(routine.targetId);
    }

    if (routine.targetType === 'GROUP') {
      return Boolean(routine.groupId && groupIds.has(routine.groupId)) || groupIds.has(routine.targetId);
    }

    return Boolean(routine.deviceId && assignedIds.has(routine.deviceId)) || assignedIds.has(routine.targetId);
  }

  private goalBelongsToActiveWorkplace(
    goal: EnergyGoal,
    assignedIds: Set<number>,
    roomIds: Set<number>,
    groupIds: Set<number>,
    activeId: number | null
  ): boolean {
    if (!activeId) {
      return false;
    }

    if (goal.scopeType === 'GENERAL') {
      return true;
    }

    if (goal.scopeType === 'WORKPLACE') {
      return goal.scopeId === activeId;
    }

    if (goal.scopeType === 'ROOM') {
      return Boolean(goal.scopeId && roomIds.has(goal.scopeId));
    }

    if (goal.scopeType === 'GROUP') {
      return Boolean(goal.scopeId && groupIds.has(goal.scopeId));
    }

    return Boolean(goal.scopeId && assignedIds.has(goal.scopeId));
  }

  private alertBelongsToActiveWorkplace(
    alert: Alert,
    assignedIds: Set<number>,
    roomIds: Set<number>,
    groupIds: Set<number>,
    activeId: number | null
  ): boolean {
    if (!activeId) {
      return false;
    }

    if (alert.sourceType === 'WORKPLACE') {
      return alert.sourceId === String(activeId);
    }

    if (alert.sourceType === 'ROOM') {
      return alert.sourceId !== null && roomIds.has(Number(alert.sourceId));
    }

    if (alert.sourceType === 'DEVICE') {
      return alert.sourceId !== null && assignedIds.has(Number(alert.sourceId));
    }

    if (alert.sourceType === 'GROUP') {
      return alert.sourceId !== null && groupIds.has(Number(alert.sourceId));
    }

    return ['SYSTEM', 'RULE', 'MODE', 'GOAL', 'REPORT', 'ROUTINE'].includes(alert.sourceType);
  }
}


============================================================
FILE: src\app\shared\presentation\pages\not-found\not-found-page.component.html
============================================================

<section class="not-found">
  <h1>{{ 'notFound.title' | translate }}</h1>

  <p>
    {{ 'notFound.content' | translate }}
  </p>

  <a routerLink="/home">
    {{ 'notFound.goHome' | translate }}
  </a>
</section>

============================================================
FILE: src\app\shared\presentation\pages\not-found\not-found-page.component.scss
============================================================

.not-found {
  min-height: 60vh;
  display: grid;
  place-items: center;
  align-content: center;
  text-align: center;
  gap: 16px;
}

h1 {
  margin: 0;
  font-size: clamp(2.2rem, 5vw, 4rem);
}

p {
  max-width: 520px;
  margin: 0;
  color: var(--text-secondary);
  line-height: 1.7;
}

a {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 44px;
  padding: 0 18px;
  border-radius: 999px;
  color: var(--text-on-action);
  background: var(--ec-action-bg);
  text-decoration: none;
  font-weight: 900;
}


============================================================
FILE: src\app\shared\presentation\pages\not-found\not-found-page.component.ts
============================================================

import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';
import { TranslateModule } from '@ngx-translate/core';

@Component({
  selector: 'app-not-found-page',
  standalone: true,
  imports: [RouterLink, TranslateModule],
  templateUrl: './not-found-page.component.html',
  styleUrls: ['./not-found-page.component.scss'],
})
export class NotFoundPageComponent {}

============================================================
FILE: src\app\shared\presentation\utils\floating-panel-position.ts
============================================================

export interface FloatingPanelPosition {
  top: number;
  left: number;
  width: number;
  maxHeight: number;
  opensAbove: boolean;
}

export interface FloatingPanelPositionOptions {
  gap?: number;
  maxHeight?: number;
  minHeight?: number;
  minWidth?: number;
  panelHeight?: number;
  preferredWidth?: number;
  viewportPadding?: number;
}

type PopoverElement = HTMLElement & {
  hidePopover?: () => void;
  showPopover?: () => void;
};

export function positionFloatingPanel(
  trigger: HTMLElement,
  panel: HTMLElement,
  options: FloatingPanelPositionOptions = {}
): FloatingPanelPosition {
  const position = computeFloatingPanelPosition(trigger, options);

  panel.style.top = `${Math.round(position.top)}px`;
  panel.style.left = `${Math.round(position.left)}px`;
  panel.style.width = `${Math.round(position.width)}px`;
  panel.style.maxHeight = `${Math.round(position.maxHeight)}px`;

  return position;
}

export function showFloatingPopover(panel: HTMLElement): void {
  const popover = panel as PopoverElement;

  if (typeof popover.showPopover !== 'function' || isPopoverOpen(panel)) {
    return;
  }

  try {
    popover.showPopover();
  } catch {
    return;
  }
}

export function hideFloatingPopover(panel: HTMLElement | null | undefined): void {
  if (!panel) {
    return;
  }

  const popover = panel as PopoverElement;

  if (typeof popover.hidePopover !== 'function' || !isPopoverOpen(panel)) {
    return;
  }

  try {
    popover.hidePopover();
  } catch {
    return;
  }
}

function computeFloatingPanelPosition(
  trigger: HTMLElement,
  options: FloatingPanelPositionOptions
): FloatingPanelPosition {
  const viewportPadding = options.viewportPadding ?? 12;
  const gap = options.gap ?? 10;
  const rect = trigger.getBoundingClientRect();
  const viewportWidth = Math.max(document.documentElement.clientWidth, window.innerWidth || 0);
  const viewportHeight = Math.max(document.documentElement.clientHeight, window.innerHeight || 0);
  const availableWidth = Math.max(0, viewportWidth - viewportPadding * 2);
  const minWidth = Math.min(options.minWidth ?? 180, availableWidth);
  const requestedWidth = options.preferredWidth ?? Math.max(rect.width, minWidth);
  const width = clamp(requestedWidth, minWidth, availableWidth);
  const left = clamp(
    rect.left,
    viewportPadding,
    Math.max(viewportPadding, viewportWidth - viewportPadding - width)
  );

  const preferredPanelHeight = options.panelHeight ?? options.maxHeight ?? 360;
  const maxPanelHeight = options.maxHeight ?? preferredPanelHeight;
  const minHeight = options.minHeight ?? 140;
  const belowSpace = viewportHeight - rect.bottom - viewportPadding - gap;
  const aboveSpace = rect.top - viewportPadding - gap;
  const opensAbove = belowSpace < Math.min(preferredPanelHeight, minHeight) && aboveSpace > belowSpace;
  const availableHeight = Math.max(0, opensAbove ? aboveSpace : belowSpace);
  const maxHeight = clamp(
    Math.min(preferredPanelHeight, maxPanelHeight),
    Math.min(minHeight, Math.max(minHeight, availableHeight)),
    Math.max(minHeight, availableHeight)
  );
  const panelHeight = Math.min(preferredPanelHeight, maxHeight);
  const top = opensAbove
    ? clamp(
        rect.top - gap - panelHeight,
        viewportPadding,
        Math.max(viewportPadding, viewportHeight - viewportPadding - panelHeight)
      )
    : clamp(
        rect.bottom + gap,
        viewportPadding,
        Math.max(viewportPadding, viewportHeight - viewportPadding - panelHeight)
      );

  return {
    top,
    left,
    width,
    maxHeight,
    opensAbove,
  };
}

function isPopoverOpen(panel: HTMLElement): boolean {
  try {
    return panel.matches(':popover-open');
  } catch {
    return false;
  }
}

function clamp(value: number, min: number, max: number): number {
  return Math.min(Math.max(value, min), max);
}


============================================================
FILE: src\app\workplace\application\commands\assign-device.command.ts
============================================================

export interface AssignDeviceCommand {
  deviceId: number;
  locationId: number;
  roomId?: number | null;
}


============================================================
FILE: src\app\workplace\application\commands\create-location.command.ts
============================================================

import { LocationType } from '../../domain/model/location.entity';

export interface CreateLocationCommand {
  name: string;
  address: string;
  type: LocationType;
}


============================================================
FILE: src\app\workplace\application\commands\create-room.command.ts
============================================================

export interface CreateRoomCommand {
  locationId: number;
  name: string;
  floor: string;
}


============================================================
FILE: src\app\workplace\application\commands\move-device-assignment.command.ts
============================================================

export interface MoveDeviceAssignmentCommand {
  assignmentId: number;
  locationId: number;
  roomId?: number | null;
}


============================================================
FILE: src\app\workplace\application\commands\update-location.command.ts
============================================================

import { LocationType } from '../../domain/model/location.entity';

export interface UpdateLocationCommand {
  locationId: number;
  name: string;
  address: string;
  type: LocationType;
}


============================================================
FILE: src\app\workplace\application\commands\update-room.command.ts
============================================================

export interface UpdateRoomCommand {
  roomId: number;
  locationId: number;
  name: string;
  floor: string;
}


============================================================
FILE: src\app\workplace\application\services\active-workplace-context.service.ts
============================================================

import { Injectable, computed, signal } from '@angular/core';

import { STORAGE_KEYS } from '../../../shared/infrastructure/constants/storage-keys';
import { Location } from '../../domain/model/location.entity';

@Injectable({
  providedIn: 'root',
})
export class ActiveWorkplaceContextService {
  private readonly activeLocationIdSignal = signal<number | null>(
    this.restoreActiveLocationId()
  );
  private readonly locationsSignal = signal<Location[]>([]);

  readonly activeLocationId = computed(() => this.activeLocationIdSignal());
  readonly activeLocation = computed(() => {
    const activeId = this.activeLocationIdSignal();

    if (!activeId) {
      return null;
    }

    return this.locationsSignal().find((location) => location.id === activeId) ?? null;
  });

  setActiveLocation(locationId: number | null): void {
    if (!locationId || !Number.isFinite(locationId)) {
      this.clearActiveLocation();
      return;
    }

    this.activeLocationIdSignal.set(locationId);
    localStorage.setItem(STORAGE_KEYS.activeWorkplaceId, String(locationId));
  }

  clearActiveLocation(): void {
    this.activeLocationIdSignal.set(null);
    localStorage.removeItem(STORAGE_KEYS.activeWorkplaceId);
  }

  ensureActiveLocation(locations: Location[]): Location | null {
    this.locationsSignal.set(locations);

    if (locations.length === 0) {
      this.clearActiveLocation();
      return null;
    }

    const activeId = this.activeLocationIdSignal();
    const current = activeId
      ? locations.find((location) => location.id === activeId)
      : null;

    if (current) {
      return current;
    }

    const fallback = locations[0];
    this.setActiveLocation(fallback.id);
    return fallback;
  }

  private restoreActiveLocationId(): number | null {
    const rawValue = localStorage.getItem(STORAGE_KEYS.activeWorkplaceId);

    if (!rawValue) {
      return null;
    }

    const parsed = Number(rawValue);

    return Number.isFinite(parsed) && parsed > 0 ? parsed : null;
  }
}


============================================================
FILE: src\app\workplace\application\services\workplace.facade.ts
============================================================

import { Injectable } from '@angular/core';
import { firstValueFrom } from 'rxjs';

import { DeviceControlFacade } from '../../../device-control/application/services/device-control.facade';

import { AssignDeviceCommand } from '../commands/assign-device.command';
import { CreateLocationCommand } from '../commands/create-location.command';
import { CreateRoomCommand } from '../commands/create-room.command';
import { MoveDeviceAssignmentCommand } from '../commands/move-device-assignment.command';
import { UpdateLocationCommand } from '../commands/update-location.command';
import { UpdateRoomCommand } from '../commands/update-room.command';

import { DeviceAssignmentsApiService } from '../../infrastructure/api/device-assignments-api.service';
import { LocationsApiService } from '../../infrastructure/api/locations-api.service';
import { RoomsApiService } from '../../infrastructure/api/rooms-api.service';

import { DeviceAssignmentAssembler } from '../../infrastructure/assemblers/device-assignment.assembler';
import { LocationAssembler } from '../../infrastructure/assemblers/location.assembler';
import { RoomAssembler } from '../../infrastructure/assemblers/room.assembler';

import { WorkplaceStore } from '../stores/workplace.store';

@Injectable({
  providedIn: 'root',
})
export class WorkplaceFacade {
  private readonly locationAssembler = new LocationAssembler();
  private readonly roomAssembler = new RoomAssembler();
  private readonly deviceAssignmentAssembler = new DeviceAssignmentAssembler();

  get locations() {
    return this.store.locations;
  }

  get rooms() {
    return this.store.rooms;
  }

  get deviceAssignments() {
    return this.store.deviceAssignments;
  }

  get loading() {
    return this.store.loading;
  }

  get error() {
    return this.store.error;
  }

  get businessLocations() {
    return this.store.businessLocations;
  }

  get branchLocations() {
    return this.store.branchLocations;
  }

  get homeLocations() {
    return this.store.homeLocations;
  }

  constructor(
    private readonly locationsApi: LocationsApiService,
    private readonly roomsApi: RoomsApiService,
    private readonly deviceAssignmentsApi: DeviceAssignmentsApiService,
    private readonly deviceControlFacade: DeviceControlFacade,
    private readonly store: WorkplaceStore
  ) {}

  async loadWorkplace(): Promise<void> {
    this.startRequest();

    try {
      await Promise.all([
        this.loadLocations(),
        this.loadRooms(),
        this.loadDeviceAssignments(),
        this.deviceControlFacade.loadDeviceControl(),
      ]);
    } catch (error) {
      console.error(error);
      this.store.setError('workplace.loadError');
    } finally {
      this.finishRequest();
    }
  }

  async loadLocations(): Promise<void> {
    const responses = await firstValueFrom(
      this.locationsApi.findAllForCurrentUser()
    );

    const locations = responses
      .map((response) => this.locationAssembler.toEntity(response))
      .sort(
        (first, second) =>
          new Date(second.createdAt).getTime() -
          new Date(first.createdAt).getTime()
      );

    this.store.setLocations(locations);
  }

  async createLocation(command: CreateLocationCommand): Promise<boolean> {
    this.startRequest();

    try {
      if (!command.name.trim() || !command.address.trim()) {
        this.store.setError('workplace.locationCreateError');
        return false;
      }

      const response = await firstValueFrom(
        this.locationsApi.create({
          name: command.name.trim(),
          address: command.address.trim(),
          type: command.type,
          createdAt: new Date().toISOString().slice(0, 10),
        })
      );

      this.store.prependLocation(this.locationAssembler.toEntity(response));
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('workplace.locationCreateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async updateLocation(command: UpdateLocationCommand): Promise<boolean> {
    this.startRequest();

    try {
      const response = await firstValueFrom(
        this.locationsApi.patch(command.locationId, {
          name: command.name.trim(),
          address: command.address.trim(),
          type: command.type,
        })
      );

      this.store.updateLocation(this.locationAssembler.toEntity(response));
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('workplace.locationUpdateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async deleteLocation(locationId: number): Promise<boolean> {
    this.startRequest();

    try {
      await firstValueFrom(this.locationsApi.delete(locationId));
      this.store.removeLocation(locationId);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('workplace.locationDeleteError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async loadRooms(): Promise<void> {
    const responses = await firstValueFrom(
      this.roomsApi.findAllForCurrentUser()
    );

    const rooms = responses
      .map((response) => this.roomAssembler.toEntity(response))
      .sort(
        (first, second) =>
          new Date(second.createdAt).getTime() -
          new Date(first.createdAt).getTime()
      );

    this.store.setRooms(rooms);
  }

  async createRoom(command: CreateRoomCommand): Promise<boolean> {
    this.startRequest();

    try {
      if (!command.name.trim() || !command.floor.trim()) {
        this.store.setError('workplace.roomCreateError');
        return false;
      }

      const response = await firstValueFrom(
        this.roomsApi.create({
          locationId: Number(command.locationId),
          name: command.name.trim(),
          floor: command.floor.trim(),
          createdAt: new Date().toISOString().slice(0, 10),
        })
      );

      this.store.prependRoom(this.roomAssembler.toEntity(response));
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('workplace.roomCreateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async updateRoom(command: UpdateRoomCommand): Promise<boolean> {
    this.startRequest();

    try {
      const response = await firstValueFrom(
        this.roomsApi.patch(command.roomId, {
          locationId: Number(command.locationId),
          name: command.name.trim(),
          floor: command.floor.trim(),
        })
      );

      this.store.updateRoom(this.roomAssembler.toEntity(response));
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('workplace.roomUpdateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async deleteRoom(roomId: number): Promise<boolean> {
    this.startRequest();

    try {
      await firstValueFrom(this.roomsApi.delete(roomId));
      await Promise.all([this.loadRooms(), this.loadDeviceAssignments()]);

      const roomStillExists = this.store.rooms().some((room) => room.id === roomId);

      if (roomStillExists) {
        this.store.setError('workplace.roomDeleteError');
        return false;
      }

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('workplace.roomDeleteError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async loadDeviceAssignments(): Promise<void> {
    const responses = await firstValueFrom(
      this.deviceAssignmentsApi.findAllForCurrentUser()
    );

    const assignments = responses
      .map((response) => this.deviceAssignmentAssembler.toEntity(response))
      .sort(
        (first, second) =>
          new Date(second.assignedAt).getTime() -
          new Date(first.assignedAt).getTime()
      );

    this.store.setDeviceAssignments(assignments);
  }

  async assignDevice(command: AssignDeviceCommand): Promise<boolean> {
    this.startRequest();

    try {
      const alreadyAssigned = this.store
        .deviceAssignments()
        .some((assignment) => assignment.deviceId === Number(command.deviceId));

      if (alreadyAssigned) {
        this.store.setError('workplace.deviceAlreadyAssigned');
        return false;
      }

      const response = await firstValueFrom(
        this.deviceAssignmentsApi.create({
          deviceId: Number(command.deviceId),
          locationId: Number(command.locationId),
          roomId: command.roomId ? Number(command.roomId) : null,
          assignedAt: new Date().toISOString().slice(0, 10),
        })
      );

      this.store.prependDeviceAssignment(
        this.deviceAssignmentAssembler.toEntity(response)
      );

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('workplace.assignmentCreateError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async moveDeviceAssignment(command: MoveDeviceAssignmentCommand): Promise<boolean> {
    this.startRequest();

    try {
      const response = await firstValueFrom(
        this.deviceAssignmentsApi.patch(command.assignmentId, {
          locationId: Number(command.locationId),
          roomId: command.roomId ? Number(command.roomId) : null,
        })
      );

      this.store.updateDeviceAssignment(
        this.deviceAssignmentAssembler.toEntity(response)
      );

      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('workplace.assignmentMoveError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  async deleteDeviceAssignment(assignmentId: number): Promise<boolean> {
    this.startRequest();

    try {
      await firstValueFrom(this.deviceAssignmentsApi.delete(assignmentId));
      this.store.removeDeviceAssignment(assignmentId);
      return true;
    } catch (error) {
      console.error(error);
      this.store.setError('workplace.assignmentDeleteError');
      return false;
    } finally {
      this.finishRequest();
    }
  }

  getLocationName(locationId: number): string {
    return (
      this.store.locations().find((location) => location.id === locationId)
        ?.name ?? `Location #${locationId}`
    );
  }

  getRoomName(roomId: number | null): string {
    if (roomId === null) {
      return 'Sin habitacion';
    }

    return (
      this.store.rooms().find((room) => room.id === roomId)?.name ??
      `Room #${roomId}`
    );
  }

  getDeviceName(deviceId: number): string {
    return this.deviceControlFacade.getDeviceName(deviceId);
  }

  getRoomsByLocation(locationId: number): ReturnType<WorkplaceStore['rooms']> {
    return this.store.rooms().filter((room) => room.locationId === locationId);
  }

  clearMessages(): void {
    this.store.clearMessages();
  }

  private startRequest(): void {
    this.store.setLoading(true);
    this.store.clearMessages();
  }

  private finishRequest(): void {
    this.store.setLoading(false);
  }
}


============================================================
FILE: src\app\workplace\application\stores\workplace.store.ts
============================================================

import { Injectable, computed, signal } from '@angular/core';

import { DeviceAssignment } from '../../domain/model/device-assignment.entity';
import { Location } from '../../domain/model/location.entity';
import { Room } from '../../domain/model/room.entity';

@Injectable({
  providedIn: 'root',
})
export class WorkplaceStore {
  private readonly locationsSignal = signal<Location[]>([]);
  private readonly roomsSignal = signal<Room[]>([]);
  private readonly deviceAssignmentsSignal = signal<DeviceAssignment[]>([]);

  private readonly loadingSignal = signal<boolean>(false);
  private readonly errorSignal = signal<string | null>(null);

  readonly locations = computed(() => this.locationsSignal());
  readonly rooms = computed(() => this.roomsSignal());
  readonly deviceAssignments = computed(() => this.deviceAssignmentsSignal());

  readonly loading = computed(() => this.loadingSignal());
  readonly error = computed(() => this.errorSignal());

  readonly businessLocations = computed(() =>
    this.locationsSignal().filter((location) => location.isBusiness)
  );

  readonly branchLocations = computed(() =>
    this.locationsSignal().filter((location) => location.isBranch)
  );

  readonly homeLocations = computed(() =>
    this.locationsSignal().filter((location) => location.isHome)
  );

  setLocations(value: Location[]): void {
    this.locationsSignal.set(value);
  }

  prependLocation(value: Location): void {
    this.locationsSignal.update((locations) => [value, ...locations]);
  }

  updateLocation(value: Location): void {
    this.locationsSignal.update((locations) =>
      locations.map((location) => (location.id === value.id ? value : location))
    );
  }

  removeLocation(locationId: number): void {
    this.locationsSignal.update((locations) =>
      locations.filter((location) => location.id !== locationId)
    );

    this.roomsSignal.update((rooms) =>
      rooms.filter((room) => room.locationId !== locationId)
    );

    this.deviceAssignmentsSignal.update((assignments) =>
      assignments.filter((assignment) => assignment.locationId !== locationId)
    );
  }

  setRooms(value: Room[]): void {
    this.roomsSignal.set(value);
  }

  prependRoom(value: Room): void {
    this.roomsSignal.update((rooms) => [value, ...rooms]);
  }

  updateRoom(value: Room): void {
    this.roomsSignal.update((rooms) =>
      rooms.map((room) => (room.id === value.id ? value : room))
    );
  }

  removeRoom(roomId: number): void {
    this.roomsSignal.update((rooms) => rooms.filter((room) => room.id !== roomId));

    this.deviceAssignmentsSignal.update((assignments) =>
      assignments.map((assignment) =>
        assignment.roomId === roomId
          ? new DeviceAssignment({
              id: assignment.id,
              userId: assignment.userId,
              deviceId: assignment.deviceId,
              locationId: assignment.locationId,
              roomId: null,
              assignedAt: assignment.assignedAt,
            })
          : assignment
      )
    );
  }

  setDeviceAssignments(value: DeviceAssignment[]): void {
    this.deviceAssignmentsSignal.set(value);
  }

  prependDeviceAssignment(value: DeviceAssignment): void {
    this.deviceAssignmentsSignal.update((assignments) => [value, ...assignments]);
  }

  updateDeviceAssignment(value: DeviceAssignment): void {
    this.deviceAssignmentsSignal.update((assignments) =>
      assignments.map((assignment) =>
        assignment.id === value.id ? value : assignment
      )
    );
  }

  removeDeviceAssignment(assignmentId: number): void {
    this.deviceAssignmentsSignal.update((assignments) =>
      assignments.filter((assignment) => assignment.id !== assignmentId)
    );
  }

  setLoading(value: boolean): void {
    this.loadingSignal.set(value);
  }

  setError(value: string | null): void {
    this.errorSignal.set(value);
  }

  clearMessages(): void {
    this.errorSignal.set(null);
  }

  reset(): void {
    this.locationsSignal.set([]);
    this.roomsSignal.set([]);
    this.deviceAssignmentsSignal.set([]);
    this.loadingSignal.set(false);
    this.errorSignal.set(null);
  }
}


============================================================
FILE: src\app\workplace\domain\model\device-assignment.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export class DeviceAssignment extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _deviceId: number;
  private readonly _locationId: number;
  private readonly _roomId: number | null;
  private readonly _assignedAt: string;

  constructor(props: {
    id: number;
    userId: number;
    deviceId: number;
    locationId: number;
    roomId?: number | null;
    assignedAt: string;
  }) {
    super(props.id);
    this._userId = props.userId;
    this._deviceId = props.deviceId;
    this._locationId = props.locationId;
    this._roomId = props.roomId ?? null;
    this._assignedAt = props.assignedAt;
  }

  get userId(): number {
    return this._userId;
  }

  get deviceId(): number {
    return this._deviceId;
  }

  get locationId(): number {
    return this._locationId;
  }

  get roomId(): number | null {
    return this._roomId;
  }

  get hasRoom(): boolean {
    return this._roomId !== null;
  }

  get assignedAt(): string {
    return this._assignedAt;
  }
}


============================================================
FILE: src\app\workplace\domain\model\location.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export type LocationType = 'HOME' | 'BUSINESS' | 'BRANCH';

export class Location extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _name: string;
  private readonly _address: string;
  private readonly _type: LocationType;
  private readonly _createdAt: string;

  constructor(props: {
    id: number;
    userId: number;
    name: string;
    address: string;
    type: LocationType;
    createdAt: string;
  }) {
    super(props.id);
    this._userId = props.userId;
    this._name = props.name;
    this._address = props.address;
    this._type = props.type;
    this._createdAt = props.createdAt;
  }

  get userId(): number {
    return this._userId;
  }

  get name(): string {
    return this._name;
  }

  get address(): string {
    return this._address;
  }

  get type(): LocationType {
    return this._type;
  }

  get createdAt(): string {
    return this._createdAt;
  }

  get isBusiness(): boolean {
    return this._type === 'BUSINESS';
  }

  get isBranch(): boolean {
    return this._type === 'BRANCH';
  }

  get isHome(): boolean {
    return this._type === 'HOME';
  }
}

============================================================
FILE: src\app\workplace\domain\model\room.entity.ts
============================================================

import { BaseEntity } from '../../../shared/domain/model/base.entity';

export class Room extends BaseEntity<number> {
  private readonly _userId: number;
  private readonly _locationId: number;
  private readonly _name: string;
  private readonly _floor: string;
  private readonly _createdAt: string;

  constructor(props: {
    id: number;
    userId: number;
    locationId: number;
    name: string;
    floor: string;
    createdAt: string;
  }) {
    super(props.id);
    this._userId = props.userId;
    this._locationId = props.locationId;
    this._name = props.name;
    this._floor = props.floor;
    this._createdAt = props.createdAt;
  }

  get userId(): number {
    return this._userId;
  }

  get locationId(): number {
    return this._locationId;
  }

  get name(): string {
    return this._name;
  }

  get floor(): string {
    return this._floor;
  }

  get createdAt(): string {
    return this._createdAt;
  }
}

============================================================
FILE: src\app\workplace\infrastructure\api\device-assignments-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { DeviceAssignment } from '../../domain/model/device-assignment.entity';
import { DeviceAssignmentAssembler } from '../assemblers/device-assignment.assembler';
import { DeviceAssignmentResource } from '../resources/device-assignment.resource';
import { DeviceAssignmentResponse } from '../responses/device-assignment.response';

@Injectable({
  providedIn: 'root',
})
export class DeviceAssignmentsApiService extends BaseApiService<
  DeviceAssignment,
  DeviceAssignmentResource,
  DeviceAssignmentResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(http, apiBaseUrl, 'workplace/device-assignments', new DeviceAssignmentAssembler());
  }

  findAllForCurrentUser(): Observable<DeviceAssignmentResponse[]> {
    return this.http.get<DeviceAssignmentResponse[]>(this.resourceEndpoint);
  }

  findByDeviceId(deviceId: number): Observable<DeviceAssignmentResponse[]> {
    return this.http.get<DeviceAssignmentResponse[]>(`${this.resourceEndpoint}?deviceId=${deviceId}`);
  }
}


============================================================
FILE: src\app\workplace\infrastructure\api\locations-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { Location } from '../../domain/model/location.entity';
import { LocationAssembler } from '../assemblers/location.assembler';
import { LocationResource } from '../resources/location.resource';
import { LocationResponse } from '../responses/location.response';

@Injectable({
  providedIn: 'root',
})
export class LocationsApiService extends BaseApiService<
  Location,
  LocationResource,
  LocationResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(http, apiBaseUrl, 'workplace/locations', new LocationAssembler());
  }

  findAllForCurrentUser(): Observable<LocationResponse[]> {
    return this.http.get<LocationResponse[]>(this.resourceEndpoint);
  }
}


============================================================
FILE: src\app\workplace\infrastructure\api\rooms-api.service.ts
============================================================

import { Inject, Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

import { API_BASE_URL } from '../../../shared/infrastructure/api/api-config';
import { BaseApiService } from '../../../shared/infrastructure/api/base-api.service';

import { Room } from '../../domain/model/room.entity';
import { RoomAssembler } from '../assemblers/room.assembler';
import { RoomResource } from '../resources/room.resource';
import { RoomResponse } from '../responses/room.response';

@Injectable({
  providedIn: 'root',
})
export class RoomsApiService extends BaseApiService<
  Room,
  RoomResource,
  RoomResponse
> {
  constructor(
    http: HttpClient,
    @Inject(API_BASE_URL) apiBaseUrl: string
  ) {
    super(http, apiBaseUrl, 'workplace/rooms', new RoomAssembler());
  }

  findAllForCurrentUser(): Observable<RoomResponse[]> {
    return this.http.get<RoomResponse[]>(this.resourceEndpoint);
  }

  findByLocationId(locationId: number): Observable<RoomResponse[]> {
    return this.http.get<RoomResponse[]>(`${this.resourceEndpoint}?locationId=${locationId}`);
  }
}


============================================================
FILE: src\app\workplace\infrastructure\assemblers\device-assignment.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { DeviceAssignment } from '../../domain/model/device-assignment.entity';
import { DeviceAssignmentResource } from '../resources/device-assignment.resource';
import { DeviceAssignmentResponse } from '../responses/device-assignment.response';

export class DeviceAssignmentAssembler extends BaseAssembler<
  DeviceAssignment,
  DeviceAssignmentResource,
  DeviceAssignmentResponse
> {
  override toEntity(response: DeviceAssignmentResponse): DeviceAssignment {
    return new DeviceAssignment({
      id: response.id,
      userId: response.userId,
      deviceId: response.deviceId,
      locationId: response.locationId,
      roomId: response.roomId,
      assignedAt: response.assignedAt,
    });
  }

  override toResource(entity: DeviceAssignment): DeviceAssignmentResource {
    return {
      userId: entity.userId,
      deviceId: entity.deviceId,
      locationId: entity.locationId,
      roomId: entity.roomId,
      assignedAt: entity.assignedAt,
    };
  }
}

============================================================
FILE: src\app\workplace\infrastructure\assemblers\location.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { Location } from '../../domain/model/location.entity';
import { LocationResource } from '../resources/location.resource';
import { LocationResponse } from '../responses/location.response';

export class LocationAssembler extends BaseAssembler<
  Location,
  LocationResource,
  LocationResponse
> {
  override toEntity(response: LocationResponse): Location {
    return new Location({
      id: response.id,
      userId: response.userId,
      name: response.name,
      address: response.address,
      type: response.type,
      createdAt: response.createdAt ?? '',
    });
  }

  override toResource(entity: Location): LocationResource {
    return {
      userId: entity.userId,
      name: entity.name,
      address: entity.address,
      type: entity.type,
      createdAt: entity.createdAt,
    };
  }
}


============================================================
FILE: src\app\workplace\infrastructure\assemblers\room.assembler.ts
============================================================

import { BaseAssembler } from '../../../shared/infrastructure/assemblers/base.assembler';

import { Room } from '../../domain/model/room.entity';
import { RoomResource } from '../resources/room.resource';
import { RoomResponse } from '../responses/room.response';

export class RoomAssembler extends BaseAssembler<
  Room,
  RoomResource,
  RoomResponse
> {
  override toEntity(response: RoomResponse): Room {
    return new Room({
      id: response.id,
      userId: response.userId,
      locationId: response.locationId,
      name: response.name,
      floor: response.floor,
      createdAt: response.createdAt ?? '',
    });
  }

  override toResource(entity: Room): RoomResource {
    return {
      userId: entity.userId,
      locationId: entity.locationId,
      name: entity.name,
      floor: entity.floor,
      createdAt: entity.createdAt,
    };
  }
}


============================================================
FILE: src\app\workplace\infrastructure\resources\device-assignment.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';

export interface DeviceAssignmentResource extends BaseResource {
  userId?: number;
  deviceId: number;
  locationId: number;
  roomId?: number | null;
  assignedAt: string;
}


============================================================
FILE: src\app\workplace\infrastructure\resources\location.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';
import { LocationType } from '../../domain/model/location.entity';

export interface LocationResource extends BaseResource {
  userId?: number;
  name: string;
  address: string;
  type: LocationType;
  createdAt: string;
}


============================================================
FILE: src\app\workplace\infrastructure\resources\room.resource.ts
============================================================

import { BaseResource } from '../../../shared/infrastructure/resources/base.resource';

export interface RoomResource extends BaseResource {
  userId?: number;
  locationId: number;
  name: string;
  floor: string;
  createdAt: string;
}


============================================================
FILE: src\app\workplace\infrastructure\responses\device-assignment.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';

export interface DeviceAssignmentResponse extends BaseResponse<number> {
  userId: number;
  deviceId: number;
  locationId: number;
  roomId?: number | null;
  assignedAt: string;
}


============================================================
FILE: src\app\workplace\infrastructure\responses\location.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';
import { LocationType } from '../../domain/model/location.entity';

export interface LocationResponse extends BaseResponse<number> {
  userId: number;
  name: string;
  address: string;
  type: LocationType;
  createdAt: string;
}

============================================================
FILE: src\app\workplace\infrastructure\responses\room.response.ts
============================================================

import { BaseResponse } from '../../../shared/infrastructure/responses/base.response';

export interface RoomResponse extends BaseResponse<number> {
  userId: number;
  locationId: number;
  name: string;
  floor: string;
  createdAt: string;
}

============================================================
FILE: src\app\workplace\presentation\components\device-assignment-form\device-assignment-form.component.html
============================================================

<form class="assignment-form" (ngSubmit)="onSubmit()">
  <app-dropdown
    [options]="deviceOptions"
    [value]="deviceValue"
    [placeholder]="'workplace.selectDevice' | translate"
    (valueChange)="selectDevice($event)"
  />

  <app-dropdown
    [options]="locationOptions"
    [value]="locationValue"
    [placeholder]="'workplace.selectLocation' | translate"
    (valueChange)="selectLocation($event)"
  />

  <app-dropdown
    [options]="roomOptions"
    [value]="roomValue"
    [placeholder]="'workplace.assignments.noRoom' | translate"
    (valueChange)="selectRoom($event)"
  />

  <app-button type="submit" variant="create" [disabled]="loading">
    {{ 'workplace.assignDevice' | translate }}
  </app-button>
</form>


============================================================
FILE: src\app\workplace\presentation\components\device-assignment-form\device-assignment-form.component.scss
============================================================

.assignment-form {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr auto;
  gap: 12px;
}

app-dropdown {
  width: 100%;
}

app-button {
  align-self: stretch;
}

@media (max-width: 980px) {
  .assignment-form {
    grid-template-columns: 1fr;
  }
}


============================================================
FILE: src\app\workplace\presentation\components\device-assignment-form\device-assignment-form.component.ts
============================================================

import { Component, EventEmitter, Input, OnChanges, Output } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { Device } from '../../../../device-control/domain/model/device.entity';
import { AssignDeviceCommand } from '../../../application/commands/assign-device.command';
import { Location } from '../../../domain/model/location.entity';
import { Room } from '../../../domain/model/room.entity';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';

@Component({
  selector: 'app-device-assignment-form',
  standalone: true,
  imports: [FormsModule, TranslateModule, AppButtonComponent, AppDropdownComponent],
  templateUrl: './device-assignment-form.component.html',
  styleUrls: ['./device-assignment-form.component.scss'],
})
export class DeviceAssignmentFormComponent implements OnChanges {
  @Input() devices: Device[] = [];
  @Input() locations: Location[] = [];
  @Input() rooms: Room[] = [];
  @Input() loading = false;

  @Output() assigned = new EventEmitter<AssignDeviceCommand>();

  deviceId: number | null = null;
  locationId: number | null = null;
  roomId: number | null = null;

  constructor(private readonly translate: TranslateService) {}

  ngOnChanges(): void {
    if (this.locations.length === 1 && this.locationId !== this.locations[0].id) {
      this.locationId = this.locations[0].id;
      this.onLocationChange();
    }
  }

  get availableRooms(): Room[] {
    if (!this.locationId) return [];

    return this.rooms.filter((room) => room.locationId === Number(this.locationId));
  }

  get deviceValue(): string | null {
    return this.deviceId ? String(this.deviceId) : null;
  }

  get locationValue(): string | null {
    return this.locationId ? String(this.locationId) : null;
  }

  get roomValue(): string {
    return this.roomId ? String(this.roomId) : 'none';
  }

  get deviceOptions(): DropdownOption[] {
    return this.devices.map((device) => ({
      label: device.name,
      value: String(device.id),
      description: `${this.deviceTypeLabel(device.type)} - ${device.powerWatts} W`,
    }));
  }

  get locationOptions(): DropdownOption[] {
    return this.locations.map((location) => ({
      label: location.name,
      value: String(location.id),
      description: location.address || this.t('workplace.assignments.form.operationalSite'),
    }));
  }

  get roomOptions(): DropdownOption[] {
    return [
      {
        label: 'none',
        labelKey: 'workplace.assignments.noRoom',
        value: 'none',
        description: 'site-only',
        descriptionKey: 'workplace.assignments.form.siteOnly',
      },
      ...this.availableRooms.map((room) => ({
        label: room.name,
        value: String(room.id),
        description: this.t('workplace.assignments.form.availableRoom'),
      })),
    ];
  }

  deviceTypeLabel(type: Device['type']): string {
    const labelKeys: Record<Device['type'], string> = {
      PLUG: 'workplace.deviceTypes.plug',
      LIGHT: 'workplace.deviceTypes.light',
      SWITCH: 'workplace.deviceTypes.switch',
      SENSOR: 'workplace.deviceTypes.sensor',
      OTHER: 'workplace.deviceTypes.other',
    };

    return this.t(labelKeys[type]);
  }

  selectDevice(value: string): void {
    const deviceId = Number(value);
    this.deviceId = Number.isFinite(deviceId) ? deviceId : null;
  }

  selectLocation(value: string): void {
    const locationId = Number(value);
    this.locationId = Number.isFinite(locationId) ? locationId : null;
    this.onLocationChange();
  }

  selectRoom(value: string): void {
    if (value === 'none') {
      this.roomId = null;
      return;
    }

    const roomId = Number(value);
    this.roomId = Number.isFinite(roomId) ? roomId : null;
  }

  onLocationChange(): void {
    const roomStillAvailable = this.availableRooms.some(
      (room) => room.id === Number(this.roomId)
    );

    if (!roomStillAvailable) {
      this.roomId = null;
    }
  }

  onSubmit(): void {
    if (!this.deviceId || !this.locationId) return;

    this.assigned.emit({
      deviceId: Number(this.deviceId),
      locationId: Number(this.locationId),
      roomId: this.roomId ? Number(this.roomId) : null,
    });

    this.deviceId = null;
    this.locationId = null;
    this.roomId = null;
  }

  private t(key: string, params?: Record<string, unknown>): string {
    return this.translate.instant(key, params);
  }
}


============================================================
FILE: src\app\workplace\presentation\components\location-card\location-card.component.html
============================================================

<app-section-card class="location-card" size="medium">
  <div class="location-card__identity">
    <span class="location-card__type">{{ typeLabelKey | translate }}</span>
    <h2>{{ location.name }}</h2>
    <p>{{ location.address }}</p>
    <small>{{ 'workplace.locations.card.createdAt' | translate }}: {{ formattedCreatedAt }}</small>
  </div>

  <div class="location-card__metrics">
    <article>
      <span>{{ 'nav.rooms' | translate }}</span>
      <strong>{{ roomCount }}</strong>
    </article>
    <article>
      <span>{{ 'nav.devices' | translate }}</span>
      <strong>{{ deviceCount }}</strong>
    </article>
    <article>
      <span>{{ 'nav.deviceGroups' | translate }}</span>
      <strong>{{ groupCount }}</strong>
    </article>
    <article>
      <span>{{ 'workplace.locations.card.activePower' | translate }}</span>
      <strong>{{ activeWatts }} W</strong>
    </article>
    <article>
      <span>{{ 'workplace.locations.card.withoutRoom' | translate }}</span>
      <strong>{{ unroomedDeviceCount }}</strong>
    </article>
    <article>
      <span>{{ 'workplace.locations.card.coverage' | translate }}</span>
      <strong>{{ coverage }}%</strong>
    </article>
  </div>

  <div class="location-card__actions">
    <app-button
      class="location-card__open-action"
      variant="ghost"
      size="sm"
      [label]="'workplace.locations.card.goToRooms' | translate"
      [link]="'/spaces/rooms'"
      [ariaLabel]="'workplace.locations.card.goToRoomsAria' | translate"
      (pressed)="onOpen()"
    />
    <app-button variant="danger" size="sm" (pressed)="onRemove()">
      {{ 'common.delete' | translate }}
    </app-button>
  </div>
</app-section-card>


============================================================
FILE: src\app\workplace\presentation\components\location-card\location-card.component.scss
============================================================

:host {
  display: block;
}

.location-card ::ng-deep .section-card {
  grid-template-columns: minmax(240px, 0.9fr) minmax(360px, 1.5fr) auto;
  align-items: center;
  column-gap: 20px;
}

.location-card__identity {
  min-width: 0;
  display: grid;
  gap: 7px;
}

.location-card__type {
  width: fit-content;
  padding: 7px 10px;
  border-radius: 999px;
  color: var(--accent-color);
  background: var(--ec-accent-soft);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.location-card h2,
.location-card p,
.location-card small {
  margin: 0;
}

.location-card h2 {
  color: var(--text-primary);
  font-size: 1.28rem;
  font-weight: 950;
}

.location-card p,
.location-card small {
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.location-card__metrics {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 10px;
  padding-left: 20px;
  border-left: 1px solid var(--border-color);
}

.location-card__metrics article {
  min-height: 72px;
  padding: 11px;
  border: 1px solid var(--border-color);
  border-radius: 14px;
  background: var(--surface-card);
}

.location-card__metrics span {
  color: var(--text-muted);
  font-size: 0.68rem;
  font-weight: 950;
  text-transform: uppercase;
}

.location-card__metrics strong {
  display: block;
  margin-top: 7px;
  color: var(--text-primary);
  font-weight: 950;
}

.location-card__actions {
  display: grid;
  gap: 10px;
  justify-items: end;
  min-width: 92px;
  padding-left: 20px;
  border-left: 1px solid var(--border-color);
}

.location-card__open-action {
  min-width: 48px;
}

.location-card__actions ::ng-deep .location-card__open-action .app-button {
  min-width: 48px;
  min-height: 36px;
  padding-inline: 14px;
}

@media (max-width: 1020px) {
  .location-card ::ng-deep .section-card {
    grid-template-columns: 1fr;
  }

  .location-card__metrics,
  .location-card__actions {
    padding-top: 18px;
    padding-left: 0;
    border-top: 1px solid var(--border-color);
    border-left: 0;
  }

  .location-card__actions {
    display: flex;
    justify-items: start;
    justify-content: flex-start;
  }
}

@media (max-width: 640px) {
  .location-card__metrics {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}


============================================================
FILE: src\app\workplace\presentation\components\location-card\location-card.component.ts
============================================================

import { Component, EventEmitter, Input, Output } from '@angular/core';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { SectionCardComponent } from '../../../../shared/presentation/components/section-card/section-card.component';
import { Location, LocationType } from '../../../domain/model/location.entity';

const localeByLanguage: Record<string, string> = {
  es: 'es-PE',
  en: 'en-US',
  pt: 'pt-BR',
};

@Component({
  selector: 'app-location-card',
  standalone: true,
  imports: [TranslateModule, AppButtonComponent, SectionCardComponent],
  templateUrl: './location-card.component.html',
  styleUrls: ['./location-card.component.scss'],
})
export class LocationCardComponent {
  @Input({ required: true }) location!: Location;
  @Input() deviceCount = 0;
  @Input() roomCount = 0;
  @Input() groupCount = 0;
  @Input() unroomedDeviceCount = 0;
  @Input() activeWatts = 0;
  @Input() coverage = 0;

  @Output() selected = new EventEmitter<number>();
  @Output() remove = new EventEmitter<number>();

  constructor(
    private readonly translate: TranslateService,
    private readonly uiPreferences: UiPreferencesService
  ) {}

  get typeLabel(): string {
    return this.translate.instant(this.typeLabelKey);
  }

  get typeLabelKey(): string {
    const labels: Record<LocationType, string> = {
      HOME: 'workplace.locationTypes.home',
      BUSINESS: 'workplace.locationTypes.business',
      BRANCH: 'workplace.locationTypes.branch',
    };

    return labels[this.location.type] ?? this.location.type;
  }

  get formattedCreatedAt(): string {
    if (!this.location.createdAt) {
      return this.translate.instant('workplace.locations.card.noDate');
    }

    const date = new Date(this.location.createdAt);

    if (Number.isNaN(date.getTime())) {
      return this.location.createdAt;
    }

    return date.toLocaleDateString(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
    });
  }

  onOpen(): void {
    this.selected.emit(this.location.id);
  }

  onRemove(): void {
    this.remove.emit(this.location.id);
  }

  private currentLocale(): string {
    return localeByLanguage[this.uiPreferences.currentLanguage()] ?? localeByLanguage['es'];
  }
}


============================================================
FILE: src\app\workplace\presentation\components\room-card\room-card.component.html
============================================================

<app-section-card class="room-card" size="medium">
  <div class="room-card__identity">
    <span>
      @if (locationName) {
        {{ locationName }}
      } @else {
        {{ statusLabelKey | translate }}
      }
    </span>
    <h2>{{ room.name }}</h2>
    <p>{{ statusLabelKey | translate }}</p>
    <small>{{ 'workplace.rooms.card.createdAt' | translate }}: {{ formattedCreatedAt }}</small>
  </div>

  <div class="room-card__metrics">
    <article>
      <span>{{ 'nav.devices' | translate }}</span>
      <strong>{{ deviceCount }}</strong>
    </article>
    <article>
      <span>{{ 'workplace.rooms.card.activeDevices' | translate }}</span>
      <strong>{{ activeDeviceCount }}</strong>
    </article>
    <article>
      <span>{{ 'workplace.rooms.card.consumption' | translate }}</span>
      <strong>{{ currentWatts }} W</strong>
    </article>
    <article>
      <span>{{ 'nav.deviceGroups' | translate }}</span>
      <strong>{{ groupCount }}</strong>
    </article>
  </div>

  <div class="room-card__context">
    @if (primaryGroupName) {
      <p><span>{{ 'workplace.rooms.card.relatedGroup' | translate }}</span>{{ primaryGroupName }}</p>
    } @else {
      <p><span>{{ 'workplace.rooms.card.relatedGroup' | translate }}</span>{{ 'workplace.rooms.card.noLinkedGroup' | translate }}</p>
    }

    <p><span>{{ 'workplace.rooms.card.relatedRoutines' | translate }}</span>{{ routineCount }}</p>
  </div>

  <div class="room-card__actions">
    <app-button
      class="room-card__open-action"
      variant="ghost"
      size="sm"
      [label]="'workplace.rooms.card.goToAssignments' | translate"
      [link]="'/spaces/assignments'"
      [ariaLabel]="'workplace.rooms.card.goToAssignmentsAria' | translate"
    />
    <app-button variant="danger" size="sm" (pressed)="onRemove()">
      {{ 'common.delete' | translate }}
    </app-button>
  </div>
</app-section-card>


============================================================
FILE: src\app\workplace\presentation\components\room-card\room-card.component.scss
============================================================

:host {
  display: block;
}

.room-card ::ng-deep .section-card {
  grid-template-columns: minmax(190px, 0.85fr) minmax(300px, 1.25fr) minmax(210px, 0.72fr) auto;
  align-items: center;
  column-gap: 20px;
}

.room-card__identity,
.room-card__context,
.room-card__actions {
  min-width: 0;
}

.room-card__identity {
  display: grid;
  gap: 7px;
}

.room-card__identity > span {
  width: fit-content;
  padding: 7px 10px;
  border-radius: 999px;
  color: var(--accent-color);
  background: var(--ec-accent-soft);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.room-card h2,
.room-card p,
.room-card small {
  margin: 0;
}

.room-card h2 {
  color: var(--text-primary);
  font-size: 1.24rem;
  font-weight: 950;
}

.room-card p,
.room-card small {
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.room-card__metrics {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 10px;
  padding-left: 20px;
  border-left: 1px solid rgba(148, 163, 184, 0.18);
}

.room-card__metrics article,
.room-card__context p {
  padding: 11px;
  border: 1px solid var(--border-color);
  border-radius: 14px;
  background: var(--surface-card);
}

.room-card__metrics article {
  min-height: 72px;
}

.room-card__metrics span,
.room-card__context span {
  display: block;
  color: var(--text-muted);
  font-size: 0.68rem;
  font-weight: 950;
  text-transform: uppercase;
}

.room-card__metrics strong {
  display: block;
  margin-top: 7px;
  color: var(--text-primary);
  font-weight: 950;
}

.room-card__context {
  display: grid;
  gap: 10px;
  padding-left: 20px;
  border-left: 1px solid rgba(148, 163, 184, 0.18);
}

.room-card__context p {
  color: var(--text-primary);
  font-weight: 850;
}

.room-card__actions {
  display: grid;
  gap: 10px;
  justify-items: end;
  padding-left: 20px;
  border-left: 1px solid rgba(148, 163, 184, 0.18);
}

.room-card__open-action {
  min-width: 48px;
}

.room-card__actions ::ng-deep .room-card__open-action .app-button {
  min-width: 48px;
  min-height: 36px;
  padding-inline: 14px;
}

@media (max-width: 1180px) {
  .room-card ::ng-deep .section-card {
    grid-template-columns: 1fr;
  }

  .room-card__actions {
    justify-items: start;
  }

  .room-card__metrics,
  .room-card__context,
  .room-card__actions {
    padding-top: 18px;
    padding-left: 0;
    border-top: 1px solid rgba(148, 163, 184, 0.18);
    border-left: 0;
  }
}

@media (max-width: 720px) {
  .room-card__metrics {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}


============================================================
FILE: src\app\workplace\presentation\components\room-card\room-card.component.ts
============================================================

import { Component, EventEmitter, Input, Output } from '@angular/core';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { UiPreferencesService } from '../../../../shared/application/services/ui-preferences.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { SectionCardComponent } from '../../../../shared/presentation/components/section-card/section-card.component';
import { Room } from '../../../domain/model/room.entity';

const localeByLanguage: Record<string, string> = {
  es: 'es-PE',
  en: 'en-US',
  pt: 'pt-BR',
};

@Component({
  selector: 'app-room-card',
  standalone: true,
  imports: [TranslateModule, AppButtonComponent, SectionCardComponent],
  templateUrl: './room-card.component.html',
  styleUrls: ['./room-card.component.scss'],
})
export class RoomCardComponent {
  @Input({ required: true }) room!: Room;
  @Input() locationName = '';
  @Input() deviceCount = 0;
  @Input() activeDeviceCount = 0;
  @Input() groupCount = 0;
  @Input() routineCount = 0;
  @Input() currentWatts = 0;
  @Input() primaryGroupName = '';

  @Output() remove = new EventEmitter<number>();

  constructor(
    private readonly translate: TranslateService,
    private readonly uiPreferences: UiPreferencesService
  ) {}

  get statusLabel(): string {
    return this.translate.instant(this.statusLabelKey);
  }

  get statusLabelKey(): string {
    if (this.deviceCount === 0) {
      return 'workplace.rooms.status.noDevices';
    }

    if (this.activeDeviceCount > 0) {
      return 'workplace.rooms.status.active';
    }

    return 'workplace.rooms.status.idle';
  }

  get formattedCreatedAt(): string {
    if (!this.room.createdAt) {
      return this.translate.instant('workplace.rooms.card.noDate');
    }

    const date = new Date(this.room.createdAt);

    if (Number.isNaN(date.getTime())) {
      return this.room.createdAt;
    }

    return date.toLocaleDateString(this.currentLocale(), {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
    });
  }

  onRemove(): void {
    this.remove.emit(this.room.id);
  }

  private currentLocale(): string {
    return localeByLanguage[this.uiPreferences.currentLanguage()] ?? localeByLanguage['es'];
  }
}


============================================================
FILE: src\app\workplace\presentation\pages\device-assignments\device-assignments-page.component.html
============================================================

<section class="assignments-page ec-page">
  <header class="ec-page-heading">
    <p class="ec-page-eyebrow">{{ 'nav.spaces' | translate }}</p>
    <h1 class="ec-page-title">{{ 'nav.assignments' | translate }}</h1>
    <p class="ec-page-description">
      {{ 'workplace.assignments.pageDescription' | translate }}
    </p>
  </header>

  <section class="assignment-summary">
    <app-section-card
      size="small"
      [eyebrow]="'workplace.assignments.summary.activeLocationEyebrow' | translate"
      [title]="activeLocationName"
    >
      <strong>{{ activeRooms.length }}</strong>
      <p>{{ 'workplace.assignments.summary.availableRooms' | translate }}</p>
    </app-section-card>

    <app-section-card
      size="small"
      [eyebrow]="'nav.assignments' | translate"
      [title]="'workplace.assignments.summary.movableTitle' | translate"
    >
      <strong>{{ activeAssignments.length }}</strong>
      <p>{{ 'workplace.assignments.summary.connectedDevices' | translate }}</p>
    </app-section-card>

    <app-section-card
      size="small"
      [eyebrow]="'workplace.assignments.summary.interactionEyebrow' | translate"
      [title]="'workplace.assignments.summary.dragTitle' | translate"
    >
      <strong>{{ 'workplace.assignments.summary.directValue' | translate }}</strong>
      <p>{{ 'workplace.assignments.summary.dragDescription' | translate }}</p>
    </app-section-card>
  </section>

  <app-section-card class="movement-panel" size="large">
    <div class="movement-panel__header">
      <div>
        <span class="movement-panel__section-tag">{{ 'workplace.assignments.movement.eyebrow' | translate }}</span>
        <h2>{{ 'workplace.assignments.movement.title' | translate }}</h2>
        <p>
          {{ 'workplace.assignments.movement.description' | translate }}
        </p>
      </div>
    </div>

    <div class="movement-controls">
      <label>
        <span>{{ 'workplace.assignments.panels.roomOne' | translate }}</span>
        <app-dropdown
          [options]="roomOneDropdownOptions"
          [value]="roomOneValue"
          [placeholder]="'workplace.selectRoom' | translate"
          (valueChange)="selectRoomOne($event)"
        />
      </label>

      <label>
        <span>{{ 'workplace.assignments.panels.roomTwo' | translate }}</span>
        <app-dropdown
          [options]="roomTwoDropdownOptions"
          [value]="roomTwoValue"
          [placeholder]="'workplace.selectRoom' | translate"
          (valueChange)="selectRoomTwo($event)"
        />
      </label>
    </div>
  </app-section-card>

  @if (workplaceFacade.loading() || deviceControlFacade.loading()) {
    <div class="loading-wrapper">
      <app-loading-spinner />
    </div>
  } @else if (activeRooms.length === 0) {
    <app-empty-state
      [eyebrow]="'nav.rooms' | translate"
      [title]="'workplace.assignments.emptyRooms.title' | translate"
      [description]="'workplace.assignments.emptyRooms.description' | translate"
      [actionLabel]="'workplace.assignments.go' | translate"
      actionLink="/spaces/rooms"
    />
  } @else if (activeAssignments.length === 0) {
    <app-empty-state
      [eyebrow]="'nav.assignments' | translate"
      [title]="'workplace.assignments.emptyAssignments.title' | translate"
      [description]="'workplace.assignments.emptyAssignments.description' | translate"
      [actionLabel]="'workplace.assignments.go' | translate"
      actionLink="/operation/devices"
      actionVariant="neutral"
    />
  } @else {
    <section class="movement-board">
      @for (panel of roomPanels; track panel.labelKey) {
        <app-section-card
          class="movement-column"
          [class.movement-column--drop-active]="activeDropRoomId === panel.room?.id"
          size="medium"
          (dragover)="handleRoomDragOver(panel.room?.id ?? null, $event)"
          (drop)="handleRoomDrop(panel.room?.id ?? null, $event)"
        >
          <div class="movement-column__header">
            <span>{{ panel.labelKey | translate }}</span>
            <h2>{{ panel.room?.name ?? roomLabel(null) }}</h2>
            <p>
              @if (panel.room) {
                {{ 'workplace.assignments.panelItemCount' | translate: { count: panel.assignments.length } }}
              } @else {
                {{ 'workplace.assignments.noSecondRoomAvailable' | translate }}
              }
            </p>
          </div>

          <div
            class="movable-list room-drop-zone"
            [class.room-drop-zone--active]="activeDropRoomId === panel.room?.id"
          >
            @if (panel.room) {
              @for (assignment of panel.assignments; track assignment.id) {
                <article
                  class="movable-item"
                  [class.movable-item--dragging]="draggedAssignmentId === assignment.id"
                  draggable="true"
                  (dragstart)="handleDragStart(assignment, $event)"
                  (dragend)="handleDragEnd()"
                >
                  <span class="device-token" aria-hidden="true">
                    {{ deviceIcon(deviceById(assignment.deviceId)) }}
                  </span>

                  <div class="assignment-device-copy">
                    <strong>{{ deviceName(assignment.deviceId) }}</strong>
                    <span>{{ devicePowerLabel(assignment.deviceId) }}</span>
                    <small>{{ deviceStateLabel(deviceById(assignment.deviceId)) }}</small>
                  </div>
                </article>
              } @empty {
                <app-empty-state
                  [compact]="true"
                  [eyebrow]="panel.labelKey | translate"
                  [title]="'workplace.assignments.emptyPanel.title' | translate"
                  [description]="'workplace.assignments.emptyPanel.description' | translate"
                />
              }
            } @else {
              <app-empty-state
                [compact]="true"
                [eyebrow]="panel.labelKey | translate"
                [title]="'workplace.assignments.unavailablePanel.title' | translate"
                [description]="'workplace.assignments.unavailablePanel.description' | translate"
              />
            }
          </div>
        </app-section-card>
      }
    </section>
  }
</section>


============================================================
FILE: src\app\workplace\presentation\pages\device-assignments\device-assignments-page.component.scss
============================================================

.assignments-page {
  display: grid;
  gap: 24px;
  overflow: visible;
}

.assignment-summary {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;
  align-items: stretch;

  app-section-card {
    display: block;
    height: 100%;
  }

  ::ng-deep .section-card {
    height: 100%;
    grid-template-rows: auto auto minmax(0, 1fr);
  }

  strong {
    display: block;
    margin-bottom: 6px;
    color: var(--text-color);
    font-size: clamp(1.6rem, 3vw, 2.2rem);
    line-height: 1;
  }

  p {
    margin: 0;
    color: var(--ec-copy-color);
    font-weight: var(--ec-copy-weight);
    line-height: var(--ec-copy-line-height);
  }
}

.movement-panel {
  position: relative;
  z-index: 20;
  display: block;
  overflow: visible;

  ::ng-deep .section-card {
    position: relative;
    z-index: 20;
    overflow: visible;
    display: grid;
    gap: 20px;
  }
}

.movement-panel__header {
  display: flex;
  justify-content: space-between;
  gap: 16px;
  align-items: flex-start;

  h2 {
    margin: 0;
    color: var(--text-color);
    font-size: 1.3rem;
  }

  p {
    max-width: 680px;
    margin: 8px 0 0;
    color: var(--ec-copy-color);
    font-weight: var(--ec-copy-weight);
    line-height: var(--ec-copy-line-height);
  }
}

.movement-panel__section-tag {
  width: fit-content;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 22px;
  border-radius: 999px;
  padding: 5px 10px;
  color: var(--accent-color);
  background: color-mix(in srgb, var(--accent-color) 11%, transparent);
  font-size: 0.74rem;
  font-weight: 900;
  line-height: 1;
  letter-spacing: 0.04em;
  margin-bottom: 8px;
}

.movement-controls {
  position: relative;
  z-index: 30;
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14px;
  overflow: visible;

  label {
    position: relative;
    z-index: 1;
    display: grid;
    gap: 8px;
    min-width: 0;
    overflow: visible;
  }

  label:focus-within {
    z-index: 50;
  }

  label > span {
    color: var(--text-secondary);
    font-size: 0.78rem;
    font-weight: 900;
  }

  app-dropdown {
    position: relative;
    z-index: 2;
  }
}

.loading-wrapper {
  min-height: 220px;
  display: grid;
  place-items: center;
}

.movement-board {
  position: relative;
  z-index: 1;
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  grid-auto-rows: minmax(430px, 1fr);
  gap: 18px;
  align-items: stretch;
}

.movement-column {
  display: block;
  height: 100%;
  min-height: 430px;

  ::ng-deep .section-card {
    display: grid;
    grid-template-rows: auto minmax(0, 1fr);
    gap: 18px;
    height: 100%;
    min-height: 430px;
  }
}

.movement-column--drop-active {
  ::ng-deep .section-card {
    border-color: color-mix(in srgb, var(--accent-color) 28%, var(--border-color));
  }
}

.movement-column__header {
  display: grid;
  gap: 6px;

  > span {
    width: fit-content;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 22px;
    border-radius: 999px;
    padding: 5px 10px;
    color: var(--accent-color);
    background: color-mix(in srgb, var(--accent-color) 11%, transparent);
    font-size: 0.74rem;
    font-weight: 900;
    line-height: 1;
    letter-spacing: 0.04em;
    text-transform: none;
  }

  h2 {
    margin: 0;
    color: var(--text-color);
    font-size: 1.4rem;
  }

  p {
    margin: 0;
    color: var(--ec-copy-color);
    font-weight: var(--ec-copy-weight);
    line-height: var(--ec-copy-line-height);
  }
}

.movable-list {
  min-height: 0;
  height: 100%;
  max-height: min(52vh, 460px);
  display: grid;
  align-content: start;
  gap: 12px;
  padding: 12px;
  overflow: auto;
  border: 1px solid color-mix(in srgb, var(--border-color) 88%, var(--accent-color));
  border-radius: 18px;
  background:
    linear-gradient(180deg, transparent 62%, rgba(56, 189, 248, 0.07)),
    color-mix(in srgb, var(--app-bg) 44%, transparent);
}

.room-drop-zone {
  transition:
    border-color 180ms ease,
    box-shadow 180ms ease,
    background 180ms ease;
}

.room-drop-zone--active {
  border-color: color-mix(in srgb, var(--accent-color) 58%, var(--border-color));
  box-shadow: 0 0 0 4px color-mix(in srgb, var(--accent-color) 10%, transparent);
  background:
    linear-gradient(180deg, transparent 58%, rgba(56, 189, 248, 0.11)),
    color-mix(in srgb, var(--app-bg) 44%, transparent);
}

.movable-item {
  display: grid;
  grid-template-columns: auto minmax(0, 1fr);
  gap: 14px;
  align-items: center;
  min-height: 84px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  padding: 14px;
  background: color-mix(in srgb, var(--surface-color) 92%, var(--app-bg) 8%);
  cursor: grab;
  user-select: none;
  transition:
    border-color 180ms ease,
    background 180ms ease,
    box-shadow 180ms ease,
    transform 180ms ease,
    opacity 180ms ease;

  .assignment-device-copy {
    display: grid;
    gap: 5px;
    min-width: 0;
  }

  strong {
    color: var(--text-color);
    font-size: 0.98rem;
  }

  span {
    color: var(--ec-copy-color);
    font-size: 0.84rem;
    font-weight: var(--ec-copy-weight);
    line-height: var(--ec-copy-line-height);
  }

  small {
    color: var(--text-secondary);
    font-size: 0.78rem;
    font-weight: 850;
    line-height: 1.35;
  }
}

.movable-item:hover {
  border-color: color-mix(in srgb, var(--accent-color) 38%, var(--border-color));
  background: color-mix(in srgb, var(--accent-color) 8%, var(--surface-color));
  box-shadow: 0 16px 34px color-mix(in srgb, var(--accent-color) 10%, transparent);
}

.movable-item:active {
  cursor: grabbing;
}

.movable-item--dragging {
  opacity: 0.5;
  transform: scale(0.99);
}

.movable-item--selected {
  border-color: color-mix(in srgb, var(--accent-color) 55%, var(--border-color));
  background: color-mix(in srgb, var(--accent-color) 10%, var(--surface-color));
  box-shadow: 0 16px 34px color-mix(in srgb, var(--accent-color) 16%, transparent);
}

.device-token {
  width: 44px;
  height: 44px;
  display: inline-grid;
  place-items: center;
  border: 1px solid color-mix(in srgb, var(--accent-color) 22%, var(--border-color));
  border-radius: 16px;
  color: var(--text-color);
  background:
    radial-gradient(circle at 35% 20%, var(--ec-accent-soft), transparent 58%),
    color-mix(in srgb, var(--accent-color) 14%, var(--surface-color));
  font-size: 1.24rem;
  font-weight: 950;
  line-height: 1;
  box-shadow: var(--ec-inset-highlight);
}

@media (max-width: 940px) {
  .assignment-summary,
  .movement-controls,
  .movement-board {
    grid-template-columns: 1fr;
  }

  .movement-column ::ng-deep .section-card {
    height: auto;
    min-height: 0;
  }

  .movement-column {
    min-height: 0;
  }

  .movable-list {
    max-height: none;
  }
}

@media (max-width: 640px) {
  .movable-item {
    grid-template-columns: 1fr;
    align-items: flex-start;
  }
}


============================================================
FILE: src\app\workplace\presentation\pages\device-assignments\device-assignments-page.component.ts
============================================================

import { Component, OnInit } from '@angular/core';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { DeviceControlFacade } from '../../../../device-control/application/services/device-control.facade';
import { Device } from '../../../../device-control/domain/model/device.entity';
import { Routine } from '../../../../device-control/domain/model/routine.entity';
import { ConfirmDialogOptions, ConfirmDialogService } from '../../../../shared/application/services/confirm-dialog.service';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { SectionCardComponent } from '../../../../shared/presentation/components/section-card/section-card.component';
import { ActiveWorkplaceContextService } from '../../../application/services/active-workplace-context.service';
import { WorkplaceFacade } from '../../../application/services/workplace.facade';
import { DeviceAssignment } from '../../../domain/model/device-assignment.entity';
import { Room } from '../../../domain/model/room.entity';

interface RoomPanel {
  labelKey: string;
  room: Room | null;
  assignments: DeviceAssignment[];
}

@Component({
  selector: 'app-device-assignments-page',
  standalone: true,
  imports: [
    TranslateModule,
    AppDropdownComponent,
    EmptyStateComponent,
    LoadingSpinnerComponent,
    SectionCardComponent,
  ],
  templateUrl: './device-assignments-page.component.html',
  styleUrls: ['./device-assignments-page.component.scss'],
})
export class DeviceAssignmentsPageComponent implements OnInit {
  roomOneValue: string | null = null;
  roomTwoValue: string | null = null;
  draggedAssignmentId: number | null = null;
  activeDropRoomId: number | null = null;

  constructor(
    readonly workplaceFacade: WorkplaceFacade,
    readonly deviceControlFacade: DeviceControlFacade,
    private readonly activeWorkplaceContext: ActiveWorkplaceContextService,
    private readonly toastService: ToastService,
    private readonly confirmDialog: ConfirmDialogService,
    private readonly translate: TranslateService
  ) {}

  async ngOnInit(): Promise<void> {
    await Promise.all([
      this.workplaceFacade.loadWorkplace(),
      this.deviceControlFacade.loadDeviceControl(),
    ]);

    this.activeWorkplaceContext.ensureActiveLocation(this.workplaceFacade.locations());
    this.initializeRoomSelection();

    if (this.workplaceFacade.error()) {
      this.toastService.error(this.t('workplace.assignments.toasts.loadError'));
    }
  }

  get activeLocationId(): number | null {
    return this.activeWorkplaceContext.activeLocationId();
  }

  get activeLocationName(): string {
    const locationId = this.activeLocationId;
    return locationId
      ? this.workplaceFacade.getLocationName(locationId)
      : this.t('workplace.assignments.activeLocationFallback');
  }

  get activeRooms() {
    const locationId = this.activeLocationId;
    return locationId
      ? this.workplaceFacade.rooms().filter((room) => room.locationId === locationId)
      : [];
  }

  get activeAssignments() {
    const locationId = this.activeLocationId;

    if (!locationId) {
      return [];
    }

    const visibleDeviceIds = new Set(
      this.deviceControlFacade
        .devices()
        .filter((device) => !device.isRemoved)
        .map((device) => device.id)
    );

    return [...this.currentAssignmentsByDevice().values()]
      .filter((assignment) =>
        assignment.locationId === locationId && visibleDeviceIds.has(assignment.deviceId)
      )
      .sort((first, second) =>
        this.assignmentTimestamp(second) - this.assignmentTimestamp(first) ||
        second.id - first.id
      );
  }

  get roomOneDropdownOptions(): DropdownOption[] {
    return this.roomSelectionOptions(this.roomTwoValue);
  }

  get roomTwoDropdownOptions(): DropdownOption[] {
    const options = this.roomSelectionOptions(this.roomOneValue);

    if (options.length === 0) {
      return [
        {
          label: 'no-second-room',
          labelKey: 'workplace.assignments.noSecondRoomOption',
          value: '__NO_SECOND_ROOM__',
          emptyState: true,
          disabled: true,
        },
      ];
    }

    return options;
  }

  get roomOne(): Room | null {
    return this.roomFromValue(this.roomOneValue);
  }

  get roomTwo(): Room | null {
    return this.roomFromValue(this.roomTwoValue);
  }

  selectRoomOne(value: string): void {
    if (!this.roomFromValue(value)) {
      return;
    }

    this.roomOneValue = value;

    if (this.roomTwoValue === value) {
      this.roomTwoValue = this.firstAvailableRoomValue(value);
    }

    this.clearDragState();
  }

  selectRoomTwo(value: string): void {
    if (!this.roomFromValue(value)) {
      return;
    }

    this.roomTwoValue = value;

    if (this.roomOneValue === value) {
      this.roomOneValue = this.firstAvailableRoomValue(value);
    }

    this.clearDragState();
  }

  get roomPanels(): RoomPanel[] {
    return [
      {
        labelKey: 'workplace.assignments.panels.roomOne',
        room: this.roomOne,
        assignments: this.roomOne ? this.assignmentsForRoom(this.roomOne.id) : [],
      },
      {
        labelKey: 'workplace.assignments.panels.roomTwo',
        room: this.roomTwo,
        assignments: this.roomTwo ? this.assignmentsForRoom(this.roomTwo.id) : [],
      },
    ];
  }

  assignmentsForRoom(roomId: number | null): DeviceAssignment[] {
    if (roomId === null) {
      return [];
    }

    return this.activeAssignments.filter((assignment) => assignment.roomId === roomId);
  }

  handleDragStart(assignment: DeviceAssignment, event: DragEvent): void {
    this.draggedAssignmentId = assignment.id;
    this.activeDropRoomId = null;

    if (event.dataTransfer) {
      event.dataTransfer.effectAllowed = 'move';
      event.dataTransfer.setData('text/plain', String(assignment.id));
    }
  }

  handleDragEnd(): void {
    this.clearDragState();
  }

  handleRoomDragOver(roomId: number | null, event: DragEvent): void {
    if (roomId === null || this.draggedAssignmentId === null) {
      return;
    }

    event.preventDefault();

    if (event.dataTransfer) {
      event.dataTransfer.dropEffect = 'move';
    }

    this.activeDropRoomId = roomId;
  }

  async handleRoomDrop(roomId: number | null, event: DragEvent): Promise<void> {
    event.preventDefault();

    const assignment = this.draggedAssignment(event);

    if (!assignment || roomId === null || assignment.roomId === roomId) {
      this.clearDragState();
      return;
    }

    try {
      await this.moveAssignmentToRoom(assignment, roomId);
    } finally {
      this.clearDragState();
    }
  }

  deviceName(deviceId: number): string {
    return this.workplaceFacade.getDeviceName(deviceId);
  }

  deviceById(deviceId: number): Device | null {
    return this.deviceControlFacade.devices().find((device) => device.id === deviceId) ?? null;
  }

  deviceIcon(device: Device | null): string {
    if (!device) {
      return '⚡';
    }

    const icons: Record<Device['type'], string> = {
      PLUG: '🔌',
      LIGHT: '💡',
      SWITCH: '🔘',
      SENSOR: '📡',
      OTHER: '⚡',
    };

    return icons[device.type] ?? '⚡';
  }

  deviceTypeLabel(type: Device['type']): string {
    const labelKeys: Record<Device['type'], string> = {
      PLUG: 'workplace.deviceTypes.plug',
      LIGHT: 'workplace.deviceTypes.light',
      SWITCH: 'workplace.deviceTypes.switch',
      SENSOR: 'workplace.deviceTypes.sensor',
      OTHER: 'workplace.deviceTypes.other',
    };

    return this.t(labelKeys[type]);
  }

  devicePowerLabel(deviceId: number): string {
    const device = this.deviceById(deviceId);

    if (!device) {
      return this.t('workplace.assignments.device.noPowerInfo');
    }

    return `${this.deviceTypeLabel(device.type)} - ${device.powerWatts} W`;
  }

  deviceStateLabel(device: Device | null): string {
    if (!device) {
      return this.t('workplace.assignments.device.notAvailable');
    }

    if (device.isOn) {
      return this.t('workplace.assignments.device.on');
    }

    if (device.isInMaintenance) {
      return this.t('workplace.assignments.device.maintenance');
    }

    return this.t('workplace.assignments.device.off');
  }

  roomLabel(value: string | null): string {
    if (!value || value === 'none') {
      return this.t('workplace.assignments.noRoom');
    }

    return this.workplaceFacade.getRoomName(Number(value));
  }

  private initializeRoomSelection(): void {
    this.roomOneValue = this.activeRooms[0] ? String(this.activeRooms[0].id) : null;
    this.roomTwoValue = this.firstAvailableRoomValue(this.roomOneValue);
    this.clearDragState();
  }

  private roomSelectionOptions(excludedValue: string | null): DropdownOption[] {
    return this.activeRooms
      .filter((room) => String(room.id) !== excludedValue)
      .map((room) => ({
        label: room.name,
        value: String(room.id),
        description: this.activeLocationName,
      }));
  }

  private roomFromValue(value: string | null): Room | null {
    if (!value) {
      return null;
    }

    return this.activeRooms.find((room) => String(room.id) === value) ?? null;
  }

  private firstAvailableRoomValue(excludedValue: string | null): string | null {
    const room = this.activeRooms.find((item) => String(item.id) !== excludedValue);
    return room ? String(room.id) : null;
  }

  private currentAssignmentsByDevice(): Map<number, DeviceAssignment> {
    const currentAssignments = new Map<number, DeviceAssignment>();

    for (const assignment of this.workplaceFacade.deviceAssignments()) {
      const current = currentAssignments.get(assignment.deviceId);

      if (!current || this.isNewerAssignment(assignment, current)) {
        currentAssignments.set(assignment.deviceId, assignment);
      }
    }

    return currentAssignments;
  }

  private isNewerAssignment(candidate: DeviceAssignment, current: DeviceAssignment): boolean {
    const candidateTime = this.assignmentTimestamp(candidate);
    const currentTime = this.assignmentTimestamp(current);

    return candidateTime > currentTime ||
      (candidateTime === currentTime && candidate.id > current.id);
  }

  private assignmentTimestamp(assignment: DeviceAssignment): number {
    const assignedAt = Date.parse(assignment.assignedAt);
    return Number.isNaN(assignedAt) ? 0 : assignedAt;
  }

  private async moveAssignmentToRoom(assignment: DeviceAssignment, destinationRoomId: number): Promise<void> {
    const warnings = this.buildImpactWarnings(assignment, assignment.roomId);

    if (warnings.length > 0) {
      const accepted = await this.confirmDialog.confirmSequence(warnings);

      if (!accepted) {
        return;
      }
    }

    const moved = await this.workplaceFacade.moveDeviceAssignment({
      assignmentId: assignment.id,
      locationId: assignment.locationId,
      roomId: destinationRoomId,
    });

    if (moved) {
      this.toastService.success(this.t('workplace.assignments.toasts.moveSuccess'));
      return;
    }

    this.toastService.error(this.t('workplace.assignments.toasts.moveError'));
  }

  private buildImpactWarnings(assignment: DeviceAssignment, originRoomId: number | null): ConfirmDialogOptions[] {
    const deviceIds = new Set<number>([assignment.deviceId]);
    const groupIds = this.relatedGroupIds([assignment.deviceId]);
    const warnings: ConfirmDialogOptions[] = [];

    const groupNames = this.deviceControlFacade
      .deviceGroups()
      .filter((group) => group.deviceIds.some((deviceId) => deviceId === assignment.deviceId))
      .map((group) => group.name);

    for (const groupName of groupNames) {
      warnings.push({
        title: this.t('workplace.assignments.warnings.groupTitle'),
        message: this.t('workplace.assignments.warnings.groupMessage', { group: groupName }),
        confirmLabel: this.t('common.continue'),
        cancelLabel: this.t('common.cancel'),
        tone: 'warning',
      });
    }

    if (this.hasRoutineImpact(deviceIds, groupIds, originRoomId)) {
      warnings.push({
        title: this.t('workplace.assignments.warnings.routinesTitle'),
        message: this.t('workplace.assignments.warnings.routinesMessage'),
        confirmLabel: this.t('common.continue'),
        cancelLabel: this.t('common.cancel'),
        tone: 'warning',
      });
    }

    if (this.hasModeImpact(deviceIds, groupIds, originRoomId)) {
      warnings.push({
        title: this.t('workplace.assignments.warnings.modesTitle'),
        message: this.t('workplace.assignments.warnings.modesMessage'),
        confirmLabel: this.t('common.continue'),
        cancelLabel: this.t('common.cancel'),
        tone: 'warning',
      });
    }

    return warnings;
  }

  private relatedGroupIds(deviceIds: number[]): Set<number> {
    const ids = new Set(deviceIds);
    return new Set(
      this.deviceControlFacade
        .deviceGroups()
        .filter((group) => group.deviceIds.some((deviceId) => ids.has(deviceId)))
        .map((group) => group.id)
    );
  }

  private hasRoutineImpact(deviceIds: Set<number>, groupIds: Set<number>, originRoomId: number | null): boolean {
    return this.deviceControlFacade.routines().some((routine) =>
      routine.enabled && this.routineImpactsMovement(routine, deviceIds, groupIds, originRoomId)
    );
  }

  private routineImpactsMovement(
    routine: Routine,
    deviceIds: Set<number>,
    groupIds: Set<number>,
    originRoomId: number | null
  ): boolean {
    if (routine.targetType === 'DEVICE') {
      return Boolean(routine.deviceId && deviceIds.has(routine.deviceId)) || deviceIds.has(routine.targetId);
    }

    if (routine.targetType === 'GROUP') {
      return Boolean(routine.groupId && groupIds.has(routine.groupId)) || groupIds.has(routine.targetId);
    }

    if (routine.targetType === 'ROOM') {
      return originRoomId !== null && routine.targetId === originRoomId;
    }

    return false;
  }

  private hasModeImpact(deviceIds: Set<number>, groupIds: Set<number>, originRoomId: number | null): boolean {
    const locationId = this.activeLocationId;

    return this.deviceControlFacade.operationModes().some((mode) => {
      if (!locationId || mode.locationId !== locationId || mode.status === 'ARCHIVED') {
        return false;
      }

      const modeDeviceIds = [
        ...mode.deviceIds,
        ...mode.turnOnDeviceIds,
        ...mode.turnOffDeviceIds,
        ...mode.keepOnDeviceIds,
      ];

      return (
        modeDeviceIds.some((deviceId) => deviceIds.has(deviceId)) ||
        mode.groupIds.some((groupId) => groupIds.has(groupId)) ||
        (originRoomId !== null && mode.roomIds.includes(originRoomId))
      );
    });
  }

  private draggedAssignment(event: DragEvent): DeviceAssignment | null {
    const rawId = this.draggedAssignmentId ?? Number(event.dataTransfer?.getData('text/plain') ?? '');

    if (!Number.isFinite(rawId) || rawId <= 0) {
      return null;
    }

    return this.activeAssignments.find((assignment) => assignment.id === rawId) ?? null;
  }

  private clearDragState(): void {
    this.draggedAssignmentId = null;
    this.activeDropRoomId = null;
  }

  private t(key: string, params?: Record<string, unknown>): string {
    return this.translate.instant(key, params);
  }
}


============================================================
FILE: src\app\workplace\presentation\pages\locations\locations-page.component.html
============================================================

<section class="workplace-page ec-page">
  <header class="ec-page-heading">
    <p class="ec-page-eyebrow">{{ 'nav.spaces' | translate }}</p>
    <h1 class="ec-page-title">{{ 'nav.locations' | translate }}</h1>
    <p class="ec-page-description">
      {{ 'workplace.locations.pageDescription' | translate }}
    </p>
  </header>

  <section class="workplace-summary">
    <app-section-card
      size="small"
      [eyebrow]="'nav.locations' | translate"
      [title]="'workplace.locations.summary.registeredTitle' | translate"
    >
      <strong>{{ workplaceFacade.locations().length }}</strong>
      <p>{{ 'workplace.locations.summary.registeredDescription' | translate }}</p>
    </app-section-card>

    <app-section-card
      size="small"
      [eyebrow]="'nav.devices' | translate"
      [title]="'workplace.locations.summary.assignedTitle' | translate"
    >
      <strong>{{ assignedDeviceCount }}</strong>
      <p>{{ 'workplace.locations.summary.unassignedDevices' | translate: { count: unassignedDeviceCount } }}</p>
    </app-section-card>

    <app-section-card
      size="small"
      [eyebrow]="'workplace.locations.summary.coverageEyebrow' | translate"
      [title]="'nav.rooms' | translate"
    >
      <strong>{{ energyCoverage }}%</strong>
      <p>{{ 'workplace.locations.summary.unroomedDevices' | translate: { count: unroomedDeviceCount } }}</p>
    </app-section-card>
  </section>

  <section class="workplace-toolbar">
    <div>
      <p class="section-eyebrow">{{ 'workplace.locations.toolbar.eyebrow' | translate }}</p>
      <h2>{{ 'workplace.locations.toolbar.title' | translate }}</h2>
      <p>{{ 'workplace.locations.toolbar.description' | translate }}</p>
    </div>

    <app-button variant="create" (pressed)="openCreateModal()">{{ 'onboarding.createLocationAction' | translate }}</app-button>
  </section>

  @if (workplaceFacade.loading()) {
    <div class="loading-wrapper">
      <app-loading-spinner />
    </div>
  } @else {
    <section class="workplace-list">
      @for (location of workplaceFacade.locations(); track location.id) {
        <app-location-card
          [location]="location"
          [deviceCount]="deviceCountForLocation(location.id)"
          [roomCount]="roomCountForLocation(location.id)"
          [groupCount]="groupCountForLocation(location.id)"
          [unroomedDeviceCount]="unroomedDeviceCountForLocation(location.id)"
          [activeWatts]="activeWattsForLocation(location.id)"
          [coverage]="coverageForLocation(location.id)"
          (selected)="setActiveLocation($event)"
          (remove)="deleteLocation($event)"
        />
      } @empty {
        <app-empty-state
          [eyebrow]="'nav.locations' | translate"
          [title]="'workplace.locations.empty.title' | translate"
          [description]="'workplace.locations.empty.description' | translate"
          [actionLabel]="'onboarding.createLocationAction' | translate"
          actionLink="/spaces/sites"
        />
      }
    </section>
  }

  <app-modal-form-shell
    [open]="createModalOpen"
    [eyebrow]="'workplace.locations.form.eyebrow' | translate"
    [title]="'onboarding.createLocationAction' | translate"
    [description]="'workplace.locations.form.description' | translate"
    (closed)="closeCreateModal()"
  >
    <form class="modal-form" (ngSubmit)="createLocation()">
      <label>
        <span>{{ 'workplace.locationName' | translate }}</span>
        <input
          type="text"
          name="name"
          [(ngModel)]="name"
          [placeholder]="'workplace.locations.form.namePlaceholder' | translate"
          required
        />
      </label>

      <label>
        <span>{{ 'workplace.locations.form.typeLabel' | translate }}</span>
        <app-dropdown
          [options]="typeOptions"
          [value]="type"
          [placeholder]="'workplace.locations.form.typePlaceholder' | translate"
          (valueChange)="selectType($event)"
        />
      </label>

      <label class="field-wide">
        <span>{{ 'workplace.locations.form.addressLabel' | translate }}</span>
        <input
          type="text"
          name="address"
          [(ngModel)]="address"
          [placeholder]="'workplace.locations.form.addressPlaceholder' | translate"
          required
        />
      </label>

      <section class="map-field field-wide">
        <div class="map-field__copy">
          <span>{{ 'workplace.locations.map.title' | translate }}</span>
          <p>
            {{ 'workplace.locations.map.description' | translate }}
          </p>
        </div>

        <div class="map-picker">
          <iframe
            [title]="'workplace.locations.map.iframeTitle' | translate"
            [src]="mapPreviewUrl"
            loading="lazy"
          ></iframe>
          <div
            class="map-picker__surface"
            role="button"
            tabindex="0"
            [attr.aria-label]="'workplace.locations.map.surfaceLabel' | translate"
            (click)="selectMapPoint($event)"
          >
            <span
              class="map-picker__marker"
              [style.left.%]="markerX"
              [style.top.%]="markerY"
            ></span>
          </div>
        </div>

        <p class="coordinate-note">
          {{ 'workplace.locations.map.selectedLabel' | translate }}:
          <strong>{{ selectedCoordinateLabel }}</strong>
        </p>
      </section>

      <div class="form-actions field-wide">
        <app-button variant="neutral" (pressed)="closeCreateModal()">{{ 'common.cancel' | translate }}</app-button>
        <app-button type="submit" variant="create" [disabled]="workplaceFacade.loading()">
          {{ 'workplace.createLocation' | translate }}
        </app-button>
      </div>
    </form>
  </app-modal-form-shell>
</section>


============================================================
FILE: src\app\workplace\presentation\pages\locations\locations-page.component.scss
============================================================

.workplace-page {
  gap: 30px;
}

.workplace-summary,
.workplace-list {
  display: grid;
  gap: 14px;
}

.workplace-summary {
  grid-template-columns: repeat(3, minmax(0, 1fr));
}

.workplace-summary strong {
  color: var(--text-primary);
  font-size: 1.75rem;
  font-weight: 950;
}

.workplace-toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  padding: 22px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background:
    var(--ec-card-highlight),
    var(--ec-section-bg);
  box-shadow: var(--ec-shadow-soft);
}

.workplace-toolbar h2,
.workplace-toolbar p,
.section-eyebrow {
  margin: 0;
}

.workplace-toolbar h2 {
  color: var(--text-primary);
  font-size: 1.2rem;
  font-weight: 950;
}

.workplace-toolbar p:not(.section-eyebrow),
.map-field__copy p,
.coordinate-note {
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.section-eyebrow,
.map-field__copy span,
.modal-form label > span {
  color: var(--accent-color);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.loading-wrapper {
  display: grid;
  place-items: center;
  min-height: 220px;
}

.modal-form {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
}

.field-wide {
  grid-column: 1 / -1;
}

.modal-form label,
.map-field {
  display: grid;
  gap: 8px;
  min-width: 0;
}

.modal-form input {
  width: 100%;
  min-height: 46px;
  border: 1px solid var(--border-color);
  border-radius: 12px;
  padding: 0 14px;
  color: var(--text-primary);
  background: var(--ec-input-bg);
  font: inherit;
  font-weight: 850;
  outline: none;
}

.modal-form input:focus {
  border-color: var(--accent-color);
}

.map-field {
  padding: 14px;
  border: 1px solid var(--border-color);
  border-radius: 16px;
  background: var(--surface-card);
}

.map-field__copy {
  display: grid;
  gap: 6px;
}

.map-field__copy p,
.coordinate-note {
  margin: 0;
}

.map-picker {
  position: relative;
  overflow: hidden;
  min-height: 230px;
  border: 1px solid var(--border-color);
  border-radius: 14px;
  background: var(--surface-color);
}

.map-picker iframe,
.map-picker__surface {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
}

.map-picker iframe {
  border: 0;
  pointer-events: none;
}

.map-picker__surface {
  border: 0;
  background:
    linear-gradient(90deg, var(--border-color) 1px, transparent 1px),
    linear-gradient(0deg, var(--border-color) 1px, transparent 1px);
  background-size: 44px 44px;
  cursor: crosshair;
}

.map-picker__marker {
  position: absolute;
  width: 18px;
  height: 18px;
  border: 3px solid var(--surface-elevated-solid);
  border-radius: 50%;
  background: var(--accent-color);
  box-shadow: var(--ec-shadow-soft);
  transform: translate(-50%, -50%);
}

.coordinate-note strong {
  color: var(--text-primary);
}

.form-actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
}

@media (max-width: 900px) {
  .workplace-summary,
  .modal-form {
    grid-template-columns: 1fr;
  }

  .workplace-toolbar,
  .form-actions {
    align-items: stretch;
    flex-direction: column;
  }
}


============================================================
FILE: src\app\workplace\presentation\pages\locations\locations-page.component.ts
============================================================

import { Component, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { DomSanitizer, SafeResourceUrl } from '@angular/platform-browser';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { DeviceControlFacade } from '../../../../device-control/application/services/device-control.facade';
import { ConfirmDialogService } from '../../../../shared/application/services/confirm-dialog.service';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { AppDropdownComponent } from '../../../../shared/presentation/components/app-dropdown/app-dropdown.component';
import { DropdownOption } from '../../../../shared/presentation/components/app-dropdown/dropdown-option.model';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { ModalFormShellComponent } from '../../../../shared/presentation/components/modal-form-shell/modal-form-shell.component';
import { SectionCardComponent } from '../../../../shared/presentation/components/section-card/section-card.component';
import { ActiveWorkplaceContextService } from '../../../application/services/active-workplace-context.service';
import { WorkplaceFacade } from '../../../application/services/workplace.facade';
import { LocationType } from '../../../domain/model/location.entity';
import { LocationCardComponent } from '../../components/location-card/location-card.component';

@Component({
  selector: 'app-locations-page',
  standalone: true,
  imports: [
    FormsModule,
    TranslateModule,
    AppButtonComponent,
    AppDropdownComponent,
    EmptyStateComponent,
    LoadingSpinnerComponent,
    LocationCardComponent,
    ModalFormShellComponent,
    SectionCardComponent,
  ],
  templateUrl: './locations-page.component.html',
  styleUrls: ['./locations-page.component.scss'],
})
export class LocationsPageComponent implements OnInit {
  name = '';
  address = '';
  type: LocationType = 'HOME';
  createModalOpen = false;
  selectedLatitude = -12.0464;
  selectedLongitude = -77.0428;
  markerX = 50;
  markerY = 50;

  readonly types: LocationType[] = ['HOME', 'BUSINESS', 'BRANCH'];
  private readonly mapCenterLatitude = -12.0464;
  private readonly mapCenterLongitude = -77.0428;
  private readonly mapHalfSpan = 0.045;

  constructor(
    readonly workplaceFacade: WorkplaceFacade,
    readonly deviceControlFacade: DeviceControlFacade,
    private readonly activeWorkplaceContext: ActiveWorkplaceContextService,
    private readonly toastService: ToastService,
    private readonly confirmDialog: ConfirmDialogService,
    private readonly translate: TranslateService,
    private readonly sanitizer: DomSanitizer
  ) {}

  async ngOnInit(): Promise<void> {
    await this.workplaceFacade.loadWorkplace();
  }

  get typeOptions(): DropdownOption[] {
    return this.types.map((type) => ({
      label: type,
      labelKey: this.locationTypeLabelKey(type),
      value: type,
    }));
  }

  get assignedDeviceCount(): number {
    return this.workplaceFacade.deviceAssignments().length;
  }

  get unassignedDeviceCount(): number {
    return Math.max(this.deviceControlFacade.devices().length - this.assignedDeviceCount, 0);
  }

  get unroomedDeviceCount(): number {
    const devicesWithRoom = new Set(
      this.workplaceFacade
        .deviceAssignments()
        .filter((assignment) => Boolean(assignment.roomId))
        .map((assignment) => assignment.deviceId)
    );

    return Math.max(this.deviceControlFacade.devices().length - devicesWithRoom.size, 0);
  }

  get energyCoverage(): number {
    const totalDevices = this.deviceControlFacade.devices().length;

    if (totalDevices === 0) {
      return 0;
    }

    return Math.round((this.assignedDeviceCount / totalDevices) * 100);
  }

  get selectedCoordinateLabel(): string {
    return `${this.selectedLatitude.toFixed(5)}, ${this.selectedLongitude.toFixed(5)}`;
  }

  get mapPreviewUrl(): SafeResourceUrl {
    const west = this.mapCenterLongitude - this.mapHalfSpan;
    const east = this.mapCenterLongitude + this.mapHalfSpan;
    const south = this.mapCenterLatitude - this.mapHalfSpan;
    const north = this.mapCenterLatitude + this.mapHalfSpan;
    const url = `https://www.openstreetmap.org/export/embed.html?bbox=${west}%2C${south}%2C${east}%2C${north}&layer=mapnik&marker=${this.selectedLatitude}%2C${this.selectedLongitude}`;

    return this.sanitizer.bypassSecurityTrustResourceUrl(url);
  }

  openCreateModal(): void {
    this.createModalOpen = true;
  }

  closeCreateModal(): void {
    this.createModalOpen = false;
  }

  async createLocation(): Promise<void> {
    const success = await this.workplaceFacade.createLocation({
      name: this.name,
      address: this.address,
      type: this.type,
    });

    if (success) {
      this.name = '';
      this.address = '';
      this.type = 'HOME';
      this.closeCreateModal();
      this.toastService.success(this.t('workplace.locationCreateSuccess'));
      return;
    }

    this.toastService.error(this.t('workplace.locationCreateError'));
  }

  async deleteLocation(locationId: number): Promise<void> {
    const locationName = this.workplaceFacade.getLocationName(locationId);
    const confirmed = await this.confirmDialog.confirm({
      title: this.t('workplace.locations.deleteTitle'),
      message: this.t('workplace.locations.deleteMessage', { name: locationName }),
      confirmLabel: this.t('common.delete'),
      cancelLabel: this.t('common.cancel'),
      tone: 'danger',
    });

    if (!confirmed) {
      return;
    }

    const success = await this.workplaceFacade.deleteLocation(locationId);

    if (success) {
      this.toastService.info(this.t('workplace.locations.deleteSuccess'));
    }
  }

  setActiveLocation(locationId: number): void {
    this.activeWorkplaceContext.setActiveLocation(locationId);
  }

  locationTypeLabel(type: LocationType): string {
    return this.t(this.locationTypeLabelKey(type));
  }

  locationTypeLabelKey(type: LocationType): string {
    const labels: Record<LocationType, string> = {
      HOME: 'workplace.locationTypes.home',
      BUSINESS: 'workplace.locationTypes.business',
      BRANCH: 'workplace.locationTypes.branch',
    };

    return labels[type] ?? type;
  }

  selectType(value: string): void {
    this.type = value as LocationType;
  }

  selectMapPoint(event: MouseEvent): void {
    const target = event.currentTarget as HTMLElement;
    const rect = target.getBoundingClientRect();
    const x = Math.min(Math.max((event.clientX - rect.left) / rect.width, 0), 1);
    const y = Math.min(Math.max((event.clientY - rect.top) / rect.height, 0), 1);

    this.markerX = x * 100;
    this.markerY = y * 100;
    this.selectedLongitude = this.mapCenterLongitude + (x - 0.5) * this.mapHalfSpan * 2;
    this.selectedLatitude = this.mapCenterLatitude + (0.5 - y) * this.mapHalfSpan * 2;

    const selectedReference = this.t('workplace.locations.map.selectedAddress', {
      coordinates: this.selectedCoordinateLabel,
    });

    if (!this.address.trim() || this.isGeneratedMapReference(this.address)) {
      this.address = selectedReference;
    }
  }

  deviceCountForLocation(locationId: number): number {
    return this.assignmentsForLocation(locationId).length;
  }

  roomCountForLocation(locationId: number): number {
    return this.workplaceFacade.rooms().filter((room) => room.locationId === locationId).length;
  }

  groupCountForLocation(locationId: number): number {
    const deviceIds = new Set(
      this.assignmentsForLocation(locationId).map((assignment) => assignment.deviceId)
    );

    return this.deviceControlFacade
      .deviceGroups()
      .filter((group) => group.deviceIds.some((deviceId) => deviceIds.has(deviceId)))
      .length;
  }

  unroomedDeviceCountForLocation(locationId: number): number {
    return this.assignmentsForLocation(locationId).filter((assignment) => !assignment.hasRoom).length;
  }

  activeWattsForLocation(locationId: number): number {
    const deviceIds = new Set(
      this.assignmentsForLocation(locationId).map((assignment) => assignment.deviceId)
    );

    return this.deviceControlFacade
      .devices()
      .filter((device) => deviceIds.has(device.id) && device.isOn)
      .reduce((total, device) => total + device.powerWatts, 0);
  }

  coverageForLocation(locationId: number): number {
    const assignments = this.assignmentsForLocation(locationId);

    if (assignments.length === 0) {
      return 0;
    }

    const roomedDevices = assignments.filter((assignment) => assignment.hasRoom).length;
    return Math.round((roomedDevices / assignments.length) * 100);
  }

  private assignmentsForLocation(locationId: number) {
    return this.workplaceFacade
      .deviceAssignments()
      .filter((assignment) => assignment.locationId === locationId);
  }

  private isGeneratedMapReference(value: string): boolean {
    const normalizedValue = value.trim().toLowerCase();
    return [
      'ubicacion seleccionada',
      'ubicación seleccionada',
      'selected location',
      'localizacao selecionada',
      'localização selecionada',
    ].some((prefix) => normalizedValue.startsWith(prefix));
  }

  private t(key: string, params?: Record<string, unknown>): string {
    return this.translate.instant(key, params);
  }
}


============================================================
FILE: src\app\workplace\presentation\pages\rooms\rooms-page.component.html
============================================================

<section class="rooms-page ec-page">
  <header class="ec-page-heading">
    <p class="ec-page-eyebrow">{{ 'nav.spaces' | translate }}</p>
    <h1 class="ec-page-title">{{ 'nav.rooms' | translate }}</h1>
    <p class="ec-page-description">
      {{ 'workplace.rooms.pageDescription' | translate }}
    </p>
  </header>

  <section class="rooms-summary">
    <app-section-card
      class="rooms-summary-card"
      size="small"
      [eyebrow]="'workplace.rooms.summary.activeLocationEyebrow' | translate"
      [title]="activeLocationName"
    >
      <strong>{{ activeRooms.length }}</strong>
      <p>{{ 'workplace.rooms.summary.availableRooms' | translate }}</p>
    </app-section-card>

    <app-section-card
      class="rooms-summary-card"
      size="small"
      [eyebrow]="'nav.rooms' | translate"
      [title]="'workplace.rooms.summary.registeredTitle' | translate"
    >
      <strong>{{ activeRooms.length }}</strong>
      <p>{{ 'workplace.rooms.summary.registeredDescription' | translate }}</p>
    </app-section-card>

    <app-section-card
      class="rooms-summary-card"
      size="small"
      [eyebrow]="'nav.devices' | translate"
      [title]="'workplace.rooms.summary.assignedTitle' | translate"
    >
      <strong>{{ activeDeviceCount }}</strong>
      <p>{{ 'workplace.rooms.summary.linkedGroups' | translate: { count: activeGroupCount } }}</p>
    </app-section-card>
  </section>

  <section class="rooms-toolbar">
    <div>
      <p class="section-eyebrow">{{ 'workplace.rooms.toolbar.eyebrow' | translate }}</p>
      <h2>{{ 'workplace.rooms.toolbar.title' | translate: { location: activeLocationName } }}</h2>
      <p>{{ 'workplace.rooms.toolbar.description' | translate }}</p>
    </div>

    <app-button variant="create" [disabled]="!activeLocationId" (pressed)="openCreateModal()">
      {{ 'workplace.createRoom' | translate }}
    </app-button>
  </section>

  @if (workplaceFacade.loading()) {
    <div class="loading-wrapper">
      <app-loading-spinner />
    </div>
  } @else {
    <section class="rooms-list">
      @for (room of activeRooms; track room.id) {
        <app-room-card
          [room]="room"
          [locationName]="activeLocationName"
          [deviceCount]="getRoomDeviceCount(room)"
          [activeDeviceCount]="getRoomActiveDeviceCount(room)"
          [groupCount]="getRoomGroupCount(room)"
          [routineCount]="getRoomRoutineCount(room)"
          [currentWatts]="getRoomCurrentWatts(room)"
          [primaryGroupName]="getRoomPrimaryGroupName(room)"
          (remove)="deleteRoom($event)"
        />
      } @empty {
        <app-empty-state
          [eyebrow]="'nav.rooms' | translate"
          [title]="'workplace.rooms.empty.title' | translate"
          [description]="'workplace.rooms.empty.description' | translate"
          [actionLabel]="'workplace.createRoom' | translate"
          actionLink="/spaces/rooms"
        />
      }
    </section>
  }

  <app-modal-form-shell
    [open]="createModalOpen"
    [eyebrow]="'workplace.rooms.form.eyebrow' | translate"
    [title]="'workplace.createRoom' | translate"
    [description]="'workplace.rooms.form.description' | translate"
    (closed)="closeCreateModal()"
  >
    <form class="modal-form" (ngSubmit)="createRoom()">
      <label>
        <span>{{ 'workplace.roomName' | translate }}</span>
        <input
          type="text"
          name="name"
          [(ngModel)]="name"
          [placeholder]="'workplace.rooms.form.namePlaceholder' | translate"
          required
        />
      </label>

      <p class="form-context">
        {{ 'workplace.rooms.form.siteLabel' | translate }}:
        <strong>{{ activeLocationName }}</strong>
      </p>

      <div class="form-actions">
        <app-button variant="neutral" (pressed)="closeCreateModal()">{{ 'common.cancel' | translate }}</app-button>
        <app-button type="submit" variant="create" [disabled]="workplaceFacade.loading() || !activeLocationId">
          {{ 'workplace.createRoom' | translate }}
        </app-button>
      </div>
    </form>
  </app-modal-form-shell>
</section>


============================================================
FILE: src\app\workplace\presentation\pages\rooms\rooms-page.component.scss
============================================================

.rooms-page {
  gap: 30px;
}

.rooms-summary,
.rooms-list {
  display: grid;
  gap: 14px;
}

.rooms-summary {
  align-items: stretch;
  grid-template-columns: repeat(3, minmax(0, 1fr));
}

.rooms-summary-card {
  display: block;
  height: 100%;
}

.rooms-summary-card ::ng-deep .section-card {
  height: 100%;
  min-height: 164px;
  grid-template-rows: auto auto auto 1fr;
  align-content: start;
}

.rooms-summary strong {
  color: var(--text-primary);
  font-size: 1.75rem;
  font-weight: 950;
  line-height: 1;
}

.rooms-summary-card p {
  align-self: end;
}

.rooms-toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  padding: 22px;
  border: 1px solid var(--border-color);
  border-radius: 18px;
  background:
    var(--ec-card-highlight),
    var(--ec-section-bg);
  box-shadow: var(--ec-shadow-soft);
}

.rooms-toolbar h2,
.rooms-toolbar p,
.section-eyebrow {
  margin: 0;
}

.rooms-toolbar h2 {
  color: var(--text-primary);
  font-size: 1.2rem;
  font-weight: 950;
}

.rooms-toolbar p:not(.section-eyebrow),
.form-context {
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.section-eyebrow,
.modal-form label > span {
  color: var(--accent-color);
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.loading-wrapper {
  display: grid;
  place-items: center;
  min-height: 220px;
}

.modal-form {
  display: grid;
  gap: 16px;
}

.modal-form label {
  display: grid;
  gap: 8px;
}

.modal-form input {
  width: 100%;
  min-height: 46px;
  border: 1px solid var(--border-color);
  border-radius: 12px;
  padding: 0 14px;
  color: var(--text-primary);
  background: var(--ec-input-bg);
  font: inherit;
  font-weight: 850;
  outline: none;
}

.modal-form input:focus {
  border-color: var(--accent-color);
}

.form-context {
  margin: 0;
  padding: 12px;
  border: 1px solid var(--border-color);
  border-radius: 12px;
  background: var(--surface-card);
}

.form-context strong {
  color: var(--text-primary);
}

.form-actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
}

@media (max-width: 900px) {
  .rooms-summary {
    grid-template-columns: 1fr;
  }

  .rooms-toolbar,
  .form-actions {
    align-items: stretch;
    flex-direction: column;
  }
}


============================================================
FILE: src\app\workplace\presentation\pages\rooms\rooms-page.component.ts
============================================================

import { Component, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { TranslateModule, TranslateService } from '@ngx-translate/core';

import { DeviceControlFacade } from '../../../../device-control/application/services/device-control.facade';
import { Routine } from '../../../../device-control/domain/model/routine.entity';
import { ConfirmDialogService } from '../../../../shared/application/services/confirm-dialog.service';
import { ToastService } from '../../../../shared/application/services/toast.service';
import { AppButtonComponent } from '../../../../shared/presentation/components/app-button/app-button.component';
import { EmptyStateComponent } from '../../../../shared/presentation/components/empty-state/empty-state.component';
import { LoadingSpinnerComponent } from '../../../../shared/presentation/components/loading-spinner/loading-spinner.component';
import { ModalFormShellComponent } from '../../../../shared/presentation/components/modal-form-shell/modal-form-shell.component';
import { SectionCardComponent } from '../../../../shared/presentation/components/section-card/section-card.component';
import { ActiveWorkplaceContextService } from '../../../application/services/active-workplace-context.service';
import { WorkplaceFacade } from '../../../application/services/workplace.facade';
import { Room } from '../../../domain/model/room.entity';
import { RoomCardComponent } from '../../components/room-card/room-card.component';

@Component({
  selector: 'app-rooms-page',
  standalone: true,
  imports: [
    FormsModule,
    TranslateModule,
    AppButtonComponent,
    EmptyStateComponent,
    LoadingSpinnerComponent,
    ModalFormShellComponent,
    RoomCardComponent,
    SectionCardComponent,
  ],
  templateUrl: './rooms-page.component.html',
  styleUrls: ['./rooms-page.component.scss'],
})
export class RoomsPageComponent implements OnInit {
  name = '';
  createModalOpen = false;
  private readonly defaultRoomFloor = 'General';

  constructor(
    readonly workplaceFacade: WorkplaceFacade,
    readonly deviceControlFacade: DeviceControlFacade,
    private readonly activeWorkplaceContext: ActiveWorkplaceContextService,
    private readonly toastService: ToastService,
    private readonly confirmDialog: ConfirmDialogService,
    private readonly translate: TranslateService
  ) {}

  async ngOnInit(): Promise<void> {
    await this.workplaceFacade.loadWorkplace();
    this.activeWorkplaceContext.ensureActiveLocation(this.workplaceFacade.locations());

    if (this.workplaceFacade.error()) {
      this.toastService.error(this.t('workplace.rooms.toasts.loadError'));
    }
  }

  get activeLocationId(): number | null {
    return this.activeWorkplaceContext.activeLocationId();
  }

  get activeRooms() {
    const locationId = this.activeLocationId;
    return locationId
      ? this.workplaceFacade.rooms().filter((room) => room.locationId === locationId)
      : [];
  }

  get activeAssignments() {
    const locationId = this.activeLocationId;
    return locationId
      ? this.workplaceFacade.deviceAssignments().filter((assignment) => assignment.locationId === locationId)
      : [];
  }

  get activeDeviceCount(): number {
    return new Set(this.activeAssignments.map((assignment) => assignment.deviceId)).size;
  }

  get activeGroupCount(): number {
    const activeDeviceIds = new Set(this.activeAssignments.map((assignment) => assignment.deviceId));

    return this.deviceControlFacade
      .deviceGroups()
      .filter((group) => group.deviceIds.some((deviceId) => activeDeviceIds.has(deviceId)))
      .length;
  }

  get activeLocationName(): string {
    const locationId = this.activeLocationId;
    return locationId
      ? this.workplaceFacade.getLocationName(locationId)
      : this.t('workplace.rooms.activeLocationFallback');
  }

  openCreateModal(): void {
    this.createModalOpen = true;
  }

  closeCreateModal(): void {
    this.createModalOpen = false;
  }

  async createRoom(): Promise<void> {
    const locationId = this.activeLocationId;

    if (!locationId) {
      this.toastService.warning(this.t('workplace.rooms.toasts.locationRequired'));
      return;
    }

    const success = await this.workplaceFacade.createRoom({
      locationId,
      name: this.name,
      floor: this.defaultRoomFloor,
    });

    if (success) {
      this.name = '';
      this.closeCreateModal();
      this.toastService.success(this.t('workplace.roomCreateSuccess'));
      return;
    }

    this.toastService.error(this.t('workplace.roomCreateError'));
  }

  async deleteRoom(roomId: number): Promise<void> {
    const room = this.activeRooms.find((item) => item.id === roomId);
    const deviceCount = room ? this.getRoomDeviceCount(room) : 0;
    const confirmed = await this.confirmDialog.confirm({
      title: this.t('workplace.rooms.deleteTitle'),
      message: deviceCount > 0
        ? this.t('workplace.rooms.deleteMessageWithDevices', { count: deviceCount })
        : this.t('workplace.rooms.deleteMessageEmpty'),
      confirmLabel: this.t('common.delete'),
      cancelLabel: this.t('common.cancel'),
      tone: 'danger',
    });

    if (!confirmed) {
      return;
    }

    const success = await this.workplaceFacade.deleteRoom(roomId);

    if (success) {
      this.toastService.info(this.t('workplace.rooms.toasts.deleteSuccess'));
      return;
    }

    this.toastService.error(this.t('workplace.roomDeleteError'));
  }

  getRoomDeviceCount(room: Room): number {
    return this.getRoomDeviceIds(room).size;
  }

  getRoomActiveDeviceCount(room: Room): number {
    const deviceIds = this.getRoomDeviceIds(room);
    return this.deviceControlFacade
      .devices()
      .filter((device) => deviceIds.has(device.id) && device.isOn)
      .length;
  }

  getRoomCurrentWatts(room: Room): number {
    const deviceIds = this.getRoomDeviceIds(room);
    return this.deviceControlFacade
      .devices()
      .filter((device) => deviceIds.has(device.id) && device.isOn)
      .reduce((total, device) => total + device.powerWatts, 0);
  }

  getRoomGroupCount(room: Room): number {
    const deviceIds = this.getRoomDeviceIds(room);
    return this.deviceControlFacade
      .deviceGroups()
      .filter((group) => group.deviceIds.some((deviceId) => deviceIds.has(deviceId)))
      .length;
  }

  getRoomRoutineCount(room: Room): number {
    return this.getRoomRoutines(room).length;
  }

  getRoomPrimaryGroupName(room: Room): string {
    const deviceIds = this.getRoomDeviceIds(room);
    return (
      this.deviceControlFacade
        .deviceGroups()
        .find((group) => group.deviceIds.some((deviceId) => deviceIds.has(deviceId)))
        ?.name ?? ''
    );
  }

  private getRoomDeviceIds(room: Room): Set<number> {
    return new Set(
      this.activeAssignments
        .filter((assignment) => assignment.roomId === room.id)
        .map((assignment) => assignment.deviceId)
    );
  }

  private getRoomGroupIds(room: Room): Set<number> {
    const deviceIds = this.getRoomDeviceIds(room);

    return new Set(
      this.deviceControlFacade
        .deviceGroups()
        .filter((group) => group.deviceIds.some((deviceId) => deviceIds.has(deviceId)))
        .map((group) => group.id)
    );
  }

  private getRoomRoutines(room: Room): Routine[] {
    const deviceIds = this.getRoomDeviceIds(room);
    const groupIds = this.getRoomGroupIds(room);
    const locationId = this.activeLocationId;

    return this.deviceControlFacade.routines().filter((routine) => {
      if (routine.targetType === 'ROOM') {
        return routine.targetId === room.id;
      }

      if (routine.targetType === 'DEVICE') {
        return Boolean(routine.deviceId && deviceIds.has(routine.deviceId)) || deviceIds.has(routine.targetId);
      }

      if (routine.targetType === 'GROUP') {
        return Boolean(routine.groupId && groupIds.has(routine.groupId)) || groupIds.has(routine.targetId);
      }

      return Boolean(locationId && routine.targetId === locationId);
    });
  }

  private t(key: string, params?: Record<string, unknown>): string {
    return this.translate.instant(key, params);
  }
}


============================================================
FILE: src\app\workplace\presentation\routes\workplace.routes.ts
============================================================

import { Routes } from '@angular/router';

import { activeSubscriptionGuard } from '../../../shared/application/guards/active-subscription.guard';

export const WORKPLACE_ROUTES: Routes = [
  {
    path: 'spaces/sites',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Sedes' },
    loadComponent: () =>
      import('../pages/locations/locations-page.component').then(
        (m) => m.LocationsPageComponent
      ),
  },
  {
    path: 'spaces/rooms',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Habitaciones' },
    loadComponent: () =>
      import('../pages/rooms/rooms-page.component').then(
        (m) => m.RoomsPageComponent
      ),
  },
  {
    path: 'spaces/assignments',
    canActivate: [activeSubscriptionGuard],
    data: { title: 'Asignaciones' },
    loadComponent: () =>
      import(
        '../pages/device-assignments/device-assignments-page.component'
        ).then((m) => m.DeviceAssignmentsPageComponent),
  },
  {
    path: 'workplace',
    redirectTo: 'spaces/sites',
    pathMatch: 'full',
  },
  {
    path: 'workplace/locations',
    redirectTo: 'spaces/sites',
    pathMatch: 'full',
  },
  {
    path: 'workplace/rooms',
    redirectTo: 'spaces/rooms',
    pathMatch: 'full',
  },
  {
    path: 'workplace/device-assignments',
    redirectTo: 'spaces/assignments',
    pathMatch: 'full',
  },
];


============================================================
FILE: src\environments\environment.prod.ts
============================================================

export const environment = {
  production: true,
  apiBaseUrl: '/api/v1',
};

============================================================
FILE: src\environments\environment.ts
============================================================

export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080/api/v1',
};

============================================================
FILE: src\index.html
============================================================

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>ElectroCorp</title>
  <base href="/">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="icon" type="image/x-icon" href="favicon.ico">
</head>
<body>
<app-root></app-root>
</body>
</html>

============================================================
FILE: src\main.ts
============================================================

import 'zone.js';
import './assets/load-fonts.js';

import { bootstrapApplication } from '@angular/platform-browser';

import { appConfig } from './app/app.config';
import { App } from './app/app';

bootstrapApplication(App, appConfig)
  .catch((err) => console.error(err));

============================================================
FILE: src\main\docs\templates\index.html
============================================================

<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>ElectroCorp</title>
    <link rel="stylesheet" th:href="@{/css/styles.css}">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>

<div class="sidebar">
    <div class="logo">⚡ ElectroCorp</div>

    <div class="menu-item active-menu" onclick="showSection('centro', this)">⚡ Centro Energético</div>
    <div class="menu-item" onclick="showSection('analitica', this)">📊 Analítica</div>
    <div class="menu-item" onclick="showSection('dispositivos', this)">🔌 Dispositivos</div>
    <div class="menu-item" onclick="showSection('historial', this)">📜 Historial</div>
    <div class="menu-item" onclick="showSection('insights', this)">💡 IA Energética</div>
</div>

<div class="main">

    <!-- CENTRO -->
    <div id="centro" class="section active">
        <h1>Centro Energético</h1>

        <div class="cards">
            <div class="card stat"><h2>Consumo mensual</h2><p>320 kWh</p></div>
            <div class="card stat"><h2>Gasto estimado</h2><p>S/ 145</p></div>
            <div class="card stat"><h2>Ahorro</h2><p>18%</p></div>
            <div class="card stat"><h2>Dispositivos</h2><p>6</p></div>
        </div>

        <div class="card big chart-wrapper"><canvas id="chart"></canvas></div>

        <button class="btn-primary" onclick="openModal()">Agregar consumo</button>
    </div>

    <!-- ANALITICA -->
    <div id="analitica" class="section">
        <h1>Analítica Avanzada</h1>

        <div class="cards">
            <div class="card stat"><h2>Hora pico</h2><p>7 PM</p></div>
            <div class="card stat"><h2>Día mayor consumo</h2><p>Martes</p></div>
            <div class="card stat"><h2>Promedio diario</h2><p>10.5 kWh</p></div>
            <div class="card stat"><h2>Predicción</h2><p>S/ 180</p></div>
        </div>

        <div class="card chart-wrapper"><canvas id="chartBar"></canvas></div>
    </div>

    <!-- DISPOSITIVOS -->
    <div id="dispositivos" class="section">
        <h1>Dispositivos</h1>

        <div class="card">
            <table>
                <thead>
                <tr><th>Dispositivo</th><th>Estado</th><th>Acción</th></tr>
                </thead>
                <tbody>
                <tr>
                    <td>Refrigeradora</td>
                    <td id="estado-1" class="estado-activo">Activado</td>
                    <td>
                        <button id="btn-1" class="btn-danger" onclick="toggleDevice(1, 'btn-1')">
                            Desactivar
                        </button>
                    </td>
                </tr>
                <tr>
                    <td>TV</td>
                    <td id="estado-2" class="estado-activo">Activado</td>
                    <td>
                        <button id="btn-2" class="btn-danger" onclick="toggleDevice(2, 'btn-2')">
                            Desactivar
                        </button>
                    </td>
                </tr>
                <tr>
                    <td>Aire acondicionado</td>
                    <td id="estado-3" class="estado-inactivo">Desactivado</td>
                    <td>
                        <button id="btn-3" class="btn-success" onclick="toggleDevice(3, 'btn-3')">
                            Activar
                        </button>
                    </td>
                </tr>
                </tbody>
            </table>
        </div>
    </div>

    <!-- HISTORIAL -->
    <div id="historial" class="section">
        <h1>Historial</h1>

        <div class="card">
            <ul class="history-list">
                <li><span>Hoy</span><strong>12 kWh</strong></li>
                <li><span>Ayer</span><strong>10 kWh</strong></li>
                <li><span>Lunes</span><strong>14 kWh</strong></li>
                <li><span>Domingo</span><strong>9 kWh</strong></li>
                <li><span>Sábado</span><strong>11 kWh</strong></li>
            </ul>
        </div>
    </div>

    <!-- INSIGHTS -->
    <div id="insights" class="section">
        <h1>IA Energética</h1>

        <div class="insight">⚠️ Consumo elevado detectado en horas pico</div>
        <div class="insight">💡 Reduce el aire acondicionado 2°C para ahorrar 12%</div>
        <div class="insight">✅ Ahorro proyectado de S/ 35 este mes</div>
        <div class="insight">🔋 Tu refrigeradora consume más que el promedio</div>
    </div>

</div>

<!-- MODAL -->
<div class="modal" id="modal">
    <div class="modal-content">
        <h3>Nuevo Consumo</h3>
        <input id="kwh-input" placeholder="kWh" type="number">
        <div class="modal-actions">
            <button class="btn-ghost" onclick="closeModal(false)">Cancelar</button>
            <button class="btn-primary" onclick="closeModal(true)">Guardar</button>
        </div>
    </div>
</div>

<div class="toast" id="toast">Acción realizada</div>
<div class="loader" id="loader"></div>

<script th:src="@{/js/app.js}"></script>
</body>
</html>


============================================================
FILE: src\styles.scss
============================================================

* {
  box-sizing: border-box;
}

:root {
  color-scheme: dark;

  --accent-color: #38bdf8;
  --ec-accent-strong: #0ea5e9;
  --ec-accent-soft: rgba(56, 189, 248, 0.14);

  --ec-action-bg: linear-gradient(135deg, #132b4f 0%, #174a78 58%, #0f6f9e 100%);
  --ec-action-bg-hover: linear-gradient(135deg, #0d203d 0%, #123c64 58%, #0b5c82 100%);
  --ec-action-shadow: 0 16px 32px rgba(15, 77, 132, 0.26), 0 0 20px rgba(56, 189, 248, 0.12);
  --ec-action-shadow-hover: 0 12px 26px rgba(15, 77, 132, 0.24), 0 0 16px rgba(56, 189, 248, 0.1);
  --ec-create-bg: linear-gradient(135deg, #22d3ee 0%, #0ea5e9 54%, #2563eb 100%);
  --ec-create-bg-hover: linear-gradient(135deg, #0fb9d1 0%, #0b87c4 54%, #1d4ed8 100%);
  --ec-create-shadow: 0 18px 36px rgba(34, 211, 238, 0.24);
  --ec-create-shadow-hover: 0 16px 32px rgba(14, 165, 233, 0.3);
  --ec-danger-bg: linear-gradient(135deg, #7f1d1d 0%, #b4232c 58%, #c2413b 100%);
  --ec-danger-bg-hover: linear-gradient(135deg, #641717 0%, #941b24 58%, #a53330 100%);
  --ec-danger-shadow: 0 15px 30px rgba(127, 29, 29, 0.22);
  --ec-danger-shadow-hover: 0 13px 26px rgba(127, 29, 29, 0.28);
  --ec-neutral-bg: linear-gradient(180deg, rgba(24, 39, 64, 0.96), rgba(13, 24, 43, 0.98));
  --ec-neutral-bg-hover: linear-gradient(180deg, rgba(30, 52, 84, 0.98), rgba(15, 32, 58, 0.98));
  --ec-ghost-bg: rgba(255, 255, 255, 0.05);
  --ec-ghost-bg-hover: rgba(56, 189, 248, 0.1);

  --bg-primary: #0b1020;
  --surface-color: rgba(255, 255, 255, 0.075);
  --surface-elevated: linear-gradient(180deg, rgba(16, 29, 52, 0.94), rgba(9, 18, 37, 0.96));
  --surface-elevated-solid: #101b2f;
  --surface-card: linear-gradient(180deg, rgba(15, 27, 52, 0.88), rgba(9, 18, 37, 0.92));
  --field-bg: linear-gradient(180deg, rgba(15, 27, 52, 0.92), rgba(9, 18, 37, 0.96));
  --ec-input-bg: var(--field-bg);
  --field-bg-hover: linear-gradient(180deg, rgba(18, 34, 62, 0.96), rgba(9, 20, 40, 0.98));
  --menu-bg: linear-gradient(180deg, rgba(18, 32, 60, 0.98), rgba(9, 18, 37, 0.99));
  --border-color: rgba(255, 255, 255, 0.12);
  --border-strong: rgba(125, 211, 252, 0.34);
  --ec-surface-subtle: rgba(255, 255, 255, 0.04);
  --ec-surface-muted: rgba(15, 23, 42, 0.58);
  --ec-surface-strong: rgba(2, 6, 23, 0.38);
  --ec-surface-hover: rgba(15, 23, 42, 0.68);
  --surface-muted: var(--ec-surface-muted);
  --ec-card-highlight: linear-gradient(135deg, rgba(56, 189, 248, 0.09), transparent 42%);
  --ec-inset-highlight: inset 0 1px 0 rgba(255, 255, 255, 0.04);
  --ec-control-thumb-bg: #e2e8f0;
  --ec-control-thumb-shadow: 0 4px 10px rgba(0, 0, 0, 0.24);
  --ec-slider-track: linear-gradient(90deg, #38bdf8, #6366f1);
  --ec-slider-thumb-bg: #0f172a;
  --ec-slider-thumb-border: #e0f2fe;
  --ec-disabled-track-bg: rgba(148, 163, 184, 0.24);

  --ec-status-stable-text: #ccfbf1;
  --ec-status-stable-bg: rgba(20, 184, 166, 0.16);
  --ec-status-stable-border: rgba(20, 184, 166, 0.42);
  --ec-status-info-text: #dbeafe;
  --ec-status-info-bg: rgba(59, 130, 246, 0.16);
  --ec-status-info-border: rgba(59, 130, 246, 0.42);
  --ec-status-success-text: #dcfce7;
  --ec-status-success-bg: rgba(34, 197, 94, 0.16);
  --ec-status-success-border: rgba(34, 197, 94, 0.42);
  --ec-status-success-solid: #16a34a;
  --ec-status-warning-text: #fef3c7;
  --ec-status-warning-bg: rgba(250, 204, 21, 0.16);
  --ec-status-warning-border: rgba(250, 204, 21, 0.48);
  --ec-status-danger-text: #fee2e2;
  --ec-status-danger-bg: rgba(248, 113, 113, 0.18);
  --ec-status-danger-bg-strong: rgba(127, 29, 29, 0.35);
  --ec-status-danger-border: rgba(248, 113, 113, 0.5);
  --ec-status-danger-solid: #b91c1c;

  --text-primary: #f8fafc;
  --text-color: var(--text-primary);
  --text-secondary: #cbd5e1;
  --text-muted: #94a3b8;
  --text-on-action: #ffffff;
  --ec-copy-color: color-mix(in srgb, var(--text-secondary) 92%, var(--text-primary));
  --ec-copy-weight: 700;
  --ec-copy-line-height: 1.58;

  --ec-shell-bg: radial-gradient(circle at top left, rgba(56, 189, 248, 0.18), transparent 34%), var(--bg-primary);
  --app-bg: var(--bg-primary);
  --ec-topbar-bg: rgba(8, 13, 24, 0.86);
  --ec-sidebar-bg: rgba(8, 13, 24, 0.97);
  --ec-overlay-bg: rgba(2, 6, 23, 0.74);
  --ec-overlay-glow: radial-gradient(circle at 50% 42%, rgba(56, 189, 248, 0.12), transparent 32%);
  --ec-modal-bg: linear-gradient(180deg, rgba(19, 31, 55, 0.98), rgba(13, 24, 43, 0.98));
  --ec-section-bg: var(--surface-card);
  --ec-shadow-soft: 0 18px 42px rgba(3, 7, 18, 0.18);
  --ec-shadow-panel: 0 24px 60px rgba(3, 7, 18, 0.48);
  --ec-shadow-modal: 0 30px 90px rgba(0, 0, 0, 0.52);
  --ec-sidebar-shadow: -24px 0 60px rgba(0, 0, 0, 0.36);
  --shadow-soft: var(--ec-shadow-soft);
  --ec-focus-ring: 0 0 0 4px rgba(56, 189, 248, 0.16), 0 0 0 1px rgba(125, 211, 252, 0.42);

  --scrollbar-track: rgba(9, 18, 37, 0.72);
  --scrollbar-thumb: rgba(125, 211, 252, 0.52);
  --scrollbar-thumb-hover: rgba(96, 165, 250, 0.78);

  --ec-settings-nav-item-bg:
    linear-gradient(180deg, rgba(18, 47, 78, 0.9), rgba(12, 35, 60, 0.92)),
    var(--surface-color);
  --ec-settings-nav-item-hover-bg:
    linear-gradient(180deg, rgba(21, 57, 94, 0.94), rgba(13, 40, 70, 0.96)),
    var(--surface-color);
  --ec-settings-nav-active-bg:
    radial-gradient(circle at top left, rgba(186, 230, 253, 0.24), transparent 58%),
    rgba(56, 189, 248, 0.34);
  --ec-settings-nav-marker-bg: linear-gradient(180deg, rgba(147, 197, 253, 0.58), rgba(56, 189, 248, 0.48));
}

:root.light-theme {
  color-scheme: light;

  --accent-color: #0369a1;
  --ec-accent-strong: #075985;
  --ec-accent-soft: rgba(14, 165, 233, 0.12);

  --bg-primary: #eef4fb;
  --surface-color: rgba(255, 255, 255, 0.78);
  --surface-elevated: linear-gradient(180deg, rgba(255, 255, 255, 0.96), rgba(242, 247, 252, 0.94));
  --surface-elevated-solid: #ffffff;
  --surface-card: linear-gradient(180deg, rgba(255, 255, 255, 0.92), rgba(241, 247, 252, 0.9));
  --field-bg: linear-gradient(180deg, rgba(255, 255, 255, 0.94), rgba(241, 247, 252, 0.96));
  --ec-input-bg: var(--field-bg);
  --field-bg-hover: linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(232, 241, 249, 0.98));
  --menu-bg: linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(239, 246, 252, 0.98));
  --border-color: rgba(15, 23, 42, 0.12);
  --border-strong: rgba(3, 105, 161, 0.3);
  --ec-surface-subtle: rgba(3, 105, 161, 0.05);
  --ec-surface-muted: rgba(255, 255, 255, 0.72);
  --ec-surface-strong: rgba(241, 247, 252, 0.9);
  --ec-surface-hover: rgba(232, 241, 249, 0.96);
  --surface-muted: var(--ec-surface-muted);
  --ec-card-highlight: linear-gradient(135deg, rgba(14, 165, 233, 0.11), transparent 42%);
  --ec-inset-highlight: inset 0 1px 0 rgba(255, 255, 255, 0.74);
  --ec-control-thumb-bg: #ffffff;
  --ec-control-thumb-shadow: 0 4px 10px rgba(15, 23, 42, 0.16);
  --ec-slider-track: linear-gradient(90deg, #0284c7, #4f46e5);
  --ec-slider-thumb-bg: #ffffff;
  --ec-slider-thumb-border: #bae6fd;
  --ec-disabled-track-bg: rgba(100, 116, 139, 0.2);

  --ec-status-stable-text: #0f766e;
  --ec-status-stable-bg: rgba(20, 184, 166, 0.12);
  --ec-status-stable-border: rgba(15, 118, 110, 0.28);
  --ec-status-info-text: #1d4ed8;
  --ec-status-info-bg: rgba(59, 130, 246, 0.12);
  --ec-status-info-border: rgba(29, 78, 216, 0.28);
  --ec-status-success-text: #166534;
  --ec-status-success-bg: rgba(34, 197, 94, 0.12);
  --ec-status-success-border: rgba(22, 101, 52, 0.28);
  --ec-status-success-solid: #16a34a;
  --ec-status-warning-text: #92400e;
  --ec-status-warning-bg: rgba(250, 204, 21, 0.16);
  --ec-status-warning-border: rgba(146, 64, 14, 0.26);
  --ec-status-danger-text: #b91c1c;
  --ec-status-danger-bg: rgba(248, 113, 113, 0.12);
  --ec-status-danger-bg-strong: rgba(254, 226, 226, 0.82);
  --ec-status-danger-border: rgba(185, 28, 28, 0.3);
  --ec-status-danger-solid: #dc2626;

  --text-primary: #0f172a;
  --text-color: var(--text-primary);
  --text-secondary: #334155;
  --text-muted: #64748b;
  --text-on-action: #ffffff;

  --ec-neutral-bg: linear-gradient(180deg, rgba(255, 255, 255, 0.96), rgba(232, 241, 249, 0.98));
  --ec-neutral-bg-hover: linear-gradient(180deg, rgba(241, 247, 252, 0.98), rgba(221, 235, 247, 0.98));
  --ec-ghost-bg: rgba(3, 105, 161, 0.06);
  --ec-ghost-bg-hover: rgba(3, 105, 161, 0.11);
  --ec-shell-bg: radial-gradient(circle at top left, rgba(14, 165, 233, 0.18), transparent 34%), linear-gradient(180deg, #f4f8fc 0%, #e7f0f8 100%);
  --app-bg: var(--bg-primary);
  --ec-topbar-bg: rgba(248, 251, 255, 0.9);
  --ec-sidebar-bg: rgba(248, 251, 255, 0.97);
  --ec-overlay-bg: rgba(15, 23, 42, 0.36);
  --ec-overlay-glow: radial-gradient(circle at 50% 42%, rgba(14, 165, 233, 0.16), transparent 34%);
  --ec-modal-bg: linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(238, 246, 252, 0.98));
  --ec-section-bg: var(--surface-card);
  --ec-shadow-soft: 0 16px 34px rgba(15, 23, 42, 0.1);
  --ec-shadow-panel: 0 24px 60px rgba(15, 23, 42, 0.16);
  --ec-shadow-modal: 0 30px 90px rgba(15, 23, 42, 0.24);
  --ec-sidebar-shadow: -24px 0 60px rgba(15, 23, 42, 0.16);
  --shadow-soft: var(--ec-shadow-soft);
  --ec-focus-ring: 0 0 0 4px rgba(14, 165, 233, 0.16), 0 0 0 1px rgba(3, 105, 161, 0.34);

  --scrollbar-track: rgba(226, 232, 240, 0.82);
  --scrollbar-thumb: rgba(59, 130, 246, 0.46);
  --scrollbar-thumb-hover: rgba(37, 99, 235, 0.68);

  --ec-settings-nav-item-bg:
    linear-gradient(180deg, rgba(232, 246, 255, 0.94), rgba(214, 235, 249, 0.92)),
    var(--surface-color);
  --ec-settings-nav-item-hover-bg:
    linear-gradient(180deg, rgba(214, 235, 249, 0.98), rgba(188, 221, 243, 0.96)),
    var(--surface-color);
  --ec-settings-nav-active-bg:
    radial-gradient(circle at top left, rgba(14, 165, 233, 0.16), transparent 58%),
    rgba(14, 165, 233, 0.16);
  --ec-settings-nav-marker-bg: linear-gradient(180deg, rgba(125, 211, 252, 0.44), rgba(3, 105, 161, 0.34));
}

:root.dark-theme {
  color-scheme: dark;

  --accent-color: #38bdf8;
  --ec-accent-strong: #0ea5e9;
  --ec-accent-soft: rgba(56, 189, 248, 0.14);

  --bg-primary: #0b1020;
  --surface-color: rgba(255, 255, 255, 0.075);
  --surface-elevated: linear-gradient(180deg, rgba(16, 29, 52, 0.94), rgba(9, 18, 37, 0.96));
  --surface-elevated-solid: #101b2f;
  --surface-card: linear-gradient(180deg, rgba(15, 27, 52, 0.88), rgba(9, 18, 37, 0.92));
  --field-bg: linear-gradient(180deg, rgba(15, 27, 52, 0.92), rgba(9, 18, 37, 0.96));
  --ec-input-bg: var(--field-bg);
  --field-bg-hover: linear-gradient(180deg, rgba(18, 34, 62, 0.96), rgba(9, 20, 40, 0.98));
  --menu-bg: linear-gradient(180deg, rgba(18, 32, 60, 0.98), rgba(9, 18, 37, 0.99));
  --border-color: rgba(255, 255, 255, 0.12);
  --border-strong: rgba(125, 211, 252, 0.34);
  --ec-surface-subtle: rgba(255, 255, 255, 0.04);
  --ec-surface-muted: rgba(15, 23, 42, 0.58);
  --ec-surface-strong: rgba(2, 6, 23, 0.38);
  --ec-surface-hover: rgba(15, 23, 42, 0.68);
  --surface-muted: var(--ec-surface-muted);
  --ec-card-highlight: linear-gradient(135deg, rgba(56, 189, 248, 0.09), transparent 42%);
  --ec-inset-highlight: inset 0 1px 0 rgba(255, 255, 255, 0.04);
  --ec-control-thumb-bg: #e2e8f0;
  --ec-control-thumb-shadow: 0 4px 10px rgba(0, 0, 0, 0.24);
  --ec-slider-track: linear-gradient(90deg, #38bdf8, #6366f1);
  --ec-slider-thumb-bg: #0f172a;
  --ec-slider-thumb-border: #e0f2fe;
  --ec-disabled-track-bg: rgba(148, 163, 184, 0.24);

  --ec-status-stable-text: #ccfbf1;
  --ec-status-stable-bg: rgba(20, 184, 166, 0.16);
  --ec-status-stable-border: rgba(20, 184, 166, 0.42);
  --ec-status-info-text: #dbeafe;
  --ec-status-info-bg: rgba(59, 130, 246, 0.16);
  --ec-status-info-border: rgba(59, 130, 246, 0.42);
  --ec-status-success-text: #dcfce7;
  --ec-status-success-bg: rgba(34, 197, 94, 0.16);
  --ec-status-success-border: rgba(34, 197, 94, 0.42);
  --ec-status-success-solid: #16a34a;
  --ec-status-warning-text: #fef3c7;
  --ec-status-warning-bg: rgba(250, 204, 21, 0.16);
  --ec-status-warning-border: rgba(250, 204, 21, 0.48);
  --ec-status-danger-text: #fee2e2;
  --ec-status-danger-bg: rgba(248, 113, 113, 0.18);
  --ec-status-danger-bg-strong: rgba(127, 29, 29, 0.35);
  --ec-status-danger-border: rgba(248, 113, 113, 0.5);
  --ec-status-danger-solid: #b91c1c;

  --text-primary: #f8fafc;
  --text-color: var(--text-primary);
  --text-secondary: #cbd5e1;
  --text-muted: #94a3b8;
  --text-on-action: #ffffff;

  --ec-neutral-bg: linear-gradient(180deg, rgba(24, 39, 64, 0.96), rgba(13, 24, 43, 0.98));
  --ec-neutral-bg-hover: linear-gradient(180deg, rgba(30, 52, 84, 0.98), rgba(15, 32, 58, 0.98));
  --ec-ghost-bg: rgba(255, 255, 255, 0.05);
  --ec-ghost-bg-hover: rgba(56, 189, 248, 0.1);
  --ec-shell-bg: radial-gradient(circle at top left, rgba(56, 189, 248, 0.18), transparent 34%), var(--bg-primary);
  --app-bg: var(--bg-primary);
  --ec-topbar-bg: rgba(8, 13, 24, 0.86);
  --ec-sidebar-bg: rgba(8, 13, 24, 0.97);
  --ec-overlay-bg: rgba(2, 6, 23, 0.74);
  --ec-overlay-glow: radial-gradient(circle at 50% 42%, rgba(56, 189, 248, 0.12), transparent 32%);
  --ec-modal-bg: linear-gradient(180deg, rgba(19, 31, 55, 0.98), rgba(13, 24, 43, 0.98));
  --ec-section-bg: var(--surface-card);
  --ec-shadow-soft: 0 18px 42px rgba(3, 7, 18, 0.18);
  --ec-shadow-panel: 0 24px 60px rgba(3, 7, 18, 0.48);
  --ec-shadow-modal: 0 30px 90px rgba(0, 0, 0, 0.52);
  --ec-sidebar-shadow: -24px 0 60px rgba(0, 0, 0, 0.36);
  --shadow-soft: var(--ec-shadow-soft);
  --ec-focus-ring: 0 0 0 4px rgba(56, 189, 248, 0.16), 0 0 0 1px rgba(125, 211, 252, 0.42);

  --scrollbar-track: rgba(9, 18, 37, 0.72);
  --scrollbar-thumb: rgba(125, 211, 252, 0.52);
  --scrollbar-thumb-hover: rgba(96, 165, 250, 0.78);

  --ec-settings-nav-item-bg:
    linear-gradient(180deg, rgba(18, 47, 78, 0.9), rgba(12, 35, 60, 0.92)),
    var(--surface-color);
  --ec-settings-nav-item-hover-bg:
    linear-gradient(180deg, rgba(21, 57, 94, 0.94), rgba(13, 40, 70, 0.96)),
    var(--surface-color);
  --ec-settings-nav-active-bg:
    radial-gradient(circle at top left, rgba(186, 230, 253, 0.24), transparent 58%),
    rgba(56, 189, 248, 0.34);
  --ec-settings-nav-marker-bg: linear-gradient(180deg, rgba(147, 197, 253, 0.58), rgba(56, 189, 248, 0.48));
}

html,
body {
  min-height: 100%;
}

html {
  overflow-y: scroll;
  scrollbar-gutter: stable;
}

body {
  margin: 0;
  color: var(--text-primary);
  background: var(--bg-primary);
  font-family:
    Inter,
    ui-sans-serif,
    system-ui,
    -apple-system,
    BlinkMacSystemFont,
    "Segoe UI",
    sans-serif;
}

html.ec-modal-open {
  overflow-y: scroll;
  scrollbar-gutter: stable;
}

body.ec-modal-open {
  position: fixed;
  right: 0;
  left: 0;
  width: 100%;
  overflow: hidden;
}

body.ec-modal-open app-layout .content {
  overflow: hidden;
}

body.ec-modal-open app-header {
  position: fixed;
  top: 0;
  right: 0;
  left: 0;
  z-index: 100001;
}

button,
input,
select,
textarea {
  font: inherit;
}

:where(button, a, input, select, textarea) {
  transition:
    border-color 0.2s ease,
    box-shadow 0.2s ease,
    background 0.2s ease,
    color 0.2s ease,
    opacity 0.2s ease;
}

:where(button, a, input, select, textarea):focus-visible {
  outline: 0;
  box-shadow: var(--ec-focus-ring);
}

:where(button[disabled], button:disabled, input:disabled, select:disabled, textarea:disabled) {
  opacity: 0.55;
  cursor: not-allowed;
  transform: none;
}

:where(button:not([class])) {
  min-height: 42px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 0 16px;
  border: 1px solid var(--border-strong);
  border-radius: 12px;
  color: var(--text-primary);
  background: var(--ec-neutral-bg);
  box-shadow:
    var(--ec-shadow-soft),
    inset 0 1px 0 rgba(255, 255, 255, 0.04);
  cursor: pointer;
  font-weight: 900;
}

:where(button:not([class])):hover:not(:disabled) {
  border-color: var(--border-strong);
  background: var(--ec-neutral-bg-hover);
  box-shadow:
    var(--ec-focus-ring),
    var(--ec-shadow-soft);
}

:where(
  button[type='submit'],
  button.submit-btn,
  button.primary-action,
  button.secondary-action,
  button.apply-mode-button,
  button.export-btn,
  button.clear-filters-btn,
  button.toggle-btn,
  a.primary-btn
):not(.chip):not(.time-step) {
  min-height: 42px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  border: 0 !important;
  border-radius: 999px !important;
  color: var(--text-on-action) !important;
  background: var(--ec-action-bg) !important;
  box-shadow: var(--ec-action-shadow) !important;
  cursor: pointer;
  font-weight: 950;
  text-decoration: none;
  transition:
    box-shadow 0.2s ease,
    background 0.2s ease,
    opacity 0.2s ease;
}

:where(
  button[type='submit'],
  button.submit-btn,
  button.primary-action,
  button.secondary-action,
  button.apply-mode-button,
  button.export-btn,
  button.clear-filters-btn,
  button.toggle-btn,
  a.primary-btn
):not(.chip):not(.time-step):hover:not(:disabled) {
  background: var(--ec-action-bg-hover) !important;
  box-shadow: var(--ec-action-shadow-hover) !important;
}

:where(
  button.danger,
  button.danger-btn,
  button.danger-action,
  button.archive-mode-button
) {
  border: 0 !important;
  border-radius: 999px !important;
  color: var(--text-on-action) !important;
  background: var(--ec-danger-bg) !important;
  box-shadow: var(--ec-danger-shadow) !important;
  font-weight: 950;
}

:where(
  button.danger,
  button.danger-btn,
  button.danger-action,
  button.archive-mode-button
):hover:not(:disabled) {
  background: var(--ec-danger-bg-hover) !important;
  box-shadow: 0 12px 26px rgba(185, 28, 28, 0.2) !important;
}

:where(app-layout .content a:not([class])) {
  min-height: 38px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0 13px;
  border: 1px solid var(--border-color);
  border-radius: 12px;
  color: var(--text-primary);
  background: var(--ec-neutral-bg);
  text-decoration: none;
  font-weight: 900;
}

:where(app-layout .content a:not([class])):hover {
  color: var(--text-primary);
  border-color: var(--border-strong);
  box-shadow:
    var(--ec-focus-ring),
    var(--ec-shadow-soft);
}

:where(
  input:not([class]):not([type='checkbox']):not([type='radio']):not([type='range']):not([type='file']):not([type='color']),
  select:not([class]),
  textarea:not([class])
) {
  width: 100%;
  min-height: 46px;
  border: 1px solid var(--border-color);
  border-radius: 14px;
  color: var(--text-primary);
  background: var(--field-bg);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.03),
    0 10px 24px rgba(3, 7, 18, 0.08);
  font-weight: 800;
}

:where(
  input:not([class]):not([type='checkbox']):not([type='radio']):not([type='range']):not([type='file']):not([type='color']),
  select:not([class])
) {
  padding: 0 14px;
}

:where(textarea:not([class])) {
  min-height: 98px;
  padding: 13px 14px;
  resize: vertical;
}

:where(select:not([class])) {
  appearance: none;
  padding-right: 38px;
  background-image:
    linear-gradient(45deg, transparent 50%, var(--accent-color) 50%),
    linear-gradient(135deg, var(--accent-color) 50%, transparent 50%),
    var(--field-bg);
  background-position:
    calc(100% - 20px) 50%,
    calc(100% - 14px) 50%,
    0 0;
  background-size:
    6px 6px,
    6px 6px,
    100% 100%;
  background-repeat: no-repeat;
}

:where(
  input:not([class]):not([type='checkbox']):not([type='radio']):not([type='range']):not([type='file']):not([type='color']):focus,
  select:not([class]):focus,
  textarea:not([class]):focus
) {
  border-color: var(--border-strong);
  background: var(--field-bg-hover);
}

input:not([class])::placeholder,
textarea:not([class])::placeholder {
  color: var(--text-muted);
}

html,
* {
  scrollbar-width: thin;
  scrollbar-color: var(--scrollbar-thumb) var(--scrollbar-track);
}

*::-webkit-scrollbar {
  width: 12px;
  height: 12px;
}

*::-webkit-scrollbar-track {
  border-radius: 999px;
  background: var(--scrollbar-track);
}

*::-webkit-scrollbar-thumb {
  min-height: 44px;
  border: 3px solid transparent;
  border-radius: 999px;
  background:
    linear-gradient(180deg, var(--scrollbar-thumb-hover), var(--scrollbar-thumb))
    padding-box;
}

*::-webkit-scrollbar-thumb:hover {
  background:
    linear-gradient(180deg, color-mix(in srgb, var(--scrollbar-thumb-hover) 72%, var(--text-on-action)), var(--scrollbar-thumb-hover))
    padding-box;
}

*::-webkit-scrollbar-corner {
  background: transparent;
}

.ec-scrollbar {
  scrollbar-gutter: stable;
}

@keyframes ecRouteEnter {
  from {
    opacity: 0;
    transform: translateY(10px) scale(0.992);
    filter: blur(2px);
  }

  to {
    opacity: 1;
    transform: translateY(0) scale(1);
    filter: blur(0);
  }
}

@keyframes ecItemEnter {
  from {
    opacity: 0;
    transform: translateY(8px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes energy-sweep-on {
  0% {
    opacity: 0;
    transform: translateX(-42%) rotate(10deg);
  }

  34% {
    opacity: 0.92;
  }

  100% {
    opacity: 0;
    transform: translateX(34%) rotate(10deg);
  }
}

@keyframes energy-sweep-off {
  0% {
    opacity: 0;
    transform: translateX(32%) rotate(10deg);
  }

  35% {
    opacity: 0.64;
  }

  100% {
    opacity: 0;
    transform: translateX(-34%) rotate(10deg);
  }
}

@keyframes icon-charge {
  0% {
    transform: scale(0.96);
  }

  45% {
    transform: scale(1.12);
    box-shadow:
      0 18px 38px rgba(34, 197, 94, 0.34),
      0 0 0 7px rgba(34, 197, 94, 0.12);
  }

  100% {
    transform: scale(1);
  }
}

@keyframes icon-discharge {
  0% {
    transform: scale(1.08);
  }

  55% {
    transform: scale(0.94);
    box-shadow:
      0 10px 22px rgba(15, 23, 42, 0.32),
      0 0 0 5px rgba(148, 163, 184, 0.08);
  }

  100% {
    transform: scale(1);
  }
}

@keyframes status-pop {
  0% {
    transform: scale(0.9);
  }

  50% {
    transform: scale(1.1);
  }

  100% {
    transform: scale(1);
  }
}

@keyframes status-dim {
  0% {
    transform: scale(1.08);
  }

  55% {
    transform: scale(0.92);
  }

  100% {
    transform: scale(1);
  }
}

@keyframes power-panel-on {
  0% {
    border-color: rgba(148, 163, 184, 0.14);
  }

  50% {
    border-color: rgba(74, 222, 128, 0.62);
    box-shadow:
      0 0 0 1px rgba(74, 222, 128, 0.12) inset,
      0 18px 42px rgba(34, 197, 94, 0.14);
  }

  100% {
    border-color: rgba(34, 197, 94, 0.26);
  }
}

@keyframes power-panel-off {
  0% {
    border-color: rgba(34, 197, 94, 0.26);
  }

  55% {
    border-color: rgba(96, 165, 250, 0.42);
    box-shadow: 0 16px 34px rgba(59, 130, 246, 0.1);
  }

  100% {
    border-color: rgba(148, 163, 184, 0.14);
  }
}

@keyframes device-card-remove {
  0% {
    opacity: 1;
    transform: translateX(0) scale(1);
    filter: blur(0);
  }

  42% {
    opacity: 0.9;
    transform: translateX(18px) scale(0.99);
    filter: blur(0);
  }

  100% {
    opacity: 0;
    transform: translateX(76px) scale(0.965);
    filter: blur(2px);
  }
}

@keyframes group-card-remove {
  0% {
    opacity: 1;
    transform: translateX(0) scale(1);
    filter: blur(0);
  }

  42% {
    opacity: 0.9;
    transform: translateX(18px) scale(0.99);
    filter: blur(0);
  }

  100% {
    opacity: 0;
    transform: translateX(78px) scale(0.965);
    filter: blur(2px);
  }
}

@keyframes routine-energy-sweep-on {
  0% {
    opacity: 0;
    transform: translateX(-36%) rotate(10deg);
  }

  34% {
    opacity: 0.86;
  }

  100% {
    opacity: 0;
    transform: translateX(36%) rotate(10deg);
  }
}

@keyframes routine-energy-sweep-off {
  0% {
    opacity: 0;
    transform: translateX(34%) rotate(10deg);
  }

  38% {
    opacity: 0.74;
  }

  100% {
    opacity: 0;
    transform: translateX(-34%) rotate(10deg);
  }
}

@keyframes routine-icon-charge {
  0% {
    transform: scale(0.96);
    filter: brightness(0.9);
  }

  55% {
    transform: scale(1.12);
    filter: brightness(1.3);
  }

  100% {
    transform: scale(1);
    filter: brightness(1);
  }
}

@keyframes routine-icon-discharge {
  0% {
    transform: scale(1.04);
    filter: brightness(1.18);
  }

  100% {
    transform: scale(1);
    filter: brightness(0.96);
  }
}

@keyframes routine-card-remove {
  0% {
    opacity: 1;
    transform: translateX(0) scale(1);
    filter: blur(0);
  }

  42% {
    opacity: 0.9;
    transform: translateX(18px) scale(0.99);
    filter: blur(0);
  }

  100% {
    opacity: 0;
    transform: translateX(76px) scale(0.965);
    filter: blur(2px);
  }
}

@keyframes ec-modal-backdrop-enter {
  from {
    opacity: 0;
    backdrop-filter: blur(0);
    background: rgba(3, 7, 18, 0);
  }

  to {
    opacity: 1;
    backdrop-filter: blur(9px);
    background: rgba(3, 7, 18, 0.74);
  }
}

@keyframes ec-modal-backdrop-exit {
  from {
    opacity: 1;
    backdrop-filter: blur(9px);
    background: rgba(3, 7, 18, 0.74);
  }

  to {
    opacity: 0;
    backdrop-filter: blur(0);
    background: rgba(3, 7, 18, 0);
  }
}

@keyframes ec-modal-panel-enter {
  0% {
    opacity: 0;
    transform: translateY(26px) scale(0.965);
    filter: blur(5px);
  }

  62% {
    opacity: 1;
    transform: translateY(-3px) scale(1.006);
    filter: blur(0);
  }

  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
    filter: blur(0);
  }
}

@keyframes ec-modal-panel-exit {
  from {
    opacity: 1;
    transform: translateY(0) scale(1);
    filter: blur(0);
  }

  to {
    opacity: 0;
    transform: translateY(18px) scale(0.975);
    filter: blur(4px);
  }
}

.ec-modal-backdrop {
  animation: ec-modal-backdrop-enter 0.22s ease-out both;
}

.ec-modal-backdrop--closing {
  pointer-events: none;
  animation: ec-modal-backdrop-exit 0.2s ease-in both;
}

.ec-modal-panel {
  transform-origin: center;
  will-change: opacity, transform, filter;
  animation: ec-modal-panel-enter 0.36s cubic-bezier(0.16, 1, 0.3, 1) both;
}

.ec-modal-panel--closing {
  animation: ec-modal-panel-exit 0.22s cubic-bezier(0.45, 0, 0.55, 1) both;
}

:where(app-layout .content > router-outlet + *) {
  display: block;
  animation: ecRouteEnter 0.28s cubic-bezier(0.16, 1, 0.3, 1);
}

:where(
  .ec-page article,
  .ec-section,
  .summary-grid > article,
  .dashboard-grid > article,
  .list > *,
  .room-list > *,
  .device-table > *,
  .compact-list > *,
  .empty-state,
  .status-panel,
  app-device-card,
  app-device-group-card,
  app-routine-card,
  app-room-card,
  app-location-card,
  app-report-card,
  app-invoice-card,
  app-support-ticket-card,
  app-maintenance-ticket-card,
  app-alert-rule-card,
  app-goal-progress-card,
  app-plan-card
) {
  animation: ecItemEnter 0.24s cubic-bezier(0.16, 1, 0.3, 1) both;
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.001ms !important;
    animation-iteration-count: 1 !important;
    scroll-behavior: auto !important;
    transition-duration: 0.001ms !important;
  }
}

/* =========================================================
   ELECTROCORP PAGE STRUCTURE
   Reusable page heading + section spacing
   ========================================================= */

.ec-page {
  display: grid;
  gap: 40px;
}

.ec-page-heading {
  display: grid;
  gap: 16px;
  max-width: 980px;
  margin-bottom: 6px;
}

.ec-page-eyebrow {
  width: fit-content;
  margin: 0;
  padding: 9px 15px;
  border: 1px solid var(--border-color);
  border-radius: 999px;
  color: var(--accent-color);
  background: var(--surface-color);
  font-size: 0.88rem;
  font-weight: 950;
  text-transform: uppercase;
  letter-spacing: 0.12em;
  line-height: 1;
}

.ec-page-title {
  margin: 0;
  color: var(--text-primary);
  font-size: clamp(2.6rem, 5vw, 4.7rem);
  line-height: 1;
  letter-spacing: -0.055em;
}

.ec-page-description {
  margin: 0;
  max-width: 850px;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: 1.7;
}

.ec-section {
  display: grid;
  gap: 28px;
  padding: 32px;
  border: 1px solid var(--border-color);
  border-radius: 30px;
  background: var(--ec-section-bg);
  box-shadow: 0 18px 45px rgba(3, 7, 18, 0.18);
}

.ec-section-heading {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 18px;
}

.ec-section-heading h2 {
  margin: 0 0 10px;
  color: var(--text-primary);
  font-size: 2rem;
  line-height: 1.1;
  letter-spacing: -0.025em;
}

.ec-section-heading p {
  margin: 0;
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

:where(app-layout .content) :where(
  p:not(.ec-page-eyebrow):not(.section-eyebrow):not(.home-eyebrow):not(.invoice-eyebrow):not(.home-more-note),
  small
) {
  color: var(--ec-copy-color);
  font-weight: var(--ec-copy-weight);
  line-height: var(--ec-copy-line-height);
}

.ec-grid-gap {
  display: grid;
  gap: 24px;
}

.dropdown.nav {
  position: relative;
  width: fit-content;
  padding-bottom: 0;
}

.dropdown.nav::after {
  content: '';
  position: absolute;
  inset: 100% 0 auto;
  height: 12px;
}

.dropdown.nav .dropdown__trigger {
  width: fit-content;
  height: 40px;
  min-height: 40px;
  display: inline-flex;
  justify-content: center;
  gap: 8px;
  padding: 0 16px;
  border-radius: 999px;
  background: var(--ec-ghost-bg);
  box-shadow: none;
  font-size: 0.95rem;
  font-weight: 900;
  line-height: 1;
  white-space: nowrap;
}

.dropdown.nav .dropdown__trigger:hover,
.dropdown.nav.open .dropdown__trigger,
.dropdown.nav.has-selection .dropdown__trigger {
  color: var(--text-primary);
  border-color: var(--border-strong);
  background: var(--ec-ghost-bg-hover);
  box-shadow: 0 0 0 3px rgba(79, 195, 255, 0.045);
}

.dropdown.nav.has-selection .dropdown__trigger {
  background: var(--ec-neutral-bg);
  box-shadow:
    inset 0 0 0 1px rgba(125, 211, 252, 0.05),
    0 0 0 3px rgba(79, 195, 255, 0.055);
}

.dropdown.nav .dropdown__arrow {
  width: 7px;
  height: 7px;
  margin: 0;
  border-color: currentColor;
  opacity: 0.82;
}

.dropdown.nav .dropdown__menu {
  top: calc(100% + 12px);
  left: 50%;
  z-index: 999999;
  width: max-content;
  min-width: 250px;
  border-color: var(--border-strong);
  background: var(--menu-bg) !important;
  box-shadow:
    0 22px 54px rgba(0, 0, 0, 0.46),
    0 0 0 1px rgba(255, 255, 255, 0.025);
  translate: -50% 0;
  transform-origin: top center;
}

.dropdown.nav .dropdown__option {
  position: relative;
  gap: 2px;
  font-size: 0.9rem;
  font-weight: 900;
}

.dropdown.nav .dropdown__option small {
  margin-top: 2px;
  font-size: 0.73rem;
  line-height: 1.3;
}

.invoice-backdrop {
  --invoice-topbar-height: 70px;
  --invoice-viewport-gap: 24px;

  position: fixed;
  inset: var(--invoice-topbar-height) 0 0;
  z-index: 100000;
  display: grid;
  place-items: center;
  padding: var(--invoice-viewport-gap);
  overflow: hidden;
  background: rgba(3, 7, 18, 0.74);
  backdrop-filter: blur(9px);
}

.invoice-modal {
  width: min(1180px, calc(100vw - 32px));
  max-height: min(
    calc(100vh - var(--invoice-topbar-height) - (var(--invoice-viewport-gap) * 2)),
    820px
  );
  display: grid;
  grid-template-rows: auto minmax(0, 1fr);
  overflow: hidden;
  border: 1px solid rgba(125, 211, 252, 0.2);
  border-radius: 28px;
  background:
    radial-gradient(circle at top right, rgba(79, 195, 255, 0.12), transparent 34%),
    linear-gradient(180deg, rgba(28, 37, 63, 0.98), rgba(14, 20, 37, 0.98));
  box-shadow: 0 28px 90px rgba(0, 0, 0, 0.48);
}

.invoice-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 18px;
  padding: 28px 30px;
  border-bottom: 1px solid rgba(148, 163, 184, 0.14);
}

.invoice-scroll {
  min-height: 0;
  overflow-y: auto;
  overscroll-behavior: contain;
}

.invoice-eyebrow {
  margin: 0 0 8px;
  color: #60a5fa;
  font-size: 0.74rem;
  font-weight: 950;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

.invoice-header h2 {
  margin: 0 0 8px;
  color: #f8fafc;
  font-size: 1.8rem;
  font-weight: 950;
}

.invoice-header span {
  color: #9fb2cc;
  font-weight: 800;
}

.invoice-close {
  width: 38px;
  height: 38px;
  flex: 0 0 auto;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border: 1px solid rgba(148, 163, 184, 0.22);
  border-radius: 14px;
  padding: 0;
  background: rgba(9, 18, 37, 0.48);
  color: #dbeafe;
  cursor: pointer;
  font-family: Arial, sans-serif;
  font-size: 1.18rem;
  font-weight: 950;
  line-height: 1;
  box-shadow:
    inset 0 0 0 1px rgba(255, 255, 255, 0.03),
    0 12px 22px rgba(3, 7, 18, 0.22);
}

.invoice-close:hover {
  border-color: rgba(125, 211, 252, 0.46);
  background:
    linear-gradient(180deg, rgba(48, 75, 116, 0.96), rgba(19, 42, 75, 0.98));
  color: #ffffff;
  box-shadow:
    inset 0 0 0 1px rgba(255, 255, 255, 0.04),
    0 0 0 4px rgba(79, 195, 255, 0.08),
    0 16px 28px rgba(3, 7, 18, 0.28);
}

@media (max-width: 760px) {
  .invoice-backdrop {
    --invoice-topbar-height: 64px;
    --invoice-viewport-gap: 14px;
  }
}

.invoice-hero {
  display: grid;
  grid-template-columns: 1fr 1.25fr 1fr;
  gap: 16px;
  padding: 24px 30px 10px;
}

.invoice-hero article,
.invoice-panel {
  border: 1px solid rgba(148, 163, 184, 0.16);
  border-radius: 22px;
  background: rgba(9, 18, 37, 0.42);
  box-shadow:
    0 14px 34px rgba(3, 7, 18, 0.2),
    inset 0 1px 0 rgba(255, 255, 255, 0.02);
}

.invoice-hero article {
  display: grid;
  gap: 8px;
  padding: 20px;
}

.invoice-hero__accent {
  border-color: rgba(125, 211, 252, 0.28) !important;
  background: rgba(14, 116, 144, 0.16) !important;
}

.invoice-hero span,
.invoice-panel__title span,
.invoice-facts span,
.top-device span {
  color: #9fb2cc;
  font-size: 0.82rem;
  font-weight: 850;
}

.invoice-hero strong {
  color: #f8fafc;
  font-size: 1.65rem;
  font-weight: 950;
  line-height: 1.05;
}

.invoice-hero small {
  color: #7dd3fc;
  font-weight: 800;
  line-height: 1.35;
}

.invoice-grid {
  display: grid;
  grid-template-columns: minmax(0, 1.12fr) minmax(340px, 0.88fr);
  gap: 16px;
  align-items: stretch;
  padding: 16px 30px 0;
}

.invoice-panel {
  min-height: 250px;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  padding: 20px;
}

.invoice-panel--wide {
  margin: 16px 30px 0;
}

.invoice-panel--wide:last-child {
  margin-bottom: 30px;
}

.invoice-panel__title {
  display: flex;
  justify-content: space-between;
  gap: 14px;
  align-items: center;
  margin-bottom: 16px;
}

.invoice-panel__title strong {
  color: #f8fafc;
  font-size: 0.95rem;
  font-weight: 950;
}

.invoice-facts {
  flex: 1;
  display: grid;
  grid-template-columns: repeat(4, minmax(118px, 1fr));
  grid-template-rows: repeat(2, minmax(0, 1fr));
  gap: 12px;
  align-content: stretch;
  justify-content: center;
}

.invoice-facts div {
  min-width: 0;
  min-height: 0;
  display: grid;
  gap: 6px;
  align-content: center;
  padding: 14px;
  border-radius: 16px;
  background: rgba(3, 10, 24, 0.38);
}

.invoice-facts strong,
.top-device strong {
  color: #f8fafc;
  font-weight: 950;
}

.invoice-facts strong,
.top-device strong {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.top-devices {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 10px;
}

.top-device {
  min-width: 0;
  min-height: 58px;
  display: flex;
  justify-content: space-between;
  gap: 16px;
  align-items: center;
  padding: 14px;
  border-radius: 16px;
  background: rgba(3, 10, 24, 0.38);
}

.top-device div {
  min-width: 0;
  display: grid;
  gap: 4px;
}

.top-device b {
  flex: 0 0 auto;
  color: #7dd3fc;
  font-weight: 950;
}

.invoice-table-wrap {
  overflow-x: auto;
}

.invoice-table {
  width: 100%;
  min-width: 980px;
  border-collapse: collapse;
}

.invoice-table th,
.invoice-table td {
  padding: 14px 12px;
  border-bottom: 1px solid rgba(148, 163, 184, 0.12);
  text-align: left;
}

.invoice-table th {
  color: #94a3b8;
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.invoice-table td {
  color: #e5eef9;
  font-size: 0.86rem;
  font-weight: 800;
}

.share-meter {
  width: 72px;
  height: 8px;
  display: inline-flex;
  margin-right: 8px;
  overflow: hidden;
  border-radius: 999px;
  background: rgba(148, 163, 184, 0.16);
  vertical-align: middle;
}

.share-meter i {
  display: block;
  border-radius: inherit;
  background: linear-gradient(90deg, #38bdf8, #6366f1);
}

.invoice-empty {
  margin: 0;
  color: var(--text-secondary);
}

@media (max-width: 1120px) {
  .invoice-grid {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 940px) {
  .invoice-hero {
    grid-template-columns: 1fr;
  }

  .invoice-facts {
    grid-template-columns: repeat(2, minmax(132px, 1fr));
  }

  .invoice-panel--wide {
    margin-inline: 20px;
  }

  .invoice-hero,
  .invoice-grid {
    padding-inline: 20px;
  }
}

@media (max-width: 560px) {
  .invoice-facts {
    grid-template-columns: minmax(180px, 260px);
  }

  .invoice-facts div {
    text-align: center;
  }
}

@media (max-width: 860px) {
  .ec-section {
    padding: 26px;
    border-radius: 26px;
  }

  .ec-section-heading {
    flex-direction: column;
  }
}

@media (max-width: 640px) {
  .ec-page {
    gap: 32px;
  }

  .ec-page-title {
    font-size: clamp(2.3rem, 12vw, 3.2rem);
  }

  .ec-section {
    padding: 22px;
    border-radius: 24px;
  }
}


============================================================
FILE: tsconfig.app.json
============================================================

/* To learn more about Typescript configuration file: https://www.typescriptlang.org/docs/handbook/tsconfig-json.html. */
/* To learn more about Angular compiler options: https://angular.dev/reference/configs/angular-compiler-options. */
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "rootDir": "./src",
    "types": []
  },
  "files": [
    "src/main.ts"
  ],
  "include": [
    "src/**/*.d.ts"
  ],
  "exclude": [
    "src/**/*.spec.ts"
  ]
}


============================================================
FILE: tsconfig.json
============================================================

/* To learn more about Typescript configuration file: https://www.typescriptlang.org/docs/handbook/tsconfig-json.html. */
/* To learn more about Angular compiler options: https://angular.dev/reference/configs/angular-compiler-options. */
{
  "compileOnSave": false,
  "compilerOptions": {
    "strict": true,
    "noImplicitOverride": true,
    "noPropertyAccessFromIndexSignature": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "isolatedModules": true,
    "experimentalDecorators": true,
    "importHelpers": true,
    "target": "ES2022",
    "module": "preserve"
  },
  "angularCompilerOptions": {
    "enableI18nLegacyMessageIdFormat": false,
    "strictInjectionParameters": true,
    "strictInputAccessModifiers": true,
    "strictTemplates": true
  },
  "files": [],
  "references": [
    {
      "path": "./tsconfig.app.json"
    },
    {
      "path": "./tsconfig.spec.json"
    }
  ]
}

============================================================
FILE: tsconfig.spec.json
============================================================

/* To learn more about Typescript configuration file: https://www.typescriptlang.org/docs/handbook/tsconfig-json.html. */
/* To learn more about Angular compiler options: https://angular.dev/reference/configs/angular-compiler-options. */
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/spec",
    "types": [
      "vitest/globals"
    ]
  },
  "include": [
    "src/**/*.d.ts",
    "src/**/*.spec.ts"
  ]
}
